---
title: Enhanced Self-Supervised Multi-Image Super-Resolution for Camera Array Images
title_zh: 针对相机阵列图像的增强型自监督多图像超分辨率
authors: "Yating Chen, Feng Huang, Xianyu Wu, Jing Wu, Ying Shen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06816v2"
tags: ["query:q7"]
score: 8.0
evidence: 自监督多图像超分辨率与修复
tldr: 针对相机阵列多图像超分辨率（MISR）中监督学习易过拟合及自监督学习细节恢复不足的问题，本文提出一种增强型自监督框架。该框架结合了“多对一”与“多对多”自监督方法的优势，并引入双Transformer网络结构，通过融合深度学习与物理变分方法，有效提升了从混叠伪影中恢复高频细节的能力，在合成及真实数据集上均表现优异。
selection_source: fresh_fetch
motivation: 传统MISR方法难以处理相机阵列特有的空间分布特性，且现有自监督方法在恢复精细纹理方面存在局限。
method: 提出一种由“多对一”引导的“多对多”自监督学习框架，并设计了适用于该框架的双Transformer网络。
result: 实验证明该方法在合成和真实世界数据集上均能生成具有高保真度和丰富纹理细节的图像。
conclusion: 该研究为深度神经网络与经典物理变分方法的结合提供了新范式，显著提升了相机阵列图像的超分辨率性能。
---

## 摘要
传统的多图像超分辨率（MISR）方法，如连拍（burst）和视频超分辨率，依赖于单相机的序列帧。因此，它们受到复杂的图像退化和严重遮挡的影响，增加了准确图像恢复的难度。相比之下，多孔径相机阵列成像捕获空间分布的视图，其采样偏移形成稳定的盘状分布，增强了观测数据的非冗余性。现有的 MISR 算法未能充分利用这些独特属性。有监督 MISR 方法往往会过拟合训练数据中的退化模式，而目前的自监督学习（SSL）技术难以恢复细粒度的细节。为解决这些问题，本文深入研究了多图像到单图像（Multi-to-Single）和多图像到多图像（Multi-to-Multi）自监督学习方法的优势、局限性及适用边界。我们提出了“多到单引导的多到多”自监督学习框架（Multi-to-Single-Guided Multi-to-Multi SSL），结合了多到单和多到多方法的优点，以生成具有丰富纹理细节、视觉效果良好且高保真的图像。该框架为深度神经网络与经典的基于物理的变分方法的集成提供了一种新范式。为了增强 MISR 网络从混叠伪影中恢复高频细节的能力，本文提出了一种适用于自监督学习的新型相机阵列超分辨率网络，称为双 Transformer（dual Transformer）。在合成数据集和真实世界数据集上的实验证明了所提方法的优越性。

## Abstract
Conventional multi-image super-resolution (MISR) methods, such as burst and video SR, rely on sequential frames from a single camera. Consequently, they suffer from complex image degradation and severe occlusion, increasing the difficulty of accurate image restoration. In contrast, multi-aperture camera-array imaging captures spatially distributed views with sampling offsets forming a stable disk-like distribution, which enhances the non-redundancy of observed data. Existing MISR algorithms fail to fully exploit these unique properties. Supervised MISR methods tend to overfit the degradation patterns in training data, and current self-supervised learning (SSL) techniques struggle to recover fine-grained details. To address these issues, this paper thoroughly investigates the strengths, limitations and applicability boundaries of multi-image-to-single-image (Multi-to-Single) and multi-image-to-multi-image (Multi-to-Multi) SSL methods. We propose the Multi-to-Single-Guided Multi-to-Multi SSL framework that combines the advantages of Multi-to-Single and Multi-to-Multi to generate visually appealing and high-fidelity images rich in texture details. The Multi-to-Single-Guided Multi-to-Multi SSL framework provides a new paradigm for integrating deep neural network with classical physics-based variational methods. To enhance the ability of MISR network to recover high-frequency details from aliased artifacts, this paper proposes a novel camera-array SR network called dual Transformer suitable for SSL. Experiments on synthetic and real-world datasets demonstrate the superiority of the proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhanced Self-Supervised Multi-Image Super-Resolution for Camera Array Images》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
本文聚焦于**相机阵列（Camera Array）图像的多图像超分辨率（MISR）**。
*   **背景**：传统的视频或连拍（Burst）SR依赖单相机序列，常受复杂退化和严重遮挡困扰。相机阵列通过空间分布的多个孔径捕获视图，具有稳定的采样偏移和低冗余性，是提升分辨率的理想硬件。
*   **核心问题**：
    1.  **监督学习局限性**：依赖成对的LR-HR数据，在真实场景中难以获取，且模型易过拟合特定的退化模式。
    2.  **现有自监督（SSL）局限性**：“多对一（Multi-to-Single）”方法倾向于平滑纹理；“多对多（Multi-to-Multi）”方法虽能增强细节，但易产生拉链伪影（zipper artifacts）。
    3.  **技术挑战**：如何充分利用相机阵列的物理特性，在无需高分辨率真值（Ground Truth）的情况下，从混叠伪影中恢复高频细节。

### 2. 方法论
论文提出了一种结合物理先验与深度学习的新范式，核心组成部分包括：

*   **CASR-DSAT 网络架构（双 Transformer）**：
    *   **特征提取（CSATB）**：采用通道自注意力 Transformer，捕捉全局信息并保留空间维度的互补混叠信息。
    *   **运动补偿（SPMC）**：利用 FNet 估计孔径偏移，通过子像素运动补偿层进行显式对齐，保留子像素精度。
    *   **特征融合（HRPAF）**：设计了高分辨率伪相机阵列自适应融合模块，根据内容和噪声水平进行边缘感知融合。
    *   **特征重建（SSATB）**：采用空间自注意力 Transformer（Swin Block），利用非局部感受野消除残余噪声和平滑边缘。
*   **学习策略：Multi-to-Single-Guided Multi-to-Multi SSL**：
    *   **双分支框架**：分支一通过“多对一”损失学习平滑边缘先验；分支二通过“多对多”损失恢复精细纹理。
    *   **正则化引导**：分支一的输出作为分支二的正则化约束，既保留了细节又消除了拉链伪影。
    *   **频率分离损失**：在平坦区域进行去噪，在纹理区域利用高频分量进行细节增强。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：使用 Zurich raw-to-RGB、DIV2K（自然场景）、PROBA-V（卫星图像）和 Medical Dataset（医学磁共振图像）。
    *   **真实数据**：作者自行构建了两套 9 孔径相机阵列系统（分别使用 Basler 和 Edmund 镜头），采集了 269 组真实序列。
*   **Benchmark 与对比方法**：
    *   **物理方法**：ML, MAP-LR, MAP-LRGSC。
    *   **监督学习方法**：DBSR, MFIR, BIPNet, Burstormer（在合成数据上训练）。
    *   **自监督方法**：DSA, HDR-DSP。
*   **评价指标**：PSNR, SSIM, LPIPS（合成数据）；NIQE, MUSIQ, MTF/西门子星图（真实数据）。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 80GB PCIe GPU** 进行训练。
*   **软件/环境**：物理方法在 Intel Xeon W-2235 CPU 上运行（MATLAB）。
*   **训练时长**：
    *   FNet 预训练 50 epochs。
    *   CASR-DSAT 在合成数据上训练 100 epochs。
    *   在真实数据上微调 25 epochs。
*   **模型规模**：参数量为 14.45M，FLOPs 为 558.86G（输入 112x168x9 时）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括 3 个不同领域的合成数据集和 2 套不同硬件系统的真实数据集。
*   **消融实验**：设计了 A1-A7 共 7 组消融实验，验证了 CSATB、HRPAF 和 SSATB 每个模块的有效性，结果显示完整模型比 Baseline 提升了 13.68 dB。
*   **公平性**：对比了不同偏移范围（大偏移 vs 小偏移）对 SSL 的影响，并分析了模型大小与运行时间的权衡。实验设计覆盖了从算法性能到泛化能力的多个维度，逻辑严密且客观。

### 6. 主要结论与发现
*   **性能领先**：在所有合成数据集上，所提方法在 PSNR 指标上比次优方法至少高出 **1.0 dB**。
*   **SSL 范式突破**：证明了“多对一”引导的“多对多”策略能有效解决自监督学习中的细节-伪影权衡问题。
*   **架构优势**：双 Transformer 结构在处理混叠伪影和非局部特征重建方面显著优于传统的 CNN 架构。
*   **真实场景泛化**：在未经真值训练的情况下，该方法在真实相机阵列图像上恢复的纹理比监督学习方法更真实，且 SR 放大倍率更高。

### 7. 优点
*   **创新范式**：提出了一种将深度学习与经典物理变分方法结合的新范式，通过迭代更新网络参数而非图像像素，提升了推理效率。
*   **双 Transformer 设计**：针对特征提取和重建分别优化通道和空间注意力，针对性强。
*   **实用性**：不依赖 HR 真值，直接适用于新型光电成像系统的在线部署。
*   **数据集贡献**：提供了真实相机阵列数据集，填补了该领域的空白。

### 8. 不足与局限
*   **运动补偿限制**：目前使用的 FNet 是固定内核的全卷积网络，在处理真实世界中极其复杂的遮挡或非刚性运动时可能存在局限。
*   **计算开销**：虽然推理时间（0.49s）优于部分物理方法，但相比于轻量级 CNN 仍有一定计算负担。
*   **硬件依赖**：该方法高度依赖相机阵列的亚像素偏移稳定性，若硬件装配误差过大，可能会影响恢复效果。

（完）
