---
title: "PHAC: Promptable Human Amodal Completion"
title_zh: PHAC：可提示的人体非遮挡补全
authors: "Seung Young Noh, Ju Yong Chang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14741v1"
tags: ["query:q1"]
score: 9.0
evidence: 人体非全貌补全与不可见区域幻觉生成
tldr: 本研究针对人体非全貌补全（HAC）中用户控制力不足的问题，提出了PHAC框架。该方法允许用户通过点（如关节位置）或边界框等简单提示来引导遮挡部位的生成。PHAC利用ControlNet编码提示信号并注入预训练扩散模型，仅微调交叉注意力层以保持生成先验，并结合修复细化模块确保可见区域的完整性与无缝融合。实验证明，PHAC在保持外观一致性和提示对齐方面显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的人体非全貌补全模型缺乏对生成内容的控制，而姿态引导合成方法又难以在保持特定实例外观的同时实现良好的提示对齐。
method: 通过ControlNet模块编码点和边界框提示，并将其注入扩散模型，同时利用微调的交叉注意力机制和修复细化模块来生成并优化补全结果。
result: 在HAC和PGPIS基准测试上的实验表明，该方法在物理合理性、图像质量以及与用户提示的对齐度上均取得了显著提升。
conclusion: PHAC通过引入多模态提示控制，成功解决了人体补全任务中的可控性难题，为人体中心图像生成提供了更灵活且高质量的方案。
---

## 摘要
条件图像生成方法在以人为中心的应用中得到越来越广泛的使用，然而现有的人体非遮挡补全（HAC）模型对补全内容的控制能力有限。给定一张被遮挡的人物图像，这些模型在保留可见区域的同时对不可见区域进行幻觉生成，但无法可靠地整合用户指定的约束，如期望的姿态或空间范围。因此，用户往往不得不反复对模型进行采样，直到获得满意的输出。姿态引导的人物图像合成（PGPIS）方法允许显式的姿态调节，但往往无法保留特定实例的可见外观，且倾向于偏向训练分布，即使是建立在强大的扩散模型先验之上也是如此。为了解决这些局限性，我们引入了可提示人体非遮挡补全（PHAC），这是一项新任务，旨在补全被遮挡的人物图像，同时满足可见外观约束和多个用户提示。用户提供简单的基于点的提示，例如目标姿态的额外关节或所需区域的边界框；这些提示使用针对每种提示类型专门化的 ControlNet 模块进行编码。这些模块将提示信号注入预训练的扩散模型中，我们仅微调交叉注意力块，以在不降低底层生成先验的情况下获得强大的提示对齐。为了进一步保留可见内容，我们提出了一个基于修复的细化模块，该模块从带有轻微噪声的粗略补全开始，忠实地保留可见区域，并确保遮挡边界处的无缝融合。在 HAC 和 PGPIS 基准测试上的大量实验表明，与现有的非遮挡补全和姿态引导合成方法相比，我们的方法产生了更符合物理规律且质量更高的补全结果，同时显著提高了提示对齐度。

## Abstract
Conditional image generation methods are increasingly used in human-centric applications, yet existing human amodal completion (HAC) models offer users limited control over the completed content. Given an occluded person image, they hallucinate invisible regions while preserving visible ones, but cannot reliably incorporate user-specified constraints such as a desired pose or spatial extent. As a result, users often resort to repeatedly sampling the model until they obtain a satisfactory output. Pose-guided person image synthesis (PGPIS) methods allow explicit pose conditioning, but frequently fail to preserve the instance-specific visible appearance and tend to be biased toward the training distribution, even when built on strong diffusion model priors. To address these limitations, we introduce promptable human amodal completion (PHAC), a new task that completes occluded human images while satisfying both visible appearance constraints and multiple user prompts. Users provide simple point-based prompts, such as additional joints for the target pose or bounding boxes for desired regions; these prompts are encoded using ControlNet modules specialized for each prompt type. These modules inject the prompt signals into a pre-trained diffusion model, and we fine-tune only the cross-attention blocks to obtain strong prompt alignment without degrading the underlying generative prior. To further preserve visible content, we propose an inpainting-based refinement module that starts from a slightly noised coarse completion, faithfully preserves the visible regions, and ensures seamless blending at occlusion boundaries. Extensive experiments on the HAC and PGPIS benchmarks show that our approach yields more physically plausible and higher-quality completions, while significantly improving prompt alignment compared with existing amodal completion and pose-guided synthesis methods.

---

## 论文详细总结（自动生成）

### PHAC: 可提示的人体非遮挡补全论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在补全被遮挡人体图像时，既能**精确保留可见区域的外观细节**，又能根据**用户意图（如特定姿态或范围）**进行可控生成。
*   **研究背景**：
    *   **人体非遮挡补全（HAC）**：现有模型缺乏控制手段，用户无法指定补全后的姿态，往往需要多次采样才能获得满意结果。
    *   **姿态引导人体合成（PGPIS）**：虽然可以控制姿态，但容易丢失输入图像的特定外观（如鞋子、面部细节），且生成结果常偏向训练集分布（如 DeepFashion 数据集偏见）。
*   **动机**：提出一种“可提示”（Promptable）的新任务框架，通过简单的点或框提示，实现高保真、高对齐的人体补全。

#### 2. 方法论
PHAC 框架分为两个主要阶段：**粗略图像生成**和**基于修复的细化**。
*   **用户提示编码**：
    *   支持五种提示类型：姿态点（ppo）、兴趣区域框（pib）、全景框（peb）及其组合。
    *   将点坐标转换为“提示图像”（Prompt Image），通过专门的 **ControlNet** 模块注入空间约束。
*   **粗略图像生成网络（CIG）**：
    *   基于预训练扩散模型（Stable Diffusion），仅微调**交叉注意力块（Cross-attention blocks）**。
    *   这种策略在保持扩散模型强大的生成先验（保证人体物理合理性）的同时，实现了与用户提示的强对齐。
*   **不可见掩码预测**：
    *   使用轻量级 U-Net 根据输入图、粗略生成图和可见掩码，预测需要补全的“不可见区域掩码”。
*   **基于修复的细化模块（Refinement）**：
    *   **核心思想**：解决潜空间去噪导致的可见区域细节模糊问题。
    *   **流程**：将原始可见像素与粗略生成像素拼接，对不可见区域加入少量噪声（强度 $s=0.5$），利用预训练的 SDXL Inpainting 模型进行少量步数的去噪。
    *   **效果**：确保可见区域 100% 还原，同时消除遮挡边界的伪影，实现无缝融合。

#### 3. 实验设计
*   **数据集**：
    *   **OccThuman2.0（合成）**：基于 THuman2.0 的 3D 人体模型渲染，包含 5,260 张配对图像，模拟各种遮挡场景。
    *   **AHP（真实）**：包含 56 张具有复杂遮挡的真实世界图像，用于验证泛化性。
*   **Benchmark 与对比方法**：
    *   **PGPIS 类**：PIDM, MCLD。
    *   **HAC 类**：pix2gestalt, SDHDO。
*   **评价指标**：
    *   图像质量：LPIPS, SSIM, KID, MSE, PSNR。
    *   对齐精度：**Joint Error**（生成图像与真值之间的像素级关节误差）。

#### 4. 资源与算力
*   **训练算力**：
    *   **扩散模型与 ControlNet**：使用 **4 台 NVIDIA RTX A6000 GPU**，训练约 16 小时（1,750 个 epoch）。
    *   **掩码预测 U-Net**：使用 **1 台 NVIDIA RTX 3090 GPU**，训练约 30 分钟。
*   **推理效率**：
    *   掩码预测：约 50 FPS（0.02s/图）。
    *   细化模块（SDXL）：约 0.25 FPS（4s/图）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量对比（表 1）和定性展示（图 4、图 S10-S12）。
*   **消融实验**：
    *   不同用户提示类型（ppo, pib 等）的效果分析。
    *   细化模块中噪声强度 $s$ 的影响。
    *   细化模块作为“即插即用”组件对其他模型（如 MCLD, SDHDO）的提升效果。
*   **充分性评价**：实验设计非常充分，涵盖了从合成到真实、从整体到局部区域的评估，且通过随机种子采样证明了结果的稳定性，而非“樱桃拾取”（Cherry-picking）。

#### 6. 主要结论与发现
*   **性能领先**：PHAC 在所有图像质量指标和姿态对齐指标上均显著优于现有 HAC 和 PGPIS 方法。
*   **提示有效性**：姿态提示（ppo）对结构约束最强，而兴趣区域框（pib）在单位用户投入下的收益最高（Prompt Efficiency）。
*   **细化模块的价值**：该模块不仅提升了 PHAC 的表现，还能显著改善其他扩散模型的输出质量，减少边界伪影。
*   **物理合理性**：相比于 pix2gestalt 等方法，PHAC 生成的人体结构更符合解剖学逻辑，减少了多肢体或断肢现象。

#### 7. 优点与亮点
*   **交互性强**：首次将多模态点/框提示引入人体非遮挡补全任务，极大提升了实用性。
*   **高保真度**：通过细化模块巧妙解决了扩散模型在去噪过程中丢失原始图像细节的顽疾。
*   **即插即用**：细化模块具有通用性，可作为独立的后处理工具提升其他补全模型的性能。
*   **训练策略高效**：仅微调交叉注意力层，平衡了新知识学习与旧先验保留。

#### 8. 不足与局限
*   **推理延迟**：细化阶段使用 SDXL 模型，单张图像 4 秒的耗时对于实时交互应用来说仍然较慢。
*   **掩码依赖**：补全质量高度依赖于第一阶段生成的粗略图以及掩码预测的准确性，若初始生成结构极度扭曲，细化模块也难以挽回。
*   **应用限制**：目前主要针对单人场景，在多人极端重叠或超大比例遮挡（如 >80%）下的表现仍有待进一步探索。

（完）
