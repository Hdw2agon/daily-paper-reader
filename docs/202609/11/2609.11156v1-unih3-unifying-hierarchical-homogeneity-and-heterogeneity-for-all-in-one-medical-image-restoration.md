---
title: "UniH$^3$: Unifying Hierarchical Homogeneity and Heterogeneity for All-in-One Medical Image Restoration"
title_zh: UniH$^3$：统一分层同质性与异质性以实现一体化医学图像恢复
authors: "Zhiwen Yang, Jiayin Li, Chengyu Liu, Hui Zhang, Bingzheng Wei, Yan Xu"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11156v1"
tags: ["query:q3"]
score: 8.0
evidence: 多合一医学图像修复框架
tldr: UniH3是一个用于医学图像修复的通用框架，旨在解决多模态和多退化类型的全能修复任务。该研究指出，现有方法过度关注任务间的异质性，而忽视了医学图像中共享的解剖结构同质性。UniH3通过引入分层同质性记忆模块（H2M）提取先验知识，并结合分层异质性平衡器（H2B）优化多任务学习，在多个大规模基准测试中实现了SOTA性能，推动了通用医学图像修复的发展。
selection_source: fresh_fetch
motivation: 现有医学图像修复方法多关注任务间的异质性，却忽视了医学图像中广泛存在的解剖结构同质性。
method: 提出了UniH3框架，通过分层同质性记忆模块（H2M）提取并注入先验知识，同时利用分层异质性平衡器（H2B）缓解多任务学习中的冲突。
result: 在MedIR-2D-500K和MedIR-3D-3K大规模基准测试中，UniH3在全能和单任务修复任务上均达到了最优性能。
conclusion: UniH3通过统一处理同质性和异质性，为通用医学图像修复提供了一个高效且强大的基准模型。
---

## 摘要
一体化医学图像恢复（MedIR）旨在利用单一通用模型解决跨模态和多种退化类型的多样化任务。现有方法通常优先考虑建模任务间的异质性（例如不同的数据分布和退化类型）。然而，它们在很大程度上忽略了医学图像中固有的同质性，例如模态内部和模态之间广泛共享的解剖结构，而这些结构可以被利用来简化模型训练并提高泛化能力。为此，我们提出了 UniH$^3$，这是一个统一分层同质性与异质性以实现一体化医学图像恢复的新型框架。具体而言，为了全面利用同质性，我们引入了分层同质性记忆（H2M）模块，该模块在训练过程中从高质量图像中逐步提取任务内和任务间的同质性先验，并自适应地检索与输入最相关的先验以引导恢复。随后，这些检索到的先验通过高效的同质性引导注意力（HGA）机制注入到恢复流程中。此外，为了全面解决异质性，我们设计了分层异质性平衡器（H2B），它在优化过程中缓解了任务间和任务内的冲突，从而促进了平衡且有效的多任务学习。在 MedIR-2D-500K 和 MedIR-3D-3K 两个大规模基准测试上的大量实验表明，UniH$^3$ 在一体化和单任务医学图像恢复方面均达到了最先进的性能。我们希望这项工作能建立一个强大的基准，并推动通用医学图像恢复模型的发展。代码可在 https://github.com/Yaziwel/UniH3 获取。

## Abstract
All-in-One medical image restoration (MedIR) aims to address diverse tasks across modalities and degradation types using a single universal model. Existing methods typically prioritize modeling inter-task heterogeneity (e.g., distinct data distributions and degradation types). However, they largely neglect the inherent homogeneity present in medical images, such as widely shared anatomical structures within and across modalities, which can be leveraged to ease model training and improve generalization. To this end, we propose UniH3, a novel framework that Unifies Hierarchical Homogeneity and Heterogeneity for all-in-one medical image restoration. Specifically, to comprehensively exploit homogeneity, we introduce a Hierarchical Homogeneity Memory (H2M) module that progressively distills intra- and inter-task homogeneity priors from high-quality images during training, and adaptively retrieves the most relevant priors tailored to the input for guided restoration. These retrieved priors are then injected into the restoration pipeline via an efficient Homogeneity-Guided Attention (HGA) mechanism. Furthermore, to comprehensively address heterogeneity, we design a Hierarchical Heterogeneity Balancer (H2B) that mitigates both inter- and intra-task conflicts during optimization, facilitating balanced and effective multi-task learning. Extensive experiments on two large-scale benchmarks, MedIR-2D-500K and MedIR-3D-3K, demonstrate that UniH3 achieves state-of-the-art performance on both all-in-one and single-task medical image restoration. We hope this work establishes a strong benchmark and advances the development of general-purpose medical image restoration models. Code is available at https://github.com/Yaziwel/UniH3.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity，很高兴为你分析这篇关于 **UniH$^3$** 的医学图像恢复论文。以下是该论文的结构化总结：

---

### 1. 核心问题与研究动机
*   **背景**：一体化医学图像恢复（MedIR）旨在用单一模型处理多种模态（如 CT、MRI）和多种退化类型（如去噪、超分辨率、去伪影）。
*   **痛点**：现有研究过度关注任务间的“异质性”（数据分布差异、退化差异），导致模型在处理复杂任务时难以收敛或泛化能力不足。
*   **核心洞察**：医学图像中存在广泛的“同质性”（如跨模态共享的解剖结构），这些先验知识被现有方法忽视了。UniH$^3$ 旨在通过统一处理同质性与异质性，实现更高效的通用模型。

### 2. 方法论：UniH$^3$ 框架
*   **核心思想**：通过“记忆”提取同质性先验，通过“平衡”解决异质性冲突。
*   **关键技术**：
    *   **分层同质性记忆模块 (H2M)**：在训练过程中从高质量图像中提取并存储解剖结构先验。推理时，根据输入自适应检索最相关的先验知识。
    *   **同质性引导注意力 (HGA)**：将检索到的先验知识注入到恢复网络中，引导模型关注关键解剖特征。
    *   **分层异质性平衡器 (H2B)**：在多任务学习过程中，动态调整不同任务的权重，缓解任务间（不同模态/退化）和任务内（不同样本）的梯度冲突。

### 3. 实验设计
*   **数据集**：使用了两个大规模基准测试：**MedIR-2D-500K**（2D 图像）和 **MedIR-3D-3K**（3D 图像）。
*   **Benchmark**：涵盖了多种医学成像模态及多种退化场景。
*   **对比方法**：与当前主流的通用图像恢复模型及针对特定任务的 SOTA 方法进行了对比。

### 4. 资源与算力
*   **说明**：论文摘要及元数据中未明确提及具体的 GPU 型号、数量或具体的训练时长。通常此类大规模基准测试需要高性能计算集群（如多卡 A100/H100），建议查阅论文附录或 GitHub 仓库获取详细配置。

### 5. 实验数量与充分性
*   **实验覆盖**：包含全能（All-in-One）恢复实验和单任务恢复实验，并进行了消融实验以验证 H2M 和 H2B 模块的有效性。
*   **评价**：实验设计较为充分，覆盖了 2D 和 3D 两种维度，且在多个大规模数据集上验证，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能表现**：UniH$^3$ 在 MedIR-2D-500K 和 MedIR-3D-3K 上均达到了 SOTA 水平。
*   **有效性**：证明了显式建模医学图像的“同质性”对于提升恢复质量至关重要，同时“异质性平衡”策略能有效提升多任务学习的稳定性。

### 7. 优点
*   **创新性**：首次明确提出利用医学图像的“同质性”作为先验，弥补了现有方法仅关注异质性的不足。
*   **通用性**：框架设计兼顾了 2D 和 3D 场景，具有较强的通用医学图像处理潜力。
*   **可解释性**：通过记忆模块（H2M）提取先验，使得模型恢复过程具有一定的可解释性。

### 8. 不足与局限
*   **计算开销**：记忆模块（H2M）的检索和注入机制可能会增加推理时的计算延迟，对于实时性要求极高的临床场景可能存在挑战。
*   **数据依赖**：该方法高度依赖高质量图像作为先验来源，若在缺乏高质量参考数据的极端模态下，其性能提升可能受限。
*   **泛化边界**：虽然在现有基准上表现优异，但对于未见过的罕见病灶或极端退化类型，其泛化能力仍需进一步验证。

---

（完）
