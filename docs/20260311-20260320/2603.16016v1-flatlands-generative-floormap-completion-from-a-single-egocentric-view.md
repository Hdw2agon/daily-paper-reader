---
title: "FlatLands: Generative Floormap Completion From a Single Egocentric View"
title_zh: FlatLands：基于单视角第一人称视图的生成式平面图补全
authors: "Subhransu S. Bhattacharjee, Dylan Campbell, Rahul Shome"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.16016v1"
tags: ["query:q1"]
score: 9.0
evidence: 生成式平面图补全与随机生成模型
tldr: 本研究针对室内导航中单视角图像视野受限的问题，提出了FlatLands数据集和基准，旨在从单张第一人称视角图像生成完整的鸟瞰图（BEV）平面图。该数据集包含来自1.7万个真实场景的27万余条观测数据。研究对比了确定性模型与生成式模型，并构建了端到端的RGB到平面图生成管线，为具备不确定性感知能力的室内制图和具身导航提供了严谨的测试平台。
selection_source: fresh_fetch
motivation: 单张第一人称视角图像仅能捕捉局部地面信息，而室内导航需要完整的度量可通行性地图。
method: 引入包含27万余条观测的大规模数据集FlatLands，并构建了一个端到端的单目RGB到鸟瞰图平面图生成的生成式模型管线。
result: 通过对训练模型、确定性模型、集成模型及随机生成模型的对比评估，验证了生成式方法在处理不确定性地图补全方面的有效性。
conclusion: FlatLands为室内环境下的不确定性感知制图和生成式补全任务提供了重要的数据支持和评估基准。
---

## 摘要
单张第一人称图像通常只能捕捉到地板的一小部分，然而，周围环境的完整度量可通行性地图将更好地服务于室内导航等应用。我们介绍了 FlatLands，这是一个用于单视角鸟瞰图（BEV）平面图补全的数据集和基准。该数据集包含来自 6 个现有数据集的 17,656 个真实度量室内场景的 270,575 次观测，并配有对齐的观测图、可见性图、有效性图和地面真值 BEV 地图；该基准包括分布内和分布外评估协议。我们对比了免训练方法、确定性模型、集成模型和随机生成模型。最后，我们将该任务实例化为一个端到端的单目 RGB 到平面图的流水线。FlatLands 为具身导航中的不确定性感知室内建图和生成式补全提供了一个严谨的测试平台。

## Abstract
A single egocentric image typically captures only a small portion of the floor, yet a complete metric traversability map of the surroundings would better serve applications such as indoor navigation. We introduce FlatLands, a dataset and benchmark for single-view bird's-eye view (BEV) floor completion. The dataset contains 270,575 observations from 17,656 real metric indoor scenes drawn from six existing datasets, with aligned observation, visibility, validity, and ground-truth BEV maps, and the benchmark includes both in- and out-of-distribution evaluation protocols. We compare training-free approaches, deterministic models, ensembles, and stochastic generative models. Finally, we instantiate the task as an end-to-end monocular RGB-to-floormaps pipeline. FlatLands provides a rigorous testbed for uncertainty-aware indoor mapping and generative completion for embodied navigation.

---

## 论文详细总结（自动生成）

以下是对论文《FlatLands: Generative Floormap Completion From a Single Egocentric View》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张第一人称视角（Egocentric）图像推断出完整的、度量准确的室内鸟瞰图（BEV）可通行性平面图。
*   **研究动机**：
    *   **局部观测限制**：机器人传感器视野有限且存在遮挡，单帧图像仅能捕捉局部地面，而导航任务需要全局或更大范围的地图。
    *   **多解性挑战**：从局部推断全局是一个典型的贝叶斯逆问题，存在多种合理的补全可能性（如墙后可能是走廊也可能是房间）。
    *   **确定性模型的不足**：传统模型给出单一预测，无法表达空间布局的不确定性，这对安全导航至关重要。

### 2. 方法论
*   **核心思想**：将任务解耦为“感知”和“补全”两个阶段。首先从RGB图像提取局部观测，然后利用生成式模型对未观测区域进行多假设补全。
*   **关键技术流程**：
    1.  **感知前端（Perception Stage）**：使用 DepthPro 估计深度，SegFormer 进行地面分割，结合相机内参通过正交投影生成局部观测平面图 $F_{obs}$ 和未观测掩码 $U$。
    2.  **生成式补全（Generative Stage）**：
        *   **条件生成**：以 $(F_{obs}, U)$ 为条件，利用随机生成模型（如扩散模型或流匹配模型）预测未观测区域。
        *   **证据钳位（Evidence Clamping）**：在生成过程中强制保留已观测到的证据，确保输出与原始观测完全一致。
    3.  **核心模型（FM+XAttn）**：提出了一种带有交叉注意力机制（Cross-Attention）的条件流匹配模型，通过编码器提取全局布局线索，并在解码阶段引导生成。

### 3. 实验设计
*   **数据集（FlatLands）**：
    *   **来源**：整合了 Matterport3D, ScanNet, ScanNet++, ARKitScenes, 3RScan 和 ZInD 六个真实室内数据集。
    *   **规模**：包含 17,656 个真实场景，共 270,575 条观测数据。
    *   **划分**：严格的场景不相交划分，其中 ScanNet++ 仅用于分布外（OOD）测试。
*   **对比方法**：共对比了 11 种方法，包括：
    *   **基准方法**：全障碍物、全地面、最近邻传播、均匀随机。
    *   **确定性模型**：U-Net, PConv-UNet, LaMa。
    *   **集成模型**：LaMa-Ensemble（4个种子）。
    *   **随机生成模型**：扩散模型（Diffusion）、流匹配（Flow Matching）、FM+XAttn。
*   **评估指标**：除了 IoU 和 F1，引入了 **UMR**（未观测区域误分类率）和 **MES**（掩码能量分数，用于同时衡量保真度与多样性）。

### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA A100 (40GB) GPU** 进行分布式全精度训练。
*   **训练时长**：总训练步数为 300,000 步，累计消耗约 **280 个 A100 GPU 小时**。
*   **推理算力**：推理可在消费级显卡（如 NVIDIA RTX 4090）上运行。
*   **效率**：确定性模型约 12-15 FPS，生成式模型由于需要多次迭代，速度较慢（0.2-0.6 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：在超过 2.8 万条测试数据上进行了评估，涵盖了分布内（ID）和分布外（OOD）场景。
*   **消融与分析**：
    *   对比了不同难度等级（Easy/Learnable）下的表现。
    *   进行了边界方差分解分析，验证模型是否能将不确定性集中在结构模糊处。
    *   进行了端到端（从原始RGB图像开始）的完整管线测试。
*   **客观性**：实验采用了统一的输入接口和评估掩码，确保了不同范式模型（确定性 vs 随机性）之间的公平比较。

### 6. 主要结论与发现
*   **生成式模型占优**：在 Oracle（最佳样本）评估下，所有随机生成模型均优于确定性模型，证明了多假设采样的必要性。
*   **不确定性定位**：FM+XAttn 模型能将方差集中在真实的布局边界（如墙壁位置），而集成模型（Ensemble）的方差分布较为杂乱，无法有效区分模型不确定性与环境歧义。
*   **泛化能力**：模型在未见过的 ScanNet++ 数据集上保持了性能排名，说明学习到的室内布局先验具有跨数据集的通用性。

### 7. 优点与亮点
*   **数据集贡献**：填补了室内 BEV 度量平面图补全领域缺乏大规模真实数据集的空白。
*   **端到端管线**：不仅研究算法，还构建了从 RGB 到地图的完整实用管线。
*   **评估创新**：提出的 MES 指标和边界方差分析方法为生成式地图补全提供了更科学的评价手段。

### 8. 不足与局限
*   **语义缺失**：目前仅关注二值可通行性（地板 vs 障碍物），未包含房间类型、家具类别等语义信息。
*   **推理延迟**：生成式模型（扩散/流匹配）的迭代采样过程导致推理速度远低于实时要求，限制了其在高速移动机器人上的直接应用。
*   **极端遮挡失效**：当观测信号极度稀疏（如 >80% 区域被遮挡）时，所有模型都会出现边界泄漏或幻觉错误。
*   **简化假设**：假设了简化的针孔相机模型，未考虑真实机器人相机可能存在的畸变或多相机融合情况。

（完）
