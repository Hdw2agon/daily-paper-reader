---
title: "Loop-Mamba: A Loop Mamba with Degradation-Aware and Shared Memory for Old Photo Restoration"
title_zh: Loop-Mamba：一种用于老照片修复的具有退化感知和共享内存的循环 Mamba 框架
authors: "Runci Bai, Yucheng Xin, Pu Wang, Yongcong Wang, Chen Wu, Dianjie Lu, Guijuan Zhang, Pengwen Dai, Guangwei Gao, Siyuan Yao, Zhuoran Zheng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02346v1"
tags: ["query:q1"]
score: 8.0
evidence: 旧照片中缺失区域的修复
tldr: 针对老照片修复中复杂的耦合退化问题，本文提出Loop-Mamba框架。该框架采用轻量级循环状态空间模型，通过语义引导退化估计器（SGDE）感知异构退化，并利用共享结构记忆Mamba（S2M-Mamba）在迭代中维持持久修复状态。该方法通过一阶状态递归减少计算开销并缓解梯度稀释，同时引入多方向扫描增强结构连续性，在SynOld基准测试中表现优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 传统老照片修复方法在处理复杂的耦合退化时，面临计算开销大、长程结构重建困难及梯度稀释等挑战。
method: 提出一种基于循环状态空间的修复框架，结合语义引导的退化估计器和共享结构记忆机制，实现渐进式的状态演化与修复。
result: 在SynOld基准测试上，Loop-Mamba在传统指标及新提出的ODRS指标上均显著超越了现有的最先进方法。
conclusion: Loop-Mamba通过循环递归机制和退化感知引导，为老照片修复提供了一种高效且鲁棒的轻量级解决方案。
---

## 摘要
老照片通常遭受多种耦合退化的影响，包括划痕、裂纹、褪色、模糊、噪声和区域缺失，这严重降低了其视觉质量和语义内容。我们提出了 Loop-Mamba，这是一个轻量级的基于循环的状态空间框架，它将老照片修复建模为渐进式状态演化，通过迭代计算不断传播和细化持久的修复状态。具体而言，我们引入了语义引导退化估计器（SGDE），通过联合预测局部退化图和全局退化分数来显式建模异构退化，为状态演化提供退化感知引导。我们进一步开发了共享结构内存 Mamba（S$^2$M-Mamba），它在迭代过程中维持持久的修复状态，通过共享结构内存实现持久的状态演化，以实现鲁棒的长程结构重建。得益于一阶状态递归，Loop-Mamba 通过循环转换传播潜在修复状态，而不是重复堆叠深层特征变换，从而在缓解梯度稀释的同时，避免了基于 CNN 和 Transformer 的迭代修复框架固有的计算开销。此外，轻量级多向扫描策略增强了方向信息聚合并保持了结构连续性。为了更好地评估修复质量，我们引入了面向任务的老照片损伤恢复评分（ODRS），该评分联合衡量退化恢复和结构重建的保真度。在公开的 SynOld 基准测试上的实验结果表明，Loop-Mamba 在传统修复指标和所提出的 ODRS 上均持续优于之前的最先进方法。

## Abstract
Old photographs often suffer from multiple coupled degradations, including scratches, cracks, fading, blur, noise, and missing regions, severely degrading both visual quality and semantic content. We propose Loop-Mamba, a lightweight loop-based state-space framework that formulates old photo restoration as progressive state evolution, where a persis- tent restoration state is continuously propagated and refined through iterative computation. Specifically, we introduce a Semantic-Guided Degradation Estimator (SGDE) to explicitly model heterogeneous degradations by jointly predicting local degradation maps and global degradation scores, providing degradation-aware guidance for state evolution. We further develop a Shared Structural Memory Mamba (S$^2$M- Mamba), which maintains a persistent restoration state across iterations, enabling persistent state evolution through shared structural memory for robust long-range structural reconstruction. Benefiting from first-order state recursion, Loop-Mamba propagates latent restoration states through recurrent tran- sitions instead of repeatedly stacking deep feature transformations, thereby alleviating gradient dilution while avoiding the computational overhead inherent in iterative CNN- and Transformer-based restoration frameworks. A lightweight multi-directional scanning strategy further enhances direc- tional information aggregation and preserves structural continuity. To better evaluate restoration quality, we introduce the task-oriented Old Photo Damage Recovery Score (ODRS), which jointly measures degradation recovery and structural reconstruction fidelity. Experimental results on the public SynOld benchmark demonstrate that Loop-Mamba consistently outperforms previous state-of-the-art methods across both conventional restoration metrics and the proposed ODRS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Loop-Mamba: A Loop Mamba with Degradation-Aware and Shared Memory for Old Photo Restoration》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
老照片修复（Old Photo Restoration, OPR）面临极大的挑战，因为照片通常存在多种**耦合退化**（如划痕、裂纹、褪色、模糊、噪声和区域缺失）。
*   **研究动机**：传统的基于 CNN 或 Transformer 的方法在处理复杂退化时，要么计算开销巨大（通过堆叠深层网络），要么难以建模长程结构信息，且在迭代修复过程中容易出现梯度稀释问题。
*   **核心目标**：提出一种轻量级且高效的框架，能够感知异构退化，并在迭代过程中保持长程结构的连续性，实现渐进式的图像修复。

### 2. 论文提出的方法论
论文提出了 **Loop-Mamba** 框架，将修复过程建模为“渐进式状态演化”。其核心组件包括：
*   **语义引导退化估计器 (SGDE)**：
    *   通过联合预测**局部退化图**（定位划痕、裂纹等）和**全局退化分数**（评估模糊、褪色程度），显式地为修复过程提供退化感知引导。
*   **共享结构记忆 Mamba (S$^2$M-Mamba)**：
    *   利用状态空间模型（SSM）的特性，在迭代过程中维持一个持久的修复状态。
    *   通过**共享结构内存**机制，在多次循环中不断细化特征，确保长程结构的重建鲁棒性。
*   **一阶状态递归 (First-order State Recursion)**：
    *   不同于重复堆叠不同的层，Loop-Mamba 通过循环转换传播潜在状态，减少了参数量并缓解了深层网络中的梯度稀释。
*   **轻量级多向扫描策略**：
    *   针对 Mamba 的扫描机制进行了优化，通过多方向扫描增强方向信息的聚合，从而更好地保持图像的结构连续性。
*   **ODRS 指标**：
    *   提出“面向任务的老照片损伤恢复评分”（Old Photo Damage Recovery Score），专门用于衡量退化恢复和结构重建的保真度。

### 3. 实验设计
*   **数据集**：主要在 **SynOld** 基准测试集上进行验证。这是一个模拟真实老照片退化的大型合成数据集。
*   **对比方法**：与当前最先进的（SOTA）老照片修复方法进行了对比（涵盖了基于 CNN、GAN 和 Transformer 的主流模型）。
*   **评价指标**：
    *   传统指标：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似性）。
    *   新指标：ODRS（论文提出的损伤恢复评分）。

### 4. 资源与算力
*   **算力说明**：论文摘要和提取文本中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **效率特征**：论文强调了其“轻量级”特性，指出通过循环递归机制避免了传统迭代框架的高额计算开销。

### 5. 实验数量与充分性
*   **实验规模**：论文在 SynOld 基准上进行了详尽的定量对比，并引入了新指标 ODRS 进行多维度评估。
*   **消融实验**：文中提到了对 SGDE 和 S$^2$M-Mamba 等核心组件的有效性验证。
*   **充分性评价**：实验设计较为充分，不仅关注传统图像质量指标，还通过 ODRS 针对老照片修复的特定需求（如划痕去除、结构补全）进行了专项评估，体现了评价的客观性。

### 6. 主要结论与发现
*   **性能领先**：Loop-Mamba 在所有主要指标（PSNR, SSIM, LPIPS, ODRS）上均显著优于之前的 SOTA 方法。
*   **状态演化的有效性**：证明了将修复视为“状态演化”而非简单的特征映射，能更有效地处理复杂的耦合退化。
*   **轻量化优势**：通过参数共享和循环机制，在保持高性能的同时显著降低了模型的复杂度和计算负担。

### 7. 优点
*   **架构创新**：首次将循环 Mamba 结构引入老照片修复领域，解决了长程依赖与计算效率的矛盾。
*   **退化感知**：SGDE 模块通过局部与全局结合的方式，使模型对异构退化的处理更具针对性。
*   **评价体系完善**：提出的 ODRS 指标填补了老照片修复领域缺乏针对性量化评估工具的空白。

### 8. 不足与局限
*   **真实数据泛化性**：虽然在 SynOld 合成数据集上表现优异，但在面对极度多样化、非线性的真实历史照片退化时，其泛化能力仍需更多实证。
*   **迭代次数敏感性**：作为循环框架，修复效果可能受迭代次数 $T$ 的影响，如何在推理速度与修复质量之间取得最优平衡可能需要针对不同图像进行微调。
*   **语义依赖**：SGDE 的效果依赖于语义引导，如果原始照片损毁严重到无法提取有效语义，模型的引导效果可能会打折扣。

（完）
