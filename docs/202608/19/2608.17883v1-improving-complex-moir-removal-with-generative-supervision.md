---
title: Improving Complex Moiré Removal with Generative Supervision
title_zh: 利用生成式监督改进复杂摩尔纹去除
authors: "Xinyang Gu, Zhilu Zhang, Honglei Xu, Yanting Mei, Yukang Ding, Wangmeng Zuo"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17883v1"
tags: ["query:q3"]
score: 8.0
evidence: 针对难以获取干净对照图像的修复任务，提出生成式监督方法
tldr: 针对现有去摩尔纹数据集难以覆盖真实场景中复杂、大尺度且多色摩尔纹的问题，本文提出一种利用生成式基础模型产生训练监督信号的新型数据引擎。该方法通过收集真实摩尔纹图像并结合图像条件生成模型产生候选参考图，并辅以补丁级质量控制，构建了包含6.8K对数据的WildMoiré数据集。实验证明，该生成式监督方案能显著提升多种模型在处理复杂摩尔纹时的性能。
selection_source: fresh_fetch
motivation: 现有数据集难以涵盖真实世界中复杂且难以获取干净对照图的大尺度、多色摩尔纹模式。
method: 利用图像条件生成模型产生候选参考图，并通过补丁级质量控制筛选最优结果，构建了WildMoiré数据集。
result: 在ESDNet、SDXL和Qwen-Image-Edit等模型上的实验均证明，该生成式监督方法能有效提升复杂摩尔纹的去除性能。
conclusion: 通过生成式基础模型辅助构建高质量训练数据，为解决复杂图像恢复任务中的数据匮乏问题提供了新思路。
---

## 摘要
高质量成对数据的可用性对于训练基于学习的图像去摩尔纹模型至关重要。然而，现有数据集仍难以涵盖在不受控的真实场景中捕获的复杂摩尔纹图案。此类退化通常表现为大尺度、多彩的摩尔纹图案。此外，这些图案经常出现在难以获取清晰对应图像的照片中，例如从公共显示屏或现有在线资源获取的照片。在这项工作中，我们提出了一种新型数据引擎，旨在通过生成训练监督来改进复杂摩尔纹的去除。具体而言，我们首先收集包含复杂摩尔纹的真实世界图像，并定位相应的屏幕区域。随后，部署多个以图像为条件的生成式基础模型以生成候选参考图像。为了建立可靠的监督，这些候选图像经过块级质量控制，以过滤并选择最佳结果。基于这一系统范式，我们构建了 WildMoiré 数据集，其中包含 6.8K 个摩尔纹-真值（GT）训练对。为了进行评估，我们还构建了一个独立的测试集，包含约 250 对具有捕获的清晰真值的图像对。在 ESDNet、SDXL 和 Qwen-Image-Edit 上的广泛实验表明，所提出的生成式监督一致地提高了复杂摩尔纹去除的性能。

## Abstract
The availability of high-quality paired data is essential for training learning-based image demoiréing models. However, it remains challenging for existing datasets to encompass the complex moiré patterns captured in uncontrolled real-world scenarios. Such degradations typically manifest as large-scale, multicolored moiré patterns. Moreover, these patterns frequently occur in images for which clean counterparts are difficult to obtain, such as photographs acquired from public displays or existing online resources. In this work, we propose a novel data engine designed to improve the removal of complex moiré patterns by generating training supervision. Specifically, we initially collect real-world images containing complex moiré patterns and localize the corresponding screen regions. Multiple image-conditioned generative foundation models are subsequently deployed to produce candidate references. To establish reliable supervision, these candidates are subjected to patch-level quality control to filter and select the optimal results. Based on this systematic paradigm, we construct the WildMoiré dataset, which contains 6.8K moiré-GT training pairs. For evaluation, we additionally build an independent test set comprising $\sim$250 pairs with captured clean ground truth. Extensive experiments on ESDNet, SDXL, and Qwen-Image-Edit demonstrate that the proposed generative supervision consistently improves the performance of complex moiré removal.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Complex Moiré Removal with Generative Supervision》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的图像去摩尔纹（Demoiréing）模型高度依赖高质量的成对数据（有摩尔纹的图 vs. 干净的真值图）。然而，现有的数据集多在受控实验室环境下拍摄，难以覆盖真实世界中复杂、大尺度且多色的摩尔纹模式。
*   **研究背景**：在现实场景（如拍摄公共显示屏或网络图片）中，获取对应的“干净图像”几乎是不可能的。这导致模型在处理野外（In-the-wild）复杂摩尔纹时效果不佳。
*   **整体含义**：本文旨在通过“生成式监督”解决数据匮乏问题，即利用强大的生成式基础模型来“伪造”高质量的参考图像，从而构建大规模的真实场景训练集。

### 2. 方法论：核心思想与技术细节
论文提出了一种新型的**数据引擎（Data Engine）**，其核心流程如下：
1.  **数据收集与定位**：从互联网和现实场景收集含有复杂摩尔纹的图像，并利用分割或检测技术精确定位图像中的屏幕区域。
2.  **生成候选参考图**：利用多个以图像为条件的生成式基础模型（如基于 Stable Diffusion 的 ControlNet 或其他图像翻译模型），以带摩尔纹的图像为输入，生成多张去除摩尔纹后的候选“干净”图像。
3.  **补丁级质量控制（Patch-level Quality Control）**：由于生成模型可能产生幻觉或细节丢失，系统并不直接使用整张生成图。而是将图像切块，通过预定义的质量评估指标（如感知相似度、边缘保持度等）进行筛选，剔除质量差的补丁，保留最接近真实且清晰的部分。
4.  **构建 WildMoiré 数据集**：最终合成包含 **6.8K 对**“摩尔纹-生成真值”的训练集。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：WildMoiré（6.8K 对生成式监督数据）。
    *   **测试集**：专门构建了一个包含约 **250 对** 具有真实捕获真值（Captured GT）的独立测试集，用于客观评估。
*   **Benchmark 与对比方法**：
    *   **基准模型**：涵盖了三种不同架构的模型：**ESDNet**（经典 CNN 架构）、**SDXL**（扩散模型架构）和 **Qwen-Image-Edit**（多模态大模型架构）。
    *   **对比实验**：对比了在传统数据集（如 FHDMi, LCDMoire）上训练的模型与在 WildMoiré 上微调/训练后的性能差异。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心片段中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **推测**：考虑到涉及 SDXL 的微调和 6.8K 图像的生成，通常需要 A100 或 H100 等级别的 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨模型验证（CNN vs. Diffusion vs. MLLM），证明了方法的普适性。
    *   包含了消融实验，验证了“补丁级质量控制”对提升监督信号质量的必要性。
    *   在独立的真实测试集上进行了定量（PSNR, SSIM, LPIPS）和定性对比。
*   **充分性评价**：实验设计较为充分，特别是引入了多模态大模型（Qwen）作为评估对象，体现了方法的前瞻性。使用真实捕获的测试集作为 Benchmark，保证了评估的客观性。

### 6. 主要结论与发现
*   **生成式监督有效性**：利用生成模型产生的“伪真值”进行训练，能显著提升模型处理真实世界复杂摩尔纹的能力，性能优于仅在合成或受控数据集上训练的模型。
*   **跨架构增益**：无论是轻量级的 CNN 还是参数量巨大的扩散模型和 MLLM，都能从 WildMoiré 数据集中获益。
*   **质量控制的关键性**：简单的生成并不能保证效果，补丁级的筛选是确保监督信号可靠、避免模型学到“幻觉”的关键。

### 7. 优点（亮点）
*   **创新性数据获取**：突破了“必须拍摄成对数据”的限制，利用生成式 AI 的先验知识来反哺底层视觉任务。
*   **实用性强**：WildMoiré 数据集更贴近实际应用场景（如手机翻拍屏幕）。
*   **范式领先**：提出了一种“收集-生成-筛选”的自动化数据增强闭环，可推广至其他难以获取 GT 的图像修复任务。

### 8. 不足与局限
*   **幻觉风险**：尽管有质量控制，生成模型仍可能改变原图的纹理或文字细节，导致去摩尔纹后的图像在严苛场景（如文档识别）下失真。
*   **计算成本**：构建该数据集需要运行大型生成模型，对于资源有限的研究者来说，复现该数据引擎的成本较高。
*   **依赖基础模型**：该方法的上限取决于所使用的生成式基础模型的能力，如果基础模型本身无法理解某种特殊的摩尔纹，则无法生成正确的监督信号。

（完）
