---
title: "Tuning Real-World Image Restoration at Inference: A Test-Time Scaling Paradigm for Flow Matching Models"
title_zh: 推理阶段的真实场景图像修复微调：一种针对流匹配模型的测试时缩放范式
authors: "Purui Bai, Junxian Duan, Pin Wang, Jinhua Hao, Ming Sun, Chao Zhou, Huaibo Huang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22027v1"
tags: ["query:q3"]
score: 8.0
evidence: 无需重训的推理侧图像修复测试时缩放
tldr: 本文针对真实世界图像修复（Real-IR）中预训练大模型利用率不足的问题，提出了基于FLUX.1-dev流匹配模型的ResFlow-Tuner框架。该框架通过统一多模态融合（UMMF）充分发挥MM-DiT架构优势，并创新性地引入了无需训练的推理时缩放（TTS）范式，利用奖励模型在推理阶段动态引导去噪方向。实验证明该方法在多个基准测试中达到SOTA水平，为大模型在底层视觉任务中的高效推理提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决如何高效利用超大规模预训练文本生成图像模型并充分挖掘其在真实世界图像修复任务中潜力的问题。
method: 提出了ResFlow-Tuner框架，结合了统一多模态融合技术与基于奖励模型反馈的推理时缩放（TTS）引导机制。
result: 在多个标准基准测试中取得了当前最先进的性能表现，显著提升了图像修复的质量。
conclusion: 本文验证了流匹配模型在底层视觉任务中的强大能力，并提出了一种适用于大规模预训练模型的高效推理时缩放新范式。
---

## 摘要
尽管基于扩散模型的真实场景图像修复（Real-IR）已取得显著进展，但如何高效利用超大规模预训练文本生成图像（T2I）模型并充分挖掘其潜力仍是重大挑战。为解决这一问题，我们提出了 ResFlow-Tuner，这是一个基于最先进流匹配模型 FLUX.1-dev 的图像修复框架，它集成了统一多模态融合（UMMF）与测试时缩放（TTS），以实现前所未有的修复性能。我们的方法通过将多模态条件编码为统一序列来引导高质量图像的合成，充分利用了多模态扩散 Transformer（MM-DiT）架构的优势。此外，我们引入了一种专为图像修复量身定制的免训练测试时缩放范式。在推理过程中，该技术通过奖励模型（RM）的反馈动态引导去噪方向，从而在计算开销可控的情况下实现显著的性能提升。大量实验表明，我们的方法在多个标准基准测试中均达到了最先进的性能。这项工作不仅验证了流匹配模型在底层视觉任务中的强大能力，更重要的是，提出了一种适用于大型预训练模型的新颖且高效的推理阶段缩放范式。

## Abstract
Although diffusion-based real-world image restoration (Real-IR) has achieved remarkable progress, efficiently leveraging ultra-large-scale pre-trained text-to-image (T2I) models and fully exploiting their potential remain significant challenges. To address this issue, we propose ResFlow-Tuner, an image restoration framework based on the state-of-the-art flow matching model, FLUX.1-dev, which integrates unified multi-modal fusion (UMMF) with test-time scaling (TTS) to achieve unprecedented restoration performance. Our approach fully leverages the advantages of the Multi-Modal Diffusion Transformer (MM-DiT) architecture by encoding multi-modal conditions into a unified sequence that guides the synthesis of high-quality images. Furthermore, we introduce a training-free test-time scaling paradigm tailored for image restoration. During inference, this technique dynamically steers the denoising direction through feedback from a reward model (RM), thereby achieving significant performance gains with controllable computational overhead. Extensive experiments demonstrate that our method achieves state-of-the-art performance across multiple standard benchmarks. This work not only validates the powerful capabilities of the flow matching model in low-level vision tasks but, more importantly, proposes a novel and efficient inference-time scaling paradigm suitable for large pre-trained models.

---

## 论文详细总结（自动生成）

### 论文总结：ResFlow-Tuner —— 推理阶段的真实场景图像修复微调

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行大规模全量微调的情况下，高效利用超大规模预训练流匹配模型（如 FLUX.1）的强大先验知识，并解决真实世界图像修复（Real-IR）中复杂退化导致的高质量重建难题。
*   **研究动机**：
    *   现有的基于扩散模型（SD）的修复方法在处理复杂退化时仍存在细节丢失或语义偏差。
    *   流匹配模型（Flow Matching）相比扩散模型具有更直观的确定性轨迹，但在修复任务中缺乏有效的推理侧优化手段。
    *   传统的条件注入方式（如简单的特征相加或 Adapter）限制了模型对多模态先验（图像+文本）的深度融合。

#### 2. 方法论
ResFlow-Tuner 框架主要包含两个核心阶段：
*   **统一多模态融合（UMMF）**：
    *   **条件准备**：先用轻量级 SwinIR 去除基础噪声得到参考图，再利用视觉语言模型（Qwen-2.5-VL）生成详细的语义描述文本。
    *   **序列化融合**：借鉴 MM-DiT 架构，将潜在变量（Latent）、低质图像特征和文本特征全部编码为 Token 序列并拼接，通过自注意力机制实现深度的跨模态交互，而非传统的交叉注意力注入。
*   **测试时缩放（Test-Time Scaling, TTS）**：
    *   **ODE-SDE 转换**：流匹配模型本质是确定性的 ODE 轨迹。为了在推理时进行搜索，作者引入随机扩散项将其转化为等效的 SDE，从而在确定性轨迹周围引入可探索的“扰动”。
    *   **搜索与评估循环**：在去噪的关键时间步，生成多个候选扰动状态，利用**多步部分去噪估计器（MSPDE）**快速预测最终结果，并通过**奖励模型集成（Verifier Ensemble）**进行评分。
    *   **奖励模型集成**：综合美学评分（Aesthetic Score）、语义一致性（CLIP-Score）和人类偏好（ImageReward），采用基于排名的集成策略选择最佳路径。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：DIV2K, Flickr2K, LSDIR, DIV8K。
    *   **合成测试集**：DIV2K-Val（分为轻度、中度、重度退化）、LSDIR-Val。
    *   **真实测试集**：RealPhoto60, RealSR, DRealSR。
*   **Benchmark 与指标**：
    *   有参考指标：PSNR, SSIM, LPIPS, DISTS。
    *   无参考指标（更符合感知质量）：FID, MUSIQ, CLIPIQA+, MANIQA, PaQ-2-PiQ。
*   **对比方法**：对比了 13 种最先进方法，包括基于 GAN 的（Real-ESRGAN）、基于扩散模型的（StableSR, DiffBIR, SUPIR, FaithDiff 等）以及基于流匹配的（PnP-Flow, FlowSR）。

#### 4. 资源与算力
*   **模型规模**：基础模型为 12B 参数的 FLUX.1-dev，文本编码器包含 Qwen2.5-VL-7B。
*   **训练细节**：使用 LoRA 进行高效微调（Rank=16），仅训练 58M 参数（约占总量的 0.5%）。
*   **训练配置**：分辨率 512x512，有效 Batch Size 为 64，训练 30,000 次迭代。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100 数量）及总训练时长，但提到使用了 Prodigy 优化器以实现参数无关的快速收敛。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成退化到真实场景的 5 个主流数据集，并进行了下游任务（OCR 识别准确率）的定量评估。
*   **消融实验**：非常充分。分别验证了文本提示词的必要性、UMMF 融合策略的优越性、不同奖励模型组合的效果，以及 TTS 搜索参数（候选数 $N$ 和干预步数 $K$）对性能的单调提升关系。
*   **主观评价**：组织了 64 名参与者的用户研究（User Study），通过成对比较和 Bradley-Terry 模型进行排名，证明了其在人类视觉偏好上的领先地位。
*   **客观性**：实验对比了多种不同架构（GAN, Diffusion, Flow）的方法，评价维度全面，结果具有较高的说服力。

#### 6. 主要结论与发现
*   **性能领先**：ResFlow-Tuner 在无参考质量指标上显著超越了 SUPIR 和 FaithDiff 等强力竞争对手，建立了新的 SOTA。
*   **推理侧缩放有效性**：证明了通过增加推理时的计算量（更多搜索分支），可以单调地提升图像修复质量，这为底层视觉任务提供了一种“以计算换质量”的新范式。
*   **语义引导的重要性**：详细的文本描述能显著修正修复过程中的语义错误（如文字恢复、特定纹理重建）。

#### 7. 优点
*   **免训练缩放**：TTS 机制不需要重新训练模型，具有很强的灵活性。
*   **参数高效**：仅微调 0.5% 的参数即可带动 12B 大模型的修复能力，训练成本相对较低。
*   **理论完备**：将 ODE 转化为 SDE 进行路径搜索，为流匹配模型的推理优化提供了坚实的数学基础。
*   **感知质量极高**：生成的图像在细节纹理和语义正确性上表现优异。

#### 8. 不足与局限
*   **推理开销大**：为了达到最佳效果，TTS 需要约 15 倍于基准模型的计算量（NFEs），这限制了其在实时场景中的应用。
*   **显存要求高**：基于 12B 的 FLUX 模型，推理时需要较大的显存支持，普通消费级显卡可能难以运行。
*   **奖励模型依赖**：修复质量高度依赖于预训练奖励模型的准确性，若奖励模型存在偏见，可能会导致生成伪影。

（完）
