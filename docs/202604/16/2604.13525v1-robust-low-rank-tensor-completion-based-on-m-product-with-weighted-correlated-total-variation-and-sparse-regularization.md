---
title: Robust Low-Rank Tensor Completion based on M-product with Weighted Correlated Total Variation and Sparse Regularization
title_zh: 基于 M-积、加权相关全变分与稀疏正则化的鲁棒低秩张量补全
authors: "Biswarup Karmakar, Ratikanta Behera"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13525v1"
tags: ["query:q1"]
score: 8.0
evidence: 恢复具有缺失条目的受损高维张量数据
tldr: 本研究针对高维张量数据恢复中的缺失项、离群点和稀疏噪声问题，提出了一种基于M-product框架的鲁棒低秩张量补全方法。针对传统方法均匀正则化导致结构丢失的缺陷，引入了加权相关全变分（TWCTV）正则化项，结合加权Schatten-p范数和加权稀疏正则化，通过自适应权重保护关键结构。实验证明该方法在图像补全、去噪和背景建模任务中表现优异。
selection_source: fresh_fetch
motivation: 传统张量补全方法采用均匀正则化会导致关键奇异值和稀疏成分被过度收缩，从而破坏张量的结构细节。
method: 提出基于M-product框架的加权相关全变分（TWCTV）正则化器，结合梯度张量的加权Schatten-p范数与加权稀疏项，并利用增强的ADMM算法求解。
result: 在图像补全、去噪及背景减除等任务中，该方法在恢复精度和细节保持方面均显著优于现有的基准方法。
conclusion: 该研究通过自适应加权机制和M-product框架，有效提升了鲁棒低秩张量补全的性能，为处理复杂高维数据提供了更具结构保护能力的支撑。
---

## 摘要
鲁棒低秩张量补全问题旨在解决现实应用中常见的、包含缺失项、离群值和稀疏噪声的受损高维张量数据的恢复挑战。现有方法由于依赖统一的正则化方案（特别是张量核范数和 $\ell_1$ 范数正则化方法）而面临根本性的局限。这些方法对所有奇异值和稀疏分量无差别地应用相等的收缩，从而损害了关键张量结构的保持。所提出的张量加权相关全变分（TWCTV）正则化项通过 $M$-积框架解决了这些缺陷，该框架结合了梯度张量上的加权 Schatten-$p$ 范数以实现低秩性，并结合了平滑性约束和加权稀疏分量以抑制噪声。所提出的加权方案自适应地降低阈值水平，以保留主导奇异值和稀疏分量，从而改善了恢复信号中关键结构元素和细微细节的重建。通过系统的算法方法，我们引入了一种增强的交替方向乘子法（ADMM），该方法兼具计算效率和理论依据，并在 $M$-积框架内对其收敛性质进行了全面分析。在图像补全、去噪和背景减除任务上的全面数值评估验证了该方法相对于现有基准方法的优越性能。

## Abstract
The robust low-rank tensor completion problem addresses the challenge of recovering corrupted high-dimensional tensor data with missing entries, outliers, and sparse noise commonly found in real-world applications. Existing methodologies have encountered fundamental limitations due to their reliance on uniform regularization schemes, particularly the tensor nuclear norm and $\ell_1$ norm regularization approaches, which indiscriminately apply equal shrinkage to all singular values and sparse components, thereby compromising the preservation of critical tensor structures. The proposed tensor weighted correlated total variation (TWCTV) regularizer addresses these shortcomings through an $M$-product framework that combines a weighted Schatten-$p$ norm on gradient tensors for low-rankness with smoothness enforcement and weighted sparse components for noise suppression. The proposed weighting scheme adaptively reduces the thresholding level to preserve both dominant singular values and sparse components, thus improving the reconstruction of critical structural elements and nuanced details in the recovered signal. Through a systematic algorithmic approach, we introduce an enhanced alternating direction method of multipliers (ADMM) that offers both computational efficiency and theoretical substantiation, with convergence properties comprehensively analyzed within the $M$-product framework.Comprehensive numerical evaluations across image completion, denoising, and background subtraction tasks validate the superior performance of this approach relative to established benchmark methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于 $M$-积（M-product）框架的鲁棒低秩张量补全（RLRTC）方法，旨在解决高维数据恢复中的结构保护问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从含有缺失值、离群点和稀疏噪声的受损高维张量数据中，精确恢复其低秩结构并保持局部平滑性。
*   **研究动机**：
    *   **传统方法的局限性**：现有的张量核范数（TNN）和 $\ell_1$ 范数正则化通常采用“均匀收缩”策略，即对所有奇异值或稀疏分量施加相同的惩罚。这会导致重要的结构信息（较大的奇异值）被过度抑制，从而丢失图像边缘或视频细节。
    *   **结构耦合需求**：低秩性（全局特征）和平滑性（局部特征）往往被分开处理，缺乏一种能同时捕捉两者的统一非凸框架。

### 2. 方法论
*   **核心思想**：在 $M$-积代数框架下，提出**张量加权相关全变分（TWCTV）**正则化器，结合非凸的**加权 Schatten-$p$ 范数**和**加权 $\ell_1$ 范数**。
*   **关键技术细节**：
    *   **$M$-积框架**：利用可逆线性变换（如 DCT、DFT）将张量变换到频域，在不进行张量展开（Unfolding）的情况下处理高维结构，避免破坏空间相关性。
    *   **TWCTV 正则化**：在梯度域（Gradient Domain）应用加权 Schatten-$p$ 范数（$0 < p < 1$）。通过对梯度张量施加低秩约束，同时实现了空间平滑性和低秩性的耦合。
    *   **自适应权重设计**：
        *   **奇异值权重**：采用基于 Sigmoid 函数的指数权重，对较小的奇异值施加较大惩罚，对较大的奇异值施加较小惩罚。
        *   **稀疏项权重**：为噪声项 $E$ 设计自适应权重，以更精准地识别并抑制稀疏噪声。
    *   **算法流程**：使用增强的**交替方向乘子法（ADMM）**。将复杂的非凸问题分解为 $X$（干净张量）、$G$（梯度张量）、$E$（稀疏噪声）等子问题，并利用广义奇异值阈值（GSVT）算子求解。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：用于相变分析（Phase Transition）和收敛性验证。
    *   **彩色图像补全**：使用 Facade 图像，对比随机缺失和文字遮挡。
    *   **多光谱图像（MSI）补全**：使用 cloth_ms 数据集。
    *   **高光谱图像（HSI）去噪**：使用 hsi-Pac 数据集，处理不同比例的椒盐噪声。
    *   **视频背景建模**：使用 CDNet 2014 视频序列（如 highway），进行前景目标检测。
*   **对比方法（Benchmarks）**：包括 SNN, BCPF, KBR, IRTNN, TNN, TNN+TV, SNN+TV, SPC+TV, TCTV, LRTV, LRTDTV, TLR-HTV 等十余种先进方法。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-12900K (3.2 GHz) 处理器，32 GB RAM。
*   **软件环境**：MATLAB R2023b，运行于 Windows 11。
*   **算力说明**：文中未提及使用 GPU 加速，所有实验均在 CPU 上完成。计算复杂度主要集中在每轮迭代的 SVD 分解和 FFT/DCT 变换上。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从合成数据到多种真实场景（RGB、MSI、HSI、视频）的广泛测试。
*   **充分性**：
    *   **消融实验**：对比了“双非凸”与“单非凸”模型的性能，验证了加权 $\ell_1$ 端的必要性。
    *   **参数敏感性**：分析了 $p$ 值选择、惩罚参数 $\rho$ 以及不同变换矩阵（DCT vs DFT）对结果的影响。
    *   **评价指标**：使用了 PSNR, SSIM, FSIM, ERGAS, Precision, Recall, F-Measure 等多维度指标，实验设计客观且公平。

### 6. 主要结论与发现
*   **性能优越性**：在所有测试任务中，TWCTV 方法在 PSNR 和 SSIM 等指标上均显著优于现有方法，尤其是在高缺失率（如 95% 缺失）和高噪声水平下表现稳健。
*   **变换矩阵影响**：离散余弦变换（DCT）在处理非周期性图像数据时，比离散傅里叶变换（DFT）具有更好的能量集中特性，恢复误差更低且计算速度更快。
*   **非凸优势**：$p < 1$ 的 Schatten-$p$ 范数比传统的核范数（$p=1$）能更有效地逼近张量秩，减少恢复偏差。

### 7. 优点
*   **自适应性强**：通过指数权重机制，模型能根据数据内容自动调整惩罚力度，保护了图像的边缘和细节。
*   **理论完备**：提供了详细的收敛性证明，确保了 ADMM 算法在非凸设置下的稳定性。
*   **多功能性**：一个框架同时适用于补全（Completion）和鲁棒主成分分析（RPCA）任务。

### 8. 不足与局限
*   **计算开销**：尽管使用了 $M$-积简化计算，但每一轮迭代仍需对大量频率分片进行 SVD 分解，对于超大规模高维张量，计算时间可能较长。
*   **参数依赖**：虽然权重是自适应的，但超参数（如 $\lambda, p, \rho$）仍需根据具体应用场景进行微调，缺乏完全自动化的参数推荐机制。
*   **实时性限制**：由于算法涉及多次迭代和复杂的数学变换，目前可能难以直接应用于超高帧率的实时视频处理。

（完）
