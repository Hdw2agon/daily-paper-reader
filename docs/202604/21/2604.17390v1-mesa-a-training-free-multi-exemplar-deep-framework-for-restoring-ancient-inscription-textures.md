---
title: "MESA: A Training-Free Multi-Exemplar Deep Framework for Restoring Ancient Inscription Textures"
title_zh: MESA：一种用于修复古代碑刻纹理的无需训练的多范例深度框架
authors: "Vasileios Toulatzis, Ioannis Fudos"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17390v1"
tags: ["query:q3"]
score: 8.0
evidence: 无需训练的缺失区域图像级修复
tldr: 针对古代碑文因侵蚀或破损导致难以辨识的问题，本文提出了一种无需训练的深度框架MESA。该方法利用同源或风格相似的完好碑文作为参考样本，通过VGG19提取纹理和笔画特征，并结合OCR估计的字符宽度动态调整层权重，实现对受损区域的精准修复。MESA在保持原图完好部分的同时，有效解决了传统方法在缺乏大规模训练数据时的修复难题。
selection_source: fresh_fetch
motivation: 古代碑文常因自然风化或人为破坏而残缺不全，且由于缺乏大规模标注数据，传统的深度学习修复方法难以直接应用。
method: 提出MESA框架，利用VGG19特征和Gram矩阵捕捉参考样本的纹理风格，通过最小化均方位移选择最佳样本，并结合OCR字符宽度优化层权重。
result: 实验结果表明，MESA在碑文纹理恢复和文字结构重建方面优于现有的图像修复和生成对抗网络（GAN）方法。
conclusion: MESA为缺乏训练数据的古代文献修复提供了一种高效、无需训练且具备风格感知能力的实用解决方案。
---

## 摘要
古代碑刻经常因碎片化、侵蚀或其他损坏而出现缺失或损坏区域，阻碍了阅读和分析。我们回顾了先前的图像修复方法及其在碑刻图像恢复中的适用性，随后介绍了 MESA（多范例、风格感知）——一种图像级修复方法，它利用保存完好的范例碑刻（来自同一碑文古迹、材质或具有相似字形）来指导受损文本的重建。MESA 将 VGG19 卷积特征编码为 Gram 矩阵，以捕捉范例的纹理、风格和笔画结构；对于每个神经网络层，它会选择与受损输入之间的均方位移（MSD）最小的范例。逐层贡献权重源自范例集中通过光学字符识别（OCR）估计的字符宽度，从而使滤波器偏向于匹配字母几何形状的尺度，并且训练掩码保留了完整区域，使合成仅限于受损区域。我们还总结了先前的网络架构以及范例和单图像合成、修复和生成对抗网络（GAN）方法，并强调了 MESA 所解决的局限性。对比实验证明了 MESA 的优势。最后，我们为在给定可用范例和元数据的情况下选择修复策略提供了一个实用的路线图。

## Abstract
Ancient inscriptions frequently suffer missing or corrupted regions from fragmentation, erosion, or other damage, hindering reading, and analysis. We review prior image restoration methods and their applicability to inscription image recovery, then introduce MESA (Multi-Exemplar, Style-Aware) -an image-level restoration method that uses well-preserved exemplar inscriptions (from the same epigraphic monument, material, or similar letterforms) to guide reconstruction of damaged text. MESA encodes VGG19 convolutional features as Gram matrices to capture exemplar texture, style, and stroke structure; for each neural network layer it selects the exemplar minimizing Mean-Squared Displacement (MSD) to the damaged input. Layer-wise contribution weights are derived from Optical Character Recognition-estimated character widths in the exemplar set to bias filters toward scales matching letter geometry, and a training mask preserves intact regions so synthesis is restricted to damaged areas. We also summarize prior network architectures and exemplar and single-image synthesis, inpainting, and Generative Adversarial Network (GAN) approaches, highlighting limitations that MESA addresses. Comparative experiments demonstrate the advantages of MESA. Finally, we provide a practical roadmap for choosing restoration strategies given available exemplars and metadata.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MESA**（Multi-Exemplar, Style-Aware）的无需训练（Training-Free）深度框架，专门用于修复古代碑刻图像的纹理和文字。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：古代碑刻常因自然风化（侵蚀、碎片化）或人为破坏（划痕）导致文字模糊或缺失，严重阻碍了考古研究。
*   **研究动机**：
    *   现有的文字修复方法多集中在字符级预测（如利用大语言模型预测缺失字母），忽略了图像层面的纹理和视觉结构恢复。
    *   传统的深度学习修复模型（如 GAN）需要大量“受损-清晰”对照的训练数据，而古代碑刻的标注数据极度匮乏。
    *   需要一种能够利用少量相似风格范例（Exemplars）且无需针对特定数据集进行大规模预训练的通用修复方案。

### 2. 方法论
MESA 的核心思想是利用预训练的卷积神经网络（VGG19）作为特征提取器，通过多范例引导的风格迁移技术实现修复。
*   **多范例引导（Multi-Exemplar）**：利用来自同一碑文古迹或风格相似的完好碑刻作为参考。对于 VGG19 的每一层，系统会自动选择与受损输入之间均方位移（MSD）最小的范例，以确保每一层都能获得最匹配的纹理引导。
*   **Gram 矩阵与风格感知**：将特征图编码为 Gram 矩阵，捕捉碑刻的纹理、笔画结构和艺术风格，而非精确的像素位置。
*   **OCR 驱动的层权重优化**：这是本文的一大创新。利用 Tesseract OCR 统计范例中字母的平均宽度，并拟合概率分布。根据 VGG 各层的感受野（滤波器尺寸）与字母宽度的匹配程度分配权重，使网络更关注与文字几何特征相符的尺度。
*   **空间掩码（Masking）**：在优化过程中使用二值掩码，确保只有受损区域被重新合成，而完好的原始区域被严格保留。
*   **算法流程**：输入受损图和一组范例图 -> 提取 VGG 特征 -> 计算加权 Gram 矩阵损失 -> 使用 L-BFGS 算法迭代优化输入图像的受损像素。

### 3. 实验设计
*   **数据集**：使用了三个不同的碑刻数据集（Dataset A, B, C），涵盖了不同的材质和字体风格。
*   **受损场景**：人工合成了两种主要的退化类型：**环境噪声**（模拟风化）和**人为划痕**。
*   **对比方法（Benchmark）**：
    *   **无需训练类**：Deep Image Prior (DIP) 的去噪和修复变体。
    *   **需训练类（有监督）**：DNCNN（去噪）、DRUNET（通用修复）、ESRGAN（超分辨率生成网络）。
*   **评估指标**：
    *   图像质量：PSNR、SSIM、LPIPS（感知相似度）。
    *   文字准确性：Levenshtein 距离（编辑距离），以及论文新提出的 **TRS（Text Recovery Score）** 和 **LLS（Log-scaled Levenshtein Similarity）**，旨在更客观地衡量文字恢复的比例。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA GeForce RTX 2080 Ti GPU** (11GB GDDR6)。
*   **优化细节**：采用 L-BFGS 优化算法，每个输入图像的优化上限为 **5000 个 Epoch**。
*   **训练时长**：由于是无需训练的方法，其“训练”过程即为针对单张图的优化过程，具体单张修复耗时未明确给出，但通常此类方法单张处理需数分钟。

### 5. 实验数量与充分性
*   **实验规模**：在三个数据集上分别针对噪声和划痕进行了多组实验，并计算了平均指标。
*   **充分性**：实验设计较为全面，不仅对比了同类无需训练的方法，还挑战了需要大量数据预训练的有监督模型。
*   **客观性**：通过引入 OCR 识别结果作为文字恢复的硬性指标（编辑距离），弥补了传统图像指标（如 PSNR）在文字可读性评价上的不足，实验结果较为客观。

### 6. 主要结论与发现
*   **修复效果**：MESA 在视觉恢复和文字可读性（TRS/LLS 指标）上均优于 DIP 等无需训练的方法。
*   **对比优势**：在缺乏配对训练数据的情况下，MESA 的表现甚至能与经过专门训练的有监督模型（如 ESRGAN）相媲美，且在保持原始纹理一致性方面更具优势。
*   **权重作用**：基于 OCR 的层权重分配能显著提升文字结构的恢复质量，使修复后的笔画更符合碑刻原有的几何特征。

### 7. 亮点
*   **无需训练**：极大地降低了在文化遗产保护领域的应用门槛，无需收集数万张标注图。
*   **跨学科结合**：巧妙地将 OCR 统计信息（文字几何尺度）引入深度学习的层权重分配中，增强了物理意义。
*   **新指标**：提出的 TRS 和 LLS 指标解决了长文本在编辑距离计算中容易出现的数值溢出或不敏感问题。

### 8. 不足与局限
*   **范例依赖性**：MESA 的效果高度依赖于“范例”的选择。如果没有风格高度相似的完好碑刻作为参考，修复效果会大打折扣。
*   **脚本局限性**：目前的 OCR 权重方案主要针对拉丁语和希腊语等字母文字。对于象形文字（如甲骨文、象形文字）或楔形文字，现有的 OCR 可能无法准确估计“字符宽度”，导致权重失效。
*   **计算成本**：针对每一张受损图像都需要进行数千次迭代优化，在大规模数字化修复任务中，处理速度可能成为瓶颈。

（完）
