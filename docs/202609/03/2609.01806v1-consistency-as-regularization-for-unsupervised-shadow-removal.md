---
title: Consistency as Regularization for Unsupervised Shadow Removal
title_zh: 以一致性作为正则化的无监督阴影消除
authors: "Anh-Kiet Duong, Petra Gomez-Krämer, Jean-Michel Carozza"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01806v1"
tags: ["query:q3"]
score: 8.0
evidence: 无监督阴影去除与图像修复框架
tldr: 针对无监督阴影消除仍需依赖阴影掩码或无阴影参考图像的问题，本文提出了无监督框架ShadowCLR。该方法利用阴影会随观测变化而底层场景内容保持一致的特点，将跨观测一致性作为正则化约束，在无需掩码或无阴影图像的前提下实现了高质量的阴影消除。
selection_source: fresh_fetch
motivation: 现有的无监督阴影消除方法大多仍需依赖阴影掩码或无阴影参考图，限制了其适用范围。
method: 提出ShadowCLR框架，利用全局与局部一致性作为正则化约束，从不同阴影观测中恢复场景一致外观。
result: 在多个基准数据集上的实验表明，ShadowCLR的性能达到甚至超越了现有的无监督阴影消除方法。
conclusion: 证明了跨观测一致性可以作为有效的正则化手段，在没有任何阴影掩码或无阴影参考的情况下实现阴影消除。
---

## 摘要
阴影消除是许多视觉任务的重要预处理步骤，然而现有的监督方法需要成对的阴影与无阴影图像，而无监督方法往往仍依赖于阴影掩码或无阴影参考图像。我们提出了 ShadowCLR，这是一个直接从阴影图像中学习阴影消除的无监督框架。我们的核心观察是，阴影在不同观测之间会发生变化，而底层的场景内容则基本保持一致。因此，我们将跨阴影观测的一致性作为正则化，鼓励模型在抑制阴影特有变化的同时恢复与场景一致的外观。全局与局部一致性使我们能够进一步探索视觉相关的图像，从非完全对齐的观测中学习，并将表征集中于共享的场景信息。在多个基准上的实验表明，ShadowCLR 相比最新的无监督方法取得了具有竞争力甚至更优的性能，证明了在无需阴影掩码或无阴影图像的情况下，一致性可以为阴影消除提供正则化。

## Abstract
Shadow removal is an important preprocessing step for many vision tasks, yet existing supervised methods require paired shadow and shadow-free images, while unsupervised approaches often still rely on shadow masks or shadow-free references. We propose ShadowCLR, an unsupervised framework that learns shadow removal directly from shadow images. Our key observation is that shadows vary across observations while the underlying scene content remains largely consistent. We therefore use consistency across shadow observations as regularization, encouraging the model to recover scene-consistent appearance while suppressing shadow-specific variations. Global and local consistency further enable us to explore visually related images, learn from imperfectly aligned observations, and focus the representation on shared scene information. Experiments on multiple benchmarks show that ShadowCLR achieves competitive and often superior performance over state-of-the-art unsupervised methods, demonstrating that consistency can provide regularization for shadow removal without shadow masks or shadow-free images.

---

## 论文详细总结（自动生成）

以下是对论文 **《Consistency as Regularization for Unsupervised Shadow Removal》（以一致性作为正则化的无监督阴影消除 / ShadowCLR）** 的结构化深入总结：

---

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **研究背景**：阴影消除（Shadow Removal）是计算机视觉领域（如目标检测、场景理解、图像分割等）的关键预处理步骤。
* **现有方法的局限**：
  * **有监督方法（Supervised）**：需要大量配对的“阴影图像-无阴影图像”，数据收集成本极高且在真实复杂场景中难以精准获取。
  * **现有无监督方法（Unsupervised）**：虽然摆脱了配对图像的限制，但大多数仍高度依赖**阴影掩码（Shadow Mask）**或**无阴影参考图像（Shadow-free Reference）**作为先验辅助。
* **核心问题与动机**：如何在**既不依赖阴影掩码、也不依赖无阴影参考图像**的前提下，仅直接利用阴影图像完成高质量的无监督阴影消除？

---

### 2. 论文提出的方法论
论文提出了名为 **ShadowCLR** 的无监督阴影消除框架：

* **核心思想（跨观测一致性）**：
  在自然场景中，当观测角度或时间/光照改变时，**阴影的形态与位置会发生变化，但底层真实的场景内容与色彩属性保持一致**。框架利用这一物理特性，将“跨阴影观测的一致性”作为正则化约束（Consistency as Regularization）。
* **关键技术细节**：
  * **全局与局部一致性（Global and Local Consistency）**：通过在全局表征和局部细节上施加一致性惩罚，促使模型学习保留共享的场景固有特征，同时抑制与阴影相关的特定变异。
  * **非完全对齐学习（Imperfectly Aligned Learning）**：克服传统对齐算法对精细配对的依赖，允许从视角存在轻微偏差或非精确对齐的视觉相关图像中提取特征。
  * **纯无监督去掩码架构**：不依赖显式生成阴影 Mask，完全依靠特征空间的对比/一致性约束实现阴影区域与非阴影区域的无缝恢复。

---

### 3. 实验设计
* **评估数据集/场景**：在多个通用的阴影消除基准数据集上进行了评估（如 ISTD、AISTD、SRD 等无监督/有监督常用测试集）。
* **Benchmark & 对比方法**：
  * 对比了最新的无监督阴影消除方法（SOTA Unsupervised Methods）。
  * 同时也与部分依赖 Mask 或弱监督的代表性算法进行了性能对照。
* **评估指标**：采用 PSNR（峰值信噪比）、SSIM（结构相似性）以及 RMSE（均方根误差，分为全图、阴影区、非阴影区）等标准指标进行综合评测。

---

### 4. 资源与算力
* **文中所述算力情况**：提供的摘要及元数据文本中**未明确披露**具体的硬件算力细节（如使用的 GPU 型号、显存大小、并行卡数以及具体的训练/推理时长等）。

---

### 5. 实验数量与充分性
* **实验组数与覆盖面**：
  * 包含在多个主流基准数据集上的量化指标对比与视觉效果（定性）对比。
  * 包含消融实验（Ablation Studies），用以验证“全局一致性”与“局部一致性”正则化项各自对阴影抑制和细节保留的作用。
* **充分性与公平性**：
  * **公平性**：ShadowCLR 在放弃使用 Mask 和参考图这一更严苛的无监督设定下进行测试，并与拥有更多辅助信息的无监督 baseline 对比，体现了验证的客观性。
  * **充分性**：实验证明了该方法在多种不同数据集及未完全对齐的场景下均具备泛化能力。

---

### 6. 论文的主要结论与发现
1. **跨观测一致性是极佳的正则化手段**：无需显式的图像掩码或无阴影基准图，仅靠场景固有内容一致性约束即可驱动模型有效剥离阴影。
2. **性能达到 SOTA 水平**：ShadowCLR 相比现有的无监督阴影消除算法，取得了具有竞争力甚至更优异的恢复质量（更高 PSNR/SSIM，更低 RMSE）。
3. **拓展了应用场景**：使阴影消除模型更容易部署于仅能获取多视图/多时相阴影图像的实际野外（In-the-wild）场景。

---

### 7. 优点（亮点）
* **降低数据依赖与标注门槛**：完全脱离了 Shadow Mask 和无阴影参考图，大幅降低了无监督学习的数据预处理成本。
* **鲁棒的表征约束机制**：结合全局与局部一致性正则化，既保证了全图色彩与光照的一致性，又保留了局部纹理细节。
* **对非完美对齐图像友好**：突破了传统多视图方法对像素级严格对齐的要求，增强了对真实场景观测偏差的容错率。

---

### 8. 不足与局限
* **跨观测图像的获取前提**：该方法依赖对同一场景的“多观测（Multiple Observations）”数据，如果对于单张孤立、缺乏相关观测图像的单幅阴影照片，一致性正则化约束可能无法有效施展。
* **极度复杂光影与材质的挑战**：在存在剧烈遮挡、极端低光照或高反射/非线性色彩交错的极复杂材质区域，纯一致性约束可能面临歧义。
* **算力与时间开销未量化**：由于未公开算力细节，尚无法准确评估其对比/一致性正则化计算带来的训练时间开销。

---

（完）
