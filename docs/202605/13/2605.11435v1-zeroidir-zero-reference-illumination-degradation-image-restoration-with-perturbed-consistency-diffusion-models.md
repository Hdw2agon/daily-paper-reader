---
title: "ZeroIDIR: Zero-Reference Illumination Degradation Image Restoration with Perturbed Consistency Diffusion Models"
title_zh: ZeroIDIR：基于扰动一致性扩散模型的零参考光照退化图像恢复
authors: "Hai Jiang, Zhen Liu, Yinjie Lei, Songchen Han, Bing Zeng, Shuaicheng Liu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11435v1"
tags: ["query:q3"]
score: 9.0
evidence: 基于扩散模型的零参考图像修复
tldr: ZeroIDIR是一个零参考扩散模型框架，旨在解决光照退化图像恢复问题。该方法仅利用低质量图像进行训练，将恢复过程解耦为自适应光照校正和基于扩散的细节重建。通过引入自适应伽马校正模块和扰动一致性扩散模型，有效缓解了曝光偏差并抑制了噪声。实验证明，该方法在无监督设置下达到了领先水平，且具有较强的泛化能力。
selection_source: fresh_fetch
motivation: 针对光照退化图像恢复中缺乏成对训练数据以及难以平衡曝光校正与细节重建的问题。
method: 提出一种解耦框架，利用自适应伽马校正模块调整光照，并结合扰动一致性扩散模型进行细节重建与去噪。
result: 在多个基准数据集上优于现有的无监督方法，且在性能上可与有监督方法相媲美，具有更好的场景泛化性。
conclusion: ZeroIDIR通过零参考学习和扰动一致性约束，为无监督光照增强和图像恢复提供了一种稳健且高效的扩散模型方案。
---

## 摘要
在本文中，我们提出了一种名为 ZeroIDIR 的零参考扩散框架，用于光照退化图像恢复。该框架将恢复过程解耦为自适应光照校正和基于扩散的重建，且仅在低质量退化图像上进行训练。具体而言，我们设计了一个自适应伽马校正模块，执行空间变化的曝光校正，以生成仅经过光照校正的表示，从而减轻曝光偏差，并为后续扩散过程提供可靠输入。其中，引入了直方图引导的光照校正损失，以将校正后的光照分布正则化为自然场景的分布。随后，光照校正后的图像被视为所提出的扰动一致性扩散模型的中间噪声状态，用于重建细节并抑制噪声。此外，提出了一种扰动扩散一致性损失，用于约束最终恢复图像的前向扩散轨迹，使其与扰动状态保持一致，从而在缺乏监督的情况下提高恢复的保真度和稳定性。在公开基准数据集上的广泛实验表明，所提方法优于现有的无监督竞争方法，并与有监督方法相当，同时对各种场景具有更强的泛化能力。代码可在 https://github.com/JianghaiSCU/ZeroIDIR 获取。

## Abstract
In this paper, we propose a zero-reference diffusion-based framework, named ZeroIDIR, for illumination degradation image restoration, which decouples the restoration process into adaptive illumination correction and diffusion-based reconstruction while being trained solely on low-quality degraded images. Specifically, we design an adaptive gamma correction module that performs spatially varying exposure correction to generate illumination-corrected only representations to mitigate exposure bias and serve as reliable inputs for subsequent diffusion processes, where a histogram-guided illumination correction loss is introduced to regularize the corrected illumination distribution toward that of natural scenes. Subsequently, the illumination-corrected image is treated as an intermediate noisy state for the proposed perturbed consistency diffusion model to reconstruct details and suppress noise. Moreover, a perturbed diffusion consistency loss is proposed to constrain the forward diffusion trajectory of the final restored image to remain consistent with the perturbed state, thus improving restoration fidelity and stability in the absence of supervision. Extensive experiments on publicly available benchmarks show that the proposed method outperforms state-of-the-art unsupervised competitors and is comparable to supervised methods while being more generalizable to various scenes. Code is available at https://github.com/JianghaiSCU/ZeroIDIR.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《ZeroIDIR: Zero-Reference Illumination Degradation Image Restoration with Perturbed Consistency Diffusion Models》** 的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在极低光照、逆光或曝光过度等挑战性光照条件下拍摄的图像，通常存在可见度低、噪声大等退化问题。
*   **核心挑战**：
    1.  **数据依赖**：现有的监督学习方法高度依赖成对（Paired）数据集，导致在真实场景中泛化能力差。
    2.  **生成偏差**：扩散模型（Diffusion Models）虽然在细节生成上表现出色，但在处理低频信息（如曝光校正）时容易产生偏差。
    3.  **无监督困境**：现有的无监督或零样本方法往往在细节恢复上过于平滑，或在曝光控制上不稳定。
*   **研究目标**：提出一种无需成对参考图像（Zero-Reference）、能够同时处理多种光照退化任务（低光、逆光、曝光校正）的扩散模型框架。

### 2. 方法论
ZeroIDIR 将恢复过程解耦为两个阶段：**自适应光照校正**和**基于扩散的细节重建**。

*   **自适应伽马校正模块 (AGCM)**：
    *   **核心思想**：利用 Retinex 理论将图像分解为反射图（Reflectance）和光照图（Illumination）。
    *   **技术细节**：设计了两个分支分别预测空间变化的伽马图（Gamma Maps）和权重图，针对欠曝光和过曝光区域进行差异化补偿。
    *   **直方图引导损失 ($L_{hic}$)**：引入经验性的自然场景光照直方图作为先验，通过 KL 散度约束校正后的光照分布，使其符合自然光影特征。
*   **扰动一致性扩散模型 (PCDM)**：
    *   **核心思想**：将第一阶段校正后的图像视为扩散过程中的一个“中间噪声状态” ($x_{t^*}$)，而非从纯高斯噪声开始生成。
    *   **扰动一致性损失 ($L_{pdc}$)**：为了在无监督下保持恢复的保真度，该损失强制要求最终生成的清晰图像在前向扩散 $t^*$ 步后的特征，与 AGCM 输出的特征保持一致。
    *   **训练流程**：首先冻结扩散模型训练 AGCM；随后冻结 AGCM，利用 $L_{diff}$ 和 $L_{pdc}$ 优化去噪网络。

### 3. 实验设计
*   **任务场景**：涵盖低光图像增强 (LLIE)、逆光图像增强 (BIE) 和多重曝光校正 (MEC)。
*   **数据集**：
    *   **LLIE**: LOL, LSRW, MIT5K。
    *   **BIE**: BAID (成对), Backlit300 (真实世界无参考)。
    *   **MEC**: MSEC, SICE。
*   **对比方法**：
    *   **有监督**：RetinexNet, KinD++, UHDFour, Reti-Diff, CoTF, SLOT 等。
    *   **无监督/零样本**：Zero-DCE, EnlightenGAN, CLIP-LIT, LightenDiff, AGLLDiff 等。
*   **评价指标**：PSNR, SSIM, LPIPS (有参考)；NIQE, CLIPIQA (无参考)。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA A100 GPU**。
*   **训练参数**：Batch size 为 4，Patch size 为 256×256。
*   **训练时长**：第一阶段（AGCM）迭代 $1 \times 10^5$ 次；第二阶段（PCDM）迭代 $1 \times 10^6$ 次。

### 5. 实验数量与充分性
*   **实验规模**：论文在 **7 个主流基准数据集**上进行了定量测试，涵盖了从合成到真实世界的多种光照退化类型。
*   **消融实验**：针对 AGCM 的有效性（Retinex 分解、直方图损失）、PCDM 的输入策略以及一致性损失 $L_{pdc}$ 进行了详尽的对比实验。
*   **客观性**：实验不仅对比了无监督方法，还跨领域对比了最先进的有监督方法，并展示了在未见数据集（Unseen datasets）上的泛化表现，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能领先**：ZeroIDIR 在所有无监督竞争对手中达到了 SOTA 水平，且在多个指标上超越了部分有监督方法。
*   **泛化性强**：由于不依赖特定成对数据，该方法在处理不同来源、不同退化程度的图像时表现出极强的鲁棒性。
*   **解耦优势**：通过先校正曝光再重建细节的策略，有效解决了扩散模型在全局亮度控制上的不稳定性。

### 7. 优点（亮点）
*   **零参考学习**：完全摆脱了对成对数据的依赖，极大地降低了训练数据的获取成本。
*   **物理先验与生成模型结合**：将传统的 Retinex 理论、直方图先验与现代扩散模型结合，既保证了物理上的合理性，又发挥了生成模型的细节重建能力。
*   **扰动一致性约束**：提出的 $L_{pdc}$ 损失有效地解决了无监督扩散模型容易产生伪影或偏离输入内容的问题。

### 8. 不足与局限
*   **训练复杂性**：采用两阶段训练策略，且扩散模型的训练迭代次数高达 100 万次，计算开销和训练时间成本较高。
*   **推理速度**：尽管采样步数压缩到了 20 步，但相比于单次前向传播的 CNN 或 Transformer 方法，扩散模型的推理延迟仍然是实时应用中的瓶颈。
*   **先验依赖**：直方图引导损失依赖于预先收集的“自然光照分布”，如果目标场景的光照分布极其特殊（如舞台艺术灯光），该先验可能会失效。

（完）
