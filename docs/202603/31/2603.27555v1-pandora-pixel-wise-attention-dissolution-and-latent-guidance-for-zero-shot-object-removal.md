---
title: "PANDORA: Pixel-wise Attention Dissolution and Latent Guidance for Zero-Shot Object Removal"
title_zh: PANDORA：像素级注意力溶解与潜空间引导的零样本物体移除
authors: "Dinh-Khoi Vo, Van-Loc Nguyen, Tam V. Nguyen, Minh-Triet Tran, Trung-Nghia Le"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27555v1"
tags: ["query:q3"]
score: 10.0
evidence: 使用预训练扩散模型且无需微调的零样本物体移除
tldr: 针对自然图像中物体移除面临的纹理不一致和背景破坏等挑战，本文提出 PANDORA 框架。该方法直接作用于预训练扩散模型，无需微调、提示词或推理优化，通过像素级注意力溶解技术消除目标物体的注意力流，并结合局部注意力解耦引导，实现了高保真、非刚性且可扩展的多物体零样本移除。
selection_source: fresh_fetch
motivation: 现有物体移除方法通常依赖微调或复杂提示词，且在处理多物体移除时存在纹理不一致、伪影明显及前景背景解耦困难等问题。
method: 提出像素级注意力溶解技术以消除掩码区域的物体特征关联，并引入局部注意力解耦引导来驱动去噪过程向无物体的潜在流形演进。
result: 实验结果表明，PANDORA 在视觉保真度和语义合理性上均优于现有最先进方法，且具备出色的多物体同时移除能力。
conclusion: PANDORA 为零样本物体移除提供了一种无需提示词且高效的解决方案，显著增强了扩散模型在复杂图像编辑任务中的表现。
---

## 摘要
从自然图像中移除物体具有挑战性，因为在保持背景完整性的同时合成语义连贯的内容非常困难。现有方法通常依赖于微调、提示词工程或推理时优化，但仍面临纹理不一致、僵硬伪影、前景-背景解耦弱以及多物体移除扩展性差等问题。我们提出了一种名为 PANDORA 的新型零样本物体移除框架，该框架直接在预训练的文本到图像扩散模型上运行，无需微调、提示词或优化。我们提出了像素级注意力溶解（Pixel-wise Attention Dissolution），通过使掩码像素最相关的注意力键失效来移除物体，从而有效地从自注意力流中消除该物体，并允许背景上下文主导重建。我们进一步引入了局部注意力解耦引导（Localized Attentional Disentanglement Guidance），以引导去噪过程向有利于彻底移除物体的潜流形方向进行。这些组件共同实现了单次推理中精确、非刚性、无需提示词且可扩展的多物体擦除。实验表明，与现有最先进方法相比，该方法具有更优越的视觉保真度和语义合理性。项目页面见 https://vdkhoi20.github.io/PANDORA。

## Abstract
Removing objects from natural images is challenging due to difficulty of synthesizing semantically coherent content while preserving background integrity. Existing methods often rely on fine-tuning, prompt engineering, or inference-time optimization, yet still suffer from texture inconsistency, rigid artifacts, weak foreground-background disentanglement, and poor scalability for multi-object removal. We propose a novel zero-shot object removal framework, namely PANDORA, that operates directly on pre-trained text-to-image diffusion models, requiring no fine-tuning, prompts, or optimization. We propose Pixel-wise Attention Dissolution to remove object by nullifying the most correlated attention keys for masked pixels, effectively eliminating the object from self-attention flow and allowing background context to dominate reconstruction. We further introduce Localized Attentional Disentanglement Guidance to steer denoising toward latent manifolds favorable to clean object removal. Together, these components enable precise, non-rigid, prompt-free, and scalable multi-object erasure in a single pass. Experiments demonstrate superior visual fidelity and semantic plausibility compared to state-of-the-art methods. The project page is available at https://vdkhoi20.github.io/PANDORA.

---

## 论文详细总结（自动生成）

以下是对论文《PANDORA: Pixel-wise Attention Dissolution and Latent Guidance for Zero-Shot Object Removal》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不进行模型微调、不编写复杂提示词（Prompt）且不进行推理时优化的前提下，从自然图像中干净地移除特定物体，并实现背景的无缝重建。
*   **研究背景**：传统的物体移除方法（如基于补丁或 GAN）常导致纹理不一致或伪影。现有的扩散模型方法虽然提升了真实感，但往往依赖繁琐的提示词工程或昂贵的微调，且在处理**多物体移除**或**密集相似物体**时，容易出现物体残留、背景破坏或前景背景解耦不彻底的问题。

### 2. 论文提出的方法论
PANDORA 是一个**零样本（Zero-shot）**框架，直接作用于预训练扩散模型的自注意力机制和去噪轨迹。其核心包含三个协同模块：
*   **背景保留适配（BPA）**：利用 DDIM 逆转（Inversion）获取原始图像的中间潜变量，通过掩码引导的注意力机制，将原始背景的语义内容注入到生成过程中，确保非掩码区域绝对忠实于原图。
*   **像素级注意力溶解（PAD）**：这是核心创新。在自注意力层中，针对掩码区域的查询（Query），通过百分比阈值识别并屏蔽掉与其最相关的 Top-k 个键（Key）。通过将这些强相关权重设为 $-\infty$，切断了掩码像素与物体特征的联系，迫使模型利用周围的背景上下文进行填充。
*   **局部注意力解耦引导（LADG）**：在潜空间（Latent Space）引入空间门控引导机制。它在掩码内部将去噪轨迹推离原始物体的分布，而在掩码外部保持原有轨迹。这种局部引导能有效消除残余伪影，加速物体的彻底消失。

### 3. 实验设计
*   **数据集与场景**：构建了一个包含三个挑战场景的新 Benchmark：
    *   **单物体移除**（75个样本）：基础测试。
    *   **多物体移除**（17个样本）：测试扩展性。
    *   **大量相似物体移除**（94个样本）：如从一堆水果中移除部分水果，测试精细解耦能力。
    *   数据来源包括 PIE-Bench、Open Images 和 Ranjan 等数据集。
*   **对比方法**：
    *   **微调类**：LaMa、PowerPaint、SD2-Inpaint（含带提示词版本）。
    *   **零样本类**：CPAM、Attentive Eraser。
*   **评估指标**：MSE、LPIPS（背景保真度）、CLIP Score（移除彻底程度）、FID（整体真实感）以及用户偏好率（UPR）。

### 4. 资源与算力
*   **模型基础**：基于 Stable Diffusion v1.5 和 v2.1 官方权重。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或推理耗时。但由于该方法属于零样本算法，**无需任何训练阶段**，其主要开销在于 50 步的 DDIM 逆转和单次前向去噪推理。

### 5. 实验数量与充分性
*   **实验规模**：在 186 个多样化样本上进行了定量测试，并展示了大量的定性对比图。
*   **消融实验**：分别验证了 PAD 和 LADG 的必要性，结果显示缺失 PAD 会导致移除失败，缺失 LADG 会导致残留伪影。
*   **参数敏感性**：对 PAD 的百分比阈值（1% 到 25%）进行了详细分析，确定了 2%-5% 为最佳平衡点。
*   **客观性**：引入了 20 人参与的双盲用户研究（400 组响应），从人类视觉感知角度验证了方法的优越性。实验设计覆盖了不同架构（SD 1.5/2.1），具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：PANDORA 在零样本方法中达到了 SOTA 水平，CLIP Score 最高，证明其移除物体的能力最强。
*   **无需提示词的优势**：相比需要手动输入“empty scene”等提示词的方法，PANDORA 仅凭掩码即可实现更自然的重建。
*   **多物体处理能力**：PAD 模块能有效处理多个独立或聚集的物体，解决了以往方法在物体密集时容易“误伤”背景或移除不净的痛点。

### 7. 优点（亮点）
*   **完全零样本**：无需微调、无需提示词、无需推理时优化，即插即用。
*   **精细化控制**：PAD 模块在像素级别溶解注意力，比传统的全局缩放注意力权重更精准。
*   **非刚性移除**：能够处理形状复杂、非规则的物体移除，且背景重建具有高度的语义连贯性。

### 8. 不足与局限
*   **掩码依赖性**：高度依赖输入掩码的准确性。虽然对略大的掩码有容忍度，但如果掩码过小，边缘会产生明显的残留。
*   **阈值固定**：目前使用固定的百分比阈值，对于某些极端光照或复杂纹理场景，可能需要动态调整阈值以获得最佳效果。
*   **生成上限**：其效果受限于预训练扩散模型本身的先验知识，如果背景极其罕见，模型可能无法完美重建。

（完）
