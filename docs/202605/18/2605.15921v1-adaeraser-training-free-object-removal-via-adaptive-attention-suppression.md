---
title: "AdaEraser: Training-Free Object Removal via Adaptive Attention Suppression"
title_zh: AdaEraser：通过自适应注意力抑制实现无需训练的物体移除
authors: Dingming Liu
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15921v1"
tags: ["query:q3"]
score: 9.0
evidence: 无需训练的对象移除与受损区域修复
tldr: 本研究针对图像物体移除任务，提出了无需训练的 AdaEraser 框架。传统方法在移除区域盲目抑制自注意力，导致背景重建质量下降。AdaEraser 通过分析去噪过程中自注意力图的演变，引入了 Token 级别的自适应注意力抑制策略，能够根据目标物体概念的估计存在情况动态调节抑制强度。该方法在保持背景一致性的同时有效消除了指定物体，在性能上超越了现有的训练和非训练方法。
selection_source: fresh_fetch
motivation: 现有的无需训练方法在移除区域过度抑制自注意力，导致模型难以在这些区域有效重建背景内容并影响生成质量。
method: 提出一种自适应框架，通过分析去噪步长中自注意力图的变化，动态调整 Token 级别的注意力抑制强度以平衡物体消除与背景重建。
result: 实验结果显示，AdaEraser 在物体移除效果上表现卓越，其性能甚至优于许多需要专门训练的方法。
conclusion: 通过动态感知物体移除进度并自适应调节注意力机制，可以在无需额外训练的情况下实现高质量的物体移除与背景补全。
---

## 摘要
物体移除旨在从图像中消除指定物体，同时用背景内容合理地修复受影响区域。目前的无需训练方法通常在图像生成过程中阻断自注意力层对物体区域的注意力，利用周围的背景信息来恢复图像。然而，在腾空区域无差别地抑制自注意力会降低生成质量，因为模型必须在这些区域同时重建背景内容。为了解决这一冲突，我们提出了 AdaEraser，这是一个根据目标物体概念的估计存在情况动态调节注意力的自适应框架。通过分析移除前后去噪时间步中自注意力图的演变，我们开发了一种逐 token 的自适应注意力抑制策略。该方法能够在整个去噪过程中逐步感知物体移除，并自适应地调整自注意力层中的抑制强度。广泛的实验表明，AdaEraser 在物体移除方面取得了卓越的性能，甚至优于基于训练的方法。

## Abstract
Object removal aims to eliminate specified objects from images while plausibly inpainting the affected regions with background content. Current training-free methods typically block attention to object regions within self-attention layers during the image generation process, leveraging surrounding background information to restore the image. However, indiscriminate suppression of self-attention in the vacated areas can degrade generation quality, as the model must simultaneously reconstruct background content in these regions. To solve this conflict, we propose AdaEraser, an adaptive framework that dynamically modulates attention based on the estimated presence of target object concepts. Through analysis of self-attention map evolution across denoising timesteps before and during removal, we develop a token-wise adaptive attention suppression strategy. This approach enables progressive perception of object removal throughout the denoising process, with the suppression strength in self-attention layers adjusted adaptively. Extensive experiments demonstrate that AdaEraser achieves superior performance in object removal, outperforming even training-based methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaEraser: Training-Free Object Removal via Adaptive Attention Suppression》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
物体移除任务要求在消除指定物体的同时，无缝地重建背景。
*   **研究动机**：现有的“无需训练”（Training-free）方法（如 AttentiveEraser）通常在自注意力层中完全阻断对物体区域的注意力。这种“一刀切”的抑制虽然能移除物体，但会破坏背景的生成质量，导致结构扭曲或视觉伪影，因为背景重建同样依赖于自注意力机制。
*   **核心矛盾**：物体消除（需要抑制注意力）与背景重建（需要恢复注意力）之间存在冲突。
*   **整体含义**：论文提出了一种自适应框架 **AdaEraser**，旨在通过动态监测物体在去噪过程中的“残留程度”，灵活调整抑制强度，从而在彻底移除物体的同时保证背景的高质量修复。

### 2. 方法论：核心思想与关键技术
AdaEraser 的核心思想是**基于物体存在得分（Presence Score）的 Token 级自适应抑制**。

*   **双分支参考机制**：
    1.  **参考分支**：对原始图像进行 $t$ 步加噪并执行 1 步去噪，提取原始物体的自注意力图作为基准。
    2.  **目标分支**：执行实际的物体移除和去噪过程。
*   **Token 级存在得分**：通过计算目标分支与参考分支在相同位置（Token）的自注意力图之间的**余弦相似度**，来估计该位置物体的残留程度。相似度越高，表示物体特征越明显。
*   **自适应抑制策略**：
    *   计算抑制系数 $\eta(i)$：对于物体区域内的 Token，抑制强度与存在得分成正比。
    *   **修改 Softmax 权重**：在自注意力计算中，利用 $\eta(i)$ 对 Key 向量进行加权，动态调节 Query 对物体区域 Token 的关注度。
*   **流程**：在每个去噪步长中，动态计算得分并调整注意力，最后通过 Mask 将生成的物体区域与原始背景进行融合。

### 3. 实验设计
*   **数据集**：
    *   **Mulan**：包含来自 COCO 和 Laion 的图像，具有高质量的前景掩码和地面真值。
    *   **OABench**：真实场景下的物体移除基准数据集。
*   **对比方法**：对比了 7 种最先进的方法，包括基于训练的（SDXL-INP, PowerPaint, ClipAway, SmartEraser, RORem, OmniPaint）和无需训练的（AttentiveEraser）。
*   **评价指标**：
    *   图像一致性：FID、LPIPS、PSNR。
    *   移除质量：ReMOVE、CFD（背景相干性）。
    *   人类评估：平均人类排名（AHR）。

### 4. 资源与算力
*   **模型架构**：主要基于 **Stable Diffusion XL (SDXL)**，但也验证了在 SD1.5、SD2.1 和 FLUX 上的通用性。
*   **算力开销**：
    *   **推理时间**：约 15.41 秒/张（相比 AttentiveEraser 的 13.98 秒略有增加）。
    *   **显存占用**：约 9014 MiB（相比基准增加约 13%）。
*   **训练时长**：由于是 **Training-free** 方法，**无需任何训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了大规模定量测试，并进行了 30 人参与的人类主观评价。
*   **消融实验**：
    *   对比了“基于时间步”和“基于区域”的抑制策略，证明了“逐 Token”抑制的优越性。
    *   验证了不同噪声水平参考源（$x_t$ vs $x_T$）对结果的影响。
*   **充分性评价**：实验设计全面，涵盖了定量、定性、人类主观评价及跨模型验证，客观地展示了该方法在不同场景（如重叠物体、复杂背景）下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：AdaEraser 在所有核心指标上均优于现有的无需训练方法，甚至在 FID 和 PSNR 等指标上**超越了需要大量数据训练的模型**（如 RORem 和 OmniPaint）。
*   **动态演变**：实验发现自注意力图在去噪过程中能有效反映语义特征，且不同 Token 的物体消失速度不一致，这证明了 Token 级自适应调节的必要性。
*   **背景保真度**：通过减少后期去噪阶段的抑制，模型能更有效地利用预训练先验来填充合理的背景纹理。

### 7. 优点（亮点）
*   **无需训练**：直接利用预训练扩散模型的先验，部署成本极低。
*   **细粒度控制**：Token 级别的自适应机制比全局或区域级控制更精准，有效解决了背景扭曲问题。
*   **通用性强**：不依赖特定架构，可无缝集成到 SDXL、FLUX 等多种扩散模型中。
*   **即插即用**：提供了一种通过注意力图相似度来监测生成进度的创新视角。

### 8. 不足与局限
*   **掩码依赖性**：高度依赖用户提供的 Mask 质量。如果 Mask 未覆盖物体阴影或反射，移除效果会打折扣。
*   **复杂结构挑战**：在需要重建极其复杂的语义结构（如精细的人体部位或文字）时，仍可能出现结构性失真。
*   **蒸馏模型兼容性**：在 SDXL-Lightning 等极速采样（1-4步）模型上表现不佳，因为自注意力图的演变在极短步数内不够充分。
*   **计算开销**：由于需要双分支并行计算注意力图，显存和时间开销比最简单的移除方法略高。

（完）
