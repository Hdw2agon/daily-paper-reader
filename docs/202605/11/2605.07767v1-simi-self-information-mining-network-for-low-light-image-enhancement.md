---
title: "SIMI: Self-information Mining Network for Low-light Image Enhancement"
title_zh: SIMI：用于低光照图像增强的自信息挖掘网络
authors: "Xuanshuo Fu, Lei Kang, Javier Vazquez-Corral"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07767v1"
tags: ["query:q3"]
score: 8.0
evidence: 用于低光照图像增强和修复的无监督框架
tldr: 针对低光照图像增强中现有模型过于复杂且忽视图像内在信息的问题，本文提出了一种名为SIMI的无监督自信息挖掘网络。该方法通过位平面分解将低光照图像分解为多个分量，无需外部数据即可挖掘图像内在特征。SIMI不仅加速了模型收敛，还降低了计算开销，在多个标准基准测试中达到了最先进的性能，具有极强的实际应用价值。
selection_source: fresh_fetch
motivation: 现有低光照增强方法往往追求模型复杂性，却忽视了图像本身蕴含的内在信息。
method: 提出一种基于位平面分解的无监督框架SIMI，通过分解图像分量来挖掘内在信息。
result: 实验证明SIMI在标准基准测试上达到了最先进的性能，且具有更快的收敛速度和更低的计算开销。
conclusion: SIMI通过挖掘图像自信息，为无监督低光照图像增强提供了一种高效且高性能的解决方案。
---

## 摘要
不良的光照条件显著影响图像质量，为图像编辑和可视化带来了巨大挑战。许多现有的增强方法致力于提出复杂的模型，却忽略了低光照图像中包含的内在信息。在这项工作中，我们提出了自信息挖掘（SIMI）网络，这是一种创新的无监督框架，它基于位平面分解将低光照图像分解为多个分量。我们的方法允许在不依赖外部数据的情况下挖掘内在信息。这不仅加速了模型收敛，还提高了性能并减少了计算开销。我们方法的无监督特性促进了其在现实世界中的适用性。在标准基准测试上进行的实验表明，SIMI 达到了最先进的性能。

## Abstract
Poor lighting conditions significantly impact image quality, posing substantial challenges for image editing and visualization. Many existing enhancement methods aim at proposing complex models while neglecting the intrinsic information contained within low-light images. In this work, we propose the Self-Information Mining (SIMI) network, an innovative unsupervised framework that decomposes low-light images into multiple components based on bit-plane decomposition. Our approach allows mining intrinsic information without relying on external data. This not only accelerates model convergence but also improves performance and reduces computational overhead. The unsupervised nature of our method facilitates real-world applicability. Experiments conducted on standard benchmarks demonstrate that SIMI achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《SIMI: Self-information Mining Network for Low-light Image Enhancement》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：低光照图像增强（LLIE）面临亮度低、对比度差、噪声放大和色彩失真等挑战。
*   **研究动机**：
    *   **现有方法局限性**：监督学习方法依赖大量成对数据，泛化性差且计算开销大；现有的无监督方法往往追求复杂的网络结构，却忽视了低光照图像本身蕴含的**内在信息（Intrinsic Information）**。
    *   **核心目标**：提出一种轻量级、无监督的框架，通过挖掘图像自身的潜在线索（如隐藏在暗部的边缘和纹理）来实现自适应增强。

### 2. 方法论
SIMI 框架由**自信息挖掘模块**和**增强模块**两部分组成：
*   **核心思想**：利用**位平面分解（Bit-plane Decomposition）**将 8-bit 图像分解为多个二进制平面。高位平面包含粗略结构，低位平面包含精细细节和噪声。
*   **关键技术细节**：
    1.  **自信息挖掘模块**：将 RGB 输入分解为 $3 \times 8 = 24$ 个位平面图。通过平滑单元（卷积 + SiLU）处理这些平面，以减轻离散化伪影并提取潜在的边界和纹理。
    2.  **增强模块**：
        *   **注意力机制**：采用类似 CBAM 的空间-通道注意力，自适应分配权重。
        *   **DEA 块（Detail-Enhanced Attention）**：级联多个 DEA 块，递归预测光照特征（$L_1$）和结构特征（$L_2$）。
        *   **递归更新公式**：通过 Sigmoid 门控机制调制增强强度，在纠正光照的同时保持结构保真度。
    3.  **损失函数**：采用无监督组合损失，包括局部色彩一致性（$L_{lc}$）、全局色度保真度（$L_g$）、亮度保持（$L_{lu}$）和曲线平滑正则化（$L_{sj}$）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：SCIE Part I。
    *   **测试集（跨数据集评估）**：LOLV1、LSRW、SCIE Part II。
*   **Benchmark（对比方法）**：
    *   **监督学习**：Retinex-Net, KinD, KinD++。
    *   **无监督学习**：EnlightenGAN, ZeroDCE, RUAS, SCI, SRDACE。
*   **评价指标**：PSNR、SSIM、LPIPS（感知质量）、FLOPs 和参数量。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA RTX 3090 GPU**。
*   **软件/参数**：基于 PyTorch 框架，使用 AdamW 优化器。
*   **训练细节**：学习率 $10^{-5}$，权重衰减 $10^{-4}$，Batch Size 为 8。模型每 200 次迭代保存一次检查点。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流基准数据集上进行了跨数据集测试，验证了泛化能力。
    *   **消融实验**：验证了“自信息挖掘模块”的有效性，以及对比了不同分解策略（对数阈值、量化阈值）。
*   **充分性与客观性**：实验设计较为客观。作者采用了“在 A 数据集训练，在 B/C/D 数据集直接测试”的严苛设置，这比同分布测试更能体现无监督模型的鲁棒性。消融实验明确展示了该模块对收敛速度的巨大提升。

### 6. 主要结论与发现
*   **性能领先**：SIMI 在 LOLV1 和 LSRW 数据集上的 PSNR 达到了 SOTA 水平，甚至超过了部分监督学习方法（如 KinD++）。
*   **收敛极快**：加入自信息挖掘模块后，模型在 **7,400 次迭代**内即可收敛，而没有该模块时需要 **423,000 次迭代**，效率提升了约 57 倍。
*   **轻量化**：参数量仅为 **0.122M**，计算量为 **31.845 GFLOPS**，在性能和效率之间取得了极佳平衡。
*   **视觉效果**：能够有效恢复暗部细节，且不会产生过度增强、光晕或色彩偏差。

### 7. 优点（亮点）
*   **创新性**：首次将位平面分解引入无监督低光照增强，提供了一种挖掘“自信息”的新视角。
*   **高效性**：极大地缩短了训练时间，且模型轻量，适合移动端或实时应用。
*   **鲁棒性**：跨数据集表现优异，证明了位平面特征作为物理先验的通用性。

### 8. 不足与局限
*   **计算开销对比**：虽然比监督模型轻量，但与极度精简的无监督模型（如 RUAS 的 3.5G FLOPS）相比，SIMI 的计算开销（31.8G FLOPS）仍稍高。
*   **极端噪声处理**：论文主要关注光照和结构恢复，对于极端低光照下的强传感器噪声，位平面分解可能会将噪声误认为纹理，文中未深入讨论极端噪声抑制的上限。
*   **应用限制**：作为无监督方法，其性能上限仍受限于预设的损失函数组合，可能在某些极端非典型光照场景下需要调整超参数（如损失权重）。

（完）
