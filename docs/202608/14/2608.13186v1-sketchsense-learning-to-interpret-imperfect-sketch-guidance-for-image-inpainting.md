---
title: "SketchSense: Learning to Interpret Imperfect Sketch Guidance for Image Inpainting"
title_zh: SketchSense：学习解释用于图像修复的不完美草图引导
authors: Zian Yang
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13186v1"
tags: ["query:q1"]
score: 10.0
evidence: 草图引导的图像补全与修复框架
tldr: 针对草图引导图像修复中存在的草图不完美（如位移、不完整等）问题，本文提出 SketchSense 框架。该框架通过同步去噪 RGB 和结构流，并利用双向注意力融合机制耦合外观生成与结构恢复。结合短语级目标对齐语义及空间调节机制，该方法能有效解释并修正不准确的草图引导，在自然和复杂图像修复中显著提升了生成质量和结构忠实度。
selection_source: fresh_fetch
motivation: 现有的草图引导修复方法难以处理真实场景中不准确、不完整或位移的草图输入，容易导致生成结果出现结构错误。
method: 提出一种同步去噪 RGB 和结构流的双流框架，通过双向注意力融合、语义对齐目标和空间调节机制来动态解释和修正草图。
result: 在自然及复杂结构图像上的实验表明，该方法在图像恢复质量和结构保真度方面均优于现有技术。
conclusion: SketchSense 证明了通过协同演化外观与结构，可以有效解决不完美草图引导下的高质量图像修复难题。
---

## 摘要
草图引导的图像修复提供了直观的结构控制，但真实的草图往往混合了可靠的全局意图与局部拥挤、偏移、不完整或刻意非常规的笔触。现有方法通常要么在整个去噪过程中将输入草图作为固定条件保留，要么在 RGB 合成之前将其细化为清晰的结构。前者假设笔触是均匀可靠的，并可能在整个生成过程中传播局部错误；后者则必须在外观和语义上下文出现之前解决模糊的结构问题。我们提出了 SketchSense，这是一个通过同步去噪相互作用的 RGB 流和结构流来解释不完美草图引导的框架。双向注意力融合将外观生成与结构恢复耦合，产生一个细化的结构，从而揭示模型不断演变的草图解释。短语级目标对齐了两个流的语义基础。草图感知空间调节通过调制注意力和融合过程，进一步使草图的使用适应局部生成状态，而可选的有符号先验则将“保留与纠正”的意图注入特征表示和注意力行为中。在自然和结构复杂图像上的实验表明，该方法在修复质量和结构保真度方面均显著优于现有方法。

## Abstract
Sketch-guided image inpainting provides intuitive structural control, yet real sketches often mix reliable global intent with locally crowded, displaced, incomplete, or deliberately unconventional strokes. Existing approaches typically either retain the input sketch as a fixed condition throughout denoising or refine it into a clean structure before RGB synthesis. The former assumes uniformly reliable strokes and can propagate local errors throughout generation; the latter must resolve ambiguous structure before emerging appearance and semantic context become available. We propose SketchSense, a framework that interprets imperfect sketch guidance by synchronously denoising interacting RGB and structure streams. Bidirectional Attention Fusion couples appearance generation with structural recovery, producing a refined structure that exposes the model's evolving sketch interpretation. A phrase-level objective aligns the semantic grounding of the two streams. Sketch-Aware Spatial Regulation further adapts sketch use to local generation states by modulating attention and the fusion process, while an optional signed prior injects preserve-versus-correct intent into feature representations and attention behavior. Experiments on natural and structurally complex imagery show substantial gains over existing methods in both restoration quality and structural fidelity.

---

## 论文详细总结（自动生成）

以下是对论文《SketchSense: Learning to Interpret Imperfect Sketch Guidance for Image Inpainting》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：草图引导的图像修复（Inpainting）能提供直观的结构控制，但现实中的用户草图往往是“不完美”的（如笔触偏移、不完整、局部过于拥挤或比例失调）。
*   **现有挑战**：
    *   **固定条件法**：将草图视为固定约束，会导致草图中的错误直接传播到生成图像中。
    *   **先细化后合成法**：在缺乏颜色、纹理等语义上下文的情况下预先修复草图，容易产生结构歧义。
*   **核心目标**：开发一个能够动态解释、修正并利用不完美草图的框架，使生成的外观与结构在去噪过程中协同演化。

### 2. 论文提出的方法论
SketchSense 采用双流架构，同步处理 RGB 图像生成和结构（边缘）恢复。
*   **双流同步去噪**：RGB 分支负责外观修复，结构分支将不完美草图转化为细化结构。两者共享去噪步长，并在每个 Transformer 块中交互。
*   **双向注意力融合 (BAF)**：
    *   利用 LoRA 适配器修改原生 QKV 投影，实现跨分支信息交换。
    *   RGB 查询（Query）从结构分支获取当前边缘假设；边缘查询从 RGB 分支获取颜色和语义上下文。
*   **短语级语义一致性目标**：引入辅助损失函数，确保两个分支在处理相同文本短语（如“莲花”）时，其空间注意力分布保持一致，防止语义偏移。
*   **草图感知空间调节**：
    *   **内在调节**：通过“可靠性预测器”评估草图笔触的准确性，动态调整注意力权重。
    *   **显式有符号建模**：引入可选的“有符号先验”（Signed Prior），允许用户指定特定区域是“必须保留”还是“允许修正”。

### 3. 实验设计
*   **数据集**：
    *   **ArtBench**：艺术作品数据集。
    *   **COCO**：通用自然场景图像。
    *   **DhMural1714**：结构复杂的敦煌壁画数据集。
*   **评估基准 (Benchmarks)**：将测试集分为简单（Easy）、中等（Medium）、困难（Hard）三个等级。
*   **对比方法**：PDDP、MaGIC（适配版）、SketchRefiner（适配版）、OminiControl。
*   **评价指标**：PSNR、SSIM、LPIPS（感知质量）以及 Edge F1（结构准确度）。

### 4. 资源与算力
*   **硬件设备**：使用 **1 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   训练时长：约 **7 天**。
    *   Batch Size：12。
    *   优化器：Adafactor，学习率 $10^{-5}$。
    *   基础模型：FLUX.1 Fill [dev]。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖三个数据集的不同难度等级）和定性视觉对比。
*   **消融实验**：
    *   验证了跨分支融合、同步恢复、空间调节和有符号建模等核心组件的有效性。
    *   对比了“同步恢复”与“顺序恢复（先结构后 RGB）”的性能差异。
*   **充分性评价**：实验设计较为全面，不仅涵盖了自然图像，还针对结构极度复杂的壁画场景进行了专项测试。通过可视化空间门控（Spatial Gate）的激活图，客观展示了模型如何动态选择性地利用草图信息。

### 6. 主要结论与发现
*   **同步演化至关重要**：RGB 信息能显著辅助结构分支理清模糊的草图意图，而细化的结构又能反过来引导更高质量的图像合成。
*   **性能领先**：在所有难度等级下，SketchSense 的修复质量（LPIPS）和结构保真度均优于现有最先进方法。
*   **灵活性**：通过有符号先验，模型能够理解用户的局部意图（如“修正这个画错的轮廓”），而不仅仅是盲目跟随草图。

### 7. 优点
*   **处理不确定性**：创新性地解决了草图引导中“信任度”空间异质性的问题。
*   **可解释性**：模型在生成图像的同时输出“细化后的结构图”，使用户能直观看到模型对草图的理解过程。
*   **语义对齐**：通过短语级损失解决了多模态生成中常见的空间定位不一致问题。

### 8. 不足与局限
*   **计算开销**：双流架构相比单流模型增加了推理时的内存占用和计算量。
*   **分辨率限制**：目前实验主要在 512x512 分辨率下进行，更高分辨率的性能表现尚待验证。
*   **交互成本**：虽然支持有符号先验，但需要用户额外提供意图图（Region Prior），增加了交互的复杂性。

（完）
