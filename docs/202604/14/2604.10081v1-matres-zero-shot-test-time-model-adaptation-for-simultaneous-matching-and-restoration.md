---
title: "MatRes: Zero-Shot Test-Time Model Adaptation for Simultaneous Matching and Restoration"
title_zh: MatRes：用于同时匹配与修复的零样本测试时模型自适应
authors: "Kanggeon Lee, Soochahn Lee, Kyoung Mu Lee"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10081v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于图像修复的零样本测试时自适应
tldr: MatRes是一个零样本测试时自适应框架，旨在解决真实场景中图像退化与视角变化导致的匹配与修复相互干扰的问题。该方法仅需一对低质量和高质量图像，通过在对应位置强制执行条件相似性，在不进行离线训练的情况下，动态更新轻量级模块以同时提升修复质量和几何对齐精度，为多视角、多质量图像处理提供了高效解决方案。
selection_source: fresh_fetch
motivation: 传统的图像修复与几何匹配在独立处理时会相互干扰，且真实场景中往往缺乏配对的训练数据。
method: 提出一种零样本测试时自适应框架，通过冻结预训练模型并仅更新轻量级模块，利用对应位置的条件相似性约束来实现联合优化。
result: 在多种退化与视角变化的组合实验中，MatRes在修复质量和对应估计精度上均显著优于独立使用修复或匹配模型的效果。
conclusion: MatRes证明了通过测试时自适应可以有效解决匹配与修复的互干扰问题，为实际应用中多源图像的联合处理提供了新思路。
---

## 摘要
现实世界的图像对通常同时存在严重的退化和剧烈的视角变化，这使得图像修复和几何匹配在被独立处理时成为相互干扰的任务。在这项工作中，我们提出了 MatRes，这是一个零样本测试时自适应框架，仅使用单对低质量和高质量图像即可共同提升修复质量和对应关系估计。通过在对应位置强制执行条件相似性，MatRes 仅更新轻量级模块，同时保持所有预训练组件处于冻结状态，无需离线训练或额外的监督。在多种组合上的广泛实验表明，与单独使用修复或匹配模型相比，MatRes 在修复和几何对齐方面均取得了显著提升。MatRes 为现实场景提供了一种实用且广泛适用的解决方案，在这些场景中，用户通常会拍摄同一场景下具有不同视角和质量的多张图像，从而有效地解决了匹配与修复之间经常被忽视的相互干扰问题。

## Abstract
Real-world image pairs often exhibit both severe degradations and large viewpoint changes, making image restoration and geometric matching mutually interfering tasks when treated independently. In this work, we propose MatRes, a zero-shot test-time adaptation framework that jointly improves restoration quality and correspondence estimation using only a single low-quality and high-quality image pair. By enforcing conditional similarity at corresponding locations, MatRes updates only lightweight modules while keeping all pretrained components frozen, requiring no offline training or additional supervision. Extensive experiments across diverse combinations show that MatRes yields significant gains in both restoration and geometric alignment compared to using either restoration or matching models alone. MatRes offers a practical and widely applicable solution for real-world scenarios where users commonly capture multiple images of a scene with varying viewpoints and quality, effectively addressing the often-overlooked mutual interference between matching and restoration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MatRes** 的框架，旨在解决真实场景中图像匹配与修复相互干扰的问题。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：在实际拍摄中，用户常会从不同视角拍摄多张照片，其中部分图像可能存在模糊、噪声或低分辨率（LQ），而另一张可能质量较高（HQ）。
*   **核心问题**：传统的图像修复（Restoration）和几何匹配（Matching）通常被视为独立任务。然而，在现实中，严重的退化会降低匹配精度，而未对齐的视角又使得修复网络难以利用参考图像的信息。
*   **研究动机**：打破“匹配”与“修复”之间的互干扰瓶颈，利用两者之间的互补性，实现联合优化。

### 2. 方法论
*   **核心思想**：提出一种**零样本测试时自适应（Zero-Shot Test-Time Adaptation）**框架。该方法不需要离线训练，而是直接在推理阶段针对给定的一对图像进行迭代优化。
*   **关键技术细节**：
    *   **三网络架构**：包含预训练的匹配网络 $M_\theta$、预训练的修复网络 $R_\phi$ 以及一个轻量级的 **LoRA 适配器 $H_\psi$**。
    *   **互引导机制（Mutual Guidance）**：匹配网络提取的生成先验（Generative Priors）通过适配器注入修复网络，提供对齐感知的条件约束；修复后的图像反过来提升匹配的准确性。
    *   **冻结与更新**：在优化过程中，预训练的 $M_\theta$ 和 $R_\phi$ 参数保持冻结（仅允许梯度回传），只更新零初始化的轻量级适配器 $H_\psi$。
*   **算法流程与损失函数**：
    1.  从 $M_\theta$ 提取特征并估计初始变换。
    2.  将 LQ 图像变换后与 HQ 图像一同输入 $R_\phi$，并通过 $H_\psi$ 调整特征。
    3.  **损失函数**：由**对角线损失（Off-diagonal Loss）**和**像素匹配损失（Pixel-matching Loss）**组成。前者通过约束特征相似性矩阵的稀疏性来优化对齐，后者通过最小化增强图像与参考图像在重叠区域的像素差异来优化修复。

### 3. 实验设计
*   **任务场景**：涵盖了超分辨率（SR）、去噪（Denoising）和去模糊（Deblurring）三大任务。
*   **数据集**：
    *   **SR**：RealSR, DIV2K, Set5, Set14, Urban100, BSD100。
    *   **去噪**：Set5, Set14, Urban100。
    *   **去模糊**：GoPro, HIDE, RealBlur-J/R, CelebA。
*   **对比基准（Benchmark）**：
    *   **匹配模型**：RoMa, ART, DIFT, LoftUp, Mast3R 等。
    *   **修复模型**：EDSR, ESRGAN, SwinIR, HAT, Restormer, NAFNet 等。
    *   **SOTA 对比**：与特定任务的最先进方法（如 AutoLUT, MRPNet, Blur2Blur, MatchAnything 等）进行对比。

### 4. 资源与算力
*   **硬件使用**：实验使用了 **NVIDIA A100 GPU**。
*   **训练/优化时长**：该方法不需要离线训练。在测试时，针对每一对图像进行迭代优化，直到扩散损失（Diffusion Loss）不再下降为止。文中未给出单对图像的具体耗时秒数，但提到这是一种迭代优化过程。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其广泛的组合实验，将 5 种匹配模型与 10 余种修复模型在 10 多个数据集上进行了交叉验证。
*   **充分性与客观性**：
    *   实验不仅对比了“有无 MatRes”的效果，还与多种任务特定的 SOTA 方法进行了量化对比（PSNR, SSIM, mAUC）。
    *   包含了定性（视觉效果）和定量分析，证明了该框架在不同退化类型和不同模型组合下的泛化能力。实验设计逻辑严密，对比维度全面。

### 6. 主要结论与发现
*   **互补效应显著**：引入匹配引导后，修复网络的 PSNR 和 SSIM 显著提升；同时，修复后的清晰特征也大幅提高了匹配模型的 mAUC（平均曲线下面积）。
*   **零样本优势**：无需任何标注数据或离线预训练，MatRes 即可在未见过的领域和退化类型上表现出色。
*   **通用性**：该框架具有“插件式”特征，可以兼容几乎所有现有的基于网格特征的匹配和修复架构。

### 7. 优点
*   **实用性强**：解决了真实世界中“退化+视角变化”并存的痛点。
*   **无需训练数据**：摆脱了对成对训练数据（GT 清洁图像、GT 变换场）的依赖。
*   **轻量化适配**：仅更新极少数 LoRA 参数，保留了预训练大模型的原始知识。
*   **即插即用**：对现有模型架构具有高度的兼容性。

### 8. 不足与局限
*   **推理延迟**：由于需要在测试时进行迭代优化，其处理速度必然慢于直接的前向推理，可能不适用于实时性要求极高的场景。
*   **依赖预训练质量**：如果初始匹配模型在极端视角下完全无法建立对应关系，或者修复模型基础太差，MatRes 的优化上限可能会受限。
*   **计算开销**：虽然参数量小，但每对图像都要进行多次梯度更新，在处理大规模数据集时总计算成本较高。

（完）
