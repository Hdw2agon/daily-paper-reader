---
title: "BRIDGE: Background Routing and Isolated Discrete Gating for Coarse-Mask Local Editing"
title_zh: BRIDGE：用于粗掩码局部编辑的背景路由与隔离离散门控
authors: "Peilin Xiong, Honghui Yuan, Junwen Chen, Keiji Yanai"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07846v2"
tags: ["query:q1"]
score: 8.0
evidence: 粗掩码局部图像编辑与背景保留
tldr: 针对粗略掩码局部图像编辑中的掩码形状偏差问题，本文提出 BRIDGE 框架。该框架通过双路径生成（背景主路径与主体生成路径）和离散几何门控机制，实现了背景稳定与主体生成的解耦。BRIDGE 避免了在 DiT 内部注入掩码，利用可学习的路由模块动态调整位置编码，在保持背景一致性的同时赋予生成主体几何自由度，显著提升了编辑质量。
selection_source: fresh_fetch
motivation: 粗略掩码常被误当作形状先验，导致生成物体受限于掩码轮廓，无法灵活遵循指令。
method: 采用双路径生成策略并引入离散几何门控，通过 token 级位置编码路由实现背景锚定与主体自由生成的平衡。
result: 在 BRIDGE-Bench 等多个基准测试中，BRIDGE 的性能显著优于 FLUX.1-Fill 和 ACE++，且参数量仅为 13.31M。
conclusion: BRIDGE 有效解决了局部编辑中的掩码偏差问题，在保持背景稳定的同时实现了高质量、高灵活性的图像局部修改。
---

## 摘要
粗掩码局部图像编辑要求模型在保留周围场景的同时，修改用户指定的区域。然而在实践中，粗略的掩码往往会成为非预期的形状先验：掩码不仅没有起到灵活编辑支撑的作用，反而会将生成的物体拉向其偶然形成的边界。我们将这种失效现象研究为“掩码形状偏差”，并通过“双区约束”来界定该任务，即背景应保持稳定，而可编辑区域应遵循指令，而不被强制继承掩码轮廓。BRIDGE 通过将掩码置于 DiT 主干网络之外进行支撑构建和混合来解决这一设定，从而避免了 DiT 内部的掩码注入和复制的控制分支。它采用了 BridgePath 生成方式，其中主路径（Main Path）保留背景上下文，主体路径（Subject Path）从独立噪声中生成可编辑内容。受一项诊断性 Qwen-Image 实验的启发（该实验表明位置嵌入和注意力连接调节了视觉 Token 复用哪些图像上下文），BRIDGE 引入了一个可学习的离散几何门（Discrete Geometric Gate），用于 Token 级的位置嵌入路由。该门控允许主体 Token 在融合区域附近借用以背景为锚点的坐标，或者为了几何自由度而保留以主体为中心的坐标。我们在 BRIDGE-Bench、MagicBrush 和 ICE-Bench 上评估了 BRIDGE。在 BRIDGE-Bench 上，BRIDGE 将 Local SigLIP2-T 指标从 FLUX.1-Fill 的 0.262 和 ACE++ 的 0.390 提升至 0.503，并在局部 DINO 和 DreamSim 指标上取得了同步提升。在 MagicBrush 和 ICE-Bench 上的零样本结果进一步表明，在精选基准测试之外，该方法具有竞争力的对齐性和源图像保留能力，同时与 ControlNet 风格的复制分支相比，新增的路由模块参数量仅为 13.31M，保持了紧凑性。

## Abstract
Coarse-mask local image editing asks a model to modify a user-indicated region while preserving the surrounding scene. In practice, however, rough masks often become unintended shape priors: instead of serving as flexible edit support, the mask can pull the generated object toward its accidental boundary. We study this failure as mask-shape bias and frame the task through a Two-Zone Constraint, where the background should remain stable while the editable region should follow the instruction without being forced to inherit the mask contour. BRIDGE addresses this setting by keeping masks outside the DiT backbone for support construction and blending, avoiding DiT-internal mask injection and copied control branches. It uses BridgePath generation, where a Main Path preserves background context and a Subject Path generates editable content from independent noise. Motivated by a diagnostic Qwen-Image experiment showing that positional embeddings and attention connectivity regulate which image context visual tokens reuse, BRIDGE introduces a learnable Discrete Geometric Gate for token-level positional-embedding routing. This gate lets subject tokens borrow background-anchored coordinates near fusion regions or keep subject-centric coordinates for geometric freedom. We evaluate BRIDGE on BRIDGE-Bench, MagicBrush, and ICE-Bench. On BRIDGE-Bench, BRIDGE improves Local SigLIP2-T from 0.262 with FLUX.1-Fill and 0.390 with ACE++ to 0.503, with parallel gains in local DINO and DreamSim. Zero-shot results on MagicBrush and ICE-Bench further indicate competitive alignment and source preservation beyond the curated benchmark, while the added routing module remains compact at 13.31M parameters compared with ControlNet-style copied branches.

---

## 论文详细总结（自动生成）

### 论文总结：BRIDGE —— 粗掩码局部编辑的背景路由与隔离离散门控

#### 1. 核心问题与整体含义（研究动机和背景）
在局部图像编辑中，用户通常只提供粗略的掩码（如涂鸦或方框）来指示编辑区域。然而，现有的扩散模型（如 FLUX.1-Fill, ACE++）往往会将这些粗略掩码误认为是目标物体的精确轮廓，导致生成的物体形状僵硬、不自然，这种现象被称为**“掩码形状偏差”（mask-shape bias）**。论文的核心动机是解决“双区约束”下的矛盾：既要严格保留背景区域，又要让编辑区域内的物体能够摆脱粗略掩码的形状束缚，生成符合指令且几何形状自然的物体。

#### 2. 方法论：核心思想与关键技术
BRIDGE 的核心思想是将**掩码定位**与**几何生成**解耦，通过在 DiT（Diffusion Transformer）主干网络之外处理掩码，并引入动态路由机制。
*   **BridgePath 架构**：采用双路径生成策略。
    *   **主路径（Main Path）**：负责保留原始图像的背景上下文。
    *   **主体路径（Subject Path）**：从独立噪声开始，在掩码对应的边界框内生成新的编辑内容。
*   **离散几何门控（Discrete Geometric Gate）**：这是论文的关键创新。
    *   在 DiT 的每一层中，模型学习一个 Token 级的二元路由变量。
    *   **路由逻辑**：主体路径的 Token 可以选择使用“背景锚定坐标”（用于边缘融合，保持一致性）或“主体中心坐标”（用于几何自由度，摆脱掩码形状）。
    *   **公式化表达**：通过直通估计器（Straight-Through Estimator）实现离散门控的端到端训练，动态切换位置编码（PE）。
*   **训练策略**：使用 LoRA（秩为 512）对预训练的 Qwen-Image 进行微调，同时训练轻量级的 GateBlocks（仅 13.31M 参数），避免了类似 ControlNet 那种庞大的分支复制。

#### 3. 实验设计
*   **数据集**：
    *   **BRIDGE-Bench**：论文自建的基准测试，包含 1,444 对经过 VLM 筛选、SAM 掩码生成和 DreamSim 质量审计的高质量编辑对。
    *   **MagicBrush**：用于测试零样本泛化能力。
    *   **ICE-Bench**：用于评估源图像保留和指令遵循的综合性能。
*   **对比方法**：包括 FLUX.1-Fill、ACE++、ACE、UltraEdit、Q-Control 等当前主流的局部编辑和指令引导模型。
*   **评价指标**：采用局部与全局的 SigLIP2-T（文本对齐）、DINO（特征相似度）和 DreamSim（感知相似度）。

#### 4. 资源与算力
*   **硬件**：使用了 **5 台 NVIDIA RTX PRO 6000 Blackwell (96GB)** GPU。
*   **训练细节**：进行了 30,000 个微步（micro-steps）的训练，约合 1,875 次优化器更新，在训练集上运行了约 4.5 个 Epoch。
*   **参数量**：LoRA 适配器约 4.7B 参数（推理时融合），新增的 GateBlocks 仅为 **13.31M** 参数。

#### 5. 实验数量与充分性
*   **实验规模**：训练集包含 42,425 个样本，测试集 1,444 个。
*   **消融实验**：针对“主体路径”和“离散门控”进行了专门的消融研究，证明了动态 PE 路由对防止生成结果坍缩和提升局部精度的必要性。
*   **客观性**：在多个第三方公开数据集（MagicBrush, ICE-Bench）上进行了零样本评估，并提供了详细的超参数网格搜索（CFG 与混合比例 Alpha）分析，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **解决偏差**：BRIDGE 有效消除了掩码形状偏差，生成的物体能够自然地超出或改变粗略掩码的边界。
*   **性能提升**：在 BRIDGE-Bench 上，局部文本对齐指标（Local SigLIP2-T）从 0.390（ACE++）提升至 **0.503**。
*   **背景保留**：在实现大幅度几何改变的同时，通过主路径的锚定作用，背景保留能力（SRC 指标）依然保持在极高水平（0.942）。

#### 7. 优点
*   **架构优雅**：通过位置编码路由而非增加复杂的视觉条件分支来解决形状控制问题，设计巧妙且轻量。
*   **控制灵活**：提供了用户可调的融合边界（Bounding Box vs. Mask-based Blending），在结构自由度和严格边界遵循之间提供了平衡。
*   **数据质量高**：提出了一套自动化的数据清洗和复合流水线，解决了局部编辑训练数据中常见的背景漂移问题。

#### 8. 不足与局限
*   **推理开销**：由于采用了双路径（BridgePath）设计，推理时的 Token 数量增加，导致延迟和显存占用比单路径模型高出约 **30%-40%**。
*   **经验性设计**：虽然实验效果显著，但离散注意力路由的理论收敛性仍属于经验性探索。
*   **复杂掩码处理**：对于极度不规则的掩码，虽然可以生成自然物体，但在最终的边缘融合阶段仍可能面临“自由度”与“边界严丝合缝”之间的权衡。

（完）
