---
title: "VisionNVS: Self-Supervised Inpainting for Novel View Synthesis under the Virtual-Shift Paradigm"
title_zh: VisionNVS：虚拟偏移范式下的自监督修复新视角合成
authors: "Hongbo Lu, Liang Yao, Chenghao He, Fan Liu, Wenlong Liao, Tao He, Pai Peng"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17382v1"
tags: ["query:q7"]
score: 10.0
evidence: 用于新视角合成的自监督修复任务
tldr: VisionNVS是一个针对自动驾驶场景的纯相机新视角合成框架。针对训练时缺乏新轨迹真值图像的问题，该研究提出了“虚拟偏移”范式，利用单目深度代理模拟遮挡模式，将视角合成转化为自监督修复任务。通过伪3D接缝合成策略增强空间一致性，该方法在无需LiDAR的情况下，实现了超越现有基准的几何保真度和视觉质量，为可扩展的驾驶仿真提供了稳健方案。
selection_source: fresh_fetch
motivation: 自动驾驶新视角合成面临训练时缺乏新轨迹真值图像的监督缺口问题。
method: 提出“虚拟偏移”策略将视角合成转化为自监督修复任务，并结合伪3D接缝合成策略提升多相机空间一致性。
result: 实验表明，VisionNVS在几何保真度和视觉质量上均优于依赖LiDAR的基准模型。
conclusion: 该框架通过自监督修复范式有效消除了领域差距，为大规模自动驾驶仿真提供了高效的纯视觉解决方案。
---

## 摘要
自动驾驶中新视角合成（NVS）的一个根本瓶颈是新轨迹上固有的监督差距：模型在推理阶段被要求合成未见过的视角，但在训练阶段缺乏这些偏移位姿的真值图像。在本文中，我们提出了 VisionNVS，这是一个仅使用摄像头的框架，它从根本上将视角合成从一个病态的外推问题重新表述为一个自监督的修复任务。通过引入“虚拟偏移”（Virtual-Shift）策略，我们利用单目深度代理来模拟遮挡模式，并将其映射到原始视图上。这种范式转变允许使用原始录制的图像作为像素级的完美监督，有效地消除了以往方法中固有的领域差距。此外，我们通过伪 3D 接缝合成（Pseudo-3D Seam Synthesis）策略解决了空间一致性问题，该策略在训练期间整合了相邻摄像头的视觉数据，以显式建模现实世界的光度差异和校准误差。实验表明，与依赖 LiDAR 的基准模型相比，VisionNVS 实现了更优的几何保真度和视觉质量，为可扩展的驾驶模拟提供了一个鲁棒的解决方案。

## Abstract
A fundamental bottleneck in Novel View Synthesis (NVS) for autonomous driving is the inherent supervision gap on novel trajectories: models are tasked with synthesizing unseen views during inference, yet lack ground truth images for these shifted poses during training. In this paper, we propose VisionNVS, a camera-only framework that fundamentally reformulates view synthesis from an ill-posed extrapolation problem into a self-supervised inpainting task. By introducing a ``Virtual-Shift'' strategy, we use monocular depth proxies to simulate occlusion patterns and map them onto the original view. This paradigm shift allows the use of raw, recorded images as pixel-perfect supervision, effectively eliminating the domain gap inherent in previous approaches. Furthermore, we address spatial consistency through a Pseudo-3D Seam Synthesis strategy, which integrates visual data from adjacent cameras during training to explicitly model real-world photometric discrepancies and calibration errors. Experiments demonstrate that VisionNVS achieves superior geometric fidelity and visual quality compared to LiDAR-dependent baselines, offering a robust solution for scalable driving simulation.

---

## 论文详细总结（自动生成）

以下是对论文《VisionNVS: Self-Supervised Inpainting for Novel View Synthesis under the Virtual-Shift Paradigm》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在自动驾驶领域，新视角合成（NVS）对于闭环仿真至关重要。然而，现有方法面临一个根本性的**监督缺口**：在训练阶段，模型被要求合成新轨迹下的视角，但由于车辆实际行驶路径固定，缺乏这些偏移位姿下的真值（Ground Truth）图像。
*   **现有方案缺陷**：以往方法（如 FreeVS）通常利用 LiDAR 投影生成“伪图像”作为训练目标，但这会引入严重的投影伪影（如黑洞、边缘噪声）。模型在训练中被迫拟合这些有缺陷的目标，导致推理时保真度受限。
*   **研究目标**：提出一种纯相机（Camera-only）框架，将视角合成从不适定（ill-posed）的外推问题转变为良适的**自监督修复（Inpainting）任务**，从而利用原始录制图像作为完美监督。

### 2. 论文提出的方法论
VisionNVS 的核心思想是：如果模型学会了如何修复由视角偏移产生的遮挡区域，它就能在推理时重建新视角下的未知区域。
*   **虚拟偏移策略（Virtual-Shift Strategy）**：
    1.  利用单目深度估计将原始图像提升为 3D 点云。
    2.  模拟一个虚拟位姿偏移，计算在该偏移下哪些像素会因遮挡或超出视场而丢失，生成**遮挡掩码（Mask）**。
    3.  将该掩码应用回**原始图像**。这样，训练目标就是原始的、无损的录制图像，而输入则是带掩码的图像。
*   **伪 3D 接缝合成（Pseudo-3D Seam Synthesis）**：
    为了处理大偏移下的“盲区”，利用相邻摄像头的图像填充掩码区域。即使相邻视图存在光度差异或校准误差，模型也会被强制学习如何修正这些不一致性，从而增强鲁棒性。
*   **生成式修复网络**：
    *   采用 **DiT（Diffusion Transformer）** 架构。
    *   使用 **Flow Matching（流匹配）** 目标进行稳定训练。
    *   集成 **Wan2.2 的流式 VAE**，实现高效的潜在空间编码。

### 3. 实验设计
*   **数据集**：使用大规模自动驾驶数据集 **nuScenes**（1000 个场景，包含 360° 环视相机数据）。
*   **评测指标**：FID（图像质量）和 FVD（视频一致性）。
*   **对比基准（Benchmarks）**：
    *   重建类方法：PVG, EmerNeRF, StreetGaussian, OmniRe。
    *   生成类方法：FreeVS, DiST-4D（当前 SOTA，依赖 LiDAR）。
*   **实验场景**：分别在横向偏移 $\pm 1m, \pm 2m, \pm 4m$ 的情况下进行测试。

### 4. 资源与算力
*   **硬件环境**：使用了由 **32 块 NVIDIA L20 GPU** 组成的集群（4 个节点 $\times$ 8 块 GPU）。
*   **训练细节**：
    *   训练时长：300 个 Epoch。
    *   全局 Batch Size：256（梯度累积步数为 8）。
    *   数据格式：处理后的 49 帧视频序列。
    *   精度：使用 BF16 混合精度训练。

### 5. 实验数量与充分性
*   **实验规模**：论文在 nuScenes 验证集上进行了广泛的定量对比，涵盖了从近场到远场的多种偏移距离。
*   **消融实验**：针对“虚拟偏移策略”和“伪 3D 接缝合成”分别做了消融，验证了各模块对 FID/FVD 提升的贡献。
*   **效率分析**：对比了推理延迟和显存占用，证明了其流式推理范式的优势。
*   **充分性评价**：实验设计较为全面，不仅对比了视觉质量，还通过视频序列展示了时间一致性，对比对象包含了目前最强的依赖 LiDAR 的模型，体现了实验的客观性和挑战性。

### 6. 论文的主要结论与发现
*   **性能超越**：尽管 VisionNVS 仅使用相机数据，但在大偏移（$\pm 4m$）情况下，其 FVD 指标优于依赖高精度 LiDAR 的 DiST-4D。
*   **范式优势**：将 NVS 转化为修复任务能有效消除训练与监督信号之间的领域差距（Domain Gap）。
*   **鲁棒性**：通过在输入中引入“不完美”的相邻视图拼接，模型学会了自动修正光度差异和几何对齐误差。
*   **高效推理**：采用流式 VAE 使得显存占用不随视频长度增加，推理速度比同类方法快约 3 倍。

### 7. 优点（亮点）
*   **摆脱 LiDAR 依赖**：降低了高保真仿真对昂贵传感器的需求，提高了方案的可扩展性。
*   **完美的监督信号**：巧妙地通过掩码反转，使得原始图像直接作为 GT，解决了 NVS 训练中长期存在的“伪图像”干扰问题。
*   **时间一致性强**：生成的视频在长序列中表现出极高的几何稳定性和物体持久性，减少了闪烁和扭曲。

### 8. 不足与局限
*   **单目深度依赖**：虽然模型对深度噪声有鲁棒性，但初始的掩码生成仍依赖单目深度估计的质量，在极端长距离或极暗场景下可能受限。
*   **上下文限制**：伪 3D 接缝合成依赖于相邻相机的重叠视场，如果车辆传感器布局重叠度极低，该策略的效果可能会打折扣。
*   **动态物体处理**：论文主要强调了轨迹偏移带来的背景一致性，对于复杂交通流中动态物体的交互生成细节描述相对较少。

（完）
