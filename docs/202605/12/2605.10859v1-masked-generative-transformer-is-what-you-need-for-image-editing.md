---
title: Masked Generative Transformer Is What You Need for Image Editing
title_zh: 掩码生成式 Transformer 是图像编辑之所需
authors: "Wei Chow, Linfeng Li, Xian Sun, Lingdong Kong, Zefeng Li, Qi Xu, Hang Song, Tian Ye, Xian Wang, Jinbin Bai, Shilin Xu, Xiangtai Li, Junting Pan, Shaoteng Liu, Ran Zhou, Tianshu Yang, Songhua Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10859v1"
tags: ["query:q4"]
score: 8.0
evidence: 用于局部图像编辑的掩码生成式 Transformer
tldr: 针对扩散模型在图像编辑中因全局去噪导致非编辑区域被误改的问题，本文提出首个基于掩码生成式Transformer（MGT）的编辑框架EditMGT。通过利用MGT的局部Token预测特性，结合多层注意力整合与区域保持采样技术，实现了精确的局部编辑。此外，研究团队构建了包含200万高分辨率样本的CrispEdit-2M数据集。实验表明，EditMGT在保持极高图像相似度的同时，推理速度比扩散模型快6倍。
selection_source: fresh_fetch
motivation: 传统扩散模型的全局去噪机制容易导致编辑操作蔓延至非目标区域，造成背景或无关细节的意外改变。
method: 提出EditMGT框架，利用MGT的局部预测范式，并通过多层注意力整合生成定位信号以及区域保持采样来锁定非编辑区域。
result: 在多个基准测试中达到SOTA级别的图像相似度，且仅需960M参数即可实现比扩散模型快6倍的编辑速度。
conclusion: 本研究证明了掩码生成式Transformer在图像编辑领域是扩散模型强有力的替代方案，展现出更高的精确度和推理效率。
---

## 摘要
扩散模型在图像编辑领域占据主导地位，但其全局去噪机制使编辑区域与周围上下文纠缠在一起，导致修改传播到本应保持不变的区域。我们提出了一种根本不同的方法，利用掩码生成式 Transformer (MGT)，其局部标记预测范式自然地将更改限制在目标区域内。我们提出了 EditMGT，这是首个基于 MGT 的编辑框架。我们的方法采用多层注意力整合，将交叉注意力图聚合为精确的编辑定位信号，并使用区域保持采样来显式防止非目标区域的标记翻转。为了支持训练，我们构建了 CrispEdit-2M，这是一个包含 200 万个样本、跨越七个类别的高分辨率（>1024）编辑数据集。仅凭 9.6 亿个参数，EditMGT 在多个基准测试中实现了最先进的图像相似度，同时编辑速度提高了 6 倍，证明了 MGT 是基于扩散的编辑方法的一个极具吸引力的替代方案。

## Abstract
Diffusion models dominate image editing, yet their global denoising mechanism entangles edited regions with surrounding context, causing modifications to propagate into areas that should remain intact. We propose a fundamentally different approach by leveraging Masked Generative Transformers (MGTs), whose localized token-prediction paradigm naturally confines changes to intended regions. We present EditMGT, an MGT-based editing framework that is the first of its kind. Our approach employs multi-layer attention consolidation to aggregate cross-attention maps into precise edit localization signals, and region-hold sampling to explicitly prevent token flipping in non-target areas. To support training, we construct CrispEdit-2M, a 2M-sample high-resolution (>1024) editing dataset spanning seven categories. With only 960M parameters, EditMGT achieves state-of-the-art image similarity on multiple benchmarks while delivering 6x faster editing, demonstrating that MGTs offer a compelling alternative to diffusion-based editing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Masked Generative Transformer Is What You Need for Image Editing》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的扩散模型（Diffusion Models, DMs）在图像编辑中存在“编辑泄漏”（Edit Leakage）问题。由于扩散模型采用全局去噪机制，在修改特定区域（如给狗戴帽子）时，往往会无意中改变非目标区域的细节（如皮毛颜色、背景光照或环境布局）。
*   **研究背景**：现有的解决方法（如大规模训练、预定义掩码或反演技术）虽有缓解，但无法从架构层面根本解决全局去噪带来的干扰。本文提出利用**掩码生成式 Transformer (MGT)** 的局部 Token 预测特性，为图像编辑提供一种天然的区域隔离机制。

### 2. 方法论
EditMGT 框架的核心思想是将图像编辑转化为局部 Token 的预测与替换过程：
*   **注意力注入（Attention Injection）**：将原始图像作为条件 Token 引入，与生成 Token 共享参数和空间位置编码。通过引入偏置矩阵 $E$ 来调节原始图像对生成过程的影响权重，无需增加额外参数即可将预训练的文生图模型转变为编辑模型。
*   **多层注意力整合（Multi-Layer Attention Consolidation）**：
    *   利用模型内部的交叉注意力图（Cross-Attention Maps）来定位编辑区域。
    *   由于单层激活图不够清晰，该方法聚合了特定层（第28-36块）的注意力图，并进行自适应滤波处理，生成精确的编辑定位信号。
*   **区域保持采样（Region-Hold Sampling）**：在迭代解码过程中，根据定位评分判断 Token 是否属于非目标区域。若评分低于阈值 $\lambda$，则强制将该 Token 恢复为原始图像的值，从而在架构上保证非编辑区域绝对不变。
*   **数据集构建**：发布了 **CrispEdit-2M**，包含 200 万个高分辨率（≥1024）编辑样本，涵盖 7 大类别，并经过严格的质量过滤。

### 3. 实验设计
*   **数据集与基准**：在 Emu Edit、MagicBrush、AnyBench 和 GEdit-EN-full 等主流基准测试上进行了评估。
*   **对比方法**：对比了包括 InstructPix2Pix、MagicBrush、UltraEdit、Emu Edit、AnyEdit 等专用模型，以及 OmniGen、GoT、OminiGen2、VAREdit 等通用多模态大模型。
*   **评估指标**：采用 CLIP-im（图像相似度）、CLIP-out（指令遵循度）、L1 距离和 DINO 分数。

### 4. 资源与算力
*   **模型规模**：参数量为 9.6 亿（0.96B），文本编码器使用 Gemma2-2B。
*   **训练过程**：分为三个阶段：基础模型适配（5k 步）、全量微调（50k 步）和人类偏好对齐精炼（1k 步）。
*   **推理效率**：在 1024×1024 分辨率下，推理仅需约 **2 秒**，显存占用 13.8 GB。
*   **算力说明**：文中未明确提及具体的 GPU 型号和总训练时长（如 A100/H100 的机时），但强调了其推理速度比同类模型快 6 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个权威数据集上进行了定量分析，并提供了大量的定性对比图（涵盖物体转换、场景替换、材质替换等）。
*   **消融实验**：虽然简短，但提及了对注意力整合层选择的实证研究。
*   **充分性评价**：实验覆盖了从 1B 到 12B 不同规模的模型对比，结果显示 0.96B 的 EditMGT 在图像相似度上超越了许多参数量大其 10 倍的模型，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **MGT 的优越性**：证明了 MGT 的局部解码范式比扩散模型的全局去噪更适合精确的图像编辑。
*   **性能突破**：EditMGT 在 MagicBrush 等数据集上达到了 SOTA（最先进）水平，尤其在保持原始图像结构方面表现卓越。
*   **效率优势**：以极小的参数量实现了极快的推理速度，打破了“高质量编辑必须依赖超大模型或长时间扩散采样”的固有印象。

### 7. 优点
*   **架构创新**：首次将 MGT 应用于指令驱动的图像编辑，解决了扩散模型的固有缺陷。
*   **零新增参数**：通过注意力注入实现功能转换，保持了模型的轻量化。
*   **高精度定位**：无需用户手动绘制掩码，通过内部注意力整合即可实现自动、精准的局部控制。
*   **高质量数据贡献**：开源的 CrispEdit-2M 为社区提供了宝贵的高分辨率训练资源。

### 8. 不足与局限
*   **依赖预训练底座**：模型的上限受限于底层 MGT（如 Meissonic）的生成能力。
*   **阈值敏感性**：区域保持采样中的阈值 $\lambda$ 可能需要针对不同类型的编辑任务进行微调，以平衡“修改幅度”与“内容保持”。
*   **极端复杂场景**：对于需要大规模重构全局语义的编辑任务，局部保持机制可能会过于保守。

（完）
