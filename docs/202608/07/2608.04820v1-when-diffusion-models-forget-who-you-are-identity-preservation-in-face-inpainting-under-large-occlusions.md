---
title: "When Diffusion Models Forget Who You Are: Identity Preservation in Face Inpainting under Large Occlusions"
title_zh: 当扩散模型忘记你是谁：大面积遮挡下人脸修复中的身份保持
authors: "Feng Ding, Shuhuai Xie, Yue Zhou, Yulan Zhang, Guopu Zhu, Mengyao Xiao"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04820v1"
tags: ["query:q1"]
score: 10.0
evidence: 基于扩散模型的大面积遮挡人脸修复
tldr: 针对扩散模型在人脸修复中面临的大面积遮挡和身份信息丢失问题，本文提出了ReSem-Face框架。该框架采用级联扩散结构并引入显式的身份条件语义先验，通过从多张参考图中提取身份特征来重建缺失的语义区域，进而引导扩散过程。实验证明，该方法在严重遮挡下能有效保持身份一致性，并显著提升了文本驱动编辑的质量与可靠性。
selection_source: fresh_fetch
motivation: 扩散模型在处理大面积遮挡和冲突文本引导的人脸修复时，难以维持原有的身份特征。
method: 提出一种名为ReSem-Face的级联扩散框架，利用多参考图像的身份特征重建语义先验并引导多流条件扩散。
result: 在CelebAHQ-IDI-5和VGGFace2数据集上，该方法在严重遮挡下的身份保持能力和文本编辑质量均优于现有基准。
conclusion: 通过引入显式的身份语义约束，ReSem-Face有效解决了人脸修复中的身份遗忘问题，增强了生成结果的保真度。
---

## 摘要
基于扩散模型的人脸修复近期在视觉质量上取得了令人印象深刻的成果，但在大面积遮挡和冲突的文本引导下，保持身份保真度仍然是一个重大挑战。为了解决这一问题，我们提出了 ReSem-Face（Reference Semantic Inpainting for Face），这是一种级联扩散框架，为多参考人脸修复引入了显式的身份条件语义先验。我们的方法从多个参考图像中提取具有代表性的身份特征，以重建缺失的语义区域，随后通过多流条件架构引导扩散过程。该设计在像素缺失时提供了强大的语义约束，并在保持与提示词驱动编辑兼容的同时，稳定了身份重建。在 CelebAHQ-IDI-5 和 VGGFace2 上的实验表明，与代表性基准模型相比，ReSem-Face 在严重语义遮挡下能实现更可靠的身份保持补全，并提升了文本控制编辑的质量。

## 速览
**TLDR**：针对扩散模型在人脸修复中面临的大面积遮挡和文本冲突导致的身份丢失问题，本文提出 ReSem-Face 框架。该方法通过多参考图像提取身份特征，构建显式的身份语义先验，并利用多流调节架构引导扩散过程。实验证明，该方法在严重遮挡下能更可靠地保持身份一致性，并提升了文本驱动编辑的质量。 \
**Motivation**：现有的扩散模型在处理大面积遮挡或与文本引导冲突时，难以维持人脸身份的一致性。 \
**Method**：提出 ReSem-Face 框架，利用多参考图像提取身份特征并重建缺失语义区域，通过多流调节架构约束扩散过程。 \
**Result**：在 CelebAHQ-IDI-5 和 VGGFace2 数据集上的实验表明，该方法在严重遮挡下具有更强的身份保持能力和更好的文本编辑效果。 \
**Conclusion**：通过引入身份调节的语义先验，ReSem-Face 有效解决了人脸修复中的身份遗忘问题，实现了高质量的身份保持与灵活编辑。

---

## Abstract
Face inpainting with diffusion models has recently achieved impressive visual quality, yet preserving identity fidelity under significant occlusion and conflicting text guidance remains a major challenge. To address this issue, we present Reference Semantic Inpainting for Face (ReSem-Face), a cascaded diffusion framework that introduces an explicit identity-conditioned semantic prior for multi-reference face inpainting. Our approach distills representative identity features from multiple references to reconstruct missing semantic regions, which then guide the diffusion process through a multi-stream conditioning architecture. This design provides strong semantic constraints when pixels are absent and stabilizes identity reconstruction while remaining compatible with prompt-driven edits. Experiments on CelebAHQ-IDI-5 and VGGFace2 demonstrate that ReSem-Face yields more reliable identity-preserving completion under severe semantic masks and improves text-controlled editing quality compared with representative baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《When Diffusion Models Forget Who You Are: Identity Preservation in Face Inpainting under Large Occlusions》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然扩散模型在图像修复上表现出色，但在人脸修复任务中，当遮挡面积过大或文本引导与身份特征冲突时，模型容易丢失特定人物的身份特征（即“身份漂移”）。
*   **核心问题**：如何在极端遮挡条件下，既能根据文本指令进行编辑，又能精准保持参考图像中的人物身份保真度。
*   **整体含义**：本文提出了 **ReSem-Face** 框架，通过引入显式的“身份条件语义先验”，将身份信息的提取与扩散去噪过程解耦，从而在复杂修复场景下稳定身份重建。

### 2. 方法论
ReSem-Face 采用级联扩散架构，核心包含三个关键组件：
*   **多参考身份语义聚合（Identity Semantic Bank）**：利用预训练的身份编码器（如基于 ArcFace 的 ResNet-50）从多张参考图中提取特征，通过多头注意力机制消除单张图像的变异，形成统一的身份语义库。
*   **身份感知语义预修复模块（Semantic Pre-Inpainting）**：在进入扩散模型之前，先预测缺失区域的语义 Token。该模块包含几何头（预测形状结构）和纹理头（预测外观特征），并使用知识蒸馏技术从教师模型中学习高层语义。
*   **三流条件扩散架构（Tri-Conditioned Diffusion）**：在扩散 U-Net 的每个 Transformer 块中，并行集成三个分支：
    1.  **文本交叉注意力**：处理提示词引导。
    2.  **参考身份注意力 (RIA)**：直接从参考图中检索身份线索。
    3.  **参考语义注意力 (ReSemAttn)**：注入预修复模块生成的语义先验。
*   **损失函数**：结合了标准扩散损失、身份一致性损失（ID Loss）以及针对几何与纹理的语义损失。

### 3. 实验设计
*   **数据集**：
    *   **CelebAHQ-IDI-5**：专门用于多参考人脸修复的基准，包含 1,963 个身份。
    *   **VGGFace2**：用于评估模型在“野外”非对齐场景下的泛化能力，包含超过 330 万张图像。
*   **对比方法（Baselines）**：对比了 8 种代表性方法，包括通用扩散修复（LDI）、个性化生成（Custom Diffusion, Textual Inversion）以及最新的参考引导修复模型（PVA, OmniGen, HiFi-Inpaint 等）。
*   **评价指标**：身份相似度（CosFace ID Score）、视觉质量（FID, KID, PSNR, SSIM, LPIPS）以及文本控制力（CLIP-Score, ImageReward, 属性准确率）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA RTX A40 GPU**。
*   **训练时长**：
    *   第一阶段（语义预修复器）：20K 次迭代。
    *   第二阶段（扩散主干微调）：200K 次迭代。
*   **推理开销**：在推理阶段，针对每个目标身份需要进行约 **40 步** 的轻量化个性化微调（耗时约 1 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比、定性视觉展示、跨数据集验证（从 CelebA 到 VGGFace2）以及消融实验。
*   **用户研究**：邀请了 10 位具有不同专业背景的评估者，对 1000 张测试图像的身份保真度、文本对齐度和视觉真实感进行打分。
*   **充分性评价**：实验设计较为全面，涵盖了从随机遮挡到特定语义遮挡（如下半脸、眼部）的多种场景，消融实验验证了语义先验注入的必要性，整体论证逻辑严密、客观。

### 6. 主要结论与发现
*   **身份保持能力**：ReSem-Face 在 CelebAHQ-IDI-5 上达到了 **0.766** 的 ID 分数，显著高于所有基准模型。
*   **语义先验的重要性**：显式的语义预测比隐式的身份注入更能抵抗大面积遮挡带来的干扰。
*   **权衡关系**：实验发现增加引导尺度（Guidance Scale）虽能提升文本对齐度，但会损害身份相似度；论文确定了 $s=4.0$ 是最佳平衡点。
*   **多参考优势**：随着参考图像数量从 1 增加到 6，身份保持的稳定性持续提升。

### 7. 优点
*   **解耦设计**：将几何结构与纹理细节分开预测，有效避免了“语义模糊”现象。
*   **强约束力**：通过预修复模块在去噪开始前就提供了高层先验，使模型在“盲猜”缺失区域时有据可依。
*   **编辑性与保真度的平衡**：在保持身份的同时，对文本指令（如“戴墨镜”、“变老”）的响应更加自然。

### 8. 不足与局限
*   **推理延迟**：虽然微调仅需 40 步，但对于实时应用或大规模在线服务来说，每个身份都需要额外微调仍存在时间成本。
*   **跨域挑战**：在 VGGFace2 等非对齐、低质量图像数据集上，虽然表现优于基准，但 ID 分数较对齐数据集有明显下降，说明模型对原始图像质量仍有一定依赖。
*   **算力需求**：级联架构和多流注意力机制增加了训练和推理时的显存占用。

（完）
