---
title: Semi-Supervised Flow Matching for Mosaiced and Panchromatic Fusion Imaging
title_zh: 用于马赛克与全色融合成像的半监督流匹配
authors: "Peiming Luo, Nan Wang, Litong Liu, Jiahan Huang, Chenxu Wu, Renwei Dian, Junming Hou"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20128v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于图像融合与修复的半监督流匹配
tldr: 本文针对单次曝光高光谱成像中低分辨率马赛克图像与高分辨率全色图像融合的病态问题，提出了一种半监督流匹配框架。该方法结合了无监督先验预训练与条件流匹配生成模型，通过两阶段训练和随机投票机制实现鲁棒融合，并在推理阶段采用无冲突梯度引导确保时空一致性。实验证明该方法在多个数据集上显著优于现有方法，且具有良好的任务扩展性。
selection_source: fresh_fetch
motivation: 旨在解决单次曝光高光谱成像中，低分辨率马赛克图像与高分辨率全色图像融合过程中的严重病态性及现有扩散模型效率与泛化性不足的问题。
method: 提出一种两阶段半监督流匹配框架，利用无监督先验生成初始图像，并结合条件流匹配与随机投票机制进行迭代细化，推理时辅以梯度引导。
result: 在多个基准数据集上的实验结果表明，该方法在定量指标和定性视觉效果上均显著优于现有的代表性基准模型。
conclusion: 该研究为图像融合提供了一个高效且通用的生成式框架，能够灵活扩展至其他图像恢复任务并与无监督算法集成。
---

## 摘要
将低分辨率（LR）马赛克高光谱图像（HSI）与高分辨率（HR）全色（PAN）图像融合，为通过单次曝光实现视频级高分辨率高光谱成像提供了一条极具前景的途径，但其严重的病态性（ill-posed nature）仍是一个重大挑战。在这项工作中，我们提出了一种用于马赛克与全色图像融合的新型半监督流匹配框架。与以往受限于特定协议或手工假设的基于扩散的方法不同，我们的方法将无监督方案与流匹配无缝集成，从而构建了一个具有泛化性且高效的生成式框架。具体而言，我们的方法遵循两阶段训练流程。首先，我们预训练一个无监督先验网络以生成初始的伪高分辨率高光谱图像。在此基础上，我们训练一个条件流匹配模型来生成目标高分辨率高光谱图像，并引入了一种随机投票机制来迭代优化初始的高分辨率高光谱图像估计，从而实现鲁棒且有效的融合。在推理过程中，我们采用了一种无冲突梯度引导策略，以确保高分辨率高光谱图像重建在光谱和空间上的一致性。在多个基准数据集上的实验表明，与代表性基准方法相比，我们的方法在定量和定性性能上均取得了显著提升。除了马赛克与全色融合外，我们的方法还提供了一个灵活的生成式框架，可以轻松扩展到其他图像融合任务，并与无监督或盲图像恢复算法相结合。

## Abstract
Fusing a low resolution (LR) mosaiced hyperspectral image (HSI) with a high resolution (HR) panchromatic (PAN) image offers a promising avenue for video-rate HR-HSI imaging via single-shot acquisition, yet its severely ill-posed nature remains a significant challenge. In this work, we propose a novel semi-supervised flow matching framework for mosaiced and PAN image fusion. Unlike previous diffusion-based approaches constrained by specific protocols or handcrafted assumptions, our method seamlessly integrates an unsupervised scheme with flow matching, resulting in a generalizable and efficient generative framework. Specifically, our method follows a two-stage training pipeline. First, we pretrain an unsupervised prior network to produce an initial pseudo HR-HSI. Building on this, we then train a conditional flow matching model to generate the target HR-HSI, introducing a random voting mechanism that iteratively refines the initial HR-HSI estimate, enabling robust and effective fusion. During inference, we employ a conflict-free gradient guidance strategy that ensures spectrally and spatially consistent HR-HSI reconstruction. Experiments on multiple benchmark datasets demonstrate that our method achieves superior quantitative and qualitative performance by a significant margin compared to representative baselines. Beyond mosaiced and PAN fusion, our approach provides a flexible generative framework that can be readily extended to other image fusion tasks and integrated with unsupervised or blind image restoration algorithms.

---

## 论文详细总结（自动生成）

以下是对论文《Semi-Supervised Flow Matching for Mosaiced and Panchromatic Fusion Imaging》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效地将单次曝光获取的低分辨率（LR）马赛克高光谱图像与高分辨率（HR）全色（PAN）图像融合，以重建高质量的 HR-HSI。
*   **研究动机**：
    *   **现有技术局限**：传统的扩散模型在处理此类任务时，要么依赖于监督学习（需要难以获取的真值，且存在领域偏差），要么依赖于无监督学习（基于低秩分解等强假设，在复杂现实场景下鲁棒性差）。
    *   **病态性挑战**：马赛克图像在空间和光谱上都存在严重的欠采样，融合过程极具挑战性。
    *   **效率需求**：扩散模型推理速度慢，且缺乏针对马赛克数据的专用生成框架。

### 2. 方法论
论文提出了一种**两阶段半监督流匹配（Flow Matching）框架**：
*   **第一阶段：无监督先验预训练**
    *   利用等变图像（Equivariant Imaging, EI）框架预训练一个轻量级先验网络，通过空间-光谱观测一致性损失和等变损失，生成初步的“伪 HR-HSI”。
*   **第二阶段：条件流匹配模型训练**
    *   **核心思想**：学习 HR-HSI 与通道扩展后的 PAN 图像之间的残差分布。
    *   **随机投票机制（RVM）**：这是本文的关键创新。在训练过程中，利用不同阶段的模型检查点对预测结果进行“投票”，根据空间-光谱一致性准则动态更新训练目标（伪标签），从而避免模型陷入早期错误预测的局部最优。
    *   **条件流匹配（CFM）**：相比传统扩散模型，流匹配通过学习向量场实现更直观、高效的概率路径映射。
*   **推理阶段：无冲突梯度引导（ConFIG）**
    *   在采样过程中，引入空间和光谱双重梯度引导。为了解决两个目标梯度可能存在的方向冲突（导致震荡或偏差），采用正交化策略统一梯度信号，确保重建结果在空间纹理和光谱保真度上达到平衡。

### 3. 实验设计
*   **数据集**：
    *   **仿真数据集**：CAVE（32幅图像，16个波段）、Chikusei（航空高光谱数据，16个波段）。
    *   **真实数据集**：Real-world dataset（60对户外采集的马赛克与PAN图像对）。
*   **对比方法（Benchmark）**：
    *   **两阶段流水线**：组合了3种去马赛克算法（PPID, LSAN, PMNet）和3种全色锐化算法（PanGAN, VBPN, WFANet），共9种组合。
    *   **单阶段方法**：EFN（无监督等变融合网络）。
    *   **扩散模型基准**：Diff-Pan（监督）、ARGS-Diff（无监督）。
*   **评价指标**：PSNR, SSIM, SAM, ERGAS（有参考）；$D_\lambda, D_S, QNR$（无参考）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练细节**：Adam优化器，Batch size为16。流匹配模型学习率为 $10^{-4}$，PAN退化网络学习率为 $10^{-5}$。
*   **推理效率**：采样步数仅需 **10步**，在保持高质量的同时显著优于传统扩散模型的推理速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到真实场景的全面测试，并在两个主流仿真数据集上进行了定量对比。
*   **消融实验**：针对“随机投票机制”和“梯度引导强度”进行了专门的消融研究，验证了各组件的有效性。
*   **客观性**：对比了多种不同范式（传统、深度学习、生成式）的方法，实验设计较为公平，且在真实数据集上使用了针对原始数据特性调整后的无参考指标。

### 6. 主要结论与发现
*   **性能领先**：在所有数据集上，该方法在 PSNR 和 SAM 等核心指标上均显著优于现有最先进方法（如 EFN）。
*   **RVM 的有效性**：实验证明，动态更新伪标签比固定伪标签能显著提升模型最终的重建精度。
*   **高效性**：流匹配框架结合 ConFIG 引导，使得模型在极少的采样步数下即可生成具有丰富纹理和准确光谱的图像。

### 7. 优点
*   **半监督范式创新**：通过 RVM 解决了无监督学习中伪标签质量低的问题，无需真值即可在真实数据上训练。
*   **数学严谨性**：引入无冲突梯度引导（ConFIG），从偏微分方程（PDE）角度解决了多目标优化的梯度冲突问题。
*   **通用性强**：框架不依赖于特定的退化协议，可灵活扩展至其他盲图像恢复或融合任务。

### 8. 不足与局限
*   **对初始先验的依赖**：虽然有 RVM 优化，但第一阶段预训练的先验网络质量仍会影响收敛速度和最终上限。
*   **计算开销**：尽管推理步数减少，但作为生成式模型，其训练成本和显存占用仍高于轻量级的端到端 CNN 方法。
*   **噪声敏感度**：在真实场景中，若传感器噪声极大，梯度引导可能会放大噪声，需进一步研究更鲁棒的去噪集成策略。

（完）
