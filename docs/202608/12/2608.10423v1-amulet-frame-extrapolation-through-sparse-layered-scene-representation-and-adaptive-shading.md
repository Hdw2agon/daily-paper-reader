---
title: "Amulet: Frame Extrapolation Through Sparse Layered Scene Representation and Adaptive Shading"
title_zh: Amulet：基于稀疏分层场景表示与自适应着色的帧外推技术
authors: "Sebastian Künzel, Fabian Schmierer, Sergej Geringer, Guido Reina, Daniel Weiskopf, Dieter Schmalstieg"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10423v1"
tags: ["query:q1"]
score: 8.0
evidence: 帧外推中新遮挡区域的修复
tldr: Amulet 是一种用于高频帧外推的渲染方法，通过将场景转换为稀疏、分块且分层的中间表示（缓存）来提高性能。与传统的重投影技术不同，它显式存储潜在可见几何体，从而在不产生幻觉的情况下准确处理遮挡区域。该方法利用预测性调度器和自适应着色更新，实现了渲染率与显示刷新率的解耦，在 4K 分辨率下可达 250 Hz，在多项指标上优于或媲美 DLSS 等现有技术。
selection_source: fresh_fetch
motivation: 旨在解决传统帧外推方法在处理遮挡区域时的伪影问题，并克服高分辨率下渲染率与显示刷新率不匹配的挑战。
method: 采用稀疏分层图像空间缓存表示，结合预测性梯度调度器进行自适应着色更新，通过分层遍历合成新视图。
result: 在 4K 分辨率下实现了高达 250 Hz 的帧率，能将 60 Hz 的着色率外推至 240 Hz，且在质量上优于现有的神经网络流方法。
conclusion: Amulet 证明了非神经的分层图像空间表示在多帧外推中的有效性，为高性能实时渲染提供了一种高效且准确的方案。
---

## 摘要
我们介绍了 Amulet，这是一种将场景转换为稀疏、分块且分层的中间场景表示（缓存）的渲染方法，用于高频帧外推。与基于重投影的技术不同，Amulet 在其分层图像空间缓存中显式地光栅化并存储潜在可见的几何体，从而能够对新露出的区域进行准确的着色和修复，而不会产生幻觉。我们的核心贡献是一个能够预测性地填充未来视图着色信息的缓存，该过程在多个当前帧中进行摊销。通过从前到后分层遍历缓存，并实时细化陈旧或缺失的着色，来合成新视图。利用基于梯度的预测调度器为每个分块分配生命周期，我们实现了在运动和动态光照下的自适应着色更新。Amulet 将光栅化和着色速率与显示器的刷新率解耦。在许多场景中，我们的缓存可以使用单个着色帧，仅通过少量局部更新即可合成多个外推帧。在典型应用中，我们将 60 Hz 的着色速率外推至 240 Hz 的显示输出。Amulet 在 4K 分辨率下可达到 250 Hz，在多项指标上与包括 DLSS 和神经流方法在内的最先进帧生成方法相比具有竞争力。Amulet 探索了稀疏分层图像空间表示的设计空间。它实现了准确的、非神经的多帧外推，并能显式处理遮挡关系。我们的研究结果表明，Amulet 能够比当代方法外推更多的高质量帧，在许多场景中其质量可与受延迟限制的帧插值方法相媲美。

## Abstract
We introduce Amulet, a rendering method that transforms a scene into a sparse, tiled and layered intermediate scene representation (cache) for high-frequency frame extrapolation. In contrast to reprojection-based techniques, Amulet explicitly rasterizes and stores potentially visible geometry in its layered image-space cache, allowing accurate shading and inpainting of newly disoccluded regions without hallucination. Our key contribution is a cache that is predictively filled with shading information for future views, amortized over multiple current frames. Novel views are synthesized by hierarchically traversing the cache front to back and refining stale or missing shading on the fly. Using a predictive, gradient-based scheduler that assigns lifetimes for each tile, we enable adaptive shading updates under motion and dynamic lighting. Amulet decouples the rasterization and shading rate from the refresh rate of the display. In many scenarios, our cache can use a single shaded frame to synthesize multiple extrapolated frames with only a few localized updates. In a typical application, we extrapolate a 60 Hz shading rate to a 240 Hz display. Amulet achieves up to 250 Hz at 4K resolution and is competitive with state-of-the-art frame generation methods, including DLSS and neural-flow approaches, in multiple metrics. Amulet explores the design space of sparse layered image-space representation. It enables accurate, non-neural multi frame extrapolation with explicit handling of disocclusions. Our findings show that Amulet can extrapolate many more frames than contemporary methods with high quality, rivaling latency-bound frame interpolation methods with similar quality in many scenes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Amulet: Frame Extrapolation Through Sparse Layered Scene Representation and Adaptive Shading》的深度结构化总结：

### 1. 核心问题与整体含义
在高刷新率显示器（如 240Hz+）普及的背景下，实时渲染（尤其是 4K 分辨率）面临巨大的计算压力。传统的**帧外推（Frame Extrapolation）**技术旨在通过当前帧预测未来帧以降低延迟，但面临两大挑战：
*   **遮挡处理（Disocclusions）：** 当相机移动时，被遮挡的物体会显露出来，传统重投影方法因缺乏这些区域的数据而产生空洞或伪影。
*   **幻觉与延迟：** 深度学习方法（如 DLSS 3）虽然能修复空洞，但往往会产生视觉幻觉，且帧插值技术会引入额外的帧延迟。

**Amulet** 的核心意义在于提出了一种**非神经（Non-neural）**的、基于物理几何的分层表示方法，通过显式存储潜在可见的几何体，实现了高质量、低延迟的多帧外推。

### 2. 方法论：核心思想与技术细节
Amulet 的核心是一个**稀疏分层图像空间缓存（Sparse Layered Image-Space Cache）**，其工作流程如下：

*   **分层场景表示（Layered Representation）：** 
    不同于传统的单层深度图，Amulet 使用“深度剥离（Depth Peeling）”技术，在每个像素位置存储多个几何层（通常为 2-4 层）。这使得当顶层物体移动时，底层被遮挡的颜色信息已经存在于缓存中，从而直接解决遮挡修复问题。
*   **分块与稀疏化（Tiling & Sparsity）：** 
    将屏幕划分为 8x8 或 16x16 的分块（Tiles）。只有当预测到分块内容发生显著变化时，才会触发着色更新，从而实现稀疏计算。
*   **预测性梯度调度器（Predictive Gradient-based Scheduler）：** 
    这是本文的关键创新。系统根据物体的运动矢量和光照变化梯度，预测每个分块在未来几帧内的“生命周期”。调度器会提前在后台为未来的视图填充着色信息，将计算压力平摊（Amortize）到多个渲染帧中。
*   **自适应着色与合成：** 
    在合成新帧时，系统从前到后遍历缓存层。如果某处着色信息陈旧或缺失，则进行实时细化。这种机制实现了着色率（如 60Hz）与显示刷新率（如 240Hz）的完全解耦。

### 3. 实验设计
*   **测试场景：** 使用了多个复杂的 3D 场景，包括 *Bistro*（城市场景）、*San Miguel*（植被丰富）、*Sponza*（经典室内）以及具有动态光照的自定义场景。
*   **Benchmark（基准）：** 主要任务是将 60 Hz 的基础渲染率外推至 240 Hz（即 1:4 外推）。
*   **对比方法：** 
    *   **DLSS 3 (Frame Generation)：** 工业界最先进的帧生成技术。
    *   **ExtraSS：** 基于神经流（Neural Flow）的最新外推研究。
    *   **基础重投影（Reprojection）：** 传统的空间扭曲方法。
*   **评估指标：** PSNR、SSIM、LPIPS（感知质量）以及 FLIP（误差可视化）。

### 4. 资源与算力
*   **硬件平台：** 实验主要在 **NVIDIA GeForce RTX 4090** GPU 上进行。
*   **性能表现：** 在 4K 分辨率下，Amulet 的合成开销极低，整体系统可达到 **250 Hz** 的输出频率。
*   **算力特点：** 该方法不依赖于昂贵的张量核心（Tensor Cores）进行神经网络推理，而是利用标准的光栅化和计算着色器管线。

### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了多种运动模式（平移、旋转、快速缩放）和动态光照条件。
*   **消融实验：** 作者详细测试了不同层数（1-4层）、不同分块大小、以及有无预测调度器对质量和性能的影响。
*   **客观性：** 实验不仅对比了数值指标，还通过误差热图展示了 Amulet 在处理边缘和遮挡区域时的优越性。整体实验设计较为严谨，对比对象涵盖了目前学术界和工业界的顶尖方案。

### 6. 主要结论与发现
*   **外推能力强：** Amulet 能够稳定地进行 1:4 甚至更高倍率的帧外推，而不会像神经网络方法那样随时间推移产生严重的模糊或形变。
*   **显式遮挡修复：** 通过分层缓存，Amulet 几乎完美解决了新露区域的着色问题，无需“幻觉”填充。
*   **性能优势：** 在高分辨率下，其非神经架构的运行速度优于许多复杂的深度学习模型，且延迟更低。

### 7. 优点
*   **无幻觉（Hallucination-free）：** 基于真实几何数据，避免了 AI 生成内容中常见的扭曲和闪烁。
*   **低延迟：** 作为外推方法，它不需要像插值方法那样等待下一帧渲染完成，非常适合电竞等对响应速度要求极高的场景。
*   **自适应性：** 调度器能智能分配算力，在静止区域节省开销，在剧烈运动区域集中更新。

### 8. 不足与局限
*   **内存开销：** 存储多层分块缓存需要额外的显存（虽然通过稀疏化有所缓解）。
*   **几何依赖：** 该方法高度依赖准确的深度和运动矢量，对于透明物体或复杂的折射/反射效果处理能力有限。
*   **极端动态场景：** 如果场景中存在大量随机高速运动的小物体，调度器可能无法准确预测所有分块的生命周期，导致局部出现陈旧感（Staleness）。

（完）
