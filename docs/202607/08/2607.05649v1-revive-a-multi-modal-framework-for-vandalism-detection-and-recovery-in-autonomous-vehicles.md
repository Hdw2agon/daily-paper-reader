---
title: "REVIVE: A Multi-Modal Framework for Vandalism Detection and Recovery in Autonomous Vehicles"
title_zh: REVIVE：一种用于自动驾驶车辆破坏检测与恢复的多模态框架
authors: "Abdullah Tariq Choudhry, Tapadhir Das"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05649v1"
tags: ["query:q1"]
score: 9.0
evidence: 使用 Stable Diffusion 修复进行图像恢复
tldr: 针对自动驾驶车辆面临的故意遮挡攻击（VOA），本文提出REVIVE多模态框架。该框架集成了检测、分类、分割和修复功能，利用BLIP引导的Stable Diffusion、直接像素替换和自适应中值滤波等技术，实现了对受损图像的针对性修复。研究显著提升了下游目标检测任务的召回率，为自动驾驶系统在物理破坏环境下的鲁棒性提供了结构化解决方案。
selection_source: fresh_fetch
motivation: 自动驾驶车辆易受物理遮挡攻击影响感知能力，而现有研究缺乏对受损图像实用性的有效恢复手段。
method: 提出REVIVE框架，通过EfficientNet-U-Net进行分割，并根据遮挡类型路由至Stable Diffusion修复、像素替换或中值滤波。
result: 实验显示，直接像素替换可将YOLOv8l的检测召回率从0.588提升至0.967，且质量门控机制确保了修复后的图像质量不低于原始受损状态。
conclusion: REVIVE框架为自动驾驶系统应对故意遮挡攻击提供了一套从检测到恢复的完整流程，增强了系统的安全性和可靠性。
---

## 摘要
自动驾驶车辆（AVs）面临着日益严重的破坏诱导遮挡攻击（VOAs）威胁，这些攻击会损害基于摄像头的感知能力。虽然检测框架可以识别被破坏的图像，但在物理遮挡后恢复摄像头流的效用仍未得到充分探索。本文提出了“视觉卓越破坏图像恢复与增强”（REVIVE）框架，这是一个集成了以下功能的破坏恢复流水线：(1) 二进制 VOA 检测，(2) 多类别 VOA 模式识别，(3) 基于 EfficientNet 的 U-Net 分割，以及 (4) 使用引导式语言-图像预训练（BLIP）引导的 Stable Diffusion 修复、直接像素替换或自适应中值滤波的类型感知恢复。Stable Diffusion 在不同 VOA 模式下表现出不同的重建性能（每种模式的 SSIM 为 0.667-0.867，PSNR 为 15.4-26.7dB），而对齐的直接像素替换在对齐参考条件下实现了近乎一致的重建。在 500 对跟踪的清洁/破坏图像对上，未恢复的 VOA 将 YOLOv8l 目标检测召回率降低至 0.588，而在对齐参考条件下，直接像素替换将召回率恢复至 0.967，F1 分数恢复至 0.970。LaMa、Telea 和 Navier-Stokes 基准模型提高了图像相似度，但在下游检测恢复方面效果有限；Stable Diffusion 被视为受质量门控约束的异步恢复分支，而非阻塞式实时感知步骤。我们评估了一个参考可用的质量门控，用于在下游使用前过滤恢复的候选图像：如果没有它，类型感知路由会将每张图像的召回率降低至 0.304，而有了它，召回率回升至 0.608，达到或高于未恢复的基准水平，从而确保转发的视频流绝不会比未恢复的帧更差。因此，REVIVE 为自动驾驶车辆中的 VOA 提供了一个结构化的恢复框架。

## Abstract
Autonomous vehicles (AVs) face increasing threats from vandalism-induced occlusion attacks (VOAs) that compromise camera-based perception. While detection frameworks can identify vandalized images, restoring camera-stream utility after physical occlusion remains underexplored. This paper presents present the Recovery and Enhancement of Vandalized Images for Vision Excellence (REVIVE) framework, a vandalism recovery pipeline integrating: (1) binary VOA detection, (2) multi-class VOA pattern identification, (3) EfficientNet-based U-Net segmentation, and (4) type-aware recovery using Bootstrapping Language-Image Pre-training (BLIP)-guided Stable Diffusion inpainting, direct pixel replacement, or adaptive median filtering. Stable Diffusion shows variable reconstruction performance (per-pattern SSIM 0.667-0.867, PSNR 15.4-26.7dB) across VOA patterns, while aligned direct pixel replacement achieves near-identical reconstruction under the aligned-reference condition. On 500 tracked clean/vandalized image pairs, unrecovered VOAs reduce YOLOv8l object-detection recall to 0.588, while direct pixel replacement restores recall to 0.967 and F1-score to 0.970 under that aligned-reference condition. LaMa, Telea, and Navier-Stokes baselines improve image similarity but provide more limited downstream detection recovery, and Stable Diffusion is treated as an asynchronous recovery branch subject to a quality gate rather than a blocking real-time perception step. We evaluate a reference-available quality gate that filters recovered candidates before downstream use: without it, type-aware routing degrades per-image recall to 0.304, whereas with it, recall returns to 0.608, at or above the unrecovered baseline, ensuring the forwarded stream is never worse than the unrecovered frame. REVIVE therefore, provides a structured recovery framework from VOAs in AVs.

---

## 论文详细总结（自动生成）

这是一份关于论文《REVIVE: A Multi-Modal Framework for Vandalism Detection and Recovery in Autonomous Vehicles》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动驾驶车辆（AV）高度依赖摄像头感知，但极易受到**破坏诱导遮挡攻击（VOA）**，如喷漆、贴纸、泥浆或碎片遮挡。现有的防御研究多集中在检测攻击，而忽略了如何**恢复**受损图像的实用性以维持下游感知任务（如目标检测）。
*   **研究背景**：随着 Waymo 等无人驾驶出租车在城市中遭遇破坏事件增加，物理层面的传感器完整性保护变得至关重要。本文旨在提出一个端到端的框架，不仅能识别破坏，还能根据破坏类型选择最优策略进行图像修复。

### 2. 方法论：核心思想与技术细节
REVIVE 框架采用四阶段分层策略：
1.  **二元破坏检测**：使用轻量级 CNN 快速判断图像是否被破坏，确保清洁图像直接跳过后续流程以节省算力。
2.  **多类别 VOA 模式识别**：将破坏分为五类（随机、中心向外、顶到底、底到顶、针对性），用于路由后续的修复策略。
3.  **局部化分割**：采用基于 **EfficientNet 的 U-Net** 架构，结合 VOA 类型作为条件输入，生成精确的像素级遮挡掩码。
4.  **类型感知恢复（核心创新）**：
    *   **统计中值滤波**：针对散布的“随机”噪声类遮挡。
    *   **BLIP 引导的 Stable Diffusion 重建**：针对大面积结构化遮挡，利用 BLIP 生成场景描述词引导生成式 AI 进行语义填充。
    *   **直接像素替换**：在拥有近期对齐参考帧的条件下，作为性能上限的基准方法。
5.  **质量门控（Quality Gate）**：在输出给下游感知模块前，对比修复前后图像的结构相似度（SSIM）和目标检测一致性，若修复效果不佳则触发“低置信度”警告，防止生成式 AI 的“幻觉”误导决策。

### 3. 实验设计
*   **数据集**：主要使用 **BDD100K** 驾驶数据集，提取 5000 张图像并人工合成五种 VOA 模式。此外，使用 **Raindrops-on-Windshield** 数据集进行真实遮挡几何形状的压力测试。
*   **下游任务**：使用 **YOLOv8l** 进行目标检测，评估修复图像对感知能力的实际恢复程度。
*   **对比基准（Benchmarks）**：
    *   传统修复：Telea、Navier-Stokes。
    *   深度学习修复：LaMa（基于傅里叶卷积）。
    *   生成式修复：Stable Diffusion。
    *   参考基准：直接像素替换（Pixel Replacement）。

### 4. 资源与算力
*   **硬件平台**：论文明确提到所有延迟分析均在 **Apple Silicon (MPS/PyTorch)** 环境下进行。
*   **算力细节**：未明确说明具体的 GPU 型号（如 A100 等）或总训练时长，但提供了详细的推理延迟数据：
    *   REVIVE 预处理（检测+分割+门控）：约 **22.7ms**，符合 30 FPS 的实时要求。
    *   Stable Diffusion 分支：约 **8610ms**（8.6秒），因此被设计为异步处理分支。

### 5. 实验数量与充分性
*   **实验规模**：使用了 5000 张训练/验证图像，以及 500 对经过严格跟踪的“清洁-破坏”图像对进行下游检测评估。
*   **充分性**：实验涵盖了从像素级指标（SSIM, PSNR）到任务级指标（mAP50, Recall, F1）的全面评估。
*   **客观性**：作者诚实地指出“直接像素替换”是一个理想化的上限（假设参考帧完美对齐），并进行了**对齐敏感度实验**（平移 2-40 像素），证明了该方法在车辆移动时的局限性，体现了实验的客观性。

### 6. 主要结论与发现
*   **感知恢复效果**：未修复的遮挡使 YOLOv8l 的召回率降至 **0.588**；通过直接像素替换可恢复至 **0.967**；LaMa 修复可恢复至 **0.667**。
*   **生成式 AI 的双刃剑**：Stable Diffusion 虽然能生成视觉上“好看”的图像，但容易产生幻觉，导致目标检测召回率反而下降（0.339），证明了**质量门控**的必要性。
*   **实时性**：REVIVE 的在线路径（不含生成式分支）增加的延迟极低（22.7ms），适合集成进自动驾驶感知流水线。
*   **门控机制的价值**：引入质量门控后，系统确保了转发给感知的图像流质量永远不会低于未修复的原始受损帧。

### 7. 优点与亮点
*   **类型感知路由**：不使用单一算法处理所有遮挡，而是根据遮挡特征（如噪声 vs 结构化块）匹配最合适的修复手段。
*   **感知导向的评估**：不仅关注图像修复得“像不像”，更关注下游目标检测器能否“看清”物体。
*   **实用的质量门控**：解决了生成式 AI 在安全敏感领域（如自动驾驶）中因幻觉带来的风险问题。

### 8. 不足与局限
*   **合成数据偏差**：实验主要基于合成的遮挡掩码，虽然使用了雨滴数据集增强真实感，但与真实的喷漆、半透明污渍等复杂物理纹理仍有差距。
*   **参考帧依赖**：性能最好的“像素替换”高度依赖于摄像头静止或极低速移动时的参考帧对齐，在高速行驶中难以应用。
*   **质量门控的实现**：目前的门控评估依赖于清洁参考帧（Reference-available），在实际部署中如何实现完全“无参考”的实时质量评估仍是挑战。

（完）
