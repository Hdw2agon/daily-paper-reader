---
title: "Continuous Expert Assembly: Instance-Conditioned Low-Rank Residuals for All-in-One Image Restoration"
title_zh: 连续专家组装：用于全能图像修复的实例条件低秩残差
authors: "Haisen He, Xiangyu Zou, SongLin Dong, Heng Li, Yihong Gong, Zhiheng Ma"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06127v1"
tags: ["query:q3"]
score: 8.0
evidence: 无需测试时退化标签的一体化图像修复
tldr: 针对真实世界中复杂多变的图像退化问题，本文提出了一种名为连续专家组装（CEA）的动态参数化框架。该方法通过轻量级的交叉注意力超适配器，为每个空间标记动态生成实例条件的低秩路由基准和残差方向，实现了无需外部提示或静态专家库的逐标记密集路由。在多个全能图像修复基准测试中，CEA在处理空间非均匀和复合退化方面表现优异，同时保持了高效的参数和计算性能。
selection_source: fresh_fetch
motivation: 现有的全能图像修复方法依赖全局提示或静态专家路由，难以应对空间非均匀且复杂的未知退化模式。
method: 提出连续专家组装（CEA）框架，利用交叉注意力超适配器为每个标记动态合成低秩残差更新，实现细粒度的实例感知修复。
result: 在AIO-3、AIO-5和CDD-11等多个数据集上，CEA在修复质量上超越了基于提示和专家的主流基准，尤其在处理复合退化时优势明显。
conclusion: CEA通过逐标记的动态参数组装，为全能图像修复提供了一种高效且透明的解决方案，显著提升了模型对复杂退化的适应能力。
---

## 摘要
现实世界的图像退化通常是未知的、空间非均匀的且具有组合性的，这要求全能修复模型在没有测试时退化标签的情况下，使单组权重适应多样的局部损坏模式。现有方法通常使用全局提示或退化描述符来调制共享主干网络，或者通过预定义的专家池路由特征。然而，紧凑的全局条件可能会限制局部退化证据的表达，而静态专家路由可能会产生同质化的更新，或依赖于不稳定的稀疏分配。我们提出了连续专家组装（CEA），这是一种用于全能图像修复的逐 token 动态参数化框架。CEA 采用轻量级的交叉注意力超适配器来探测中间空间特征，并合成实例条件的低秩路由基底和残差方向。每个空间 token 随后通过对生成的秩分量进行密集有符号点积亲和力计算，来组装其自身的残差更新，从而避免了外部提示、静态专家库和离散的 Top-k 选择。由此产生的组装规则还支持线性注意力视角，使其密集的逐 token 路由行为变得透明。在 AIO-3、AIO-5 和 CDD-11 上的实验表明，CEA 在平均修复质量上优于基于提示、描述符和专家的强基准模型，在空间变化和组合退化方面提升最为显著，同时保持了良好的参数量、FLOPs 和运行效率。

## Abstract
Real-world image degradation is often unknown, spatially non-uniform, and compositional, requiring all-in-one restoration models to adapt a single set of weights to diverse local corruption patterns without test-time degradation labels. Existing methods typically modulate a shared backbone with global prompts or degradation descriptors, or route features through predefined expert pools. However, compact global conditioning can bottleneck localized degradation evidence, while static expert routing may produce homogeneous updates or rely on unstable sparse assignments. We propose \textbf{Continuous Expert Assembly} (CEA), a token-wise dynamic parameterization framework for all-in-one image restoration. CEA employs a lightweight \textbf{Cross-Attention Hyper-Adapter} to probe intermediate spatial features and synthesize instance-conditioned low-rank routing bases and residual directions. Each spatial token then assembles its own residual update via dense signed dot-product affinities over the generated rank-wise components, avoiding external prompts, static expert banks, and discrete Top-   selection. The resulting assembly rule also admits a linear-attention perspective, making its dense token-wise routing behavior transparent. Experiments on AIO-3, AIO-5, and CDD-11 show that CEA improves average restoration quality over strong prompt-, descriptor-, and expert-based baselines, with the clearest gains on spatially varying and compositional degradations, while maintaining favorable parameter, FLOP, and runtime efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：Continuous Expert Assembly (CEA)

#### 1. 核心问题与整体含义
论文针对的是**全能图像修复（All-in-One Image Restoration）**任务。在现实场景中，图像退化往往是未知的、空间非均匀的（同一张图不同位置退化不同）且具有组合性（如低光+下雨+模糊并存）。
*   **研究动机**：现有的方法主要分为两类：一是使用全局提示（Prompts）或描述符，这会丢失局部退化细节；二是使用静态专家池（MoE），这存在路由不稳定、容量受限或更新同质化的问题。
*   **核心目标**：提出一种能够根据输入图像动态生成、且在空间上逐标记（Token-wise）自适应的参数调整机制。

#### 2. 方法论
论文提出了**连续专家组装（Continuous Expert Assembly, CEA）**框架，其核心思想是“不从固定专家库中选择，而是为每个实例动态合成专家分量并进行组装”。
*   **交叉注意力超适配器（Cross-Attention Hyper-Adapter）**：
    *   使用一组可学习的查询（Queries）作为探测器。
    *   通过交叉注意力机制从中间空间特征中提取退化上下文。
    *   解码生成实例相关的低秩矩阵：路由基底 $A(x)$ 和残差方向 $B(x)$。
*   **连续组装规则**：
    *   每个空间标记（Token） $X_n$ 与路由基底计算有符号点积亲和力。
    *   根据亲和力权重，将残差方向线性组合，生成该标记特有的残差更新 $\Delta Y_n$。
    *   **线性注意力视角**：该过程在代数上等同于无 Softmax 的点积注意力 $QK^TV$，其中 $K$ 和 $V$ 是动态生成的，这保证了计算的高效性（线性复杂度）。
*   **RankNorm**：为了解决动态分解中的尺度模糊问题，对生成的秩分量进行实例规范化，确保训练稳定。

#### 3. 实验设计
*   **数据集**：
    *   **AIO-3**：包含去噪、去雨、去雾任务。
    *   **AIO-5**：在 AIO-3 基础上增加去模糊和低光增强。
    *   **CDD-11**：专门针对复合退化（如低光+雾+雨）的挑战性基准。
*   **Benchmark 与对比方法**：
    *   对比了 AirNet, PromptIR, IDR, OneRestore, MoCE-IR 等主流全能修复模型。
    *   涵盖了不同规模的模型（Light 和 Heavy）。
*   **评估指标**：PSNR、SSIM、参数量（Params）、计算量（FLOPs）和推理耗时（Runtime）。

#### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **4 张 NVIDIA RTX 4090 GPU** 的工作站上完成。
*   **训练时长**：
    *   轻量级模型（CEA-IR-S）在 CDD-11 数据集上训练约 **15 小时**。
    *   在 AIO-3/5 数据集上训练约 **30 至 37.5 小时**。
*   **推理效率**：在单张 RTX 4090 上测量，CEA-IR-S 的推理速度约为 29.92ms，优于对比的 MoCE-IR 和 PromptIR。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准上进行了详尽测试，并提供了完整的每项任务得分。
*   **消融实验**：非常充分。作者对比了：
    *   静态因子 vs. 动态生成因子。
    *   稀疏 Top-K 路由 vs. 密集有符号点积组装。
    *   不同的秩（Rank）大小（4, 8, 16）。
    *   不同的注入位置（Q, K, V, FFN）。
    *   RankNorm 的有效性。
*   **统计严谨性**：引入了**配对自助法（Paired Bootstrap）**分析，通过 10,000 次重采样证明了 CEA 相比基准模型在统计学上具有显著的性能提升。

#### 6. 主要结论与发现
*   **性能领先**：CEA 在所有 AiO 基准上均优于基于提示和专家的模型，尤其在复合退化（CDD-11）上 PSNR 提升高达 0.74dB。
*   **动态组装的优越性**：实验证明，动态生成的低秩因子比静态学习的因子更能捕捉复杂的退化模式。
*   **密集路由的优势**：相比于离散的专家选择（Top-K），连续的、有符号的亲和力组装能提供更细粒度的修复。
*   **高效性**：CEA 在提升质量的同时，减少了参数量和 FLOPs，实现了更好的效能平衡。

#### 7. 优点
*   **机制创新**：将 MoE 的“选择”逻辑转变为“组装”逻辑，解决了离散路由不连续和静态专家容量固定的问题。
*   **空间自适应**：通过逐标记的动态参数化，天然适合处理空间非均匀的退化。
*   **数学优雅性**：利用线性注意力的形式实现低秩更新，既有理论支撑又保证了工程上的高效。
*   **无需先验**：不需要测试时的退化标签或复杂的辅助分类任务。

#### 8. 不足与局限
*   **架构依赖**：目前主要在 U-Shaped Transformer 架构上验证，尚未探讨其在纯 CNN 或新兴的 Mamba 架构中的表现。
*   **任务扩展**：实验集中在图像修复，尚未扩展到视频修复或其他低算力要求的实时移动端场景。
*   **超参数敏感性**：虽然做了秩的消融，但在极端复杂的退化组合下，如何自动确定最优的秩大小仍是一个挑战。

（完）
