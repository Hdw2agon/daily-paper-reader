---
title: "SARU: A Shadow-Aware and Removal Unified Framework for Remote Sensing Images with New Benchmarks"
title_zh: SARU：一种针对遥感图像的阴影感知与去除统一框架及新基准
authors: "Zi-Yang Bo, Wei Lu, Hongruixuan Chen, Si-Bao Chen, Bin Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25432v1"
tags: ["query:q3"]
score: 8.0
evidence: 无需成对图像训练的阴影去除
tldr: 针对遥感图像中阴影导致质量下降及下游任务受限的问题，本文提出SARU统一框架。该框架包含双分支检测模块DBCSF-Net以生成高保真阴影掩码，以及无需训练的物理算法N2SGSR进行光照恢复。通过整合检测与去除流程并消除对成对训练数据的依赖，SARU在多个基准测试中达到SOTA水平，并贡献了RSISD和SiSRB两个新数据集。
selection_source: fresh_fetch
motivation: 传统的阴影处理方法通常将检测与去除分离，导致流程繁琐、误差累积且过度依赖难以获取的成对训练数据。
method: 提出由融合多颜色空间特征的检测模块DBCSF-Net和基于单图邻近区域属性转移的物理恢复算法N2SGSR组成的统一框架。
result: 在公开数据集AISD及新提出的RSISD、SiSRB基准上，该框架均取得了当前最先进的性能表现。
conclusion: SARU通过整合检测与去除流程并摆脱成对数据限制，为现实场景下的遥感图像阴影处理提供了一个稳健且实用的解决方案。
---

## 摘要
阴影是遥感图像（RSI）中普遍存在的问题，它会降低视觉质量，并严重限制目标检测和语义分割等下游任务的性能。大多数先前的工作将阴影检测和去除视为独立的级联任务，这可能导致流程繁琐和误差累积。此外，许多深度学习方法依赖成对的阴影和非阴影图像进行训练，而这在实践中往往难以获得。为了应对这些挑战，我们提出了阴影感知与去除统一（SARU）框架，这是一个内聚的两阶段框架。首先，其双分支检测模块（DBCSF-Net）融合了多色彩空间和语义特征，以生成高保真阴影掩码，从而有效区分阴影与深色物体。然后，利用这些掩码，一种新颖的、无需训练的物理算法（N$^2$SGSR）通过从单张输入图像内的相邻非阴影区域转移属性来恢复光照。为了促进严格的评估并推动未来的工作，我们还引入了两个新的基准数据集：遥感图像阴影检测（RSISD）数据集和单图像阴影去除基准（SiSRB）。大量实验表明，SARU 在公开的 AISD 数据集和我们新引入的基准上均达到了最先进的性能。通过整体集成阴影检测和去除以减轻误差传播，并消除对成对训练数据的依赖，SARU 为现实世界的遥感图像分析建立了一个稳健且实用的框架。源代码和数据集可在以下网址公开获取：https://github.com/AeroVILab-AHU/SARU-Framework。

## Abstract
Shadows are a prevalent problem in remote sensing imagery (RSI), degrading visual quality and severely limiting the performance of downstream tasks like object detection and semantic segmentation. Most prior works treat shadow detection and removal as separate, cascaded tasks, which can lead to cumbersome process and error accumulation. Furthermore, many deep learning methods rely on paired shadow and non-shadow images for training, which are often unavailable in practice. To address these challenges, we propose Shadow-Aware and Removal Unified (SARU) Framework , a cohesive two-stage framework. First, its dual-branch detection module (DBCSF-Net) fuses multi-color space and semantic features to generate high-fidelity shadow masks, effectively distinguishing shadows from dark objects. Then, leveraging these masks, a novel, training-free physical algorithm (N$^2$SGSR) restores illumination by transferring properties from adjacent non-shadow regions within the single input image. To facilitate rigorous evaluation and foster future work, we also introduce two new benchmark datasets: the RSI Shadow Detection (RSISD) dataset and the Single-image Shadow Removal Benchmark (SiSRB). Extensive experiments demonstrate that SARU achieves state-of-the-art performance on both the public AISD dataset and our newly introduced benchmarks. By holistically integrating shadow detection and removal to mitigate error propagation and eliminating the dependency on paired training data, SARU establishes a robust, practical framework for real-world RSI analysis. The source code and datasets are publicly available at: https://github.com/AeroVILab-AHU/SARU-Framework.

---

## 论文详细总结（自动生成）

这是一份关于论文《SARU: A Shadow-Aware and Removal Unified Framework for Remote Sensing Images with New Benchmarks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：遥感图像（RSI）中的阴影会造成光谱畸变和信息丢失，严重干扰目标检测、语义分割等下游任务。
*   **研究动机**：
    *   **误差累积**：传统方法将阴影检测与去除视为独立的级联任务，检测阶段的微小误差（如边缘模糊、漏检）会直接放大并恶化去除效果。
    *   **数据依赖**：现有的深度学习去除模型高度依赖“成对”的阴影/非阴影图像进行训练，但在现实遥感场景中，获取同一位置、同一时间且仅有阴影差异的成对数据几乎是不可能的。
    *   **区分困难**：在复杂城市背景下，阴影与深色物体（如柏油路、深色建筑）极易混淆。

### 2. 论文提出的方法论
论文提出了 **SARU（阴影感知与去除统一框架）**，包含两个核心阶段：

*   **阶段一：阴影检测（DBCSF-Net）**
    *   **双分支架构**：采用多颜色空间组合（MCSC）编码器提取RGB、HSV、Lab空间的色彩特征，同时利用轻量化DecoupleNet提取高层语义特征。
    *   **特征融合模块（FFM）**：通过通道和空间注意力机制，自适应地融合色彩与语义信息，有效区分阴影与深色物体。
    *   **渐进式解码器**：利用转置卷积和跳跃连接恢复空间分辨率，生成高保真阴影掩码。

*   **阶段二：阴影去除（N$^2$SGSR）**
    *   **核心思想**：基于物理成像原理（$I = L \cdot R$），利用单张图像内相邻非阴影区域的属性来恢复阴影区光照。
    *   **算法流程**：
        1.  **超像素分割**：使用SLIC算法对图像进行分割。
        2.  **邻域搜索与匹配**：为每个阴影超像素寻找$n$个最近邻的非阴影超像素。
        3.  **相似性加权**：利用EMD（色彩分布）、LBP（纹理相似性）和a通道均值差（光谱衰减）计算权重，确保参考区域与阴影区域材质一致。
        4.  **光照补偿**：根据加权后的参考值计算光照比率，恢复阴影区像素。
        5.  **双边边界平滑**：针对半影区（过渡带）进行扩散平滑，消除边缘伪影。

### 3. 实验设计
*   **数据集**：
    *   **AISD**：公开的城市遥感阴影数据集。
    *   **RSISD（新提出）**：涵盖中国11个城市的1286对高分辨率（0.3m）图像，包含工厂、学校、体育场等多样化场景。
    *   **SiSRB（新提出）**：首个专门用于单图阴影去除评估的基准，包含55张带有人工标注评估掩码的图像。
*   **对比方法**：
    *   **检测对比**：与RSiSD、FDRNet、SDDNet、SILT等11种SOTA方法对比。
    *   **去除对比**：与G2R-ShadowNet、Self-ShadowGAN、RS-GSSR等7种方法对比。
*   **下游任务验证**：在去阴影后的图像上运行车辆检测模型（SCAF-Net），验证预处理对实际应用的效果提升。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **软件/参数**：基于PyTorch框架；使用Adam优化器；学习率 $2 \times 10^{-4}$；Batch Size为4。
*   **训练时长**：检测网络训练了30个Epoch。阴影去除算法（N$^2$SGSR）为**无需训练（Training-free）**的物理算法，仅需推理算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集（AISD, RSISD）上进行了详尽的检测性能测试。
    *   在SiSRB上进行了去除效果的定量分析（使用SRI, CD, NIQE, BRISQUE等指标）。
    *   **消融实验**：针对检测网络的不同分支（RGB/HSV/Lab/语义）、去除算法的邻居数量$n$、以及边界平滑机制分别做了对比实验。
*   **充分性评价**：实验设计非常充分且客观。不仅涵盖了标准指标对比，还通过下游任务（车辆检测）验证了实用性，并专门讨论了缺乏局部参考区域的极端挑战场景，体现了科研的严谨性。

### 6. 论文的主要结论与发现
*   **检测性能**：DBCSF-Net在IoU和F1-score上均达到最优，证明了融合多颜色空间和语义信息的必要性。
*   **去除效果**：N$^2$SGSR在无需成对数据训练的情况下，其恢复质量（SRI/CD指标）和视觉自然度（BRISQUE指标）优于许多复杂的GAN模型。
*   **效率优势**：N$^2$SGSR处理一张512×512图像仅需约1.3秒，远快于需要迭代生成的扩散模型或复杂GAN。
*   **下游增益**：去除阴影后，车辆检测的漏检率显著降低，证明了该框架作为预处理工具的价值。

### 7. 优点（亮点）
*   **统一框架**：通过紧密耦合检测与去除，减少了级联误差。
*   **摆脱数据依赖**：去除算法无需成对训练数据，极大地提升了在现实世界（尤其是缺乏标注的遥感领域）的适用性。
*   **物理驱动**：N$^2$SGSR结合了超像素和物理成像模型，比纯黑盒模型更具可解释性。
*   **贡献基准**：开源了两个高质量数据集，填补了遥感单图阴影去除评估的空白。

### 8. 不足与局限
*   **植被阴影挑战**：在AISD数据集上的BER指标并非最高，论文指出这主要是由于植被阴影边缘极不规则且标注质量不一导致的。
*   **极端场景限制**：当图像中出现大面积阴影且完全缺乏同材质的非阴影参考区域时（如整个水池被遮挡），算法虽有全局搜索机制，但恢复效果仍受限于全局上下文的丰富度。
*   **超参数依赖**：超像素大小和邻居数量$n$仍需根据经验设定，对不同分辨率图像的自适应性有待进一步加强。

（完）
