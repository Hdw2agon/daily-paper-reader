---
title: Null-Space Diffusion Restoration with Adaptive Uncertainty-Guided Fusion for Ultrasound Speckle Reduction
title_zh: 结合自适应不确定性引导融合的零空间扩散修复超声斑点抑制
authors: "Juneyong Lee, Jaeyoung Choi"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29820v1"
tags: ["query:q3"]
score: 9.0
evidence: 无地面真值的无监督扩散修复
tldr: 针对超声成像中缺乏无噪真值图像及对数压缩域处理易放大伪影的问题，本文提出了一种名为UGNS的无监督零空间扩散恢复框架。该方法通过在逆对数压缩后的稳定包络域执行一致性修正，结合结构先验提取、自适应范围-零空间重构及不确定性引导融合技术，实现了在抑制斑点噪声的同时保留精细解剖结构。实验证明其在PICMUS和离体数据集中具有优异的对比度信噪比和分辨率保持能力。
selection_source: fresh_fetch
motivation: 现有的超声去噪方法受限于缺乏真值图像的“真值悖论”，且在非线性对数压缩域进行数据一致性约束容易放大背景伪影。
method: 提出UGNS框架，在稳定包络域提取结构先验，利用自适应范围-零空间重构机制保护组织区域，并通过不确定性引导融合降低采样变异性。
result: 在PICMUS基准和离体数据集上的实验表明，该方法在广义对比度信噪比（gCNR）和空间分辨率保持方面均达到了竞争性水平。
conclusion: UGNS为无监督超声图像去噪提供了一种有效方案，能够在不依赖真值标签的情况下平衡噪声抑制与解剖结构保护。
---

## 摘要
超声 B 模式成像通常受到斑点噪声和伪影的影响，需要在对比度、分辨率和解剖结构保留之间取得微妙的平衡。尽管最近开发的去噪方法取得了一些进展，但监督学习方法仍从根本上受到“地面真值悖论”（ground truth paradox）的限制，这是由于在活体场景中缺乏无噪声的地面真值参考图像。现有的基于扩散的无监督方法通常直接在非线性对数压缩域中强制执行数据一致性，当映射回包络域时，这可能会不成比例地放大背景伪影。为了克服这些局限性，我们提出了一种不确定性引导的零空间扩散（UGNS）框架，这是一种新颖的无标签解决方案，它在通过反向对数压缩获得的稳定正包络代理上执行一致性修正。所提出的 UGNS 引入了多项技术创新：(a) 在稳定包络域中提取结构先验，以产生保留解剖结构的鲁棒信号包络；(b) 开发了一种自适应值域-零空间（range-null）重建机制，利用自适应权重掩码通过值域空间投影保留组织区域；(c) 以自适应方式引入不确定性引导融合，以减轻采样变异性。使用 PICMUS 基准测试和活体数据集进行了广泛的对比实验。结果表明，UGNS 在不同数据集上均实现了具有竞争力的广义对比度噪声比（gCNR）值。此外，实验成功验证了 UGNS 在保留精细空间分辨率的同时，能有效抑制斑点噪声。代码可在 https://github.com/yousirong/UGNS.git 获取。

## Abstract
Ultrasound B-mode imaging commonly suffers from speckle noise and artifacts, requiring a delicate balance between contrast, resolution, and preservation of anatomical structures. Although recently developed despeckling methods have achieved some progress, supervised learning approaches remain fundamentally limited by the ground truth paradox, which arises from the absence of noise-free, ground truth reference images in in vivo scenarios. Existing unsupervised diffusion-based methods typically enforce data consistency directly in the nonlinear log-compressed domain, which can disproportionately amplify background artifacts when mapped back to the envelope domain. To overcome these limitations, we propose an uncertainty-guided null-space diffusion (UGNS) framework, a novel label-free solution that enforces consistency correction on a stabilized positive-envelope proxy obtained via inverse log compression. The proposed UGNS introduces several technical novelties: (a) extraction of a structural prior in the stabilized envelope domain to produce a robust signal envelope that preserves anatomical structure, (b) development of an adaptive range-null reconstruction mechanism that uses an adaptive weight mask to preserve tissue regions via range-space projection, and (c) introduction of uncertainty-guided fusion in an adaptive way to mitigate sampling variability. Extensive and comparative experiments were conducted using the PICMUS benchmark and in vivo datasets. The results demonstrate that UGNS achieves competitive generalized contrast-to-noise ratio (gCNR) values across diverse datasets. In addition, it is successfully validated that UGNS effectively suppresses speckle noise while preserving fine spatial resolution. Code is available at https://github.com/yousirong/UGNS.git.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **UGNS（Uncertainty-Guided Null-Space Diffusion）** 的无监督超声图像去噪框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：超声 B 模式成像普遍存在斑点噪声（Speckle Noise），严重影响解剖结构的对比度和分辨率。
*   **现有挑战**：
    *   **地面真值悖论**：监督学习方法需要“无噪图像”作为标签，但在活体成像中根本无法获取。
    *   **对数域伪影**：现有的扩散模型通常在非线性对数压缩域执行一致性约束，这会导致背景噪声被不成比例地放大，产生虚假的纹理。
    *   **物理模型依赖**：一些先进方法（如 DRUS）依赖于复杂的声学系统矩阵和 SVD 分解，计算量大且对硬件参数敏感。
*   **核心目标**：开发一种无需标签、无需显式系统矩阵、且能有效平衡噪声抑制与细节保留的生成式修复框架。

### 2. 方法论
UGNS 框架由三个核心阶段组成：
*   **物理一致性结构先验提取**：
    *   首先通过**逆对数压缩**将图像转回稳定的正包络代理域（Envelope Domain），避免非线性放大。
    *   结合 **SRAD（各向异性扩散滤波）** 和**小波低通滤波**提取鲁棒的结构包络，并生成自适应权重掩码 $M$。
*   **自适应范围-零空间（Range-Null Space）重构**：
    *   利用离散小波变换将信号分解。将观测图像的低频成分（值域空间，代表组织结构）与扩散模型生成的细节（零空间，用于填充缺失信息）相结合。
    *   通过掩码 $M$ 进行自适应混合：高置信度组织区域倾向于保留观测值，背景区域则由扩散先验主导去噪。
*   **不确定性引导融合**：
    *   由于扩散模型具有随机性，通过生成 10 个样本构建集成（Ensemble）。
    *   计算像素级方差作为**不确定性图**。
    *   根据不确定性权重，将集成平均值（稳定性高）与结构最清晰的代表性样本进行融合，从而在抑制随机噪声的同时保留边缘锐度。

### 3. 实验设计
*   **数据集/场景**：
    *   **PICMUS 2016 挑战赛基准**：包括模拟/实验对比度（SC/EC）和模拟/实验分辨率（SR/ER）数据集。
    *   **活体数据**：颈动脉横截面（CC）和纵向（CL）视图。
    *   **额外验证**：使用 TPAC Pioneer 系统采集的独立颈动脉序列。
*   **对比方法**：
    *   传统方法：DAS（1/11/75 角度平面波合成）。
    *   自监督方法：Noise2Noise, Noise2Void。
    *   扩散模型基准：DENO, DRUS (mean/var), SDPS。
*   **评价指标**：gCNR（广义对比度信噪比）、CNR、SNR、FWHM（半高全宽，衡量分辨率）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **预训练**：使用 5,929 张无标签超声图像进行生成式预训练。
*   **推理耗时**：
    *   单样本生成（N=1）：约 **6.0 秒/张**。
    *   集成融合（N=10）：约 **54 秒/张**。
*   **显存占用**：约 2.7 GB 至 7.9 GB。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了标准的幻影（Phantom）基准测试和多种活体场景，实验设计较为全面。
*   **消融实验**：针对结构先验、范围锚定、不确定性融合等 6 个关键组件进行了消融测试，验证了各部分的贡献。
*   **统计显著性**：采用了配对单侧 t 检验，并进行了 **Bonferroni 校正**（110 次测试），确保结果在统计学上是严谨且非偶然的。
*   **公平性**：在相同的硬件和评估协议下对比了多种 SOTA 方法，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：UGNS 在 PICMUS 对比度数据集上获得了最高的 gCNR（如 EC 达到 0.9948），显著优于 DAS 和其他扩散模型。
*   **单角度实现高质量**：仅使用单平面波（1PW）输入，UGNS 的重建质量在视觉上接近甚至超过了 75 角度合成（75PWs）的 DAS 结果。
*   **平衡性**：相比于 DRUSvar 等方法容易产生过度平滑（“二值化”）的视觉感，UGNS 保留了更自然的背景纹理和更清晰的血管壁边界。

### 7. 优点
*   **无需标签**：彻底解决了活体超声缺乏真值图像的难题。
*   **系统无关性**：不依赖特定的换能器几何参数或声学矩阵，具有更好的跨设备通用性。
*   **域感知处理**：通过在包络域执行一致性修正，有效抑制了对数域处理带来的背景伪影。
*   **不确定性量化**：利用集成采样降低了生成模型的随机变异性，提升了临床可靠性。

### 8. 不足与局限
*   **计算效率**：由于扩散模型需要多次迭代采样，推理速度较慢（分钟级），目前无法满足实时临床成像需求。
*   **泛化范围**：实验主要集中在颈动脉和幻影数据，尚需在更多器官（如心脏、腹部）和病理类型上验证。
*   **频率假设**：基于小波的范围-零空间分解假设组织结构主要在低频，这可能导致包含高频成分的极细微解剖结构被部分削弱。

（完）
