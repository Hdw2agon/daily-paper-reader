---
title: "FlashClear: Ultra-Fast Image Content Removal via Efficient Step Distillation and Feature Caching"
title_zh: FlashClear：通过高效步数蒸馏与特征缓存实现超快速图像内容移除
authors: "Yixin Tang, Jiawei Guo, Junxian Li, Zhiteng Li, Jixin Zhao, Bingya Zhang, Chenbo Wang, Yulun Zhang, Shangchen Zhou"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09003v1"
tags: ["query:q1"]
score: 9.0
evidence: 基于扩散的对象移除与内容补全
tldr: 针对扩散模型在图像内容移除中计算开销大、推理慢的问题，本文提出 FlashClear。通过区域感知对抗蒸馏（RAD）实现高效的少步推理，并结合无需训练的前景优先非对称注意力与缓存策略（FPAC）进一步加速。该方法在保持高视觉质量的同时，实现了显著的推理加速，在 OBER 基准测试中比现有模型快 8.26 至 122 倍，显著提升了实用性。
selection_source: fresh_fetch
motivation: 现有的扩散移除模型对全图所有 token 进行无差别去噪，导致在处理局部移除任务时存在严重的计算冗余和推理延迟。
method: 提出区域感知对抗蒸馏（RAD）以实现少步生成，并引入前景优先的非对称注意力与特征缓存策略（FPAC）进行免训练加速。
result: 在 OBER 基准上，FlashClear 相比 ObjectClear 和 OmniPaint 分别实现了 8.26 倍和 122 倍的加速，且保持了极高的视觉保真度。
conclusion: FlashClear 通过结合蒸馏与特征缓存技术，成功解决了扩散模型在图像移除任务中的效率瓶颈，实现了超快速且高质量的内容移除。
---

## 摘要
最近，基于扩散的物体移除模型在消除物体及其相关视觉效果方面取得了令人瞩目的成果。然而，这些模型在所有时间步中不加区分地对所有 token 进行去噪，忽略了移除操作通常仅涉及较小的前景区域。这种策略引入了巨大的计算开销并延长了推理时间。为了克服这一计算负担，我们提出了一个潜空间判别器来实现区域感知对抗蒸馏（RAD），从而产生了一个名为 FlashClear 的高效少步模型。此外，针对少步扩散模型，我们提出了 FPAC（前景优先非对称注意力与缓存），这是一种无需训练的加速策略。大量实验表明，我们的框架在提供巨大加速的同时，保持或超过了基础模型 ObjectClear 的性能。值得注意的是，在 OBER 基准测试中，我们的 FlashClear 相比 ObjectClear 和 OmniPaint 分别实现了高达 8.26 倍和 122 倍的加速，同时保持了极高的视觉质量和保真度。

## Abstract
Recently, diffusion-based object removal models have achieved impressive results in eliminating objects and their associated visual effects. However, they indiscriminately denoise all tokens across all timesteps, ignoring that removal usually involves small foreground regions. This strategy introduces substantial computational overhead and prolonged inference times. To overcome this computational burden, we propose a latent discriminator to implement Region-aware Adversarial Distillation (RAD), yielding a highly efficient few-step model named FlashClear. Furthermore, tailored to few-step diffusion models, we propose FPAC (Foreground-Prioritized Asymmetric Attention and Caching), a training-free acceleration strategy. Extensive experiments demonstrate that our framework provides massive acceleration while maintaining or exceeding the performance of our base model, ObjectClear. Notably, on the OBER benchmark, our FlashClear achieves up to 8.26$\times$ and 122$\times$ speedup over ObjectClear and OmniPaint, respectively, while maintaining high visual quality and fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FlashClear: Ultra-Fast Image Content Removal via Efficient Step Distillation and Feature Caching》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于扩散模型的物体移除方法虽然效果出色，但面临严重的**推理延迟**问题。
*   **研究动机**：
    *   **计算冗余**：传统模型在所有时间步中对图像的所有 Token 进行无差别去噪，而物体移除任务通常只涉及局部的“前景”区域，背景大部分保持不变。
    *   **实时性需求**：物体移除常用于交互式编辑，需要即时反馈，多步扩散过程（如 20-50 步）成为了移动端部署和实时应用的瓶颈。
    *   **现有加速技术的局限**：步数蒸馏（Step Distillation）在强空间约束任务中易导致模糊；特征缓存（Feature Caching）在极少步数（如 4 步）下因时间冗余度降低而失效。

### 2. 方法论：核心思想与关键技术
FlashClear 结合了**少步蒸馏**与**空间感知缓存**两大核心策略：
*   **区域感知对抗蒸馏 (RAD)**：
    *   **核心思想**：利用对抗学习将多步去噪压缩至 4 步甚至 2 步。
    *   **技术细节**：使用预训练 U-Net 作为潜空间判别器。为了处理物体及其关联效果（如阴影、反射），生成器仅接收物体掩码（Object Mask），而判别器接收扩大的“物体+效果”掩码（Obj+Eff Mask），强制模型学习消除物体诱发的物理影响。
    *   **损失函数**：结合了扩散 L2 损失、感知损失（LPIPS）、对抗损失以及注意力引导的掩码损失（Mask Loss）。
*   **前景优先非对称注意力与缓存 (FPAC)**：
    *   **核心思想**：利用物体移除任务的**空间冗余性**，仅重算受影响区域。
    *   **技术细节**：
        *   **动态掩码**：通过交叉注意力图（Cross-Attention Maps）自动识别前景和受影响区域。
        *   **非对称注意力**：前景 Token 作为 Query 去查询全局的 Key 和 Value，而背景 Token 直接从缓存中读取特征，跳过计算。
        *   **无损融合**：在最后一步将生成的前景与原始背景进行注意力引导的无缝融合。

### 3. 实验设计
*   **数据集**：
    *   **OBER-Test**：评估 512×512 分辨率下的通用物体移除能力。
    *   **RORD-Val**：包含 960×540 高分辨率复杂场景的真实世界数据集。
    *   **CausRem**：专门用于测试阴影和反射移除的挑战性场景。
*   **Benchmark 与对比方法**：
    *   **对比模型**：包括 SDXL-Inpainting、PowerPaint、GeoRemover、OmniPaint、ObjectClear、RORem 等。
    *   **加速方法对比**：与 ToCa、SiTo、HiCache、QuantCache 等主流扩散模型加速方案进行对比。
*   **评价指标**：LPIPS（感知质量）、PSNR（像素相似度）、FLOPs（理论计算量）、PSNR-mask（局部保真度）。

### 4. 资源与算力
*   **训练资源**：使用 **2 张 NVIDIA A800 GPU** (80GB)。
*   **训练时长**：总计 10,000 次迭代，耗时约 **24 小时**。
*   **推理资源**：在单张 **NVIDIA A6000 GPU** (48GB) 上进行测试。
*   **配置细节**：基于 SDXL 架构，使用 LoRA 调优（Rank=256），混合精度为 bfloat16。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 Benchmark 上进行了详尽的定量对比。
    *   针对 4 步和 2 步模型分别进行了性能评估。
    *   **消融实验**：验证了 RAD 中不同损失组件（GAN、LPIPS、Mask Loss）的作用，以及 FPAC 相比其他缓存策略的优越性。
    *   **用户研究 (User Study)**：邀请参与者对 200 组结果进行主观评价，证明了加速后的模型在视觉上与原模型（ObjectClear）难辨优劣。
*   **评价**：实验设计客观、公平，通过理论计算量（FLOPs）而非单纯的硬件延迟来衡量加速比，排除了硬件环境的干扰。

### 6. 主要结论与发现
*   **极致加速**：FlashClear 在 OBER 基准上比基础模型 ObjectClear 快 **8.26 倍**，比 OmniPaint 快 **122 倍**。
*   **质量保持**：在大幅减少计算量的同时，FlashClear 的 LPIPS 和 PSNR 指标甚至优于许多多步迭代的 SOTA 模型。
*   **空间先验的重要性**：实验证明，在极少步数下，基于空间冗余（FPAC）的缓存比基于时间冗余的传统缓存更有效。
*   **效果感知**：RAD 策略成功使模型在 4 步内就能识别并抹除物体相关的阴影和反射。

### 7. 优点：亮点总结
*   **任务定制化加速**：不同于通用的模型压缩，FlashClear 敏锐捕捉到了物体移除任务“局部修改、全局参考”的本质。
*   **免训练加速插件**：FPAC 模块不需要额外训练，可直接应用于蒸馏后的模型，具有很强的灵活性。
*   **解决冲突**：成功解决了“步数蒸馏”与“特征缓存”在低步数下的不兼容问题。

### 8. 不足与局限
*   **任务局限性**：该方法高度依赖于物体移除任务的空间局部性，对于需要全图大幅度重绘的任务（如风格迁移）可能效果有限。
*   **系统开销**：虽然理论 FLOPs 大幅下降，但实际端到端加速受限于 I/O 操作和内存通信，在极其轻量化的推理中，这些非计算开销占比会增加。
*   **掩码依赖**：模型性能仍部分取决于输入掩码的准确性，尽管 RAD 增强了对关联效果的感知，但极端复杂的物理交互仍可能存在残留。

（完）
