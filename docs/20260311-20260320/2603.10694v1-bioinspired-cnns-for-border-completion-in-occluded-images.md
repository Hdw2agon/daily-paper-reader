---
title: Bioinspired CNNs for border completion in occluded images
title_zh: 用于遮挡图像边界补全的仿生卷积神经网络
authors: "Catarina P. Coutinho, Aneeqa Merhab, Janko Petkovic, Ferdinando Zanchetta, Rita Fioresi"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10694v1"
tags: ["query:q1"]
score: 9.0
evidence: 使用仿生卷积神经网络进行遮挡图像的边界补全
tldr: 本研究受视觉皮层边界补全机制的启发，提出了一种新型卷积神经网络架构 BorderNet。通过将视觉皮层处理遮挡的数学模型融入 CNN 滤波器设计，该方法旨在增强模型在图像受损或被遮挡时的识别鲁棒性。在 MNIST 等多个数据集上的实验表明，BorderNet 在面对条纹和网格状遮挡时，其性能显著优于传统模型，为开发更具抗干扰能力的计算机视觉系统提供了新思路。
selection_source: fresh_fetch
motivation: 针对图像遮挡导致的识别性能下降问题，借鉴视觉皮层的边界补全机制来提升卷积神经网络的鲁棒性。
method: 提出了一种名为 BorderNet 的架构，利用视觉皮层边界补全的数学模型来设计 CNN 滤波器。
result: 在 MNIST、Fashion-MNIST 和 EMNIST 数据集上的条纹和网格遮挡测试中，BorderNet 均表现出优于传统 CNN 的性能。
conclusion: 仿生设计的边界补全机制能有效增强 CNN 处理遮挡图像的能力，提升模型的鲁棒性。
---

## 摘要
我们利用视觉皮层中边界补全问题的数学建模，设计了卷积神经网络（CNN）滤波器，以增强对图像遮挡的鲁棒性。我们在三种遮挡数据集（MNIST、Fashion-MNIST 和 EMNIST）上，针对条纹和网格两种遮挡类型，评估了我们的 CNN 架构 BorderNet。在所有情况下，BorderNet 都表现出性能提升，其增益随遮挡严重程度和数据集的不同而有所变化。

## Abstract
We exploit the mathematical modeling of the border completion problem in the visual cortex to design convolutional neural network (CNN) filters that enhance robustness to image occlusions. We evaluate our CNN architecture, BorderNet, on three occluded datasets (MNIST, Fashion-MNIST, and EMNIST) under two types of occlusions: stripes and grids. In all cases, BorderNet demonstrates improved performance, with gains varying depending on the severity of the occlusions and the dataset.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何借鉴生物视觉皮层的数学模型来提升卷积神经网络（CNN）在面对图像遮挡时的鲁棒性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 CNN 在面对图像遮挡（如物体被部分遮盖）时性能往往大幅下降，而高等哺乳动物的视觉系统（尤其是初级视觉皮层 V1）具有强大的“边界补全”能力，能通过局部信息推断出被遮挡的轮廓。
*   **研究动机**：利用微分几何（亚黎曼几何）对视觉皮层进行数学建模，并将这种生物启发机制转化为 CNN 的结构设计，从而在不增加训练复杂度的前提下，增强模型对遮挡图像的识别能力。

### 2. 方法论
*   **核心思想**：将 V1 皮层建模为“接触束”（Contact Bundle），利用亚黎曼测地线（Sub-Riemannian Geodesics）来描述神经元如何跨越遮挡区域连接相似方向的边缘。
*   **关键技术细节**：
    *   **数学建模**：采用哈密顿形式（Hamiltonian formalism）计算亚黎曼测地线。通过定义方向图（Orientation Map），将图像边缘检测视为在 $R^2 \times S^1$ 空间（位置+角度）中的路径寻找问题。
    *   **BorderNet 架构**：在经典 LeNet5 架构的基础上进行改进。在网络前端引入了四个自定义的**方向滤波器**（水平、垂直、两条对角线）。
    *   **滤波器设计**：滤波器尺寸为 $7 \times 7$，条纹宽度为 3 像素。这些滤波器是预定义的（非学习得到），模拟了视觉皮层中对特定方向敏感的简单细胞（Simple Cells）的感受野。
    *   **工作流程**：这些仿生滤波器首先对输入图像进行预处理，提取并强化特定方向的边缘信息，随后再进入标准的卷积层进行特征提取。

### 3. 实验设计
*   **数据集**：使用了三个标准数据集：**MNIST**（手写数字）、**Fashion-MNIST**（衣物分类）和 **EMNIST**（扩展手写字符）。
*   **遮挡场景**：
    *   **条纹遮挡（Stripes）**：斜向黑色条纹。
    *   **网格遮挡（Grid）**：水平和垂直交叉的黑色网格。
    *   **参数变量**：遮挡条纹的宽度 ($w$) 和间距 ($s$) 均在 1 到 10 像素之间变化，以模拟不同程度的遮挡。
*   **Benchmark（基准）**：标准的 **Vanilla LeNet5** 模型。
*   **对比策略**：所有模型仅在**原始无遮挡**的数据集上训练，仅在测试阶段使用遮挡图像，以验证模型的泛化鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到使用了 ADAM 优化器，学习率为 0.001，训练 10 个 epoch，batch size 为 64。由于 LeNet5 属于轻量级网络，预计所需算力较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个数据集和每种遮挡类型，遍历了 $w, s \in [1, 10]$ 的所有组合（共 100 种遮挡配置）。
    *   每个实验循环执行 **100 次**并取平均值，以消除随机性影响。
    *   使用了 **100,000 次自助法（Bootstrap）** 采样来计算中值改进和 95% 置信区间。
*   **充分性评价**：实验设计非常严谨且充分。通过大规模的参数扫描（$w$ 和 $s$ 的组合）和统计学显著性分析，客观地展示了模型在不同遮挡强度下的表现。

### 6. 主要结论与发现
*   **性能提升**：在绝大多数遮挡情况下，BorderNet 的准确率显著优于 Vanilla LeNet5。
*   **鲁棒性增强**：即使模型从未在遮挡图像上训练过，仿生滤波器也能有效提取残存的边缘特征，帮助网络完成分类。
*   **遮挡敏感度**：改进效果随遮挡严重程度而异。在极度严重的遮挡下（如条纹过宽），所有模型性能都会失效，但在中等程度遮挡下，BorderNet 展现出明显的优势（部分案例提升超过 30%-80%）。

### 7. 优点
*   **理论支撑深厚**：不同于纯经验性的架构调整，该方法有坚实的神经科学和微分几何数学基础。
*   **零样本鲁棒性**：无需在遮挡数据上重新训练，具有很强的实用价值。
*   **轻量高效**：仅通过在前端添加少量固定滤波器即可实现性能提升，不增加模型参数量和训练负担。

### 8. 不足与局限
*   **架构单一**：实验仅基于 LeNet5 这种较旧的网络，尚未在 ResNet、Vision Transformer 等现代主流架构上验证。
*   **数据集复杂度低**：MNIST 等数据集背景简单、特征明显，在自然场景（如 ImageNet）下的复杂遮挡效果尚待观察。
*   **滤波器固定**：目前使用的是预定义的静态滤波器，如果能结合可学习的仿生约束，可能会有更好的适应性。
*   **遮挡类型受限**：仅测试了规则的几何遮挡（条纹/网格），未测试不规则形状或真实物体的遮挡。

（完）
