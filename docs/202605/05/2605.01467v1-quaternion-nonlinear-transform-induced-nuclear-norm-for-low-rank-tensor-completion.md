---
title: Quaternion Nonlinear Transform-Induced Nuclear Norm for Low-Rank Tensor Completion
title_zh: 面向低秩张量补全的四元数非线性变换诱导核范数
authors: "Biswarup Karmakar, Ratikanta Behera"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01467v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于恢复缺失数据的低秩张量补全
tldr: 针对现有非线性变换张量核范数（NTTNN）仅限于实值张量且无法处理四元数数据的局限性，本文提出了一种四元数非线性变换诱导张量核范数（QNTTNN）。通过四元数的实嵌入技术，解决了非交换性带来的优化难题，并构建了相应的张量补全模型。实验证明，该方法在彩色视频修复等任务中优于现有技术，能更有效地捕捉多通道间的内在关联。
selection_source: fresh_fetch
motivation: 现有的非线性张量核范数方法无法处理四元数数据，难以有效建模彩色图像和视频中的通道间依赖关系。
method: 提出QNTTNN模型，利用四元数实嵌入技术定义可计算的核范数，并开发了具有收敛性保证的近端交替最小化算法。
result: 在基准彩色视频修复数据集上的实验结果表明，该方法在恢复性能上显著优于现有的线性及实值非线性张量补全方法。
conclusion: QNTTNN通过结合非线性变换与四元数表示，为高维信号恢复提供了一种更强大的低秩建模框架。
---

## 摘要
张量补全已成为一种通过利用低秩张量结构来恢复多维信号中缺失数据的强大框架。在现有方法中，基于线性变换的张量核范数（TNN）方法通过对变换后的前向切片施加低秩性，取得了显著成功。然而，线性变换所揭示的低秩结构本质上仍然有限。为了更好地捕捉内在相关性，研究者提出了基于非线性变换的 TNN（NTTNN）模型，通过复合变换显著增强了低秩表示。尽管这些方法有效，但现有的 NTTNN 方法仅限于实值张量，无法对四元数值数据进行建模，而四元数对于保持彩色图像和视频中的通道间依赖关系至关重要。由于四元数乘法的非交换性和四元数奇异值分解的复杂性，将非线性 TNN 模型扩展到四元数域具有挑战性。为了解决先前工作中的局限性，我们通过四元数的实嵌入提出了一种四元数非线性变换诱导张量核范数（QNTTNN），从而实现了可处理的核范数定义和高效优化。在 QNTTNN 的基础上，我们构建了一个四元数张量补全模型，并开发了一种具有严格收敛性保证的近端交替最小化算法。在基准彩色视频修复数据集上的大量实验验证了所提方法优于现有方法的性能。

## Abstract
Tensor completion has emerged as a powerful framework for recovering missing data in multidimensional signals by exploiting low-rank tensor structures. Among existing approaches, linear transform-based tensor nuclear norm (TNN) methods have achieved considerable success by enforcing low-rankness on transformed frontal slices. However, the low-rank structure revealed by linear transforms remains inherently limited. To better capture intrinsic correlations, nonlinear transform-based TNN (NTTNN) models have been proposed, significantly enhancing low-rank representation through composite transforms. Despite their effectiveness, existing NTTNN methods are restricted to real-valued tensors and fail to model quaternion-valued data, which are essential for preserving inter-channel dependencies in color images and videos. Extending nonlinear TNN models to the quaternion domain is challenging due to the non-commutativity of quaternion multiplication and the complexity of quaternion singular value decomposition. To address the limitations encountered in prior works, we propose a quaternion nonlinear transform-induced tensor nuclear norm (QNTTNN) via a real embedding of quaternions, enabling tractable nuclear norm definitions and efficient optimization. Building upon QNTTNN, we formulate a quaternion tensor completion model and develop a proximal alternating minimization algorithm with rigorous convergence guarantees. Extensive experiments on benchmark color video inpainting datasets validate the superior performance of the proposed method over existing approaches.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**四元数非线性变换诱导张量核范数（QNTTNN）**的新框架，旨在解决彩色视频补全中跨通道相关性刻画不足以及线性变换表示能力有限的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：彩色视频数据（RGB）通常被视为三阶或四阶张量。传统的实值张量补全方法（如 TNN）往往独立处理三个颜色通道，忽略了通道间的内在相关性。
*   **技术瓶颈**：
    1.  **线性变换局限性**：现有的张量核范数多基于线性变换（如 DFT、DCT），在低采样率或复杂动态场景下，难以充分压缩奇异值能量。
    2.  **四元数建模难点**：虽然四元数能将 RGB 耦合为单一代数实体，但由于四元数乘法的非交换性和奇异值分解（QSVD）的复杂性，将非线性变换引入四元数域存在巨大的数学挑战。
*   **核心目标**：结合四元数表示的结构优势与非线性变换的强大表达能力，提升彩色视频在极低采样率下的恢复质量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：通过“实嵌入（Real Embedding）”技术，将复杂的四元数非线性优化问题转化为等价的实值矩阵运算，从而定义可计算的非线性诱导核范数。
*   **关键技术细节**：
    1.  **复合非线性变换**：定义变换 $\Psi(\mathcal{X}) = \Phi(\mathcal{X} \times_3 T)$，其中 $T$ 是学习到的实值半正交线性变换，$\Phi$ 是作用于实嵌入域的元素级非线性激活函数（如 $tanh$）。
    2.  **实结构保持表示**：将 $n_1 \times n_2$ 的四元数矩阵映射为 $4n_1 \times 4n_2$ 的实块矩阵。证明了四元数核范数等于其对应实嵌入矩阵核范数的 $1/4$。
    3.  **QNTTNN 定义**：在变换域的实嵌入切片上施加核范数约束，利用非线性收缩进一步增强低秩性。
*   **算法流程**：采用**近端交替最小化（PAM）**算法。
    *   **X-子问题**：通过观测数据投影更新。
    *   **Y-子问题**：利用四元数奇异值阈值（QSVT）算子在实嵌入域求解。
    *   **Z-子问题**：通过牛顿法（Newton's method）处理元素级非线性映射。
    *   **T-子问题**：通过正交 Procrustes 问题求解最优线性变换。

### 3. 实验设计
*   **数据集**：使用了标准的彩色视频数据集 `carphone_qcif.y4m`（分辨率 144×176×100）。
*   **实验场景**：随机缺失掩码（Random Mask），采样率（Sampling Rate）分别设置为 5%、10%、20% 和 30%。
*   **对比方法（Benchmark）**：
    1.  **TNN**：基于 t-SVD 的线性张量核范数方法。
    2.  **NTTNN**：基于实值非线性变换的方法（逐通道处理）。
    3.  **QNTTNN**：本文提出的四元数非线性方法。
*   **评价指标**：峰值信噪比（PSNR）、结构相似性（SSIM）和相对平方误差（RSE）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-12900K CPU (3.2 GHz)，32 GB RAM。
*   **软件环境**：MATLAB R2023b，Windows 11。
*   **算力说明**：文中**未提及使用 GPU**，所有实验均在 CPU 上完成。未明确给出具体的训练或推理总时长，但分析了每轮迭代的浮点运算量（Flops）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同采样率进行了对比实验。
    *   进行了**消融实验**，测试了 5 种不同的激活函数（tanh, sigmoid, ReLU, ELU, Swish）对结果的影响。
*   **充分性评价**：实验设计较为标准，涵盖了性能曲线分析和视觉效果对比。消融实验证明了平滑且有界的激活函数（如 tanh）优于非平滑函数（如 ReLU）。但**数据集单一**（仅详细展示了一个视频序列），对于不同风格视频的泛化性验证略显不足。

### 6. 论文的主要结论与发现
*   **性能领先**：QNTTNN 在所有采样率下均优于 TNN 和逐通道的 NTTNN。在 5% 极低采样率下，PSNR 比 TNN 高出约 2.48 dB。
*   **四元数优势**：实验证明，将 RGB 通道作为四元数整体处理，比独立处理通道能更好地保持颜色一致性和结构细节。
*   **收敛性**：通过 Kurdyka-Lojasiewicz (KL) 理论证明了算法能够全局收敛至临界点。

### 7. 优点
*   **理论严谨**：通过实嵌入技术巧妙解决了四元数非交换性带来的优化难题，并提供了严格的收敛性证明。
*   **模型创新**：首次将非线性变换诱导核范数扩展到四元数张量领域，填补了该方向的空白。
*   **效果显著**：在低采样率下的视觉修复效果（如边缘和色彩还原）明显优于传统线性方法。

### 8. 不足与局限
*   **计算复杂度**：由于实嵌入将矩阵维度扩大了 4 倍（$4n_1 \times 4n_2$），SVD 的计算开销显著增加，处理超大规模高分辨率视频时可能存在效率瓶颈。
*   **实验覆盖面**：测试的数据集较少，缺乏对不同噪声类型（如高斯噪声、脉冲噪声）下的鲁棒性测试。
*   **参数敏感性**：模型涉及多个超参数（$\alpha, \beta, \rho_i$），文中虽给出了取值范围，但未深入讨论参数选择的自适应性。

（完）
