---
title: "RefineAnything: Multimodal Region-Specific Refinement for Perfect Local Details"
title_zh: RefineAnything：旨在实现完美局部细节的多模态区域特定细化
authors: "Dewei Zhou, You Li, Zongxin Yang, Yi Yang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06870v1"
tags: ["query:q1"]
score: 8.0
evidence: 特定区域图像细化与细粒度细节修复
tldr: 针对图像生成中常见的局部细节崩溃（如文字、标志扭曲）问题，本文提出 RefineAnything 框架。该框架采用多模态扩散模型，通过“聚焦并精炼”策略将分辨率预算重新分配至目标区域，并利用混合掩码粘贴技术确保背景像素严格不变。配合新提出的边界一致性损失和 Refine-30K 数据集，该方法在保持背景一致性的同时，显著提升了局部修复的精细度。
selection_source: fresh_fetch
motivation: 现有图像编辑模型在处理局部微小缺陷时，往往无法在修复细节的同时保证非编辑区域的严格不变。
method: 采用“聚焦并精炼”策略对目标区域进行裁剪缩放修复，并引入边界一致性损失以消除粘贴痕迹。
result: 在 RefineEval 基准测试中，RefineAnything 在局部保真度和背景保持方面均表现优异，超越了现有竞争模型。
conclusion: 该研究为高精度局部图像精炼提供了有效的多模态解决方案，并建立了相应的评估基准和数据集。
---

## 摘要
我们将区域特定图像细化（region-specific image refinement）作为一个专门的问题设置提出：给定一张输入图像和一个用户指定的区域（例如涂鸦掩码或边界框），目标是在保持所有非编辑像素严格不变的同时，恢复细粒度的细节。尽管图像生成取得了飞速进展，现代模型仍经常面临局部细节崩溃的问题（例如扭曲的文本、标志和细微结构）。现有的指令驱动编辑模型强调粗粒度的语义编辑，往往要么忽略了细微的局部缺陷，要么在无意中改变了背景，特别是当感兴趣区域仅占固定分辨率输入的一小部分时。我们提出了 RefineAnything，这是一种基于扩散的多模态细化模型，支持基于参考和无参考的细化。基于一个反直觉的观察，即在固定的 VAE 输入分辨率下，裁剪并缩放（crop-and-resize）可以显著改善局部重建，我们提出了 Focus-and-Refine，这是一种聚焦区域的“细化并贴回”策略，通过将分辨率预算重新分配给目标区域来提高细化的有效性和效率，而混合掩码贴回则保证了严格的背景保留。我们进一步引入了边界感知的边界一致性损失（Boundary Consistency Loss），以减少接缝伪影并提高贴回的自然度。为了支持这一新设置，我们构建了 Refine-30K 数据集（包含 2 万个基于参考的样本和 1 万个无参考样本），并引入了 RefineEval，这是一个评估编辑区域保真度和背景一致性的基准。在 RefineEval 上，RefineAnything 相比竞争基线取得了显著改进，并实现了近乎完美的背景保留，为高精度局部细化提供了一个实用的解决方案。项目页面：https://limuloo.github.io/RefineAnything/。

## Abstract
We introduce region-specific image refinement as a dedicated problem setting: given an input image and a user-specified region (e.g., a scribble mask or a bounding box), the goal is to restore fine-grained details while keeping all non-edited pixels strictly unchanged. Despite rapid progress in image generation, modern models still frequently suffer from local detail collapse (e.g., distorted text, logos, and thin structures). Existing instruction-driven editing models emphasize coarse-grained semantic edits and often either overlook subtle local defects or inadvertently change the background, especially when the region of interest occupies only a small portion of a fixed-resolution input. We present RefineAnything, a multimodal diffusion-based refinement model that supports both reference-based and reference-free refinement. Building on a counter-intuitive observation that crop-and-resize can substantially improve local reconstruction under a fixed VAE input resolution, we propose Focus-and-Refine, a region-focused refinement-and-paste-back strategy that improves refinement effectiveness and efficiency by reallocating the resolution budget to the target region, while a blended-mask paste-back guarantees strict background preservation. We further introduce a boundary-aware Boundary Consistency Loss to reduce seam artifacts and improve paste-back naturalness. To support this new setting, we construct Refine-30K (20K reference-based and 10K reference-free samples) and introduce RefineEval, a benchmark that evaluates both edited-region fidelity and background consistency. On RefineEval, RefineAnything achieves strong improvements over competitive baselines and near-perfect background preservation, establishing a practical solution for high-precision local refinement. Project Page: https://limuloo.github.io/RefineAnything/.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RefineAnything**，一个专门用于图像局部细节修复（如文字、标志、人脸）的多模态扩散模型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代图像生成模型（如 Stable Diffusion, Flux）在处理局部微小细节时经常出现“细节崩溃”（如文字扭曲、标志模糊、细微结构断裂）。
*   **研究动机**：
    *   **现有模型局限性**：目前的指令驱动编辑模型多关注粗粒度的语义改变，难以修复极小的局部缺陷。
    *   **分辨率瓶颈**：在固定分辨率（如 1024px）下，局部区域占用的像素极少，导致 VAE 编解码过程中信息损失严重。
    *   **背景漂移**：现有的编辑方法在修改局部时，往往无法保证非编辑区域的像素严格不变。
*   **目标**：在保持背景像素**绝对不变**的前提下，实现高精度的局部细节修复。

### 2. 方法论：核心思想与技术细节
*   **核心策略：Focus-and-Refine（聚焦并精炼）**：
    *   **反直觉观察**：作者发现，将局部区域裁剪并放大到模型输入分辨率（即使不增加新信息），也能显著提升 VAE 的重建质量。
    *   **流程**：1. 根据用户提供的掩码或框确定目标区域；2. 裁剪并缩放至固定分辨率；3. 在该局部视图上进行条件生成；4. 将结果无缝贴回原图。
*   **模型架构**：
    *   基于 **Qwen-Image** 的多模态扩散模型。
    *   **多模态编码**：使用冻结的 Qwen2.5-VL 提取文本指令、输入图像、参考图像和区域掩码的特征。
    *   **双重调节**：结合 VLM 提供的语义特征和 VAE Latent 提供的低级视觉特征，通过 MMDiT 块进行去噪。
*   **关键技术**：
    *   **混合掩码贴回（Blended-mask Paste-back）**：通过对掩码进行膨胀和高斯模糊处理，确保修复区域与原背景自然过渡。
    *   **边界一致性损失（Boundary Consistency Loss）**：在训练时增加编辑边界区域的损失权重，强制模型学习如何消除接缝伪影。

### 3. 实验设计
*   **数据集**：
    *   **Refine-30K**：自建数据集，包含 2 万组“基于参考”的修复对（利用 Gemini 3.0 和 SAM 3 构建）和 1 万组“无参考”修复对。
*   **基准测试（RefineEval）**：
    *   包含 67 个案例，通过 3 种不同退化方法生成了 402 个测试样本。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 3.0 Pro, Gemini 2.5 Flash。
    *   **开源模型**：OmniGen2, BAGEL, Flux Kontext, Qwen-Edit。
*   **评估指标**：
    *   **局部保真度**：MSE, LPIPS, SSIM, DINO/CLIP 相似度。
    *   **背景一致性**：MSE_bg, LPIPS_bg, SSIM_bg。
    *   **主观评估**：利用 MLLM 对视觉质量、自然度、美观度等维度打分。

### 4. 资源与算力
*   **训练细节**：使用 LoRA 调优（秩为 256），仅优化注意力层的投影参数。
*   **超参数**：学习率 2e-4，Batch Size 为 8，训练步数为 20,000 步。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量，但根据模型规模（基于 Qwen-Image）和训练步数推测，通常需要 8 张 A100 或 H100 级别的显卡进行数天的训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量实验**：在 RefineEval 的 402 个样本上进行了详尽测试。
    *   **消融实验**：针对“聚焦策略”和“边界一致性损失”分别做了对比，证明了各组件的必要性。
    *   **定性展示**：提供了大量关于文字、人脸、标志修复的视觉对比图。
*   **充分性评价**：实验设计较为充分，涵盖了有参考和无参考两种实际应用场景。对比基线包含了当前最顶尖的商业和开源模型，评估维度全面（兼顾了局部修复质量与背景不变性）。

### 6. 主要结论与发现
*   **性能领先**：RefineAnything 在局部修复的 MSE 和 LPIPS 指标上显著优于所有基线，且背景保持能力接近完美（MSE_bg 为 0）。
*   **分辨率分配的重要性**：验证了“将分辨率预算集中在目标区域”是解决局部细节崩溃的关键，而非单纯增加模型参数。
*   **无缝集成**：通过边界损失和混合掩码，解决了局部修复常见的“接缝”问题。

### 7. 优点与亮点
*   **实用性极强**：解决了图像生成领域“最后 1%”的细节问题，对电商、广告等高精度需求行业有直接价值。
*   **策略巧妙**：利用简单的“裁剪-缩放-修复-贴回”逻辑，绕过了 VAE 潜空间的压缩瓶颈。
*   **数据贡献**：开源了 Refine-30K 和 RefineEval，填补了局部细化领域缺乏高质量基准的空白。

### 8. 不足与局限
*   **依赖前置步骤**：修复效果高度依赖于用户提供的掩码或边界框的准确性。
*   **语义一致性风险**：在无参考模式下，如果文本指令模糊，模型生成的细节可能与原图整体风格存在细微的语义偏差。
*   **推理开销**：由于需要进行裁剪和多次处理，其推理流程比直接生成单张图像略显复杂。

（完）
