---
title: "Gimbal360: Differentiable Auto-Leveling for Canonicalized $360^\\circ$ Panoramic Image Completion"
title_zh: Gimbal360：用于规范化 $360^\circ$ 全景图像补全的可微分自动调平
authors: "Yuqin Lu, Haofeng Liu, Yang Zhou, Jun Liang, Shengfeng He, Jing Li"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23179v1"
tags: ["query:q1"]
score: 10.0
evidence: 使用扩散模型进行全景图像补全和外扩
tldr: 针对从无位姿透视图像生成360°全景图时存在的几何与拓扑不匹配问题，本文提出了Gimbal360框架。该框架引入规范化视图空间和可微分自动调平模块，无需相机参数即可稳定特征方向。同时，通过在潜空间实施拓扑等变性，解决了等距柱状投影（ERP）的边界不连续问题。配合新发布的Horizon360大规模数据集，该方法在全景补全任务中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的扩散模型在处理透视投影与球面全景之间的几何差异及拓扑不匹配（如边界不连续）时存在困难。
method: 提出了包含规范化视图空间、可微分自动调平模块以及潜空间拓扑等变性约束的生成框架。
result: 在结构一致的360°场景补全任务中取得了最先进的性能，并发布了大规模重力对齐全景数据集Horizon360。
conclusion: 通过显式标准化几何与拓扑先验，Gimbal360有效提升了从单张透视图生成高质量、无缝全景图的能力。
---

## 摘要
扩散模型在 2D 图像外扩（outpainting）方面表现出色，但由于透视投影与球面全景图之间存在几何与拓扑不匹配，将其扩展到基于无位姿透视图像的 $360^\circ$ 全景补全仍具有挑战性。我们提出了 Gimbal360，这是一个显式桥接透视观测与球面全景图的规范化框架。我们引入了一个“规范化视角空间”（Canonical Viewing Space），用于正则化投影几何，并在两个领域之间提供一致的中间表示。为了将自然场景下的输入锚定到该空间，我们提出了一个可微分自动调平模块，在推理时无需相机参数即可稳定特征方向。全景生成还带来了拓扑挑战：标准的生成架构假设图像平面是有界的欧几里得平面，而等距柱状投影（ERP）全景图则具有内在的 $S^1$ 周期性。因此，欧几里得操作会破坏边界连续性。我们通过在潜空间中强制执行拓扑等变性来解决这一不匹配问题，从而保持无缝的周期性结构。为了支持这一方案，我们推出了 Horizon360，这是一个精选的重力对齐全景环境大规模数据集。大量实验表明，通过显式标准化几何与拓扑先验，Gimbal360 在结构一致的 $360^\circ$ 场景补全中实现了最先进的性能。

## Abstract
Diffusion models excel at 2D outpainting, but extending them to $360^\circ$ panoramic completion from unposed perspective images is challenging due to the geometric and topological mismatch between perspective projections and spherical panoramas. We present Gimbal360, a principled framework that explicitly bridges perspective observations and spherical panoramas. We introduce a Canonical Viewing Space that regularizes projective geometry and provides a consistent intermediate representation between the two domains. To anchor in-the-wild inputs to this space, we propose a Differentiable Auto-Leveling module that stabilizes feature orientation without requiring camera parameters at inference. Panoramic generation also introduces a topological challenge. Standard generative architectures assume a bounded Euclidean image plane, while Equirectangular Projection (ERP) panoramas exhibit intrinsic $S^1$ periodicity. Euclidean operations therefore break boundary continuity. We address this mismatch by enforcing topological equivariance in the latent space to preserve seamless periodic structure. To support this formulation, we introduce Horizon360, a curated large-scale dataset of gravity-aligned panoramic environments. Extensive experiments show that explicitly standardizing geometric and topological priors enables Gimbal360 to achieve state-of-the-art performance in structurally consistent $360^\circ$ scene completion.

---

## 论文详细总结（自动生成）

以下是对论文《Gimbal360: Differentiable Auto-Leveling for Canonicalized 360° Panoramic Image Completion》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的扩散模型在处理 2D 图像外扩时表现优异，但在处理 $360^\circ$ 全景补全时面临两大挑战：
    *   **几何不匹配（投影方差）**：现实中的透视图像往往带有随机的俯仰（Pitch）和横滚（Roll）角度。直接投影到球面会导致直线变成复杂的正弦曲线，破坏了扩散模型学习到的结构先验。
    *   **拓扑不匹配（边界断裂）**：全景图（ERP 格式）具有水平方向的周期性（$S^1$ 拓扑），而标准神经网络（如 VAE 和 Transformer）假设图像是有界的欧几里得平面，导致生成的全景图在左右接缝处出现明显的断裂和伪影。
*   **核心目标**：提出一个能够自动调平输入图像、并在规范化空间内生成无缝全景图的框架。

### 2. 方法论
Gimbal360 框架由三个核心组件构成：
*   **规范化视图空间（Canonical Viewing Space）**：定义了一个严格重力对齐的坐标系，使环境地平线与全景图赤道重合。
*   **可微分自动调平模块（DAL）**：
    *   **核心思想**：在推理时无需相机参数，自动将倾斜的输入图像对齐到规范空间。
    *   **技术细节**：使用 SegNeXt 预测稠密对应场，通过一个**可微分 soft-argmin 求解器**将高维流压缩为低秩的刚性变换（3-DOF 旋转）。这种设计充当了几何正则化器，过滤掉扩散梯度中的噪声，防止出现“果冻状”的非刚性变形。
*   **拓扑等变生成（TEG）**：
    *   **循环潜编码**：将 VAE 中的所有水平零填充（Zero-padding）替换为循环填充（Circular padding）。
    *   **孪生位移等变训练（Siamese Shift-Equivariance）**：在训练时引入一致性损失，要求网络对原始输入和水平循环位移后的输入预测出相同的噪声（经位移还原后）。这迫使模型在架构层面尊重 $S^1$ 周期性。
*   **Horizon360 数据集**：构建了一个包含 2 万个高质量、经过严格重力对齐校准的全景场景数据集，为模型提供结构化先验。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：自建的 Horizon360（20k 全景图）。
    *   **测试集**：Structured3D（室内，500 张）和 CVRG-Pano（室外，500 张）。
*   **Benchmark 与对比方法**：
    *   对比了当前最先进的方法：**HunyuanWorld 1.0**、**DiT360** 和 **WorldGen**。
*   **评估指标**：FID、KID（视觉质量）、FAED（全景感知质量）和 CLIPScore（文本一致性）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 GPU**（具体型号未标明，但考虑到 Flux.1 模型规模，推测为 A100 或 H100 级别）。
*   **训练配置**：基于 `Flux.1-fill-dev` 骨干网络，使用 LoRA 进行微调。
*   **时长**：训练了 10,000 个 step，batch size 为 8。
*   **推理**：使用 Euler 采样器，30 步去噪。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了室内、室外以及“野外”（In-the-wild）真实拍摄图像的对比。
*   **消融实验**：详细验证了 DAL（自动调平）和 TEG（拓扑等变）模块的有效性。
*   **充分性评价**：实验设计较为充分。通过定量指标和定性视觉对比，证明了该方法在处理倾斜输入和消除边界接缝方面的显著优势。特别是针对“野外”无参数图像的测试，展示了极强的泛化能力。

### 6. 主要结论与发现
*   **显式几何约束至关重要**：相比于让模型隐式学习投影变换，显式的“自动调平”能显著提升建筑结构的合理性。
*   **架构级拓扑支持优于后处理**：通过孪生训练实现的拓扑等变性，比传统的推理时循环混合（Circular blending）更能保留高频细节并消除接缝。
*   **规范化数据集的价值**：高质量的重力对齐数据是训练稳健全景生成模型的基石。

### 7. 优点
*   **无需相机先验**：DAL 模块解决了现实应用中透视图相机参数缺失的问题。
*   **结构稳定性**：通过 soft-argmin 瓶颈设计，有效解决了扩散模型在空间变换时的不稳定性。
*   **原生无缝生成**：从数学和架构层面解决了全景图的边界连续性问题，而非简单的像素平滑。

### 8. 不足与局限
*   **特定类别畸变**：当输入图像中存在占据主导地位的人物时，生成的全景图中人物可能会出现严重的解剖学畸变（ERP 投影特性导致）。
*   **长焦镜头限制**：对于视场角（FOV）极小的长焦输入，由于缺乏足够的结构上下文，模型生成的全局布局可能存在不一致性。
*   **布局控制力有限**：目前主要依赖数据驱动生成，缺乏对场景布局（如墙壁位置、家具摆放）的显式语义控制。

（完）
