---
title: "Beyond Fixed Inference: Quantitative Flow Matching for Adaptive Image Denoising"
title_zh: 超越固定推理：用于自适应图像去噪的定量流匹配
authors: "Jigang Duan, Genwei Ma, Xu Jiang, Wenfeng Xu, Ping Yang, Xing Zhao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02392v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于自适应图像修复的流生成模型
tldr: 针对扩散和流匹配模型在处理未知及多变噪声时推理轨迹固定的局限性，本文提出了一种定量流匹配框架（QFM）。该方法通过局部像素统计量估计输入图像的噪声水平，并据此动态调整推理过程的起始点、步数和步长策略。这种自适应机制不仅提高了去噪精度，还优化了计算效率，在自然、医学及显微图像等多种场景下展现出强大的鲁棒性和泛化能力。
selection_source: fresh_fetch
motivation: 传统的流匹配模型在面对未知或变化的噪声水平时，由于推理轨迹固定，容易导致去噪质量下降和计算资源浪费。
method: 提出一种定量流匹配框架，通过估计输入噪声水平来动态调整推理轨迹的起始点、积分步数和步长计划。
result: 实验证明该方法在自然、医学和显微图像等多种数据集上均实现了更高的恢复精度和推理效率。
conclusion: 通过将定量噪声估计与自适应流推理相结合，该研究为实现高效且鲁棒的图像去噪提供了一种新颖且通用的解决方案。
---

## 摘要
扩散模型和基于流的生成模型在图像修复方面展现出强大的潜力。然而，在未知且变化的噪声条件下进行图像去噪仍然具有挑战性，因为学习到的向量场在不同噪声水平下可能变得不一致，导致训练与推理不匹配时修复质量下降。为解决这一问题，我们提出了一种用于自适应图像去噪的定量流匹配框架。该方法首先根据局部像素统计信息估计输入噪声水平，然后利用该定量估计值来调整推理轨迹，包括起始点、积分步数和步长计划。通过这种方式，去噪过程能更好地与每个输入的实际损坏程度对齐，在减少轻度损坏图像不必要计算的同时，为重度退化图像提供充分的细化处理。通过将定量噪声估计与噪声自适应流推理相结合，所提方法同时提高了修复精度和推理效率。在自然图像、医学图像和显微图像上的广泛实验证明了其在不同噪声水平和成像条件下的鲁棒性和强大的泛化能力。

## Abstract
Diffusion and flow-based generative models have shown strong potential for image restoration. However, image denoising under unknown and varying noise conditions remains challenging, because the learned vector fields may become inconsistent across different noise levels, leading to degraded restoration quality under mismatch between training and inference. To address this issue, we propose a quantitative flow matching framework for adaptive image denoising. The method first estimates the input noise level from local pixel statistics, and then uses this quantitative estimate to adapt the inference trajectory, including the starting point, the number of integration steps, and the step-size schedule. In this way, the denoising process is better aligned with the actual corruption level of each input, reducing unnecessary computation for lightly corrupted images while providing sufficient refinement for heavily degraded ones. By coupling quantitative noise estimation with noise-adaptive flow inference, the proposed method improves both restoration accuracy and inference efficiency. Extensive experiments on natural, medical, and microscopy images demonstrate its robustness and strong generalization across diverse noise levels and imaging conditions.

---

## 论文详细总结（自动生成）

### 论文总结：超越固定推理——用于自适应图像去噪的定量流匹配（QFM）

#### 1. 核心问题与整体含义（研究动机和背景）
传统的基于扩散（Diffusion）和流匹配（Flow Matching）的生成模型在图像修复中表现优异，但面临一个核心瓶颈：**推理轨迹的固定性**。在实际应用中，图像噪声水平往往是未知且波动的。现有的方法通常假设一个固定的起始点（如纯噪声状态 $t=1$）和固定的积分步数，这导致了两个问题：
*   **性能退化**：当输入图像的实际噪声水平与训练时的假设不匹配时，学习到的向量场会产生歧义，导致过度平滑或伪影。
*   **效率低下**：对于轻微损坏的图像，执行完整的长轨迹推理会造成计算资源浪费；而对于重度损坏的图像，固定步数可能不足以完成高质量修复。
本文的研究动机在于打破这种“固定推理”模式，通过定量估计噪声水平来动态调整流匹配的推理过程。

#### 2. 方法论：核心思想与技术细节
论文提出了 **定量流匹配（Quantitative Flow Matching, QFM）** 框架，其核心流程分为三个阶段：
*   **定量噪声估计**：利用自然图像的局部平滑先验，将图像划分为非重叠的 $2 \times 2$ 像素块。通过计算块内的“全距（Range）”和“中位距（Middle Range）”统计量，并进行随机融合与校准，从而在无需训练的情况下快速、准确地估计全局噪声标准差 $\hat{\sigma}$。
*   **归一化向量场训练**：为了使模型能处理不同强度的噪声，论文引入了归一化时间轴。模型 $v_\theta$ 在训练时以估计的噪声水平为条件，学习将不同噪声强度的图像映射到统一的线性插值路径上，确保向量场在不同噪声状态下的一致性。
*   **自适应流推理**：
    *   **自适应起点**：根据 $\hat{\sigma}$ 将输入图像映射到归一化时间轴上的特定点 $t_{start}$，而非从 $t=1$ 开始。
    *   **自适应步长计划**：采用“由粗到细”的策略。在远离清洁图像的高噪声阶段使用大步长以提高效率，在接近清洁图像的末端阶段使用小步长进行精细修复。

#### 3. 实验设计
论文在三个具有代表性的场景下进行了验证：
*   **合成自然图像去噪**：使用 **PASCAL VOC** 进行训练，在 **BSDS 500** 上测试，涵盖了从低到极高的多种高斯噪声水平。
*   **荧光显微图像去噪**：使用 **FMDD** 数据集，包含真实的泊松-高斯混合噪声。
*   **低剂量 CT 图像去噪**：使用 **Mayo Clinic** 数据集，模拟临床中的低剂量成像条件。
*   **对比方法（Benchmark）**：包括监督学习模型（DVT, TransUNet）、自监督模型（MASH）以及先进的生成式流模型（DeltaFM, CE-CFM, MOTFM, FlowSDF）。

#### 4. 资源与算力
*   **硬件环境**：使用了 4 台 **NVIDIA GeForce RTX 4090** GPU（每台 24GB 显存）。
*   **软件环境**：基于 PyTorch 实现，配备 Intel Xeon Platinum 8358 CPU 和 384GB 内存。
*   **训练细节**：使用 Adam 优化器，学习率为 $1 \times 10^{-4}$，Batch Size 为 4，训练时长为 100 个 Epoch。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多领域覆盖**：不仅在合成噪声上做实验，还深入到了医学影像和生物显微影像等真实复杂噪声场景。
*   **定量与定性结合**：提供了详尽的 PSNR 和 SSIM 指标对比，并展示了大量感兴趣区域（ROI）的放大对比图。
*   **消融实验**：专门设计了消融组，对比了“有无噪声估计”对推理结果的影响，验证了自适应起点的必要性。
*   **泛化性验证**：实验结果显示模型在未见过的噪声水平下依然保持稳定，证明了其归一化向量场设计的有效性。

#### 6. 主要结论与发现
*   **性能领先**：QFM 在所有测试数据集上均显著优于现有的监督和生成式方法。在极高噪声下，其 PSNR 提升尤为明显（最高提升超过 20%）。
*   **效率与质量的平衡**：通过自适应起点，QFM 成功减少了轻噪声图像的推理步数，同时保证了重噪声图像的修复质量。
*   **向量场一致性**：实验证实，显式引入噪声水平作为条件，可以有效解决流匹配在处理变强度噪声时的目标歧义问题。

#### 7. 优点与亮点
*   **即插即用的噪声估计器**：提出的基于 $2 \times 2$ 统计量的估计方法简单且无需预训练，具有极高的实用价值。
*   **推理灵活性**：打破了生成模型必须从“纯噪声”开始的思维定式，实现了按需推理。
*   **跨场景鲁棒性**：在自然图像和专业医疗图像上表现出高度的一致性，证明了该框架的通用性。

#### 8. 不足与局限
*   **全局噪声假设**：目前的噪声估计基于全局统计，对于空间非均匀噪声（如某些传感器产生的局部噪声）可能估计不准。
*   **非平稳噪声挑战**：对于高度信号依赖或非平稳的噪声，简单的线性插值路径可能不是最优的传输方案。
*   **依赖骨干网络**：修复质量上限仍受限于 U-Net 等骨干网络的表达能力以及预训练流模型的质量。

（完）
