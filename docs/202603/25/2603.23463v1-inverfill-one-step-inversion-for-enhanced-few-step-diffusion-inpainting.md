---
title: "InverFill: One-Step Inversion for Enhanced Few-Step Diffusion Inpainting"
title_zh: InverFill：用于增强少步扩散图像修复的一步反演方法
authors: "Duc Vu, Kien Nguyen, Trong-Tung Nguyen, Ngan Nguyen, Phong Nguyen, Khoi Nguyen, Cuong Pham, Anh Tran"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23463v1"
tags: ["query:q1"]
score: 10.0
evidence: 基于扩散模型的图像修复与语义信息注入
tldr: 针对扩散模型在少步数图像修复中因随机噪声初始化导致的语义不匹配和伪影问题，本文提出了InverFill。这是一种单步反转方法，通过将掩码图像的语义信息注入初始噪声，使少步数文本生成图像模型能实现高质量修复。该方法无需重新训练，仅增加极小推理开销，在低采样步数下显著提升了图像和谐度与文本一致性。
selection_source: fresh_fetch
motivation: 现有的少步数扩散模型在修复任务中因随机高斯噪声初始化导致背景与修复区域不协调且存在伪影。
method: 提出一种单步反转技术，将输入掩码图像的语义信息注入初始噪声，并结合混合采样流程利用预训练的少步数文本生成模型进行修复。
result: 实验表明InverFill在低采样步数下显著优于传统的混合采样方法，且在图像质量和文本一致性上可媲美专门的修复模型。
conclusion: InverFill提供了一种高效、无需重训的方案，通过优化初始噪声分布显著增强了少步数扩散模型的图像修复能力。
---

## 摘要
最近基于扩散的模型在图像修复中实现了逼真的效果，但需要大量的采样步骤，限制了其实际应用。少步文本生成图像模型提供了更快的生成速度，但直接将其应用于图像修复会导致背景与修复区域之间的协调性差并产生伪影。我们将此原因归结为随机高斯噪声初始化，在低函数评估次数（NFE）下，这会导致语义失配和保真度降低。为了克服这一问题，我们提出了 InverFill，这是一种专为图像修复定制的一步反演方法，它将输入掩码图像的语义信息注入初始噪声中，从而实现高保真度的少步修复。InverFill 无需训练修复模型，而是利用少步文本生成图像模型，在混合采样流水线中以语义对齐的噪声作为输入，显著改进了原始的混合采样，甚至在低 NFE 下能与专门的修复模型相媲美。此外，InverFill 不需要真实图像监督，且仅增加极小的推理开销。广泛的实验表明，InverFill 能够持续提升基准少步模型的性能，在无需昂贵的重新训练或繁重的迭代优化的情况下，改善了图像质量和文本一致性。

## Abstract
Recent diffusion-based models achieve photorealism in image inpainting but require many sampling steps, limiting practical use. Few-step text-to-image models offer faster generation, but naively applying them to inpainting yields poor harmonization and artifacts between the background and inpainted region. We trace this cause to random Gaussian noise initialization, which under low function evaluations causes semantic misalignment and reduced fidelity. To overcome this, we propose InverFill, a one-step inversion method tailored for inpainting that injects semantic information from the input masked image into the initial noise, enabling high-fidelity few-step inpainting. Instead of training inpainting models, InverFill leverages few-step text-to-image models in a blended sampling pipeline with semantically aligned noise as input, significantly improving vanilla blended sampling and even matching specialized inpainting models at low NFEs. Moreover, InverFill does not require real-image supervision and only adds minimal inference overhead. Extensive experiments show that InverFill consistently boosts baseline few-step models, improving image quality and text coherence without costly retraining or heavy iterative optimization.

---

## 论文详细总结（自动生成）

以下是对论文《InverFill: One-Step Inversion for Enhanced Few-Step Diffusion Inpainting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的少步数（Few-step）扩散模型虽然生成速度快，但在执行图像修复（Inpainting）任务时效果较差。传统的“混合采样”（Blended Sampling）策略在多步模型中表现良好，但在少步模型中，由于每一步的更新幅度过大，初始的**随机高斯噪声**与背景区域之间存在严重的**语义失配**，导致修复区域与背景不协调、产生伪影或模糊。
*   **研究动机**：作者认为修复失败的根源在于初始噪声的随机性。如果能为少步模型提供一个包含背景语义信息的“初始噪声”，就能在极短的步数内实现高质量、高和谐度的修复。

### 2. 核心方法论
InverFill 提出了一种专为修复设计的**一步反演（One-step Inversion）网络**，其核心思想是将掩码图像映射为一个语义对齐的初始噪声潜变量。
*   **核心思想**：不改变预训练的少步生成模型，而是训练一个轻量级的反演网络 $F_\theta$，将“掩码图像 + 文本提示”转化为一个特殊的噪声潜变量，替代纯随机噪声。
*   **关键技术细节**：
    *   **一步反演架构**：继承预训练生成器的权重，通过单次前向传播生成噪声。
    *   **重混合操作（Re-Blending）**：在训练和推理时，将反演网络预测的噪声潜变量中属于掩码的部分替换为随机高斯噪声。这能防止模型直接从掩码区域“偷看”原图信息（信息泄漏），同时保留背景的语义特征。
    *   **高斯正则化损失（Gaussian Regularization Loss）**：通过矩匹配（Moment-matching）损失，强制生成的反演噪声在统计上符合标准高斯分布，确保其与扩散生成器的输入分布一致。
    *   **无图像训练流水线**：利用生成器自身合成“图像-掩码-文本”三元组进行训练，无需真实世界的配对数据集。
    *   **对抗性损失**：引入 LADD 风格的对抗监督，进一步提升生成图像的视觉保真度。

### 3. 实验设计
*   **数据集**：
    *   **BrushBench**：包含 600 张图像及标注掩码，用于评估修复质量。
    *   **MagicBrush**：包含 535 张图像，用于评估图像编辑/修复能力。
    *   **FFHQ & DIV2K**：用于验证在人脸和通用场景下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   **少步模型基准**：SANA-Sprint (0.6B)、SDXL-Turbo。
    *   **对比方法**：原始混合采样（Vanilla BLD）、BrushNet（专门的修复插件）、TurboFill。
    *   **多步模型参考**：SDXL-Inpainting、HD-Painter（30-50步）。
*   **评估指标**：ImageReward (IR)、HPS v2、Aesthetic Score (AS) 以及 CLIP Similarity（文本一致性）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 40GB GPU**。
*   **训练时长**：针对每个模型（如 SANA 或 SDXL）的训练大约需要 **8 到 10 小时**。
*   **推理开销**：InverFill 极其高效，在 A100 上仅增加约 **0.04 到 0.06 秒** 的额外开销。

### 5. 实验数量与充分性
*   **实验规模**：作者在四个主流数据集上进行了定量评估，并提供了大量的定性对比图。
*   **消融实验**：非常充分。分别针对损失函数权重（$\lambda_{noise}, \lambda_{reg}, \lambda_{adv}$）、核心组件（Re-Blending、高斯正则化、对抗损失）以及不同反演方法（如 DDIM Inversion）进行了对比。
*   **客观性**：通过引入增强型提示词（Enhanced Captions）进一步测试了模型在复杂文本下的表现，证明了方法的泛化能力。实验设计涵盖了 DiT（SANA）和 UNet（SDXL）两种主流架构，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：InverFill 显著提升了少步模型在修复任务中的表现，使其在 2-4 步下的效果能媲美甚至超过需要 30 步的专门修复模型（如 HD-Painter）。
*   **语义对齐**：通过反演获得的初始噪声能有效解决背景与修复区域的衔接问题，使生成内容在结构和风格上与原图更统一。
*   **高效性**：作为一种“即插即用”的方法，它不需要对底座模型进行微调，且推理延迟几乎可以忽略不计。

### 7. 优点
*   **极速推理**：实现了真正意义上的实时、高质量图像修复。
*   **架构通用性**：证明了该方法在 DiT 和 UNet 架构上均有效。
*   **训练简便**：采用 Image-free 训练，不需要搜集大规模的真实修复数据集，降低了开发门槛。
*   **即插即用**：可以与现有的修复插件（如 BrushNet）结合，进一步增强其性能。

### 8. 不足与局限
*   **颜色一致性风险**：在某些极端情况下，修复区域与背景之间仍可能存在细微的色差（Failure Cases 中有提及）。
*   **依赖底座模型**：其上限受限于所使用的少步生成模型（如 SDXL-Turbo）本身的生成能力。
*   **潜在滥用风险**：由于修复效果过于逼真且速度极快，可能被用于制造误导性的深度伪造内容。

（完）
