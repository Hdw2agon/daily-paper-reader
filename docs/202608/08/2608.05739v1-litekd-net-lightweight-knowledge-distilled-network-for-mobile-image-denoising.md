---
title: "LiteKD-Net: Lightweight Knowledge-Distilled Network for Mobile Image Denoising"
title_zh: LiteKD-Net：用于移动端图像去噪的轻量级知识蒸馏网络
authors: Zhou Zhiyi
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05739v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于图像修复的自监督噪声模拟
tldr: 针对移动端图像去噪对高质量与低功耗的双重需求，本文提出LiteKD-Net。该方法首先通过物理引导的噪声模拟生成训练数据，随后利用知识蒸馏技术将改进的Real-ESRGAN教师网络的修复能力迁移至基于深度可分离卷积构建的轻量化学生网络。实验证明，该模型在保持优异去噪效果的同时，显著降低了推理延迟，在性能与效率之间取得了极佳平衡。
selection_source: fresh_fetch
motivation: 移动端图像去噪面临计算资源受限及大规模真实配对数据获取困难的挑战。
method: 采用物理引导的噪声模拟生成数据，并通过特征级知识蒸馏将教师网络的去噪能力迁移至轻量化的Lite-RRDB学生网络。
result: 在真实数据集上，LiteKD-Net在推理速度和运行时间上表现优异，且各项指标均优于SwinIR。
conclusion: LiteKD-Net为移动端图像去噪提供了一种兼顾修复质量与计算效率的高效解决方案。
---

## 摘要
移动端图像去噪既需要良好的修复质量，又需要较低的计算成本。此外，收集大规模的低质量-高质量（LQ-GT）干净样本对非常困难。因此，我们提出了 LiteKD-Net，一种用于移动端图像去噪的轻量级知识蒸馏网络。首先，与应用于相机的流水线相比，一种物理引导的噪声模拟流水线通过添加像素串扰来生成成对的训练数据。接着，我们将 Real-ESRGAN 适配于等分辨率去噪，并利用基于深度可分离卷积的 Lite-RRDB 模块构建了一个轻量级的学生网络。第三，应用特征级知识蒸馏，在不引入额外推理成本的情况下，将教师网络的修复能力迁移到学生网络。在真实世界数据集上的实验表明，我们的模型在保持良好修复质量的同时，大幅减少了运行时间并提高了推理速率。与 SwinIR 相比，我们的模型在所有指标上均达到了最优。这些结果表明，LiteKD-Net 在修复质量和计算效率之间实现了极佳的权衡。

## Abstract
Mobile image denoising requires both good restoration quality and low computational cost. In addition, it's annoying to collect large-scale LQ-GT clean pairs. As a result, we propose LiteKD-Net, a lightweight knowledge-distilled network for mobile image denoising. First, a physics-guided noise simulation pipeline generates paired training data by adding pixel crosstalk compared with pipelines applied to cameras. Next, we adapt the Real-ESRGAN to identity-resolution denoising and construct a lightweight Student using Lite-RRDB blocks based on depthwise separable convolutions. Third, feature-level knowledge distillation is applied to transfer the Teacher's restoration capability to the Student without introducing additional inference cost. Experiments on real-world datasets show that our model reaches great reduction in runtime and increase in the inference rate with good restoration quality. Our model also reaches the best in all metrics compared with SwinIR. These results indicate that LiteKD-Net provides a great trade-off between restoration quality and computational efficiency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LiteKD-Net** 的轻量级图像去噪网络，专门针对移动端设备设计。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：移动设备传感器尺寸小、像素密度高，导致成像信噪比低。现有的深度学习去噪模型虽然效果好，但参数量巨大、计算开销高，难以在移动端实时运行。此外，获取大规模真实世界的“噪声-清晰”对齐图像对（LQ-GT）极其困难。
*   **研究动机**：开发一种既能保持高质量修复效果，又能满足移动端低延迟、低功耗要求的去噪方案，并解决训练数据匮乏的问题。

### 2. 核心方法论
论文提出了一个统一的框架，包含三个关键组件：
*   **物理引导的噪声模拟流水线 (NSP)**：
    *   在传统物理噪声模型（光子散粒噪声、读出噪声、行噪声、量化噪声）的基础上，创新性地引入了**像素串扰 (Pixel Crosstalk)** 建模。
    *   通过一个特定的卷积核（Crosstalk Kernel）模拟相邻像素间的电学或光学泄漏，使生成的合成噪声具有空间相关性，更接近真实传感器表现。
*   **Lite-RRDB 架构**：
    *   基于 Real-ESRGAN 的 RRDB 模块进行轻量化改造。
    *   **核心改进**：将标准卷积替换为**深度可分离卷积 (DSConv)**；移除了 RDB 块内部复杂的密集连接（Dense Concatenation），改为顺序执行，以显著降低参数量和计算量（MACs/FLOPs）。
*   **Noise-KD（噪声知识蒸馏）**：
    *   采用“教师-学生”架构。教师网络是高性能的重型 RRDB 网络（已预训练并冻结）。
    *   **特征级蒸馏**：在训练阶段，通过最小化学生网络与教师网络中间特征层之间的均方误差（L2 Loss），将教师的修复能力迁移给轻量化的学生网络。
    *   **优势**：蒸馏仅发生在训练阶段，推理时只需运行学生网络，不增加任何额外开销。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 DocVQA (400张) 和 LSDIR (2000张) 的清晰图像，通过 NSP 流水线生成合成噪声对。
    *   **测试集**：使用真实的移动端去噪数据集 Mobile AI Denoising Dataset 和 MIDD。
*   **Benchmark 与对比方法**：
    *   **基准 (Baseline)**：适配为去噪任务的 Real-ESRGAN（即教师网络）。
    *   **对比对象**：SwinIR（基于 Transformer 的先进修复模型）。
*   **评价指标**：
    *   **质量指标**：PSNR, SSIM（全参考）；LPIPS, DISTS（感知质量）；MUSIQ, CLIP-IQA, BRISQUE（无参考）。
    *   **效率指标**：参数量 (Params)、计算量 (MACs/FLOPs)、推理耗时 (Runtime)、帧率 (FPS)、峰值内存占用。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX 3090 GPU** (24GB 显存)。
*   **训练细节**：
    *   教师网络训练 100,000 次迭代（Adam 优化器，初始学习率 2e-4）。
    *   学生网络分两阶段训练（基础训练 + 知识蒸馏训练），共 100,000 次迭代。
    *   Batch size 为 4，Patch size 为 128×128。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主对比实验和消融实验。
*   **消融实验**：分别验证了“仅使用 Lite-RRDB”和“Lite-RRDB + 知识蒸馏”的效果。
*   **充分性评价**：实验涵盖了从合成数据到真实场景的迁移，评价维度全面（质量+效率）。虽然对比的模型数量不算极多（主要对比了 SwinIR 和 Real-ESRGAN），但针对移动端场景的效率分析非常详尽，客观地展示了模型在性能下降极小的情况下获得的巨大速度提升。

### 6. 主要结论与发现
*   **效率极大提升**：LiteKD-Net 的参数量仅为 1.67M，约为教师网络的 **1/10**；推理速度（FPS）提升了约 **2倍**。
*   **性能表现优异**：在各项图像质量指标上，LiteKD-Net 显著优于 SwinIR，且非常接近重型教师网络（Real-ESRGAN）的水平。
*   **蒸馏的有效性**：消融实验证明，知识蒸馏（Noise-KD）能有效弥补轻量化带来的精度损失，使模型在保持轻量的同时恢复更锐利的边缘和纹理。

### 7. 优点与亮点
*   **物理引导的创新**：NSP 模拟中加入像素串扰建模，提高了合成数据对真实移动端噪声的覆盖能力。
*   **极佳的平衡性**：通过 DSConv 和移除密集连接，精准切中了移动端部署的痛点（内存和算力限制）。
*   **无成本增强**：利用知识蒸馏在不增加推理负担的前提下提升了模型上限。

### 8. 不足与局限
*   **域偏差风险**：NSP 流水线是在近似线性化的 **sRGB 图像**上操作，而非真正的相机 **RAW 数据**。这意味着它没有完全模拟相机内部 ISP（如去马赛克、白平衡、色调映射）的复杂非线性过程。
*   **实验覆盖面**：未在多种不同算力的移动芯片（如骁龙、联发科、苹果 A 系列）上进行实机部署测试，仅提供了基于 GPU 的推理时间参考。
*   **应用限制**：由于模型是针对特定噪声分布训练的，对于极端低光或特定传感器特有的噪声模式，泛化能力可能受限。

（完）
