---
title: "DreamStereo: Towards Real-Time Stereo Inpainting for HD Videos"
title_zh: DreamStereo：迈向高清视频的实时立体修复
authors: "Yuan Huang, Sijie Zhao, Jing Cheng, Hao Xu, Shaohui Jiao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12270v1"
tags: ["query:q1"]
score: 10.0
evidence: 立体视频修复以填充遮挡区域
tldr: 本研究针对立体视频修复中高质量数据集匮乏和计算冗余的挑战，提出了DreamStereo框架。该框架通过梯度感知视差变形（GAPW）和双投影策略（PBDP）在无需立体视频的情况下生成高质量训练对，并利用稀疏感知立体修复（SASI）大幅减少冗余计算。该方法在保持视觉一致性的同时，实现了高清视频的实时修复，推理速度提升10.7倍。
selection_source: fresh_fetch
motivation: 现有的立体视频修复方法受限于高质量数据集的缺失，且对全图进行等同处理导致了严重的计算资源浪费。
method: 提出了GAPW变形技术、PBDP双投影策略以及SASI稀疏感知推理机制，旨在提升修复质量并降低计算开销。
result: 在单张A100 GPU上实现了768x1280高清视频的25 FPS实时处理，推理速度提升10.7倍且质量无损。
conclusion: DreamStereo通过创新的数据生成和稀疏推理技术，有效解决了立体视频修复的效率与一致性难题。
---

## 摘要
立体视频修复旨在用视觉连贯的内容填充扭曲视频中的遮挡区域，同时保持时间一致性，这仍然是一个具有挑战性的开放性问题。待填充区域散布在物体边界沿线，仅占每帧的一小部分，这导致了两个关键挑战。首先，由于高质量立体修复数据集的稀缺，现有方法在这些任务上表现不佳，限制了它们学习有效修复先验的能力。其次，这些方法对图像帧的所有区域进行同等处理，即使大多数像素不需要修改，从而导致了大量的冗余计算。为了解决这些问题，我们引入了三个相互关联的组件。我们首先提出了梯度感知视差扭曲（GAPW），它利用反向扭曲和坐标映射函数的梯度来获得连续的边缘和光滑的遮挡区域。然后，引入了一种基于视差的双重投影（PBDP）策略，该策略结合了 GAPW，在不需要立体视频的情况下生成几何一致的立体修复对和准确的遮挡掩码。最后，我们提出了稀疏感知立体修复（SASI），它减少了超过 70% 的冗余 token，在扩散推理过程中实现了 10.7 倍的加速，并提供了与其全量计算版本相当的结果，从而在单张 A100 GPU 上实现了高清（768 x 1280）视频以 25 FPS 的实时处理。

## Abstract
Stereo video inpainting, which aims to fill the occluded regions of warped videos with visually coherent content while maintaining temporal consistency, remains a challenging open problem. The regions to be filled are scattered along object boundaries and occupy only a small fraction of each frame, leading to two key challenges. First, existing approaches perform poorly on such tasks due to the scarcity of high-quality stereo inpainting datasets, which limits their ability to learn effective inpainting priors. Second, these methods apply equal processing to all regions of the frame, even though most pixels require no modification, resulting in substantial redundant computation. To address these issues, we introduce three interconnected components. We first propose Gradient-Aware Parallax Warping (GAPW), which leverages backward warping and the gradient of the coordinate mapping function to obtain continuous edges and smooth occlusion regions. Then, a Parallax-Based Dual Projection (PBDP) strategy is introduced, which incorporates GAPW to produce geometrically consistent stereo inpainting pairs and accurate occlusion masks without requiring stereo videos. Finally, we present Sparsity-Aware Stereo Inpainting (SASI), which reduces over 70% of redundant tokens, achieving a 10.7x speedup during diffusion inference and delivering results comparable to its full-computation counterpart, enabling real-time processing of HD (768 x 1280) videos at 25 FPS on a single A100 GPU.

---

## 论文详细总结（自动生成）

以下是对论文《DreamStereo: Towards Real-Time Stereo Inpainting for HD Videos》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着AR/VR设备的普及，立体（3D）视频需求激增，但双目拍摄成本高昂。将现有的海量单目视频转换为立体视频（2D-to-3D）是关键，而其中的核心难点在于**立体视频修复（Stereo Inpainting）**——即填充因视差偏移产生的遮挡区域。
*   **核心挑战**：
    1.  **高质量数据匮乏**：现有的自动生成数据集常采用前向变形（Forward Warping），导致物体边缘像素破碎、掩码不连续。
    2.  **计算冗余严重**：遮挡区域通常仅占画面的极小部分（如5%-10%），但现有的扩散模型（Diffusion Models）会对全图所有像素进行等同计算，导致推理速度极慢，无法满足实时高清处理需求。

### 2. 方法论
论文提出了三个相互关联的核心组件：
*   **梯度感知视差变形 (GAPW)**：
    *   **核心思想**：放弃产生空洞和破碎像素的前向变形，采用**反向变形（Backward Warping）**。
    *   **技术细节**：利用坐标映射函数的梯度（雅可比矩阵）来识别遮挡。当映射函数的梯度超过设定阈值时，判定为遮挡区域。这种方法能产生边缘更平滑、几何更连续的遮挡掩码。
*   **基于视差的双重投影 (PBDP)**：
    *   **流程**：利用单目视频 + 深度估计模型，通过两次 GAPW 投影（视角1→视角2，再视角2→视角1）构造训练对。
    *   **意义**：无需真实的立体视频标注，即可从海量单目视频中自动生成高质量的立体修复训练数据。
*   **稀疏感知立体修复 (SASI)**：
    *   **核心思想**：利用修复任务的**空间稀疏性**，只对需要修改的区域进行计算。
    *   **算法流程**：在 Diffusion Transformer (DiT) 结构中，根据下采样的遮挡掩码进行 **Token 选择**。仅保留掩码区域及其周围一圈（通过膨胀算法扩展）的视觉 Token 参与注意力机制计算，剔除超过 70% 的背景冗余 Token。

### 3. 实验设计
*   **数据集**：
    *   **HD-100**：从互联网收集的 100 段 4K 真实视频。
    *   **Dynamic Replica**：具有真实深度信息的合成立体数据集。
    *   **SVD (Spatial Video Dataset)**：使用 Apple Vision Pro 采集的真实空间视频。
*   **对比方法**：
    *   **通用修复模型**：ProPainter (非扩散模型), VACE-1.3B。
    *   **立体生成模型**：StereoCrafter, ZeroStereo, Deep3D, Owl3D (工业界方案)。
*   **评价指标**：PSNR, SSIM, LPIPS（感知质量）。

### 4. 资源与算力
*   **硬件**：训练使用了 **4 张 NVIDIA A100 GPU**。
*   **训练时长**：分为两个阶段。第一阶段（通用修复预训练）10,000 步；第二阶段（伪立体数据微调）2,500 步。
*   **推理性能**：在单张 A100 上，处理 768×1280 高清视频可达 **25 FPS**（实时）；在 RTX 4090 上约为 30ms/帧。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成到真实、从互联网视频到专业空间视频的多种场景。
*   **消融实验**：非常充分。分别验证了：
    1.  数据构建策略（GAPW vs. 传统方法）的有效性。
    2.  Token 保留率对速度与质量的平衡（证明减少 70% Token 质量几乎无损）。
    3.  VAE 蒸馏技术的加速效果。
    4.  大视差（Wide-Baseline）下的鲁棒性。
*   **客观性**：通过与未开源的最新方法（如 SpatialDreamer, M2SVid）进行视觉对比，并提供了失败案例分析，表现出较高的学术客观性。

### 6. 主要结论与发现
*   **性能飞跃**：SASI 机制使 DiT 推理速度提升了 **10.7 倍**，且 PSNR/SSIM 指标下降极小。
*   **实时性**：首次实现了高清（768p）立体视频修复的实时化处理。
*   **数据质量**：证明了高质量的伪立体数据构建比单纯增加模型参数对修复效果的影响更大。

### 7. 优点
*   **极高的实用价值**：解决了扩散模型在视频处理中“慢”的痛点，直接推动了 2D 转 3D 技术的工业化应用。
*   **创新的稀疏计算**：巧妙利用了修复任务的天然特性（局部修改），在不改变模型核心架构的前提下实现了大幅加速。
*   **端到端优化**：从数据生成、模型训练到推理加速（包括 VAE 蒸馏）进行了全链路优化。

### 8. 不足与局限
*   **深度依赖**：修复质量高度依赖于前端深度估计模型的准确性，若深度图有误，修复结果会产生几何畸变。
*   **复杂物理现象处理较弱**：在处理**透明物体**（如玻璃窗）、**镜面反射**以及**极高频纹理**时，仍会出现模糊或扭曲（论文末尾坦诚展示了这些失败案例）。
*   **硬件限制**：虽然实现了实时，但仍需 A100 或 RTX 3090/4090 等高端显卡，在移动端或低功耗设备上的表现尚未讨论。

（完）
