---
title: "From Artefact to Insight: Efficient Low-Rank Adaptation of BrushNet for Scanning Probe Microscopy Image Restoration"
title_zh: 从伪影到洞察：用于扫描探针显微镜图像修复的 BrushNet 高效低秩自适应
authors: "Ziwei Wei, Yao Shen, Wanheng Lu, Ghim Wei Ho, Kaiyang Zeng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14850v1"
tags: ["query:q1"]
score: 9.0
evidence: 图像修复的生成式补全视角
tldr: "本研究针对扫描探针显微镜（SPM）图像中常见的结构化伪影问题，提出了一种基于扩散模型的轻量化修复框架。通过对BrushNet进行低秩自适应（LoRA）微调，仅需训练不到0.2%的参数，即可在有限的实验数据集上实现高效的图像恢复。该方法在SPM InpBench基准测试中显著提升了图像质量，不仅大幅降低了计算资源需求，还成功抑制了生成模型常见的幻觉伪影，为纳米级成像分析提供了高效、可扩展的解决方案。"
selection_source: fresh_fetch
motivation: 传统的SPM图像去伪影方法多局限于简单的去噪或插值，尚未充分探索生成式修复在处理复杂结构伪影方面的潜力。
method: 采用LoRA技术对预训练的BrushNet进行极低参数量的微调，利用从实验扫描中提取的配对数据训练扩散模型以适应科学灰度图像。
result: 该方法在基准测试中将PSNR提升了6.61 dB并将LPIPS减半，在单GPU上即可达到甚至超越全量微调的修复精度。
conclusion: 该轻量化框架实现了SPM图像的高效修复与细节还原，为扩散模型在纳米科学成像领域的广泛应用奠定了基础。
---

## 摘要
扫描探针显微镜（SPM）提供纳米级分辨率，但经常受到结构化伪影的干扰，如扫描线丢失、增益诱导噪声、探针卷积和相位跳变。虽然大多数现有方法将 SPM 伪影去除视为孤立的去噪或插值任务，但生成式修复视角在很大程度上仍未被探索。在这项工作中，我们介绍了一种专为科学灰度图像定制的基于扩散模型的修复框架。通过使用秩受限的低秩自适应（LoRA）微调不到 0.2% 的 BrushNet 权重，我们仅使用从 739 个实验扫描中提取的 7390 对“伪影-清洁”图像对，就完成了一个预训练扩散模型的适配。在我们即将发布的公开 SPM InpBench 基准测试中，与零样本推理相比，LoRA 增强模型将峰值信噪比（PSNR）提升了 6.61 dB，并将学习感知图像块相似度（LPIPS）降低了一半，同时其准确度达到或略微超过了全量重新训练的效果，且仅需在单个 GPU 上训练，而非四个高显存显卡。该方法可推广到包括高度、振幅和相位在内的各种 SPM 图像通道，能够忠实地恢复细微的结构细节，并抑制了从自然图像先验中继承的幻觉伪影。这种轻量级框架实现了对不可替代的 SPM 图像的高效、可扩展恢复，并为扩散模型在纳米级成像分析中的更广泛应用铺平了道路。

## Abstract
Scanning Probe Microscopy or SPM offers nanoscale resolution but is frequently marred by structured artefacts such as line scan dropout, gain induced noise, tip convolution, and phase hops. While most available methods treat SPM artefact removal as isolated denoising or interpolation tasks, the generative inpainting perspective remains largely unexplored. In this work, we introduce a diffusion based inpainting framework tailored to scientific grayscale imagery. By fine tuning less than 0.2 percent of BrushNet weights with rank constrained low rank adaptation (LoRA), we adapt a pretrained diffusion model using only 7390 artefact, clean pairs distilled from 739 experimental scans. On our forthcoming public SPM InpBench benchmark, the LoRA enhanced model lifts the Peak Signal to Noise Ratio or PSNR by 6.61 dB and halves the Learned Perceptual Image Patch Similarity or LPIPS relative to zero-shot inference, while matching or slightly surpassing the accuracy of full retraining, trainable on a single GPU instead of four high-memory cards. The approach generalizes across various SPM image channels including height, amplitude and phase, faithfully restores subtle structural details, and suppresses hallucination artefacts inherited from natural image priors. This lightweight framework enables efficient, scalable recovery of irreplaceable SPM images and paves the way for a broader diffusion model adoption in nanoscopic imaging analysis.

---

## 论文详细总结（自动生成）

以下是对论文《From Artefact to Insight: Efficient Low-Rank Adaptation of BrushNet for Scanning Probe Microscopy Image Restoration》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扫描探针显微镜（SPM）在纳米级成像中极易产生结构化伪影（如扫描线丢失、增益噪声、探针卷积、相位跳变等），严重影响数据分析。
*   **研究动机**：
    *   **现有方法局限**：传统的插值或滤波方法在处理多种复杂伪影并存时效果较差；硬件补偿成本高且无法处理随机事件。
    *   **生成式模型挑战**：扩散模型虽具备强大的修复潜力，但在 SPM 领域面临三大障碍：预训练模型基于自然图像（与灰度科学图像差异大）、对像素级准确性的高要求、以及缺乏大规模标注的 SPM 数据集。

### 2. 方法论
*   **核心思想**：将伪影去除视为广义的“图像补全（Inpainting）”问题，利用 **BrushNet**（一种解耦掩码特征与生成噪声的双分支扩散架构）作为基础，通过 **LoRA（低秩自适应）** 技术进行轻量化微调。
*   **关键技术细节**：
    *   **BrushNet 架构**：采用双分支设计，冻结预训练的 Stable Diffusion 主干，通过额外的分支提取掩码图像特征，避免对主干权重的直接破坏。
    *   **LoRA 微调**：在 BrushNet 的 17 个关键卷积层中插入秩为 8（Rank-8）的适配器，仅训练不到 0.2% 的参数（约 75 万个参数）。
    *   **掩码生成流水线**：结合 APL-SAM（Segment Anything Model 的变体）进行零样本分割，并辅以专家审核和物理启发式过滤，生成高质量的伪影掩码。
    *   **忽略区域损失（Ignore-region Loss）**：在训练中排除不确定像素，使轻度污染的图像也能贡献于背景纹理的学习。

### 3. 实验设计
*   **数据集**：
    *   **训练集 (SPM-InpTrain)**：包含 739 帧实验扫描图，通过随机采样生成 7390 对“伪影-清洁”图像对。
    *   **基准测试集 (SPM-InpBench)**：包含 415 帧未参与训练的地面真值（Ground-truth）图像。
    *   **样本多样性**：涵盖金属有机框架（MOF）、聚合物复合材料、铁电晶体、生物组织（小鼠骨骼）等多种材料。
*   **对比方法**：
    *   **深度学习基准**：零样本推理（Test-only）、全量参数重训练（Full BrushNet retraining）。
    *   **传统非学习基准**：双谐波样条插值（Biharmonic）、Navier-Stokes 扩散、PatchMatch 样本复制、多项式曲面拟合、Telea 快速行进法。
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）和 MSE（均方误差）。

### 4. 资源与算力
*   **全量训练**：需要 4 张 NVIDIA A5000 (24GB) 显卡，耗时约 0.75 小时达到最佳步数。
*   **LoRA 微调**：仅需 **1 张 NVIDIA A5000 (24GB)** 显卡，显存占用低至 7-21 GB，耗时约 3.5-4.5 小时。
*   **参数量**：可训练参数从 6.19 亿减少至 74.8 万，降低了 99.88%。

### 5. 实验数量与充分性
*   **实验规模**：进行了 415 组基准测试对比，以及针对真实实验数据（如 MoS₂、PVDF 复合材料）的定性验证。
*   **消融实验**：专门针对“文本提示词（Prompt）”的影响做了消融研究，对比了空提示、通用提示和领域特定提示的效果。
*   **充分性评价**：实验设计较为充分，不仅有定量指标，还通过视频（SV1-SV6）展示了在不同材料类别上的泛化能力。对比实验涵盖了从传统算法到最先进生成模型的全频谱，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能卓越**：LoRA 增强模型比零样本推理提升了 **6.61 dB 的 PSNR**，并将感知误差（LPIPS）降低了一半。
*   **稳定性更高**：相比全量重训练，LoRA 在小样本数据集上表现更稳定，有效防止了过拟合和“灾难性遗忘”。
*   **抑制幻觉**：适配后的模型能有效抑制扩散模型在处理科学图像时常出现的“自然图像幻觉”（如将纹理误认为叶子或眼睛）。
*   **提示词反作用**：研究发现，详细的领域特定提示词（如描述材料成分）反而会降低修复质量，因为 CLIP 文本编码器与纳米级图像之间存在语义鸿沟。

### 7. 优点
*   **高效轻量**：极低的硬件门槛（单卡可跑），便于在普通实验室推广。
*   **物理保真度**：能够恢复纳米级的边缘和纹理，而非仅仅生成“看起来合理”的图案。
*   **多通道适用**：证明了该方法在高度、振幅、相位等不同 SPM 通道上的通用性。

### 8. 不足与局限
*   **掩码依赖**：修复效果高度依赖于掩码的准确性，目前仍需部分人工干预来精细化掩码。
*   **语义鸿沟**：预训练模型的文本编码器（CLIP）无法理解专业的纳米科学术语，限制了通过文本引导修复的能力。
*   **数据偏差**：虽然涵盖多种材料，但对于极少数极其罕见的特定物理现象，模型可能仍存在泛化风险。

（完）
