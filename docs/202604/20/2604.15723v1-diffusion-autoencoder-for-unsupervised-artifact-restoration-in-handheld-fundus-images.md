---
title: Diffusion Autoencoder for Unsupervised Artifact Restoration in Handheld Fundus Images
title_zh: 用于手持式眼底图像无监督伪影修复的扩散自动编码器
authors: "Mathumetha Palani, Kavya Puthumana, Ayantika Das, Ganapathy Krishnamurthi"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15723v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于伪影修复的无监督扩散自编码器
tldr: 针对手持眼底相机成像中常见的闪光反射、曝光异常和运动模糊等伪影问题，本文提出一种无监督扩散自编码器。该方法通过结合上下文编码器与去噪扩散过程，仅利用高质量台式眼底图像进行训练，即可实现对手持设备捕获的受损图像进行语义修复。实验证明，该方法显著提升了图像质量及后续疾病诊断的准确率，为低成本眼底筛查提供了有效的技术支撑。
selection_source: fresh_fetch
motivation: 手持眼底成像易产生多种复杂且非结构化的伪影，且缺乏成对的训练数据，导致现有修复方法难以适用。
method: 提出一种无监督扩散自编码器，通过上下文编码器学习高质量图像的语义表示，并在推理阶段对受损图像进行去噪修复。
result: "在多种伪影条件下，该方法在未见数据集上的诊断准确率提升至81.17%，且在定量和定性评估中均表现优异。"
conclusion: 本研究验证了利用高质量图像训练的扩散模型在无监督修复手持眼底图像伪影方面的强大潜力。
---

## 摘要
手持式眼底成像设备的出现使得眼科诊断和疾病筛查变得更加便捷、高效且具有成本效益。然而，通过这些设备捕获的图像通常存在诸如闪光反射、曝光变化和运动模糊等伪影，这些伪影会降低图像质量并阻碍下游分析。虽然生成模型在图像修复方面非常有效，但大多数模型依赖于成对监督或预定义的伪影结构，这使得它们难以适应手持式眼底图像中常见的非结构化退化。针对这一问题，我们提出了一种无监督扩散自动编码器，它将上下文编码器与去噪过程相结合，以学习用于伪影修复的具有语义意义的表示。该模型仅在高质量的台式眼底图像上进行训练，并推断修复受伪影影响的手持式采集图像。我们通过定量和定性评估验证了修复效果，结果表明，在未见数据集和多种伪影条件下，诊断准确率提高到了 81.17%。

## Abstract
The advent of handheld fundus imaging devices has made ophthalmologic diagnosis and disease screening more accessible, efficient, and cost-effective. However, images captured from these setups often suffer from artifacts such as flash reflections, exposure variations, and motion-induced blur, which degrade image quality and hinder downstream analysis. While generative models have been effective in image restoration, most depend on paired supervision or predefined artifact structures, making them less adaptable to unstructured degradations commonly observed in handheld fundus images. To address this, we propose an unsupervised diffusion autoencoder that integrates a context encoder with the denoising process to learn semantically meaningful representations for artifact restoration. The model is trained only on high-quality table-top fundus images and infers to restore artifact-affected handheld acquisitions. We validate the restorations through quantitative and qualitative evaluations, and have shown that diagnostic accuracy increases to 81.17% on an unseen dataset and multiple artifact conditions

---

## 论文详细总结（自动生成）

这是一份关于论文《DIFFUSION AUTOENCODER FOR UNSUPERVISED ARTIFACT RESTORATION IN HANDHELD FUNDUS IMAGES》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：手持式眼底成像设备虽然便携且成本低，但拍摄的图像极易产生**非结构化伪影**（如闪光反射、曝光过度/不足、运动模糊），严重影响临床诊断和AI自动分析。
*   **研究动机**：现有的图像修复模型大多依赖“成对数据”（即同一位置的受损图与清晰图）进行监督学习，或者需要预定义的伪影掩码。然而，在实际应用中，获取成对的手持设备受损图与高质量台式设备清晰图几乎是不可能的。
*   **整体含义**：本文旨在开发一种**无监督**的修复框架，仅利用高质量的台式眼底图像进行训练，使其具备修复手持设备图像中复杂伪影的能力，从而提升下游诊断任务的可靠性。

### 2. 论文提出的方法论
核心思想是采用**扩散自编码器（Diffusion Autoencoder, DiffAE）**框架，将生成能力与语义理解相结合。
*   **核心架构**：
    *   **上下文编码器 ($Enc_\phi$)**：将清晰的眼底图像 $x_0$ 映射为一个 512 维的紧凑潜向量 $z$。该向量捕捉全局视网膜结构（如血管布局、视盘位置），作为去噪过程的语义先验。
    *   **去噪 UNet ($f_\theta$)**：在反向扩散过程中，UNet 不仅接收噪声图像 $x_t$，还以潜向量 $z$ 为条件进行预测，确保生成的图像在结构上与原图一致。
*   **训练策略**：模型仅在高质量的台式眼底图像上进行训练，学习“清晰眼底图”的分布。
*   **推理与修复流程**：
    *   **掩码生成**：通过图像处理技术识别手持图像中的伪影区域并生成二值掩码 $M$。
    *   **修复机制**：在推理阶段的每个去噪步长 $t$ 中，利用掩码 $M$ 将已知区域（非伪影区）与生成的区域（伪影修复区）进行融合：$x_t^{inpaint} = M \odot x_t + (1 - M) \odot x_{noisy, 0}$。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：EyePACS 数据集（12,098 张高质量台式健康眼底图）。
    *   **测试集（真实伪影）**：mBRSET 移动眼底图像数据集（197 张包含真实伪影的图像）。
    *   **合成集（定量评估）**：基于 DRIVE 数据集合成的 40 对“伪影-清晰”图像对。
*   **Benchmark 与对比方法**：
    *   对比了 5 种主流模型：**MISF**（多级滤波）、**EdgeConnect**（边缘引导 GAN）、**ShiftNet**（特征重排 CNN）、**DeepFillV2**（门控卷积 GAN）和 **ClarityNet**（基于扩散的修复模型）。
*   **评估指标**：PSNR、SSIM（图像质量）、Dice 系数（血管分割准确度）、AutoMorph 分级（质量评估）以及糖尿病视网膜病变（DR）分类准确率。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H100 GPU (80 GB 显存)**。
*   **软件/参数**：基于 PyTorch 实现；使用 Adam 优化器；初始学习率 $1 \times 10^{-4}$；训练 **150 个 Epoch**；Batch Size 为 4；图像分辨率统一为 512x512。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从合成数据到真实临床数据的多维度验证。
*   **充分性评价**：
    *   **多任务验证**：不仅评估了图像本身的重建质量（PSNR/SSIM），还通过下游任务（血管分割、疾病分级）验证了修复后的临床实用性，这在医学影像论文中是非常充分的。
    *   **消融实验**：通过对比使用“受损图编码”与“插值编码”的效果，验证了上下文编码器 $z$ 对修复质量的关键作用。
    *   **客观性**：使用了由眼科医生验证的真实伪影数据集，并采用了自动化的质量评估工具（AutoMorph），减少了主观偏差。

### 6. 论文的主要结论与发现
*   **性能领先**：在合成集上，该方法的 PSNR 比表现最好的基准模型（DeepFillV2）高出 **1.11 dB**。
*   **临床增益**：修复后的图像使糖尿病视网膜病变（DR）的分类准确率提升至 **81.17%**（未修复前为 77.11%）。
*   **结构保持**：在血管分割任务中，该方法获得了最高的 Dice 分数（0.891），证明其能有效恢复被伪影遮挡的细微血管结构。
*   **无监督优势**：证明了仅靠高质量图像训练的扩散模型，通过引入语义潜向量，可以有效处理未见过的、复杂的真实伪影。

### 7. 优点（亮点）
*   **无需成对数据**：解决了医学影像中高质量标注数据和成对受损数据稀缺的痛点。
*   **语义一致性**：通过 DiffAE 引入的上下文编码器，克服了传统扩散模型在修复大面积伪影时可能出现的“结构断裂”或“内容幻觉”问题。
*   **端到端实用性**：实验直接对接了下游诊断模型，证明了该技术在实际筛查场景中的落地潜力。

### 8. 不足与局限
*   **掩码依赖**：目前的修复过程仍依赖于预先生成的伪影掩码。虽然文中提到使用了图像处理流水线生成掩码，但如果掩码识别不准，修复效果会大打折扣。
*   **计算开销**：扩散模型（Diffusion Models）的推理速度通常较慢，论文未详细讨论在移动端或资源受限环境下进行实时修复的可行性。
*   **极端伪影处理**：对于极度过曝或完全遮挡关键解剖结构（如视盘完全消失）的情况，模型生成的图像虽然“看起来真实”，但其病理特征的真实性仍需更严格的临床验证。

（完）
