---
title: "RIRF: Reasoning Image Restoration Framework"
title_zh: RIRF：推理图像修复框架
authors: "Wending Yan, Rongkai Zhang, Kaihua Tang, Yu Cheng, Qiankun Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09511v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 带有诊断推理的通用图像修复框架
tldr: "针对通用图像恢复（UIR）中缺乏显式诊断推理的问题，本文提出了RIRF（又称R&R）框架。该框架将结构化思维链（CoT）推理引入恢复流程，利用微调的Qwen3-VL作为推理器，对退化类型、程度及场景语义进行诊断。通过将推理结果作为先验并结合强化学习信号指导恢复器，RIRF实现了语义诊断与像素级恢复的紧密耦合，在多个基准测试中达到SOTA性能并提升了过程的可解释性。"
selection_source: fresh_fetch
motivation: 现有的通用图像恢复方法主要关注像素重建，缺乏对退化成分、严重程度及场景语义的显式诊断推理。
method: 提出RIRF框架，利用微调的Qwen3-VL进行结构化CoT推理以生成诊断先验，并利用退化程度作为强化学习信号指导恢复器。
result: 在多个通用图像恢复基准测试中取得了最先进的性能，并为恢复过程提供了独特的可解释性。
conclusion: 将语义诊断推理与像素级恢复紧密结合，是提升通用图像恢复效果和可解释性的有效途径。
---

## 摘要
通用图像修复（UIR）旨在利用统一模型从多样且未知的退化中恢复清晰图像。现有的 UIR 方法主要侧重于像素重建，在修复前往往缺乏对退化构成、严重程度和场景语义的显式诊断推理。我们提出了“推理与修复”（Reason and Restore, R&R），这是一种将结构化思维链（CoT）推理集成到图像修复流水线中的新型框架。R&R 引入了一个显式推理器（通过微调 Qwen3-VL 实现），用于诊断退化类型、量化退化严重程度、推断关键退化相关因素，并描述相关的场景和物体语义。由此产生的结构化推理为修复器提供了可解释且细粒度的诊断先验。为了进一步提高修复质量，推理器生成的量化退化严重程度被用作强化学习（RL）信号，以引导和增强修复器。与现有的将推理与底层视觉任务解耦的基于多模态大语言模型（LLM）的智能体系统不同，R&R 在统一框架中将语义诊断推理与像素级修复紧密耦合。在多个 UIR 基准测试上的广泛实验表明，R&R 在实现最先进性能的同时，为修复过程提供了独特的可解释性。

## Abstract
Universal image restoration (UIR) aims to recover clean images from diverse and unknown degradations using a unified model. Existing UIR methods primarily focus on pixel reconstruction and often lack explicit diagnostic reasoning over degradation composition, severity, and scene semantics prior to restoration. We propose Reason and Restore (R\&R), a novel framework that integrates structured Chain-of-Thought (CoT) reasoning into the image restoration pipeline. R\&R introduces an explicit reasoner, implemented by fine-tuning Qwen3-VL, to diagnose degradation types, quantify degradation severity, infer key degradation-related factors, and describe relevant scene and object semantics. The resulting structured reasoning provides interpretable and fine-grained diagnostic priors for the restorer. To further improve restoration quality, the quantified degradation severity produced by the reasoner is leveraged as reinforcement learning (RL) signals to guide and strengthen the restorer. Unlike existing multimodal LLM-based agentic systems that decouple reasoning from low-level vision tasks, R\&R tightly couples semantic diagnostic reasoning with pixel-level restoration in a unified framework. Extensive experiments across diverse UIR benchmarks demonstrate that R\&R achieves state-of-the-art performance while offering unique interpretability into the restoration process.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reason and Restore: Improving Universal Image Restoration with Chain-of-Thought Reasoning Framework》（简称 R&R 或 RIRF）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的通用图像修复（UIR）方法大多遵循“看图修复”（Look-and-Restore）范式，直接进行像素级重建，缺乏对图像退化成分（如雨、雾、模糊、噪声的混合）、严重程度及场景语义的显式诊断。
*   **研究动机**：真实世界环境复杂，退化往往是多种因素交织的。作者认为，在修复前进行类似人类的“诊断推理”可以显著提升修复的可靠性和适应性。
*   **整体含义**：提出了一种“先推理后修复”（Reason-and-Restore）的框架，将大语言模型（LLM）的思维链（CoT）推理能力引入底层视觉任务，使修复过程具备可解释性和针对性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 UIR 建模为“推理阶段”和“修复阶段”的双阶段过程，并引入强化学习进行对齐。
*   **关键技术细节**：
    *   **推理阶段（Reason Phase）**：基于微调的 **Qwen3-VL**。通过思维链（CoT）输出结构化诊断：1) 退化类型识别；2) 严重程度评分（0-100）；3) 退化特定参数（如大气光、模糊核统计）；4) 场景语义描述（不含退化描述的纯场景信息）。
    *   **修复阶段（Restore Phase）**：基于 **Qwen-Image-Edit**。将推理阶段生成的结构化诊断作为先验输入，引导模型进行像素级重建。
    *   **强化学习对齐（RL with GRPO）**：使用组相对策略优化（GRPO）算法。利用推理器生成的“退化严重程度下降值”作为奖励信号（Reward），强制修复器不仅要像素对齐，还要符合诊断出的退化消除逻辑。
*   **退化模型**：构建了一个半真实世界的退化模型，涵盖雾、运动模糊、雨痕和传感器噪声的随机组合。

### 3. 实验设计
*   **数据集**：
    *   **合成基准**：OTS（户外场景，1861训练/200测试）和 RESIDE（户内场景，1399训练/50测试）。
    *   **真实世界数据集**：作者收集了一个包含 15 个代表性场景、超过 700 张图像的新测试集。
*   **评价指标**：PSNR（峰值信噪比）和 SSIM（结构相似性）。
*   **对比方法**：
    *   传统流水线（3D：去雨+去模糊+去噪组合）。
    *   扩散模型/基础模型：FoundIR、Img2Img-Turbo、Stable Diffusion 3。
    *   多模态大模型：原始 Qwen-Image。

### 4. 资源与算力
*   **硬件**：论文明确提到训练是在 **NVIDIA H200 GPU** 上进行的。
*   **软件/时长**：使用了 Qwen 系列模型作为底座进行微调。文中未明确说明具体的训练总时长或使用的 GPU 确切数量，但考虑到 H200 的使用和 VLM 的微调，算力需求属于较高水平。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流合成基准上进行了定量对比。
    *   在 700+ 张真实图像上进行了定性对比。
    *   **消融实验**：针对推理器微调、评分模块、参数模块、语义模块、强化学习（RL）等 5 个核心组件分别进行了消融测试。
*   **充分性与客观性**：实验设计较为全面，涵盖了从合成到真实的泛化性测试。通过消融实验证明了每个模块的增益，对比基线包含了当前最先进的生成式修复模型，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：R&R 在 OTS 和 RESIDE 数据集上均取得了最高的 PSNR 和 SSIM 分数（如 OTS 达到 19.564 dB）。
*   **推理的重要性**：显式诊断推理（尤其是严重程度评分和参数推断）对保持图像结构和颜色一致性至关重要。
*   **RL 的作用**：通过强化学习对齐诊断意图与修复执行，能显著减少修复过程中的“幻觉”现象。
*   **可解释性**：框架能够输出中间推理文本，让用户了解模型“认为”图像哪里坏了以及坏到什么程度。

### 7. 优点
*   **范式创新**：首次将结构化 CoT 推理作为硬性先验注入 VLM 修复模型，而非简单的提示词工程。
*   **紧耦合设计**：不同于以往将推理与工具调用解耦的 Agent 方案，R&R 实现了语义诊断与像素生成的端到端耦合。
*   **鲁棒性**：在处理极端雾气、重度噪声等复杂混合退化时，表现出比 Stable Diffusion 等模型更好的结构稳定性。

### 8. 不足与局限
*   **计算开销**：由于引入了大型 VLM（Qwen3-VL）作为推理器，推理延迟可能高于传统的轻量级修复网络，难以满足实时处理需求。
*   **退化模型覆盖**：虽然涵盖了四种常见退化，但对于更复杂的现实退化（如压缩伪影、夜间低光照、非均匀模糊等）的覆盖范围仍有待扩展。
*   **依赖底座能力**：修复效果高度依赖于预训练 VLM 的编辑能力，若底座模型在某些纹理生成上有偏差，R&R 也会受限。

（完）
