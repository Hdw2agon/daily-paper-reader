---
title: Amortized Guidance for Image Inpainting with Pretrained Diffusion Models
title_zh: 预训练扩散模型图像修复的摊销引导
authors: "Yilie Huang, Xun Yu Zhou"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13010v1"
tags: ["query:q1"]
score: 10.0
evidence: 基于预训练扩散模型的图像补全
tldr: "本文提出了一种名为AID的图像修复新方法，旨在平衡预训练扩散模型的通用性与推理效率。AID保持预训练骨干网络固定，通过离线训练一个轻量级的可重用引导模块，实现了无需逐样本优化的快速修复。研究团队引入了辅助高斯公式和连续时间演员-评论家算法来训练该模块，在显著提升修复质量的同时，仅增加了不到1%的可训练参数量。"
selection_source: fresh_fetch
motivation: 现有的图像修复方法在专用模型训练的复杂性与推理阶段逐样本优化的低效性之间存在矛盾。
method: 提出摊销引导框架AID，利用辅助高斯公式将确定性引导问题转化为可学习的任务，并采用演员-评论家算法训练轻量级引导模块。
result: 在多个主流数据集和掩码类型下，AID在保持极低计算开销的同时，显著改善了图像修复的质量与生成速度的权衡。
conclusion: AID通过引入离线训练的摊销引导模块，为预训练扩散模型在下游任务中的高效应用提供了一种极具潜力的解决方案。
---

## 摘要
我们研究了基于生成式扩散模型的图像修复。现有方法通常要么训练专门的任务特定模型，要么在部署时针对每个掩码图像分别调整预训练扩散模型。我们引入了一种折中模型，称为扩散摊销修复（Amortized Inpainting with Diffusion, AID），该模型保持预训练扩散主干固定，离线训练一个小型可重用引导模块，并在不同掩码图像间重用，无需逐实例优化。我们将其表述为具有监督终端目标的确定性引导问题。为了使该问题在高维空间中可学习，我们推导了一个辅助高斯公式，并证明解决该随机化问题可以恢复最优确定性引导场。这一桥梁产生了一种基于原则的连续时间 Actor-Critic 算法，用于以完全数据驱动的方式学习引导模块。实验表明，在像素级 EDM 流水线下的 AFHQv2 和 FFHQ 数据集，以及潜在 EDM2 流水线下的 ImageNet 数据集上，AID 在多种掩码类型下均比强大的固定主干和摊销修复基线一致地改进了质量与速度的权衡，同时仅增加了不到 1% 的可训练开销。

## Abstract
We study image inpainting with generative diffusion models. Existing methods typically either train dedicated task-specific models, or adapt a pretrained diffusion model separately for each masked image at deployment. We introduce a middle-ground model, termed Amortized Inpainting with Diffusion (AID), which keeps a pretrained diffusion backbone fixed, trains a small reusable guidance module offline, and then reuses it across masked images without per-instance optimization. We formulate it as a deterministic guidance problem with a supervised terminal objective. To make this problem learnable in high dimensions, we derive an auxiliary Gaussian formulation and prove that solving this randomized problem recovers the optimal deterministic guidance field. This bridge yields a principled continuous-time actor--critic algorithm for learning the guidance module in a fully data-driven manner. Empirically, on AFHQv2 and FFHQ under the pixel EDM pipeline and on ImageNet under the latent EDM2 pipeline, AID consistently improves the quality--speed trade-off over strong fixed-backbone and amortized inpainting baselines across multiple mask types, while adding less than one percent trainable overhead.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AID (Amortized Inpainting with Diffusion)** 的图像修复框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持预训练扩散模型（Diffusion Models）权重不变的情况下，实现高效且高质量的图像修复（Inpainting）。
*   **研究背景**：
    *   **现有路径 A（专用模型）**：如 LaMa、BrushNet。优点是推理快（摊销推理），缺点是需要训练庞大的额外模块或从头训练，成本高。
    *   **现有路径 B（零样本/测试时优化）**：如 RePaint、DPS。优点是直接利用预训练模型，无需训练；缺点是推理极慢，每个样本都需要进行迭代优化或多次采样。
*   **研究动机**：寻找一种“中间地带”——既能保留预训练模型的强大生成能力（固定主干），又能通过离线训练一个极小的模块来实现快速推理，避免逐样本优化。

### 2. 方法论
*   **核心思想**：将图像修复视为一个**受控扩散过程**。通过离线训练一个轻量级的“引导模块”（Guidance Module），在推理时直接输出引导场，从而引导预训练模型生成符合掩码约束的内容。
*   **关键技术细节**：
    *   **确定性引导建模**：将修复任务表述为带有监督终端目标（Terminal Objective）的确定性控制问题。
    *   **辅助高斯公式（理论桥梁）**：这是本文的核心理论贡献。作者证明了在高维空间中，通过引入一个辅助的随机高斯策略问题，其最优策略的“均值”恰好等于原确定性问题的最优引导场。
    *   **连续时间 Actor-Critic 算法**：基于上述理论，开发了一种数据驱动的算法。**Actor** 负责生成引导（即引导模块），**Critic** 负责评估状态价值。训练完成后，仅保留 Actor 用于推理。
*   **算法流程**：
    1.  固定预训练扩散模型。
    2.  离线阶段：在带标签的数据集上，利用 Actor-Critic 框架训练引导模块。
    3.  部署阶段：丢弃 Critic 和随机噪声，使用 Actor 的确定性输出作为引导场，配合预训练模型进行单次采样。

### 3. 实验设计
*   **数据集**：AFHQv2 (64x64)、FFHQ (64x64) 以及 ImageNet (512x512)。
*   **实验场景**：涵盖了像素空间（EDM 流水线）和潜在空间（EDM2 流水线）。
*   **对比方法（Baselines）**：
    *   **固定模型方法**：Unguided、Replacement、MCG、DPS、RePaint（公认的强基线）。
    *   **摊销适配方法**：LatentPaint（针对潜在空间的最新方法）。
*   **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知质量）以及推理时间（NFE/秒）。

### 4. 资源与算力
*   **算力设备**：实验主要在 Apple M2 Max (MPS) 和 Google Colab T4 GPU 上运行。
*   **训练时长**：
    *   AFHQv2/FFHQ：约 5 小时。
    *   ImageNet：约 8 小时（单次过夜运行即可完成）。
*   **参数规模**：引导模块极其轻量，仅约 **409K - 411K** 个参数，占预训练主干网络参数量的 **不到 1%**（在 EDM2 上仅为 0.33%）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，每个设置包含 1000 张测试图。
*   **泛化性测试**：除了训练时使用的随机涂抹掩码（Free-form），还测试了未见过的中心掩码（Center）和条带掩码（Strip），证明了模块的泛化能力。
*   **消融实验**：对控制权重 $\beta$、探索温度 $\lambda$ 以及损失函数权重进行了详细的消融研究。
*   **客观性**：使用了 3 个随机种子取平均值，对比了多种不同原理的基线方法，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能卓越**：AID-18（18 步采样）在所有指标上均优于 RePaint，且推理速度快了约 **10 倍**。
*   **低延迟优势**：即使缩减到 AID-12（12 步采样），其表现依然优于除 RePaint 之外的所有基线方法。
*   **极低开销**：仅需极小的额外参数和计算开销，即可显著提升固定主干网络的修复效果。
*   **跨空间适用**：证明了该理论框架在像素空间和潜在空间扩散模型中同样有效。

### 7. 优点
*   **理论严谨**：通过数学证明建立了随机强化学习与确定性引导之间的等价关系，而非纯启发式设计。
*   **高效摊销**：解决了扩散模型修复任务中“高质量”与“高速度”不可兼得的痛点。
*   **即插即用**：无需微调庞大的预训练模型，保护了原始模型的生成先验。

### 8. 不足与局限
*   **监督依赖**：训练过程需要成对的“干净图像-掩码图像”，属于有监督学习，无法直接在无标注的损坏数据上训练。
*   **任务局限**：目前仅针对图像修复（Inpainting）进行了验证，对于其他逆问题（如超分辨率、去噪等）是否具有同样的适配效率尚待研究。
*   **分辨率上限**：虽然测试了 512x512 分辨率，但在更高分辨率（如 1024+）下的表现和参数需求未做进一步探讨。

（完）
