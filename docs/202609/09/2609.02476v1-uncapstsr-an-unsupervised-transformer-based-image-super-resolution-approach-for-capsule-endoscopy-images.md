---
title: "UnCapsTSR: An Unsupervised Transformer-based Image Super-Resolution Approach for Capsule Endoscopy Images"
title_zh: UnCapsTSR：一种用于胶囊内镜图像的无监督基于Transformer的图像超分辨率方法
authors: "Anjali Sarvaiya, Shubh Kawa, Lalit Agrawal, Jagrit Joshi, Kishor Upla, Kiran Raja"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02476v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于无监督 Transformer GAN 的图像超分辨率
tldr: 针对无线胶囊内镜（WCE）图像分辨率低的问题，本文提出了UnCapsTSR，一种基于Transformer的无监督生成对抗网络（GAN）框架。该方法无需成对的低/高分辨率图像数据，通过引入双边全变分（BTV）损失保证空间连续性，并提出了专门的内镜质量评估指标EndoQM，有效提升了WCE图像的超分辨率重建效果。
selection_source: fresh_fetch
motivation: 解决无线胶囊内镜因硬件限制导致的图像分辨率低、细节缺失问题。
method: 提出一种基于Transformer的无监督GAN框架，利用双边全变分损失实现无需成对数据的超分辨率重建。
result: "在多个数据集上表现优于现有无监督方法，EndoQM指标提升了40%至80%。"
conclusion: UnCapsTSR为无监督内镜图像超分辨率提供了一种高效且通用的解决方案，并为领域评估提供了新标准。
---

## 摘要
无线胶囊内镜（WCE）在穿过患者胃肠道（GI）时捕获并传输视频，用于检查其异常情况。尽管与传统内镜相比具有优势，但WCE受限于胶囊尺寸和无线传输，导致图像分辨率较低。本文提出了UnCapsTSR，这是一个基于Transformer的无监督生成对抗网络（GAN）框架，用于提高低分辨率（LR）WCE图像的空间分辨率。该方法在无需对真实世界LR数据进行显式退化估计的情况下实现了超分辨率（SR），并消除了对真实LR-HR图像对的需求。UnCapsTSR采用双边全变分（BTV）损失函数来确保SR图像的空间连续性。此外，本文还展示了一个从Kvasir Capsule数据集整理出的新数据集，用于训练WCE SR模型。该方法的泛化能力在训练过程中未使用的KID和GIANA数据集上得到了验证。本文还引入了一种新的无参考指标——内镜质量指标（EndoQM），用于对特定领域的WCE数据进行定量评估。实验表明，与现有的无监督SR方法相比，该方法在NIQE、BRISQUE、PIQE和EndoQM指标上均有持续提升。统计评估显示，在所评估的数据集中，EndoQM指标从LR到SR提升了40%至80%。

## Abstract
Wireless Capsule Endoscopy (WCE) captures and streams video while passing through a patient's Gastrointestinal (GI) tract and is used to examine its irregularities. Although advantageous over conventional endoscopy, WCE suffers from limitations related to capsule size and wireless transmission, resulting in images with coarser resolution. This work presents UnCapsTSR, an unsupervised transformer-based Generative Adversarial Network (GAN) framework for improving the spatial resolution of Low-Resolution (LR) WCE images. The proposed method accomplishes SR without explicit degradation estimation of real-world LR data and eliminates the need for true LR-HR pairs. UnCapsTSR employs a Bilateral Total Variation (BTV) loss to ensure spatial continuity in SR images. A newly curated dataset from the Kvasir Capsule dataset is also presented for training WCE SR models. Generalizability is validated on KID and GIANA datasets that are not used during training. A new non-reference metric, Endoscopy Quality Metric (EndoQM), is introduced for quantitative evaluation of domain-specific WCE data. Experiments demonstrate consistent improvement over state-of-the-art unsupervised SR approaches using NIQE, BRISQUE, PIQE, and EndoQM. Statistical evaluation shows 40 to 80 percent improvement in EndoQM from LR to SR across the evaluated datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《UnCapsTSR: An Unsupervised Transformer-based Image Super-Resolution Approach for Capsule Endoscopy Images》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：无线胶囊内镜（WCE）是检查胃肠道异常的重要工具，但受限于胶囊体积、电池容量及无线传输带宽，采集的图像分辨率较低，细节缺失，严重影响临床诊断的准确性。
*   **背景挑战**：传统的超分辨率（SR）方法多依赖监督学习，需要成对的低分辨率（LR）和高分辨率（HR）图像。但在医学领域，获取真实的高质量HR内镜图像极其困难，且模拟退化过程（如双三次下采样）生成的合成数据与真实临床图像存在显著的分布差异，导致模型泛化能力差。

### 2. 方法论：UnCapsTSR
*   **核心思想**：提出一种基于 Transformer 的无监督生成对抗网络（GAN）框架，无需成对数据，直接从 unpaired（非成对）的 LR WCE 图像和 HR 传统内镜图像中学习超分辨率映射。
*   **关键技术**：
    *   **Transformer 生成器**：采用包含局部自注意力（L-SA）和递归泛化自注意力（RG-SA）的架构，有效捕捉全局与局部空间依赖。
    *   **双判别器框架**：引入双判别器（DI 和 DII），分别关注高频细节（边缘/纹理）和整体结构一致性，提升重建图像的真实感。
    *   **BTV 损失函数**：提出双边全变分（Bilateral Total Variation）损失，在保持边缘锐度的同时平滑空间连续性，减少伪影。
    *   **多重损失组合**：结合颜色一致性损失、对抗损失、纹理损失（基于 Gram 矩阵）和 BTV 损失，确保重建图像在色彩、纹理和结构上的临床可靠性。

### 3. 实验设计
*   **数据集**：使用新整理的 Kvasir Capsule 衍生数据集进行训练；使用 KID 和 GIANA 数据集进行跨数据集泛化能力测试。
*   **对比方法**：对比了 SRResCGAN、DUSGAN、dSRVAE、ZSSR、DASR 和 MDASR 等多种主流无监督 SR 方法。
*   **评估指标**：除传统的 BRISQUE、NIQE、PIQE 外，作者专门提出了 **EndoQM**（内镜质量指标），该指标通过在高质量内镜图像上拟合多元高斯分布，更精准地评估医学图像的诊断质量。

### 4. 资源与算力
*   **算力说明**：论文中未明确提及具体的 GPU 型号、数量或具体的训练耗时（仅提到训练迭代次数为 100K 次，Batch size 为 32）。

### 5. 实验数量与充分性
*   **实验覆盖**：包含定性视觉对比、定量指标评估、消融实验（验证损失函数与网络配置有效性）以及统计学显著性检验（ANOVA、Z-test、K-S test）。
*   **客观性**：通过在完全未参与训练的 KID 和 GIANA 数据集上进行测试，验证了模型的泛化性；通过统计学检验，证明了性能提升并非随机波动，实验设计较为严谨且客观。

### 6. 主要结论与发现
*   UnCapsTSR 在所有评估指标上均优于现有的无监督 SR 方法。
*   EndoQM 指标在 LR 到 SR 的转换中实现了 40% 至 80% 的显著提升，证明了该方法在恢复细微血管结构和病变纹理方面的有效性。
*   Transformer 架构在处理医学图像的全局上下文信息方面优于传统的 CNN 架构。

### 7. 优点
*   **无监督范式**：彻底摆脱了对成对 LR-HR 数据的依赖，解决了医学领域数据获取的瓶颈。
*   **领域专用性**：提出的 EndoQM 指标填补了内镜图像质量评估缺乏领域专用标准的空白。
*   **鲁棒性**：BTV 损失的引入有效平衡了图像平滑与边缘保留，提升了临床诊断的可用性。

### 8. 不足与局限
*   **计算复杂度**：Transformer 架构虽然性能优越，但相比轻量级 CNN，其推理和训练的计算开销可能较大，在胶囊内镜实时处理场景下可能存在挑战。
*   **病理特异性**：虽然模型提升了整体清晰度，但文中未深入探讨针对特定病理（如特定类型的息肉或出血点）的诊断准确率提升情况。
*   **硬件依赖**：尽管是无监督方法，但仍需大量高质量的传统内镜图像作为 HR 域的先验，若缺乏高质量 HR 域数据，模型效果可能受限。

（完）
