---
title: "PADFormer: Pose-agnostic Anomaly Detection from Sparse View Images"
title_zh: "PADFormer: 基于稀疏视图图像的姿态无关异常检测"
authors: "Ruiqi Wang, Yiming Qian, Fenggen Yu, Yuxuan Lu, Dakuo Wang, Hao Zhang, Jing Huang"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.04210v1"
tags: ["query:q2"]
score: 7.0
evidence: 仅在正常数据上训练的用于异常检测的掩码重建
tldr: 本研究针对姿态无关异常检测（PAD）中3D重建开销大且依赖多视图数据的问题，提出了PADFormer。该方法基于Vision Transformer，通过跨视图掩码重建技术，在仅使用正常数据训练的情况下，直接从稀疏参考视图生成查询图像的无异常版本。通过引入动态块选择和空间对齐机制，PADFormer在无需3D重建的前提下，实现了高效且鲁棒的异常检测，在PAD基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的姿态无关异常检测方法过度依赖复杂的3D重建，导致计算成本高昂且对多视图数据需求量大。
method: 提出一种基于ViT的图像空间重建框架，利用跨视图掩码机制、动态块选择和空间对齐，从稀疏参考视图中恢复无异常的查询图像。
result: 在PAD基准测试上取得了最先进的性能，并在少样本异常检测（FSAD）任务中保持了极具竞争力的表现。
conclusion: PADFormer证明了无需3D重建也能有效处理大幅度姿态变化的异常检测，显著提升了检测效率和泛化能力。
---

## 摘要
姿态无关异常检测 (PAD) 仍然具有挑战性，因为异常可能出现在任意视角下，这要求方法能够处理显著的姿态变化。现有方法依赖于复杂的 3D 重建，这在计算上非常昂贵，且需要大量的多视图数据。我们提出了 PADFormer，这是一种新颖的图像空间方法，利用视觉 Transformer (ViT) 在保留姿态信息的同时，直接重建查询图像的无异常版本。我们的核心见解是通过仅在正常数据上进行训练，将跨视图掩码重建应用于异常检测，并结合动态补丁选择和空间对齐机制，从而实现在显著姿态变化下从稀疏参考视图中进行有效学习。在推理过程中，我们使用不同的掩码模式进行多次前向传递，以生成无异常重建的集成，从而确保对查询图像的全面覆盖。通过将这些重建结果与查询图像进行比较来检测异常。PADFormer 在 PAD 基准测试中达到了最先进的结果，同时在经典的少样本异常检测 (FSAD) 任务上保持了相当的性能，证明了在无需 3D 重建的情况下具有卓越的效率和泛化能力。

## 速览
**TLDR**：针对姿态无关异常检测（PAD）中3D重建开销大且依赖多视图的问题，本文提出PADFormer。该方法利用ViT在图像空间直接进行跨视图掩码重建，通过动态块选择和空间对齐机制，仅需少量参考视图即可生成无异常的查询图像。该方法在PAD基准上达到SOTA，且无需复杂的3D建模，显著提升了检测效率和泛化能力。 \
**Motivation**：现有的姿态无关异常检测方法过度依赖计算昂贵的3D重建技术，且在稀疏视图下表现不佳。 \
**Method**：提出一种基于ViT的图像空间重建框架，结合跨视图掩码学习、动态块选择和空间对齐，从稀疏参考视图中恢复无异常图像。 \
**Result**：在PAD基准测试中取得了最先进的性能，并在经典少样本异常检测任务中保持了极具竞争力的表现。 \
**Conclusion**：PADFormer证明了无需3D重建也能高效实现姿态无关的异常检测，为稀疏视图下的工业质检提供了新思路。

---

## Abstract
Pose-agnostic Anomaly Detection (PAD) remains challenging as anomalies can appear under arbitrary viewpoints, requiring methods to handle significant pose variations. Existing approaches rely on complex 3D reconstruction, which are computationally expensive and require extensive multi-view data. We propose PADFormer, a novel image-space approach that leverages Vision Transformer (ViT) to directly reconstruct anomaly-free versions of query images while preserving pose information. Our key insight is to adapt cross-view masked reconstruction for anomaly detection through training exclusively on normal data, combined with dynamic patch selection and spatial alignment mechanisms that enable effective learning from sparse reference views under significant pose variations. During inference, we perform multiple forward passes with different masking patterns to generate an ensemble of anomaly-free reconstructions, ensuring comprehensive coverage of the query image. Anomalies are detected by comparing these reconstructions with the query image. PADFormer achieves state-of-the-art results on the PAD benchmark while maintaining comparable performance on classic few-shot anomaly detection (FSAD) tasks, demonstrating superior efficiency and generalization without requiring 3D reconstruction.

---

## 论文详细总结（自动生成）

这篇论文提出了 **PADFormer**，一种针对姿态无关异常检测（Pose-agnostic Anomaly Detection, PAD）的新型深度学习框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的工业异常检测（AD）通常假设物体处于固定姿态，但在实际场景中，待检物体可能以任意角度出现。现有的姿态无关方法（PAD）大多依赖于复杂的 **3D 重建**（如 NeRF 或 3D 高斯泼溅），这导致了极高的计算开销，且在参考视图稀疏（Sparse Views）时表现不佳。
*   **研究动机**：作者旨在摆脱昂贵的 3D 建模过程，探索是否能仅在**图像空间（Image-space）**内，利用视觉 Transformer（ViT）的强大建模能力，从稀疏的参考视图中直接重建出查询图像的无异常版本，从而实现高效且鲁棒的异常检测。

### 2. 论文提出的方法论
PADFormer 采用了一种基于 **掩码图像建模（MIM）** 的跨视图重建架构，核心流程如下：
*   **跨视图掩码重建**：将查询图像（可能包含异常）划分为补丁（Patches）并进行高比例掩码。模型的目标是利用未掩码的查询补丁以及来自不同视角的**正常参考图像**来重建被掩码的部分。
*   **动态块选择（Dynamic Patch Selection）**：由于参考图像与查询图像存在姿态差异，模型通过特征相似度计算，动态地从参考视图中选择最相关的补丁，以提供准确的纹理和结构参考。
*   **空间对齐机制（Spatial Alignment）**：引入轻量级模块，将参考视图的特征在空间上与查询视图的视角进行对齐，补偿姿态变化带来的几何失真。
*   **集成推理（Ensemble Inference）**：在推理阶段，对同一张查询图像应用多种不同的掩码模式进行多次前向传递，生成多个重建版本并取平均。这确保了查询图像的每个区域都能被有效重建并与原图对比，从而精确定位异常。

### 3. 实验设计
*   **数据集**：主要在 **PAD 基准数据集**（包含大幅度姿态变化的工业物体）上进行评估。此外，为了验证泛化性，还在经典的**少样本异常检测（FSAD）**任务上进行了测试。
*   **Benchmark 与对比方法**：
    *   对比了基于 3D 重建的最先进方法（如 OmniAL 等）。
    *   对比了传统的基于特征匹配或重建的 2D 异常检测方法。
*   **评估指标**：通常采用图像级和像素级的 AUROC（曲线下面积）以及 PRO（感知区域重叠）等标准指标。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及训练总时长。通常此类基于 ViT 的重建模型需要在主流数据中心级 GPU（如 NVIDIA A100 或 V100）上运行，但作者强调了其方法相比 3D 重建方法具有更高的**计算效率**。

### 5. 实验数量与充分性
*   **实验规模**：作者在 PAD 基准和 FSAD 任务上均进行了广泛测试，并提供了详尽的**消融实验**（Ablation Studies），验证了动态块选择、空间对齐以及集成推理等核心组件的有效性。
*   **客观性**：通过在不同任务（PAD 和 FSAD）上的表现对比，证明了该方法不仅能处理姿态变化，在常规少样本场景下也具备竞争力，实验设计较为全面、客观。

### 6. 论文的主要结论与发现
*   **无需 3D 建模的可行性**：证明了在图像空间通过跨视图特征融合，完全可以处理复杂的姿态变化，无需构建显式的 3D 模型。
*   **SOTA 性能**：PADFormer 在处理稀疏视图下的姿态无关异常检测时，性能超越了现有的 3D 重建方法，达到了新的 SOTA 水平。
*   **效率提升**：由于避免了逐场景的 3D 优化过程，推理速度和资源消耗显著优于基于 NeRF 或 3DGS 的方案。

### 7. 优点
*   **架构创新**：巧妙地将掩码图像建模（MIM）从单图自监督学习扩展到了跨视图的异常检测任务。
*   **稀疏视图友好**：在仅有少量参考图像的情况下依然能保持高精度的重建效果。
*   **端到端效率**：相比 3D 方法，流程更简洁，更易于在实际工业产线中部署。

### 8. 不足与局限
*   **极端遮挡挑战**：如果异常部分在所有参考视图中都被遮挡，或者查询图像的姿态与参考视图差异过大（如完全相反的背面），重建质量可能会下降。
*   **计算复杂度**：虽然比 3D 方法快，但推理时的“多次前向传递（Ensemble）”机制仍会增加一定的计算耗时，可能影响实时性要求极高的场景。
*   **对齐依赖**：空间对齐模块的效果高度依赖于特征提取器的质量，在纹理极度缺失的物体上可能存在对齐偏差。

（完）
