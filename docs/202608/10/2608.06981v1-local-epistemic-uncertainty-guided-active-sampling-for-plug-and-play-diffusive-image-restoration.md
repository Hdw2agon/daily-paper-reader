---
title: Local Epistemic Uncertainty Guided Active Sampling for Plug-and-play Diffusive Image Restoration
title_zh: 局部认知不确定性引导的主动采样用于即插即用扩散图像修复
authors: "Jiaqi Zhang, Zheng Pang, Rongrong Gao, Qiyuan Zhang, Yang Yang"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06981v1"
tags: ["query:q3"]
score: 9.0
evidence: 即插即用的扩散图像修复
tldr: 针对扩散模型在图像修复中因固定约束和均匀步长导致的结构失真及计算冗余问题，本文提出LEADer框架。该框架利用局部认知不确定性，在空间上动态调节先验强度以平衡细节与伪影，在时间上通过不确定性迹实现自适应轨迹剪枝以加速收敛。作为即插即用的方案，LEADer在提升多种SOTA方法性能的同时显著降低了采样耗时。
selection_source: fresh_fetch
motivation: 现有的扩散模型图像修复方法因采用固定的数据约束和均匀步长，难以应对非均匀退化且存在计算冗余。
method: 提出LEADer框架，利用像素级不确定性动态调节空间先验强度，并基于不确定性迹实现时间维度的自适应采样轨迹剪枝。
result: 实验表明该方法能显著提升多种主流DMIR模型的修复质量，并在极低内存开销下大幅缩短采样时间。
conclusion: LEADer通过引入局部认知不确定性指导，为实现高效且高质量的即插即用扩散模型图像修复提供了理论支撑和实践方案。
---

## 摘要
扩散模型在图像修复任务中展现出了卓越的有效性。然而，在引导图像重建时，现有的基于扩散模型的图像修复（DMIR）方法通常依赖于固定的数据约束和均匀的步长，从而忽略了生成过程的动态特性。这种僵化的设计使得模型容易受到空间非均匀退化的影响，从而导致结构失真和细节丢失。同时，均匀步长引入了计算冗余，而简单的步长缩减策略往往会累积近似误差。为了解决这些局限性，我们提出了一种局部认知不确定性引导的主动采样框架（LEADer）。在空间域中，LEADer 利用像素级不确定性来动态调节零空间（null space）内的先验强度，从而有效平衡了细节保持与伪影抑制。在时间域中，它通过不确定性迹（uncertainty trace）量化采样稳定性，以实现自适应轨迹剪枝，从而加速收敛。理论证明表明，我们的框架实现了严格的数据一致性，而轨迹剪枝策略具有确定的误差界限，从而保证了跳跃采样下的稳定收敛。值得注意的是，我们的即插即用方法可以无缝集成到各种 DMIR 基准模型中。大量实验表明，LEADer 提升了多种最先进 DMIR 方法的性能，同时在内存开销可忽略不计的情况下显著减少了采样时间。代码可在 https://github.com/JiaqiZhang-Sengoku/LEADer 获取。

## Abstract
Diffusion models have demonstrated remarkable effectiveness in image restoration tasks. However, when guiding image reconstruction, existing Diffusion Model-based Image Restoration (DMIR) methods typically rely on fixed data constraints and uniform step sizes, thereby overlooking the dynamic nature of the generative process. Such rigid designs render the models vulnerable to spatially non-uniform degradations, thus resulting in structural distortions and loss of fine details. Meanwhile, uniform step sizes introduce computational redundancy, whereas naïve step reduction strategies tend to accumulate approximation errors. To address these limitations, we propose a Local Epistemic Uncertainty Guided Active Sampling framework (LEADer). In the spatial domain, LEADer leverages pixel-wise uncertainty to dynamically modulate the prior strength within the null space, which effectively balances detail preservation and artifact suppression. In the temporal domain, it quantifies sampling stability via the uncertainty trace to enable adaptive trajectory pruning, thereby accelerating convergence. Theoretical proofs demonstrate that our framework achieves strict data consistency, while the trajectory pruning strategy admits a deterministic error bound, thereby guaranteeing stable convergence under skip sampling. Notably, our plug-and-play method can be seamlessly integrated into various DMIR baselines. Extensive experiments show that LEADer improves the performance of multiple state-of-the-art DMIR methods, while significantly reducing sampling time with negligible memory overhead. Code is available at https://github.com/JiaqiZhang-Sengoku/LEADer.

---

## 论文详细总结（自动生成）

这篇论文提出了 **LEADer**，一个旨在提升即插即用扩散图像修复（DMIR）效率与质量的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的基于扩散模型的图像修复方法在引导生成过程中，通常采用**固定的数据约束强度**和**均匀的采样步长**。
*   **研究背景**：
    *   **空间局限性**：图像退化往往是非均匀的，固定约束会导致在某些区域出现结构失真或细节丢失（无法平衡数据一致性与生成先验）。
    *   **时间冗余性**：均匀步长忽略了生成过程中的动态稳定性，导致在图像结构已趋于稳定的后期仍进行冗余计算，且简单的减步策略会累积近似误差。
*   **整体含义**：通过引入“局部认知不确定性”（Local Epistemic Uncertainty），实现空间上的动态先验调节和时间上的自适应轨迹剪枝。

### 2. 方法论：LEADer 框架
*   **核心思想**：利用扩散模型在推理过程中的认知不确定性作为“指示灯”，指导模型在何时、何地应该更多地依赖原始数据或生成先验。
*   **关键技术细节**：
    *   **局部认知不确定性估计**：通过量化扩散模型输出的方差（或利用得分函数的波动）来评估像素级的不确定性。
    *   **空间维度：动态先验调节**：在零空间（Null Space）投影过程中，根据像素级不确定性动态调整权重。高不确定性区域（模型不确定的地方）加强数据约束，低不确定性区域则允许生成更多细节。
    *   **时间维度：自适应轨迹剪枝**：引入“不确定性迹”（Uncertainty Trace）。当连续步间的不确定性变化低于设定阈值时，认为采样轨迹已进入稳定状态，从而跳过后续冗余步骤（剪枝）。
    *   **理论保证**：论文证明了该框架能保持严格的数据一致性，并为轨迹剪枝提供了确定的误差界限，确保了跳跃采样下的收敛稳定性。

### 3. 实验设计
*   **数据集**：涵盖了主流的基准数据集，包括 **FFHQ**（人脸）、**ImageNet**（通用物体）、**LSUN-Churches** 和 **LSUN-Bedrooms**（场景）。
*   **应用场景（任务）**：超分辨率（SR）、图像修复（Inpainting）、彩色化（Colorization）、高斯去模糊（Deblurring）。
*   **对比方法（Benchmark）**：对比了多种最先进的（SOTA）DMIR 模型，如 **DDRM, PiGDM, MCG, DPS, ResDiff** 等。
*   **评估指标**：PSNR、SSIM（保真度）、LPIPS（感知质量）、FID（生成分布质量）以及采样耗时。

### 4. 资源与算力
*   **算力说明**：论文摘要和提取文本中未明确给出具体的 GPU 型号、数量或总训练时长。
*   **开销备注**：作者强调该方法具有“**可忽略不计的内存开销**”（negligible memory overhead），因为它主要是在推理阶段对现有模型进行引导，而非重新训练大型模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种以上的图像修复任务。
    *   在多个不同分布的数据集上进行了验证。
    *   进行了详尽的**消融实验**，验证了空间调节和时间剪枝各自的贡献。
*   **充分性评价**：实验设计较为充分，通过与多种不同类型的 DMIR 基准模型集成，证明了其作为“即插即用”模块的普适性和鲁棒性。对比实验遵循了学术界的标准流程，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：LEADer 能显著提升现有 DMIR 方法的图像重建质量，减少伪影并保留更多真实细节。
*   **效率优化**：通过自适应轨迹剪枝，在不损失（甚至提升）质量的前提下，大幅缩短了采样时间。
*   **普适性**：证明了认知不确定性是引导扩散模型修复任务的一个极佳度量工具，且该框架具有很强的兼容性。

### 7. 优点
*   **即插即用**：无需重新训练模型，可直接应用于现有的预训练扩散模型。
*   **自适应性强**：解决了非均匀退化下的处理难题，比固定参数的方法更灵活。
*   **理论与实践结合**：不仅有直观的启发式设计，还提供了关于误差界限和一致性的数学证明。
*   **高效性**：在提升质量的同时降低了计算成本，具有实际应用价值。

### 8. 不足与局限
*   **超参数依赖**：虽然实现了自适应，但初始的不确定性阈值等参数可能仍需针对不同任务进行微调。
*   **极端退化挑战**：在极高噪声或极低分辨率下，认知不确定性的估计准确度可能会下降，从而影响引导效果。
*   **实时性限制**：尽管减少了步数，但扩散模型本身的迭代特性决定了其距离真正的实时处理仍有距离。

（完）
