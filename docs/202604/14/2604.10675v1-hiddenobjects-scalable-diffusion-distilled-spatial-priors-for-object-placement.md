---
title: "HiddenObjects: Scalable Diffusion-Distilled Spatial Priors for Object Placement"
title_zh: HiddenObjects：用于物体放置的可扩展扩散蒸馏空间先验
authors: "Marco Schouten, Ioannis Siglidis, Serge Belongie, Dim P. Papadopoulos"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10675v1"
tags: ["query:q1"]
score: 9.0
evidence: 基于扩散的图像补全流水线用于物体移除
tldr: 本研究提出了一种从扩散模型中蒸馏隐式知识以学习物体放置空间先验的自动化框架。通过扩散修复流水线评估真实背景上的密集物体放置，构建了包含2700万标注的大规模数据集HiddenObjects，解决了人工标注规模受限和传统方法伪影导致捷径学习的问题。该方法在图像编辑任务中表现优于人工标注，且蒸馏后的轻量化模型推理速度提升了23万倍，实现了高效且精准的物体放置预测。
selection_source: fresh_fetch
motivation: 现有的物体放置方法受限于人工标注的规模瓶颈或自动生成数据中的伪影干扰，难以获得高质量且可扩展的空间先验。
method: 利用扩散修复流水线在真实背景上评估密集物体放置，构建大规模数据集并将其蒸馏至轻量化模型中。
result: 实验显示该方法在图像编辑任务中显著优于人工标注和现有基准，且推理速度比原始扩散模型快23万倍。
conclusion: 通过从扩散模型中蒸馏空间先验，可以有效克服数据规模限制，为自然场景下的物体放置提供高效、准确的解决方案。
---

## 摘要
我们提出了一种方法，通过蒸馏文本条件扩散模型中编码的隐式放置知识，来学习自然场景中物体放置的显式、类别条件空间先验。先前的工作要么依赖于手动标注的数据（其规模本质上受限），要么依赖于基于修复（inpainting）的物体移除流水线（其产生的伪影会导致捷径学习）。为了解决这些局限性，我们引入了一个全自动且可扩展的框架，该框架利用基于扩散的修复流水线，在高质量真实背景上评估密集的物体放置。利用该流水线，我们构建了 HiddenObjects，这是一个包含 2700 万个放置标注的大规模数据集，涵盖了 2.7 万个不同场景的评估，并针对不同图像和物体类别提供了排序后的边界框插入。实验结果表明，在下游图像编辑任务中，我们的空间先验优于稀疏的人类标注（VLM-Judge 评分为 3.90 对 2.68），并且显著超过了现有的物体放置基准模型和零样本视觉语言模型。此外，我们将这些先验蒸馏到一个轻量级模型中，以实现快速的实际推理（速度提升 230,000 倍）。

## Abstract
We propose a method to learn explicit, class-conditioned spatial priors for object placement in natural scenes by distilling the implicit placement knowledge encoded in text-conditioned diffusion models. Prior work relies either on manually annotated data, which is inherently limited in scale, or on inpainting-based object-removal pipelines, whose artifacts promote shortcut learning. To address these limitations, we introduce a fully automated and scalable framework that evaluates dense object placements on high-quality real backgrounds using a diffusion-based inpainting pipeline. With this pipeline, we construct HiddenObjects, a large-scale dataset comprising 27M placement annotations, evaluated across 27k distinct scenes, with ranked bounding box insertions for different images and object categories. Experimental results show that our spatial priors outperform sparse human annotations on a downstream image editing task (3.90 vs. 2.68 VLM-Judge), and significantly surpass existing placement baselines and zero-shot Vision-Language Models for object placement. Furthermore, we distill these priors into a lightweight model for fast practical inference (230,000x faster).

---

## 论文详细总结（自动生成）

以下是对论文《HiddenObjects: Scalable Diffusion-Distilled Spatial Priors for Object Placement》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在自然场景中，物体的放置遵循特定的空间规律（如：披萨在桌上而非地板上）。现有的物体放置（Object Placement）研究面临两大瓶颈：
    1.  **人工标注不可扩展**：手动标注成本高且标注稀疏（通常一张图仅一个位置），难以捕捉完整的概率分布。
    2.  **自动生成存在偏差**：传统方法通过“先移除物体再修复背景”来获取训练数据，但修复过程留下的视觉伪影（Artifacts）会让模型学会走捷径（Shortcut Learning）。
*   **核心目标**：利用扩散模型（Diffusion Models）中蕴含的隐式空间知识，通过全自动流水线提取并蒸馏出显式的、类别相关的空间先验，构建大规模、高质量的物体放置数据集。

### 2. 方法论
*   **核心思想**：将物体放置视为“图像补全”任务。通过在背景图的不同位置尝试“插入”物体，并利用验证器和评分器筛选出合理的放置点。
*   **关键技术流程**：
    1.  **空间先验提取（Extraction）**：
        *   **修复器（Inpainter）**：使用 Qwen-Image 结合 ControlNet，根据给定的边界框（BBox）和类别标签在背景图中生成物体。
        *   **验证器（Verifier）**：使用 Grounded-SAM-2 检测生成的物体是否符合预期类别及位置。
        *   **评分器（Ranker）**：使用 ImageReward 模型对验证通过的图像进行人类偏好评分，量化放置的合理性。
    2.  **早期停止加速（Speedup）**：通过测量扩散模型在去噪过程中有条件分支与无条件分支的噪声差异（L1-difference），提前终止无效的生成尝试，实现 2.4 倍的生成加速。
    3.  **模型蒸馏（Distillation）**：
        *   将提取的 2700 万个标注蒸馏到一个轻量级的 **DETR 风格 Transformer 模型**中。
        *   该模型以背景图和目标类别为输入，直接预测多个候选 BBox 及其合理性得分。

### 3. 实验设计
*   **数据集与场景**：
    *   **背景图**：来自 Places365 数据集，涵盖 126 种场景类别。
    *   **物体类别**：选取 COCO 中的 50 种常见物体类别。
    *   **构建规模**：HiddenObjects 数据集包含 2.7 万个场景和 2700 万个评估过的放置标注。
*   **Benchmark 与对比方法**：
    *   **基准数据集**：OPA (Object Placement Assessment)。
    *   **对比对象**：包括零样本（Zero-shot）大模型（如 Qwen2.5-VL-72B、LLaVA-1.5）、专用放置模型（PlaceNet、GracoNet、TerseNet）以及人工标注。
    *   **评价指标**：mAP、IoU、以及使用 ImgEdit-Judge（基于 Qwen2.5-VL）进行的图像编辑质量评分（1-5分）。

### 4. 资源与算力
*   **算力设备**：文中提到在 **NVIDIA H100 GPU** 上进行推理测试。
*   **效率对比**：
    *   **原始流水线**：处理一张图像约需 14.49 分钟，占用 65.9 GB 显存。
    *   **蒸馏后的模型**：处理一张图像仅需 **3.77 毫秒**，占用 188.9 MB 显存。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，训练最多 200 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的消融实验，对比了不同的修复器（SDXL, FLUX, Qwen）、评分器（CLIP, Aesthetic, ImageReward）对先验质量的影响。
*   **充分性评价**：实验设计较为全面。不仅在自有数据集上验证，还在第三方数据集（OPA）上进行了交叉验证。通过 ImgEdit-Judge 引入了自动化的主观质量评估，增强了结果的客观性。此外，还对数据集的中心偏差（Center Bias）和尺度分布进行了深入的统计分析。

### 6. 主要结论与发现
*   **超越人工标注**：在下游图像编辑任务中，使用该方法生成的放置建议，其图像合成质量（3.90）显著高于基于人工标注的建议（2.68）。这表明人工标注在物体放置任务中具有模糊性，且不一定适合生成模型。
*   **蒸馏效果显著**：蒸馏后的轻量化模型在保持高精度的同时，实现了 **230,000 倍的推理加速**。
*   **分布特性**：HiddenObjects 数据集相比于传统的检测数据集（如 COCO），具有更均匀的空间分布（缓解了摄影师的中心构图偏差），但在极小物体尺度上仍受限于扩散模型的生成能力。

### 7. 优点
*   **可扩展性极强**：全自动流程，无需人工干预即可生成千万级标注。
*   **高质量背景**：避免了“物体移除”带来的背景伪影，确保了背景的真实性。
*   **密集先验**：相比于人工的稀疏标注，该方法能提供密集的概率热力图，更符合物体放置的本质分布。

### 8. 不足与局限
*   **尺度偏差**：由于扩散模型在极小区域内生成质量较差，导致数据集在远景/极小物体上的标注相对稀疏。
*   **继承偏差**：空间先验源于扩散模型的预训练数据，可能会继承互联网图像中的摄影美学偏差（如特定物体的常规构图）。
*   **类别限制**：虽然流水线是通用的，但蒸馏模型目前主要针对选定的 50 个类别进行了优化，对于长尾类别的泛化性有待进一步验证。

（完）
