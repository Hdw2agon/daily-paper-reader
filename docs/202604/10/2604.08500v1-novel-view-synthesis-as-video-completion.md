---
title: Novel View Synthesis as Video Completion
title_zh: 将新视角合成视为视频补全
authors: "Qi Wu, Khiem Vuong, Minsik Jeon, Srinivasa Narasimhan, Deva Ramanan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08500v1"
tags: ["query:q1"]
score: 9.0
evidence: 将合成任务表述为视频补全任务
tldr: 本研究将稀疏视角合成（NVS）重新定义为低帧率视频补全任务，旨在利用视频扩散模型中蕴含的隐式多视角知识。针对视频模型依赖帧序而NVS输入无序的矛盾，提出了FrameCrafter框架，通过移除时间位置编码等架构修改实现置换不变性。该方法仅需少量监督即可使视频模型适应稀疏视角合成，在多个基准测试中展现出极具竞争力的性能。
selection_source: fresh_fetch
motivation: 现有的单图扩散模型缺乏多视角一致性，而视频模型天然具备建模空间一致性的潜力，更适合处理稀疏视角合成任务。
method: 提出FrameCrafter框架，通过采用每帧潜编码并移除时间位置嵌入，使视频模型能够处理无序的输入图像集并保持置换不变性。
result: 实验结果表明，经过修改的视频模型能够有效忽略时间维度，在稀疏视角合成基准上取得了优异的性能表现。
conclusion: 将NVS建模为视频补全任务是可行的，通过简单的架构调整即可释放视频扩散模型在多视角生成方面的强大能力。
---

## 摘要
我们利用视频扩散模型解决稀疏新视角合成（NVS）问题；给定场景的 $K$（约 5 张）多视图图像及其相机位姿，我们预测目标相机位姿下的视图。许多先前的方法利用了通过扩散模型编码的生成式图像先验。然而，在单张图像上训练的模型缺乏多视图知识。相反，我们认为视频模型已经包含了隐式的多视图知识，因此应该更容易适配于 NVS。我们的核心见解是将稀疏 NVS 建模为低帧率视频补全任务。然而，一个挑战是稀疏 NVS 是在无序输入集上定义的，这些输入通常过于稀疏，无法形成有意义的顺序，因此模型应该对该输入集的排列具有不变性。为此，我们提出了 FrameCrafter，它通过若干架构修改，将视频模型（自然地在连贯帧序列上训练）适配为排列不变的 NVS，这些修改包括逐帧潜变量编码和移除时间位置嵌入。我们的结果表明，视频模型可以通过极少的监督轻松地被训练为“忘记”时间，从而在稀疏视图 NVS 基准测试中产生具有竞争力的性能。项目页面：https://frame-crafter.github.io/

## Abstract
We tackle the problem of sparse novel view synthesis (NVS) using video diffusion models; given $K$ ($\approx 5$) multi-view images of a scene and their camera poses, we predict the view from a target camera pose. Many prior approaches leverage generative image priors encoded via diffusion models. However, models trained on single images lack multi-view knowledge. We instead argue that video models already contain implicit multi-view knowledge and so should be easier to adapt for NVS. Our key insight is to formulate sparse NVS as a low frame-rate video completion task. However, one challenge is that sparse NVS is defined over an unordered set of inputs, often too sparse to admit a meaningful order, so the models should be $\textit{invariant}$ to permutations of that input set. To this end, we present FrameCrafter, which adapts video models (naturally trained with coherent frame orderings) to permutation-invariant NVS through several architectural modifications, including per-frame latent encodings and removal of temporal positional embeddings. Our results suggest that video models can be easily trained to "forget" about time with minimal supervision, producing competitive performance on sparse-view NVS benchmarks. Project page: https://frame-crafter.github.io/

---

## 论文详细总结（自动生成）

这篇论文由卡内基梅隆大学（CMU）的研究团队提出，探讨了如何将预训练的视频扩散模型转化为高效的新视角合成（NVS）模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **核心问题**：如何在给定极少数（约5张）稀疏输入图像及其相机位姿的情况下，合成高质量、几何一致的新视角图像。
*   **研究动机**：
    *   传统的基于图像扩散模型（如 Stable Diffusion）的方法缺乏内在的多视角一致性，通常需要海量的多视角标注数据来学习几何关系。
    *   视频模型在预训练阶段已经通过连续帧学习了丰富的空间一致性和视角变换规律（即隐式的“世界模型”知识）。
    *   **挑战**：视频模型通常依赖严格的帧顺序（时间轴），而稀疏视角合成的输入是无序的集合。

### 2. 方法论：FrameCrafter 框架
*   **核心思想**：将稀疏视角合成重新表述为一个“低帧率视频补全”任务，通过轻量级修改使视频模型具备**置换不变性（Permutation Invariance）**。
*   **关键技术细节**：
    *   **独立逐帧 VAE 编码**：放弃视频 VAE 的因果 3D 卷积（会产生时间压缩和顺序依赖），将每个输入视角视为独立的“单帧视频”进行编码，确保潜空间表示不随输入顺序改变。
    *   **相机几何注入**：使用 **Plücker 射线图（Ray maps）** 表示相机位姿，并通过 **Pixel Unshuffle** 下采样技术保留精确的几何对应关系，将其与图像潜变量在通道维度拼接。
    *   **移除时间位置编码**：在扩散 Transformer（DiT）中移除 3D 旋转位置嵌入（RoPE）的时间组件，使模型仅依赖相机几何而非帧索引来识别视角。
    *   **查询中心坐标系**：将所有相机位姿归一化到以“目标查询视角”为中心的坐标系，消除对“第一张图”的依赖。
*   **算法流程**：输入 $K$ 张图及其位姿 $\rightarrow$ 独立 VAE 编码 $\rightarrow$ 拼接射线图 $\rightarrow$ 进入去噪 Transformer（仅训练 LoRA 参数） $\rightarrow$ 预测目标帧潜变量 $\rightarrow$ VAE 解码输出。

### 3. 实验设计
*   **数据集**：
    *   **DL3DV-10K**：大型真实场景数据集，用于训练（仅用 1K 场景）和基准测试。
    *   **Mip-NeRF 360**：用于评估模型在无界场景下的泛化能力。
*   **对比方法**：
    *   **SEVA**（当前 SOTA，基于图像扩散模型，使用了 80K 场景训练）。
    *   **EscherNet**（基于图像扩散模型）。
    *   **Aether**（基于视频模型的相机控制生成）。
    *   **LVSM / E-RayZer**（基于回归的前馈模型）。
*   **评价指标**：PSNR、SSIM、LPIPS 和 DreamSim（感知相似度）。

### 4. 资源与算力
*   **GPU 型号与阶段**：
    *   **低分辨率阶段**（192x336）：使用 **NVIDIA A6000** GPU。
    *   **高分辨率阶段**（480x832）：使用 **NVIDIA H100** GPU。
*   **训练效率**：采用 **LoRA**（秩 r=32）进行微调，仅更新约 1% 的参数。相比 SEVA 使用了 80,000 个场景，本方法仅需 **1,000 个场景**（数据量缩减 80 倍）即可达到同等甚至更优性能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 6 视图和 3 视图设置下的定量对比。
    *   **消融实验**：详细验证了独立编码、Pixel Unshuffle、仅监督目标帧、移除时间 RoPE 等设计的必要性。
    *   **置换鲁棒性测试**：通过 10 次随机打乱输入顺序，证明了模型对输入顺序的极低敏感度（标准差极小）。
    *   **数据缩放分析**：展示了从 20 个场景到 2000 个场景的性能增长曲线。
*   **充分性评价**：实验设计非常严谨，特别是针对“置换不变性”的专项测试，有力支撑了论文的核心论点。

### 6. 主要结论与发现
*   **视频模型是更强的先验**：视频预训练模型比图像模型更易适配 NVS，且对多视角标注数据的依赖程度大幅降低。
*   **架构修改的有效性**：简单的架构调整（如移除时间 RoPE）足以让视频模型“忘记”时间，转而利用几何信息。
*   **扩展性**：随着视频底座模型（如从 CogVideoX 升级到 Wan2.1）的增强，NVS 的性能会自然提升。

### 7. 优点
*   **极高的数据效率**：在极小规模的数据集上实现了 SOTA 级别的性能。
*   **置换不变性设计**：巧妙地解决了视频模型应用于无序集合时的归纳偏置问题。
*   **即插即用**：该框架可以轻松适配各种先进的开源视频扩散底座。

### 8. 不足与局限
*   **计算开销**：尽管微调是轻量级的，但推理时仍需运行大型视频 Transformer（如 14B 参数），计算成本较高。
*   **静态场景限制**：目前主要针对静态场景，尚未充分探讨如何利用视频模型的动态先验处理 4D（动态）新视角合成。
*   **位姿依赖**：依然依赖精确的相机参数，对于无位姿输入的场景无法直接适用。

（完）
