---
title: "DR.WILSS: Diffusion-Based Replay for Weakly Supervised Continual Semantic Segmentation"
title_zh: DR.WILSS：基于扩散模型的弱监督持续语义分割重放方法
authors: "Leon Arthur Marx, Francesco Barbato, Matteo Caligiuri, Pietro Zanuttigh"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18444v1"
tags: ["query:q1"]
score: 8.0
evidence: 采用自修复和基于扩散的生成重放技术
tldr: DR.WILSS 针对弱监督持续语义分割（WILSS）中的灾难性遗忘问题，提出了一种基于扩散模型的生成式重放框架。该方法利用语言引导扩散过程，结合自修复与 LoRA 微调技术生成高质量重放数据，在无需存储原始数据的情况下，有效保留了旧类别的知识，实现了训练复杂度与推理精度的最优平衡。
selection_source: fresh_fetch
motivation: 解决弱监督持续语义分割中因学习新概念而导致的旧知识灾难性遗忘问题。
method: 利用语言引导的扩散模型进行生成式重放，并通过自修复和 LoRA 微调技术提升重放数据的质量与统计一致性。
result: 在多个基准测试中达到最先进性能，且无需存储原始训练数据或依赖高资源消耗的外部工具。
conclusion: DR.WILSS 为现实场景下的持续学习提供了一种高效且高性能的解决方案。
---

## 摘要
弱监督类增量语义分割（WILSS）旨在通过多个步骤训练分割模型，每个步骤仅利用图像级监督信息学习新概念。我们提出了 DR.WILSS，这是一种利用基于扩散的生成式重放来解决持续学习中灾难性遗忘问题的创新方法。我们的框架利用语言线索引导扩散过程，采用自修复（self-inpainting）和正则化技术高效生成重放数据，从而辅助学习过程。通过生成高质量的重放数据，可以在持续更新过程中保留先前学习到的类别信息，这是增量学习场景中的一个关键挑战。为了进一步使重放数据的统计特征与训练样本对齐，我们对生成模型应用了 LoRA 技术。实验结果表明，该方法在多个基准测试和生成架构上均达到了最先进的性能，同时避免了存储训练数据以及在训练过程中使用额外的资源密集型工具。所提出的技术在训练复杂度和推理精度之间实现了最优平衡，使 DR.WILSS 成为现实应用中极具前景的解决方案。

## Abstract
Weakly supervised class-incremental semantic segmentation (WILSS) aims to train a segmentation model over multiple steps, each introducing new concepts to be learned with only image-level supervision. We introduce DR$.$WILSS, an innovative approach to address catastrophic forgetting in continual learning using diffusion-based generative replay. Our framework leverages language clues to guide the diffusion process, employing self-inpainting and regularization techniques to efficiently produce replay data, aiding the learning process. By generating high-quality replay data, the information from previously learned classes can be preserved during continual updates, a critical challenge in incremental learning scenarios. To further align the statistics of replay data with those of training samples, we apply LoRAs to the generative model. Experimental results demonstrate state-of-the-art performance across multiple benchmarks and generative architectures, while avoiding storage of training data and the use of additional resource-demanding tools during training. The proposed technique enables an optimal tradeoff between training complexity and inference-time accuracy, making DR$.$WILSS a promising solution for real-world applications.

---

## 论文详细总结（自动生成）

以下是对论文 **《DR.WILSS: Diffusion-Based Replay for Weakly Supervised Continual Semantic Segmentation》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：语义分割在自动驾驶、医疗影像等领域至关重要，但获取像素级标注成本极高。弱监督持续语义分割（WILSS）旨在仅通过图像级标签，在多个增量步骤中学习新概念。
*   **核心挑战**：在持续学习过程中，模型在学习新类别的同时会发生“灾难性遗忘”（Catastrophic Forgetting），即丢失对旧类别的识别能力。
*   **动机**：现有方法要么依赖存储旧数据（隐私风险），要么依赖外部网络数据（不可控），要么性能受限。本文旨在提出一种无需存储原始数据、无需外部数据集的生成式重放框架。

### 2. 方法论：DR.WILSS
*   **核心思想**：利用预训练的扩散模型（如 Stable Diffusion）生成高质量的合成图像作为“重放数据”，以模拟旧类别的分布，从而在训练新任务时维持旧知识。
*   **关键技术**：
    *   **语言引导生成**：利用图像级标签生成的文本描述（Caption）作为扩散模型的输入，通过打乱词序避免偏见。
    *   **LoRA 微调**：在每个增量步骤训练 LoRA 适配器，使生成模型能够对齐当前任务的统计特征，提升合成图像的真实性。
    *   **一致性检查**：引入后处理机制，通过模型预测验证生成的图像是否包含预期的类别，若不满足则重采样（最多 4 次）。
    *   **混合训练**：在训练新任务时，将真实数据与合成的重放数据按比例（$\rho=0.5$）混合，并结合知识蒸馏（KD）损失函数。

### 3. 实验设计
*   **数据集与场景**：主要使用 Pascal VOC 2012 和 COCO-to-VOC (C2V) 基准。
*   **协议**：涵盖了“不相交”（Disjoint）和“重叠”（Overlapped）两种协议，以及 10-5、15-5、10-1、15-1 等多种增量步长设置。
*   **对比方法**：与 WILSON、RaSP、WISH、Teddy 等弱监督持续学习方法进行对比，并设置了使用真实数据重放的“上限”作为参考。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA Titan RTX GPU。
*   **训练时长**：LoRA 训练约 30 分钟；生成初始类别的图像集约 18 小时；分割模型训练时间因重放数据引入而翻倍（从 40 分钟增加到 80 分钟）。

### 5. 实验数量与充分性
*   **充分性**：实验覆盖了多种增量场景，并进行了详细的消融实验（如重放比例 $\rho$ 的影响、不同扩散模型 SD2.1 vs SDXL 的对比、LoRA 的有效性）。
*   **客观性**：通过固定随机种子（42）并进行多次运行以计算标准差，评估了结果的统计显著性，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能表现**：DR.WILSS 在多个基准测试中达到了 SOTA（最先进）水平，显著优于现有的无内存（Memory-free）方法。
*   **有效性**：证明了生成式重放能有效缓解灾难性遗忘，且在单类增量（10-1, 15-1）场景下表现出极强的鲁棒性。
*   **模型选择**：发现并非模型越大越好，SD2.1 在该任务上的表现优于 SDXL，说明语义内容的准确性比视觉美感对分割任务更重要。

### 7. 优点
*   **隐私友好**：无需存储原始训练图像，完全自包含。
*   **灵活性**：支持单类增量，不依赖外部网络数据或复杂的标注工具。
*   **性能平衡**：在训练复杂度和推理精度之间取得了良好的权衡。

### 8. 不足与局限
*   **计算开销**：生成重放数据和重采样过程增加了训练前的准备时间。
*   **生成质量依赖**：尽管有重采样机制，但扩散模型生成的图像仍可能存在伪影或类别缺失，这会直接影响分割模型的训练质量。
*   **复杂场景限制**：对于非对象中心化的复杂场景（如 ADE20K），由于图像级标签的局限性，该方法可能难以直接应用。

（完）
