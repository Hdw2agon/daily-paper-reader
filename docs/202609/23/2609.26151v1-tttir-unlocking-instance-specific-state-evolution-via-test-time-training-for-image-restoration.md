---
title: "TTTIR: Unlocking Instance-Specific State Evolution via Test-Time Training for Image Restoration"
title_zh: TTTIR：通过测试时训练解锁图像恢复中的实例特定状态演化
authors: "Kaihang Zheng, Jun Li, Hang Guo, Hongyu Chi, Zimo Liu, Tao Dai, Jinpeng Wang, Yaowei Wang"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.26151v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于图像修复的自监督测试时训练
tldr: 针对图像修复中退化模式高度依赖输入的问题，现有模型依赖静态参数难以适应。本文提出TTTIR框架，将图像修复重构为实例特定的状态演化过程。通过引入渐进式状态序列生成（PSSG）构建目标状态，并利用状态转换演化（STE）进行测试时训练（TTT），实现了动态的实例级特征恢复，在多个基准测试中显著优于现有SOTA模型。
selection_source: fresh_fetch
motivation: 现有图像修复模型多采用静态全局参数，难以有效处理高度依赖输入的复杂退化模式。
method: 提出TTTIR框架，通过PSSG构建空间-频率目标状态，并利用STE机制进行面向修复任务的测试时训练以实现特征演化。
result: 实验表明TTTIR在多个图像修复基准测试中均超越了当前最先进的模型，且具备良好的计算扩展性。
conclusion: TTTIR通过测试时训练实现了实例特定的状态演化，为解决复杂图像退化问题提供了一种高效且灵活的新范式。
---

## 摘要
由于现实世界中退化问题的多样性及其对输入的强依赖性，图像恢复本质上极具挑战性。尽管 Transformer 和状态空间模型等近期架构推动了该领域的发展，但它们主要依赖于静态的、全局共享的参数，难以完全适应实例特定的退化模式。测试时训练（TTT）为生成数据依赖型算子提供了一种有前景的范式，然而其标准的自监督内循环缺乏将退化特征向清晰结构转换所需的明确指导。为了解决这一问题，我们提出了 TTTIR，这是一个将图像恢复重新表述为实例特定状态演化过程的新型框架。具体而言，我们设计了渐进式状态序列生成（PSSG）来构建互补的空频目标状态（定义恢复目标），并设计了状态转换演化（STE）通过面向恢复的 TTT 内循环来适配轻量级转换算子（确定特征演化方式）。大量实验表明，TTTIR 在多个图像恢复基准测试中始终优于最先进的模型，在实现动态实例特定恢复的同时具备良好的计算可扩展性。代码已开源至 https://github.com/Elysiaaaaaaaa/TTTIR.git。

## Abstract
Image restoration is inherently challenging due to the diverse and highly input-dependent nature of real-world degradations. While recent architectures like Transformers and state-space models have advanced the field, they predominantly rely on static, globally shared parameters, which struggle to fully accommodate instance-specific degradation patterns. Test-Time Training (TTT) offers a promising paradigm for generating data-dependent operators, yet its standard self-supervised inner loop lacks the explicit guidance required to transition degraded features toward clean structures. To address this, we propose TTTIR, a novel framework that reformulates image restoration as an instance-specific state evolution process. Specifically, we design Progressive State Sequence Generation (PSSG) to construct complementary spatial-frequency target states (defining what to recover), and State Transition Evolution (STE) to adapt lightweight transition operators via a restoration-oriented TTT inner loop (determining how the features should evolve). Extensive experiments demonstrate that TTTIR consistently outperforms state-of-the-art models across multiple image restoration benchmarks, achieving dynamic instance-specific recovery with favorable computational scalability. The code is available at https://github.com/Elysiaaaaaaaa/TTTIR.git.

---

## 论文详细总结（自动生成）

以下是对论文《TTTIR: Unlocking Instance-Specific State Evolution via Test-Time Training for Image Restoration》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：图像恢复（如去雨、去雾、低光照增强）面临退化模式高度多样且依赖输入的挑战。
*   **痛点**：现有的主流架构（CNN、Transformer、状态空间模型）主要依赖**静态、全局共享的参数**。这种参数共享机制难以针对每一张图像的特定退化模式进行精细化调整，限制了恢复效果的上限。
*   **核心含义**：论文提出将图像恢复重构为“实例特定的状态演化过程”，利用测试时训练（TTT）动态生成数据依赖型算子，从而实现针对不同输入图像的自适应恢复。

### 2. 方法论：TTTIR 框架
*   **核心思想**：通过“渐进式状态序列生成”定义恢复目标，通过“状态转换演化”实现特征的动态演化。
*   **关键技术**：
    *   **PSSG（渐进式状态序列生成）**：包含空间状态构造器（PSSC）和频率状态构造器（PFSC）。PSSC 通过递归演化扩大感受野，PFSC 利用小波变换（DWT）提取多频段信息。两者融合生成目标状态序列 $\mathcal{T}$，为恢复提供“方向性指导”。
    *   **STE（状态转换演化）**：这是该框架的核心。它将 TTT 的内循环重构为“面向恢复的优化”。给定当前状态 $Z_l$ 和目标状态 $T_{l+1}$，通过内循环更新轻量级转换算子 $\Phi$ 的权重，使特征向目标状态演化。
*   **算法流程**：输入特征 $\rightarrow$ PSSG 生成目标序列 $\rightarrow$ STE 进行内循环优化（更新算子） $\rightarrow$ 算子作用于特征 $\rightarrow$ 残差融合得到下一阶段状态 $\rightarrow$ 最终输出。

### 3. 实验设计
*   **数据集与场景**：涵盖三大类任务：
    *   **低光照增强**：LOL-v1, LOLv2-real, LOLv2-synthetic。
    *   **去雨**：Rain13K（训练），Test100, Rain100H, Rain100L, Test1200, Test2800（测试）。
    *   **去雾**：RESIDE-6K, Haze4K。
*   **对比方法**：与多种基于 CNN、Transformer 和 Mamba 的 SOTA 方法（如 Retinexformer, RetinexMamba, Wave-Mamba, CPRAformer 等）进行了对比。

### 4. 资源与算力
*   **硬件配置**：所有实验均在 **NVIDIA RTX 3080 Ti** GPU 上进行。
*   **训练细节**：文中未明确提及具体的训练时长，但提供了参数量（约 0.787M）和 FLOPs（18.71G），强调了该模型在保持高性能的同时具备良好的计算扩展性。

### 5. 实验数量与充分性
*   **实验覆盖**：论文进行了 12 个基准数据集的测试，涵盖了多种退化类型，实验覆盖面广。
*   **消融实验**：详细分析了 PSSG、STE、PSSC、PFSC 各模块的贡献，以及演化阶段数 $L$ 对性能的影响，实验设计客观且逻辑严密。
*   **公平性**：对比实验中使用了相同的数据集和评价指标（PSNR/SSIM），并对比了参数量和计算复杂度，体现了公平性。

### 6. 主要结论与发现
*   **动态适应性**：TTTIR 证明了通过测试时训练更新算子，能够有效捕捉实例特定的退化特征，优于静态参数模型。
*   **渐进式恢复**：从低频结构恢复到高频细节重建的演化路径是有效的，且 $L=2$ 是性能与计算效率的最佳平衡点。
*   **性能领先**：在多个基准测试中，TTTIR 在 PSNR 和 SSIM 指标上均达到或超过了当前最先进的方法。

### 7. 优点
*   **创新性**：将 TTT 范式引入图像恢复，并将其重构为“状态演化”过程，视角独特。
*   **轻量高效**：相比于动辄数千万参数的 Transformer 模型，TTTIR 参数量极小（<1M），且在不同分辨率下表现出更好的计算扩展性。
*   **可解释性**：通过频率能量分布和算子更新轨迹分析，直观展示了模型如何根据不同输入进行动态调整。

### 8. 不足与局限
*   **内循环开销**：尽管算子轻量，但测试时的内循环优化（Inner-loop optimization）仍会增加推理阶段的计算负担，可能不适用于对实时性要求极高的场景。
*   **超参数依赖**：演化阶段数 $L$ 和目标状态融合权重 $\alpha$ 等超参数可能需要针对不同任务进行微调。
*   **泛化性边界**：虽然在多种退化任务上表现良好，但对于极端未见过的退化类型，其自监督内循环的指导效果仍有待进一步验证。

（完）
