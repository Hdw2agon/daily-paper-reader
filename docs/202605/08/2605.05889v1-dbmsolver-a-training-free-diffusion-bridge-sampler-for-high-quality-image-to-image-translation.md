---
title: "DBMSolver: A Training-free Diffusion Bridge Sampler for High-Quality Image-to-Image Translation"
title_zh: DBMSolver：一种用于高质量图像到图像翻译的免训练扩散桥采样器
authors: "Sankarshana Venugopal, Mohammad Mostafavi, Jonghyun Choi"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05889v1"
tags: ["query:q4"]
score: 8.0
evidence: 用于修复和图像翻译的免训练扩散采样器
tldr: 扩散桥模型（DBM）在图像到图像转换中表现出色，但采样速度慢且计算开销大。本文提出 DBMSolver，一种无需训练的扩散桥采样器，通过指数积分器利用 DBM 底层 SDE 和 ODE 的半线性结构，提供高效的一阶和二阶解。该方法在减少高达 5 倍采样步数的同时显著提升了生成质量，在修复、风格化等任务中达到了新的 SOTA 效率与质量平衡，增强了其实际应用价值。
selection_source: fresh_fetch
motivation: 现有的扩散桥模型在图像转换任务中采样速度缓慢，通常需要大量的函数评估次数才能生成高质量图像。
method: 提出一种无需训练的采样器 DBMSolver，利用指数积分器处理 DBM 的半线性结构，实现高效的一阶和二阶数值求解。
result: "在减少 5 倍采样步数的情况下，DBMSolver 显著提升了图像质量，例如在 DIODE 数据集上 20 步采样时的 FID 降低了 53%。"
conclusion: DBMSolver 为扩散桥模型设定了新的效率与质量权衡标准，显著提升了高分辨率图像转换任务的实用性。
---

## 摘要
基于扩散的图像到图像（I2I）翻译在生成高保真图像方面表现出色，但在最先进的扩散桥模型（DBM）中面临采样速度慢的问题，通常需要数十次函数评估（NFE）。我们提出了 DBMSolver，这是一种免训练的采样器，它通过指数积分器利用 DBM 底层 SDE 和 ODE 的半线性结构，从而产生高效的一阶和二阶解。这使 NFE 减少了多达 5 倍，同时提升了生成质量（例如，在 20 次 NFE 下，DIODE 数据集上的 FID 相比二阶基线下降了 53%）。在分辨率高达 256x256 的图像修复、风格化和语义到图像任务上的实验表明，DBMSolver 建立了新的 SOTA 效率-质量权衡，使其具备了实际应用价值。我们的代码已在 https://github.com/snumprlab/dbmsolver 公开。

## Abstract
Diffusion-based image-to-image (I2I) translation excels in high-fidelity generation but suffers from slow sampling in state-of-the-art Diffusion Bridge Models (DBMs), often requiring dozens of function evaluations (NFEs). We introduce DBMSolver, a training-free sampler that exploits the semi-linear structure of DBM's underlying SDE and ODE via exponential integrators, yielding highly-efficient 1st- and 2nd-order solutions. This reduces NFEs by up to 5x while boosting quality (e.g., FID drops 53% on DIODE at 20 NFEs vs. 2nd-order baseline). Experiments on inpainting, stylization, and semantics-to-image tasks across resolutions up to 256x256 show DBMSolver sets new SOTA efficiency-quality tradeoffs, enabling real-world applicability. Our code is publicly available at https://github.com/snumprlab/dbmsolver.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **DBMSolver**，一种专门为扩散桥模型（Diffusion Bridge Models, DBMs）设计的免训练、高效率采样算法，旨在解决图像到图像（I2I）翻译任务中采样速度慢、计算开销大的核心痛点。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的扩散模型（DPMs）假设先验分布为高斯噪声，而图像到图像（I2I）翻译任务（如修复、风格化）的先验通常是另一幅图像，这使得针对噪声生成的快速采样器（如 DDIM, DPMSolver）在 DBMs 上理论失效。
*   **研究背景**：扩散桥模型（DBMs）虽然能处理任意分布之间的转换，但现有的采样方法（如 Hybrid Heun）需要大量的函数评估次数（NFEs，通常 >100 次）才能生成高质量图像，严重限制了其实际应用。
*   **整体含义**：本文旨在开发一种无需额外训练、能够处理非高斯先验、且在极低采样步数（如 6 步）下仍能保持高保真度的通用采样器。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用 DBM 底层随机微分方程（SDE）和常微分方程（ODE）的**半线性结构**（Semi-linear structure），通过**指数积分器（Exponential Integrators）**推导出解析解。
*   **关键技术细节**：
    *   **半线性分解**：将方程分解为关于 $x_t$ 的线性项和包含神经网络预测的非线性项。
    *   **指数积分器求解**：对线性项求精确解析解，对非线性项进行泰勒展开近似。
    *   **混合采样策略**：
        *   **初始步**：使用一阶 Bridge SDE 方案（处理从 $T$ 到 $T-\epsilon$ 的微小区间），引入必要的随机性。
        *   **后续步**：使用二阶 Bridge ODE 方案进行确定性精细化，利用改变量预测来减少离散化误差。
    *   **免训练特性**：该方法作为现有 DBM 模型的插件使用，无需修改架构或重新微调模型。

### 3. 实验设计
*   **数据集与场景**：
    *   **语义/草图到图像**：Edges2Handbags (64x64)。
    *   **表面法线到图像**：DIODE-Outdoor (256x256)。
    *   **人脸风格化**：Face2Comics (256x256)。
    *   **条件图像修复**：ImageNet (256x256) 中心遮罩修复。
    *   **语义标签到人脸**：CelebAMask-HQ (256x256)。
*   **对比基准（Benchmarks）**：FID（生成质量）、IS（多样性）、MSE/LPIPS（重建保真度）、NFE（计算效率）、分类准确率（CA）。
*   **对比方法**：包括 DDBM (Hybrid Heun), DBIM (1/2/3 阶), ODES3, 以及传统的 DDIB, SDEdit, I2SB 等。

### 4. 资源与算力
*   **训练算力**：虽然采样器本身免训练，但为了验证效果，作者在多个 GPU 平台上训练了基础 DBM 模型：
    *   使用 4 到 8 块 **NVIDIA A100、A800 或 A6000 GPU**。
    *   训练迭代次数在 120k 到 400k 之间。
*   **采样算力**：DBMSolver 显著降低了推理开销，例如将 NFE 从 119 次降至 6 次，推理速度提升约 20 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数据集，横跨不同分辨率（64 到 256）和多种 I2I 任务。
*   **消融实验**：对 SDE 阶数（k=1 vs k=2）进行了对比，验证了在初始步使用一阶解的效率优势。
*   **充分性评价**：实验设计较为充分，不仅对比了同类 DBM 采样器，还跨类别对比了 GAN 和其他扩散模型。通过 FID 随 NFE 变化的曲线图，客观展示了在不同计算预算下的性能增益。

### 6. 主要结论与发现
*   **效率飞跃**：DBMSolver 在 6 次 NFE 下的效果优于传统方法在 20 次甚至 100 次 NFE 下的表现。
*   **质量提升**：在相同低步数下，DBMSolver 的 FID 显著低于 DBIM。例如在 DIODE 数据集上，20 步时的 FID 比二阶基线降低了 53%。
*   **理论验证**：证明了 DBM 的 ODE 具有半线性结构，且二阶解析近似在处理非高斯桥接时比数值多步法（如 DBIM-2/3）更稳定、更精确。

### 7. 优点
*   **理论严谨**：基于扩散桥理论推导闭式解，而非简单的启发式改进。
*   **即插即用**：无需重新训练模型，对现有 DBM 检查点具有极高的兼容性。
*   **细节保持**：在极低步数下仍能保留复杂的结构细节（如树枝、纹理），减少了伪影。

### 8. 不足与局限
*   **复杂任务瓶颈**：在 ImageNet 修复等极高复杂度的任务中，虽然仍有提升，但增益幅度相比简单任务有所收窄。作者认为这是由于非线性项（神经网络预测）的近似误差成为了主要矛盾。
*   **分辨率限制**：实验主要集中在 256x256 及以下分辨率，尚未在大尺度高分辨率（如 1024x1024）模型上进行验证。
*   **文本引导缺失**：目前主要针对图像条件，尚未深入探讨在文本引导的 I2I 翻译中的表现。

（完）
