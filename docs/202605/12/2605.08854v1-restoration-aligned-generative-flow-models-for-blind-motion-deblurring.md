---
title: Restoration-Aligned Generative Flow Models for Blind Motion Deblurring
title_zh: 用于盲运动去模糊的修复对齐生成流模型
authors: "Insoo Kim, Jinwoo Shin"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08854v1"
tags: ["query:q4"]
score: 8.0
evidence: 用于图像修复和去模糊的生成式流模型
tldr: 针对生成式流模型在盲运动去模糊任务中保真度下降的问题，本文提出 DeblurFlow 框架。通过将流轨迹的噪声端点替换为模糊观测值，使向量场与残差对齐，并利用 LoRA 对预训练模型进行微调。结合双专家采样策略和高效的残差潜空间（r-space），该方法在保持高保真度的同时显著提升了感知质量，在多个主流数据集上达到了领先水平。
selection_source: fresh_fetch
motivation: 传统的生成式流模型训练目标与图像修复任务不一致，导致在去模糊时保真度严重受损。
method: 重新定义流轨迹，将模糊图像作为端点以对齐残差，并引入双专家采样策略和专为残差解码设计的 r-space 潜空间。
result: 在 GoPro 等数据集上，DeblurFlow 在大幅提升感知质量的同时，将保真度损失降至最低，且计算成本降低了 9 倍。
conclusion: 该研究证明了通过对齐流模型轨迹与修复目标，可以有效平衡去模糊任务中的保真度与感知真实性。
---

## 摘要
生成流模型提供了从大规模自然图像中学习到的强大先验，但由于其训练目标本质上与修复任务不一致，直接将它们应用于运动去模糊等修复任务会导致严重的保真度下降。我们提出了 DeblurFlow，这是一个通过重新表述流轨迹本身来解决这种不一致性的框架：我们将噪声端点替换为模糊观测值，这使得底层的向量场与模糊图像和清晰图像之间的残差相一致。在这种表述下，标准的流匹配损失自然地呈现为残差损失的形式，从而允许预训练的流模型通过 LoRA 适配在修复对齐的目标下进行优化。这种表述进一步实现了一种双专家采样策略：保真度专家提供高保真度的初始化（例如 PSNR 33.69 dB），而 DeblurFlow 在仅将保真度略微降低至 33.05 dB 的情况下增强了感知质量，相比之下，直接在保真度专家之上应用生成模型会将 PSNR 降低至 27.60 dB。为了使其具有实用性，我们进一步引入了 r-space，这是一种专为残差解码而非图像重建而设计的潜空间，与标准的 VAE 潜空间相比，它将编解码成本降低了高达 9 倍。在 GoPro、HIDE、RealBlur 和 RWBI 上的大量实验表明，DeblurFlow 在保持计算实用性的同时，实现了强大的修复保真度和感知真实感。

## Abstract
Generative flow models offer powerful priors learned from large-scale natural images, but directly adapting them to restoration tasks such as motion deblurring causes severe fidelity degradation, as their training objective is inherently misaligned with restoration. We present DeblurFlow, a framework that resolves this misalignment by reformulating the flow trajectory itself: we replace the noise endpoint with the blur observation, which makes the underlying vector field coincide with the residual error between blur and clean images. Under this formulation, the standard flow matching loss naturally takes the form of a residual loss, allowing pretrained flow models to be optimized under restoration-aligned objectives via LoRA adaptation. This formulation further enables a dual-expert sampling strategy: a fidelity expert provides a high-fidelity initialization, e.g., PSNR 33.69 dB, and DeblurFlow enhances perceptual quality with only a marginal fidelity reduction to 33.05 dB, whereas directly applying a generative model on top of a fidelity expert decreases PSNR to 27.60 dB. To make this practical, we further introduce r-space, a latent space tailored for residual decoding rather than image reconstruction, which reduces encoder-decoder cost by up to 9$\times$over standard VAE latents. Extensive experiments on GoPro, HIDE, RealBlur, and RWBI demonstrate that DeblurFlow achieves strong restoration fidelity and perceptual realism, while remaining computationally practical.

---

## 论文详细总结（自动生成）

这是一份关于论文《Restoration-Aligned Generative Flow Models for Blind Motion Deblurring》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在盲运动去模糊任务中，既利用生成流模型（Generative Flow Models）强大的感知先验，又避免其常见的“保真度崩塌”（Fidelity Collapse）问题。
*   **研究背景**：
    *   **修复类方法**（如 NAFNet）擅长保持像素级保真度（高 PSNR），但结果往往过于平滑，缺乏真实纹理。
    *   **生成类方法**（如扩散模型、流匹配模型）能生成逼真的细节，但由于其训练目标是从噪声到图像的映射，与从模糊到清晰的修复目标不一致，导致修复结果偏离原图结构。
*   **动机**：重新表述流轨迹，使生成模型的向量场与修复任务中的“残差”对齐，从而统一生成能力与修复精度。

### 2. 方法论
*   **核心思想**：提出 **DeblurFlow** 框架，将流匹配（Flow Matching）的轨迹终点从“高斯噪声”替换为“模糊观测图像”。
*   **关键技术细节**：
    *   **修复对齐轨迹**：定义路径为 $x^*_t = (1-t)x + ty$（其中 $x$ 为清晰图，$y$ 为模糊图）。此时，学习的向量场 $v^*$ 自然等同于残差 $y-x$。
    *   **r-space（残差潜空间）**：设计了一种专为残差解码而非图像重建设计的潜空间。通过引入跳跃连接（Skip Connections）直接从模糊输入传递结构信息，将编解码计算成本降低了约 9 倍。
    *   **双专家采样策略**：
        1.  **保真度专家**：利用预训练的去模糊网络（如 NAFNet）提供一个高保真度的初始值。
        2.  **现实感专家（DeblurFlow）**：在初始值基础上进行少量步数的流匹配采样，增强感知真实性。
    *   **LoRA 适配**：使用低秩自适应（LoRA）对大规模预训练流模型（如 SANA-0.6B）进行微调，使其从生成任务迁移到残差修复任务。

### 3. 实验设计
*   **数据集**：
    *   **GoPro**：主流合成动态场景去模糊数据集（用于训练和评估）。
    *   **HIDE**：侧重于人体运动的去模糊评估。
    *   **RealBlur-J**：真实世界拍摄的模糊-清晰对。
    *   **RWBI**：无地面真值（Ground-truth）的真实世界模糊图像，用于评估泛化性。
*   **Benchmark 与对比方法**：
    *   **修复类**：MPRNet, Restormer, NAFNet, FFTFormer 等。
    *   **生成/扩散类**：IR-SDE, DiffBIR, Hi-Diff, DiffIR, FideDiff 等。
*   **评估指标**：PSNR/SSIM（保真度）、LPIPS/DISTS（感知保真度）、NIQE/MUSIQ/MANIQA（无参考感知质量）。

### 4. 资源与算力
*   **硬件环境**：使用内部集群，配备 Intel Xeon Gold CPU 和 **4 张 NVIDIA H100 GPU (80GB)**。
*   **训练时长**：在 GoPro 数据集上训练 1000 个 epoch 耗时约 **28 小时**。
*   **推理效率**：处理 2K 分辨率图像仅需 **0.41 秒**，显著快于 DiffIR（1.40s）和 FFTFormer（2.66s）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集上进行了详尽测试，涵盖了从合成到真实的多种场景。
*   **消融实验**：非常充分，包括：
    *   不同潜空间（r-space vs VAE）的效率对比。
    *   不同流轨迹（噪声到图像 vs 模糊到图像）的性能对比。
    *   协同训练比例（y 与专家输出的混合比例）的影响。
    *   采样步数对保真度与真实感权衡的影响。
*   **客观性**：通过引入多种第三方感知指标（如 MUSIQ, MANIQA）和跨数据集验证，证明了结果的稳健性。

### 6. 主要结论与发现
*   **保真度保持**：DeblurFlow 成功解决了生成模型 PSNR 剧降的问题。在 GoPro 上，它能将保真度维持在 33.05 dB，而传统生成方法往往掉至 27 dB 左右。
*   **感知提升**：在保持高保真度的同时，感知指标（如 MUSIQ）大幅领先于纯修复方法。
*   **高效性**：r-space 的引入解决了高分辨率图像处理的显存和计算瓶颈，使生成式去模糊具备了实用价值。
*   **轨迹重要性**：实验证明“模糊到清晰”的轨迹比“噪声到残差”的轨迹更能利用预训练模型的自然图像先验。

### 7. 优点（亮点）
*   **理论优雅**：通过简单的轨迹重定义，巧妙地将流匹配损失转化为残差损失，实现了生成与修复的数学统一。
*   **架构创新**：r-space 的设计非常实用，跳跃连接解决了生成模型难以还原底层结构的痛点。
*   **性能卓越**：在速度、保真度和视觉效果三者之间达到了极佳的平衡点（Pareto 优化）。

### 8. 不足与局限
*   **依赖外部专家**：目前的框架仍需要一个预训练的“保真度专家”提供初始化，尚未实现完全的端到端单模型解决。
*   **幻觉风险**：作为生成式模型，在极端模糊的情况下仍可能产生看似合理但并非真实的纹理（Hallucination），不适用于医疗或取证等对绝对真实性要求极高的领域。
*   **任务覆盖**：虽然提到可以扩展到降噪和超分，但本文实验主要集中在去模糊，其他任务的有效性尚待验证。

（完）
