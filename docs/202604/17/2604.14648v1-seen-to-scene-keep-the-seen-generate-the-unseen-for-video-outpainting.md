---
title: "Seen-to-Scene: Keep the Seen, Generate the Unseen for Video Outpainting"
title_zh: Seen-to-Scene：保留已见，生成未见，助力视频外扩
authors: "Inseok Jeon, Minhyeok Lee, Seunghoon Lee, Minseok Kang, Suhwan Cho, Sangyoun Lee"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14648v1"
tags: ["query:q1"]
score: 9.0
evidence: 视频外扩绘制以扩展边界外的可见内容
tldr: 针对视频外扩（Video Outpainting）中存在的空间不一致和时间不连贯问题，本文提出Seen-to-Scene框架。该方法创新性地结合了传播与生成范式，利用微调的流补全网络重建运动场，并引入参考引导的潜空间传播技术。实验证明，该方法在保持视觉真实感和时间一致性方面显著优于现有技术，且具有更高的推理效率。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的视频外扩方法由于隐式时间建模和有限的空间上下文，在处理动态场景时容易出现帧内和帧间的不一致性。
method: 提出一种统一传播与生成的框架，通过端到端微调流补全网络重建运动场，并结合参考引导的潜空间传播来有效传递源视频内容。
result: 实验结果显示，该方法在时间连贯性和视觉真实感上均优于现有最先进技术，且无需针对特定输入进行适配。
conclusion: Seen-to-Scene通过融合传播与生成机制，有效解决了视频外扩中的一致性难题，为高质量视频内容扩展提供了高效可靠的方案。
---

## 摘要
视频外扩旨在将视频的可视内容扩展到原始帧边界之外，同时保持空间保真度和跨帧的时间一致性。现有方法主要依赖于大规模生成模型（如扩散模型）。然而，基于生成的方法受限于隐式时间建模和有限的空间上下文。这些局限性导致了帧内和帧间的不一致性，在动态场景和大幅度外扩场景中尤为明显。为了克服这些挑战，我们提出了 Seen-to-Scene，这是一个统一了基于传播和基于生成的视频外扩范式的新型框架。具体而言，Seen-to-Scene 利用基于流的传播，并结合了为视频修复预训练的流补全网络，通过端到端微调来弥合领域差距并重建连贯的运动场。为了进一步提高传播的效率和可靠性，我们引入了参考引导的潜空间传播，能够有效地在帧间传播源内容。大量实验表明，我们的方法在实现高效推理的同时，达到了卓越的时间一致性和视觉真实感，甚至超越了以往需要针对特定输入进行适配的最先进方法。

## Abstract
Video outpainting aims to expand the visible content of a video beyond the original frame boundaries while preserving spatial fidelity and temporal coherence across frames. Existing methods primarily rely on large-scale generative models, such as diffusion models. However, generationbased approaches suffer from implicit temporal modeling and limited spatial context. These limitations lead to intraframe and inter-frame inconsistencies, which become particularly pronounced in dynamic scenes and large outpainting scenarios. To overcome these challenges, we propose Seen-to-Scene, a novel framework that unifies propagationbased and generation-based paradigms for video outpainting. Specifically, Seen-to-Scene leverages flow-based propagation with a flow completion network pre-trained for video inpainting, which is fine-tuned in an end-to-end manner to bridge the domain gap and reconstruct coherent motion fields. To further improve the efficiency and reliability of propagation, we introduce a reference-guided latent propagation that effectively propagates source content across frames. Extensive experiments demonstrate that our method achieves superior temporal coherence and visual realism with efficient inference, surpassing even prior state-of-the-art methods that require input-specific adaptation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Seen-to-Scene**，一个旨在解决视频外扩（Video Outpainting）中时空一致性难题的新型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视频外扩要求在扩展视频边界的同时，既要保持原始区域（Seen）的忠实度，又要生成自然且连贯的未见区域（Unseen）。
*   **研究背景**：
    *   **基于传播的方法**：通过光流将相邻帧内容“拉”过来，虽能保留源内容，但在处理大幅度外扩时，光流预测会失效（领域鸿沟），且计算成本随分辨率和帧数激增。
    *   **基于生成的方法（如扩散模型）**：擅长生成新内容，但缺乏显式的时间建模，导致长序列中源内容丢失、闪烁或产生语义无关的幻觉。
*   **研究动机**：将传播的“结构稳定性”与扩散模型的“生成能力”统一，解决现有方法在动态场景和大幅度外扩下的不一致性问题。

### 2. 方法论：核心思想与关键技术细节
**核心思想**：在潜空间（Latent Space）中集成参考引导的传播机制，并将其作为扩散模型的条件输入。

*   **参考帧选择（Reference Selection）**：不采用逐帧顺序传播，而是基于结构相似性（SSIM）在滑动窗口内选择信息丰富的参考帧，减少冗余计算和误差累积。
*   **流补全网络微调（FCNet Fine-tuning）**：论文指出视频修复（Inpainting）与外扩（Outpainting）存在领域差距。作者对预训练的流补全网络进行了端到端微调，使其能适应大面积、开放式的边界扩展，减少“流溢出”现象。
*   **参考引导的潜空间传播**：
    *   在 VAE 的潜空间进行操作，利用补全后的光流将参考帧的 Latent 映射到目标帧。
    *   采用“一次性拉取”（One-shot pulling）策略，通过组合光流实现长程传播。
*   **潜空间细化模块（Latent Refinement）**：一个轻量级模块，利用外观线索（而非运动线索）修正传播过程中的局部对齐误差。
*   **视频扩散模型生成**：以 Stable Video Diffusion (SVD) 为基础，冻结空间层，仅微调时间 Transformer 块，将传播后的 Latent 与噪声 Latent 拼接作为输入进行去噪生成。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：YouTube-VOS（仅使用 10 万个视频样本，远少于同类模型）。
    *   **评估集**：DAVIS 2017（90 个序列）和 YouTube-VOS（随机采样 60 个序列）。
*   **Benchmark 与对比方法**：
    *   对比了 Dehan (CVPRW'22)、M3DDM (ACMMM'23)、MOTIA (ECCV'24)、Follow-Your-Canvas (AAAI'25) 等。
    *   涵盖了零样本（Zero-shot）和需要针对特定视频微调的一样本（One-shot）方法。
*   **评价指标**：PSNR、SSIM、LPIPS 和 **FVD**（衡量视频质量和时间连贯性的核心指标）。

### 4. 资源与算力
*   **硬件**：使用 **2 台 NVIDIA RTX A6000 GPU** 进行训练。
*   **训练细节**：迭代 100K 次，Batch Size 为 1，学习率为 1e-5。
*   **推理效率**：推理时间约为 **12.1 秒**（256x256 分辨率），显著快于 M3DDM（43.1s）和 MOTIA（373.2s）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集上进行了详尽的定量对比。
*   **消融实验**：非常充分，分别验证了“仅扩散模型”、“加入潜空间传播”、“流补全网络微调”以及“细化模块”对最终效果的增益。
*   **客观性**：作者特别指出了 YouTube-VOS 评估中存在的选择偏差，并通过随机采样 60 个视频来确保测试的公平性。此外，还对比了推理时间与显存占用，证明了其高效性。

### 6. 主要结论与发现
*   **统一范式的优越性**：Seen-to-Scene 在不进行特定视频适配的情况下，性能超越了需要微调的方法（如 MOTIA）。
*   **领域差距的影响**：首次证明了直接使用修复任务的流补全网络在外扩任务中会导致严重的几何畸变，微调是必要的。
*   **效率提升**：潜空间传播结合参考帧策略，大幅降低了显存占用和推理延迟，解决了传播类方法难以处理长视频的痛点。

### 7. 优点（亮点）
*   **零样本高性能**：无需针对每个视频进行耗时的 Test-time Adaptation，泛化能力强。
*   **架构优雅**：将显式几何约束（光流）与隐式生成先验（扩散）深度融合，而非简单的后处理。
*   **计算友好**：通过潜空间操作和参考链设计，使模型在消费级或专业级 GPU 上均有极高的实用价值。

### 8. 不足与局限
*   **细节丢失风险**：由于传播是在下采样的潜空间（Latent Space）进行的，相比像素级传播，可能会丢失极细微的纹理细节。
*   **流依赖性**：尽管有细化模块，但生成质量仍高度依赖于光流预测的准确性，在极端复杂运动（如剧烈遮挡或快速旋转）下可能仍有挑战。
*   **训练数据规模**：虽然 10 万样本已取得好成绩，但相比工业界动辄数百万视频的训练规模，模型在极端长尾场景下的表现仍有提升空间。

（完）
