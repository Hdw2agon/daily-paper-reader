---
title: "FlashClear: Ultra-Fast Image Content Removal via Efficient Step Distillation and Feature Caching"
title_zh: FlashClear：通过高效步骤蒸馏与特征缓存实现极速图像内容移除
authors: "Yixin Tang, Jiawei Guo, Junxian Li, Zhiteng Li, Jixin Zhao, Bingya Zhang, Chenbo Wang, Yulun Zhang, Shangchen Zhou"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09003v2"
tags: ["query:q1"]
score: 9.0
evidence: 基于扩散模型的物体移除和图像内容消除
tldr: 针对扩散模型在图像内容擦除中计算开销大、推理慢的问题，本文提出 FlashClear。该框架通过区域感知对抗蒸馏（RAD）实现高效的少步推理，并结合无需训练的前景优先非对称注意力与缓存策略（FPAC）进一步优化计算。FlashClear 在保持高视觉质量的同时，实现了比现有模型快数倍乃至百倍的推理速度，显著提升了图像编辑的效率。
selection_source: fresh_fetch
motivation: 现有的扩散移除模型对全图进行无差别去噪，导致在处理局部前景区域时存在巨大的计算冗余和时间延迟。
method: 提出区域感知对抗蒸馏（RAD）构建少步模型，并引入前景优先的非对称注意力与特征缓存（FPAC）实现推理加速。
result: 在 OBER 基准上，FlashClear 相比 ObjectClear 和 OmniPaint 分别实现了 8.26 倍和 122 倍的加速，且保持了极高的视觉保真度。
conclusion: FlashClear 证明了结合步数蒸馏与特征缓存策略可以极大地提升图像内容移除任务的效率，为实时图像编辑提供了可能。
---

## 摘要
最近，基于扩散的物体移除模型在消除物体及其相关视觉效果方面取得了显著成果。然而，这些模型在所有时间步中不加区分地对所有 token 进行去噪，忽略了移除操作通常仅涉及较小的前景区域。这种策略引入了巨大的计算开销并延长了推理时间。为了克服这一计算负担，我们提出了一个潜空间判别器来实现区域感知对抗蒸馏（RAD），从而产生了一个名为 FlashClear 的高效少步模型。此外，针对少步扩散模型，我们提出了 FPAC（前景优先的非对称注意力与缓存），这是一种无需训练的加速策略。大量实验表明，我们的框架在保持或超过基础模型 ObjectClear 性能的同时，实现了大幅加速。值得注意的是，在 OBER 基准测试中，FlashClear 相比 ObjectClear 和 OmniPaint 分别实现了高达 8.26 倍和 122 倍的加速，同时保持了极高的视觉质量和保真度。

## Abstract
Recently, diffusion-based object removal models have achieved impressive results in eliminating objects and their associated visual effects. However, they indiscriminately denoise all tokens across all timesteps, ignoring that removal usually involves small foreground regions. This strategy introduces substantial computational overhead and prolonged inference times. To overcome this computational burden, we propose a latent discriminator to implement Region-aware Adversarial Distillation (RAD), yielding a highly efficient few-step model named FlashClear. Furthermore, tailored to few-step diffusion models, we propose FPAC (Foreground-Prioritized Asymmetric Attention and Caching), a training-free acceleration strategy. Extensive experiments demonstrate that our framework provides massive acceleration while maintaining or exceeding the performance of our base model, ObjectClear. Notably, on the OBER benchmark, our FlashClear achieves up to 8.26$\times$ and 122$\times$ speedup over ObjectClear and OmniPaint, respectively, while maintaining high visual quality and fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FlashClear: Ultra-Fast Image Content Removal via Efficient Step Distillation and Feature Caching》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的扩散模型在图像物体移除（Object Removal）任务中表现出色，但存在严重的效率瓶颈。主要原因有二：一是传统的扩散模型需要多步迭代（如 20-50 步），推理延迟高；二是模型在去噪时对全图 token 进行无差别计算，而物体移除通常只涉及局部的“前景”区域，导致了巨大的空间计算冗余。
*   **核心问题**：如何在保证移除质量（包括物体及其诱发的阴影、反射等视觉效果）的同时，实现极速的推理，使其能够应用于实时交互编辑和移动端设备。

### 2. 方法论：核心思想与关键技术
FlashClear 结合了**少步蒸馏**和**特征缓存**两种互补的加速策略：
*   **区域感知对抗蒸馏 (RAD)**：
    *   **核心思想**：将多步扩散过程蒸馏为仅需 4 步（甚至 2 步）的生成器。
    *   **非对称掩码策略**：生成器仅接收“物体掩码”作为输入；而判别器（利用预训练 U-Net 构建）则使用“物体+效果掩码”（包含阴影、反射区域）进行监督。这种设计强制模型学习移除物体诱发的次生视觉效果。
    *   **损失函数**：结合了对抗损失、感知损失（LPIPS）、扩散 $L_2$ 损失以及注意力引导的掩码损失，确保局部移除的彻底性和全局背景的一致性。
*   **前景优先非对称注意力与缓存 (FPAC)**：
    *   **核心思想**：利用物体移除任务的“空间冗余性”，即大部分背景区域在编辑前后保持不变。
    *   **动态缓存**：通过交叉注意力图自动识别前景（需重算）和背景（可缓存）token。
    *   **非对称注意力**：在自注意力层中，仅让前景 token 作为 Query 去查询全局信息，而背景 token 直接复用缓存特征，跳过冗余计算。最后通过注意力引导的融合机制实现无损合成。

### 3. 实验设计
*   **数据集**：
    *   **OBER-Test**：评估通用物体移除能力（512×512 分辨率）。
    *   **RORD-Val**：真实世界物体移除数据集，包含复杂场景和高分辨率图像（960×540）。
*   **对比方法**：
    *   **基础模型**：SDXL-INP, PowerPaint。
    *   **物体移除专用模型**：GeoRemover, OmniEraser, Attentive Eraser, RORem, ObjectClear。
    *   **图像编辑框架**：DesignEdit, OmniPaint。
    *   **加速算法对比**：ToCa, SiTo, HiCache, QuantCache 等。
*   **评价指标**：FLOPs（理论计算量）、LPIPS（感知质量）、PSNR（像素相似度），以及针对局部区域的 LPIPS-Local 和 PSNR-mask。

### 4. 资源与算力
*   **训练资源**：使用了 **2 张 NVIDIA A800 GPU**（80GB 显存）。
*   **训练时长**：总计训练 10,000 个迭代步（iterations），耗时约 **24 小时**。
*   **推理资源**：推理和评估在单张 **NVIDIA A6000 GPU**（48GB 显存）上完成。
*   **实现细节**：基于 SDXL 架构，使用 LoRA（rank=256）进行微调，采用 bfloat16 混合精度。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准数据集上进行了详尽的定量对比，并提供了大量的定性视觉对比图。
*   **消融实验**：针对 RAD 的损失组件（GAN、LPIPS、RAD 策略）以及 FPAC 的有效性进行了独立验证。
*   **用户研究**：组织了涉及 200 组对比的二选一迫选实验（2-AFC），验证了加速后的模型在人类视觉偏好上与原模型（ObjectClear）基本持平。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了移除效果，还深入探讨了加速算法与少步模型之间的兼容性问题，证明了 FPAC 在极低步数下的优越性。

### 6. 主要结论与发现
*   **极速推理**：FlashClear 在 OBER 基准上实现了比 ObjectClear 快 **8.26 倍**、比 OmniPaint 快 **122 倍** 的惊人加速。
*   **质量保持**：尽管步数大幅减少，但通过 RAD 蒸馏，模型依然保留了感知物体诱发效果（如阴影）的能力，视觉质量甚至在某些指标上超过了多步基础模型。
*   **空间冗余的价值**：实验证明，在少步模型中，传统的“时间冗余”缓存（相邻步间复用）效果变差，而基于任务特征的“空间冗余”缓存（背景复用）是实现进一步加速的关键。

### 7. 优点（亮点）
*   **软硬件结合的视角**：不仅从算法步数（RAD）入手，还从计算架构（FPAC）优化，解决了少步模型与特征缓存不兼容的业界难题。
*   **非对称监督设计**：判别器使用更宽的掩码来监督生成器，巧妙地解决了物体移除中“次生效果残留”的痛点。
*   **无需训练的加速**：FPAC 插件式设计，无需额外训练即可显著降低计算开销。

### 8. 不足与局限
*   **任务依赖性**：该方法高度依赖于物体移除任务的局部性先验，对于需要全图大幅度改变的图像编辑任务（如风格迁移），FPAC 的加速效果和适用性会受限。
*   **系统开销抵消**：虽然理论 FLOPs 大幅下降，但在实际部署中，I/O 操作和内存通信开销在模型变得极轻量后会占据更大比例，实际端到端加速比可能略低于理论值。
*   **潜在滥用风险**：作为高效的内容篡改工具，可能被用于伪造视觉证据，存在一定的伦理和安全风险。

（完）
