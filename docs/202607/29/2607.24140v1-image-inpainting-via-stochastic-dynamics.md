---
title: Image Inpainting via Stochastic Dynamics
title_zh: 基于随机动力学的图像修复
authors: "Jiaqi Kuang, Zihao Guo, Zhongmin Qian"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24140v1"
tags: ["query:q3"]
score: 9.0
evidence: 无需网络训练或参数模型的图像补全
tldr: 本研究提出一种无需神经网络训练的非参数图像修复方法。该方法基于数据引导的随机动力学，通过逆时随机微分方程（SDE）演化缺失像素。利用从参考数据集中直接估计的核加权修正项，将重建过程引导至数据分布的高密度区域。在MNIST、Fashion-MNIST及MVTec等数据集上的实验表明，该方法在PSNR和SSIM等指标上优于传统算法，并在CelebA上展现出极具竞争力的结构修复能力，证明了经验统计量作为非参数先验的有效性。
selection_source: fresh_fetch
motivation: 旨在探索一种无需复杂神经网络训练或参数化密度建模，即可利用参考数据分布实现高质量图像修复的方法。
method: 采用基于逆时随机微分方程的非参数框架，通过参考数据集的核加权修正项引导像素向高概率密度区域演化。
result: 实验结果显示，该方法在多个标准数据集上的表现优于Mean Fill、Telea和Navier-Stokes等传统修复技术。
conclusion: 经验参考统计量可作为有效的非参数先验，为图像修复提供了一种无需训练且具备结构一致性的新途径。
---

## 摘要
图像修复旨在恢复缺失区域，同时保持结构一致性。我们提出了一种基于数据引导随机动力学的非参数化方法，无需进行网络训练。从掩码图像出发，缺失像素通过逆时随机微分方程进行演化，其中包含直接从参考数据集中估计的核权重修正项。这种经验修正引导重建过程趋向数据分布的高密度区域，而无需训练神经网络或拟合参数化密度模型。在 MNIST、Fashion-MNIST 和 MVTec 数据集上的实验表明，所提方法在 PSNR、SSIM 和视觉质量方面优于均值填充（Mean Fill）、Telea 和 Navier-Stokes 修复算法。在 CelebA 数据集上，该方法依然具有竞争力，并能针对结构敏感的遮挡生成合理的补全结果。这些结果证明了经验参考统计量作为图像修复非参数先验的有效性。

## Abstract
Image inpainting aims to recover missing regions while preserving structural consistency. We propose a non-parametric method without network training based on data-guided stochastic dynamics. Starting from a masked image, the missing pixels are evolved through a reverse-time stochastic differential equation with a kernel-weighted correction estimated directly from a reference dataset. This empirical correction guides the reconstruction toward high-density regions of the data distribution without training a neural network or fitting a parametric density model. Experiments on MNIST, Fashion-MNIST, and MVTec show that the proposed method outperforms Mean Fill, Telea, and Navier-Stokes inpainting in PSNR, SSIM, and visual quality. On CelebA, it remains competitive and produces plausible completions for structure-sensitive occlusions. These results demonstrate the effectiveness of empirical reference statistics as a non-parametric prior for image inpainting.

---

## 论文详细总结（自动生成）

这篇论文《Image Inpainting via Stochastic Dynamics》（基于随机动力学的图像修复）提出了一种创新的非参数化图像修复框架。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行大规模神经网络训练的情况下，利用外部数据集的统计先验来修复图像中的缺失区域，并保持结构一致性。
*   **研究动机**：
    *   **传统方法局限**：基于偏微分方程（PDE）的扩散方法（如 Telea）容易导致模糊；基于样本的方法（如 Criminisi）在处理复杂语义结构（如人脸、文字）时缺乏全局连贯性。
    *   **深度学习局限**：CNN、GAN 和生成式扩散模型虽然效果好，但需要海量数据训练、计算资源消耗大、模型黑盒化且容易过拟合。
    *   **研究目标**：探索一种“无需训练”（Training-free）且“非参数化”的方法，通过随机动力学直接从参考样本中提取分布信息。

### 2. 方法论
*   **核心思想**：将图像修复建模为受观测像素约束的**逆时随机微分方程（SDE）**演化过程。
*   **关键技术细节**：
    *   **前向过程**：采用 Ornstein-Uhlenbeck (OU) 过程，将原始数据分布逐渐引入随机扰动。
    *   **逆时动力学**：推导出一个包含两部分的逆时 SDE：
        1.  **线性漂移项（Linear Drift）**：基于 OU 桥过程，将状态引导至参考均值。
        2.  **数据驱动修正项（Data-guided Correction）**：这是核心创新点。它不使用神经网络估计得分函数（Score Function），而是通过**核权重经验估计**。即在每一步迭代中，计算当前状态与参考数据集中样本的距离，利用核函数加权平均来引导像素向高概率密度区域演化。
    *   **数值稳定性**：引入了平滑正则化参数 $\epsilon$，解决了 OU 桥漂移项在初始时刻（$t=0$）可能出现的奇异性（数值爆炸）问题。
    *   **局部上下文匹配**：为了提高效率和准确性，算法在检索参考样本时，仅计算缺失区域周围“环形”观测区域的距离，而非全图匹配。

### 3. 实验设计
*   **数据集**：
    *   **MNIST**：手写数字（结构简单但具有拓扑特征）。
    *   **Fashion-MNIST**：服装图像（具有更复杂的轮廓）。
    *   **MVTec AD**：工业检测图像（Bottle, Capsule），测试对稳定几何结构的恢复。
    *   **CelebA**：人脸图像（高维、语义复杂）。
*   **Benchmark 与对比方法**：
    *   **对比基准**：均为非学习类方法，包括 **Mean Fill**（均值填充）、**Telea**（基于快速行进法的修复）和 **Navier-Stokes**（基于流体力学方程的修复）。
*   **评估指标**：峰值信噪比（PSNR）和结构相似性（SSIM）。

### 4. 资源与算力
*   **算力需求**：论文明确指出该方法**可以在 CPU 上运行**，无需 GPU 资源。
*   **训练时长**：由于是**非参数化方法**，该方法**无需任何训练阶段**（Zero Training Time）。
*   **推理开销**：计算开销主要集中在每一步 SDE 迭代中与参考样本的核权重计算。文中通过候选池（Candidate Pool）采样（如从 10000 个样本中采样 1024 个）来降低计算量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MNIST 和 Fashion-MNIST 上分别进行了 100 组和 500 组测试。
    *   针对特定类别（如数字“1”、“4”、“8”，服装“Trouser”、“Coat”）进行了专项实验。
    *   进行了关于扩散系数 $\nu$ 的**消融实验**（测试了 0.05 到 1.00 的不同取值）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单灰度图到复杂 RGB 人脸图，从混合标签到单类别先验的多种场景。对比实验在相同的非学习设定下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能优越**：在所有测试数据集上，所提 SDE 方法在 PSNR 和 SSIM 上均显著优于 Telea 和 Navier-Stokes 等传统方法。
*   **结构保持**：该方法能有效恢复具有语义意义的结构（如数字的笔画、衣服的轮廓、工业品的边缘），而传统方法往往产生模糊或几何畸变。
*   **先验有效性**：实验证明，即使不通过神经网络学习，直接利用参考数据集的经验统计量也能为 SDE 提供强大的导航信号。

### 7. 优点（亮点）
*   **无需训练**：极大地降低了部署门槛，适用于隐私敏感或数据量不足以训练深度模型的场景。
*   **数学解释性强**：基于严谨的随机分析和 Feynman-Kac 公式背景，非黑盒模型。
*   **灵活性**：可以通过更换参考数据集（Reference Pool）轻松调整修复风格或类别先验。
*   **稳定性**：通过正则化手段解决了 SDE 数值求解中的奇异性问题。

### 8. 不足与局限
*   **计算复杂度**：虽然不需要 GPU 训练，但推理时需要频繁计算样本间的核权重，当参考数据集极大或图像分辨率很高时，计算压力会增大。
*   **多样性限制**：修复效果高度依赖于参考数据集的质量和覆盖范围。如果参考集中缺乏相似样本，修复质量可能下降。
*   **高维挑战**：在 CelebA（人脸）等高维异构数据上，虽然优于传统方法，但与最先进的深度学习生成模型（如 Stable Diffusion）相比，在纹理细节的生成上可能仍有差距。

（完）
