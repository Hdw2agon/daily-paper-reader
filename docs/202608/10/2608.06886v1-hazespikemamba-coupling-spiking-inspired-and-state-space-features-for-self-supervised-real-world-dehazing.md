---
title: "HazeSpikeMamba: Coupling Spiking-Inspired and State-Space Features for Self-Supervised Real-World Dehazing"
title_zh: HazeSpikeMamba：耦合脉冲启发与状态空间特征的自监督真实世界去雾
authors: "Haoran Liu, Huibin Li, Mingzhe Liu, Peng Li, Guibin Zan"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06886v1"
tags: ["query:q7"]
score: 8.0
evidence: 自监督真实世界图像修复
tldr: 针对合成雾图与真实场景差异大且缺乏配对数据的问题，本文提出HazeSpikeMamba框架。该方法在多尺度U-Net中结合了受脉冲耦合神经网络启发的局部路径（TPCNNSpike）和基于状态空间模型的全局路径，并利用预训练的退化网络在无标签目标域上进行自监督转导式适配。该模型仅含2.02M参数，在多个真实数据集上显著提升了去雾质量和视觉感知指标。
selection_source: fresh_fetch
motivation: 合成雾图模型难以捕捉真实雾气的复杂性，且真实场景中缺乏配对的清晰图像用于监督训练。
method: 结合了模拟神经元并行更新的脉冲局部路径与线性复杂度的状态空间全局路径，并引入冻结退化网络进行自监督域适配。
result: 在RTTS数据集上，BRISQUE指标从30.13降至27.72，NIMA从4.13升至4.87，在多个真实测试集上均达到领先水平。
conclusion: HazeSpikeMamba通过耦合局部脉冲特征与全局长程依赖，配合高效的自监督适配策略，实现了高性能且轻量化的真实场景去雾。
---

## 摘要
去雾网络通常在合成的雾天-清晰图像对上进行训练，但它们在真实照片上的性能往往会下降。使用大气散射模型生成的合成雾无法完全捕捉真实雾的变异性，且成对的真实雾天-清晰图像非常稀缺。在这项工作中，我们提出了 HazeSpikeMamba，这是一个紧凑的去雾框架，它在多尺度 U-Net 中结合了脉冲启发的局部路径和注意力状态空间全局路径。局部路径使用了 TPCNNSpike，这是一种受脉冲耦合神经网络 (PCNN) 邻域耦合启发的全新脉冲发射方案。与分组定向扫描不同，TPCNNSpike 利用其高斯权重邻域的先前发放状态并行更新所有神经元。全局路径采用了 MambaIRv2 的注意力状态空间模块，保留了语义提示和序列重排，同时移除了窗口自注意力分支。其状态空间处理以与序列长度呈线性关系的复杂度对远程依赖关系进行建模。为了进行目标域自适应，一个在成对 NH-HAZE 数据上预训练的冻结退化网络从去雾预测中重新合成雾。在自适应过程中，重建误差仅更新 HazeSpikeMamba 的最终修复层，无需无雾标签。共享检查点在每个完整的无标签目标集上进行一次自适应，使评估达到数据集级别且具有转导性，而非零样本或单图像优化。前向网络包含 2.02M 个激活参数，在 256x256 输入下需要 13.27G 的标称 MACs。这种自适应一致地提高了 RTTS、URHI 和 HSTS 上的 BRISQUE 和 NIMA 指标。在 RTTS 上，BRISQUE 从 30.13 降至 27.72，NIMA 从 4.13 增至 4.87。在这种转导协议下，自适应模型在 URHI 和 HSTS 上的 BRISQUE 和 NIMA 也达到了对比方法中的最优水平。

## Abstract
Dehazing networks are commonly trained on synthetic hazy-clear pairs, but their performance often drops on real photographs. Synthetic haze generated using the atmospheric scattering model does not fully capture the variability of real haze, and paired real hazy-clear images are scarce. In this work, we propose HazeSpikeMamba, a compact dehazing framework that combines a spiking-inspired local path and an attentive state-space global path in a multi-scale U-Net. The local path uses TPCNNSpike, a new spike-emission scheme inspired by the neighborhood coupling of Pulse-Coupled Neural Network (PCNN). Unlike grouped directional scanning, TPCNNSpike updates all neurons in parallel using the previous firing states of their Gaussian-weighted neighborhoods. The global path adapts the Attentive State-Space Module of MambaIRv2, retaining semantic prompting and sequence reordering while removing the window self-attention branch. Its state-space processing models long-range dependencies with complexity linear in sequence length. For target-domain adaptation, a frozen degradation network, pretrained on paired NH-HAZE data, re-synthesizes haze from the dehazed prediction. The reconstruction error updates only the final restoration layers of HazeSpikeMamba without haze-free labels during adaptation. A shared checkpoint is adapted once on each complete unlabeled target set, making the evaluation dataset-level and transductive rather than zero-shot or per-image optimization. The forward network contains 2.02M active parameters and requires 13.27G nominal MACs (measured with thop at 256x256 input). This adaptation consistently improves BRISQUE and NIMA on RTTS, URHI, and HSTS. On RTTS, BRISQUE decreases from 30.13 to 27.72 and NIMA increases from 4.13 to 4.87. Under this transductive protocol, the adapted model also achieves the best BRISQUE and NIMA on URHI and HSTS among the compared methods.

---

## 论文详细总结（自动生成）

以下是对论文《HazeSpikeMamba: Coupling Spiking-Inspired and State-Space Features for Self-Supervised Real-World Dehazing》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的去雾网络大多在合成的“雾天-清晰”图像对上训练，但由于合成雾（基于大气散射模型）与真实雾的物理特性差异巨大，模型在真实场景中往往表现不佳。此外，获取成对的真实世界去雾数据极其困难。
*   **研究背景**：为了解决合成与真实域之间的鸿沟，研究者们开始探索自监督学习和域适配技术。本文旨在设计一种既能捕捉局部细节又能建模全局长程依赖的轻量化架构，并提出一种无需清晰标签的自监督适配策略。

### 2. 论文提出的方法论
HazeSpikeMamba 采用多尺度 U-Net 架构，核心包含以下三个关键组件：
*   **TPCNNSpike（局部路径）**：受脉冲耦合神经网络（PCNN）启发，提出了一种全新的脉冲发射方案。不同于传统的序列扫描，它利用高斯权重的邻域先前发放状态来并行更新所有神经元，能够有效提取局部纹理和边缘特征。
*   **注意力状态空间模块（全局路径）**：基于 MambaIRv2 进行改进，保留了语义提示（Semantic Prompting）和序列重排，但移除了计算量大的窗口自注意力分支。利用状态空间模型（SSM）以线性复杂度建模图像的全局长程依赖。
*   **自监督转导式适配（Transductive Adaptation）**：
    *   引入一个在 NH-HAZE 数据集上预训练并**冻结**的退化网络（Degradation Network）。
    *   **流程**：去雾网络生成预测图 -> 退化网络将其重新合成雾图 -> 计算合成雾图与原始输入雾图的重建误差。
    *   **特点**：仅更新去雾网络的最终修复层，无需无雾标签，且在整个目标数据集上进行一次性适配，而非针对单张图像优化。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用合成数据集 RESIDE-OTS 进行基础训练。
    *   **测试/适配集**：RTTS（包含 4322 张真实雾图）、URHI、HSTS（真实子集）。
*   **评价指标**：由于缺乏真实清晰图，主要使用无参考指标 **BRISQUE**（越低越好）和 **NIMA**（越高越好）。
*   **对比方法**：对比了包括 DehazeNet, AOD-Net, EPDN, GCANet, FFA-Net, AECR-Net, DehazeFormer, 以及基于 Mamba 的最新方法（如 U-Mamba, MambaIR）等十余种主流算法。

### 4. 资源与算力
*   **模型规模**：前向网络非常轻量，仅包含 **2.02M** 个激活参数。
*   **计算开销**：在 256x256 输入下，标称计算量为 **13.27G MACs**（使用 thop 工具测量）。
*   **硬件信息**：论文摘要及正文提取部分**未明确说明**具体的 GPU 型号、数量及训练总时长。

### 5. 实验数量与充分性
*   **实验覆盖面**：在三个主流的真实世界去雾基准数据集（RTTS, URHI, HSTS）上进行了详尽的定量对比。
*   **消融实验**：验证了局部脉冲路径、全局 Mamba 路径以及自监督适配策略各自的贡献。
*   **客观性**：通过可视化结果展示了模型在处理真实浓雾和光晕时的优势，并使用了客观的无参考质量评价指标，实验设计较为公平合理。

### 6. 论文的主要结论与发现
*   **性能领先**：HazeSpikeMamba 在 RTTS 数据集上将 BRISQUE 从 30.13 降低至 27.72，NIMA 从 4.13 提升至 4.87，达到了 SOTA 水平。
*   **适配有效性**：提出的转导式自监督适配策略能显著提升模型在未见过的真实目标域上的表现，证明了“去雾-再退化”闭环的有效性。
*   **架构优势**：结合脉冲神经元的局部敏感性和 Mamba 的全局建模能力，可以在极小的参数量下实现高性能去雾。

### 7. 优点
*   **生物启发与高效结合**：创新性地将类脑脉冲机制与高效的状态空间模型（SSM）结合，兼顾了局部细节和全局上下文。
*   **轻量化**：2.02M 的参数量使其非常适合移动端或嵌入式设备的部署。
*   **实用的适配策略**：无需成对的真实数据即可进行域适配，解决了真实场景去雾的核心痛点。

### 8. 不足与局限
*   **退化网络的依赖**：自监督效果高度依赖于预训练退化网络的准确性，如果退化网络无法模拟目标域的复杂退化，适配效果可能受限。
*   **转导式限制**：虽然比单图优化快，但“转导式适配”仍需要在每个新数据集上进行一次微调，无法做到完全的即插即用。
*   **算力细节缺失**：文中未提供具体的训练环境和耗时，不便于其他研究者评估训练成本。

（完）
