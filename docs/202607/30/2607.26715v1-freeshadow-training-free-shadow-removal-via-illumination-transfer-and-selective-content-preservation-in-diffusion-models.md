---
title: "FreeShadow: Training-Free Shadow Removal via Illumination Transfer and Selective Content Preservation in Diffusion Models"
title_zh: FreeShadow：基于扩散模型中光照迁移与选择性内容保留的无需训练阴影去除方法
authors: "Yinan Wang, Yan Huang, Yong Xu, Patrick Le Callet"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26715v1"
tags: ["query:q3"]
score: 9.0
evidence: 基于扩散模型的免训练阴影去除
tldr: FreeShadow 是一种基于预训练扩散模型的无需训练的阴影去除方法。针对现有监督学习方法泛化性差以及零样本方法优化耗时的问题，该方法通过照明传输注意力（ITA）将非阴影区的光照信息迁移至阴影区，并结合选择性内容保留技术维持图像细节。此外，引入局部纹理保留重照明（LTPR）解决 VAE 压缩导致的失真，实现了无需额外训练的高质量阴影去除。
selection_source: fresh_fetch
motivation: 现有阴影去除方法受限于训练数据多样性或测试时优化的高昂成本，难以在泛化性与生成质量之间取得平衡。
method: 利用扩散模型先验，通过重绘自注意力图实现光照迁移，并选择性保留照明无关的高频特征以维持内容一致性。
result: 实验表明该方法在无需任何训练或优化的情况下，具有极强的泛化能力，能生成纹理真实且无残留阴影的图像。
conclusion: FreeShadow 证明了通过巧妙设计扩散模型的推理机制，可以在无需微调的情况下高效解决复杂的图像阴影去除任务。
---

## 摘要
现有的有监督和无监督阴影去除方法由于可用训练数据集的多样性不足，往往面临泛化能力有限的问题，而零样本方法则容易产生伪影且需要耗时的测试时优化。为了解决这些问题，我们提出了 FreeShadow，这是一种基于预训练扩散模型的无需训练阴影去除方法，它利用扩散先验在无需任何训练或优化的情况下实现阴影去除。在光照恢复方面，我们提出了一种光照迁移注意力（ITA），通过重新加权扩散模型中的自注意力图，将光照线索从非阴影区域迁移到阴影区域。在内容保留方面，我们分析了光照变化对扩散模型中自注意力图和潜在高频特征的影响，并选择性地保留光照不变分量，以在抑制残余阴影的同时保持内容保真度。我们还进一步提出了局部纹理保留重光照（LTPR），以减轻由 VAE 压缩引起的局部纹理错位。大量实验表明，我们的方法具有很强的泛化能力，并能生成逼真的无阴影图像。

## Abstract
Existing supervised and unsupervised shadow removal methods often suffer from limited generalization due to the insufficient diversity of available training datasets, while zero-shot methods tend to produce artifacts and require time-consuming test-time optimization. To address these issues, we propose FreeShadow, a training-free shadow removal method built upon pretrained diffusion models, which exploits diffusion priors for shadow removal without any training or optimization. For illumination recovery, we propose an illumination transfer attention (ITA), which re-weights the self-attention maps in diffusion model to transfer illumination cues from non-shadow to shadow regions. For content preservation, we analyze the effects of illumination variations on self-attention maps and latent high-frequency features in diffusion model, and selectively preserve illumination-invariant components to maintain content fidelity while suppressing residual shadows. We further propose local texture-preserving relighting (LTPR) to mitigate local texture misalignment caused by VAE compression. Extensive experiments demonstrate that our method achieves strong generalization and produces realistic shadow-free images.

---

## 论文详细总结（自动生成）

这是一份关于论文《FreeShadow: Training-Free Shadow Removal via Illumination Transfer and Selective Content Preservation in Diffusion Models》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在无需针对特定数据集进行训练或微调的情况下，实现高质量、高泛化能力的图像阴影去除。
*   **研究背景**：
    *   **有监督方法**：依赖成对的“阴影-无阴影”数据，但由于真实场景数据获取困难，模型在未见过的场景中泛化性差。
    *   **零样本/无监督方法**：虽然减少了对数据的依赖，但往往需要耗时的“测试时优化”（Test-time Optimization），且容易产生伪影或无法完全消除阴影边缘。
    *   **扩散模型的潜力**：预训练的扩散模型（如 Stable Diffusion）蕴含了丰富的图像分布先验，但如何将其有效地应用于阴影去除这一特定任务（同时保持内容一致性）仍是挑战。

### 2. 方法论
FreeShadow 的核心思想是利用预训练扩散模型的生成能力，通过操纵推理过程中的注意力机制和特征流来实现阴影消除。
*   **光照迁移注意力 (ITA, Illumination Transfer Attention)**：
    *   在扩散模型的自注意力层中，通过阴影掩码（Mask）识别阴影区和非阴影区。
    *   重新加权注意力图，使阴影区域的查询（Query）更多地关注非阴影区域的键（Key）和值（Value），从而将正常光照信息“迁移”到阴影区。
*   **选择性内容保留 (SCP, Selective Content Preservation)**：
    *   分析发现扩散模型的高频潜在特征对光照变化不敏感。
    *   该技术选择性地保留这些光照无关的高频分量，以确保在改变光照的同时，图像的纹理和细节内容不发生偏移。
*   **局部纹理保留重光照 (LTPR, Local Texture-Preserving Relighting)**：
    *   针对 VAE 编解码过程中可能出现的局部纹理错位问题，引入了一个细化步骤，通过在像素空间进行局部重光照调整，进一步增强结果的真实感。

### 3. 实验设计
*   **数据集**：在三个主流的阴影去除基准数据集上进行了评估：**ISTD**、**ISTD+** 和 **SRD**。
*   **对比方法**：
    *   **有监督方法**：如 DHAN、Param-Net、G2R 等。
    *   **基于扩散的方法**：如 ShadowDiffusion（需要测试时优化）。
*   **评估指标**：采用 RMSE（均方根误差），分别计算阴影区域、非阴影区域以及全图的误差。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心描述中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理效率**：由于该方法是“无需训练”（Training-free）且“无需优化”（Optimization-free）的，它避免了传统零样本方法在测试阶段数以百计的迭代优化，因此在推理速度上比需要测试时优化的方法（如 ShadowDiffusion）更具优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在三个不同分布的数据集上进行了详尽的 RMSE 指标对比。
    *   **定性对比**：展示了在复杂真实场景下的视觉效果图。
    *   **消融实验**：分别验证了 ITA、SCP 和 LTPR 三个核心组件对最终结果的贡献。
*   **充分性评价**：实验设计较为充分，通过跨数据集的测试有力地证明了其**泛化能力**。对比实验涵盖了从传统深度学习到最新扩散模型的多种路径，体现了客观性。

### 6. 主要结论与发现
*   **无需训练的优越性**：FreeShadow 在不使用任何阴影去除数据进行训练的情况下，性能达到了甚至超过了部分有监督学习方法。
*   **扩散先验的有效性**：证明了通过简单的注意力图重加权和特征选择，就可以引导预训练扩散模型完成复杂的光照编辑任务。
*   **内容一致性**：通过 SCP 技术，成功解决了生成式模型在图像修复时容易改变物体原有形状或纹理的通病。

### 7. 优点
*   **极强的泛化性**：不绑定于特定数据集，能处理各种光照和背景下的阴影。
*   **高效性**：相比于其他基于扩散模型的零样本方法，无需测试时优化，显著缩短了处理时间。
*   **即插即用**：可以直接应用于预训练的 Stable Diffusion 模型，无需修改模型架构。

### 8. 不足与局限
*   **依赖掩码质量**：该方法需要阴影掩码作为输入，如果掩码不准确（如边缘模糊或漏检），去阴影效果会受到直接影响。
*   **计算开销**：虽然不需要训练，但推理过程仍基于扩散模型的多步迭代，相比于轻量级的 CNN 有监督模型，实时性仍有差距。
*   **极端光照挑战**：在极度黑暗或信息丢失严重的阴影区，迁移过来的光照信息可能无法完全恢复底层真实的纹理。

（完）
