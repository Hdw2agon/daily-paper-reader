---
title: "Mover360: Controllable Object Manipulation in 360° Panoramic Images"
title_zh: Mover360：360°全景图像中的可控物体操控
authors: "Haoyi Zhong, Fang-Lue Zhang, Andrew Chalmers, Taehyun Rhee"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23238v1"
tags: ["query:q1"]
score: 8.0
evidence: 360度图像中的物体平移与移除
tldr: 本研究提出Mover360，一个专为360°全景图像设计的可控物体编辑框架。针对等距柱状投影（ERP）中的纬度畸变和水平环绕特性，该框架实现了物体的平移、插入和删除。通过轻量化适配预训练扩散Transformer，并结合UE5生成的配对数据集，Mover360支持点、框、掩码等多种交互方式，在保持全景一致性的同时实现了高质量的物体操控。
selection_source: fresh_fetch
motivation: 传统透视图像编辑工具难以处理360°全景图中特有的几何畸变、水平环绕及全局场景连续性问题。
method: 采用轻量化适配的扩散Transformer，通过统一的指令图编码多种控制方式，并利用UE5构建的大规模配对数据集进行训练。
result: 在合成与真实全景图基准测试中，Mover360在重建保真度、语义一致性和分布质量上均显著优于现有的透视编辑基准模型。
conclusion: Mover360为360°全景图像提供了一种高效且可控的物体级编辑方案，显著提升了全景场景创作的灵活性。
---

## 摘要
我们提出了 Mover360，这是一个针对 360° 图像的可控物体操控框架。与透视图像不同，等距柱状投影（ERP）格式的 360° 图像具有水平环绕、随纬度变化的畸变以及全局场景连续性等特点，这使得现有的透视编辑器难以生成物体级编辑，也让用户难以进行指定。为解决这一问题，Mover360 以物体平移（在现有全景图中重新定位指定物体）为核心，同时支持参考引导的插入和移除作为辅助任务。其界面通过将每个任务编码为固定提示词和紧凑的 ERP 对齐指令图，统一了基于点、边界框（bbox）和掩码的引导控制。在默认的点模式下，只需单击即可重新定位物体，使模型能够利用全景上下文和辅助深度条件推断出合理的尺寸、支撑关系和光照。在结构上，Mover360 是对预训练扩散 Transformer 的轻量化适配。为了生成成对的监督数据，我们构建了一个具有表面感知物体放置和随机光照的 UE5 数据生成流水线，从而产生了大规模成对数据，以及一个包含合成和真实全景图的双域基准测试，并为所有三项任务提供了真值。在两个测试域和两种评估协议下，Mover360 在重建保真度、语义一致性和分布质量方面均优于针对透视编辑、插入和修复的强基准模型。代码和基准数据集可在 https://zhonghaoyi.github.io/Mover360/ 获取。

## Abstract
We present Mover360, a controllable object manipulation framework for 360° images. Unlike perspective images, 360° images in equirectangular projection (ERP) exhibit horizontal wrap-around, latitude-dependent distortion, and global scene continuity, which makes object-level edits difficult for existing perspective editors to produce and for users to specify. To address this, Mover360 centers on object Translation (relocating a specified object within an existing panorama) while supporting reference-guided Insert and Remove as auxiliary tasks. Its interface unifies point-, bbox-, and mask-guided control by encoding each task into a fixed prompt and a compact, ERP-aligned instruction map. In the default point mode, a single click relocates an object, allowing the model to infer a plausible size, support, and illumination using panoramic context and an auxiliary depth condition. Structurally, Mover360 is a lightweight adaptation of a pretrained diffusion transformer. To generate paired supervision, we construct a UE5 data-generation pipeline with surface-aware object placement and randomized illumination, yielding large-scale paired data and a dual-domain benchmark of synthetic and real panoramas with ground truth for all three tasks. Across both test domains and two evaluation protocols, Mover360 outperforms strong baselines for perspective editing, insertion, and inpainting in reconstruction fidelity, semantic consistency, and distributional quality. Code and our benchmark dataset are available at https://zhonghaoyi.github.io/Mover360/.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Mover360**，一个专门为 360° 全景图像设计的可控物体操控框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：360° 全景图像通常以等距柱状投影（ERP）格式存储。与普通透视图像不同，ERP 图像具有严重的**纬度畸变**（两极拉伸）、**水平环绕性**（左右边界相连）以及**全局场景连续性**。
*   **核心问题**：现有的图像编辑工具（如基于 Stable Diffusion 的插件）主要针对透视图像设计，直接应用于全景图时，无法处理几何畸变，导致物体在移动或插入后形状扭曲、光照不自然或在边界处断裂。
*   **整体含义**：Mover360 旨在提供一个统一的框架，让用户能通过简单的交互（如点击、画框）在全景图中实现物体的平移（Translation）、插入（Insert）和移除（Remove），并保持几何与视觉的一致性。

### 2. 方法论
*   **核心思想**：将预训练的扩散 Transformer（DiT）进行轻量化适配，使其能够理解 ERP 几何特征，并通过统一的“指令图”编码多种用户控制信号。
*   **关键技术细节**：
    *   **统一指令图（Instruction Map）**：将点（Point）、边界框（BBox）和掩码（Mask）统一编码为 ERP 对齐的特征图。例如，在“平移”任务中，用户点击源物体和目标位置，模型通过指令图识别物体并推断其在目标位置的合理尺寸和姿态。
    *   **辅助深度条件**：引入深度图作为额外输入，帮助模型理解场景的 3D 结构，从而在物体移动时自动调整其大小、支撑关系（如贴合地面）和遮挡效果。
    *   **轻量化适配**：不从头训练大模型，而是对预训练的 DiT 模型进行适配，通过添加可训练的残差模块或层来处理全景图特有的空间关系。
    *   **数据生成流水线**：利用 UE5 构建了一个自动化的数据生成系统。该系统支持表面感知（Surface-aware）的物体放置和随机光照，生成了大量“编辑前-编辑后”的配对全景图数据，解决了全景编辑缺乏真值（Ground Truth）的问题。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：基于 UE5 生成的大规模配对全景图。
    *   **真实数据集**：包含真实拍摄的全景图基准测试，用于验证模型的泛化能力。
*   **任务类型**：物体平移（核心）、参考引导的插入、物体移除。
*   **对比方法**：
    *   **透视编辑基准**：如 Stable Diffusion (Inpainting/Edit)、ControlNet。
    *   **物体操控基准**：如 AnyDoor、TF-ID 等针对普通图像的 SOTA 模型。
*   **评估指标**：重建保真度（PSNR, SSIM, LPIPS）、语义一致性（CLIP Score）和分布质量（FID）。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心部分未详细列出具体的 GPU 型号、数量或确切的训练时长。通常此类基于 DiT 的适配任务需要高性能显卡（如 NVIDIA A100 或 H100）进行数天的训练，但文中强调了“轻量化适配”，暗示其训练成本低于全参数微调。
*   **代码开源**：作者提供了项目主页和代码链接，便于社区复现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了合成与真实两个域的测试。
    *   针对平移、插入、移除三项任务分别进行了定量对比。
    *   包含消融实验，验证了深度条件和指令图设计的有效性。
*   **充分性评价**：实验设计较为充分，特别是通过 UE5 生成真值数据的方法，解决了全景图像编辑评价难的问题。对比实验涵盖了当前主流的透视图像编辑模型，证明了专门针对全景几何建模的必要性。

### 6. 主要结论与发现
*   **几何感知至关重要**：Mover360 能够根据物体在 ERP 图像中的纬度位置自动调整其畸变程度，这是普通透视模型无法做到的。
*   **环绕一致性**：模型成功处理了跨越 0°/360° 边界的物体编辑，保持了水平环绕的连续性。
*   **智能推断**：在“点模式”下，仅需一次点击，模型就能利用全景上下文推断出物体在目标位置应有的光照、阴影和比例。

### 7. 优点
*   **交互友好**：支持点、框、掩码等多种交互，降低了全景编辑的门槛。
*   **高质量数据驱动**：UE5 生成的表面感知数据集为模型提供了高质量的物理规律监督。
*   **多任务统一**：一个框架同时解决平移、插入和移除，具有很高的实用价值。

### 8. 不足与局限
*   **深度依赖**：模型性能部分取决于输入深度图的质量，如果深度估计出错，物体的放置可能不自然。
*   **复杂遮挡处理**：在处理极端复杂的遮挡或极高密度的场景时，可能仍存在伪影。
*   **分辨率限制**：受限于基础扩散模型的架构，生成极高分辨率（如 8K 以上）全景图的局部细节可能仍有提升空间。

（完）
