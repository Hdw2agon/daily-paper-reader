---
title: Energy-oriented Diffusion Bridge for Image Restoration with Foundational Diffusion Models
title_zh: 面向能量的扩散桥接模型：利用基础扩散模型进行图像恢复
authors: "Jinhui Hou, Zhiyu Zhu, Junhui Hou"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10983v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于图像修复的扩散桥模型
tldr: 针对图像恢复中扩散桥模型轨迹复杂、效率低的问题，本文提出E-Bridge框架。该方法通过设计一种演化时间更短、起始于退化图像与高斯噪声混合点的桥接过程，有效降低了轨迹能量。结合一致性模型实现单步映射，并引入可调轨迹长度以平衡信息保留与生成能力。实验证明E-Bridge在多项任务中达到SOTA，且支持极简步数的高质量恢复。
selection_source: fresh_fetch
motivation: 现有的扩散桥模型因轨迹复杂且能量消耗高，导致图像恢复的采样效率和质量受限。
method: 提出E-Bridge框架，通过缩短演化时间并从熵正则化点出发来降低轨迹能量，并利用一致性模型实现高效的单步映射。
result: 在多种图像恢复任务上实现了最先进的性能，并能在极少甚至单步采样下完成高质量图像重建。
conclusion: E-Bridge通过优化扩散轨迹能量和引入可调参数，为高效、高质量的基础扩散模型图像恢复提供了新方案。
---

## 摘要
扩散桥接模型通过显式连接清晰图像分布和退化图像分布，在图像恢复领域展现出巨大潜力。然而，这类模型通常依赖于复杂且高成本的轨迹，限制了采样效率和最终的恢复质量。为了解决这一问题，我们提出了一种面向能量的扩散桥接（E-Bridge）框架，通过逼近一组低成本的流形测地线轨迹来提升模型性能。我们设计了一种新型桥接过程，其在较短的时间跨度内演化，并使反向过程从一个混合了退化图像与高斯噪声的熵正则化点开始，从而在理论上降低了所需的轨迹能量。为了高效求解该过程，我们借鉴一致性模型（consistency models）的思路，学习了一个单步映射函数，并通过针对该轨迹定制的连续时间一致性目标进行优化，从而能够解析地将轨迹上的任意状态映射到目标图像。值得注意的是，我们框架中的轨迹长度成为了一个可调节的任务自适应旋钮，使模型能够针对不同退化程度的任务（如去噪与超分辨率），自适应地平衡信息保持与生成能力。广泛的实验表明，E-Bridge 在多种图像恢复任务中均达到了最先进的性能，并能通过单步或极少数采样步骤实现高质量恢复。项目主页：https://jinnh.github.io/E-Bridge/。

## Abstract
Diffusion bridge models have shown great promise in image restoration by explicitly connecting clean and degraded image distributions. However, they often rely on complex and high-cost trajectories, which limit both sampling efficiency and final restoration quality. To address this, we propose an Energy-oriented diffusion Bridge (E-Bridge) framework to approximate a set of low-cost manifold geodesic trajectories to boost the performance of the proposed method. We achieve this by designing a novel bridge process that evolves over a shorter time horizon and makes the reverse process start from an entropy-regularized point that mixes the degraded image and Gaussian noise, which theoretically reduces the required trajectory energy. To solve this process efficiently, we draw inspiration from consistency models to learn a single-step mapping function, optimized via a continuous-time consistency objective tailored for our trajectory, so as to analytically map any state on the trajectory to the target image. Notably, the trajectory length in our framework becomes a tunable task-adaptive knob, allowing the model to adaptively balance information preservation against generative power for tasks of varying degradation, such as denoising versus super-resolution. Extensive experiments demonstrate that our E-Bridge achieves state-of-the-art performance across various image restoration tasks while enabling high-quality recovery with a single or fewer sampling steps. Our project page is https://jinnh.github.io/E-Bridge/.

---

## 论文详细总结（自动生成）

以下是对论文《Energy-oriented Diffusion Bridge for Image Restoration with Foundational Diffusion Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：扩散模型在图像恢复（IR）中表现优异，但存在两大痛点：一是标准扩散模型从纯噪声开始，推理速度慢且能量消耗高；二是现有的扩散桥接模型（直接连接退化图像与清晰图像）往往采用预定义的、次优的轨迹（如布朗桥），这些轨迹通常包含冗余的“再加噪”阶段，导致效率和恢复质量受限。
*   **核心问题**：如何设计一种低能量、符合数据流形测地线（Geodesic）的恢复轨迹，并实现极速（单步或极少数步）的高质量图像恢复？

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将图像恢复视为流形间的运输问题，通过重新设计扩散桥轨迹，使其在理论上逼近能量最小化的测地线，并结合一致性模型（Consistency Models）实现单步映射。
*   **关键技术细节**：
    *   **面向能量的扩散桥（E-Bridge）**：设计了一种新型随机过程，其反向恢复过程从一个“熵正则化点”开始（退化图像与高斯噪声的混合），从而避开了传统桥接模型中低效的再加噪阶段，缩短了演化时间。
    *   **测地线轨迹参数化**：证明了在受控时间范围内，线性插值轨迹是动能最小化的唯一解，并利用预训练的 Rectified Flow 模型（如 FLUX）作为动态引导场。
    *   **一致性 E-Bridge 求解器**：通过对轨迹方程进行代数求逆，推导出闭式解架构。利用连续时间一致性目标进行训练，使模型能将轨迹上任意点直接映射到清晰图像 $X_0$。
    *   **任务自适应旋钮 ($T_0$)**：引入可调参数 $T_0$ 来控制轨迹长度。小 $T_0$ 用于轻微退化（强调信息保持），大 $T_0$ 用于严重退化（强调生成能力/幻觉）。

### 3. 实验设计
*   **任务场景**：涵盖五大挑战性任务：超分辨率（SR）、去噪（Denoising）、雨滴去除（Raindrop Removal）、低光照增强（Low-light Enhancement）和图像去摩尔纹（Demoiréing）。
*   **数据集**：DIV2K, Flickr2K, Raindrop, LOLv1, UHDM。
*   **对比方法**：包括 DiffBIR, AutoDIR, UniDB, IRBridge, MaRS, UniDB++, DA-CLIP, AdaIR 等多种 SOTA 扩散模型和桥接模型。
*   **评价指标**：PSNR（保真度）、LPIPS、FID、NIQE、MUSIQ（感知质量）。

### 4. 资源与算力
*   **算力设备**：使用 **NVIDIA A800 GPU** 进行实验。
*   **模型基础**：基于大规模预训练模型 **FLUX-dev**。
*   **训练细节**：微调 10,000 次迭代，学习率 $2 \times 10^{-5}$，通过梯度累积实现有效 Batch Size 为 128，训练补丁大小为 $1024 \times 1024$。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同任务上进行了广泛测试，并提供了大量的定性视觉对比。
*   **消融实验**：
    *   验证了测地线轨迹相对于标准扩散轨迹和传统桥接轨迹的优越性。
    *   对比了一致性求解器与传统迭代 ODE 求解器（如 DDIM）的效率。
    *   深入分析了参数 $T_0$ 对不同任务性能的影响。
*   **充分性评价**：实验设计非常充分，不仅涵盖了合成退化，还测试了真实世界（In-the-wild）的超分辨率表现，客观地展示了模型在感知质量与计算效率之间的平衡。

### 6. 主要结论与发现
*   **性能领先**：E-Bridge 在感知指标（LPIPS, FID, MUSIQ）上一致达到 SOTA，尤其在极低采样步数（1-10步）下表现惊人。
*   **效率极高**：仅需 5-10 步 NFE 即可超越传统方法 50-100 步的效果，甚至支持单步高质量生成。
*   **自适应性**：通过调节 $T_0$，模型能灵活应对从“简单修复”到“复杂幻觉生成”的不同需求。

### 7. 优点
*   **理论严谨**：从随机最优控制和能量最小化角度重新审视扩散桥，提供了坚实的数学证明。
*   **推理极速**：结合一致性模型，解决了扩散模型推理慢的顽疾。
*   **即插即用**：能够很好地适配现有的基础扩散模型（如 FLUX），充分利用强大的生成先验。
*   **感知力强**：生成的图像纹理细节丰富，视觉效果甚至优于带有压缩伪影的真值（GT）。

### 8. 不足与局限
*   **空间均匀性限制**：$T_0$ 是全局参数，对于空间异质退化（如图像局部有雨、局部清晰）可能难以实现局部最优的权衡。
*   **计算开销**：虽然步数减少了，但由于基于 FLUX-dev 这种超大模型，单步推理对显存（VRAM）的要求依然很高，难以在消费级显卡上轻松部署。
*   **指标偏差**：在 PSNR 等像素级指标上并不总是占优，因为它更倾向于生成视觉真实的细节而非机械匹配像素。

（完）
