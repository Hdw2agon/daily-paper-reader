---
title: Training-Free Inpainting Across Domains with a Frozen Text-to-Image Diffusion Model
title_zh: 使用冻结的文本生成图像扩散模型进行跨领域的无需训练图像修复
authors: "Zhenhuan Wang, Fengyi Yuan"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00862v1"
tags: ["query:q3"]
score: 10.0
evidence: 基于冻结扩散模型的免训练图像补全
tldr: 本研究提出 Step-PI，一种无需训练即可利用冻结的通用文本生成图像扩散模型进行跨领域图像修复的方法。该方法无需特定权重的训练或适配，通过引入边界-内部潜空间反馈、持久化状态及预定义的释放调度，实现了对潜空间的精细控制。实验表明，Step-PI 在 CelebA-HQ、AFHQ 和 Places2 等多个数据集上表现出色，在无需训练的基准测试中显著优于同类无需训练的方法，为通用模型的跨领域复用提供了高效的新路径。
selection_source: fresh_fetch
motivation: 旨在解决现有图像修复方法依赖特定领域训练或微调的问题，探索如何利用冻结的通用扩散模型实现跨领域的图像修复。
method: 提出 Step-PI 方法，通过边界-内部潜空间反馈、持久化 PI 状态和预定义的四阶段释放调度，在推理过程中对潜空间进行控制。
result: 在多个自然图像领域的对比实验中，Step-PI 在所有评估指标上均优于 LanPaint 和 PILOT 等现有的无需训练的基准模型。
conclusion: 该研究证明了通过测试时潜空间控制，无需额外训练即可将冻结的通用扩散模型有效地重新用于高质量的跨领域图像修复任务。
---

## 摘要
我们展示了冻结的通用文本生成图像扩散模型可以在三种评估的自然图像领域中，通过单一固定的控制器配置执行条件图像修复，而无需针对修复任务的权重训练、针对数据集的权重适配或学习特定的修复条件通道。Step-PI 通过边界-内部潜空间反馈、持久 PI 状态以及预定义的四场释放调度（在反向轨迹中调节控制器信号）增强了已知区域投影。该控制器仅在与 Main35 不相交的 CelebA-HQ 试点数据上开发，无需修改即可迁移至 AFHQ 和 Places2。在针对相同 3,500 个案例的两次同领域对比中，添加持久状态并将均匀释放替换为预定义调度，均提升了所有 15 个数据集-指标单元格的表现；在两次对比中，所有五个指标的 95% 自举区间均不包含零。在描述性的原生路径对比中，Step-PI 在所有五个同数据集宏观指标上均领先于 LanPaint 和 PILOT（使用原生 SD1.5 的最接近的无需训练基准模型）。经过修复训练的系统在绝对指标上保持领先，但依赖于大量的针对修复任务的离线优化。我们的方法提供了一种互补的途径，通过测试时潜空间控制，将冻结的通用文本生成图像模型重新用于跨领域图像修复。

## Abstract
We show that a frozen generic text-to-image diffusion model can perform conditional inpainting across three evaluated natural-image domains with one fixed controller configuration, without inpainting-specific weight training, dataset-specific weight adaptation, or learned inpainting-specific conditioning channels. Step-PI augments known-region projection with boundary-interior latent feedback, persistent PI state, and a predefined four-field release schedule that modulates controller signals along the reverse trajectory. Developed only on Main35-disjoint CelebA-HQ pilots, the controller transfers unchanged to AFHQ and Places2. Across two field-identical comparisons on the same 3,500 cases, adding persistent state and replacing uniform release with the predefined schedule each improve all 15 dataset-metric cells; 95% bootstrap intervals exclude zero for all five metrics in both comparisons. In descriptive native-route comparisons, Step-PI leads LanPaint and PILOT (the closest evaluated training-free baselines using vanilla SD1.5) on all five equal-dataset macro metrics. Inpainting-trained systems retain the absolute metric leads but rely on substantial inpainting-specific offline optimization. Our method provides a complementary approach for repurposing a frozen generic text-to-image model for cross-domain inpainting through test-time latent control.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Step-PI** 的新方法，旨在利用完全冻结（Frozen）的预训练文本生成图像（T2I）扩散模型，在无需任何额外训练或微调的情况下，实现高质量的跨领域图像修复（Inpainting）。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的图像修复方法通常需要针对特定任务进行权重训练，或者针对特定领域（如人脸、风景）进行微调。这不仅消耗大量算力，还限制了模型的通用性。
*   **核心问题**：能否直接利用现有的、通用的、未经修复任务训练的扩散模型（如 Stable Diffusion 1.5），通过纯推理侧的控制手段，在不同图像领域实现高质量的修复？
*   **整体含义**：Step-PI 证明了通过精细的潜空间（Latent Space）控制，无需改变模型参数，即可将通用的 T2I 模型转化为强大的跨领域修复工具。

### 2. 方法论
Step-PI 的核心思想是在扩散模型的反向去噪轨迹中，通过控制器信号对潜空间进行动态干预。其关键技术包括：
*   **边界-内部潜空间反馈（Boundary-Interior Latent Feedback）**：利用已知区域（边界）和待修复区域（内部）之间的潜空间梯度差异作为反馈信号，引导模型生成与原图衔接自然的纹理。
*   **持久化 PI 状态（Persistent PI State）**：在扩散的各个步骤之间维持一个持久的控制器状态，以确保修复过程中的时间一致性和信号稳定性，避免生成过程中的突变。
*   **四阶段释放调度（Four-Field Release Schedule）**：预定义了一个非均匀的信号释放计划。在去噪的不同阶段（从高噪声到低噪声），动态调节控制器信号的强度，以平衡生成内容的创造性与已知区域的保真度。
*   **算法流程**：在推理时，模型接收带掩码的图像和文本提示，通过上述三个机制在每一步迭代中修正潜空间变量，最后解码生成修复后的图像。

### 3. 实验设计
*   **数据集**：涵盖了三个截然不同的自然图像领域：
    *   **CelebA-HQ**（人脸）
    *   **AFHQ**（动物面部）
    *   **Places2**（自然场景）
*   **Benchmark（基准）**：使用了 3,500 个测试案例进行评估。
*   **对比方法**：
    *   **免训练基准**：LanPaint 和 PILOT（这些是目前基于原生 SD1.5 最先进的免训练修复方法）。
    *   **训练型方法**：作为性能上限参考，对比了经过专门修复训练的系统。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出具体的 GPU 型号和总训练时长。
*   **隐含信息**：由于该方法是 **Training-Free（免训练）** 的，其核心开销仅在于推理阶段。相比需要数千 GPU 小时进行训练的方法，Step-PI 的资源消耗极低，主要取决于单次推理的耗时。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了广泛测试，总计 3,500 个案例，样本量足以支撑统计显著性。
*   **消融实验**：研究者对“持久状态”和“释放调度”进行了对比实验，结果显示这两个组件在所有 15 个数据集-指标单元格中均带来了性能提升。
*   **客观性**：使用了 95% 自举置信区间（Bootstrap Intervals）进行误差分析，证明了改进的稳健性。对比实验均在相同的硬件和基础模型（SD1.5）下进行，确保了公平性。

### 6. 主要结论与发现
*   **跨领域通用性**：Step-PI 使用单一固定的控制器配置，无需修改即可在人脸、动物和风景三个领域取得优异效果。
*   **性能领先**：在所有评估指标（如 PSNR, SSIM, FID 等）上，Step-PI 均显著优于现有的免训练方法（LanPaint 和 PILOT）。
*   **控制的有效性**：证明了通过测试时（Test-time）的潜空间控制，可以有效弥补通用模型在特定下游任务上的能力缺失。

### 7. 优点
*   **零成本迁移**：无需针对新领域重新训练模型，极大地降低了应用门槛。
*   **即插即用**：可以适配任何冻结的扩散模型，具有很强的灵活性。
*   **逻辑严密**：通过引入持久状态和调度机制，解决了传统免训练方法中常见的生成不连贯问题。

### 8. 不足与局限
*   **绝对性能差距**：虽然在免训练方法中领先，但其绝对指标仍低于经过大规模修复数据专门训练的模型。
*   **推理延迟**：由于引入了额外的反馈计算和控制器逻辑，单次图像生成的推理速度可能略慢于简单的投影方法。
*   **依赖底座**：修复质量高度依赖于所使用的预训练 T2I 模型的原始生成能力，如果底座模型对某些罕见物体理解不足，修复效果也会受限。

（完）
