---
title: "Sparse-to-Complete: From Sparse Image Captures to Complete 3D Scenes"
title_zh: Sparse-to-Complete：从稀疏图像捕获到完整的三维场景
authors: "Yiyang Shen, Yin Yang, Kun Zhou, Tianjia Shao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05664v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于场景特定图像修复与补全的扩散模型
tldr: 针对极稀疏视角（6-8张图）下的3D场景重建难题，本文提出S2C-3D框架。该框架结合了场景特定的扩散模型修复技术、免训练的视角一致性采样过程以及相机轨迹规划方案。通过微调扩散模型以消除域差异，并利用轨迹规划确保场景全覆盖，S2C-3D能够从极少输入中生成高保真、无缺失区域且无伪影的3D高斯场景，显著提升了稀疏视角重建的质量。
selection_source: fresh_fetch
motivation: 旨在解决从极稀疏图像输入中重建完整且高保真3D场景时存在的区域缺失和伪影问题。
method: 提出了一种包含场景特定扩散模型修复、视角一致性条件采样和相机轨迹规划的综合重建框架。
result: 实验表明，该方法在极稀疏输入下生成的3D场景在完整性和图像质量上均优于现有最先进技术。
conclusion: S2C-3D通过结合扩散模型的生成能力与几何一致性约束，实现了从少量捕获到完整3D场景的高效转换。
---

## 摘要
我们提出了 S2C-3D，这是一种新颖的稀疏视图三维重建框架，仅需 6 到 8 张图像即可实现高保真且完整的场景重建。我们的框架包含三个核心组件：一个用于特定场景图像修复的专用扩散模型、一个在扩散模型中用于精细化高斯优化的免训练视图一致性条件采样过程，以及一个确保全面场景覆盖的相机轨迹规划方案。该专用扩散模型通过在输入视图及其对应的退化副本上微调预训练架构而开发。对场景分布的适应使模型能够修复高斯渲染图，同时有效消除领域差距。同时，轨迹规划方案通过将每个新采样的相机与其两个最近邻连接来优化场景覆盖范围。通过迭代构建路径并仅保留那些显著增强可见性的路径，该方案建立了一条覆盖整个场景的轨迹。为了解决多视图冲突，视图一致性条件采样过程量化了相邻修复图像之间的一致性。该信息作为条件注入到冻结扩散模型的采样过程中，从而在无需额外训练的情况下促进视图一致性图像的生成。因此，我们的方法生成了对伪影具有鲁棒性的高保真三维高斯（3D Gaussians）。实验结果表明，S2C-3D 优于现有最先进的方法，在输入极度稀疏的情况下，能够构建出没有区域缺失、模糊或其他伪影的高质量场景。源代码和数据可在 https://gapszju.github.io/S2C-3D 获取。

## Abstract
We introduce S2C-3D, a novel sparse-view 3D reconstruction framework for high-fidelity and complete scene reconstruction from as few as six to eight images. Our framework features three components: a specialized diffusion model for scene-specific image restoration, a training-free view-consistency conditioned sampling process in the diffusion model for refined Gaussian optimization, and a camera trajectory planning scheme to ensure comprehensive scene coverage. The specialized diffusion model is developed by finetuning a pretrained architecture on the input views and their corresponding degraded counterparts. The adaptation to the scene distribution allows the model to repair Gaussian renderings while effectively eliminating domain gaps. Meanwhile, the trajectory planning scheme optimizes scene coverage by connecting each newly sampled camera to its two nearest neighbors. By iteratively constructing paths and retaining only those that significantly enhance visibility, the scheme establishes a trajectory that covers the entire scene. To address multi-view conflicts, the view-consistency conditioned sampling process quantifies the consistency between neighboring repaired images. This information is injected as a condition into the sampling process of the frozen diffusion model, facilitating the generation of view-consistent images without additional training. Consequently, our approach produces high-fidelity 3D Gaussians that are robust to artifacts. Experimental results demonstrate that S2C-3D outperforms state-of-the-art methods, constructing high-quality scenes that are free from missing regions, blurring, or other artifacts with very sparse inputs. The source code and data are available at https://gapszju.github.io/S2C-3D.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **S2C-3D** 的新型三维重建框架，旨在解决从极稀疏图像（仅 6-8 张）中重建高保真且完整三维场景的难题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的三维重建方法（如 NeRF 或 3DGS）通常需要数十张甚至上百张图像。在极稀疏视角（6-8 张图）下，现有方法面临三大挑战：
    1.  **视图不一致性**：扩散模型修复的图像在不同视角间存在冲突，导致重建结果模糊或产生伪影。
    2.  **领域差距（Domain Gap）**：预训练的通用扩散模型难以完美适应特定场景的细节。
    3.  **场景覆盖不足**：简单的相机插值无法覆盖遮挡或不可见的区域，导致重建场景不完整。
*   **研究背景**：随着 VR/AR 和机器人技术的发展，简化数据获取过程、实现“随手拍几张即可建模”具有重要的应用价值。

### 2. 方法论
S2C-3D 框架由三个关键组件组成：
*   **场景专用扩散模型微调**：
    *   利用输入的少量真实图像，通过添加随机噪声和掩码（Masking）生成退化图像对。
    *   在这些图像对上微调预训练扩散模型（如 Difix3D+），使其学习特定场景的分布，消除领域差距。
*   **相机轨迹规划方案**：
    *   **信息增益度量**：利用 $\pi^3$ 模型生成的点云构建泊松表面，并在其包围盒（OBB）内采样。
    *   **贪婪规划**：通过计算新相机视角对场景未覆盖区域的“信息增益”，迭代构建一条能够覆盖整个场景的虚拟相机轨迹。
*   **免训练的视图一致性条件采样**：
    *   在 3D 高斯优化阶段，冻结扩散模型。
    *   **能量函数**：利用深度图和相机位姿将相邻视角的修复图像投影到当前视角，计算像素级差异。
    *   **梯度引导**：将该差异作为一致性约束（能量梯度）注入扩散模型的采样过程，引导生成在空间上更一致的图像。

### 3. 实验设计
*   **数据集**：
    *   **Replica**：8 个合成室内场景。
    *   **ScanNet++**：100 个真实室内场景。
    *   **S2C-Scene**：作者自采的 11 个真实场景。
*   **基准方法（Baselines）**：对比了 3DGS、DNGaussian、Difix3D+、GenFusion 和 VD-3DGS 等最先进的稀疏视图重建方法。
*   **评价指标**：PSNR、SSIM 和 LPIPS（感知相似度）。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX 4090 GPU 和 AMD Ryzen 9 9950X3D CPU。
*   **训练细节**：
    *   扩散模型微调阶段：3000 步。
    *   优化器：AdamW，学习率 $2 \times 10^{-5}$。
    *   高斯精细化阶段：4 个细化步骤（Refine steps）。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同性质的数据集（合成、大规模真实、自采真实）上进行了广泛测试，涵盖了 4-8 张不同稀疏度的输入设置。
*   **消融实验**：针对“噪声/掩码微调”、“相机轨迹规划”和“视图一致性条件”分别做了对比实验，验证了每个模块的必要性。
*   **充分性评价**：实验设计较为全面，不仅有定量指标对比，还有大量的定性视觉对比和消融分析，客观地展示了该方法在处理大面积缺失区域时的优势。

### 6. 主要结论与发现
*   **高质量重建**：S2C-3D 在极稀疏输入下显著优于现有 SOTA 方法，生成的场景更完整，细节更清晰。
*   **消除伪影**：通过视图一致性引导，有效解决了扩散模型在生成未见区域时常见的“漂浮物”和模糊问题。
*   **全场景覆盖**：轨迹规划确保了即使是输入图像未直接捕捉到的角落（如天花板、桌底）也能得到合理的补全。

### 7. 优点与亮点
*   **场景自适应**：通过简单的微调让通用模型“记住”特定场景，解决了生成模型常见的风格不匹配问题。
*   **免训练一致性**：提出了一种无需重新训练扩散模型即可引入多视图几何约束的方法，具有很强的灵活性。
*   **自动化规划**：相机轨迹规划不仅考虑了插值，还考虑了全局覆盖，这在稀疏重建中是一个被长期忽视的重点。

### 8. 不足与局限
*   **应用限制**：目前主要针对封闭的室内场景，对于开放的室外场景，其重建范围仍受限于输入视图的包围区域。
*   **几何风险**：在凹陷房间（Concave rooms）中可能会产生墙壁外的异常区域；相机采样有时会进入物体内部或产生低质量视角。
*   **算法性质**：轨迹规划采用的是贪婪算法，可能并非全局最优解。
*   **计算开销**：虽然是稀疏输入，但每个场景都需要进行扩散模型微调，这增加了单场景的重建时间成本。

（完）
