---
title: "ORCA: Occlusion-Aware Refinement and Completion for Novel View Synthesis"
title_zh: ORCA：面向新视角合成的遮挡感知细化与补全
authors: "Weronika Jakubowska, Maciej Zięba, Przemysław Spurek"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17450v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于新视角合成的遮挡感知补全
tldr: ORCA是一种用于单图新视角合成的遮挡感知方法。针对现有方法过度依赖生成式模型填补遮挡区域导致幻觉的问题，ORCA通过高斯锚点表示法，利用单目深度信息构建3D结构，并根据遮挡区域大小采取差异化策略：小区域通过现有RGB-D信息修复，大区域才使用生成式补全，从而在减少幻觉的同时更好地保持了场景的原始结构与内容。
selection_source: fresh_fetch
motivation: 现有单图新视角合成方法过度依赖生成模型填补遮挡区域，导致产生不必要的图像幻觉。
method: 提出一种基于高斯锚点的遮挡感知方法，通过区分遮挡区域大小，优先利用现有场景信息修复，仅对大面积缺失进行生成式补全。
result: 在DIV2K数据集上，ORCA在MUSIQ和CLIP-IQA等指标上均显著优于现有方法VistaDream。
conclusion: 证明了通过有效复用已重建场景中的现有信息，可以显著减少对生成式补全的依赖并提升新视角合成质量。
---

## 摘要
从单张图像进行新视角合成是一个本质上具有歧义的问题。随着相机远离输入视点，原本被遮挡的区域变得可见，从而暴露出重建场景中缺失的几何结构和空洞。现有方法通常依赖生成模型来补全这些区域。然而，许多此类伪影实际上是深度边界附近的微小间隙，并不需要生成新的场景内容。为了避免昂贵的图像生成过程，我们引入了 ORCA，这是一种从单张图像重建并补全可探索 3D 场景的遮挡感知方法。ORCA 首先利用单目深度信息将 3D 结构引入高斯锚点（Gaussian-anchor）表示中，同时保留原始的相机射线对应关系。在场景探索过程中，缺失区域会根据其大小和结构进行处理。小的遮挡区域通过重建中已有的 RGB-D 信息进行修复，而生成式修复仅保留用于无法从场景中可靠恢复的较大区域。新的高斯锚点在局部进行添加和优化，且不会修改现有的表示。通过减少对生成式修复的不必要依赖，ORCA 限制了生成引起的幻觉，并更好地保留了原始场景的内容和结构。在 DIV2K 数据集上，ORCA 在所有报告的指标上均提升了新视角合成的质量，将 MUSIQ 从 61.60 提高到 68.71，将 CLIP-IQA 从 0.474 提高到 0.574。这些结果表明，通过复用重建场景中已有的信息，可以有效修复许多新视角下的伪影。

## Abstract
Novel-view synthesis from a single image is a fundamentally ambiguous problem. As the camera moves away from the input viewpoint, previously hidden regions become visible, exposing missing geometry and holes in the reconstructed scene. Existing methods often rely on generative models to complete such regions. However, many of these artifacts are small gaps near depth boundaries and do not require generating new scene content.   In order to eliminate expensive process of generating image we introduce ORCA, an occlusion-aware method for reconstructing and completing explorable 3D scenes from a single image. ORCA first introduces 3D structure into a Gaussian-anchor representation using monocular depth while preserving the original camera-ray correspondence. During scene exploration, missing regions are handled based on their size and structure. Small disocclusions are repaired using RGB-D information already available in the reconstruction, while generative inpainting is reserved for larger regions that cannot be reliably recovered from the scene. New Gaussian anchors are added and optimized locally without modifying the existing representation. By reducing unnecessary reliance on generative inpainting, ORCA limits generation-induced hallucinations and better preserves the content and structure of the original scene.   On DIV2K, ORCA improves novel-view quality over VistaDream across all reported metrics, increasing MUSIQ from 61.60 to 68.71 and CLIP-IQA from 0.474 to 0.574. These results show that many novel-view artifacts can be repaired effectively by reusing information already present in the reconstructed scene.

---

## 论文详细总结（自动生成）

以下是对论文《ORCA: Occlusion-Aware Refinement and Completion for Novel View Synthesis》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：从单张图像进行新视角合成（Novel View Synthesis）存在本质的几何歧义。当相机移动时，原本被遮挡的区域会显露出来，导致重建场景出现空洞或几何缺失。
*   **动机**：现有主流方法过度依赖生成式模型（如扩散模型）来填补这些缺失区域。然而，许多伪影仅是深度边界附近的微小间隙，并不需要“无中生有”地生成新内容。过度生成会导致场景出现不必要的幻觉，破坏原始场景的结构一致性。

### 2. 方法论：ORCA
*   **核心思想**：引入“遮挡感知”机制，区分缺失区域的性质。优先利用已重建场景中的 RGB-D 信息进行几何修复，仅对无法恢复的大面积区域才调用生成式补全。
*   **关键技术细节**：
    *   **初始表示**：基于高斯锚点（Gaussian-anchor）表示，利用输入图像及其外绘（outpainted）扩展进行初始化。
    *   **深度引导变形**：利用单目深度估计（Depth Pro）对高斯锚点进行 3D 变形，通过归一化相对深度计算位移，在保持相机射线对应关系的前提下引入 3D 结构。
    *   **分级修复策略**：
        *   **局部修复**：对于小间隙，从周围背景提取 RGB-D 信息，通过选择较远处的背景样本来填充，避免前景表面被错误拉伸。
        *   **生成式补全**：仅对大面积、紧凑的缺失区域使用 Stable Diffusion 进行修复，并限制每场景最多两次生成操作，确保生成内容与现有几何对齐。

### 3. 实验设计
*   **数据集**：DIV2K（99 张验证集图像）和 RealmDreamer 子集（11 个合成场景）。
*   **对比方法**：主要与 VistaDream 进行对比。
*   **评估指标**：MUSIQ、CLIP-IQA、LLaVA-IQA（包含噪声、边缘、结构、细节、质量等维度）以及 TSED（衡量跨视角几何一致性）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA A40 GPU。
*   **兼容性**：作者验证了该流水线也可在 NVIDIA RTX 4060 上运行。
*   **训练时长**：初始表示训练最多 30,000 次迭代，微调阶段最多 20,000 次迭代，均设有早停机制。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了自然图像（DIV2K）和合成场景（RealmDreamer），并进行了场景级细分对比。
*   **客观性**：使用了相同的相机轨迹进行对比，评估指标全面，既包含感知质量指标，也包含几何一致性指标（TSED），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能提升**：ORCA 在所有指标上均显著优于 VistaDream。在 DIV2K 上，MUSIQ 从 61.60 提升至 68.71，CLIP-IQA 从 0.474 提升至 0.574。
*   **几何一致性**：TSED 指标的显著提升证明了 ORCA 在保持跨视角几何稳定性方面具有明显优势。
*   **核心价值**：证明了通过有效复用现有场景信息，可以大幅减少对生成式模型的依赖，从而在提升视觉质量的同时，更好地保留了原始场景的结构。

### 7. 优点
*   **结构保留**：通过优先使用几何修复而非生成修复，有效抑制了生成式幻觉。
*   **高效性**：减少了昂贵的生成式推理次数，利用已有的 RGB-D 信息进行局部修复，计算效率更高。
*   **鲁棒性**：通过深度引导的几何变形，使初始场景表示更具 3D 真实感。

### 8. 不足与局限
*   **生成依赖**：虽然减少了依赖，但对于完全不可见的区域，仍需依赖生成模型，其质量受限于预训练模型的先验能力。
*   **深度估计限制**：依赖单目深度估计（Depth Pro），若深度估计在复杂场景下出现偏差，会直接影响高斯锚点的几何布局。
*   **修复上限**：对于极度复杂的遮挡，若局部背景信息不足且生成式补全次数达到上限，修复效果可能受限。

（完）
