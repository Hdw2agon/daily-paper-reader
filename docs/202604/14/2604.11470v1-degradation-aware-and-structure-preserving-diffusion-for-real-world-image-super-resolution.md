---
title: Degradation-Aware and Structure-Preserving Diffusion for Real-World Image Super-Resolution
title_zh: 面向真实场景图像超分辨率的退化感知与结构保持扩散模型
authors: "Yang Ji, Zonghao Chen, Zhihao Xue, Junqin Hu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11470v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 基于扩散模型的图像修复与超分辨率
tldr: 针对真实场景超分辨率中退化过程复杂且难以建模的问题，本文提出了一种退化感知且结构保持的扩散模型框架（DASP-SR）。该方法通过引入轻量级的退化感知令牌注入（DTI）来编码退化统计信息，并利用空间非对称噪声注入（SANI）根据局部边缘强度调节噪声，从而在提升恢复图像真实感的同时有效保留结构细节。实验证明该方法在感知质量和失真权衡方面表现优异。
selection_source: fresh_fetch
motivation: 真实世界图像退化复杂且异构，现有扩散模型难以显式建模这些退化并有效保持图像结构。
method: 提出了退化感知令牌注入（DTI）来融合退化统计量，以及空间非对称噪声注入（SANI）来根据边缘强度调制噪声。
result: 在DIV2K和RealSR数据集上，该方法在无参考感知质量和视觉真实感方面优于现有基准模型。
conclusion: DTI和SANI作为轻量级插件，能显著提升扩散模型在处理真实世界超分辨率任务时的退化适应能力和结构保持能力。
---

## 摘要
真实场景图像超分辨率对扩散模型而言具有挑战性，因为真实的退化过程复杂、异构，且很少被显式建模。我们提出了一种用于真实场景超分辨率的退化感知与结构保持扩散框架。具体而言，我们引入了退化感知令牌注入（Degradation-aware Token Injection），它从低分辨率输入中编码轻量级的退化统计信息，并将其与语义条件特征融合，从而实现显式的退化感知修复。我们进一步提出了空间非对称噪声注入（Spatially Asymmetric Noise Injection），通过局部边缘强度调制扩散噪声，以便在训练过程中更好地保持结构区域。这两个模块都是所采用的扩散超分辨率框架的轻量级插件，仅需对条件流水线进行微小修改。在 DIV2K 和 RealSR 数据集上的实验表明，与最近的基准方法相比，我们的方法提供了具有竞争力的无参考感知质量和视觉上更真实的修复结果，同时保持了良好的感知-失真权衡。消融实验证实了每个模块的有效性以及它们结合时的互补增益。代码和模型已在 https://github.com/jiyang0315/DASP-SR.git 公开。

## Abstract
Real-world image super-resolution is particularly challenging for diffusion models because real degradations are complex, heterogeneous, and rarely modeled explicitly. We propose a degradation-aware and structure-preserving diffusion framework for real-world SR. Specifically, we introduce Degradation-aware Token Injection, which encodes lightweight degradation statistics from low-resolution inputs and fuses them with semantic conditioning features, enabling explicit degradation-aware restoration. We further propose Spatially Asymmetric Noise Injection, which modulates diffusion noise with local edge strength to better preserve structural regions during training. Both modules are lightweight add-ons to the adopted diffusion SR framework, requiring only minor modifications to the conditioning pipeline. Experiments on DIV2K and RealSR show that our method delivers competitive no-reference perceptual quality and visually more realistic restoration results than recent baselines, while maintaining a favorable perception--distortion trade-off. Ablations confirm the effectiveness of each module and their complementary gains when combined. The code and model are publicly available at https://github.com/jiyang0315/DASP-SR.git.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DASP-SR**（Degradation-Aware and Structure-Preserving Diffusion）的框架，旨在解决真实场景图像超分辨率（SR）中退化建模困难和结构丢失的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：真实场景中的图像退化（如模糊、噪声、压缩伪影）极其复杂且空间多变，现有的扩散模型（Diffusion Models）通常将低分辨率（LR）图像作为整体条件输入，难以显式区分图像内容与退化模式，导致修复结果出现歧义或不自然的“幻觉”。
*   **研究背景**：
    *   传统的回归方法（如基于 PSNR 的模型）往往导致结果过于平滑。
    *   标准扩散模型在训练时采用均匀高斯噪声，这会过早抹除图像的边缘等高频结构信息，迫使模型从零开始“发明”几何结构，而非基于原有残余进行精细化修复。

### 2. 方法论
论文提出了两个轻量级的核心模块，作为现有扩散 SR 框架的插件：

*   **退化感知令牌注入（DTI, Degradation-aware Token Injection）**：
    *   **核心思想**：显式提取退化特征并将其注入模型。
    *   **技术细节**：从 LR 图像中提取一个 **6 维退化描述符**，包括：模糊程度（拉普拉斯方差）、噪声水平（局部残差）、JPEG 伪影（8x8 边界不连续性）、边缘密度（Sobel 梯度）、亮度及对比度。
    *   **流程**：通过一个轻量级 MLP 将该 6 维向量映射为 512 维的退化令牌（Token），并将其与语义令牌拼接，作为 Cross-Attention 层的输入。该令牌还会根据扩散步数（Timestep）进行动态缩放和偏移调制。

*   **空间非对称噪声注入（SANI, Spatially Asymmetric Noise Injection）**：
    *   **核心思想**：在训练阶段根据图像局部几何结构调节噪声强度。
    *   **技术细节**：利用 Sobel 算子生成边缘强度图 $E$。在加噪过程中，对边缘区域注入较少的噪声，对平滑区域注入标准噪声。
    *   **公式**：调制后的噪声 $\epsilon' = \epsilon \odot (1 - \lambda E)$，其中 $\lambda$ 控制调制强度（默认 0.6）。这使得模型在训练时能更好地感知并保留图像的“几何骨架”。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：大规模图像修复数据集 LSDIR，并使用 Real-ESRGAN 的退化流水线合成训练对。
    *   **测试集**：DIV2K（合成复杂退化）和 RealSR（真实场景 LR-HR 对）。
*   **对比方法**：包括 Real-ESRGAN（GAN 类）、SeeSR、StableSR、ResShift、PASD、DiffBIR、OSEDiff 等多种 SOTA 扩散模型方法。
*   **评价指标**：
    *   **有参考指标**：PSNR, SSIM, LPIPS。
    *   **无参考感知指标**：NIQE, NRQM, PI, CLIP-IQA, MUSIQ。

### 4. 资源与算力
*   **算力配置**：使用了 **6 张 GPU**（具体型号未指明，推测为 A100 或同级别）。
*   **训练细节**：单卡 Batch Size 为 2，梯度累积步数为 8，有效 Batch Size 为 96。采用 AdamW 优化器，学习率为 $5 \times 10^{-5}$。
*   **开销分析**：DTI 模块仅增加 **0.217M 参数**（约占总参数量的 0.01%），推理时间每张图仅增加约 **0.1 秒**。SANI 仅在训练时使用，不增加推理成本。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准数据集上进行了详尽的定量对比，并提供了大量的定性视觉对比图。
*   **消融实验**：针对 DTI 和 SANI 分别做了单模块及组合模块的对比实验（共 4 组变体），验证了两个模块的互补性。
*   **客观性**：实验涵盖了从传统指标到最新深度学习感知指标的全面评估，并诚实地讨论了感知质量与失真（PSNR）之间的权衡，实验设计较为客观公平。

### 6. 主要结论与发现
*   **感知质量领先**：DASP-SR 在 NIQE、PI 和 MUSIQ 等无参考指标上达到了最优或次优水平，视觉效果更清晰、边缘更锐利。
*   **结构保持能力强**：SANI 显著提升了模型对边缘和细微纹理的保留能力，减少了扩散模型常见的结构漂移。
*   **轻量高效**：通过极小的参数代价实现了显著的性能提升，证明了显式退化引导的有效性。

### 7. 优点
*   **可解释性**：相比于纯黑盒的潜在嵌入，6 维退化描述符具有明确的物理含义。
*   **即插即用**：作为轻量级插件，可以轻松集成到其他基于扩散模型的图像修复框架中。
*   **结构保护**：SANI 巧妙地解决了扩散模型在强噪声下容易丢失原始结构的问题。

### 8. 不足与局限
*   **推理成本**：尽管插件本身轻量，但由于依赖多步扩散采样，整体推理速度仍受限于扩散模型本身的瓶颈，难以满足实时性要求。
*   **手工特征局限**：6 维退化描述符是手工设计的，对于极其复杂或未见过的混合退化，其表达能力可能不足。
*   **伪影风险**：在极端退化情况下，模型可能会产生过度锐化的边缘或错误的纹理幻觉（论文在 Failure Case 中有所展示）。

（完）
