---
title: "DORS: Dynamic Attention Routing for Diffusion-based Object Removal in Dense Scenes"
title_zh: DORS：面向密集场景中基于扩散的对象移除的动态注意力路由
authors: "Haitong Tang, Haipeng Liu, Yang Wang"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16656v1"
tags: ["query:q4"]
score: 9.0
evidence: 基于扩散模型的物体移除与视觉一致性
tldr: 针对密集场景中物体移除不彻底和语义干扰问题，本文提出了一种基于扩散模型的动态注意力路由框架 DORS。该框架通过实例过滤注意力（IFA）抑制相似实例的干扰，并利用上下文引导路由（CGR）补充场景信息。此外，研究者还推出了专门的基准测试集 DOR-Bench。实验证明 DORS 在减少伪影和提升视觉一致性方面优于现有方法。
selection_source: fresh_fetch
motivation: 现有物体移除方法在密集场景中容易受到相似实例的语义干扰，导致目标移除不彻底或产生重复伪影。
method: 提出动态注意力路由机制，包含抑制干扰信息的实例过滤注意力（IFA）和维持视觉一致性的上下文引导路由（CGR）。
result: 在提出的 DOR-Bench 基准测试上，DORS 在减少不完整移除和重复伪影方面显著优于当前最先进的方法。
conclusion: DORS 通过动态调整注意力空间的信息流，有效解决了密集场景下的物体移除难题，提升了生成图像的语义准确性。
---

## 摘要
对象移除旨在消除由掩码指定的特定目标对象，同时保持与周围区域的视觉一致性。现有方法通常依赖于来自周围区域的上下文信息。然而，在周围区域包含与移除目标视觉相似实例的密集场景中，这种依赖往往会导致语义干扰，从而导致移除不彻底。这一问题源于注意力空间中的错误信息传播，其中掩码查询（masked queries）由于自注意力中的全局相似性匹配，往往会与这些实例对齐。为了应对这一挑战，我们提出了一种面向密集场景的基于扩散的对象移除框架，称为 DORS。该框架建立在动态注意力路由机制之上，包含两个互补组件：实例过滤注意力（Instance-Filtered Attention, IFA），通过动态构建的掩码引导注意力约束来抑制来自相似实例的误导性语义信息；以及上下文引导路由（Context-Guided Routing, CGR），通过动态路由互补的场景信息以维持视觉一致性。我们还进一步引入了 DOR-Bench，这是一个专门为密集场景中的对象移除量身定制的基准测试。大量实验表明，DORS 优于现有最先进的方法，特别是在减少移除不彻底和重复伪影方面。代码将在 https://github.com/httang1224/DORS 上发布。

## Abstract
Object removal aims to eliminate target objects specified by a mask while preserving visual consistency with the surrounding regions. Existing methods typically rely on contextual information from surrounding regions. However, in dense scenes where the surrounding regions contain instances visually similar to the removal target, such reliance often leads to semantic interference, resulting in incomplete removal. This problem arises from erroneous information propagation in the attention space, where masked queries tend to align with such instances due to global similarity matching in self-attention. To address this challenge, we propose a Diffusion-based Object Removal framework for dense Scenes, dubbed DORS, built upon a Dynamic Attention Routing mechanism comprising two complementary components: Instance-Filtered Attention (IFA), which suppresses misleading semantic information from similar instances through dynamically constructed mask-guided attention constraints, and Context-Guided Routing (CGR), which dynamically routes complementary scene information to maintain visual consistency. We further introduce DOR-Bench, a benchmark tailored for object removal in dense scenes. Extensive experiments demonstrate that DORS outperforms state-of-the-art methods, particularly in reducing incomplete removal and duplicate artifacts. The code will be available at https://github.com/httang1224/DORS.

---

## 论文详细总结（自动生成）

这是一份关于论文《DORS: Dynamic Attention Routing for Diffusion-based Object Removal in Dense Scenes》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：对象移除（Object Removal）是图像编辑中的核心任务，旨在抹除特定目标并修复背景。
*   **核心问题**：在**密集场景**（如一排相似的椅子、一群人）中，现有基于扩散模型的修复方法经常失效。
*   **失效原因**：自注意力机制（Self-Attention）具有全局相似性匹配的特性。当掩码区域（Masked Queries）寻找填充信息时，容易被周围视觉相似的实例干扰，导致“移除不彻底”或产生“重复伪影”（即把旁边的物体又画到了被移除的位置）。
*   **整体含义**：本文旨在通过改进扩散模型内部的信息流向，解决密集场景下的语义干扰问题。

### 2. 论文提出的方法论
提出了一种名为 **DORS** 的框架，其核心是**动态注意力路由机制（Dynamic Attention Routing）**，包含两个关键组件：

*   **实例过滤注意力 (Instance-Filtered Attention, IFA)**：
    *   **核心思想**：主动抑制来自干扰实例的错误信息。
    *   **技术细节**：通过动态构建的掩码引导注意力约束，识别并屏蔽掉周围与目标物体相似的实例区域。在计算注意力权重时，强制掩码区域的 Query 不去关注这些“干扰源”，从而切断错误语义的传播。
*   **上下文引导路由 (Context-Guided Routing, CGR)**：
    *   **核心思想**：在抑制干扰的同时，确保有用的背景信息能够正确流入。
    *   **技术细节**：动态地将互补的场景信息（如纯背景、非干扰的上下文）路由到掩码区域，以维持生成的视觉一致性和纹理连贯性。
*   **算法流程**：在扩散模型的去噪过程中，每一层注意力计算都会根据 IFA 和 CGR 动态调整注意力矩阵，确保模型只从“正确”的区域借用像素特征。

### 3. 实验设计
*   **基准测试集 (Benchmark)**：引入了专门针对密集场景设计的 **DOR-Bench**。该基准包含大量具有挑战性的密集重复物体场景，用于评估模型在复杂干扰下的移除能力。
*   **对比方法**：对比了当前最先进的（SOTA）方法，包括：
    *   传统的图像修复模型（如 LaMa, ZITS）。
    *   基于扩散模型的修复方法（如 Stable Diffusion Inpainting, ControlNet-Inpainting 等）。
*   **评估指标**：除了常用的 PSNR、SSIM、LPIPS 外，重点评估了移除的彻底性和伪影产生的频率。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中**未明确说明**具体的 GPU 型号、数量及训练总时长。通常此类基于扩散模型的微调任务需要 NVIDIA A100 或 H100 等级别的算力支持。
*   **代码状态**：作者承诺代码将在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 DOR-Bench 上进行了大规模定量对比。
    *   进行了定性视觉效果对比，展示了在多种密集场景（如货架、观众席、车库）下的表现。
    *   **消融实验**：分别验证了 IFA 和 CGR 两个组件对最终效果的贡献。
*   **充分性与客观性**：实验设计较为充分，特别是针对“密集场景”这一特定痛点进行了深入挖掘。通过引入专门的 Benchmark，使得对比更加公平且具有针对性。

### 6. 论文的主要结论与发现
*   **主要结论**：DORS 显著优于现有方法，能够有效解决密集场景中物体移除不干净的问题。
*   **核心发现**：注意力机制中的“全局匹配”是导致密集场景修复失败的元凶，通过“动态路由”干预注意力空间的信息流，是提升生成图像语义准确性的有效途径。

### 7. 优点
*   **针对性强**：精准捕捉到了现有模型在密集场景下的“语义干扰”痛点。
*   **机制创新**：不同于简单的图像后处理，DORS 深入扩散模型底层，通过修改注意力路由来解决问题，更具根本性。
*   **贡献基准**：推出的 DOR-Bench 为该细分领域提供了重要的评价工具。

### 8. 不足与局限
*   **依赖前置任务**：IFA 机制可能需要准确的实例分割或检测信息来识别“干扰实例”，如果前置的实例识别不准，可能会影响移除效果。
*   **计算开销**：动态构建掩码引导的注意力约束可能会增加推理时的计算复杂度。
*   **应用限制**：主要针对“密集重复场景”，对于背景极其复杂且无规律的场景，其提升幅度可能不如密集场景显著。

（完）
