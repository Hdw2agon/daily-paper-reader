---
title: Image-to-Point Cloud Registration Made Easy with Rectified Flow-based LiDAR Upsampling
title_zh: 基于修正流 LiDAR 上采样的图像到点云配准简化方法
authors: "Reon Tabata, Kenji Koide, Shuji Oishi, Masashi Yokozuka, Taku Okawara, Aoki Takanose, Jun Miura"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14639v1"
tags: ["query:q2"]
score: 8.0
evidence: 通过自监督图像补全任务进行预训练
tldr: 本研究针对图像与点云配准（I2P）中模态差异大、泛化性差的问题，提出了一种基于条件修正流（Conditional Rectified Flow）的方法。该方法将LiDAR视为成像传感器，通过将稀疏点云上采样为稠密强度图，利用预训练特征匹配器与相机图像对齐，并结合PnP-RANSAC估计6自由度位姿。该模型支持自监督预训练，无需配准对或真值位姿，在R3LIVE数据集上实现了高精度与高效配准。
selection_source: fresh_fetch
motivation: 旨在解决图像与点云之间巨大的模态差异导致的配准精度低和泛化能力弱的问题。
method: 利用条件修正流将稀疏LiDAR扫描生成稠密强度图，随后通过预训练匹配器与相机图像进行特征匹配并求解位姿。
result: 在R3LIVE数据集上达到了4.89°和1.63m的平均误差，优于现有方法，且单次配准仅需约0.68秒。
conclusion: 该方法通过将点云转换为类图像表示，有效简化了跨模态配准难题，展现了强大的泛化潜力和实用价值。
---

## 摘要
图像到点云配准（I2P）对于感知和自动驾驶系统中相机与 LiDAR 的集成至关重要，但图像与点云之间的模态差异使得同时实现高精度和强泛化能力变得困难。在本文中，我们提出了一种简单而有效的 I2P 方法，将 LiDAR 视为成像传感器：从单次稀疏 LiDAR 扫描中，我们利用条件修正流（Conditional Rectified Flow）生成稠密的 LiDAR 强度图像，使用预训练的特征匹配器将其与相机图像进行匹配，并通过 PnP-RANSAC 估计 6 自由度（6-DoF）相对位姿。该模型通过自监督图像补全任务进行预训练，并在少量 LiDAR 数据上进行微调（既不需要图像-点云对，也不需要地面真值传感器位姿），使其能够扩展到多种 LiDAR 和相机配置。在 R3LIVE 数据集上的实验表明，所提方法实现了 4.89° / 1.63 m 的平均误差，优于现有方法，同时单次配准耗时约为 0.68 秒。

## Abstract
Image-to-Point Cloud Registration (I2P) is essential for integrating camera and LiDAR in perception and autonomous systems, yet the modality gap between images and point clouds makes it difficult to achieve both high accuracy and strong generalization. In this paper, we propose a simple yet effective I2P method that treats LiDAR as an imaging sensor: from a single sparse LiDAR scan, we generate a dense LiDAR intensity image using Conditional Rectified Flow, match it with a camera image using a pre-trained feature matcher, and estimate the 6-DoF relative pose via PnP-RANSAC. The proposed model is pre-trained through a self-supervised image completion task and fine-tuned on a small amount of LiDAR data (neither image-point cloud pairs nor ground-truth sensor poses are required), enabling it to scale to diverse LiDAR and camera configurations. Experiments on the R3LIVE dataset show that the proposed method achieves a mean error of 4.89° / 1.63 m, outperforming existing methods, while completing a single registration in approximately 0.68 s.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **“基于修正流（Rectified Flow）的 LiDAR 上采样”** 的新方法，旨在简化图像到点云（I2P）的配准难题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：图像到点云配准（I2P）是自动驾驶和机器人定位的关键技术。然而，图像（稠密、纹理丰富）与点云（稀疏、几何特征为主）之间存在巨大的**模态差异**。
*   **现有痛点**：
    *   大多数学习方法需要昂贵的“图像-点云”配准对及真值位姿（Ground Truth）进行训练。
    *   现有方法在面对不同型号的 LiDAR 或新环境时，泛化能力较弱。
    *   基于扩散模型的方法虽然泛化性好，但推理速度极慢（如 FreeReg 需 20 秒以上），无法满足实际应用需求。

### 2. 方法论
*   **核心思想**：将 LiDAR 视为一种“成像传感器”。通过生成式模型将稀疏的 LiDAR 扫描图“补全”为稠密的强度图像，从而将复杂的跨模态 I2P 问题转化为相对成熟的图像对图像（I2I）匹配问题。
*   **关键技术流程**：
    1.  **投影**：利用虚拟相机模型将单帧 LiDAR 点云投影为稀疏的强度图（Intensity Image）。
    2.  **条件修正流上采样（核心）**：采用 **Conditional Rectified Flow** 模型。相比传统的扩散模型（DDPM），修正流学习的是确定性的直线轨迹，能够以极少的步数（甚至 1 步）生成高质量的稠密强度图。
    3.  **特征匹配**：使用预训练的图像匹配器（ALIKED 提取特征 + LightGlue 进行匹配）在生成的 LiDAR 稠密图与相机图像之间建立 2D-2D 关联。
    4.  **位姿估计**：将匹配点通过 LiDAR 深度信息还原为 3D 点，利用 **PnP-RANSAC** 算法估计 6 自由度（6-DoF）相对位姿。
*   **训练策略**：采用自监督预训练（在 RGB 数据集上进行图像掩码补全任务）+ 少量 LiDAR 强度数据微调，**无需**任何图像-点云配准对。

### 3. 实验设计
*   **数据集**：主要在 **R3LIVE** 数据集（包含 hkust campus, hku campus, hku park 等场景）上进行评估。
*   **Benchmark 与对比方法**：
    *   **CoFiI2P**：基于粗到精特征匹配的方法。
    *   **FreeReg**：基于大规模预训练扩散模型和单目深度估计的方法。
    *   **2D3D-MATR**：基于 Transformer 的检测器无关配准方法。
*   **评价指标**：旋转误差（RRE）、平移误差（RTE）、配准召回率（RR）以及特征匹配质量（FMR, IR, IN）。

### 4. 资源与算力
*   **硬件环境**：使用单块 **NVIDIA RTX A5000 (24GB)** GPU。
*   **算力表现**：
    *   **推理速度**：单帧配准总耗时约 **0.68 秒**（其中生成模型推理极快），远优于 FreeReg 的 22.62 秒。
    *   **显存占用**：约 **3252 MB**，对硬件要求适中。
    *   **训练细节**：文中未明确给出总训练时长，但强调微调仅需 80 个场景的少量数据，暗示训练成本较低。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同特征的子数据集上进行了测试，涵盖了建筑、公园、森林等环境。
*   **消融实验**：
    *   测试了生成步数（1, 5, 10 步）对精度的影响。
    *   验证了“预训练”对模型泛化能力的决定性作用。
    *   研究了深度搜索半径对 PnP 求解的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了精度，还对比了计算效率和特征匹配的内点率（Inlier Ratio），并展示了在不同 LiDAR 硬件（Livox AVIA, Ouster OS1-64）上的适配性，结果具有说服力。

### 6. 主要结论与发现
*   **精度领先**：在未接触过 R3LIVE 训练数据的情况下，实现了 4.89° / 1.63 m 的平均误差，优于所有基准方法。
*   **效率突破**：修正流模型在 1 步生成时即可达到实用精度，使生成式 I2P 方法从“分钟级”跨入“亚秒级”。
*   **泛化性强**：通过图像补全任务预训练，模型学会了通用的几何补全能力，能够轻松迁移到不同扫描模式的 LiDAR 上。

### 7. 优点
*   **流程简化**：避开了复杂的跨模态特征描述符设计，直接复用成熟的图像匹配算法。
*   **弱监督/无监督友好**：不需要昂贵的配准真值，极大降低了数据采集成本。
*   **速度优势**：修正流的应用解决了生成式模型在机器人实时定位中的瓶颈问题。

### 8. 不足与局限
*   **非结构化环境挑战**：在植被茂密（如 hku park）的场景中，LiDAR 强度图生成不够稳定，导致匹配失败率上升。
*   **深度缺失问题**：由于只上采样了强度而未补全深度，PnP 求解仍受限于原始点云的稀疏性，需通过邻域搜索近似深度，这可能引入几何误差。
*   **外观依赖**：如果 LiDAR 强度信息与相机图像视觉特征差异极大（如极端光照或材质反射异常），匹配效果可能会受限。

（完）
