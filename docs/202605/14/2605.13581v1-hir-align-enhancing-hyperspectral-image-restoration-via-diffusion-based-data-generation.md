---
title: "HIR-ALIGN: Enhancing Hyperspectral Image Restoration via Diffusion-Based Data Generation"
title_zh: HIR-ALIGN：通过基于扩散的数据生成增强高光谱图像恢复
authors: "Li Pang, Heng Zhao, Yijia Zhang, Deyu Meng, Xiangyong Cao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13581v1"
tags: ["query:q3"]
score: 8.0
evidence: 无干净参考的自适应目标增强修复
tldr: 针对高光谱图像（HSI）修复中源域与目标域分布不一致且缺乏目标域干净参考的问题，本文提出 HIR-ALIGN 框架。该框架通过代理生成、基于扩散模型的分布自适应合成以及对齐监督微调三个阶段，利用合成的目标域对齐数据增强训练。理论分析与实验证明，该方法在去噪和超分辨率任务上显著提升了模型在目标域的修复性能，具有良好的即插即用性。
selection_source: fresh_fetch
motivation: 现有的 HSI 修复模型在缺乏干净参考的目标域上泛化能力差，亟需一种无需额外数据即可实现目标域自适应的增强方法。
method: 提出包含代理生成、基于扩散模型的 RGB 合成与光谱迁移、以及对齐监督微调的三阶段框架 HIR-ALIGN。
result: 在模拟和真实数据集的去噪及超分辨率任务中，该方法均显著优于仅源域监督的基准模型及代表性的无监督方法。
conclusion: HIR-ALIGN 通过生成目标域对齐的合成数据，有效降低了目标域修复风险，是提升 HSI 修复模型泛化能力的有效方案。
---

## 摘要
高光谱图像（HSI）恢复对于可靠的分析至关重要，因为真实的高光谱图像常受到噪声、模糊和分辨率损失等退化的影响。然而，在源数据上训练的现有模型在缺乏干净参考图像的目标域上往往表现不佳，而这在实践中非常普遍。为了解决这一问题，我们提出了 HIR-ALIGN，这是一个即插即用的目标自适应增强框架，它通过使用合成数据增强有限的训练图像来提升高光谱图像恢复效果，这些合成数据在不使用额外数据的情况下与目标分布紧密匹配。它包含三个阶段：(i) 代理生成，利用现成的恢复模型恢复退化的目标观测值，生成保留语义的代理 HSI，以逼近目标域的干净图像；(ii) 分布自适应合成，其中模糊鲁棒的 unCLIP 扩散模型通过提示词调节和嵌入空间噪声初始化，从代理 RGB 生成目标对齐的 RGB。随后，基于扭曲的光谱传输模块通过将每个生成的 RGB 与代理 RGB 对齐，估计软分块传输权重，并将这些权重和可学习的局部插值核应用于代理 HSI，从而合成 HSI；(iii) 对齐监督微调，将在源分布上预训练的恢复网络使用代理 HSI 和合成的目标对齐 HSI 进行微调，然后部署在退化的目标图像上。我们进一步提供了理论分析，表明基于增强的微调可以通过共同提高目标分布覆盖范围和控制光谱偏差，实现更低的目标域恢复风险。在去噪和超分辨率任务的模拟及真实数据集上进行的广泛实验表明，HIR-ALIGN 持续改进了仅源监督的基准模型，优于仅源模型和代表性的无监督方法。

## Abstract
Hyperspectral image (HSI) restoration is crucial for reliable analysis, as real HSIs suffer from degradations like noise, blur, and resolution loss. However, existing models trained on source data often fail on target domains lacking clean references, a common occurrence in practice. To address this issue, we present HIR-ALIGN, a plug-and-play target-adaptive augmentation framework that enhances hyperspectral image restoration by augmenting limited training images with synthetic data that closely matches the target distribution using no extra data. It consists of three stages: (i) proxy generation, where off-the-shelf restoration models restore degraded target observations to produce semantics-preserving proxy HSIs that approximate target-domain clean images; (ii) distribution-adaptive synthesis, where a blur-robust unCLIP diffusion model generates target-aligned RGBs from proxy RGBs, with prompt conditioning and embedding-space noise initialization. Then, a warp-based spectral transfer module synthesizes HSIs by aligning each generated RGB with the proxy RGB, estimating soft patch-wise transport weights, and applying these weights and learnable local interpolation kernels to the proxy HSI; and (iii) aligned supervised finetuning, where restoration networks pretrained on the source distribution are finetuned using both the proxy HSIs and synthesized target-aligned HSIs, and are then deployed on degraded target images. We further provide theoretical analysis showing that augmentation-based finetuning can achieve lower target-domain restoration risk by jointly improving target distribution coverage and controlling spectral bias. Extensive experiments on simulated and real datasets across denoising and super-resolution tasks demonstrate that HIR-ALIGN consistently improves source-only supervised baselines, outperforming both source-only counterparts and representative unsupervised methods.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《HIR-ALIGN: Enhancing Hyperspectral Image Restoration via Diffusion-Based Data Generation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
高光谱图像（HSI）恢复（如去噪、超分辨率）在实际应用中面临严重的**领域偏移（Domain Shift）**问题。现有的深度学习模型通常在源域（如 ICVL 数据集）上训练，但在部署到目标域（如 CAVE 或真实场景）时，由于光照、场景内容和光谱统计特性的差异，性能会大幅下降。
*   **核心挑战**：在实际场景中，用户可以轻易获得退化的目标观测图像，但几乎无法获得对应的“干净”参考图像。
*   **研究动机**：如何仅利用退化的目标观测值，生成有效的目标对齐监督信号，从而使预训练模型能够自适应地提升在目标域上的恢复效果。

### 2. 方法论：核心思想与技术流程
论文提出了 **HIR-ALIGN** 框架，这是一个三阶段的即插即用目标自适应增强方案：
*   **阶段 I：代理生成（Proxy Generation）**
    利用在源域预训练的恢复模型处理退化的目标图像，生成“代理 HSI”。虽然代理图像不完美，但它保留了目标域的基础语义结构和光谱特征，作为后续合成的锚点。
*   **阶段 II：分布自适应合成（Distribution-Adaptive Synthesis）**
    *   **RGB 合成**：改进了 unCLIP 扩散模型。通过引入退化鲁棒的 CLIP 编码器、文本提示词引导和 SDEdit 风格的噪声初始化，从代理 RGB 生成具有目标域风格且多样化的 RGB 引导图。
    *   **光谱迁移（Warp-based Spectral Transfer）**：为了避免光谱幻觉，设计了一个基于扭曲的模块。它通过分块描述符匹配和检索，估计生成的 RGB 与代理 RGB 之间的软聚合权重和局部插值核，然后将这些权重直接作用于代理 HSI 的光谱。这样合成的 HSI 既拥有生成图的布局，又继承了代理图的真实光谱。
*   **阶段 III：对齐监督微调（Aligned Supervised Finetuning）**
    将代理 HSI 对和合成的 HSI 对混合，构建成监督训练对，对预训练的恢复网络进行微调。
*   **理论支撑**：论文通过 Wasserstein 距离推导了目标风险界限，证明了该方法通过扩大目标分布覆盖范围并严格控制光谱偏差，能够降低泛化风险。

### 3. 实验设计
*   **数据集**：
    *   模拟实验：**CAVE** 和 **KAIST**（从 ICVL 迁移）。
    *   真实实验：**HSIDwrD**（真实场景去噪）。
*   **任务类型**：高斯去噪、复杂混合噪声去除、超分辨率（4x）、去模糊、波段补全、图像修复（Inpainting）。
*   **Benchmark 与对比方法**：
    *   **无监督/优化方法**：LRTV, LRTDTV, RCTV, HIR-Diff 等。
    *   **监督基准（源域预训练）**：SERT, SSUMamba, MP-HSIR, PromptIR, MCNet, SFCSR 等。
    *   **生成式基准**：与 HSI 生成基础模型 HSIGene 进行对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及总的训练/合成时长。
*   **技术细节**：提到了使用 **FAISS** 库进行高效的近邻搜索以加速光谱迁移阶段的像素匹配。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 6 种不同的 HSI 恢复任务。
    *   在 3 个主流数据集上进行了验证。
    *   针对 10 种以上的先进恢复模型（Backbones）进行了即插即用测试。
*   **消融实验**：非常充分。包括：
    1.  训练数据组合（仅代理 vs 仅合成 vs 混合）。
    2.  合成数据比例的影响。
    3.  unCLIP 改进模块的有效性。
    4.  对未观测目标图像的泛化能力测试。
    5.  与“干净目标数据微调”的性能上限对比。
*   **客观性**：实验设计遵循了“不使用目标域干净参考”的严格约束，对比了多种 SOTA 方法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：HIR-ALIGN 显著提升了所有预训练模型在目标域的表现，在多个任务上打破了源域预训练模型的性能瓶颈。
*   **优于无监督方法**：相比于纯无监督方法，HIR-ALIGN 结合了源域的先验知识和目标域的分布自适应，表现更优。
*   **泛化能力**：微调后的模型不仅在已观测到的目标图像上表现好，在同分布的未观测图像上也展现了良好的泛化性。
*   **光谱一致性**：通过“光谱扭曲”而非“直接生成”的方式，有效地抑制了光谱漂移问题。

### 7. 优点（亮点）
*   **即插即用**：不需要修改现有恢复模型的架构，只需改变微调数据。
*   **理论与实践结合**：提供了严谨的数学证明来解释为什么“代理+合成”的混合策略优于单一策略。
*   **解决光谱幻觉**：巧妙地利用 RGB 扩散模型提供空间多样性，利用代理 HSI 提供光谱真实性，解决了 HSI 生成中光谱不准的痛点。

### 8. 不足与局限
*   **流程复杂性**：整个框架包含扩散模型生成、像素级匹配优化和二次微调，计算开销和流程复杂度高于简单的端到端模型。
*   **依赖初始代理**：如果源域模型在目标域上的初始表现极差（代理图像质量太低），可能会限制后续合成数据的质量。
*   **算力信息缺失**：未提供具体的推理和训练时间成本，对于实际部署的参考价值略受影响。
*   **极端偏移风险**：虽然有光谱迁移机制，但在极端光照或材质差异下，光谱偏差的风险依然存在。

（完）
