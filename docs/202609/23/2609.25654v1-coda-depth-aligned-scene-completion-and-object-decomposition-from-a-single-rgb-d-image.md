---
title: "CODA: Depth-Aligned Scene Completion and Object Decomposition from a Single RGB-D Image"
title_zh: CODA：基于单张 RGB-D 图像的深度对齐场景补全与物体分解
authors: "Dongwon Son, Junhyek Han, Yoontae Cho, Minseok Lee, Hong-seok Choi, Jiwook Choi, Hyungjin Kim, Beomjoon Kim"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.25654v1"
tags: ["query:q1"]
score: 8.0
evidence: 基于生成模型的局部观测场景补全
tldr: 针对机器人处理杂乱场景时物体检测与重建不准确的问题，本文提出了CODA模型。该模型通过单张RGB-D图像先重建完整场景几何，再进行物体分解。通过引入两种3D接地机制，CODA有效减少了生成几何与观测点云的偏差，在保持场景一致性的同时实现了更精准的物体重建与物理稳定性。
selection_source: fresh_fetch
motivation: 现有方法在处理杂乱场景时，常因漏检、误合并或重建重叠导致场景几何重建不准确且缺乏物理一致性。
method: 提出CODA模型，先进行整体场景几何补全，再执行物体分解，并利用两种3D接地机制确保生成几何与观测数据的一致性。
result: 在HomebrewedDB及自定义数据集上的实验表明，CODA在重建精度及模拟重力下的物体稳定性方面均优于现有的物体优先和场景优先基线方法。
conclusion: CODA通过先补全后分解的策略及3D接地约束，有效提升了单目RGB-D场景重建的准确性与物理合理性。
---

## 摘要
在杂乱的日常环境中安全运行的机器人，通常需要从局部观测中推断场景几何结构。现有的二维物体检测与独立重建方法在此类场景中往往表现不佳：漏检的物体无法被重建，合并的检测结果可能将两个物体混为一谈，而分别重建的网格模型之间可能出现重叠，或无法与支撑平面接触。我们提出了 CODA（先补全，后分解），这是一种生成式模型，它首先从单张未分割的 RGB-D 图像中重建完整的场景几何结构，随后将表面分离为周围环境和可移动物体。然而，生成的场景几何结构可能会偏离观测到的局部点云。为了减少这种偏差，CODA 使用了两种显式的三维接地机制，在补全不可见区域的同时，保持重建几何结构与观测表面的一致性。在 HomebrewedDB 和我们自定义的杂乱场景数据集上的实验表明，与“先物体后场景”和“先场景后物体”的基准方法相比，该方法能够实现更精确的重建，并在模拟重力作用下有更高比例的物体能够保持原位。

## Abstract
Robots operating safely in cluttered everyday environments often need to infer scene geometry from partial observations. Methods that detect objects in 2D and reconstruct them independently struggle in such scenes: a missed object is never reconstructed, a merged detection can fuse two objects, and separately reconstructed meshes may overlap or fail to touch their supporting surfaces. We introduce CODA (Complete Once, Decompose Afterward), a generative model that instead reconstructs the complete scene geometry from a single unsegmented RGB-D image, then separates the surface into the surrounding environment and movable objects. Still, generated scene geometry can drift from the observed partial point cloud. To reduce this drift, CODA uses two explicit 3D grounding mechanisms to keep reconstructed geometry consistent with observed surfaces while completing unseen regions. Experiments on HomebrewedDB and our custom cluttered-scene dataset show more accurate reconstructions and a higher fraction of objects remaining in place under simulated gravity than both object-first and scene-first baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《CODA: Depth-Aligned Scene Completion and Object Decomposition from a Single RGB-D Image》** 的结构化总结：

### 1. 核心问题与研究动机
在杂乱的日常环境中，机器人需要从单张 RGB-D 图像中推断完整的 3D 场景几何，以便进行抓取等操作。现有的“物体优先”（先检测再重建）方法极度依赖 2D 分割质量，容易出现漏检、物体合并或重建网格不接触支撑面等问题；而现有的“场景优先”方法往往缺乏精细的物体级几何细节。CODA 的核心动机是**摆脱对 2D 实例分割的依赖**，通过“先补全场景，后分解物体”的策略，实现既具备全局一致性又具备精细物体几何的重建。

### 2. 方法论：CODA (Complete Once, Decompose Afterward)
*   **核心思想**：利用预训练的 3D 生成模型（TRELLIS.2）作为骨干，通过微调将其转化为场景补全与物体身份预测模型。
*   **关键流程**：
    1.  **阶段 1（粗略补全）**：生成粗略的场景占用网格（Occupancy flow）。
    2.  **阶段 2（精细重建）**：基于阶段 1 的支持，生成精细的场景形状潜变量（Shape flow）。
    3.  **阶段 3（物体分解）**：利用纹理流（Texture flow）预测物体身份代码，通过最近邻代码分配将完整网格分割为环境和独立物体。
*   **关键技术（3D 接地机制）**：
    *   **观测表面初始化**：将观测到的点云信息直接注入生成过程的初始状态，确保生成几何与测量数据对齐。
    *   **深度引导特征匹配**：利用相机内参将 RGB 图像特征反投影到 3D 空间，并将其作为交叉注意力机制的上下文，强制生成模型关注观测到的表面细节。

### 3. 实验设计
*   **数据集**：使用了公开的 **HomebrewedDB** 数据集，以及作者自建的包含多种杂乱场景（货架、容器、桌面）的自定义数据集。
*   **对比方法**：
    *   **物体优先**：RecGen, SAM 3D, ZeroGrasp。
    *   **场景优先**：PaSCo。
    *   **通用智能体**：ChatGPT Astra。
*   **评估指标**：表面 F1 分数（F@5, F@10）、模拟重力下的姿态保持率（Pose Retention）、推理耗时。

### 4. 资源与算力
*   **硬件**：实验在 NVIDIA RTX 4090 GPU 上进行。
*   **训练细节**：每个阶段在单张 GPU 上训练，使用 AdamW 优化器，LoRA 秩（rank）设为 64。
*   **推理效率**：CODA 在自定义场景下的平均推理时间为 5.59 秒，显著快于 RecGen（100.15 秒）和 SAM3D（85.08 秒），但略慢于 PaSCo 和 ZeroGrasp。

### 5. 实验数量与充分性
*   **实验覆盖**：包含主对比实验、消融实验（验证接地机制的有效性）、物体级多样性实验（随机种子测试）以及真实世界场景测试。
*   **客观性**：实验设计了多种遮挡程度（Easy/Mid/Hard）的评估，并对比了使用 Ground Truth 掩码与自动掩码的情况，评估过程相对客观且公平。

### 6. 主要结论与发现
*   **性能优势**：CODA 在重建精度和物理稳定性（重力测试）上均优于现有的基线方法。
*   **顺序效应**：实验证明“先补全后分解”的策略能有效减少物体间的重叠和支撑面缺失问题。
*   **接地机制**：显式的 3D 接地机制对于减少生成几何与观测点云之间的漂移至关重要。

### 7. 优点
*   **鲁棒性**：无需预先进行 2D 实例分割，避免了因分割错误导致的重建失败。
*   **物理合理性**：通过整体场景重建，物体与支撑面之间的接触关系更自然，在模拟重力测试中表现更稳健。
*   **高效性**：相比于逐个物体重建，联合重建显著降低了计算开销。

### 8. 不足与局限
*   **推理速度**：虽然比传统方法快，但 5.59 秒的推理时间对于实时机器人控制仍有优化空间。
*   **依赖预训练**：模型性能高度依赖于 TRELLIS.2 等预训练骨干模型的泛化能力。
*   **复杂场景限制**：在极端遮挡或极度复杂的非结构化场景中，物体身份的准确分解仍可能存在挑战。

（完）
