---
title: "V-JEPA 2.1: Unlocking Dense Features in Video Self-Supervised Learning"
title_zh: V-JEPA 2.1：解锁视频自监督学习中的密集特征
authors: "Lorenzo Mur-Labadia, Matthew Muckley, Amir Bar, Mido Assran, Koustuv Sinha, Mike Rabbat, Yann LeCun, Nicolas Ballas, Adrien Bardes"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14482v1"
tags: ["query:q7"]
score: 8.0
evidence: 基于掩码预测损失的自监督学习
tldr: V-JEPA 2.1 是一系列自监督学习模型，旨在通过结合密集预测损失、深度自监督、多模态分词器以及模型与数据的有效扩展，学习图像和视频的高质量密集视觉表示。该方法在保留全局场景理解的同时，显著提升了空间定位和时间一致性，在机器人抓取、动作预测和深度估计等多个密集视觉理解任务中达到了 SOTA 水平。
selection_source: fresh_fetch
motivation: 旨在解决现有自监督模型在处理视频和图像时难以兼顾全局理解与高质量密集特征表示的问题。
method: 结合了密集预测损失、跨层级深度自监督、多模态分词器以及大规模模型与数据扩展。
result: 在 Ego4D、EPIC-KITCHENS、机器人抓取及导航等多个基准测试中刷新了 SOTA 记录，并显著提升了深度估计精度。
conclusion: V-JEPA 2.1 显著推进了密集视觉理解和世界建模技术的发展，证明了其在复杂视觉任务中的强大泛化能力。
---

## 摘要
我们提出了 V-JEPA 2.1，这是一系列自监督模型，能够在保持强大的全局场景理解能力的同时，为图像和视频学习密集、高质量的视觉表示。该方法结合了四个关键组件。首先，密集预测损失采用基于掩码的目标，其中可见和被掩码的 token 均对训练信号有贡献，从而鼓励显式的空间和时间定位。其次，深度自监督将自监督目标分层应用于多个中间编码器层，以提高表示质量。第三，多模态分词器实现了图像和视频的统一训练。最后，该模型受益于模型容量和训练数据的有效扩展。这些设计选择共同产生了具有空间结构、语义连贯且时间一致的表示。实验表明，V-JEPA 2.1 在多个具有挑战性的基准测试中达到了最先进的性能，包括在 Ego4D 短期物体交互预测上达到 7.71 mAP，在 EPIC-KITCHENS 高层动作预测上达到 40.8 Recall@5，以及在真实机器人抓取成功率上比 V-JEPA-2 AC 提高了 20 个百分点。该模型在机器人导航（TartanDrive 上的 ATE 为 5.687）、深度估计（NYUv2 线性探测的 RMSE 为 0.307）和全局识别（Something-Something-V2 为 77.7）方面也表现出强劲的性能。这些结果表明，V-JEPA 2.1 显著推进了密集视觉理解和世界建模的技术水平。

## Abstract
We present V-JEPA 2.1, a family of self-supervised models that learn dense, high-quality visual representations for both images and videos while retaining strong global scene understanding. The approach combines four key components. First, a dense predictive loss uses a masking-based objective in which both visible and masked tokens contribute to the training signal, encouraging explicit spatial and temporal grounding. Second, deep self-supervision applies the self-supervised objective hierarchically across multiple intermediate encoder layers to improve representation quality. Third, multi-modal tokenizers enable unified training across images and videos. Finally, the model benefits from effective scaling in both model capacity and training data. Together, these design choices produce representations that are spatially structured, semantically coherent, and temporally consistent.   Empirically, V-JEPA 2.1 achieves state-of-the-art performance on several challenging benchmarks, including 7.71 mAP on Ego4D for short-term object-interaction anticipation and 40.8 Recall@5 on EPIC-KITCHENS for high-level action anticipation, as well as a 20-point improvement in real-robot grasping success rate over V-JEPA-2 AC. The model also demonstrates strong performance in robotic navigation (5.687 ATE on TartanDrive), depth estimation (0.307 RMSE on NYUv2 with a linear probe), and global recognition (77.7 on Something-Something-V2). These results show that V-JEPA 2.1 significantly advances the state of the art in dense visual understanding and world modeling.

---

## 论文详细总结（自动生成）

以下是对论文《V-JEPA 2.1: Unlocking Dense Features in Video Self-Supervised Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在自监督学习（SSL）中同时获得高质量的**密集局部特征**（用于定位、几何、跟踪）和强大的**全局场景理解**（用于动作识别、动力学建模）？
*   **研究背景**：
    *   之前的 V-JEPA 系列在全局视频理解和动力学建模上表现出色，但其学习到的特征图较为模糊，缺乏精细的空间结构。
    *   DINO 等模型虽然在密集任务（如分割）上表现优异，但主要基于图像，难以直接学习视频中的时间动力学。
    *   **动机**：通过改进联合嵌入预测架构（JEPA），解锁视频自监督模型在密集视觉任务中的潜力，使其成为更完善的“世界模型”基础。

### 2. 方法论
V-JEPA 2.1 引入了四个关键改进：
*   **密集预测损失 (Dense Predictive Loss)**：
    *   **核心思想**：不仅对被掩码（Masked）的 token 计算损失，也对可见的上下文（Context）token 计算损失。
    *   **技术细节**：引入上下文损失 $L_{ctx}$，并采用动态权重方案。靠近掩码区域的上下文 token 权重更高，这迫使模型在可见区域也保持空间和时间的定位能力，防止可见 token 沦为无意义的全局聚合器。
*   **深度自监督 (Deep Self-Supervision)**：
    *   在编码器的多个中间层（而不只是最后一层）同时应用预测损失。通过多级预测器（Multi-level Predictor），将训练信号渗透到网络的深层和浅层，显著提升了特征质量。
*   **多模态分词器 (Multi-Modal Tokenizer)**：
    *   为图像（2D 卷积）和视频（3D 卷积）设计专门的补丁嵌入层（Patch Embedding），并加入模态学习 token，使单一编码器能原生支持两种模态的统一训练，无需将图像复制成伪视频。
*   **模型与数据扩展**：
    *   **数据**：构建了 VisionMix-163M 数据集，结合了 LVD-142M 图像和 YT-1B 视频，增加了视觉多样性。
    *   **模型**：将架构扩展至 ViT-G（20 亿参数），并引入了高分辨率的“冷却（Cool-down）”训练阶段。

### 3. 实验设计
*   **数据集与场景**：
    *   **预测任务**：Ego4D（短期物体交互预测）、EPIC-KITCHENS-100（动作预测）。
    *   **密集任务**：NYUv2 & KITTI（深度估计）、ADE20K & Cityscapes & Pascal VOC（语义分割）、YouTube-VOS & DAVIS（视频物体分割）。
    *   **全局任务**：Something-Something-v2 (SSv2)、Kinetics-400 (K400)、ImageNet-1K。
    *   **具身智能**：Franka 机器人抓取实验、Tartan Drive 导航规划。
*   **Benchmark 与对比方法**：
    *   对比了 V-JEPA 2、DINOv2、DINOv3、InternVideo2、SigLIP 2 等最先进的自监督模型。
    *   评估协议主要采用**冻结骨干网络（Frozen Backbone）**的线性探测或注意力探测，以验证特征本身的质量。

### 4. 资源与算力
*   **训练时长**：主训练阶段为 135,000 次迭代，冷却阶段为 12,000 次迭代。
*   **算力细节**：文中未明确给出训练 ViT-G 所需的 GPU 总数和总机时，但提到在机器人规划实验中使用了单张 A100 GPU 进行推理。考虑到 2B 参数量和 1.6 亿样本量，其训练成本极高（通常需要数百至上千张 H100/A100 级别 GPU）。
*   **分布式策略**：采用分布式训练，将图像和视频分配给不同的 worker，通过梯度聚合实现联合训练。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从底层几何（深度）到高层语义（动作识别），再到下游应用（机器人）的全方位评估。
*   **消融实验**：非常充分。详细对比了上下文损失权重、深度自监督层数、数据配比、分词器设计以及模型缩放的影响。
*   **客观性**：通过 PCA 可视化直观展示了特征的语义连贯性，并使用了标准化的线性探测协议，确保了与其他 SOTA 模型对比的公平性。

### 6. 主要结论与发现
*   **密集特征解锁**：通过对上下文 token 进行监督，V-JEPA 2.1 克服了前代模型特征图“嘈杂”的缺点，学习到了具有高度空间结构和时间一致性的表示。
*   **性能突破**：
    *   在 NYUv2 深度估计上达到 0.307 RMSE，超越了拥有 70 亿参数的 DINOv3。
    *   在 SSv2 动作识别上达到 77.7% 的 SOTA 精度。
    *   在机器人抓取任务中，成功率比前代提升了 20%。
*   **蒸馏效应**：大模型（ViT-G）可以有效地将其知识蒸馏给较小的模型（ViT-L/B），使其在保持轻量化的同时接近大模型的性能。

### 7. 优点
*   **统一性**：成功将图像和视频表示学习整合进一个简洁的预测框架中。
*   **特征质量**：PCA 可视化证明其特征不仅具有空间结构，还能自动识别语义相似的物体部件（如不同车辆的轮子）。
*   **实用性**：在机器人导航和操纵任务中的零样本（Zero-shot）表现证明了其作为物理世界模型的潜力。

### 8. 不足与局限
*   **复杂场景分割**：在 ADE20K 等极其复杂、类别密集的分割任务中，虽然比前代有巨大进步，但仍略逊于专门优化的图像编码器（如 DINOv3）。作者认为这可能与预训练数据中复杂场景的占比有关。
*   **计算成本**：2B 参数量的模型训练和推理成本依然高昂，尽管有蒸馏版本，但最强性能仍依赖于超大规模算力。
*   **预测偏差**：在机器人实验中发现，某些失败案例源于对夹持器动作规划的细微偏差，而非空间理解不足，说明动作条件的预测模型仍有优化空间。

（完）
