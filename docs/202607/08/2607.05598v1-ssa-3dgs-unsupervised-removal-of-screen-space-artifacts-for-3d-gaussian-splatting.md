---
title: "SSA-3DGS: Unsupervised Removal of Screen-Space Artifacts for 3D Gaussian Splatting"
title_zh: SSA-3DGS：针对 3D 高斯泼溅（3D Gaussian Splatting）的屏幕空间伪影无监督去除
authors: "Kristof Overdulve, Lode Jorissen, Nick Michiels"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05598v1"
tags: ["query:q2"]
score: 8.0
evidence: 无监督去除屏幕空间伪影和遮挡
tldr: 针对3D高斯泼溅（3DGS）在处理包含屏幕空间伪影（如镜头污渍、水印、遮挡物）的图像时会产生“漂浮物”的问题，本文提出SSA-3DGS。这是一个无监督框架，通过联合优化3D场景和可学习的2D覆盖层，利用多视图几何一致性将静态伪影与3D几何分离。该方法无需人工标注即可恢复干净的3D场景并提取伪影，在合成和真实数据集上显著提升了重建质量。
selection_source: fresh_fetch
motivation: 现实拍摄中的屏幕空间伪影（如镜头污渍或水印）违反了3DGS的多视图一致性假设，导致重建结果中出现严重的几何畸变和漂浮物。
method: 提出一种无监督框架，通过联合优化3D场景模型和每个视图的可学习2D覆盖层，利用几何共识将视图相关的伪影从3D场景中解耦。
result: 在多种合成和真实数据集上，该方法比标准3DGS在峰值信噪比（PSNR）上提升了高达9 dB，并能准确分离出伪影。
conclusion: SSA-3DGS有效地解决了3DGS对输入图像质量的强依赖问题，实现了在含有复杂屏幕空间干扰下的高质量无监督3D重建。
---

## 摘要
新视角合成（NVS）方法，如 3D 高斯泼溅（3DGS），严重依赖于输入图像是干净的、多视图一致的且带有位姿的假设。现实世界的拍摄可能会因为屏幕空间伪影而违反这一假设——这些伪影是固定在 2D 图像平面而非 3D 世界中的静态遮挡物。常见的例子包括物理传感器缺陷、环境遮挡（如镜头外壳上的雨滴或泥土）、拍摄遮挡（如相机传感器上的拇指或行车记录仪画面中可见的仪表盘）以及数字叠加层（如水印或 UI 元素）。当这些伪影存在时，它们会被错误地作为“漂浮物”或近相机伪影固化到 3D 几何结构中，从而降低新视角渲染的质量。在这项工作中，我们提出了 SSA-3DGS，这是一个无监督框架，通过联合优化 3D 场景和可学习的 2D 叠加层，来恢复干净的 3D 场景和损坏伪影。通过利用跨视图的几何共识，我们的方法在无需监督或手动输入的情况下，有效地将静态伪影从 3D 场景几何中解耦。在多种合成损坏和自采集的真实世界数据集上，相比于在相同损坏输入上训练的 3DGS，SSA-3DGS 将重建保真度提高了多达 9 dB PSNR，同时忠实地保留了损坏伪影。

## Abstract
Novel View Synthesis (NVS) methods, such as 3D Gaussian Splatting (3DGS), rely heavily on the assumption of clean, multi-view consistent, posed input images. Real-world captures can violate this assumption due to screen-space artifacts-static occlusions fixed to the 2D image plane rather than to the 3D world. Common examples include physical sensor defects, environmental obstructions (such as rain or mud on the lens enclosure), capture obstructions (such as a thumb over the camera sensor or a dashboard visible in dashcam footage), and digital overlays (such as watermarks or UI elements). When present, they are erroneously baked into the 3D geometry as "floaters" or near-camera artifacts, degrading the quality of novel-view rendering. In this work, we propose SSA-3DGS, an unsupervised framework that jointly optimizes a 3D scene and a learnable 2D overlay to recover a clean 3D scene and the corrupting artifacts. By exploiting geometric consensus across views, our method effectively disentangles static artifacts from the 3D scene geometry without supervision or manual input. Across diverse synthetic corruptions and a self-captured real-world dataset, SSA-3DGS improves reconstruction fidelity by up to 9 dB PSNR over 3DGS trained on the same corrupted inputs, while faithfully preserving the corrupting artifact.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SSA-3DGS: Unsupervised Removal of Screen-Space Artifacts for 3D Gaussian Splatting》** 的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：3D 高斯泼溅（3DGS）等新视角合成技术依赖于“多视图一致性”假设，即场景在不同视角下是稳定的。
*   **核心问题**：现实拍摄中常包含**屏幕空间伪影（Screen-Space Artifacts）**，如镜头上的污渍、雨滴、手指遮挡、水印或仪表盘。这些伪影固定在 2D 图像平面上，不随相机移动而改变 3D 位置。
*   **后果**：标准 3DGS 会试图将这些 2D 伪影解释为 3D 几何体，导致在相机附近生成大量的“漂浮物”（floaters），严重破坏重建质量和新视角渲染效果。
*   **研究意义**：提出一种无需人工标注、自动分离并去除这些伪影的框架，从而在受损数据下恢复干净的 3D 场景。

### 2. 论文提出的方法论
*   **核心思想**：利用**几何共识（Geometric Consensus）**。由于伪影在 2D 空间是静态的，而场景在 3D 空间是连贯的，通过联合优化 3D 场景和 2D 覆盖层，可以将两者解耦。
*   **关键技术细节**：
    *   **双分支架构**：系统由两部分组成：一个是标准的 **3D 高斯模型**（代表 3D 场景），另一个是**可学习的 2D 覆盖层（Learnable 2D Overlay）**。
    *   **2D 覆盖层建模**：为每个输入视图（或一组视图）学习一个包含颜色（RGB）和不透明度（Alpha）的 2D 图像。
    *   **合成公式**：最终渲染图像 $I_{final}$ 是 3D 渲染图 $I_{3D}$ 与 2D 覆盖层 $I_{2D}$ 的 Alpha 混合结果：$I_{final} = (1 - \alpha_{2D}) \cdot I_{3D} + \alpha_{2D} \cdot C_{2D}$。
    *   **优化策略**：在训练过程中，优化器发现将不随视角变化的遮挡物放入 2D 覆盖层比在 3D 空间中生成复杂的漂浮物“代价”更低，从而实现自动分离。
    *   **正则化**：为了防止 2D 覆盖层过度吸收场景内容，引入了稀疏性约束（如 L1 正则化），强制 2D 覆盖层仅解释那些无法用 3D 几何解释的区域。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成受损数据集**：在 Mip-NeRF 360 等标准数据集上人工添加水印、镜头污渍、数字 UI 元素。
    *   **真实世界数据集**：自采集的包含真实干扰的数据，如行车记录仪（含仪表盘遮挡）、镜头上有指纹或泥土的拍摄场景。
*   **Benchmark（基准）**：以原始干净场景的重建作为金标准（GT）。
*   **对比方法**：主要对比标准 **3DGS**。实验展示了在相同受损输入下，SSA-3DGS 与标准 3DGS 的表现差异。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出具体的 GPU 型号和总训练时长。但由于其基于标准 3DGS 框架且仅增加了轻量级的 2D 像素优化，可以推断其计算开销与标准 3DGS 相当，保持了 3DGS 训练快速的特性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多种类型的伪影（静态水印、动态遮挡、物理污渍），并在多个场景下进行了验证。
*   **消融实验**：验证了 2D 覆盖层中 Alpha 通道的作用以及正则化项对分离效果的影响。
*   **充分性评价**：实验设计较为充分，通过合成数据提供了量化指标（PSNR 提升高达 9 dB），通过真实数据证明了实际应用价值。对比实验直观地展示了“漂浮物”的消除过程。

### 6. 论文的主要结论与发现
*   **性能提升**：在处理带有严重屏幕空间干扰的图像时，SSA-3DGS 的重建质量远超标准 3DGS，PSNR 提升显著。
*   **解耦能力**：该方法不仅能恢复干净的 3D 场景，还能准确地提取出伪影的掩码（Mask）和内容。
*   **无监督优势**：无需预训练的分割模型或手动绘制掩码，系统能自动识别哪些像素属于“非一致性”的屏幕伪影。

### 7. 优点
*   **简单高效**：方法论直观，通过简单的 Alpha 混合公式解决了复杂的几何畸变问题。
*   **完全无监督**：极大地降低了数据准备的成本，适用于无法预知伪影类型的野外采集数据。
*   **鲁棒性强**：对不同类型的屏幕空间干扰（从半透明污渍到全不透明水印）均有效。

### 8. 不足与局限
*   **静态假设**：该方法假设伪影在屏幕空间是相对静态的。如果伪影在拍摄过程中大幅度移动（例如雨刷扫过镜头），该方法可能难以完美处理。
*   **场景依赖**：如果场景本身缺乏纹理或多视图重叠度极低，系统可能会误将部分场景内容归类为 2D 覆盖层。
*   **计算存储**：虽然计算增加不多，但为每个视图存储一个 2D 覆盖层会增加一定的内存/显存开销。

（完）
