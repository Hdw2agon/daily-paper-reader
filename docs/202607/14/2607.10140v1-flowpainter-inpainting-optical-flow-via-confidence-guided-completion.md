---
title: "FlowPainter: Inpainting Optical Flow via Confidence-Guided Completion"
title_zh: FlowPainter：通过置信度引导补全进行光流修复
authors: "Yuang Meng, Chenyang Wu, Xianshun Liu, Chun-Le Guo, Zichen Liang, Lina Lei, Jie Liang, Hui Zeng, Chongyi Li, Lei Zhang"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10140v1"
tags: ["query:q1"]
score: 9.0
evidence: 通过置信度引导的补全进行光流修复
tldr: 针对现有扩散模型在光流估计中对全场去噪导致收敛慢且训练不稳的问题，本文提出FlowPainter。该框架将光流生成重新定义为置信度引导的软修复过程，利用轻量级网络预测初始流场和置信掩码，通过置信度门控残差引导扩散模型进行精细化。实验证明，该方法在Sintel和KITTI等基准上实现了高精度和更快的收敛速度，有效结合了判别式先验与生成式模型的优势。
selection_source: fresh_fetch
motivation: 现有的扩散光流模型对包含简单区域的整个流场进行去噪，增加了计算负担并导致训练不稳定和收敛缓慢。
method: 将光流生成建模为置信度引导的软修复，利用轻量级网络生成的先验流场和置信掩码来引导扩散模型的残差去噪。
result: 在Sintel、KITTI和Spring等基准测试中，FlowPainter在精度和收敛效率上均优于现有的扩散光流方法。
conclusion: 该研究证明了通过置信度引导集成判别式先验可以显著提升扩散模型在复杂运动估计任务中的性能和实用性。
---

## 摘要
现有的光流方法大致遵循两种范式：迭代优化和基于扩散的估计。以 RAFT 为代表的迭代方法通过循环细化实现了高精度，但在处理大位移和复杂运动时仍面临挑战。基于扩散的方法引入了生成建模，并在这些模糊区域展现出潜力。然而，现有的扩散模型通常从高斯噪声中对整个密集流场进行去噪，包括那些可以通过轻量级网络估计出可靠运动的简单区域。这增加了去噪负担，并可能导致收敛缓慢和训练不稳定。为了解决这个问题，我们提出了 FlowPainter，这是一个基于扩散的光流框架，它将密集流生成重新表述为置信度引导的软修复（soft inpainting）。FlowPainter 采用轻量级置信度感知网络来预测粗略光流和像素级置信度掩码，从而区分可靠的简单区域和不确定的困难区域。由此产生的简单流先验用于基于置信度的初始化，并通过置信度门控残差引导进一步注入到迭代去噪中。通过动态衰减的引导强度，FlowPainter 在稳定早期去噪的同时，保留了扩散模型在后期细节细化方面的灵活性。在 Sintel、KITTI 和 Spring 等公开基准测试上的广泛实验表明，在同等训练设置下，FlowPainter 实现了极高的准确度，且比现有的基于扩散的光流方法收敛更高效，在具有挑战性的基准划分上取得了显著提升。我们的方法为将可靠的判别式先验与基于扩散的光流估计细化相结合提供了一种实用的途径。我们的代码可在 https://github.com/mya012/FlowPainter 获取。

## Abstract
Existing optical flow methods broadly follow two paradigms: iterative optimization and diffusion-based estimation. Iterative methods, exemplified by RAFT, achieve high accuracy through recurrent refinement, but remain challenged by large displacements and complex motion. Diffusion-based methods introduce generative modeling and show promise in such ambiguous regions. However, existing diffusion models usually denoise the entire dense flow field from Gaussian noise, including simple regions where reliable motion can already be estimated by a lightweight network. This increases the denoising burden and may cause slow convergence and unstable training. To address this issue, we introduce FlowPainter, a diffusion-based optical flow framework that reformulates dense-flow generation as confidence-guided soft inpainting. FlowPainter employs a lightweight confidence-aware network to predict a rough flow and a pixel-wise confidence mask, distinguishing reliable simple regions from uncertain hard regions. The resulting simple-flow prior is used for confidence-based initialization and further injected into iterative denoising through confidence-gated residual guidance. With dynamically decaying guidance strength, FlowPainter stabilizes early denoising while preserving the flexibility of the diffusion model for late-stage detail refinement. Extensive experiments on public benchmarks, including Sintel, KITTI, and Spring, show that FlowPainter achieves strong accuracy under comparable training settings and converges more efficiently than existing diffusion-based optical flow methods, with notable gains on challenging benchmark splits. Our approach offers a practical way to integrate reliable discriminative priors with diffusion-based refinement for optical flow estimation. Our code is publicly available at https://github.com/mya012/FlowPainter.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlowPainter: Inpainting Optical Flow via Confidence-Guided Completion》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的光流估计方法主要分为迭代优化（如 RAFT）和基于扩散的模型（如 FlowDiffuser）。迭代法在处理大位移和复杂运动时效果有限；而扩散模型虽然生成能力强，但通常从纯高斯噪声开始对**全场**进行去噪。
*   **研究动机**：作者观察到，场景中大部分区域是运动简单的“简单区域”，轻量级网络即可处理。现有的扩散模型在这些简单区域浪费了大量计算资源，导致训练不稳定、收敛缓慢且去噪负担过重。因此，如何结合判别式先验的效率与生成式模型的精细化能力，是本文的核心出发点。

### 2. 方法论：核心思想与技术细节
FlowPainter 将光流估计重新定义为一个**置信度引导的软修复（Soft Inpainting）**过程。
*   **置信度感知网络 (Confidence-Aware Network)**：
    *   采用轻量级的 PWC-Net 架构，输入图像对，输出粗略流场 $F_r$ 和像素级置信度掩码 $M_{conf}$。
    *   **关键创新**：对置信度掩码进行显式监督，结合了“硬运动区域”（通过误差图确定）和“遮挡区域”（通过前后向一致性检查确定），使掩码能准确识别哪些区域需要扩散模型介入。
*   **光流修复网络 (Flow Inpainting Network)**：
    *   **置信度初始化**：在扩散过程的起始步，高置信度区域由粗略流填充，低置信度区域由高斯噪声填充。
    *   **置信度门控残差引导**：在去噪的每一步，将粗略流作为残差引导注入扩散特征中。
    *   **时间步衰减调度 (Time Step Decay)**：引导强度随去噪步数增加而动态衰减。早期（高噪声阶段）引导强度大以稳定结构，后期（细节细化阶段）引导减弱以释放扩散模型的生成灵活性。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：FlyingChairs, FlyingThings3D, Sintel, KITTI-15, Spring。
    *   **评估基准 (Benchmark)**：Sintel (Clean/Final 两个版本), KITTI-15 (Train/Test), Spring (Train)。
*   **对比方法**：
    *   **迭代/判别式方法**：RAFT, GMA, GMFlow, FlowFormer, SEA-RAFT, FlowSeek 等。
    *   **扩散/生成式方法**：DDVM, FlowDiffuser, GA-DDVM 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练效率**：
    *   **FlowPainter**：在 Sintel 阶段仅需 50k 次迭代即可收敛。总训练时长约为 **2 天 1 小时**。
    *   **对比基准 (FlowDiffuser)**：在相同设置下需要约 180k 次迭代，总时长约为 **7 天 2 小时**。
    *   **结论**：FlowPainter 显著降低了扩散模型的训练成本，提升了收敛效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流大型基准上进行了详尽的定量对比，并提供了 Sintel Test 榜单的排名数据。
*   **消融实验**：
    *   验证了置信度掩码中“遮挡”与“硬运动”监督的各自贡献。
    *   测试了时间步衰减模块和修复策略的有效性。
    *   探讨了初始化阈值（0.3 到 0.7）对最终精度的影响。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同位移阈值下的误差，有力地证明了扩散模型在“困难区域”的优势以及本文混合范式的合理性。

### 6. 主要结论与发现
*   **精度领先**：在 Sintel Final 任务上，FlowPainter 相比之前的扩散模型基准（FlowDiffuser）在 Train 集上提升了约 40%，在 Test 集上提升了 16%。
*   **鲁棒性强**：在包含模糊、光照变化和复杂遮挡的 Sintel Final 序列中表现尤为突出。
*   **收敛极快**：通过引入判别式先验，扩散模型不再需要从零学习运动结构，训练时间缩短了 70% 以上。

### 7. 优点与亮点
*   **混合范式的成功应用**：巧妙地将轻量级判别网络作为“引导者”，扩散模型作为“修复者”，解决了纯扩散模型“眉毛胡子一把抓”的效率问题。
*   **置信度机制**：显式监督的置信度掩码比无监督掩码更清晰，能有效区分简单和困难区域。
*   **实用性高**：大幅缩短了扩散类算法的训练周期，使其在实际工程应用中更具可行性。

### 8. 不足与局限
*   **推理延迟**：尽管训练变快了，但推理时仍需进行多步扩散采样（文中为 6 步），相比 RAFT 等单次前向或少量迭代的方法，实时性仍有差距。
*   **先验依赖**：如果第一阶段的轻量级网络在某些极端场景下给出了错误的“高置信度”判断，可能会误导后续的修复过程。
*   **超参数敏感性**：初始化阈值和衰减函数的设置对结果有一定影响，可能需要针对不同数据集进行微调。

（完）
