---
title: Generative Semantic Scene Completion
title_zh: 生成式语义场景补全
authors: "Shi Chen, Weifeng Ge"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.26737v1"
tags: ["query:q1"]
score: 9.0
evidence: 基于稀疏数据的生成式语义场景补全
tldr: 针对室外LiDAR语义场景补全（SSC）中数据稀疏和类别极度不平衡的问题，本文提出了生成式语义场景补全（GSSC）框架。该框架基于离散扩散模型，包含配对稀疏-稠密场景合成（PS3）以扩充长尾数据，语义引导生成补全（SGSC）从噪声生成场景，以及结构化源离散扩散（S2D2）用于精细化现有补全结果。在SemanticKITTI测试中，该方法显著提升了性能，达到了SOTA水平。
selection_source: fresh_fetch
motivation: 旨在解决室外LiDAR场景补全中观测数据极度稀疏以及严重的类别不平衡带来的挑战。
method: 提出一种基于离散扩散的生成式框架，结合合成数据增强、条件生成补全以及单步流匹配精细化技术。
result: "在SemanticKITTI隐藏测试集上，单次采样达到38.8% mIoU，刷新了单帧、因果推理下的最佳记录。"
conclusion: 生成式扩散模型能有效处理稀疏LiDAR数据的场景补全，并通过精细化步骤显著提升现有SSC模型的性能。
---

## 摘要
室外激光雷达（LiDAR）语义场景补全（SSC）旨在从仅观测到目标体积 1% 的扫描数据中恢复稠密的语义体素网格，且面临超过 7,000 倍的类别不平衡挑战。我们将 SSC 重新定义为生成式语义场景补全（GSSC）：一种扮演三种角色的单一离散扩散公式。首先，成对稀疏-稠密场景合成（PS$^3$）生成匹配的稀疏 LiDAR 观测及其对应的稠密语义补全结果，从源头上解决了长尾分布问题，并产生了 PS$^3$-SemanticKITTI 语料库，我们将其与 SemanticKITTI 一起用于训练。其次，语义引导的生成式场景补全（SGSC）通过多项式离散扩散从噪声中生成场景，并以鸟瞰图（BEV）语义地图和稀疏 3D 特征流作为稀疏扫描的条件。第三，该框架还可以通过一个流匹配步骤来优化现有的补全结果：即结构化源离散扩散（S$^2$D$^2$）。S$^2$D$^2$ 提升了 SGSC 自身输出以及所有测试的外部 SSC 基准模型的 mIoU，且无需对基准模型进行重新训练或测试时自适应。在最强的基准模型上，不使用测试时增强的单步处理在 SemanticKITTI 隐藏测试集上达到了 38.8% 的 mIoU。据我们所知，这是该排行榜上因果、单帧、单样本设置下的最佳结果，比相同限制下之前发布的最高分高出 2.1 个百分点。在不受此限制的情况下，采用四步修正和八视图测试时增强可达到 39.2%。

## Abstract
Outdoor LiDAR semantic scene completion (SSC) recovers a dense semantic voxel grid from a scan observing 1% of the target volume, under class imbalance beyond 7,000x. We recast SSC as generative semantic scene completion (GSSC): a single discrete-diffusion formulation in three roles. First, paired sparse-dense scene synthesis (PS$^3$) generates matched sparse LiDAR observations with their dense semantic completions, addressing the long tail at its source and yielding the PS$^3$-SemanticKITTI corpus we train on alongside SemanticKITTI. Second, semantic-guided generative scene completion (SGSC) generates the scene from noise with multinomial discrete diffusion, conditioned on the sparse scan through a bird's-eye-view semantic map and a sparse 3D feature stream. Third, the same framework instead refines an existing completion in one flow-matching step: structured source discrete diffusion (S$^2$D$^2$). S$^2$D$^2$ improves the mIoU of SGSC's own output and every external SSC base tested, without base retraining or test-time adaptation. On the strongest base, one step without test-time augmentation reaches 38.8% mIoU on the SemanticKITTI hidden test. To our knowledge that is the best causal, single-sweep, single-sample result on that leaderboard, +2.1 pp over the previous best published score under the same restriction. Four correction steps with eight-view test-time augmentation reach 39.2%, outside that restriction.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **GSSC（Generative Semantic Scene Completion，生成式语义场景补全）** 的新框架，旨在解决室外 LiDAR 语义场景补全中面临的极端数据稀疏性和类别不平衡问题。

以下是对该论文的详细结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：室外 LiDAR 语义场景补全（SSC）任务极具挑战性。首先，LiDAR 扫描通常只能观测到目标体积的约 1%，数据极其**稀疏**；其次，场景中存在严重的**类别不平衡**（长尾分布），某些类别的样本量差异可达 7000 倍以上。
*   **研究背景**：传统的判别式方法在处理如此稀疏且不平衡的数据时，往往难以准确恢复稠密的几何结构和细粒度的语义信息。本文尝试将 SSC 任务重新定义为一个生成式任务，利用扩散模型的强大建模能力来填补缺失信息。

### 2. 方法论：核心思想与关键技术
论文提出了一个基于**离散扩散（Discrete Diffusion）**的统一框架，包含三个关键组件：

*   **PS³ (Paired Sparse-Dense Scene Synthesis，配对稀疏-稠密场景合成)**：
    *   **核心思想**：从源头上解决长尾分布问题。
    *   **技术细节**：通过合成技术生成大量配对的“稀疏 LiDAR 观测”及其对应的“稠密语义补全结果”。这产生了一个名为 PS³-SemanticKITTI 的增强语料库，用于扩充训练数据，特别是提升模型对稀少类别的识别能力。
*   **SGSC (Semantic-Guided Generative Scene Completion，语义引导生成式场景补全)**：
    *   **核心思想**：将场景补全建模为从噪声到稠密体素的生成过程。
    *   **技术细节**：采用多项式离散扩散模型。生成过程受到两个条件的约束：一是鸟瞰图（BEV）语义地图，二是来自原始稀疏扫描的 3D 特征流。这种条件生成方式确保了补全结果既符合物理观测又具有语义合理性。
*   **S²D² (Structured Source Discrete Diffusion，结构化源离散扩散)**：
    *   **核心思想**：作为一种后处理精细化工具，优化已有的补全结果。
    *   **技术细节**：利用**流匹配（Flow Matching）**技术，仅需一个推理步骤即可修正现有 SSC 模型（包括外部基准模型）的输出。它不需要对原始模型进行重新训练，具有很强的通用性。

### 3. 实验设计
*   **数据集**：主要在 **SemanticKITTI** 数据集上进行验证，这是室外 SSC 任务的标准基准。
*   **Benchmark 与对比方法**：
    *   在 SemanticKITTI 隐藏测试集（Hidden Test Set）上进行评测。
    *   对比了包括 SCPNet, OccFormer, MonoScene 等在内的多种 SOTA（当前最佳）方法。
    *   特别测试了 S²D² 对其他模型（如 SCPNet）的提升效果。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中**未明确说明**具体的 GPU 型号、数量及总训练时长。通常此类 3D 扩散模型训练需要高性能 GPU 集群（如 A100 或 H100），且训练成本较高。

### 5. 实验数量与充分性
*   **实验规模**：实验设计较为充分，涵盖了从单帧采样到多步修正的多种配置。
*   **验证维度**：
    *   **性能对比**：在隐藏测试集上验证了最终性能。
    *   **通用性验证**：通过 S²D² 作用于多个外部基准模型，证明了其精细化能力的普适性。
    *   **消融实验**：验证了 PS³ 合成数据对解决长尾问题的贡献。
*   **客观性**：使用了官方隐藏测试集进行盲测，结果具有较高的客观性和公信力。

### 6. 主要结论与发现
*   **性能突破**：在 SemanticKITTI 隐藏测试集上，单次采样达到了 **38.8% mIoU**，创下了单帧、因果推理设置下的新纪录，比之前的最佳发布结果高出 **2.1 个百分点**。
*   **精细化能力**：S²D² 能够显著提升所有测试的 SSC 基准模型的性能，证明了生成式模型在修正结构化错误方面的潜力。
*   **数据增强效果**：PS³ 合成数据有效地缓解了类别不平衡问题，提升了模型对小样本类别的补全精度。

### 7. 优点与亮点
*   **创新性框架**：将 SSC 任务转化为离散扩散过程，并巧妙地将数据合成、生成补全和结果精细化整合在一个框架内。
*   **即插即用**：S²D² 模块无需重新训练原模型即可提升性能，具有极高的实用价值。
*   **性能卓越**：在极度稀疏的输入下依然能保持高精度的语义和几何恢复。

### 8. 不足与局限
*   **推理开销**：尽管 S²D² 优化到了单步流匹配，但扩散模型整体的推理速度通常仍慢于纯判别式模型，可能影响实时应用。
*   **算力需求**：生成式模型的训练通常比传统方法更耗费资源。
*   **数据依赖**：PS³ 的效果取决于合成场景的质量，如果合成场景与真实场景分布差异过大，可能会引入偏差。

（完）
