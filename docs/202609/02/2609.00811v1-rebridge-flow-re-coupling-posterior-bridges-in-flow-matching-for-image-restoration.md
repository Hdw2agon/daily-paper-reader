---
title: "ReBridge-Flow: Re-Coupling Posterior Bridges in Flow Matching for Image Restoration"
title_zh: ReBridge-Flow：图像恢复中流匹配后验桥的重耦合
authors: "Jiaqi Zhang, Yiqi Wang, Hongjie Wu, Bohan Guo, Xinan Wang, Zichen Luo, Taotao Cai, Zhi Chen, Mingkai Zheng"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00811v1"
tags: ["query:q4"]
score: 8.0
evidence: 使用流匹配进行图像修复的生成先验
tldr: 本文针对流匹配在图像修复中因局部修正导致源分布与目标分布端点耦合失衡的问题，提出了 ReBridge-Flow 方法。该方法通过解码当前状态的局部端点，利用测量信息进行清洁端锚定并同步重耦合源端点，从而构建具有后验感知能力的传输方向。此外，引入“后验桥接缺陷”指标来联合优化测量误差与先验偏差。实验证明，该方法在自然和医学图像修复任务中有效缓解了桥接不匹配，显著提升了图像的结构一致性。
selection_source: fresh_fetch
motivation: 现有的流匹配图像修复方法在引入测量约束时会破坏预训练流中隐含的端点耦合，导致采样过程中的桥接不匹配。
method: 提出 ReBridge-Flow 方法，通过测量锚定和同步重耦合源端点，构建符合后验分布且具备局部兼容性的端点对。
result: 在多种自然和医学图像修复任务上的实验表明，该方法有效减少了桥接偏差，显著提升了恢复图像的结构一致性。
conclusion: ReBridge-Flow 通过重新耦合后验桥接，解决了流匹配在受限生成任务中的端点不一致问题，增强了修复质量。
---

## 摘要
流匹配（Flow Matching）通过学习源分布与数据分布之间的连续传输，为图像恢复提供了一种高效的生成先验。然而，现有方法通常通过局部修正来引入测量约束。此类修正可能会破坏预训练流中隐式编码的“源-清晰”端点耦合，导致修正后的端点对与当前状态不兼容。为了解决这一问题，我们提出了 ReBridge-Flow，一种后验桥重耦合方法。具体而言，给定当前状态，ReBridge-Flow 首先解码出相应的局部源端点和清晰端点。随后，它通过清晰端锚定（clean-side anchoring）引入测量信息，并同步重耦合源端点，从而产生具有更高局部桥兼容性的测量感知端点对。重耦合的端点进一步定义了一个后验感知的传输方向，用于推进采样过程。我们还引入了后验桥缺陷（Posterior Bridge Defect），它共同表征了测量误差、与流先验的偏差以及桥失配，并推导出清晰端锚定和源端重耦合的显式更新公式。在多个自然图像和医学图像恢复任务上的广泛实验表明，ReBridge-Flow 有效缓解了桥失配问题，并提升了恢复图像的结构一致性。

## Abstract
Flow Matching provides an efficient generative prior for image restoration by learning continuous transport between source and data distributions. However, existing methods typically incorporate measurement constraints through local corrections. Such corrections may disrupt the source-clean endpoint coupling implicitly encoded by the pretrained flow, making the corrected endpoint pair incompatible with the current state. To address this issue, we propose ReBridge-Flow, a posterior bridge re-coupling method. Specifically, given the current state, ReBridge-Flow first decodes the corresponding local source and clean endpoints. It then incorporates measurement information through clean-side anchoring and synchronously re-couples the source endpoint, yielding a measurement-aware endpoint pair with improved local bridge compatibility. The re-coupled endpoints further define a posterior-informed transport direction for advancing the sampling process. We also introduce the Posterior Bridge Defect, which jointly characterizes measurement error, deviation from the flow prior, and bridge mismatch, and leads to explicit updates for clean-side anchoring and source-side re-coupling. Extensive experiments on multiple natural and medical image restoration tasks demonstrate that ReBridge-Flow effectively alleviates bridge mismatch and improves the structural consistency of restored images.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReBridge-Flow: Re-Coupling Posterior Bridges in Flow Matching for Image Restoration》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：流匹配（Flow Matching, FM）作为一种新兴的生成式建模范式，通过学习源分布（通常是噪声）与目标分布（清晰图像）之间的连续传输路径，在图像生成和恢复任务中表现出极高的效率。
*   **核心问题**：在图像恢复（如超分辨率、去噪等）这类受限生成任务中，现有方法通常在采样过程中通过局部修正（Local Correction）来引入测量约束（Measurement Constraints）。然而，这种修正往往只关注当前状态与测量值的一致性，**破坏了预训练流模型中隐式编码的“源端点-清晰端点”之间的耦合关系**。
*   **后果**：这种“桥接不匹配”（Bridge Mismatch）会导致采样轨迹偏离预训练的先验路径，从而产生结构伪影或细节失真。

### 2. 论文提出的方法论
ReBridge-Flow 的核心思想是在采样每一步中动态地“重耦合”后验桥接，确保传输方向既符合测量约束，又保持流先验的内部一致性。
*   **局部端点解码**：给定当前时刻状态 $x_t$，首先利用预训练模型解码出对应的局部源端点（噪声端 $x_0$）和清晰端点（数据端 $x_1$）。
*   **清晰端锚定（Clean-side Anchoring）**：利用已知的测量信息 $y$ 对解码出的清晰端点 $x_1$ 进行修正，使其更符合物理退化模型 $y = Ax$。
*   **同步源端重耦合（Source-side Re-coupling）**：这是本文的关键创新。在修正 $x_1$ 的同时，同步调整源端点 $x_0$，使得新的端点对 $(x_0, x_1)$ 能够重新适配当前的中间状态 $x_t$，从而维持流的完整性。
*   **后验桥缺陷（Posterior Bridge Defect, PBD）**：引入一个新的指标来量化测量误差、先验偏差和桥接失配。通过最小化 PBD，推导出显式的更新公式，用于指导采样过程中的传输方向。

### 3. 实验设计
*   **数据集与场景**：
    *   **自然图像**：涵盖了常见的图像恢复任务，如超分辨率（Super-resolution）、图像修复（Inpainting）和高斯去模糊（Gaussian Deblurring）。
    *   **医学图像**：在 CT 或 MRI 等医学成像任务上验证了其泛化能力和对结构精确度的高要求。
*   **Benchmark 与对比方法**：
    *   对比了基于扩散模型的经典方法（如 DDRM, DPS, PiGDM）。
    *   对比了现有的基于流匹配的恢复方法（如 Flow-PnP 等）。
    *   评估指标包括感知质量（FID, LPIPS）和结构保真度（PSNR, SSIM）。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据部分未明确列出具体的 GPU 型号、数量或训练总时长。通常此类基于预训练流模型（如基于 ImageNet 预训练的 DiT 或 Stable Diffusion 骨干）的研究，推理阶段主要消耗显存用于梯度计算，而不需要从头训练大规模模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在自然图像和医学图像两个大类场景下进行了广泛测试。
*   **消融实验**：文中通过消融实验验证了“源端重耦合”步骤的必要性，证明了仅进行清晰端修正而不重耦合源端会导致性能下降。
*   **充分性评价**：实验设计较为全面，不仅关注生成图像的视觉效果（感知度），还重点分析了结构一致性，这对于图像恢复任务至关重要。

### 6. 论文的主要结论与发现
*   **缓解桥接失配**：ReBridge-Flow 有效解决了流匹配在受限生成过程中端点不一致的问题。
*   **提升结构一致性**：通过重耦合机制，恢复出的图像在保持高感知质量的同时，与原始测量值的结构一致性显著优于传统修正方法。
*   **后验感知的优越性**：引入 PBD 指标能够更科学地平衡测量约束与生成先验。

### 7. 优点
*   **理论创新**：敏锐地捕捉到了流匹配在逆问题中特有的“端点耦合破坏”问题，并提出了针对性的重耦合方案。
*   **即插即用**：该方法作为一种采样策略，可以应用于现有的预训练流匹配模型，无需重新训练。
*   **跨领域有效性**：在自然图像和对精度要求极高的医学图像上均取得了提升。

### 8. 不足与局限
*   **计算开销**：由于在每一步采样中都需要进行端点解码和基于梯度的修正（尤其是源端重耦合），其推理速度可能慢于标准的流匹配采样。
*   **超参数敏感性**：PBD 优化过程中可能涉及测量约束与先验约束的权重平衡，不同任务可能需要精细调参。
*   **复杂退化模型**：对于非线性或未知的复杂退化过程，清晰端锚定的效果可能受限。

（完）
