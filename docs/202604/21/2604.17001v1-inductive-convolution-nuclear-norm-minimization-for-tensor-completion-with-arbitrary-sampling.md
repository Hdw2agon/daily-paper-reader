---
title: Inductive Convolution Nuclear Norm Minimization for Tensor Completion with Arbitrary Sampling
title_zh: 针对任意采样张量补全的归纳式卷积核范数最小化
authors: "Wei Li, Yuyang Li, Kaile Du, Yi Yu, Guangcan Liu"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17001v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于恢复以任意方式采样的条目的张量补全
tldr: 针对任意采样下的张量补全问题，传统的卷积核范数最小化（CNNM）方法因需多次进行奇异值分解（SVD）而计算缓慢。本文提出归纳卷积核范数最小化（ICNNM），通过引入预学习的共享卷积特征向量重构优化目标，成功绕过了SVD步骤。该方法不仅大幅提升了计算效率，还利用先验知识增强了恢复性能。在视频补全与预测等任务上的实验验证了其优越性。
selection_source: fresh_fetch
motivation: 解决CNNM在处理任意采样张量补全时，因频繁进行奇异值分解（SVD）导致的计算开销大且难以并行化的问题。
method: 提出ICNNM方法，通过引入预学习的共享卷积特征向量重构优化目标，从而避免了复杂的SVD计算。
result: 实验证明ICNNM在视频补全、预测和帧插值任务中，不仅显著缩短了计算时间，且恢复精度优于CNNM及其他主流方法。
conclusion: ICNNM通过结合归纳学习与卷积特征，实现了高效且高精度的张量恢复，是解决任意采样张量补全问题的有效方案。
---

## 摘要
最近提出的卷积核范数最小化（CNNM）解决了任意采样张量补全（TCAS）问题，即从以任意方式采样的部分条目中恢复张量。尽管其性能表现优异，但 CNNM 的优化过程需要多次执行奇异值分解（SVD），这在计算上非常昂贵且难以并行化。为了解决这一问题，我们从卷积特征向量的角度重新构建了 CNNM 的优化目标。通过引入在不同张量间共享的预学习卷积特征向量，我们提出了一种名为归纳式卷积核范数最小化（ICNNM）的新方法，该方法绕过了 SVD 步骤，从而显著缩短了计算时间。此外，由于预学习卷积特征向量中编码了额外的先验知识，ICNNM 在恢复性能方面也优于 CNNM。在视频补全、预测和帧插值方面的广泛实验验证了 ICNNM 优于 CNNM 和其他几种竞争方法。

## Abstract
The recently established Convolution Nuclear Norm Minimization (CNNM) addresses the problem of \textit{tensor completion with arbitrary sampling} (TCAS), which involves restoring a tensor from a subset of its entries sampled in an arbitrary manner. Despite its promising performance, the optimization procedure of CNNM needs performing Singular Value Decomposition (SVD) multiple times, which is computationally expensive and hard to parallelize. To address the issue, we reformulate the optimization objective of CNNM from the perspective of convolution eigenvectors. By introducing pre-learned convolution eigenvectors which are shared among different tensors, we propose a novel method called Inductive Convolution Nuclear Norm Minimization (ICNNM), which bypasses the SVD step so as to decrease significantly the computational time. In addition, due to the extra prior knowledge encoded in the pre-learned convolution eigenvectors, ICNNM also outperforms CNNM in terms of recovery performance. Extensive experiments on video completion, prediction and frame interpolation verify the superiority of ICNNM over CNNM and several other competing methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**归纳式卷积核范数最小化（ICNNM）**的方法，旨在解决张量补全中极具挑战性的“任意采样”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：任意采样下的张量补全（TCAS）。传统的张量补全通常假设采样是均匀随机的，但在实际应用（如视频预测、图像超分辨率）中，缺失条目的分布往往是任意且非均匀的。
*   **研究背景**：现有的卷积核范数最小化（CNNM）方法虽然能理论上解决 TCAS 问题，但其优化过程依赖于多次对大型卷积矩阵进行奇异值分解（SVD）。
*   **研究动机**：SVD 计算开销巨大，且难以在 GPU 上并行化，导致 CNNM 处理大规模数据时效率低下。作者希望通过引入“归纳学习”的思想，利用预学习的特征来规避 SVD 计算。

### 2. 方法论
*   **核心思想**：将卷积核范数重构为卷积特征向量（Convolution Eigenvectors）的形式。作者发现卷积特征向量在同类张量间具有共享性（类似于 CNN 中的滤波器），因此可以提前从参考数据中学习这些向量。
*   **关键技术细节**：
    *   **预学习阶段**：从一组参考张量中提取卷积特征向量，构建一个正交矩阵 $K$。这一步通过计算参考张量集合的右奇异向量实现。
    *   **优化目标重构**：将 CNNM 的核范数最小化目标替换为 $\|A_k(L)K\|_{2,1}$，即最小化卷积矩阵与预学习矩阵 $K$ 乘积的 $\ell_{2,1}$ 范数。
    *   **算法流程**：采用交替方向乘子法（ADMM）求解。由于 $K$ 是预先确定的，优化过程中最耗时的步骤从 SVD 变为了矩阵乘法，极大地提升了计算速度。
*   **理论保证**：论文给出了在无噪声和有噪声情况下的精确/近似恢复定理，定义了“相对卷积秩”和“谱相关系数”来量化预学习特征与目标张量的匹配程度。

### 3. 实验设计
*   **实验场景**：涵盖了图像补全、视频补全、视频预测和视频帧插值四个任务。
*   **数据集**：
    *   图像：CSet8, Kodak。
    *   视频：CDNet 2014, UCF-101。
*   **对比方法（Benchmark）**：
    *   传统低秩方法：IRNN, HaLRTC, TNNM。
    *   卷积低秩方法：DFT $\ell_1$, CNNM。
    *   任务特定深度学习方法：ConvLSTM（视频预测）, SuperSloMo, RIFE, EMA-VFI（帧插值）。

### 4. 资源与算力
*   **硬件设备**：
    *   视频帧插值实验：NVIDIA A800 GPU。
    *   其他实验（图像/视频补全与预测）：NVIDIA GeForce RTX 4090 GPU。
*   **软件环境**：MATLAB R2024a 和 PyTorch。
*   **算力说明**：文中强调了 ICNNM 相比 CNNM 在 GPU 上的加速效果，因为矩阵乘法比 SVD 更能发挥并行计算优势。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比，包括不同缺失率（50%-90%）、不同核大小（13x13 到 43x43）以及不同参考图像方案的消融实验。
*   **充分性评价**：实验设计较为充分。作者不仅对比了同类数学优化方法，还对比了针对特定任务设计的深度学习模型（如 RIFE），证明了该通用恢复框架的竞争力。此外，通过在不同数据集上学习和测试，验证了模型的泛化能力。

### 6. 主要结论与发现
*   **效率提升**：ICNNM 的计算速度远快于 CNNM，尤其是在核尺寸较大时，加速效果更加显著。
*   **性能增强**：由于预学习的卷积特征向量引入了额外的先验知识，ICNNM 在恢复精度（PSNR/SSIM）上普遍优于 CNNM。
*   **泛化性**：即使参考图像与目标图像不完全一致（如用 CSet8 训练恢复 Kodak），ICNNM 依然表现稳健。
*   **TCAS 优势**：在非均匀采样（如整帧缺失或块缺失）情况下，ICNNM 显著优于传统的低秩张量补全方法。

### 7. 优点
*   **计算友好**：成功绕过了复杂的 SVD 步骤，使卷积低秩模型在大规模数据上的应用成为可能。
*   **理论与实践结合**：既有严谨的恢复边界证明，又有在多个视觉任务中的优异表现。
*   **通用性强**：一个统一的框架可以处理补全、预测和插值等多种任务。

### 8. 不足与局限
*   **参考数据依赖**：恢复效果受预学习特征向量质量的影响。如果参考数据与目标数据分布差异极大，性能可能会下降（虽然实验表明自然图像间具有一定的通用性）。
*   **超参数敏感性**：仍需手动设置卷积核大小 $k$ 和正则化参数 $\lambda$，缺乏自适应调整机制。
*   **深度融合不足**：目前仍属于传统优化算法范畴，虽然文中提到可以展开为深度网络，但尚未实现端到端的深度学习集成。

（完）
