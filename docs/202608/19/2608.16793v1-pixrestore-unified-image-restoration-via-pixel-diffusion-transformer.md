---
title: "PixRestore: Unified Image Restoration via Pixel Diffusion Transformer"
title_zh: PixRestore：基于像素扩散 Transformer 的统一图像修复
authors: "Lingchen Sun, Rongyuan Wu, Xiangtao Kong, Jixin Zhao, Qiaosi Yi, Yujing Sun, Shuaizheng Liu, Zhengqiang Zhang, Lei Zhang"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16793v1"
tags: ["query:q4"]
score: 9.0
evidence: 用于图像修复和合成的生成模型
tldr: PixRestore 是一种针对统一图像恢复（UIR）任务的无 VAE 像素空间扩散 Transformer 模型。针对现有基于潜在扩散模型易丢失细节和产生伪影的问题，该方法直接在像素块上进行流匹配，并利用 DINO 特征相似度预测层特征可靠性以实现自适应融合。通过对抗性微调实现单步推理，在仅 50M 参数下展现了卓越的保真度、感知质量和鲁棒性，且具备良好的可扩展性。
selection_source: fresh_fetch
motivation: 现有基于潜在扩散模型的方法因 VAE 压缩导致细节丢失，且其生成先验易产生与原图内容不一致的伪影。
method: 提出一种从零训练的像素空间 DiT，通过流匹配处理像素块，并结合 DINO 特征相似度引导的特征融合与单步生成对抗微调。
result: 在多个基准测试中，仅含 50M 参数的 PixRestore 在单步推理下实现了最优的保真度和鲁棒性，且推理效率极高。
conclusion: PixRestore 证明了无需预训练 T2I 模型，通过像素空间扩散设计也能在统一图像恢复任务中实现高效且高质量的效果。
---

## 摘要
统一图像修复 (UIR) 旨在利用单个模型从具有不同退化的低质量 (LQ) 图像中恢复高质量 (HQ) 内容。最近的大多数方法利用大型预训练文本到图像 (T2I) 潜在扩散模型，因其具有强大的能力和生成先验。然而，潜在 T2I 模型中的变分自编码器 (VAE) 可能会丢弃对修复敏感的细节，而开放式合成先验可能会引入内容不一致的伪影。我们提出了 PixRestore，一种用于 UIR 的无 VAE 像素空间扩散 Transformer (DiT)，其扩散主干网络完全从头开始训练，不依赖于 T2I 预训练。PixRestore 直接在分块后的像素上执行流匹配 (flow matching)，在保持细粒度细节的同时使 Token 序列易于处理。为了适应不同的退化，PixRestore 学习利用 LQ-HQ DINO 特征相似度来预测层特征的可靠性。来自更可靠层的特征被融合为密集条件，而可靠性较低的层则接受更强的高质量特征监督，以促进退化去除。我们在包含多样化场景和退化的大规模语料库上训练 PixRestore，并使用基于 DINO 的对抗性目标将其进一步微调为单步生成器，以实现高效推理。在公共基准测试和真实世界测试集上的实验表明，仅凭约 50M 的参数和单步推理，PixRestore 在竞争性 UIR 模型中实现了最佳的整体保真度、感知质量和退化鲁棒性，同时效率更高。更大的 PixRestore 变体可以进一步提升性能，证明了我们像素空间设计的可扩展性。代码和整理后的基准测试可在 https://github.com/csslc/PixRestore 找到。

## Abstract
Unified image restoration (UIR) aims to recover high-quality (HQ) content from low-quality (LQ) images with different degradations using a single model. Most recent methods adapt large pretrained text-to-image (T2I) latent diffusion models for their strong capacity and generative priors. However, the variational autoencoder (VAE) in latent T2I models may discard restoration-sensitive details, while the open-ended synthesis prior can introduce content-inconsistent artifacts. We present PixRestore, a VAE-free pixel-space Diffusion Transformer (DiT) for UIR, where the diffusion backbone is trained entirely from scratch, without relying on T2I pretraining. PixRestore performs flow matching directly on patchified pixels, preserving fine-grained details while keeping the token sequence tractable. To adapt to different degradations, PixRestore learns to predict the reliability of layer features using LQ--HQ DINO feature similarity. Features from more reliable layers are fused as dense conditioning, while less reliable layers receive stronger HQ-feature supervision to encourage degradation removal. We train PixRestore on a large-scale corpus of diverse scenes and degradations, and further finetune it into a one-step generator using DINO-based adversarial objectives for efficient inference. Experiments on public benchmarks and real-world test sets show that, with only about 50M parameters and single-step inference, PixRestore achieves the best overall fidelity, perceptual quality, and robustness to degradations among competing UIR models while being far more efficient. Larger PixRestore variants can further boost performance, demonstrating the scalability of our pixel-space design. Code and the curated benchmark can be found at https://github.com/csslc/PixRestore.

---

## 论文详细总结（自动生成）

### PixRestore: 基于像素扩散 Transformer 的统一图像修复总结

#### 1. 核心问题与整体含义
*   **核心问题**：统一图像修复（Unified Image Restoration, UIR）旨在用单个模型处理多种退化（如噪声、模糊、雨滴、雾霾、低光照等）。
*   **研究动机**：
    *   **现有方法的缺陷**：目前主流方法多基于预训练的文本到图像（T2I）潜在扩散模型（LDMs）。但这些模型存在三个痛点：1. **VAE 瓶颈**：变分自编码器在压缩过程中会丢失修复所需的精细纹理；2. **目标不匹配**：T2I 先验倾向于生成视觉合理但与原图不一致的虚假细节；3. **计算冗余**：模型参数量巨大且推理速度慢。
    *   **核心含义**：PixRestore 抛弃了 VAE 和 T2I 预训练，回归像素空间，提出一种轻量级、高保真且高效的“修复原生”扩散模型。

#### 2. 方法论
*   **核心思想**：在像素空间直接进行流匹配（Flow Matching），并结合自适应的层次化视觉引导。
*   **关键技术细节**：
    *   **无 VAE 像素空间 DiT**：直接将 RGB 图像切分为 Patch（分块），在像素级执行扩散过程，避免了 VAE 带来的信息损失。
    *   **流匹配（Flow Matching）**：采用线性插值路径，训练模型预测从噪声到清晰图像的“速度”向量。
    *   **自适应层次化视觉引导**：
        *   利用预训练的 **DINOv2** 提取多层特征。
        *   **自适应层路由（Adaptive Layer Router）**：根据输入图像的退化类型，动态预测各层特征的权重（基于 LQ-HQ 特征相似度训练），将可靠特征融合为条件输入。
        *   **层次化监督**：对不可靠的层施加更强的特征监督，强制模型修复特定退化。
    *   **单步推理微调**：通过基于 DINO 的对抗性目标，将多步扩散模型微调为单步生成器，大幅提升推理效率。

#### 3. 实验设计
*   **数据集/场景**：涵盖 8 种退化任务（去雨痕、去雨滴、去噪、去模糊、去雪、去雾、低光增强、超分辨率）。
*   **训练数据**：构建了一个包含约 283 万张图像的大规模语料库。
*   **Benchmark（基准测试）**：
    *   **公共基准**：GoPro, UHD-blur, RESIDE-6K, DIV2K, PolyU, RainDS-real, RealRain-1k, UHD-LL, LOL, WeatherBench, RealSR 等。
    *   **真实世界测试集**：收集了 600 张无参考图像的真实场景数据。
*   **对比方法**：包括回归模型（PromptIR）、扩散模型（DiffUIR, DA-CLIP, FoundIR）以及基于 T2I 的模型（FoundIR-v2, Flux-IR, FAPE-IR）。

#### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   多步模型预训练：250,000 次迭代。
    *   单步生成器微调：100,000 次迭代。
*   **参数规模**：PixRestore-S 仅约 **50M** 参数（含 DINO 编码器），远小于 T2I 类模型的数十亿参数。

#### 5. 实验数量与充分性
*   **实验规模**：在 15 个公共基准测试集上进行了详尽测试，并提供了 4 种不同规模的变体（S, B, L, XL）。
*   **消融实验**：针对 DINO 条件、多层特征融合方式、自适应权重、单步微调效果、像素空间 vs 潜在空间等进行了多组对比。
*   **公平性**：为了消除训练数据差异的影响，作者特意将主流对比方法在相同的数据集上进行了**重新训练（Retrained）**，确保了对比的客观性。
*   **新指标**：引入了基于 VLM（Gemini 3.1 Pro）的 **DR-Score**，解决了传统无参考指标无法准确衡量“退化去除程度”的问题。

#### 6. 主要结论与发现
*   **性能卓越**：PixRestore-S 在仅 50M 参数下，性能优于参数量大得多的 T2I 适配模型。
*   **像素空间优势**：实验证明在 UIR 任务中，像素空间建模比潜在空间（VAE）更能保持图像保真度。
*   **高效推理**：单步推理延迟仅为 **44ms**，比同类扩散模型快 7-23 倍。
*   **可扩展性**：随着模型规模（从 S 到 XL）和 Patch 大小的优化，性能呈现稳定的 Scaling 趋势。

#### 7. 优点
*   **高保真度**：无 VAE 设计彻底解决了修复任务中细节丢失的问题。
*   **极高效率**：单步推理和轻量化架构使其具备极强的实用性。
*   **自适应强**：通过 DINO 特征的自适应路由，模型能根据不同退化类型自动调整关注的特征层。
*   **评估创新**：提出的 DR-Score 指标与人类感知高度一致，为无参考图像修复评估提供了新思路。

#### 8. 不足与局限
*   **分辨率限制**：由于采用固定 Token 化的 DiT 架构，模型在处理非训练分辨率（如极高分辨率）时可能需要重新训练或架构调整。
*   **极端退化挑战**：相比于拥有数十亿参数的 T2I 模型，较小的主干网络在处理信息极度匮乏的极端退化时，生成能力可能略逊一筹。
*   **指标依赖**：DR-Score 依赖于闭源的商业 VLM 模型，可能存在版本更新导致评分标准波动的风险。

（完）
