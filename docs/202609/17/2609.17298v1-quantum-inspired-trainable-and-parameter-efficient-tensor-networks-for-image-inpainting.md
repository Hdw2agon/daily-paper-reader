---
title: Quantum-Inspired Trainable and Parameter-Efficient Tensor Networks for Image Inpainting
title_zh: 用于图像修复的量子启发式可训练且参数高效的张量网络
authors: "Shiwen An, Konstantinos Slavakis"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17298v1"
tags: ["query:q1"]
score: 10.0
evidence: 用于图像修复的张量网络
tldr: 本文提出了一种基于量子启发张量网络电路的图像修复方法。通过引入可训练的对角量子傅里叶变换（QFT）松弛架构，该方法在保持计算高效性的同时，利用电路结构天然维持相干性，无需额外惩罚项。研究实现了无约束梯度优化，在参数量极少的情况下，不仅在修复任务中超越了传统固定变换和单图优化方法，还展现了优异的泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决传统图像修复中参数量大、计算复杂及相干性难以保持的问题。
method: 利用量子启发张量网络电路作为可训练变换，并采用对角QFT松弛架构实现高效的无约束梯度优化。
result: 在参数量显著减少的情况下，该模型在图像修复任务中表现优于固定变换及单图优化方法，并媲美大型酉架构。
conclusion: 该研究证明了量子启发张量网络在图像修复中具有高效、轻量且具备良好泛化性能的潜力。
---

## 摘要
本文引入了量子启发式张量网络电路作为图像修复的可训练变换。在所提出的架构中，对角量子傅里叶变换（QFT）松弛是可逆的，对于 N×N 图像，其计算成本为 O(N² log N)；该架构通过其电路结构在训练过程中固有地保持了最小相干性，从而无需显式的相干性惩罚。无约束的基于梯度的相位优化（无需黎曼优化）实现了从随机采样训练数据中的高效学习，使学习到的变换能够泛化到通过固定采样掩码观测到的测试图像。数值测试表明，所学习的模型优于固定变换和逐图像优化方法，且在参数量远少于大型酉架构的情况下，达到了与其相当的性能。

## Abstract
This work introduces quantum-inspired tensor-network circuits as trainable transforms for image inpainting. Among the proposed architectures, the diagonal quantum Fourier transform (QFT) relaxation is invertible with $O(N^2 \log N)$ computational cost for $N\times N$ images, inherently preserving minimum coherence throughout training via its circuit structure and eliminating the need for explicit coherence penalties. Unconstrained gradient-based phase optimization (Riemannian-optimization free) enables efficient learning from randomly sampled training data, allowing the learned transform to generalize to test images observed through fixed sampling masks. Numerical tests show that the learned models outperform fixed transforms and per-image optimization while matching the performance of much larger unitary architectures, yet with far fewer parameters.

---

## 论文详细总结（自动生成）

这是一份关于论文《用于图像修复的量子启发式可训练且参数高效的张量网络》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：图像修复（Inpainting）旨在从部分观测数据中恢复完整图像。传统方法依赖于稀疏性（如小波变换）或低秩矩阵补全。
*   **痛点**：
    *   **相干性（Coherence）矛盾**：稀疏性与低相干性之间存在权衡。高相干性（如小波）虽能提供稀疏近似，但会导致修复性能下降。
    *   **参数与计算效率**：现有的深度学习或张量分解方法往往参数量巨大，或需要复杂的黎曼流形优化，计算成本高昂。
*   **动机**：利用量子电路的结构特性（如 QFT）设计一种既能保持低相干性，又能通过梯度下降高效训练的轻量级变换模型。

### 2. 方法论
*   **核心思想**：将图像视为张量，利用量子傅里叶变换（QFT）的电路结构作为可训练的变换基。
*   **关键技术**：
    *   **对角 QFT 松弛**：通过将 QFT 电路中的受控相位门进行参数化，构建可训练的变换。
    *   **结构化保证**：通过电路拓扑结构（每条线仅含一个 Hadamard 门），在数学上保证了变换矩阵的相干性 $\mu=1$（最小相干性），无需显式惩罚项。
    *   **无约束优化**：由于架构的酉性（Unitary）在参数更新中得以保持，无需复杂的黎曼优化，直接使用 Adam 优化器即可实现高效学习。
    *   **算法流程**：采用迭代硬阈值（IHT）算法，将修复过程展开为可微分的计算图，通过反向传播端到端地学习变换参数。

### 3. 实验设计
*   **数据集**：使用 DIV2K 数据集（512x512 裁剪），包含 750 张训练图、50 张验证图和 100 张测试图。
*   **基准对比**：
    *   **固定变换**：DFT、DCT-II、Haar、Daubechies-4、Symlet-8。
    *   **单图优化方法**：Tensor Train、核范数（Nuclear Norm）。
    *   **可训练变换**：U(2) Butterfly 变换、Transform Learning。
*   **评价指标**：PSNR、SSIM、MS-SSIM。

### 4. 资源与算力
*   **算力说明**：文中未明确列出具体的 GPU 型号和数量。
*   **训练成本**：提到训练过程耗时约 741 秒，相当于约 570 次 Tensor Train 的单图拟合过程，显示了该方法在训练阶段的高效性。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了不同采样率（1%-60%）、不同图像分辨率（32-512）以及不同架构变体（相位、对角、旋转模型）的对比。
*   **客观性**：实验设置了统一的测试掩码和预算，对比了多种基准方法，且在不同采样率下进行了鲁棒性测试，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能优越**：所提出的 QFT（对角）模型在 PSNR 和 MS-SSIM 指标上均优于固定变换和单图优化方法。
*   **参数高效**：在达到与大型酉架构相当性能的同时，参数量显著减少（例如相比 Butterfly 架构减少了约 64 倍）。
*   **结构优势**：证明了通过电路拓扑结构强制实现最小相干性，是提升修复性能的有效手段，且避免了训练不稳定性。

### 7. 优点
*   **理论支撑强**：利用量子电路的酉性保证了相干性，将复杂的约束优化问题转化为无约束优化。
*   **计算高效**：推理和训练成本低，具有良好的扩展性（$O(N^2 \log N)$）。
*   **泛化能力**：学习到的变换基在未见过的测试图像上表现出良好的泛化性能。

### 8. 不足与局限
*   **应用场景限制**：目前主要针对灰度图像修复，对于复杂彩色图像或超大规模图像的适用性需进一步验证。
*   **理论假设**：依赖于随机采样掩码的假设，在极端非随机缺失模式下的表现可能受限。
*   **对比局限**：虽然对比了多种传统和经典学习方法，但未与最新的基于扩散模型（Diffusion Models）的修复方法进行深度对比（尽管后者计算成本通常高得多）。

（完）
