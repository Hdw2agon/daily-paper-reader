---
title: "InpaintSLat: Inpainting Structured 3D Latents via Initial Noise Optimization"
title_zh: InpaintSLat：通过初始噪声优化修复结构化 3D 潜变量
authors: "Jaeyoung Chung, Suyoung Lee, Kyoung Mu Lee"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00664v1"
tags: ["query:q3"]
score: 9.0
evidence: 通过噪声优化的免训练3D图像补全方法
tldr: 本研究针对结构化3D潜空间扩散模型在修复任务中几何结构不稳定的问题，提出了一种无需训练的3D修复方法InpaintSLat。该方法通过优化初始噪声来增强生成内容与现有上下文的对齐。具体而言，利用基于修正流模型的反向传播近似和专门设计的频谱参数化技术，实现了高效且稳健的初始噪声更新。实验证明，该方法在上下文一致性和提示词对齐方面显著优于现有基准，为3D修复提供了一个独立且有效的控制维度。
selection_source: fresh_fetch
motivation: 结构化3D潜空间扩散模型在生成早期对初始噪声高度敏感，导致修复任务中新旧结构难以严格对齐。
method: 提出通过修正流模型的反向传播近似和频谱参数化来优化初始噪声，以实现高保真的3D修复。
result: 实验结果显示，该方法在保持上下文一致性和遵循提示词方面均优于代表性的无需训练的修复基准。
conclusion: 初始噪声控制是3D修复中一个与传统采样轨迹操作正交且独立的有效控制维度。
---

## 摘要
我们提出了一种基于初始噪声优化的无需训练的可控 3D 修复方法。在结构化 3D 潜扩散框架中，我们观察到潜在的几何结构是在扩散过程的早期阶段建立的，并且对初始噪声表现出高度敏感性。这种特性损害了修复和编辑等任务的稳定性，因为在这些任务中，模型必须在合成新结构的同时确保与现有上下文的严格对齐。在本文中，我们介绍了一种在结构化 3D 潜扩散框架内优化初始噪声的策略，以确保高保真度的 3D 修复。具体而言，我们利用基于修正流（rectified flow）模型的反向传播近似来更新初始噪声，并采用了专门为鲁棒且高效的结构化 3D 潜变量优化而设计的谱参数化。实验表明，与具有代表性的无需训练的修复基准相比，该方法在上下文一致性和提示词对齐方面取得了持续的改进，将初始噪声控制确立为 3D 修复的一个独立维度，且与传统的采样轨迹操纵相正交。

## Abstract
We present a training-free approach for controllable 3D inpainting based on initial noise optimization. In the structured 3D latent diffusion framework, we observe that the underlying geometric structure is established during the early stages of the diffusion process and exhibits high sensitivity to the initial noise. Such characteristics compromise stability in tasks like inpainting and editing, where the model must ensure strict alignment with the existing context while synthesizing a new structure. In this paper, we introduce a strategy to optimize the initial noise within the structured 3D latent diffusion framework, ensuring high-fidelity 3D inpainting. Specifically, we update the initial noise by leveraging a backpropagation approximation grounded in the rectified flow model, with the spectral parameterization specially designed for robust and efficient structured 3D latent optimization. Experiments demonstrate consistent improvements in contextual consistency and prompt alignment over representative training-free inpainting baselines, establishing initial noise control as an independent dimension for 3D inpainting, orthogonal to conventional sampling trajectory manipulation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **InpaintSLat** 的新型 3D 修复（Inpainting）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 3D 生成任务中，如何实现既符合文本提示词（Prompt）又能与现有 3D 结构完美衔接的局部修复。
*   **研究背景**：现有的 3D 扩散模型（如 TRELLIS）生成的几何结构在早期阶段就已确定，且对初始噪声（Initial Noise）极度敏感。
*   **研究动机**：传统的“免训练”修复方法（如 RePaint、SDEdit）主要通过修改采样轨迹来施加约束，但在 3D 场景下，如果初始噪声与目标结构不匹配，强行引导轨迹往往会导致几何结构崩溃或纹理破碎。因此，作者提出直接优化“初始噪声种子”，从源头上寻找最适合修复任务的起点。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：不改变模型参数，也不仅仅在采样时进行修正，而是通过反向传播优化初始的随机噪声，使其在经过扩散模型去噪后，能精确重建保留区域并合成合理的缺失区域。
*   **关键技术细节**：
    *   **近似反向传播（Approximate Backpropagation）**：为了避免存储整个扩散路径导致的显存爆炸，利用“修正流”（Rectified Flow）模型的线性特性，通过一阶线性近似来估计梯度，从而高效更新初始潜变量。
    *   **频谱参数化（Spectral Parameterization）**：针对 3D 稀疏结构特征，在频率域（通过 3D FFT）进行优化。这允许使用较大的学习率快速调整全局几何，同时抑制高频伪影，保证优化稳定性。
    *   **分布正则化（Distributional Regularization）**：引入均值、方差、偏度和峰度四项统计损失，确保优化后的噪声仍保持在标准高斯分布流形内，防止过拟合导致的生成质量下降。
    *   **两阶段优化**：分别针对稀疏结构（Sparse Structure）和结构化潜变量（SLAT）进行优化，确保宏观形状和微观细节的一致性。

### 3. 实验设计
*   **数据集**：使用了 **Toys4K**（主要定量评估）、**ABO** 和 **HSSD**（补充评估）数据集。
*   **基准方法（Benchmarks）**：对比了 6 种代表性的免训练扩散编辑方法：
    *   采样轨迹类：**RePaint**、**SDEdit**、**ILVR**。
    *   空间混合/引导类：**Blended Diffusion**、**MultiDiffusion**、**DPS**（扩散后验采样）。
*   **评估指标**：
    *   **重建质量**：PSNR、SSIM、LPIPS（外观）；Chamfer Distance、F-score（几何）；法向图指标。
    *   **生成质量**：CLIP Score（文本对齐）；FD/KD（分布相似度，基于 DINOv2 特征）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A6000 GPU** 上运行。
*   **参数设置**：默认使用 15 步优化（Adam 优化器），在实际应用中，通常 6 步即可观察到良好的收敛效果。
*   **时长**：由于涉及迭代优化，其运行时间（约 145 秒）显著高于传统的采样引导方法（约 25-30 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 Toys4K 数据集的 3180 个样本上进行了大规模定量测试，并在多个数据集上展示了定性结果。
*   **消融实验**：专门验证了“频谱参数化”和“高斯分布损失”的必要性，证明了缺少任何一项都会导致性能大幅下降或优化失败。
*   **充分性评价**：实验设计较为全面，涵盖了外观、几何、文本对齐等多个维度，对比方法均为该领域的经典或前沿算法，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **初始噪声是关键**：初始噪声优化是 3D 修复的一个独立且有效的控制维度，且与传统的采样轨迹操作是“正交”的（即可以结合使用）。
*   **性能领先**：在保持上下文一致性（保留区域的重建）和几何完整性方面，InpaintSLat 显著优于所有基于轨迹修改的基准方法。
*   **稳定性**：通过频谱优化和分布正则化，解决了 3D 潜变量优化中常见的结构不稳定问题。

### 7. 优点
*   **免训练（Training-free）**：无需对预训练模型进行微调，具有很强的通用性。
*   **几何保真度高**：相比 2D 迁移过来的方法，能更好地处理 3D 空间的占用和表面连续性。
*   **灵活性**：支持通过不同的随机种子生成多样化的修复结果，同时保持保留区域不变。

### 8. 不足与局限
*   **计算开销**：由于需要进行多次反向传播优化，推理速度比直接采样慢数倍，不适合实时交互场景。
*   **先验依赖**：修复效果高度依赖于预训练 3D 扩散模型的质量。如果提示词要求的物体在预训练分布之外，优化可能无法收敛到理想结果。
*   **近似误差**：使用的一阶梯度近似虽然节省了内存，但在处理极度非线性的扩散路径时可能存在精度损失。

（完）
