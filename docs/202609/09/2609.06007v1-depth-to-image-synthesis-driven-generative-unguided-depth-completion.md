---
title: Depth-to-Image Synthesis-Driven Generative Unguided Depth Completion
title_zh: 基于深度图转图像合成驱动的生成式无引导深度补全
authors: "Jiayi Yuan, Na Zhao, De Wen Soh"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.06007v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用2D生成模型的生成式无引导深度补全
tldr: 该论文提出了一种名为GUDC的无引导深度补全范式，旨在解决传统无引导方法缺乏视觉线索导致精度受限的问题。通过利用ControlNet将稀疏深度图合成为伪RGB图像，将无引导任务转化为语义引导任务。核心创新包括多级稠密到稀疏的特征蒸馏策略以解决对齐问题，以及伪图像语义注意力融合模块以提取有效语义并抑制伪影。实验表明，该方法在KITTI和NYUv2数据集上显著提升了深度补全的精度与鲁棒性。
selection_source: fresh_fetch
motivation: 现有无引导深度补全方法因缺乏视觉线索导致精度受限，而引导式方法又过度依赖高质量RGB输入。
method: 利用ControlNet将稀疏深度图合成为伪图像，并通过特征蒸馏和语义注意力融合模块实现语义感知的深度补全。
result: 在KITTI和NYUv2数据集上的实验证明，该方法在精度和鲁棒性方面均优于现有的深度补全方法。
conclusion: 该研究成功通过生成式模型弥补了无引导深度补全的语义缺失，为深度补全任务提供了新的范式。
---

## 摘要
引导式深度补全方法高度依赖 RGB 图像的质量与对齐，而无引导方法往往因缺乏显式视觉线索而导致精度受限。在本文中，我们提出了一种名为“深度图转图像合成驱动的生成式无引导深度补全”（GUDC）的新型补全范式。该范式创新性地将先进的二维生成模型与无引导深度补全相结合，实现了无需真实 RGB 输入的语义感知深度推理。我们的核心思想是利用 ControlNet 强大的深度条件生成能力，直接从稀疏深度图中合成伪图像，从而有效地将原始的无引导设置转化为语义引导设置。为了解决深度稀疏性导致的图像与深度图潜在的不对齐问题，我们提出了一种针对 ControlNet 微调的多级稠密到稀疏表示蒸馏策略，其中稠密深度特征作为教师信号，为稀疏深度输入蒸馏出一致的结构表示。此外，在伪图像引导的补全过程中，我们提出了一个伪图像语义注意力融合模块，用于自适应地从伪图像中提取有价值的语义线索，同时抑制伪影（如纹理幻觉）。在 KITTI 和 NYUv2 数据集上的大量实验验证了我们的 GUDC 方法在精度和鲁棒性方面均优于现有方法。

## Abstract
Guided depth completion methods heavily depend on RGB quality and alignment, while unguided ones often suffer from limited precision due to the absence of explicit visual cues. In this paper, we present Depth-to-Image Synthesis-Driven Generative Unguided Depth Completion (GUDC), a new completion paradigm that innovatively bridges advanced 2D generative models with unguided depth completion, enabling semantics-aware depth inference without real RGB inputs. Our key idea is to exploit ControlNet's powerful depth-conditioned generation capability to synthesize pseudo-images directly from sparse depth, effectively converting the original unguided setting into a semantics-guided one. To address the potential image-depth misalignment caused by depth sparsity, we propose a multi-level dense-to-sparse representation distillation strategy for ControlNet fine-tuning, where dense-depth features act as teacher signals to distill consistent structural representations for sparse-depth inputs. Furthermore, during pseudo-image-guided completion, we propose a pseudo-image semantic attention fusion module to adaptively extract informative semantic cues from pseudo-images while suppressing artifacts (e.g., texture hallucinations). Extensive experiments on KITTI and NYUv2 validate that our GUDC achieves superior accuracy and robustness over existing methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Depth-to-Image Synthesis-Driven Generative Unguided Depth Completion (GUDC)》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：深度补全旨在从稀疏测量值（如 LiDAR）恢复稠密深度图。
*   **痛点**：
    *   **引导式方法**：高度依赖 RGB 图像，在夜间、低光照或传感器未对齐场景下表现不佳。
    *   **无引导方法**：仅依赖稀疏深度，缺乏视觉语义线索，导致在物体边界和复杂结构处精度受限。
*   **目标**：在无需真实 RGB 输入的前提下，通过生成式 AI 引入显式视觉引导，提升无引导深度补全的精度与鲁棒性。

### 2. 方法论
*   **核心思想**：将“无引导”任务转化为“语义引导”任务。利用预训练的 ControlNet 将稀疏深度图合成为几何一致的“伪图像”，作为辅助信息。
*   **关键技术**：
    *   **多级稠密到稀疏表示蒸馏**：针对稀疏输入导致生成质量不稳定的问题，利用稠密深度图训练教师模型，通过层级特征对齐（Distillation Loss）将结构先验迁移至处理稀疏输入的学生模型。
    *   **伪图像语义注意力融合（PSAF）模块**：为了抑制生成伪影（如纹理幻觉），利用 Stable Diffusion 的中间层特征作为“语义锚点”，通过多源亲和力核（结构、语义、几何相似度）自适应地融合伪图像特征与深度特征。
*   **流程**：稀疏深度 $\rightarrow$ ControlNet 生成伪图像 $\rightarrow$ PSAF 模块提取语义特征 $\rightarrow$ 深度补全网络预测稠密深度。

### 3. 实验设计
*   **数据集**：KITTI（室外自动驾驶）、NYUv2（室内场景）。
*   **基准对比**：对比了 17 种 SOTA 无引导方法（如 SI-CNN, PNCNN, IR_l2 等）和 4 种主流引导式方法（如 BP-Net, GuidedNet 等）。
*   **特殊场景**：构建了 Foggy-KITTI 和 Nighttime-KITTI 模拟恶劣环境，验证模型在视觉退化条件下的鲁棒性。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX 4090 和 TITAN RTX。
*   **训练细节**：ControlNet 微调仅需不到 10% 的训练数据（6K/5K 样本），深度补全网络微调使用不到 30% 的数据。文中未明确提及具体的总训练时长，但强调了该方法在数据效率上的优势。

### 5. 实验数量与充分性
*   **覆盖面**：包含定量对比、定性可视化、消融实验（蒸馏策略、样本量、PSAF 组件）、以及对不同补全骨干网络（BP-Net, MSG-CHN, DFU-Net）的兼容性测试。
*   **客观性**：实验设计全面，通过对比多种 SOTA 方法并引入恶劣天气场景，验证了方法的泛化能力，实验设置相对公平。

### 6. 主要结论与发现
*   **性能提升**：GUDC 在 KITTI 和 NYUv2 上均达到了 SOTA 水平，显著优于现有的无引导方法。
*   **鲁棒性**：在雾天和夜间场景下，由于不依赖真实的、受损的 RGB 输入，GUDC 的表现远超传统的引导式方法。
*   **通用性**：该框架具有“即插即用”特性，可无缝集成到多种现有的深度补全骨干网络中。

### 7. 优点
*   **范式创新**：巧妙利用生成式模型弥补了无引导任务的语义缺失。
*   **数据高效**：通过蒸馏策略，仅需少量数据即可实现高质量的几何一致性生成。
*   **抗干扰能力**：通过 PSAF 模块有效过滤了生成式模型常见的纹理幻觉，保证了深度补全的几何准确性。

### 8. 不足与局限
*   **计算开销**：虽然推理时无需训练，但引入了扩散模型作为生成器，相比纯 CNN 方法，推理阶段的计算复杂度和内存占用可能较高。
*   **生成依赖**：伪图像的质量上限受限于 ControlNet 的生成能力，若在极端稀疏或完全未见的场景下，生成质量可能下降。
*   **幻觉风险**：尽管有 PSAF 模块抑制，但生成式模型本质上仍存在产生虚假结构（幻觉）的风险，可能在极少数情况下导致深度预测偏差。

（完）
