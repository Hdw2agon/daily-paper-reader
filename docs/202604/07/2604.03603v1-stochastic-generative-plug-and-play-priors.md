---
title: Stochastic Generative Plug-and-Play Priors
title_zh: 随机生成式即插即用先验
authors: "Chicago Y. Park, Edward P. Chandler, Yuyang Hu, Michael T. McCann, Cristina Garcia-Cardona, Brendt Wohlberg, Ulugbek S. Kamilov"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03603v1"
tags: ["query:q4"]
score: 8.0
evidence: 用于逆问题的生成式扩散先验
tldr: 本文研究了如何将评分扩散模型（SBDM）作为先验集成到即插即用（PnP）框架中。作者提出了随机生成式PnP（SGPnP）框架，通过在算法中注入噪声，充分利用SBDM强大的生成能力。理论分析表明，这种噪声注入诱导了对高斯平滑目标的优化，并有助于跳出严格鞍点。实验证明，SGPnP在MRI重建和图像修复等任务中显著优于传统PnP，且性能可媲美基于扩散的求解器。
selection_source: fresh_fetch
motivation: 旨在解决如何系统地将预训练的评分扩散模型作为先验引入PnP框架，以克服传统方法在处理严重病态逆问题时的局限性。
method: 提出SGPnP框架，通过在优化迭代中注入受控噪声来利用SBDM先验，并从理论上证明其实现了高斯平滑目标的优化。
result: 在多线圈MRI重建和自然图像修复实验中，该方法一致优于传统PnP，并达到了与最先进的扩散求解器相当的水平。
conclusion: 该研究建立了PnP与评分模型之间的理论联系，证明了随机性在增强逆问题求解鲁棒性和优化性能方面的关键作用。
---

## 摘要
即插即用（PnP）方法通过将去噪器集成到优化算法中，被广泛用于解决成像逆问题。基于分数的扩散模型（SBDM）最近通过在广泛噪声水平上训练的去噪器展示了强大的生成性能。尽管它们都依赖于去噪器，但如何在不依赖反向扩散采样的情况下，系统地将 SBDM 作为先验应用于 PnP 框架中仍不明确。在本文中，我们建立了 PnP 的基于分数的解释，证明了在 PnP 算法中直接使用预训练 SBDM 的合理性。基于这一联系，我们引入了一种随机生成式 PnP（SGPnP）框架，该框架通过注入噪声来更好地利用具有表现力的生成式 SBDM 先验，从而提高在严重病态逆问题中的鲁棒性。我们提供了一种新的理论，表明这种噪声注入诱导了对高斯平滑目标的优化，并促进了从严格鞍点的逃逸。在具有挑战性的逆任务（如多线圈 MRI 重建和大掩码自然图像修复）上的实验表明，该方法相比传统 PnP 方法有持续的改进，并实现了与基于扩散的求解器相当的性能。

## Abstract
Plug-and-play (PnP) methods are widely used for solving imaging inverse problems by incorporating a denoiser into optimization algorithms. Score-based diffusion models (SBDMs) have recently demonstrated strong generative performance through a denoiser trained across a wide range of noise levels. Despite their shared reliance on denoisers, it remains unclear how to systematically use SBDMs as priors within the PnP framework without relying on reverse diffusion sampling. In this paper, we establish a score-based interpretation of PnP that justifies using pretrained SBDMs directly within PnP algorithms. Building on this connection, we introduce a stochastic generative PnP (SGPnP) framework that injects noise to better leverage the expressive generative SBDM priors, thereby improving robustness in severely ill-posed inverse problems. We provide a new theory showing that this noise injection induces optimization on a Gaussian-smoothed objective and promotes escape from strict saddle points. Experiments on challenging inverse tasks, such as multi-coil MRI reconstruction and large-mask natural image inpainting, demonstrate consistent improvement over conventional PnP methods and achieve performance competitive with diffusion-based solvers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**随机生成式即插即用先验（Stochastic Generative Plug-and-Play Priors, SGPnP）**的新框架，旨在将强大的评分扩散模型（SBDM）集成到传统的优化算法中，以解决复杂的图像逆问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：即插即用（PnP）方法通过在优化迭代中嵌入去噪器来引入先验信息，但在处理**严重病态（severely ill-posed）**的逆问题（如大面积遮挡修复）时表现不佳。
*   **核心挑战**：
    1.  **分布不匹配**：PnP 迭代过程中的中间图像包含结构化伪影，而非去噪器训练时预设的高斯噪声。
    2.  **先验强度不足**：传统 PnP 使用的去噪器通常针对低噪声恢复，缺乏在信息严重缺失时的生成能力。
*   **研究目标**：系统性地将预训练的评分扩散模型（SBDM）作为先验引入 PnP 框架，既保留优化的灵活性，又利用扩散模型的生成威力。

### 2. 方法论
*   **核心思想**：建立 PnP 与评分模型之间的数学联系，通过在去噪步骤前**主动注入受控噪声**，使中间迭代值回归到扩散模型的训练分布中，并利用随机性引导优化过程。
*   **关键技术细节**：
    *   **评分适配（Score Adaptation）**：利用 Tweedie 公式将 VE（方差爆炸）和 VP（方差保持）类型的扩散模型转化为 PnP 可用的去噪器模板。
    *   **噪声注入机制**：在每次迭代的去噪步骤前加入 $n \sim \mathcal{N}(0, I)$，其强度由 $\sigma_{inject}$ 控制。
    *   **解耦控制**：允许注入噪声水平（$\sigma_{inject}$）与去噪器条件噪声水平（$\sigma_{cond}$）解耦，以应对数据一致性步骤引入的额外伪影。
    *   **理论支撑**：证明了噪声注入等同于在**高斯平滑目标函数**上进行优化，并首次在 PnP 领域证明了该机制能帮助算法**逃逸严格鞍点（Strict Saddle Points）**。
*   **算法流程**：提供了 SGPnP-ADMM、SGPnP-PGM 和 SGPnP-RED 三种变体，通过交替执行数据一致性更新和随机去噪更新来重建图像。

### 3. 实验设计
*   **数据集**：
    *   **FFHQ**：人脸图像（256x256），用于测试修复（Inpainting）、超分辨率（SR）和去模糊。
    *   **fastMRI**：多线圈脑部 MRI 扫描，用于测试加速 MRI 重建（4x 加速）。
*   **对比方法（Baselines）**：
    *   **确定性 PnP**：DPIR、PnP-ADMM、PGM（使用 DRUNet 先验）。
    *   **随机 PnP**：SNORE（使用 DRUNet 先验）。
    *   **扩散求解器**：DPS（Diffusion Posterior Sampling）、DiffPIR。
*   **评价指标**：PSNR、SSIM（保真度）和 LPIPS（感知质量）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到使用了官方代码库和 `DeepInv` 库进行复现，超参数通过在 50 个样本的验证集上进行网格搜索确定。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在 100 张测试图像上进行评估，涵盖了自然图像和医学影像两个领域。
*   **充分性评价**：
    *   **消融实验**：详细对比了“噪声水平解耦”和“噪声覆盖范围”的影响，验证了 SBDM 广域噪声训练的优势。
    *   **框架通用性**：在 ADMM、PGM 和 RED 三种主流优化框架下均验证了有效性。
    *   **客观性**：对比了当前最先进的扩散模型求解器，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能卓越**：SGPnP 在所有任务中均优于传统确定性 PnP，尤其在感知指标（LPIPS）上提升显著。
*   **生成能力**：在极具挑战性的大面积方框遮挡修复中，传统 PnP 彻底失效，而 SGPnP 能够“幻觉”出合理的语义内容。
*   **理论验证**：实验结果证实了随机注入噪声确实能提高非凸能量景观下的搜索效率，避免陷入局部劣解。
*   **与扩散模型对比**：SGPnP 达到了与 DPS 等扩散求解器相当甚至更好的性能，且具有更明确的优化目标。

### 7. 优点
*   **理论深度**：为 PnP 引入随机性提供了坚实的数学解释（高斯平滑和鞍点逃逸）。
*   **模块化强**：可以直接插入任何预训练的评分扩散模型，无需重新训练。
*   **鲁棒性高**：通过噪声解耦策略，解决了 PnP 长期存在的中间迭代值分布偏移问题。

### 8. 不足与局限
*   **超参数敏感性**：噪声退火计划（Annealing Schedule）和步长需要精细调节，不同任务间的迁移可能需要重新搜索参数。
*   **计算开销**：虽然不依赖完整的反向扩散采样，但为了达到高质量重建，仍需要较多迭代次数（如 200 次），计算成本高于简单的确定性 PnP。
*   **应用限制**：理论分析主要基于加性高斯噪声假设，对于非高斯噪声或非线性极强的逆问题，其表现尚待进一步验证。

（完）
