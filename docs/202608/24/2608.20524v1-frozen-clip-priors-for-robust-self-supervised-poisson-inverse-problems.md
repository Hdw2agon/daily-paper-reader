---
title: Frozen CLIP Priors for Robust Self-Supervised Poisson Inverse Problems
title_zh: 针对鲁棒自监督泊松逆问题的冻结 CLIP 先验
authors: "Laura C. Diaz-Delgado, Emmanuel Martinez, Henry Arguello"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.20524v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于图像逆问题和修复的自监督学习
tldr: 针对光子受限环境下的泊松逆问题，本文提出一种自监督学习框架。该方法利用预训练冻结的 CLIP 视觉编码器作为鲁棒先验，通过 ADMM 启发式展开求解器解耦数据一致性与先验模块。结合 GR2R 重污染技术与等变成像正则化实现自监督训练，在去马赛克和去模糊任务中表现出极强的鲁棒性和接近监督学习的性能。
selection_source: fresh_fetch
motivation: 在光子受限且缺乏干净真值的场景下，现有的自监督成像方法难以应对泊松噪声及数据集偏移带来的鲁棒性挑战。
method: 提出一种基于 ADMM 展开的即插即用求解器，利用冻结的 CLIP 多尺度特征构建轻量化先验，并结合 GR2R 与等变正则化进行自监督训练。
result: 在泊松去马赛克和去模糊实验中，该方法在保持高质量重建的同时，展现了优异的跨域鲁棒性，性能逼近有监督训练水平。
conclusion: 证明了利用大规模预训练基础模型的冻结特征作为先验，是提升自监督逆问题求解鲁棒性和泛化能力的有效途径。
---

## 摘要
成像逆问题的自监督学习在光子受限的场景中日益重要，在这种场景下，获取干净的地面真值是不切实际的，且重建必须在数据集和采集偏移下保持稳定。在泊松噪声下，这一挑战被进一步放大，因为泊松噪声的信号依赖统计特性会与采样算子（如 CFA 马赛克）相互作用。与此同时，在互联网规模上训练的基础视觉编码器提供了畸变不变且与内容相关的表示，这些表示在不同领域间具有良好的泛化能力，这为构建无需昂贵微调即可实现分布外迁移的先验提供了一条有前景的途径。本文提出了一种受 ADMM 启发的泊松逆问题展开式即插即用求解器，它将闭式数据一致性更新与参数高效的先验解耦。该先验实现为一个在冻结的 CLIP RN50 密集多尺度特征上运行的轻量级解码器，以较少的可训练参数适配基础表示。在自监督方面，该方法通过虚拟采集将 GR2R 测量域再损坏（re-corruption）与等变成像正则化项相结合。在泊松 CFA 去马赛克和去模糊方面的实验表明，该方法具有竞争力的质量、在偏移下更高的鲁棒性，且自监督性能接近有监督训练。

## Abstract
Self-supervised learning for imaging inverse problems is increasingly important in photon-limited settings, where acquiring clean ground truth is impractical and reconstruction must remain stable under dataset and acquisition shifts. This challenge is amplified under Poisson noise, whose signal-dependent statistics interact with sampling operators (e.g., CFA mosaicing). Meanwhile, foundation vision encoders trained at web scale offer distortion-invariant, content-related representations that generalize well across domains, suggesting a promising route to build priors that transfer beyond the training distribution without expensive fine-tuning. This paper proposes an ADMM-inspired unrolled plug-and-play solver for Poisson inverse problems that decouples a closed-form data-consistency update from a parameter-efficient prior. The prior is implemented as a lightweight decoder operating on frozen CLIP RN50 dense multi-scale features, adapting foundation representations with less trainable parameters. For self-supervision, the method integrates GR2R measurement-domain re-corruption with an Equivariant Imaging regularizer via virtual acquisitions. Experiments on Poisson CFA demosaicing and deblurring show competitive quality, improved robustness under shifts, and self-supervised performance approaching supervised training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Frozen CLIP Priors for Robust Self-Supervised Poisson Inverse Problems》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在光子受限（如低光照、医学成像）的场景下，图像受泊松噪声干扰严重。传统的深度学习方法依赖大量“干净-噪声”图像对进行监督训练，但在实际应用中获取无噪声的真值（Ground Truth）往往不切实际。
*   **核心挑战**：
    1.  **泊松噪声的复杂性**：其信号依赖性使得噪声与采样算子（如彩色滤光阵列 CFA）产生复杂的相互作用。
    2.  **鲁棒性与泛化性**：现有的自监督方法在面对训练集之外的数据分布（OOD）或采集条件变化时，性能往往大幅下降。
*   **研究目的**：提出一种无需干净数据、且对分布偏移具有高度鲁棒性的自监督学习框架，用于解决泊松逆问题（如去马赛克、去模糊）。

### 2. 方法论
该方法结合了物理模型驱动的优化算法与大规模预训练模型的先验知识：
*   **ADMM 启发式展开求解器**：将逆问题分解为**数据一致性（DC）**和**先验（Prior）**两个模块。通过交替方向乘子法（ADMM）的思路，将求解过程展开为固定步数的神经网络层，使物理约束与学习到的先验解耦。
*   **冻结 CLIP 先验**：
    *   核心思想是利用在海量互联网数据上预训练的 **CLIP (RN50)** 视觉编码器。
    *   **冻结参数**：编码器部分保持冻结，不进行微调，以保留其强大的泛化能力和对畸变的不敏感性。
    *   **轻量级解码器**：仅训练一个轻量级的多尺度特征融合解码器，将 CLIP 的密集特征映射回图像空间。
*   **自监督训练策略**：
    *   **GR2R (Generalized Recorrupted-to-Recorrupted)**：针对泊松噪声设计的重污染技术。通过对已有的观测数据再次添加模拟噪声，构建“噪声-噪声”对进行训练，从而绕过对干净数据的依赖。
    *   **等变成像正则化 (Equivariant Imaging, EI)**：利用图像在几何变换（如旋转、翻转）下的对称性。要求模型对变换后的测量值重建出的结果，应等同于对原图重建后再进行相同变换的结果。

### 3. 实验设计
*   **实验场景**：
    1.  **泊松 CFA 去马赛克**：从带有泊松噪声的马赛克图像中恢复全彩图像。
    2.  **泊松去模糊**：处理受模糊核和泊松噪声双重影响的图像。
*   **数据集**：
    *   训练集：ImageNet 的子集。
    *   测试集：**CBSD68**、**Kodak**、**McMaster**（用于验证跨领域泛化性）。
*   **基准对比（Benchmarks）**：
    *   **有监督上限**：使用干净真值训练的相同架构。
    *   **传统方法**：BM3D、PnP-ADMM（基于传统先验）。
    *   **自监督/零样本方法**：DIP（深度图像先验）、DDRM 等。

### 4. 资源与算力
*   **硬件环境**：论文中未明确给出具体的 GPU 型号和训练总时长。
*   **参数效率**：由于 CLIP 编码器被冻结，可训练参数量显著减少（仅集中在轻量级解码器和展开步长的参数上），这暗示了该方法在显存占用和训练速度上具有一定优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种主流的逆问题任务。
    *   设置了多个噪声水平（通过标量 $\alpha$ 控制光子水平）进行测试。
    *   进行了**消融实验**，分别验证了 CLIP 先验、GR2R 技术和等变正则化对最终性能的贡献。
*   **充分性评价**：实验设计较为全面，特别是通过在不同于训练集的数据集（如 McMaster）上进行测试，客观地评估了模型的**分布外（OOD）鲁棒性**。

### 6. 主要结论与发现
*   **性能逼近监督学习**：在自监督条件下，该方法在 PSNR 和 SSIM 指标上接近有监督训练的水平。
*   **极强的鲁棒性**：相比于从头训练的先验，冻结的 CLIP 先验在面对未见过的图像分布时表现出极佳的稳定性，有效缓解了深度学习模型常见的过拟合问题。
*   **自监督组合的有效性**：GR2R 与等变正则化的结合，能够有效处理泊松噪声的非加性特征，提供了可靠的训练信号。

### 7. 优点
*   **无需真值**：完全基于噪声数据训练，极具实用价值。
*   **泛化能力强**：通过引入基础模型（Foundation Model）的冻结特征，解决了自监督方法在不同数据集间迁移难的问题。
*   **物理可解释性**：ADMM 展开架构保证了重建结果符合成像物理模型。

### 8. 不足与局限
*   **依赖预训练模型**：性能上限受限于 CLIP 编码器提取特征的能力，若预训练模型存在偏见，可能会影响重建质量。
*   **计算复杂度**：虽然参数量少，但 ADMM 展开迭代（Unrolling）在推理时仍需多次前向计算，可能不如单次前向传播的网络快。
*   **特定噪声假设**：虽然针对泊松噪声设计，但对于更复杂的混合噪声（如读出噪声+泊松噪声）的适应性尚未深入探讨。

（完）
