---
title: "VOID: Video Object and Interaction Deletion"
title_zh: VOID：视频物体与交互删除
authors: "Saman Motamed, William Harvey, Benjamin Klein, Luc Van Gool, Zhuoning Yuan, Ta-Ying Cheng"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02296v1"
tags: ["query:q1"]
score: 9.0
evidence: 视频对象移除与物理合理的图像补全
tldr: 针对现有视频物体移除方法在处理复杂物理交互（如碰撞）时表现不佳的问题，本文提出了VOID框架。该框架通过构建包含反事实物理场景的新数据集，并结合视觉语言模型识别受影响区域，引导视频扩散模型生成符合物理规律的补全结果。VOID在合成和真实数据上均展现出更优的物理一致性，为提升视频编辑模型的因果推理能力提供了新思路。
selection_source: fresh_fetch
motivation: 现有的视频物体移除方法无法处理物体移除后应发生的物理连锁反应（如碰撞消失后的轨迹变化），导致结果不符合物理常识。
method: 利用Kubric和HUMOTO构建反事实数据集，并结合视觉语言模型定位受影响区域，引导视频扩散模型生成物理一致的补全内容。
result: 实验证明，VOID在处理具有复杂物理交互的视频移除任务时，比现有方法能更好地保持场景动力学的一致性。
conclusion: 该框架通过引入高层因果推理，显著提升了视频编辑模型模拟真实物理世界的能力。
---

## 摘要
现有的视频物体移除方法擅长修复物体“背后”的内容，并修正阴影和反射等外观层面的伪影。然而，当被移除的物体具有更显著的交互作用（例如与其他物体发生碰撞）时，现有模型无法对其进行修正，从而产生不合理的结果。我们提出了 VOID，这是一个旨在这些复杂场景下进行符合物理规律的修复的视频物体移除框架。为了训练该模型，我们利用 Kubric 和 HUMOTO 生成了一个新的反事实物体移除配对数据集，其中移除一个物体需要改变后续的物理交互。在推理过程中，视觉语言模型会识别受被移除物体影响的场景区域。随后，这些区域被用于引导视频扩散模型，生成物理一致的反事实结果。在合成数据和真实数据上的实验表明，与先前的视频物体移除方法相比，我们的方法在移除物体后能更好地保持一致的场景动力学。我们希望这一框架能为如何通过高层因果推理使视频编辑模型成为更好的世界模拟器提供启示。

## Abstract
Existing video object removal methods excel at inpainting content "behind" the object and correcting appearance-level artifacts such as shadows and reflections. However, when the removed object has more significant interactions, such as collisions with other objects, current models fail to correct them and produce implausible results. We present VOID, a video object removal framework designed to perform physically-plausible inpainting in these complex scenarios. To train the model, we generate a new paired dataset of counterfactual object removals using Kubric and HUMOTO, where removing an object requires altering downstream physical interactions. During inference, a vision-language model identifies regions of the scene affected by the removed object. These regions are then used to guide a video diffusion model that generates physically consistent counterfactual outcomes. Experiments on both synthetic and real data show that our approach better preserves consistent scene dynamics after object removal compared to prior video object removal methods. We hope this framework sheds light on how to make video editing models better simulators of the world through high-level causal reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《VOID: Video Object and Interaction Deletion》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视频物体移除（Video Object Removal）方法主要关注“外观修复”（如填补背景、消除阴影和反射），但在处理**物理交互**时表现糟糕。
*   **研究动机**：在现实世界中，物体的存在会产生因果影响。例如，移除正在倒下的多米诺骨牌中间的几块，后续的牌理应停止倒下；移除支撑物，上方的物体理应下落。现有模型往往只是简单地“抹除”物体，导致剩下的物体产生违背物理常识的悬浮或运动轨迹。论文旨在实现一种能够进行“反事实推理”的视频编辑框架，即模拟“如果该物体从未存在，场景会如何演变”。

### 2. 方法论
VOID 框架结合了数据驱动的训练、精细的掩码控制和视觉语言模型（VLM）的推理能力：
*   **反事实数据集构建**：利用 **Kubric**（刚体动力学模拟）和 **HUMOTO**（人体动作捕捉）生成配对视频。每组包含“有物体”和“无物体且物理演变一致”的两个版本，为模型提供物理规律的监督信号。
*   **交互感知四色掩码（Quadmask）**：在传统三色掩码（物体、受影响区、背景）基础上增加第四种颜色（深灰色），专门标识“被移除物体”与“受影响区域”重叠的部分，以消除生成时的歧义。
*   **两阶段生成流程**：
    *   **第一阶段（轨迹合成）**：基于 CogVideoX（5B 参数）扩散模型，根据 Quadmask 生成初步的反事实视频，捕捉宏观运动（如物体下落）。
    *   **第二阶段（流变形噪声稳定）**：针对第一阶段可能出现的物体形变，利用光流场引导的“扭曲噪声”（Warped Noise）进行二次精修，保持物体的结构刚性。
*   **VLM 引导推理**：在推理阶段，利用 VLM（如 Gemini 3 Pro）分析场景，识别哪些区域会因物体移除而改变（例如预测球在失去手支撑后的下落路径），并自动生成 Quadmask。

### 3. 实验设计
*   **数据集/场景**：
    *   **真实世界数据集**：收集了 75 段涉及复杂交互的视频（如碰撞、支撑移除、关节运动）。
    *   **合成数据集**：包含 30 段来自 Kubric/HUMOTO 的动态交互视频和 10 段传统的阴影/反射移除视频。
*   **对比方法**：
    *   **视频补全类**：ProPainter, DiffuEraser, ROSE, MiniMax-Remover。
    *   **层分解类**：Generative Omnimatte。
    *   **通用视频编辑类**：Runway (Gen-3)。
*   **评估指标**：人类偏好投票、VLM 自动评分（基于物理、移除质量等 6 个维度）、PSNR、LPIPS、DreamSim、DINOv2、FVD。

### 4. 资源与算力
*   **模型规模**：基于 50 亿参数的 CogVideoX 扩散变压器（DiT）架构。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到模型是在 Generative Omnimatte 的权重基础上进行微调的，且使用了约 6400 对合成视频序列进行训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了 75 组真实场景测试和 40 组合成场景量化测试。
*   **消融实验**：针对训练数据组成（仅 Kubric、仅 HUMOTO、混合数据）以及掩码策略（传统掩码 vs. Quadmask）进行了对比，验证了各组件的必要性。
*   **客观性**：引入了“VLM-as-a-judge”（使用 Gemini、GPT-4o、Qwen 三种模型作为裁判）和人类双盲测试，评估维度全面，涵盖了物理一致性、时间连续性和图像锐度，实验设计较为客观公平。

### 6. 主要结论与发现
*   **物理一致性领先**：VOID 在处理物体移除后的连锁反应（如球下落、碰撞消失）方面显著优于现有所有模型。
*   **人类偏好显著**：在真实视频测试中，VOID 的人类获胜率达到 64.8%，远超第二名 Runway (18.4%)。
*   **强大的泛化能力**：尽管只在合成数据上训练，VOID 能泛化到未见过的效果，如让气球向上飘、让搅拌机停止转动等，证明其学习到了高层的因果逻辑而非简单的像素匹配。

### 7. 优点（亮点）
*   **因果推理集成**：首次将 VLM 的高层语义推理与视频扩散模型的底层像素生成深度结合，解决了视频编辑中的“物理常识”缺失问题。
*   **创新的掩码机制**：Quadmask 有效解决了物体与受影响区域重叠时的生成歧义。
*   **两阶段策略**：通过流引导噪声解决了扩散模型在生成剧烈运动时常见的物体“果冻效应”或形变问题。

### 8. 不足与局限
*   **算力与效率**：两阶段生成和 VLM 推理流程增加了计算开销，实时性可能受限。
*   **视频长度与分辨率**：目前仅支持数秒长度的视频生成，且在高分辨率细节上仍有提升空间。
*   **极端视角限制**：在相机视角极度特殊或距离物体过近的情况下，模型可能会出现泛化失效。
*   **数据依赖**：虽然泛化性强，但核心物理逻辑仍高度依赖于合成模拟器的质量。

（完）
