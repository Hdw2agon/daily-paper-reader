---
title: "Fill My Mirror: Geometry-Constrained Mirror Inpainting"
title_zh: Fill My Mirror：几何约束下的镜面修复
authors: "Ofek Basson, Shimon Vainer, Yacov Hel-Or, Ohad Fried"
date: 2026-09-03
pdf: "https://arxiv.org/pdf/2609.03740v1"
tags: ["query:q1"]
score: 10.0
evidence: 使用几何约束生成模型的镜面修复
tldr: 镜面反射依赖于场景几何与视角，传统生成模型难以仅凭外观先验合成几何一致的反射。本文提出一种无需训练的几何约束镜面修复方法，通过估计场景几何将可见图像内容投影到镜面区域，再结合双掩码扩散策略平衡几何约束与生成先验，减少投影伪影。实验表明该方法显著提升了镜面反射的几何一致性。
selection_source: fresh_fetch
motivation: 现有生成模型因忽略视角与几何约束，难以生成几何一致的镜面反射内容。
method: 通过估计场景几何将可见内容投影至镜面，并结合双掩码扩散策略平衡几何约束与生成先验。
result: 在 MirrorBench-V2 及真实图像上的评估表明，显式利用场景几何显著提升了反射一致性。
conclusion: 结合场景几何约束与扩散生成先验，为复杂真实场景的镜面图像补全提供了有效的免训练解决方案。
---

## 摘要
镜子在真实世界的图像中十分常见，但使用生成模型产生几何一致的反射仍具挑战性。与大多数物体不同，镜子的外观取决于场景几何和视角，仅靠学习到的外观先验难以进行合成。我们在镜面修复（mirror inpainting）设定下探讨该问题，即场景保持固定，仅生成镜面区域。我们的核心见解是，镜中的许多内容在几何上受到可见场景的约束，无需凭空生成。我们通过估计场景几何并将可见内容投影到镜面中，来恢复由几何决定的反射区域。随后，生成模型通过一种平衡几何约束与模型学习先验的双掩码扩散策略完成镜面区域的补全，减少了投影伪影并提升了反射一致性。该方法无需训练，可应用于复杂的真实世界场景。我们在 MirrorBench-V2（合成数据集）和真实图像上进行了评估。使用标准指标和几何感知指标的结果表明，显式利用场景几何能有效提升一致性。

## Abstract
Mirrors are common in real-world images, yet producing geometrically consistent reflections with generative models remains challenging. Unlike most objects, mirror appearance depends on scene geometry and viewpoint, making it hard to synthesize using learned appearance priors alone. We address this in the mirror inpainting setting, where the scene is fixed and only the mirror region is generated. Our key insight is that much mirror content is geometrically constrained by the visible scene and need not be hallucinated. We estimate scene geometry and project visible content into the mirror to recover reflection regions determined by geometry. A generative model then completes the mirror region via a two-mask diffusion strategy balancing geometric constraints with the model's learned priors, reducing projection artifacts and improving reflection consistency. The method is training-free and applicable to complex real-world scenes. We evaluate on MirrorBench-V2 (synthetic) and real images. Using standard and geometry-aware metrics, we show that explicitly using scene geometry improves consistency.

---

## 论文详细总结（自动生成）

以下是对论文 **《Fill My Mirror: Geometry-Constrained Mirror Inpainting》** 的详细结构化分析与总结：

---

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **研究背景**：镜面在真实世界图像中非常普遍，但在生成式图像处理领域，合成几何一致的镜面反射一直是一个难题。
* **核心问题**：与普通物体的二维外观不同，镜面的视觉呈现高度取决于三维场景几何（Scene Geometry）以及相机视角（Viewpoint）。传统的扩散生成模型（Diffusion Models）仅依赖从海量数据中学到的二维外观先验（Appearance Priors），无法理解真实的物理反射几何规律，导致生成的镜像常出现位置偏移、视角错乱或物理不合理等伪影。
* **研究动机**：在“镜面修复（Mirror Inpainting）”任务中（即背景固定、仅生成镜面区域），镜中的很多内容本质上是外部可见场景的物理反射，并不需要模型“凭空凭感觉合成”。论文旨在通过显式引入三维场景几何约束，解决镜面修复的几何一致性难题。

---

### 2. 论文提出的方法论
* **核心思想**：提出了一个**免训练（Training-free）**的几何约束镜面修复框架。通过估计场景的三维几何关系，将视场内可见的场景内容显式投影到镜面区域内，再结合生成模型的先验修补伪影，实现物理几何与生成先验的统一。
* **关键技术细节与流程**：
  1. **场景几何估计与镜面投影（Geometry Estimation & Projection）**：
     * 利用单目深度估计或三维几何重建技术估计可见场景的几何结构。
     * 根据镜面平面方程与光线反射几何原理，将可见场景内容反向投影（Project）至镜面区域内，直接恢复由几何决定的物理反射内容。
  2. **双掩码扩散策略（Two-Mask Diffusion Strategy）**：
     * 直接几何投影会因视野盲区、物体遮挡或深度误差产生空洞与边缘伪影。
     * 论文设计了一种双掩码（Two-Mask）控制机制：一个掩码标记几何投影高置信度的区域（强几何约束），另一个掩码标记未投影或需修补的遮挡/伪影区域（由扩散模型填补）。
     * 在扩散采样过程中平衡几何约束与生成先验，既保留精确的几何反射，又利用扩散模型消除投影伪影并补全缺失纹理。

---

### 3. 实验设计
* **数据集 / 测试场景**：
  * **合成数据集**： MirrorBench-V2（具备精确 Ground Truth 几何、深度与标准镜面反射图像的渲染数据集）。
  * **真实图像集**： 包含复杂光照、多样化视角及不规则镜面的真实世界物理场景。
* **Benchmark 与对比方法**：
  * 对比了主流的标准生成式修复基线（如传统的 Diffusion Inpainting / SOTA 图像修补模型）。
* **评估指标**：
  * **标准图像质量指标**：如 PSNR、SSIM、LPIPS、FID 等，用于评估生成图像的画质与相似度。
  * **几何感知指标（Geometry-Aware Metrics）**：专门用于量化评估镜中反射内容与外部真实场景在角度、位置及空间对应上的几何一致性。

---

### 4. 资源与算力
* **训练算力**：该方法为 **免训练（Training-free / Zero-shot）** 方案，无需使用 GPU 集群进行大规模微调或从头训练模型。
* **推理算力**：摘要及提供元数据中未明确给出具体的推理 GPU 型号（如 RTX 4090 或 A100）及单张图像的具体耗时。但由于涉及单目深度估计与多步扩散采样（Diffusion Sampling），推理阶段需要消费级或服务器级单卡 GPU 支持。

---

### 5. 实验数量与充分性
* **实验充分性**：
  * 在合成数据集（MirrorBench-V2）上进行了定量分析，提供了客观的 Ground Truth 几何一致性比对。
  * 在真实世界复杂场景下进行了定性与定量测试，验证了跨领域的泛化能力。
  * 包含了消融实验（Ablation Studies），验证了“几何投影”与“双掩码扩散策略”各自对最终结果的贡献。
* **客观性与公平性**：通过引入几何感知的新度量指标，弥补了传统 PSNR/SSIM 无法衡量物理一致性的缺陷，评估较为客观公平。

---

### 6. 论文的主要结论与发现
* 显式引入物理三维几何约束是解决生成模型在镜面反射场景下“几何失真”的有效途径。
* 纯生成式模型无法自主隐式推导出正确的物理反射几何；而通过“几何投影 + 双掩码扩散”的结合，能最大程度地兼顾几何精确度与画质真实感。
* 在免训练的前提下，该方法在 MirrorBench-V2 和真实图像上的几何一致性指标均显著优于现有通用 Inpainting Baseline。

---

### 7. 优点与亮点
* **物理驱动与生成先验的结合**：巧妙地将显式物理光路/几何投影与隐式扩散生成先验结合，避免了纯生成模型的“幻觉”现象。
* **免训练（Training-Free）**：作为 Zero-shot 方法，可直接插拔应用于现有的预训练扩散模型，极大地降低了部署与使用成本。
* **双掩码控制机制**：优雅地解决了三维投影带来的遮挡空洞、伪影与未观测区域的补全问题。

---

### 8. 不足与局限
* **对上游几何/深度估计的强依赖**：方法的上线取决于单目深度估计或镜面平面估计的准确度。若深度估计在无纹理、强反光或极暗场景下失效，投影结果将产生几何偏差。
* **视线盲区（Unseen Occlusions）一致性上限**：对于完全被掩挡、无法从可见场景中找到投影源的镜中区域，模型仍需依赖生成先验自由合成，此时几何一致性无法得到绝对保证。
* **推理耗时**：涉及深度估计、几何变换及多步扩散采样，难以做到实时（Real-time）生成。

---

（完）
