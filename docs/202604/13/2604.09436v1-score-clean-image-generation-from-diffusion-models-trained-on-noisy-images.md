---
title: "SCoRe: Clean Image Generation from Diffusion Models Trained on Noisy Images"
title_zh: SCoRe：从在噪声图像上训练的扩散模型中生成清晰图像
authors: "Yuta Matsuzaki, Seiichi Uchida, Shumpei Takezaki"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09436v1"
tags: ["query:q3"]
score: 8.0
evidence: 无需训练的光谱再生方法，从噪声图像生成清晰图像
tldr: 针对扩散模型在噪声数据集上训练会导致生成图像包含高频伪影的问题，本文提出了一种无需训练的生成时方法 SCoRe。该方法利用扩散模型的光谱偏差，通过频率截止抑制生成图像中的受损高频成分，并结合 SDEdit 进行重新生成。通过理论推导确定截止频率与 SDEdit 初始化步长的映射关系，SCoRe 在合成和真实噪声数据集上均显著提升了生成质量，且无需任何微调。
selection_source: fresh_fetch
motivation: 扩散模型在噪声数据上训练会继承并放大高频噪声伪影，导致生成图像质量严重下降。
method: 提出 SCoRe 方法，通过频率截止滤除高频噪声，并利用 SDEdit 根据保留的低频信息重新生成干净的高频细节。
result: 在 CIFAR-10 和 SIDD 数据集上的实验证明，SCoRe 在无需重训练的情况下，生成效果显著优于现有的后处理和噪声鲁棒基准方法。
conclusion: SCoRe 证明了利用扩散模型的光谱特性和生成能力，可以在推理阶段有效修复训练数据噪声带来的负面影响。
---

## 摘要
在噪声数据集上训练的扩散模型通常会复现高频训练伪影，从而显著降低生成质量。为了解决这一问题，我们提出了 SCoRe（光谱截止再生，Spectral Cutoff Regeneration），这是一种无需训练的生成时光谱再生方法，用于从在噪声图像上训练的扩散模型中生成清晰图像。利用扩散模型的光谱偏差（即从低频线索推断高频细节），SCoRe 通过频率截止抑制生成图像中受损的高频分量，并通过 SDEdit 对其进行再生。关键在于，我们基于径向平均功率谱密度（RAPSD）推导出了截止频率与 SDEdit 初始化时间步之间的理论映射，从而防止在再生过程中注入过量噪声。在合成（CIFAR-10）和真实世界（SIDD）噪声数据集上的实验表明，SCoRe 显著优于后处理和噪声鲁棒基准方法，在无需任何重新训练或微调的情况下，使恢复的样本更接近清晰图像分布。

## Abstract
Diffusion models trained on noisy datasets often reproduce high-frequency training artifacts, significantly degrading generation quality. To address this, we propose SCoRe (Spectral Cutoff Regeneration), a training-free, generation-time spectral regeneration method for clean image generation from diffusion models trained on noisy images. Leveraging the spectral bias of diffusion models, which infer high-frequency details from low-frequency cues, SCoRe suppresses corrupted high-frequency components of a generated image via a frequency cutoff and regenerates them via SDEdit. Crucially, we derive a theoretical mapping between the cutoff frequency and the SDEdit initialization timestep based on Radially Averaged Power Spectral Density (RAPSD), which prevents excessive noise injection during regeneration. Experiments on synthetic (CIFAR-10) and real-world (SIDD) noisy datasets demonstrate that SCoRe substantially outperforms post-processing and noise-robust baselines, restoring samples closer to clean image distributions without any retraining or fine-tuning.

---

## 论文详细总结（自动生成）

以下是对论文《SCoRe: Clean Image Generation from Diffusion Models Trained on Noisy Images》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散模型（Diffusion Models）在训练时高度依赖数据质量。当训练集中包含大量噪声图像（如传感器噪声、压缩伪影等）时，模型会学习并复现这些高频噪声分布，导致生成的图像质量严重下降。
*   **研究动机**：
    *   **现实挑战**：在大规模数据抓取中，获取纯净数据集成本极高，混合了噪声和清晰图像的“脏数据”是常态。
    *   **现有方案不足**：传统的后处理去噪（如双边滤波）会模糊纹理；基于学习的去噪方法（如Noise2Void）独立于生成模型，无法保证符合原始数据分布；而鲁棒训练方法（如Ambient Diffusion）则需要针对特定噪声重新训练，计算成本高。
    *   **核心思路**：利用扩散模型“先生成低频结构，再推断高频细节”的光谱偏差（Spectral Bias）特性，在推理阶段修复高频噪声。

### 2. 方法论：SCoRe (Spectral Cutoff Regeneration)
SCoRe 是一种**无需训练（Training-free）**的生成时增强方法，其核心步骤如下：
*   **光谱截止（Spectral Cutoff）**：
    1.  首先使用预训练的扩散模型生成一张图像 $x_0$（此时图像包含训练集带来的噪声）。
    2.  对 $x_0$ 进行频率分析，通过低pass滤波器抑制高于截止频率 $f_{cutoff}$ 的高频成分，得到只保留宏观结构的低频图像 $x_{cutoff}$。
*   **光谱再生（Regeneration via SDEdit）**：
    1.  利用 SDEdit 技术，将 $x_{cutoff}$ 重新扩散（加噪）到特定的时间步 $t'$。
    2.  从 $t'$ 开始执行逆向去噪过程。由于高频噪声已被滤除，模型会根据保留的清晰低频结构，利用其学习到的先验知识重新“预测”并填充干净的高频细节。
*   **关键技术细节（$t'$ 的理论推导）**：
    *   论文基于**径向平均功率谱密度（RAPSD）**推导了截止频率 $f_{cutoff}$ 与初始化时间步 $t'$ 之间的映射关系。
    *   通过计算扩散过程中信号与噪声的信噪比（SNR），确定 $t'$ 为信噪比等于 1 的临界点。这确保了注入的噪声量足以让模型再生高频，又不至于破坏已保留的低频结构。

### 3. 实验设计
*   **数据集**：
    *   **合成噪声数据集**：基于 CIFAR-10，人工添加高斯噪声、泊松噪声及混合噪声（90% 噪声图 + 10% 清晰图）。
    *   **真实噪声数据集**：使用 SIDD（智能手机图像去噪数据集），包含真实的传感器噪声。
*   **对比基准（Benchmarks）**：
    *   **标准扩散模型**（Standard Diffusion）。
    *   **后处理去噪**：Post-Bilateral（双边滤波）、Post-Noise2Void（深度学习去噪）。
    *   **生成式基准**：SDEdit（直接对噪声图加噪再生）、NR-GAN（噪声鲁棒 GAN）。
*   **评价指标**：使用 FID（Fréchet Inception Distance）衡量生成图像与清晰真实图像分布的接近程度。

### 4. 资源与算力
*   **训练细节**：
    *   CIFAR-10 实验中，扩散模型训练了约 800K 次迭代，Batch Size 为 128。
    *   SIDD 实验中，训练了约 520K 次迭代，Batch Size 为 64。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但强调了 SCoRe 方法本身是“生成时（Generation-time）”方法，**不需要额外的训练算力**，只需在推理时增加少量的加噪/去噪步骤。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种合成噪声类型和 1 种复杂的真实世界噪声。
    *   进行了**消融实验**，测试了不同截止频率（$f_{cutoff}$ = 0.2, 0.3, 0.4）对结果的影响。
    *   进行了**鲁棒性实验**，改变训练集中噪声图像的比例（从 10% 到 90%），验证模型在极端污染情况下的表现。
*   **充分性评价**：实验设计较为全面，特别是针对噪声比例的敏感性分析证明了该方法在极高噪声占比（90%）下依然有效，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **SCoRe 显著优于基准**：在所有噪声设置下，SCoRe 的 FID 分数均大幅领先于后处理方法和 NR-GAN。
*   **频率操作的必要性**：单纯使用 SDEdit（不进行频率截止）无法去除噪声，因为模型会倾向于还原输入中的高频伪影；而 SCoRe 通过物理切断高频，迫使模型进行“创造性修复”。
*   **鲁棒性强**：即使在训练数据中只有 10% 是清晰图像的情况下，SCoRe 也能生成接近 100% 清晰数据训练出的图像质量。

### 7. 优点
*   **无需训练**：直接应用于现有的预训练扩散模型，极具实用价值。
*   **理论支撑**：通过 RAPSD 建立了频率与扩散时间步的数学联系，而非纯经验调参。
*   **保真度高**：相比传统滤波，它能利用生成模型的先验知识“补全”纹理，而非简单抹除。

### 8. 不足与局限
*   **超参数依赖**：虽然实验证明其对 $f_{cutoff}$ 具有一定的鲁棒性，但在面对完全未知的噪声类型时，仍需手动或通过启发式方法选择最佳的截止频率。
*   **极高频损失**：频率分析显示，SCoRe 生成的图像在极高频段的功率略低于真实清晰图像，这可能导致某些极其细微的纹理（如微小毛孔）表现不足。
*   **推理开销**：由于需要先生成再再生，推理时间大约是标准采样的 1.2 到 1.5 倍（取决于 $t'$ 的大小）。

（完）
