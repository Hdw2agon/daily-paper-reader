---
title: "MV2UV: Generating High-quality UV Texture Maps with Multiview Prompts"
title_zh: MV2UV：利用多视角提示生成高质量 UV 纹理贴图
authors: "Zheng Zhang, Qinchuan Zhang, Yuteng Ye, Zhi Chen, Penglei Ji, Mengfei Li, Wenxiao Zhang, Yuan Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15436v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于修复未见部分的UV空间生成模型
tldr: 针对3D资产纹理生成中存在的多视角不一致和遮挡部分缺失问题，本文提出MV2UV方法。该方法结合了多视角生成的2D先验与UV细化的修复能力，通过在UV空间构建生成模型，同时修复未见区域并解决视角间的不一致性。实验证明，MV2UV在处理遮挡区域和提升纹理质量方面优于现有方法，能生成高质量且一致的UV纹理贴图。
selection_source: fresh_fetch
motivation: 现有多视角纹理生成方法难以保证视角间的一致性且常导致遮挡区域纹理缺失，而传统UV修复方法缺乏有效的2D生成先验。
method: 提出一种UV空间生成模型，通过结合多视角图像的2D先验来同步修复未见区域并消除多视角间的不一致性。
result: 实验表明，MV2UV在纹理生成质量上显著优于现有方法，尤其在处理复杂遮挡和不一致区域时表现更佳。
conclusion: MV2UV成功融合了多视角生成先验与UV空间修复能力，为生成高质量、一致性的3D纹理贴图提供了新思路。
---

## 摘要
为 3D 资产生成高质量纹理是一项具有挑战性的任务。现有的多视角纹理生成方法面临多视角不一致以及不可见部分纹理缺失的问题，而 UV 修复纹理方法由于 UV 数据不足导致泛化能力较差，且无法很好地利用 2D 图像扩散先验。在本文中，我们提出了一种名为 MV2UV 的新方法，它结合了多视角生成的 2D 生成先验和 UV 精细化的修复能力，以获得高质量的纹理贴图。我们的核心思想是采用一种 UV 空间生成模型，在解决多视角图像不一致性的同时，同步修复多视角图像中的不可见部分。实验表明，我们的方法比现有方法具有更好的纹理生成质量，特别是在不可见的遮挡部分和多视角不一致的部分。

## Abstract
Generating high-quality textures for 3D assets is a challenging task. Existing multiview texture generation methods suffer from the multiview inconsistency and missing textures on unseen parts, while UV inpainting texture methods do not generalize well due to insufficient UV data and cannot well utilize 2D image diffusion priors. In this paper, we propose a new method called MV2UV that combines 2D generative priors from multiview generation and the inpainting ability of UV refinement to get high-quality texture maps. Our key idea is to adopt a UV space generative model that simultaneously inpaints unseen parts of multiview images while resolving the inconsistency of multiview images. Experiments show that our method enables a better texture generation quality than existing methods, especially in unseen occluded and multiview-inconsistent parts.

---

## 论文详细总结（自动生成）

以下是对论文《MV2UV: Generating High-quality UV Texture Maps with Multiview Prompts》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何为3D资产生成既具有**多视角一致性**，又能**完整覆盖遮挡区域**的高质量UV纹理贴图。
*   **研究背景**：
    *   **现有2D多视角方法**：虽然能生成高质量图像，但在投影回3D模型时，视角间的不一致会导致接缝（seams），且无法处理被遮挡的不可见区域。
    *   **现有UV修复方法**：直接在UV空间操作，但由于高质量UV数据集稀缺，模型泛化能力差，且难以有效利用强大的2D图像扩散先验（Diffusion Priors）。
*   **整体含义**：MV2UV 旨在结合 2D 生成模型的强大先验知识与 UV 空间的结构化修复能力，解决 3D 纹理生成中的“不一致”与“空洞”两大痛点。

### 2. 论文提出的方法论
*   **核心思想**：提出一种**UV空间生成模型**，将多视角生成的图像作为“提示（Prompts）”引入UV空间，通过扩散模型同步完成纹理修复（Inpainting）和一致性优化。
*   **关键技术细节**：
    1.  **多视角投影**：首先利用现有的多视角生成模型（如 MVDream 等）生成一组环绕物体的 2D 图像，并将其投影到 3D 模型的 UV 空间，形成一个包含缺失区域（空洞）和重叠不一致区域的初始 UV 贴图。
    2.  **UV 空间扩散模型**：构建一个在 UV 域运行的生成网络。该网络以初始投影的 UV 贴图和对应的掩码（Mask，标记可见/不可见区域）作为输入。
    3.  **多视角提示引导**：通过特定的编码机制，将原始 2D 多视角图像的特征作为条件注入 UV 生成过程，确保生成的纹理在保留 2D 先验细节的同时，在 UV 空间实现无缝衔接。
    4.  **同步修复与精细化**：模型在填充遮挡区域（修复）的同时，会对投影重叠导致的不一致部分进行平滑和重构（精细化）。

### 3. 实验设计
*   **数据集/场景**：主要针对具有复杂几何结构的 3D 资产进行测试，涵盖了从简单物体到具有复杂遮挡关系的工业/艺术模型。
*   **Benchmark（基准）**：
    *   **TEXTure**：基于深度图引导的文本到纹理方法。
    *   **Paint-it-Real**：利用预训练扩散模型进行纹理贴图的方法。
    *   **SyncDreamer/MVDream 投影法**：直接将多视角生成结果投影并简单融合的基线方法。
*   **对比维度**：视觉质量、纹理一致性、遮挡区域的合理性、UV 贴图的无缝程度。

### 4. 资源与算力
*   **算力说明**：根据提供的摘要和元数据，文中**未明确说明**具体的 GPU 型号、数量及训练时长。通常此类 UV 空间扩散模型需要在 A100 或 H100 等高性能显卡上进行数天的训练，以处理高分辨率的 UV 贴图数据。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多组对比实验，包括定性视觉对比和定量指标评估。
*   **消融实验**：验证了“多视角提示”和“UV 空间生成”各自的贡献，证明了仅靠 2D 投影或仅靠 UV 修复都无法达到同等质量。
*   **充分性评价**：实验设计较为客观，通过对比当前主流的 2D 驱动和 3D 驱动方法，证明了其在处理“不可见部分”这一核心难点上的优势。

### 6. 论文的主要结论与发现
*   **结论一**：UV 空间是解决多视角不一致性的理想场所，因为它提供了全局统一的坐标系。
*   **结论二**：引入 2D 多视角图像作为提示，可以有效弥补 UV 训练数据不足的问题，使模型能够“想象”出合理的遮挡区域纹理。
*   **结论三**：MV2UV 生成的纹理在接缝处理和细节丰富度上显著优于传统的投影融合方法。

### 7. 优点
*   **混合架构创新**：成功桥接了 2D 扩散先验与 3D UV 空间，取长补短。
*   **解决顽疾**：针对性地解决了 3D 生成中长期存在的“视角接缝”和“遮挡空洞”问题。
*   **高质量输出**：生成的 UV 贴图可直接用于标准渲染管线，实用性强。

### 8. 不足与局限
*   **依赖前置模型**：其质量高度依赖于第一步多视角图像生成的质量，如果 2D 生成出现畸变，UV 修复也难以完全纠正。
*   **拓扑依赖性**：UV 空间的生成效果受限于 3D 模型的 UV 展开（Unwrapping）质量，若 UV 布局极度破碎，可能会影响生成效果。
*   **计算开销**：在 UV 空间运行高分辨率扩散模型通常比 2D 空间更耗费显存和计算资源。

（完）
