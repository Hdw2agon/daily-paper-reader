---
title: Progressive Photorealistic Simplification
title_zh: 渐进式写实图像简化
authors: "Adi Rosenthal, Dana Berman, Yedid Hoshen, Ariel Shamir"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10409v1"
tags: ["query:q1"]
score: 9.0
evidence: 以受控方式移除并修复元素
tldr: 本研究提出了一种渐进式写实图像简化框架，旨在减少场景复杂度的同时保持照片的真实感，弥补了传统非写实渲染在真实感方面的不足。该方法采用迭代的“选择-移除-验证”流程，利用视觉语言模型识别元素并结合生成式编辑进行修复。此外，通过知识蒸馏将该过程转化为图像转视频模型以提升效率。该技术可应用于内容感知去杂、语义层分解及交互式编辑，为视觉解释提供了新机制。
selection_source: fresh_fetch
motivation: 传统的图像简化技术多依赖非写实渲染，导致照片失去真实感，因此需要一种能在保持写实外观的同时降低视觉复杂度的方案。
method: 提出一种迭代的“选择-移除-验证”流水线，结合视觉语言模型进行语义识别，并利用生成式修复技术与学习验证器确保图像的连贯性与真实性。
result: 该方法能够生成高质量的简化轨迹，实现高效的内容去杂和语义层分解，并能通过蒸馏后的视频模型直接预测简化序列。
conclusion: 渐进式写实简化为引导视觉理解和图像编辑提供了一种实用机制，是传统抽象方法的有效补充。
---

## 摘要
现有的图像简化技术通常依赖于非写实渲染（NPR），将照片转化为风格化的素描、卡通或绘画。虽然这些方法在降低视觉复杂度方面非常有效，但通常会牺牲照片的真实感。在这项工作中，我们探索了一个互补的方向：在简化图像的同时保留其写实外观。我们引入了渐进式语义图像简化，这是一个通过以受控方式移除和修复元素来迭代降低场景复杂度的框架。在每一步中，生成的图像都保持为一张合理的自然照片。我们的方法结合了语义理解与生成式编辑，利用视觉语言模型（VLM）来识别并确定待移除元素的优先级，并使用一个经过训练的验证器来确保整个过程中的写实性和连贯性。这是通过一个迭代的“选择-移除-验证”（Select-Remove-Verify）流水线实现的，该流水线能够生成高质量的简化轨迹。为了提高效率，我们进一步将该过程蒸馏为一个图像到视频生成模型，该模型可以直接从单张输入图像预测连贯的简化序列。除了生成更整洁、更聚焦的构图外，我们的方法还支持内容感知去杂、语义层分解和交互式编辑等应用。更广泛地说，我们的工作表明，通过结构化内容移除进行简化可以作为在写实领域引导视觉解释的一种实用机制，是对传统抽象方法的补充。

## Abstract
Existing image simplification techniques often rely on Non-Photorealistic Rendering (NPR), transforming photographs into stylized sketches, cartoons, or paintings. While effective at reducing visual complexity, such approaches typically sacrifice photographic realism. In this work, we explore a complementary direction: simplifying images while preserving their photorealistic appearance. We introduce progressive semantic image simplification, a framework that iteratively reduces scene complexity by removing and inpainting elements in a controlled manner. At each step, the resulting image remains a plausible natural photograph. Our method combines semantic understanding with generative editing, leveraging Vision-Language Models (VLMs) to identify and prioritize elements for removal, and a learned verifier to ensure photorealism and coherence throughout the process. This is implemented via an iterative Select-Remove-Verify pipeline that produces high-quality simplification trajectories. To improve efficiency, we further distill this process into an image-to-video generation model that directly predicts coherent simplification sequences from a single input image. Beyond generating cleaner and more focused compositions, our approach enables applications such as content-aware decluttering, semantic layer decomposition, and interactive editing. More broadly, our work suggests that simplification through structured content removal can serve as a practical mechanism for guiding visual interpretation within the photorealistic domain, complementing traditional abstraction methods.

---

## 论文详细总结（自动生成）

### 渐进式写实图像简化 (Progressive Photorealistic Simplification) 论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的图像简化技术（如非写实渲染 NPR）通常将照片转化为素描、卡通或绘画，虽然降低了视觉复杂度，但彻底丧失了“照片级真实感”。
*   **核心问题**：如何在保留图像写实外观（Photorealism）的前提下，通过逐步移除场景中不重要的语义元素，实现图像的简化与抽象？
*   **整体含义**：论文提出了一种“减法式”简化框架，认为抽象不仅是风格的转变，更是对视觉信息的过滤。该研究将视觉感知理论（如格式塔心理学）与现代生成式 AI 结合，为内容去杂、语义层分解和交互式图像编辑提供了新工具。

#### 2. 论文提出的方法论
该方法分为两个主要阶段：

*   **阶段 I：基于搜索的轨迹生成 (Search-Based Trajectory Generation)**
    *   **语义层级 (Taxonomy)**：将物体分为四个重要性等级：干扰元素（Distracting）、次要元素（Secondary）、主体元素（Primary-Subject）、背景元素（Background）。
    *   **迭代循环 (Select-Remove-Verify)**：
        1.  **选择 (Select)**：利用视觉语言模型 (VLM, 如 Gemini 3 Pro) 作为规划器，根据语义层级识别当前图像中最不重要的物体。
        2.  **移除 (Remove)**：使用生成式编辑模型进行修复（Inpainting），并配合对齐（Alignment）和混合（Blending）技术，确保修改仅局限于局部，防止全局画质退化。
        3.  **验证 (Verify)**：引入一个基于 DINOv2 的学习型验证器，自动过滤掉产生伪影或不真实修复结果的步骤。
*   **阶段 II：知识蒸馏 (Distillation via Video Generation)**
    *   **核心思想**：由于阶段 I 的迭代搜索极其耗时，研究者将生成的高质量简化轨迹作为训练数据，通过 LoRA 微调图像转视频 (I2V) 模型（如 Wan 2.2）。
    *   **实现**：将简化过程建模为“减法式定格动画”，使模型能够直接从单张输入图像预测出连贯的简化序列。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 CADB 数据集作为起点生成轨迹，最终筛选出 4,500 条高质量简化序列用于蒸馏。
    *   **测试集**：从 Open Images 中选取 93 张复杂场景图像，由 3 名专业摄影师进行精细的语义层级和分割掩码标注（共 530 个物体）。
*   **Benchmark 与对比方法**：
    *   **对比对象**：领先的商用/开源 I2V 模型，包括 **Veo 3.1** 和 **Wan 2.2 (Base)**。
    *   **评估指标**：**配对顺序准确度 (Pairwise Order Accuracy)**，基于改进的 Kendall’s $\tau_b$ 系数，衡量模型移除物体的顺序是否符合人类定义的语义层级。

#### 4. 资源与算力
*   **验证器训练**：使用单张 **NVIDIA A100 GPU**，微调 16 个 epoch。
*   **蒸馏模型训练**：微调 Wan 2.2 (14B 参数模型)，使用 **4 张 NVIDIA A100 (80GB) GPU**，每个专家模型的训练时长约为 **4-7 天**。
*   **推理效率**：
    *   阶段 I（搜索式）：每张图平均耗时 **2 小时以上**（复杂场景超 4 小时）。
    *   阶段 II（蒸馏后）：相同任务仅需约 **15 分钟**。

#### 5. 实验数量与充分性
*   **实验规模**：测试集包含 93 张图像和 530 个验证物体，虽然样本量在纯视觉任务中不算巨大，但考虑到每张图都需要精细的人工语义排序标注和生成式质量评估，其规模足以支撑结论。
*   **消融实验**：论文对轨迹过滤（Unfiltered vs HQ-Only）和验证器（Classifier）的作用进行了消融分析，证明了高质量数据筛选对蒸馏效果的关键性。
*   **客观性**：通过多名人类评分者的交叉验证（Kendall’s $\tau_b$ 在 0.63-0.75 之间）确保了语义层级定义的客观性。

#### 6. 论文的主要结论与发现
*   **语义一致性**：该方法在遵循语义层级移除物体方面的准确度（0.728）远高于通用视频模型（Veo 为 0.402，Wan 为 0.214）。
*   **写实性保持**：通过局部混合和验证器机制，成功解决了长序列编辑中的画质漂移问题。
*   **蒸馏有效性**：证明了复杂的搜索逻辑可以被蒸馏到扩散模型中，从而在保持逻辑一致性的同时大幅提升推理速度。
*   **应用潜力**：在自动去杂（Decluttering）和语义图层提取方面表现出极高的实用价值。

#### 7. 优点
*   **新颖视角**：打破了“简化即风格化”的固有思维，实现了写实域内的渐进式抽象。
*   **结构化框架**：提出的“选择-移除-验证”闭环系统有效解决了生成式编辑的不确定性。
*   **交互性**：支持用户在简化树（Simplification Tree）上进行分支选择，兼顾了自动化与人工审美主观性。

#### 8. 不足与局限
*   **推理延迟**：即便经过蒸馏，15 分钟的推理时间对于实时交互应用仍然偏慢。
*   **基础模型依赖**：简化的质量高度依赖于底层 Inpainting 模型的性能，在处理极端遮挡或复杂纹理修复时仍可能失败。
*   **主观性偏差**：尽管有层级划分，但同一层级内物体的移除顺序仍具有较强的主观性，模型可能无法完全符合所有用户的审美偏好。
*   **算力门槛**：训练 14B 规模的视频模型需要较高的算力资源，不利于小型研究团队复现。

（完）
