---
title: Leveraging Multimodal Large Language Models for All-in-One Image Restoration via a Mixture of Frequency Experts
title_zh: 利用多模态大语言模型通过频率专家混合实现全能图像修复
authors: "Eunho Lee, Youngbae Hwang, Rei Kawakami"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11444v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于多种退化的多模态大模型引导图像修复框架
tldr: 本研究针对全能图像修复中退化类型建模不连续的问题，提出了一种基于多模态大语言模型（MLLM）引导的修复框架。该框架利用MLLM提取的特征作为低级修复的引导，通过MLLM引导融合模块（MGFB）增强退化感知能力，并引入频率专家混合（MoFE）模块及关系对齐损失来优化专家路由。实验表明，该方法在多个基准测试中表现优异，特别是在CDD11数据集上刷新了SOTA记录，提升达1.35 dB。
selection_source: fresh_fetch
motivation: 现有全能图像修复方法通常将退化视为离散类别，难以有效建模复合退化中存在的连续关系结构。
method: 提出一种MLLM引导的框架，通过融合块注入多模态特征，并利用频率专家混合模块（MoFE）及关系对齐路由进行自适应修复。
result: 该方法在多个基准测试中达到SOTA水平，在CDD11数据集上相比现有最优方法提升了1.35 dB。
conclusion: 研究证明了多模态大语言模型提供的上下文线索能显著增强模型对复杂退化的感知与修复能力。
---

## 摘要
全能图像修复旨在利用统一框架，从受多种未知退化影响的输入中恢复出清晰图像。近期的方法通过识别退化特征来引导修复过程，展现出了强大的性能。然而，其中许多方法将退化视为离散类别，这限制了它们建模复合退化中出现的连续关系结构的能力。为解决这一问题，我们提出了一种多模态大语言模型（MLLM）引导的图像修复框架，利用多模态嵌入作为底层修复的引导。具体而言，通过 MLLM 引导的融合模块（MGFB）将源自 MLLM 的特征注入到编码器-解码器架构中，以增强退化感知表示。此外，我们引入了一个频率专家混合（MoFE）模块，利用 MLLM 引导的上下文线索自适应地组合频率专家。为了进一步改进专家路由，我们设计了一个带有关系对齐损失的 MLLM 引导路由器，该损失鼓励路由模式与退化输入的嵌入空间关系保持一致。在多个基准测试上的广泛实验表明，所提方法在多种修复设置下均取得了优异性能，并在具有挑战性的 CDD11 数据集上达到了新的 SOTA 水平，比以往方法提升了高达 1.35 dB。

## Abstract
All-in-one image restoration seeks to recover clean images from inputs affected by diverse and unknown degradations using a unified framework. Recent methods have shown strong performance by identifying degradation characteristics to guide the restoration process. However, many of them treat degradations as discrete categories, which limits their ability to model the continuous relational structure that arises in composite degradations. To address this issue, we propose a multimodal large language model (MLLM)-guided image restoration framework that exploits multimodal embeddings as guidance for low-level restoration. Specifically, MLLM-derived features are injected into an encoder-decoder architecture through an MLLM-guided fusion block (MGFB) to enhance degradation-aware representations. In addition, we incorporate a mixture-of-frequency-experts (MoFE) module that adaptively combines frequency experts using MLLM-guided contextual cues. To further improve expert routing, we design an MLLM-guided router with a relational alignment loss that encourages routing patterns consistent with the embedding-space relationships of degraded inputs. Extensive experiments on multiple benchmarks show that the proposed method achieves strong performance across diverse restoration settings and establishes a new state of the art on the challenging CDD11 dataset, outperforming previous methods by up to 1.35 dB.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MMFE-IR** 的框架，旨在利用多模态大语言模型（MLLM）的丰富语义知识来解决全能图像修复（All-in-One Image Restoration）中的复杂退化问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的全能图像修复方法通常将不同的退化（如噪声、雨滴、雾气）视为**离散的类别**。
*   **研究动机**：在现实场景中，退化往往是复合且连续的（例如“雾+雨”的混合，且某一种可能占主导）。传统的基于提示（Prompt）或对比学习的方法难以捕捉这种连续的关系结构，导致在处理复合退化时效果不佳。
*   **整体含义**：利用预训练 MLLM（如 Qwen2.5-VL）所具备的连续语义空间，引导模型识别并修复复杂的、非离散的图像退化。

### 2. 方法论
核心思想是将 MLLM 的内部视觉知识和高层文本语义同时注入到修复网络中。
*   **特征提取**：使用 Qwen2.5-VL 提取三种嵌入：$E_{Image}$（视觉特征）、$E_{Joint}$（视文融合特征）和 $E_{Answer}$（高层文本语义）。
*   **MLLM 引导融合模块 (MGFB)**：
    *   在编码器中注入 $E_{Image}$ 以增强结构特征提取。
    *   在解码器中注入 $E_{Joint}$ 以辅助退化感知的细节重建。
    *   采用交叉注意力机制（Cross-Attention）实现特征融合。
*   **频率专家混合模块 (MoFE)**：
    *   **频率分解**：利用离散小波变换（DWT）将图像分解为不同频段，专注于高频分量的修复。
    *   **专家系统**：设计多个轻量级卷积专家，每个专家负责特定的频率特性。
    *   **动态路由**：利用 $E_{Answer}$ 预测专家权重，实现针对不同退化类型的自适应组合。
*   **关系对齐损失 ($L_{MGL}$)**：
    *   为了让路由器学习到 MLLM 嵌入空间中的连续关系，引入该损失函数，强制路由器的输出分布与 MLLM 的语义相似度矩阵保持一致。

### 3. 实验设计
*   **数据集**：
    *   **复合退化**：CDD11 数据集（包含 11 种复合退化类型）。
    *   **全能修复（5项/3项任务）**：BSD400/68（去噪）、Rain100L（去雨）、SOTS（去雾）、GoPro（去模糊）、LOL-v1（低光增强）。
*   **Benchmark 与对比方法**：
    *   对比了 AirNet, PromptIR, OneRestore, MoCEIR, TEAFormer, DFPIR 等近年来的 SOTA 方法。
    *   评价指标采用 PSNR 和 SSIM。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX A6000 GPU**。
*   **训练时长**：
    *   全能修复设置：150 个 Epoch。
    *   复合退化设置：250 个 Epoch。
*   **策略**：采用了渐进式多尺度训练（Progressive Multi-scale Training），分辨率从 $128^2$ 逐渐增加到 $224^2$。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主要基准（复合退化、五任务全能、三任务全能）上进行了详尽测试。
*   **消融实验**：针对 MGFB 的输入类型、MoFE 模块的作用、专家数量（1 到 16 个）、空间域 vs 频率域设计、以及不同规模的引导模型（Qwen2.5-VL 3B vs FastVLM 0.5B）进行了多组对比。
*   **充分性评价**：实验设计非常充分且客观。通过可视化退化特征分布图和专家差异图，直观地证明了 MLLM 引导和频率专家的有效性。

### 6. 主要结论与发现
*   **性能突破**：在 CDD11 复合退化数据集上，MMFE-IR 比之前的最优方法提升了 **1.35 dB**。
*   **连续性建模**：证明了 MLLM 的嵌入空间能够很好地表示复合退化的过渡状态，解决了离散分类导致的修复伪影问题。
*   **频率域优势**：在频率域进行专家混合比在空间域更有效，能更好地分离和处理异构退化特征。

### 7. 优点
*   **创新性**：首次深入挖掘了 MLLM 的内部视觉特征（而非仅仅是文本输出）来引导底层视觉任务。
*   **鲁棒性**：在处理极其复杂的“低光+雾+雨”等三重复合退化时，表现出极强的恢复能力。
*   **灵活性**：证明了即使使用较小的 MLLM（如 0.5B 参数），也能获得显著的性能提升，且推理延迟增加较小。

### 8. 不足与局限
*   **推理开销**：虽然使用了轻量级 MLLM，但相比于纯 CNN 或 Transformer 模型，引入 MLLM 仍会带来额外的显存占用和推理延迟（文中提到延迟增加了约 7%~67%）。
*   **数据依赖**：在某些特定任务（如去雨）上，如果训练数据量不足，性能提升不够显著，需要额外的数据集（如 Rain13K）来弥补。
*   **应用限制**：目前主要针对静态图像修复，尚未讨论在视频修复等时序任务中的扩展性。

（完）
