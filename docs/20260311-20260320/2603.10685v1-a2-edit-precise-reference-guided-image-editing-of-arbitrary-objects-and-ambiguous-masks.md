---
title: "A$^2$-Edit: Precise Reference-Guided Image Editing of Arbitrary Objects and Ambiguous Masks"
title_zh: A$^2$-Edit：针对任意物体和模糊掩码的精确参考引导图像编辑
authors: "Huayu Zheng, Guangzhao Li, Baixuan Zhao, Siqi Luo, Hantao Jiang, Guangtao Zhai, Xiaohong Liu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10685v1"
tags: ["query:q1"]
score: 10.0
evidence: 适用于任意物体类别的统一图像补全框架
tldr: A^2-Edit是一个统一的图像编辑框架，支持使用粗略掩码对任意类别物体进行参考引导的替换。为解决现有数据集类别单一的问题，研究者构建了包含50万对图像的UniEdit-500K数据集，并提出混合Transformer（MoT）模块进行多类别差异化建模，结合掩码退火训练策略（MATS）降低了模型对精确掩码的依赖，显著提升了编辑的灵活性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有图像编辑方法受限于数据集类别覆盖不足，且往往需要极其精确的掩码才能实现理想的物体替换效果。
method: 引入了大规模多类别数据集UniEdit-500K，并采用混合Transformer架构与掩码退火训练策略来增强跨类别泛化能力和对模糊掩码的鲁棒性。
result: 在VITON-HD和AnyInsertion等基准测试中，A^2-Edit在各项指标上均优于现有方法，展现了卓越的编辑精度。
conclusion: 该研究为任意物体的精确图像编辑提供了一种高效且通用的解决方案，有效解决了类别局限与掩码精度依赖问题。
---

## 摘要
我们提出了 A$^2$-Edit，这是一个针对任意物体类别的统一修复框架，允许用户仅使用粗略掩码即可将任何目标区域替换为参考物体。为了解决现有数据集中严重的同质化和有限的类别覆盖问题，我们构建了一个大规模、多类别的数据集 UniEdit-500K，其中包括 8 个大类、209 个细粒度子类，共计 500,104 对图像。如此丰富的类别多样性给模型带来了新的挑战，要求其能够自动学习跨类别的语义关系和区别。为此，我们引入了 Mixture of Transformer 模块，通过动态专家选择对各种物体类别进行差异化建模，并通过专家间的协作进一步增强跨类别的语义迁移和泛化能力。此外，我们提出了一种掩码退火训练策略（MATS），在训练过程中逐步放宽掩码精度，从而减少模型对精确掩码的依赖，并提高在各种编辑任务中的鲁棒性。在 VITON-HD 和 AnyInsertion 等基准测试上的大量实验表明，A$^2$-Edit 在所有指标上均持续优于现有方法，为任意物体编辑提供了一种高效的新解决方案。

## Abstract
We propose \textbf{A$^2$-Edit}, a unified inpainting framework for arbitrary object categories, which allows users to replace any target region with a reference object using only a coarse mask. To address the issues of severe homogenization and limited category coverage in existing datasets, we construct a large-scale, multi-category dataset \textbf{UniEdit-500K}, which includes 8 major categories, 209 fine-grained subcategories, and a total of 500,104 image pairs. Such rich category diversity poses new challenges for the model, requiring it to automatically learn semantic relationships and distinctions across categories. To this end, we introduce the \textbf{Mixture of Transformer} module, which performs differentiated modeling of various object categories through dynamic expert selection, and further enhances cross-category semantic transfer and generalization through collaboration among experts. In addition, we propose a \textbf{Mask Annealing Training Strategy} (MATS) that progressively relaxes mask precision during training, reducing the model's reliance on accurate masks and improving robustness across diverse editing tasks. Extensive experiments on benchmarks such as VITON-HD and AnyInsertion demonstrate that A$^2$-Edit consistently outperforms existing approaches across all metrics, providing a new and efficient solution for arbitrary object editing.

---

## 论文详细总结（自动生成）

这是一份关于论文《A$^2$-Edit: Precise Reference-Guided Image Editing of Arbitrary Objects and Ambiguous Masks》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的参考引导图像编辑（如虚拟试衣、物体替换）面临两大瓶颈：
    1.  **类别局限性**：现有数据集（如 VITON-HD）多集中于特定类别（如服装），导致模型在处理任意物体时泛化能力差。
    2.  **掩码依赖性**：大多数方法要求极其精确的像素级掩码（Mask），而在实际应用中，用户往往只能提供粗略的涂鸦或边界，这会导致编辑结果出现伪影或边缘不自然。
*   **研究动机**：开发一个能够处理**任意物体类别**且对**模糊/粗略掩码**具有鲁棒性的统一图像编辑框架。

### 2. 方法论
论文提出了 A$^2$-Edit 框架，其核心技术包括：
*   **UniEdit-500K 大规模数据集**：构建了一个包含 8 个大类、209 个细粒度子类、共 500,104 对图像的数据集。通过自动化的流水线生成“参考图-目标图-掩码”三元组，解决了数据同质化问题。
*   **混合 Transformer 模块 (Mixture of Transformer, MoT)**：
    *   引入了类似 MoE（混合专家）的架构，通过动态路由器（Router）选择不同的专家模块来处理不同类别的语义特征。
    *   该设计允许模型对不同物体进行差异化建模，同时通过专家间的协作实现跨类别的语义迁移。
*   **掩码退火训练策略 (Mask Annealing Training Strategy, MATS)**：
    *   在训练初期使用精确掩码以帮助模型学习物体的准确重建。
    *   随着训练进行，逐步引入膨胀、腐蚀及随机形状的噪声来“退火”掩码的精度。
    *   这种策略迫使模型学习利用参考图的语义信息而非仅仅依赖掩码边界，从而提高对模糊掩码的容错率。

### 3. 实验设计
*   **数据集与场景**：
    *   **VITON-HD**：用于评估在特定领域（虚拟试衣）的性能。
    *   **AnyInsertion**：用于评估在通用物体插入任务中的表现。
    *   **UniEdit-500K 测试集**：验证跨类别的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了当前主流的图像编辑与修复模型（如 Paint-by-Example, AnyDoor 等，虽然摘要未详列所有名称，但明确指出在各项指标上均有超越）。
*   **评估指标**：通常包括图像质量（FID）、语义对齐度（CLIP Score）以及掩码区域的重建精度。

### 4. 资源与算力
*   **算力说明**：论文摘要及提供的文本片段中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **数据规模**：明确提到了处理了 50 万对图像，这暗示了该模型训练需要相当规模的计算资源（推测为 A100 或 H100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比实验。
    *   包含了针对 MoT 模块和 MATS 策略的**消融实验**，验证了各组件的有效性。
*   **充分性评价**：实验设计较为充分。通过在特定领域（试衣）和通用领域（AnyInsertion）双重验证，证明了模型的通用性。引入 50 万量级的数据集显著增强了实验结果的说服力。

### 6. 主要结论与发现
*   **统一性**：A$^2$-Edit 成功实现了单一模型对任意物体的精确编辑，无需针对特定类别微调。
*   **鲁棒性**：通过 MATS 策略，模型在掩码非常粗糙的情况下依然能保持极高的编辑精度，解决了实际应用中的痛点。
*   **性能领先**：在多个基准测试中，A$^2$-Edit 在视觉质量和语义一致性上均优于现有最先进（SOTA）方法。

### 7. 优点
*   **数据集贡献**：UniEdit-500K 为社区提供了一个极具价值的多类别、大规模基准。
*   **架构创新**：将 MoE 思想引入 Transformer 图像编辑，有效解决了多类别建模中的干扰问题。
*   **实用性强**：降低了用户提供掩码的门槛，使工具更易于被非专业用户使用。

### 8. 不足与局限
*   **计算开销**：MoT 架构虽然提升了性能，但相比单一 Transformer 可能会增加推理时的显存占用或计算延迟。
*   **极端情况处理**：对于背景极其复杂或参考物体与背景存在严重语义冲突（如在水里插入一团火）的情况，论文未详细讨论其物理合理性的表现。
*   **数据偏差**：尽管有 50 万对数据，但自动生成的训练对可能存在潜在的伪影，可能影响模型在极高分辨率下的精细纹理表现。

（完）
