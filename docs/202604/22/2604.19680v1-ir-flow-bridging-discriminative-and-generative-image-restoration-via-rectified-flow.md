---
title: "IR-Flow: Bridging Discriminative and Generative Image Restoration via Rectified Flow"
title_zh: IR-Flow：通过修正流桥接判别式与生成式图像恢复
authors: "Zihao Fan, Xin Lu, Jie Xiao, Dong Li, Jie Huang, Xueyang Fu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19680v1"
tags: ["query:q4"]
score: 9.0
evidence: 通过修正流实现的生成式图像修复
tldr: 针对图像恢复中判别式方法细节缺失和生成式方法采样效率低下的问题，本文提出IR-Flow框架。该方法基于修正流（Rectified Flow）构建多级数据分布流，通过累积速度场学习退化图像到清晰图像的线性传输轨迹，并引入多步一致性约束。IR-Flow在去雨、去噪等任务中仅需少量采样步数即可实现优异的失真与感知平衡，兼具判别式的高效和生成式的高质量。
selection_source: fresh_fetch
motivation: 旨在解决判别式方法因期望学习导致细节丢失，以及生成式方法采样慢且存在噪声残差耦合的局限性。
method: 通过修正流构建多级分布流和累积速度场，并结合多步一致性约束，在退化与清晰图像域间建立线性传输流。
result: 在去雨、去噪和去雨滴等多个任务中，该方法仅需少量采样步数即可取得极具竞争力的定量指标和视觉感知效果。
conclusion: IR-Flow成功桥接了判别式与生成式范式，提供了一个在效率、保真度和感知质量之间取得卓越平衡的图像恢复框架。
---

## 摘要
在图像恢复中，单步判别式映射通常通过期望学习进行，往往缺乏精细细节；而生成式范式则面临多步采样效率低下以及噪声-残差耦合的问题。为了解决这一困境，我们提出了 IR-Flow，这是一种基于修正流（Rectified Flow）的新型图像恢复方法，作为一个统一框架桥接了判别式与生成式范式之间的鸿沟。具体而言，我们首先构建了多级数据分布流，扩展了模型从不同退化水平中学习并适应的能力。随后，提出了累积速度场（cumulative velocity fields）来学习跨越不同退化水平的传输轨迹，引导中间状态趋向清晰目标；同时，引入了多步一致性约束以增强轨迹连贯性并提升少步恢复性能。我们表明，在退化图像域和清晰图像域之间直接建立线性传输流，不仅能够实现快速推理，还能提高对分布外（out-of-distribution）退化的适应性。在去雨、去噪和去雨滴任务上的广泛评估表明，IR-Flow 仅需少量采样步骤即可获得具有竞争力的定量结果，提供了一个在保持优异的失真-感知平衡（distortion-perception balance）方面高效且灵活的框架。我们的代码可在 https://github.com/fanzh03/IR-Flow 获取。

## Abstract
In image restoration, single-step discriminative mappings often lack fine details via expectation learning, whereas generative paradigms suffer from inefficient multi-step sampling and noise-residual coupling. To address this dilemma, we propose IR-Flow, a novel image restoration method based on Rectified Flow that serves as a unified framework bridging the gap between discriminative and generative paradigms. Specifically, we first construct multilevel data distribution flows, which expand the ability of models to learn from and adapt to various levels of degradation. Subsequently, cumulative velocity fields are proposed to learn transport trajectories across varying degradation levels, guiding intermediate states toward the clean target, while a multi-step consistency constraint is presented to enforce trajectory coherence and boost few-step restoration performance. We show that directly establishing a linear transport flow between degraded and clean image domains not only enables fast inference but also improves adaptability to out-of-distribution degradations. Extensive evaluations on deraining, denoising and raindrop removal tasks demonstrate that IR-Flow achieves competitive quantitative results with only a few sampling steps, offering an efficient and flexible framework that maintains an excellent distortion-perception balance. Our code is available at https://github.com/fanzh03/IR-Flow.

---

## 论文详细总结（自动生成）

以下是对论文《IR-Flow: Bridging Discriminative and Generative Image Restoration via Rectified Flow》的结构化总结：

### 1. 核心问题与整体含义
论文旨在解决图像恢复（Image Restoration）中两类主流范式的局限性：
*   **判别式方法（Discriminative）**：通常通过最小化像素级误差（如 L1/L2 损失）来学习条件期望，虽然失真度（Distortion）低，但往往导致结果过于平滑，缺乏感知细节。
*   **生成式方法（Generative，如扩散模型/SDE）**：虽然能生成丰富的纹理，但推理效率低下（需要多步采样），且存在噪声与残差耦合的问题，增加了优化难度。
**研究动机**：利用**修正流（Rectified Flow）**在退化图像和清晰图像之间建立直接的线性传输路径，从而桥接判别式的高效性与生成式的高感知质量。

### 2. 方法论
IR-Flow 的核心思想是将图像恢复建模为从退化分布到清晰分布的确定性常微分方程（ODE）轨迹学习。
*   **多级数据分布流（Multilevel Data Distribution Flows）**：构建线性插值路径 $X_t = tX_1 + (1-t)X_0$，其中 $X_1$ 是退化图像，$X_0$ 是清晰图像。这使得模型能学习不同退化程度下的中间状态。
*   **累积速度场（Cumulative Velocity Field, CVF）**：
    *   传统修正流学习瞬时速度 $v = X_1 - X_0$，而本文提出学习累积速度 $\hat{v}(X_t, t) = X_t - X_0$。
    *   **优势**：CVF 指向目标状态的矢量更直接，理论上比标准速度场减少了 3 倍的传输能量（Kinetic Energy），从而加速收敛并降低离散化误差。
*   **多步一致性训练（Multi-step Consistency Training, MCT）**：
    *   引入正则化项 $\mathcal{L}_{MCT}$，强制要求 ODE 求解器在多步迭代后的结果与真实清晰图像一致。
    *   该约束确保了传输轨迹的线性度，使得模型在极少数采样步数（如 1-4 步）下也能保持高性能。
*   **算法流程**：训练时通过混合损失（速度匹配损失 + 一致性损失）优化 U-Net 骨干网络；推理时使用一阶欧拉求解器进行快速采样。

### 3. 实验设计
*   **任务与数据集**：
    *   **去雨**：Rain100H, Rain100L。
    *   **去噪**：McMaster, Kodak24, CBSD68（高斯噪声 $\sigma \in [0, 50]$）。
    *   **去雨滴**：RainDrop。
    *   **扩展任务**：超分辨率（DIV2K）、去雾（RESIDE-6k）、去模糊（GoPro）、真实场景去噪（SIDD）。
*   **Benchmark 与对比方法**：
    *   **指标**：PSNR、SSIM（失真度）、LPIPS（感知质量）、FID（分布相似度）、NFE（函数评价次数/步数）。
    *   **对比对象**：包括判别式 SOTA（Restormer, MPRNet, DnCNN 等）和生成式 SOTA（IR-SDE, RDDM, Resfusion 等）。

### 4. 资源与算力
*   **训练配置**：使用单张 **NVIDIA A40 GPU** 进行训练。
*   **训练参数**：总迭代次数 700k 次，Batch Size 为 8，Patch Size 为 256x256。
*   **推理效率**：在 **NVIDIA 2080Ti** 上测试，推理速度比 IR-SDE 快 100 倍，比 Resfusion 快 4 倍。
*   **模型规模**：参数量约 137.1M（与 IR-SDE 持平）。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 6 种以上的图像恢复任务，并在 10 多个主流数据集上进行了定量和定性评估。
*   **消融实验**：针对 CVF 的有效性、MCT 损失的权重 $\lambda_{MCT}$、采样步数（NFE）的影响以及不同速度场定义的收敛速度做了详细对比。
*   **泛化性测试**：进行了分布外（OOD）实验，例如在 Rain100H 上训练但在真实互联网图像上测试，证明了其物理可解释性带来的鲁棒性。
*   **客观性**：对比实验涵盖了从传统卷积网络到最新扩散模型的各类范式，评价指标全面，实验设计较为公平。

### 6. 主要结论与发现
*   **性能卓越**：IR-Flow 在多个任务中达到了 SOTA 水平，尤其在失真（PSNR）与感知（LPIPS）的平衡上表现优异。
*   **极速推理**：仅需 1 步采样即可达到判别式方法的水平，2-4 步采样即可在感知质量上超越现有的多步扩散模型。
*   **CVF 的优越性**：相比标准修正流，累积速度场显著加快了训练收敛速度，并在图像生成任务（CIFAR-10）中也表现出更强的性能。

### 7. 优点
*   **统一框架**：成功将判别式回归视为修正流的一个特例（$t=1$），实现了理论上的统一。
*   **能量效率**：通过数学证明 CVF 具有更低的传输能量，为速度场优化提供了新视角。
*   **灵活性**：用户可以通过调整采样步数，在极短时间内自由切换“高保真”或“高感知”输出。

### 8. 不足与局限
*   **监督学习依赖**：目前框架仍依赖成对（Paired）数据训练，在缺乏 Ground Truth 的极端真实场景下应用受限。
*   **语义理解缺失**：对于极度退化或大面积遮挡的区域，缺乏文本或高级语义引导，可能无法恢复准确的语义细节。
*   **模型体积**：虽然推理步数少，但单次调用的参数量（137M）相比某些轻量化判别式模型仍然较大。

（完）
