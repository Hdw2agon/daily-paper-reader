---
title: "Beyond Ground-Truth: Leveraging Image Quality Priors for Real-World Image Restoration"
title_zh: 超越真值：利用图像质量先验进行真实世界图像修复
authors: "Fengyang Xiao, Peng Hu, Lei Xu, XingE Guo, Guanyi Qin, Yuqi Shen, Chengyu Fang, Rihan Zhang, Chunming He, Sina Farsiu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29773v2"
tags: ["query:q3"]
score: 8.0
evidence: 利用图像质量先验的无标签引导
tldr: 针对真实场景图像修复中Ground-Truth质量不一导致模型性能受限的问题，本文提出IQPIR框架。该框架引入预训练无参考图像质量评估（NR-IQA）模型提取的质量先验，通过质量条件Transformer、双分支码本结构及离散表示优化策略，显式引导模型生成感知质量更优的图像。实验证明该方法在真实图像修复任务中超越了现有技术，并可作为通用增强策略提升现有模型性能。
selection_source: fresh_fetch
motivation: 传统基于Ground-Truth监督的方法受限于训练数据质量的不一致性，导致模型难以达到最优的感知修复效果。
method: 提出利用NR-IQA模型提取质量先验，通过质量条件Transformer、双分支码本结构和离散表示优化策略来引导修复过程。
result: 实验结果显示，该方法在多个真实世界图像修复基准测试中均优于现有最先进方法，并展现出极强的通用性。
conclusion: 通过引入图像质量先验，可以有效克服GT质量瓶颈，显著提升真实场景下图像修复的感知质量和鲁棒性。
---

## 摘要
真实世界图像修复旨在从非受控条件下捕获的退化低质量（LQ）输入中恢复高质量（HQ）图像。现有方法通常依赖于真值（GT）监督，并假设 GT 提供了完美的参考质量。然而，GT 仍可能包含感知忠实度不一致的图像，导致模型收敛到训练数据的平均质量水平，而非达到可实现的最高感知质量。为解决这些问题，我们提出了一种名为 IQPIR 的新颖框架，该框架引入了从预训练的无参考图像质量评估（NR-IQA）模型中提取的图像质量先验（IQP），以显式引导修复过程生成感知最优的输出。我们的方法通过三种关键机制将 IQP 与学习到的码本先验协同整合：(1) 质量条件 Transformer，其中源自 NR-IQA 的分数作为条件信号，引导预测的表示趋向最大感知质量。该设计提供了一种即插即用的增强方案，无需结构修改即可与现有修复架构兼容；(2) 双分支码本结构，解耦通用特征和 HQ 特有特征，确保对通用结构信息和质量敏感属性的全面表示；(3) 基于离散表示的质量优化策略，减轻了连续潜空间中常见的过度优化效应。在真实世界图像修复上的广泛实验表明，我们的方法不仅超越了最先进的方法，还可以作为现有方法的一种通用质量引导增强策略。代码已开源。

## Abstract
Real-world image restoration aims to restore high-quality (HQ) images from degraded low-quality (LQ) inputs captured under uncontrolled conditions. Existing methods typically depend on ground-truth (GT) supervision, assuming that GT provides perfect reference quality. However, GT can still contain images with inconsistent perceptual fidelity, causing models to converge to the average quality level of the training data rather than achieving the highest perceptual quality attainable. To address these problems, we propose a novel framework, termed IQPIR, that introduces an Image Quality Prior (IQP)-extracted from pre-trained No-Reference Image Quality Assessment (NR-IQA) models-to guide the restoration process toward perceptually optimal outputs explicitly. Our approach synergistically integrates IQP with a learned codebook prior through three key mechanisms: (1) a quality-conditioned Transformer, where NR-IQA-derived scores serve as conditioning signals to steer the predicted representation toward maximal perceptual quality. This design provides a plug-and-play enhancement compatible with existing restoration architectures without structural modification; and (2) a dual-branch codebook structure, which disentangles common and HQ-specific features, ensuring a comprehensive representation of both generic structural information and quality-sensitive attributes; and (3) a discrete representation-based quality optimization strategy, which mitigates over-optimization effects commonly observed in continuous latent spaces. Extensive experiments on real-world image restoration demonstrate that our method not only surpasses cutting-edge methods but also serves as a generalizable quality-guided enhancement strategy for existing methods. The code is available.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **IQPIR** 的新型图像修复框架，旨在通过引入图像质量先验（Image Quality Prior, IQP）来突破传统方法对“真值（Ground-Truth, GT）”质量的过度依赖。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统真实世界图像修复（RWIR）方法假设 GT 图像是完美的参考标准。然而，实际训练集（如 FFHQ）中的 GT 图像质量参差不齐（平均分仅在 5-8 分之间），导致模型往往只能收敛到数据的“平均质量水平”，难以生成真正高质量（9 分以上）的图像。
*   **研究动机**：如果简单地过滤掉低质量 GT，会显著减少训练数据的多样性。因此，研究者希望在保留全量数据训练的同时，利用无参考图像质量评估（NR-IQA）模型作为先验，显式引导模型追求更高的感知质量。

### 2. 提出的方法论
IQPIR 框架将图像质量先验与离散码本（Codebook）先验相结合，包含三个核心机制：
*   **质量条件 Transformer（Quality-Conditioned Transformer）**：将 NR-IQA 模型计算出的质量分数作为条件信号输入 Transformer。在推理时，通过输入最高质量分数（如 9 分），引导模型预测出对应高质量特征的表示。
*   **双分支码本结构（Dual-Branch Codebook）**：
    *   **通用码本**：学习所有训练数据的通用结构特征。
    *   **HQ+ 码本**：专门学习高质量样本中的精细纹理和属性。
    *   通过质量分数阈值控制 HQ+ 码本的更新，实现通用结构与高质量细节的解耦。
*   **基于离散表示的质量优化策略**：直接以最大化 NR-IQA 分数为目标进行微调。由于码本空间是离散且有限的，这种方法能有效缓解在连续潜空间中常见的“过度优化（Over-optimization）”问题（即产生欺骗 IQA 模型但人类视觉不适的对抗性伪影）。
*   **质量先验集成**：为减少单一 IQA 模型的偏差，采用了包括 Musiq、TOPIQ、Q-Align 等多个先进模型的集成评分。

### 3. 实验设计
*   **应用场景与数据集**：
    *   **盲人脸修复（BFR）**：在 FFHQ 上训练，在 LFW-Test、WebPhoto-Test、WIDER-Test 上评估。
    *   **低光照增强（LLIE）**：使用 LOL-v1、LOL-v2 数据集。
    *   **水下图像增强（UIE）**：使用 UIEB 数据集。
    *   **背光图像增强（BIE）**：使用 BAID 数据集。
*   **对比方法**：对比了 CodeFormer、RestoreFormer、DifFace、DAEFR、Reti-Diff、CIDNet 等十余种 SOTA 方法。
*   **评估指标**：除了 PSNR、SSIM、FID 外，重点使用了多种感知指标（TOPIQ, Musiq, Arniqa, Q-Align, CLIP-IQA 等）和用户主观调研。

### 4. 资源与算力
*   **算力设备**：论文明确提到模型是在 **一台 NVIDIA H200 GPU** 上训练完成的。
*   **训练细节**：使用了 Adam 优化器，Batch Size 为 32，特征图尺寸为 16x16x256，码本容量为 1024。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了四类主流修复任务，每类任务均在多个公开基准数据集上进行了定量和定性对比。
*   **消融实验**：详细验证了质量条件、双码本结构、质量优化策略以及离散空间对抑制过度优化的有效性。
*   **通用性验证**：作者将 IQPIR 作为插件集成到其他 SOTA 模型（如 WaveFace, Interlcm, Reti-Diff 等）中，均取得了性能提升，证明了该方法的普适性和公平性。
*   **下游任务**：验证了修复结果对低光照目标检测（ExDark 数据集 + YOLO）的增益。

### 6. 主要结论与发现
*   **超越 GT**：通过质量先验引导，模型生成的图像在感知质量上可以超过训练集中的平均 GT 水平。
*   **质量可控**：通过调节输入的条件分数，可以实现对修复结果感知强度的连续控制。
*   **离散空间优势**：离散码本能天然过滤掉 NR-IQA 优化过程中的对抗样本，确保结果既高分又真实。
*   **SOTA 性能**：在所有测试任务中，IQPIR 在感知指标和视觉效果上均达到领先水平。

### 7. 优点与亮点
*   **范式创新**：打破了“GT 即真理”的束缚，利用外部知识（IQA 模型）来弥补训练数据的缺陷。
*   **即插即用**：提出的质量条件机制可以轻松集成到现有的基于 Transformer 或扩散模型的修复架构中。
*   **鲁棒性强**：双码本设计平衡了结构还原的准确性与视觉细节的丰富度。

### 8. 不足与局限
*   **先验偏差风险**：模型性能高度依赖于预训练 NR-IQA 模型的准确性。如果 IQA 模型本身存在偏见（例如偏好过度锐化），修复模型可能会继承这些缺陷。
*   **计算开销**：虽然推理时增加的计算量较小，但训练阶段需要运行多个 IQA 模型进行集成评分，增加了预处理或在线计算的负担。
*   **应用限制**：对于某些极度退化的图像，如果 IQA 模型无法给出可靠评分，引导效果可能会打折扣。

（完）
