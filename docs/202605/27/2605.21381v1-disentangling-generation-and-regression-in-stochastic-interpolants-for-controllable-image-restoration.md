---
title: Disentangling Generation and Regression in Stochastic Interpolants for Controllable Image Restoration
title_zh: 解耦随机插值中的生成与回归以实现可控图像修复
authors: "Yi Liu, Jia Ma, Wengen Li, Jihong Guan, Shuigeng Zhou, Yichao Zhang"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21381v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于可控图像修复的生成式方法
tldr: 针对图像恢复中生成式方法推理慢、保真度低，而回归式方法缺乏真实感的问题，本文提出DiSI框架。该框架将随机插值过程解耦为独立的生成和回归组件，实现了从纯回归到完全生成的连续可控转换。通过双分支U-Net架构和统一采样器，DiSI在保持高效推理的同时，能在单个模型中灵活平衡图像的失真度与感知质量。
selection_source: fresh_fetch
motivation: 旨在解决生成式图像恢复方法推理速度慢、像素保真度差，以及回归式方法难以生成真实纹理的矛盾。
method: 提出DiSI框架，将随机插值过程解耦为生成与回归组件，并设计了双分支U-Net架构和统一采样器以实现灵活推理。
result: 实验证明DiSI在多种图像恢复任务上达到了竞争性表现，并能在推理时动态调节失真与感知之间的平衡。
conclusion: 该研究通过解耦策略成功统一了回归与生成式图像恢复，为实现高效且可控的图像修复提供了新思路。
---

## 摘要
图像修复（IR）的最新进展主要由扩散模型和流匹配等生成式方法推动，这些方法擅长合成逼真的纹理，但面临多步推理缓慢和像素保真度受损的问题。相比之下，经典的基于回归的 IR 方法恰恰在这些方面表现出色，具有单步效率和高像素级重建保真度。为了弥补这一差距，我们提出了 DiSI，这是一个统一的框架，它将底层的随机插值（Stochastic Interpolant）过程解耦为独立的生成和回归组件。这种解耦赋予了 DiSI 卓越的通用性，使其能够实现从纯回归过程到完全生成过程的连续且可控的转换。在技术上，我们通过两条特定的采样轨迹实例化了该框架，并配备了一个统一的采样器，用于在任意轨迹上进行高质量、少步数的推理。此外，我们在像素空间设计了一个双分支 U-Net 风格的 Transformer 网络，利用专用分支增强条件引导，同时确保高吞吐量。大量实验表明，DiSI 在各种 IR 任务上高效地取得了具有竞争力的结果，同时独特地提供了在单个模型中控制失真与感知（distortion-perception）权衡的推理时灵活性。

## Abstract
Recent advances in Image Restoration (IR) have been largely driven by generative methods such as Diffusion Models and Flow Matching, which excel in synthesizing realistic textures while suffering from slow multi-step inference and compromised pixel fidelity. In contrast, classical regression-based IR methods excel precisely in these aspects, offering single-step efficiency and high pixel-level reconstruction fidelity. To bridge this gap, we propose DiSI, a unified framework that Disentangles the underlying Stochastic Interpolant process into independent generation and regression components. This decoupling endows DiSI with remarkable versatility, enabling a continuous and controllable transition from a pure regression process to a fully generative one. Technically, we instantiate this framework with two specific sampling trajectories, accompanied by a unified sampler for high-quality, few-step inference on arbitrary trajectories. Furthermore, we design a dual-branch U-Net style transformer network in pixel space, using a dedicated branch to enhance conditional guidance while ensuring high throughput. Extensive experiments demonstrate that DiSI efficiently achieves competitive results on various IR tasks, while uniquely offering the inference-time flexibility to control the distortion-perception trade-off within a single model.

---

## 论文详细总结（自动生成）

这篇论文提出了 **DiSI**（Disentangled Stochastic Interpolants），一个旨在统一并解耦图像修复（IR）中“回归”与“生成”过程的创新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：图像修复任务长期存在**失真与感知（Distortion-Perception）的权衡**。
*   **痛点**：
    *   **回归方法**（如基于 MSE 的 CNN/Transformer）：推理快（单步）、像素保真度高（PSNR 高），但结果往往过于平滑，缺乏真实纹理。
    *   **生成方法**（如扩散模型 DM、流匹配 FM）：纹理真实感强，但推理慢（需多步迭代），且容易偏离原始像素（保真度低）。
*   **核心问题**：能否在一个统一的框架内，灵活且连续地控制回归（保真度）与生成（真实感）的平衡，而无需针对不同需求重新训练模型？

### 2. 方法论
*   **核心思想**：将随机插值（Stochastic Interpolants, SI）过程解耦。传统的 SI 将数据转换和噪声注入耦合在单一时间变量 $t$ 上，而 DiSI 引入了两个独立的时间维度：
    1.  **回归时间 ($r$)**：控制从退化图像到清晰图像的确定性轨迹。
    2.  **生成时间 ($g$)**：控制随机噪声的注入强度。
*   **关键技术细节**：
    *   **DiSI 过程公式**：$x(r, g) = \lambda_g (\alpha_r x_0 + \beta_r x_1) + \gamma_g z$。通过调节 $r$ 和 $g$，模型可以遍历一个二维的时间平面。
    *   **DULiT 网络架构**：设计了“双分支 U-Net 风格线性 Transformer”。
        *   **双分支**：分别处理噪声状态和退化条件，增强引导。
        *   **线性注意力（Linear Attention）**：在像素空间直接操作，避免了潜在空间（Latent Space）压缩带来的细节损失，同时保持线性计算复杂度。
    *   **采样轨迹**：提出了**椭圆轨迹**（模拟扩散桥）和**线性轨迹**（模拟均值回归 SDE），通过超参数 $\delta$ 控制噪声水平。
    *   **统一采样器**：推导了一个解析的一阶 ODE 采样器，允许在极少步数（如 1-10 步）下实现高质量生成。

### 3. 实验设计
*   **任务与数据集**：
    *   **图像去雨**：Rain100H。
    *   **图像去模糊**：GoPro。
    *   **低光增强**：LOL。
    *   **图像补全（Inpainting）**：Celeba-HQ。
*   **对比方法（Benchmark）**：
    *   回归类：MPRNet, MAXIM, Restormer, SwinIR 等。
    *   生成类：IR-SDE, GOUB, FoD, DDRM 等。
*   **评估指标**：PSNR、SSIM（失真度）；LPIPS、FID（感知质量）；参数量（Param）和推理延迟（Latency）。

### 4. 资源与算力
*   **硬件**：使用 **4 张 NVIDIA RTX 4090 GPU** 进行训练。
*   **训练设置**：
    *   总步数：500k 步。
    *   全局 Batch Size：16。
    *   训练 Patch Size：256 $\times$ 256。
    *   精度：使用 TF32 精度加速。
*   **推理效率**：在单张 RTX 4090 上测量，DiSI-R（回归模式）延迟约为 45ms-250ms（视任务分辨率而定）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同的 IR 任务上进行了详尽对比，每项任务均提供了定量指标和定性视觉结果。
*   **消融实验**：极其充分（包含 10 余组子实验），涵盖了：
    *   框架对比（与标准 SI、IR-SDE 等对比）。
    *   轨迹连续性（$C^0$ 到 $C^\infty$ 连续性的影响）。
    *   网络组件（注意力机制类型、FFN 设计、时间调制方式、损失权重等）。
    *   采样器参数（$\delta, \eta, NFE$ 的影响）。
*   **评价**：实验设计客观，通过 DiSI-R（侧重 PSNR）和 DiSI-G（侧重感知）两个变体，公平地展示了模型在不同评价体系下的优越性。

### 6. 主要结论与发现
*   **解耦的有效性**：通过解耦 $r$ 和 $g$，DiSI 成功实现了在单个模型中通过调整推理参数来控制“失真-感知”权衡。
*   **性能领先**：DiSI 在多个任务上达到了 SOTA 或极具竞争力的表现。DiSI-R 在 PSNR 上表现优异，而 DiSI-G 在 LPIPS 和 FID 上领先。
*   **高效推理**：得益于线性注意力和专用采样器，DiSI 在保持生成质量的同时，推理速度显著快于传统的扩散模型（如 IR-SDE）。

### 7. 优点与亮点
*   **理论创新**：将一维插值扩展到二维解耦平面，为理解生成式修复提供了新视角。
*   **推理灵活性**：**无需重新训练**，仅需改变采样轨迹即可获得不同风格的结果（从平滑到锐利）。
*   **像素空间处理**：相比 Latent Diffusion，DULiT 在像素空间保留了更丰富的纹理细节。
*   **采样器推导**：解析推导的采样器解决了起始点的奇异性问题，提高了少步推理的稳定性。

### 8. 不足与局限
*   **推理开销**：虽然比 DM 快，但 DiSI-G 模式（通常需 10 步）相比纯回归方法（1 步）仍有数倍的计算延迟。
*   **超参数依赖**：为了获得最佳的感知效果，仍需手动调节推理时的噪声水平 $\delta$。
*   **极端退化挑战**：在极高噪声或大面积缺失的情况下，生成分支可能会产生不真实的幻觉纹理。
*   **应用限制**：目前主要验证了实验室环境下的合成退化，在复杂的真实世界未知退化上的泛化能力有待进一步验证。

（完）
