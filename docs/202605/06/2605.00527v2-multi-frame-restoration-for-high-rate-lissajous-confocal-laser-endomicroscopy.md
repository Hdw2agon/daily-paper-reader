---
title: Multi-frame Restoration for High-rate Lissajous Confocal Laser Endomicroscopy
title_zh: 高帧率利萨如共聚焦激光显微内窥镜的多帧修复
authors: "Minhee Lee, Sangyoon Lee, Jiwook Lee, Minki Hong, Kyuyoung Kim, Won Hwa Kim, Jaeho Lee"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00527v2"
tags: ["query:q1"]
score: 9.0
evidence: 修复扫描轨迹中的结构化空洞
tldr: Lissajous共聚焦激光显微内窥镜（CLE）在高速成像时会产生大量未采样像素空洞。本文提出了首个针对高帧率Lissajous CLE的基准数据集，包含低质视频与高质量参考图像对。同时提出轻量级循环框架MIRA，通过特征重用和位移对齐聚合时间上下文信息。实验证明MIRA在保持临床部署所需的高效率的同时，恢复质量优于现有基准。
selection_source: fresh_fetch
motivation: 解决高帧率Lissajous扫描中由于采样不足导致的图像结构性空洞问题。
method: 提出一种名为MIRA的轻量级循环框架，利用特征重用和位移对齐技术实现多帧时间上下文聚合。
result: MIRA在恢复质量上超越了轻量级和高复杂度基准模型，且计算效率优异。
conclusion: 该研究为高帧率Lissajous CLE提供了有效的数据集和恢复方案，具备临床应用潜力。
---

## 摘要
利萨如共聚焦激光显微内窥镜 (CLE) 是手持场景下高速体内光学活检的一种极具前景的解决方案。然而，利萨如扫描追踪的是共振轨迹，每帧仅对访问过的像素进行采样；在高帧率下，许多像素仍未被访问，从而产生了结构性空洞。在这项工作中，我们推出了首个高帧率利萨如 CLE 基准测试集，由低质量视频片段和对应的高质量参考图像组成。参考图像是通过拼接同一组织的稳定、慢速扫描帧获得的宽视场 (wide-FOV) 镶嵌图，从而实现了时间对齐的监督。利用该数据集，我们提出了 MIRA，这是一种用于利萨如 CLE 修复的轻量级循环框架，通过特征重用和位移对齐迭代地聚合时间上下文。我们的实验表明，MIRA 在修复质量上优于轻量级和高复杂度的基准模型，同时保持了适合临床部署的良好计算效率。

## Abstract
Lissajous confocal laser endomicroscopy (CLE) is a promising solution for high speed in vivo optical biopsy for handheld scenarios. However, Lissajous scanning traces a resonant trajectory and samples only the visited pixels per frame; at high frame rates, many pixels remain unvisited, creating structured holes. In this work, we introduce the first benchmark for high-rate Lissajous CLE, consisting of low-quality video clips paired with high-quality reference images. The reference images are wide-FOV mosaics obtained by stitching stabilized, slow-scan frames of the same tissue, enabling temporally aligned supervision. Using this dataset, we propose MIRA, a lightweight recurrent framework for Lissajous CLE restoration that iteratively aggregates temporal context through feature reuse and displacement alignment. Our experiments demonstrate that MIRA outperforms both lightweight and high-complexity baselines in restoration quality while maintaining a favorable computational efficiency suitable for clinical deployment.

---

## 论文详细总结（自动生成）

这篇论文针对高帧率利萨如共聚焦激光显微内窥镜（Lissajous CLE）的图像修复问题提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：共聚焦激光显微内窥镜（CLE）能够实现细胞级分辨率的体内成像，被称为“光学活检”。但在手持操作中，探头与组织间的微小运动会产生严重的运动伪影。
*   **核心痛点**：为了减少伪影，需要提高帧率。利萨如（Lissajous）扫描虽然比传统光栅扫描更快、更节能，但在高帧率下采样极度稀疏（缺失像素超过70%），形成“结构化空洞”，导致图像质量大幅下降。
*   **研究目标**：开发一种能够从稀疏、低质量（LQ）的视频流中实时恢复高质量（HQ）密集图像的算法，并建立该领域的首个基准数据集。

### 2. 方法论
论文提出了 **MIRA (Multi-frame Iterative RestorAtion)**，一个轻量级的循环修复框架：
*   **核心架构**：基于轻量级 U-Net，并在编码器和解码器中引入 **ResFFT 模块**。该模块结合了空间域和频率域分支，能有效抑制在频率域中更易分离的运动伪影。
*   **特征重用与缓存**：为了降低延迟，MIRA 缓存了前 4 帧的编码器特征，通过循环机制聚合时间上下文，避免了重复的特征提取计算。
*   **无流注册（Flow-free Registration）**：针对稀疏像素无法有效计算光流的问题，利用 FFT 相位相关法（Phase Correlation）估计帧间的全局位移。这种方法计算开销极低，且符合内窥镜探头与组织接触时以侧向运动为主的物理特性。
*   **特征注意力模块（FAM）**：在解码阶段递归地整合前一时刻的输出，以稳定高频细节的恢复。
*   **拒绝采样训练策略**：在训练阶段，通过比较 LQ 增强帧与 HQ 标签的块均方误差（MSE），自动剔除对齐极差或亮度严重失配的训练样本，提高模型鲁棒性。

### 3. 实验设计
*   **数据集 (MaLissa)**：作者采集了猪胃组织的 CLE 视频，构建了包含 2,060 对 LQ-HQ 图像的开源数据集。
    *   **LQ 帧**：10Hz 采样，缺失像素 > 70%。
    *   **HQ 标签**：通过拼接 2Hz 慢速扫描帧生成的宽视场（Wide-FOV）高质量镶嵌图。
*   **对比基准 (Baselines)**：
    *   **Transformer 类**：Turtle, RVRT。
    *   **CNN 类**：BasicVSR++, EMVD。
    *   **状态空间模型 (SSM)**：MedVSR。
*   **评估指标**：PSNR, SSIM, MS-SSIM（质量指标）；Latency, GFLOPs, Params（效率指标）。
*   **下游任务验证**：通过“样本区域检索”和“基于 MedSAM 的语义分割”来验证修复图像在临床应用中的实用价值。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA RTX 6000 Ada** GPU 上进行。
*   **训练细节**：使用 Adam 优化器，初始学习率 2e-4，Batch size 为 32，训练 600 个 epoch，采用余弦退火策略。
*   **推理延迟**：MIRA 的单帧处理延迟约为 **236.66ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比实验，涵盖了当前视频修复领域最先进的各种架构（CNN、Transformer、SSM）。
*   **消融实验**：针对“无流注册”、“FAM 模块”和“拒绝采样策略”分别进行了消融研究，验证了各组件对感知质量（MS-SSIM）的贡献。
*   **充分性评价**：实验设计较为客观、公平。不仅对比了图像重建质量，还重点评估了临床部署至关重要的计算效率，并利用下游任务（分割与检索）证明了模型在像素级指标之外的结构保持能力。

### 6. 主要结论与发现
*   **性能领先**：MIRA 在 PSNR（22.88dB）上优于所有对比方法，且在 MS-SSIM 等感知指标上表现突出。
*   **效率优势**：相比于高性能的 Transformer 模型（如 Turtle），MIRA 的延迟降低了约 7 倍，计算量（GFLOPs）降低了数百倍。
*   **对齐策略有效性**：在像素高度缺失的情况下，基于 FFT 的全局位移估计比复杂的光流法更稳健且更高效。

### 7. 优点
*   **首创性**：填补了高帧率利萨如扫描 CLE 修复的数据集空白。
*   **实用导向**：算法设计充分考虑了临床实时性需求，在质量和速度之间取得了极佳的平衡。
*   **鲁棒的对齐方案**：巧妙地利用相位相关法解决了稀疏采样下的帧间对齐难题。

### 8. 不足与局限
*   **实时性仍有差距**：虽然 MIRA 已经很轻量，但 236ms 的延迟仍无法满足 10Hz（即每帧 < 100ms）的绝对实时处理要求。
*   **数据多样性有限**：数据集仅来源于猪胃组织，虽然解剖学上接近人类，但尚未在更多器官或病理组织上验证泛化性。
*   **精度优化空间**：目前模型运行在 FP32 精度下，未来若引入 FP16 或 INT8 量化，可能进一步提升速度，但论文尚未对此进行深入探讨。

（完）
