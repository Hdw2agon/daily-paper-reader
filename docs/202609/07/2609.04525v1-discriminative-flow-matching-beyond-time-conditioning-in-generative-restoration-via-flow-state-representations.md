---
title: "Discriminative Flow Matching: Beyond Time-Conditioning in Generative Restoration via Flow-State Representations"
title_zh: 判别式流匹配：通过流状态表示超越生成式修复中的时间条件化
authors: "Shrishti Saha Shetu, Emanuël A. P. Habets, Andreas Brendel"
date: 2026-09-03
pdf: "https://arxiv.org/pdf/2609.04525v1"
tags: ["query:q4"]
score: 8.0
evidence: 通过流匹配进行生成式修复
tldr: 传统条件流匹配（CFM）依赖全局统一的时间坐标，无法适应修复任务中不同样本退化程度不一的问题。本文提出“判别式流状态假说”，并据此设计了判别式流匹配（DFM）方法，利用判别模型提取的“流状态表示”取代显式时间坐标来引导生成速度场。实验证明，该方法能更好地表征恢复进度并实现自适应推理，性能全面超越标准CFM及扩散模型基线。
selection_source: fresh_fetch
motivation: 传统流匹配使用全局统一的时间坐标，无法反映修复任务中不同样本在退化程度和恢复难度上的个体差异。
method: 提出判别式流匹配（DFM），利用判别模型提取的“流状态表示”替代显式时间坐标来条件化流匹配的速度场。
result: 在语音增强和图像去噪任务中，该方法能精准刻画恢复进度并实现自适应推理，性能显著优于传统CFM与扩散模型。
conclusion: 判别式特征为生成式流匹配提供了一种有效的状态感知替代方案，为结合判别式与生成式建模开辟了新视角。
---

## 摘要
现有的条件流匹配（CFM）公式使用显式的插值坐标（通常解释为时间）来描述传输进度，并假设单一的全局变量足以表示样本在生成轨迹上的位置。然而，在修复任务中，传输进度取决于样本本身，因为初始分布与目标分布之间可能存在不同的统计依赖关系。因此，处于相同插值坐标的样本在退化程度、到目标分布的距离以及修复难度上可能存在显著差异。我们研究了由判别式训练模型学习到的信号表示是否能为基于 CFM 的修复提供有意义的生成传输状态描述。通过系统的潜在空间分析，我们证明了判别式表示会根据退化严重程度进行组织，并在生成过程中遵循向清晰数据流形演进的一致轨迹。受这些观察结果的启发，我们提出了“判别式流状态假设”，该假设认为判别式表示编码了控制生成式修复的传输状态。基于此假设，我们提出了“判别式流匹配”，它将流匹配速度场条件化于判别式流状态表示，而非显式的时间坐标。在语音增强和图像去噪方面的实验表明，这些表示能够表征修复进度，实现自适应推理，并始终优于 CFM 和基于扩散模型的基线方法。我们的研究结果表明，判别式表示为显式时间条件化提供了一种有效的状态感知替代方案，并为判别式建模与基于 CFM 的生成式建模之间的关系提供了新的视角。

## Abstract
Existing Conditional Flow Matching (CFM) formulations describe transport progress using an explicit interpolation coordinate, commonly interpreted as time, assuming that a single global variable adequately represents a sample's position along the generative trajectory. In restoration tasks, however, transport progress is sample-dependent because the initial distribution may exhibit varying statistical dependencies with the target distribution. Thus, samples at the same interpolation coordinate can differ substantially in degradation level, distance to the target distribution, and restoration difficulty. We investigate whether signal representations learned by discriminatively trained models provide a meaningful description of generative transport state in CFM-based restoration. Through systematic latent-space analysis, we show that discriminative representations organize according to degradation severity and follow a consistent trajectory toward the clean-data manifold during generation. Motivated by these observations, we introduce the Discriminative Flow-State Hypothesis, which posits that discriminative representations encode a transport state governing generative restoration. Based on this hypothesis, we propose Discriminative Flow Matching, which conditions the Flow-Matching velocity field on Discriminative Flow-State Representations rather than explicit time coordinates. Experiments on speech enhancement and image denoising show that these representations characterize restoration progress, enable adaptive inference, and consistently outperform CFM and diffusion-related baselines. Our findings suggest that discriminative representations provide an effective state-aware alternative to explicit time conditioning and offer a novel perspective on the relationship between discriminative and CFM-based generative modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discriminative Flow Matching: Beyond Time-Conditioning in Generative Restoration via Flow-State Representations》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：条件流匹配（CFM）在生成式修复任务（如语音增强、图像去噪）中表现优异，但传统方法依赖“时间坐标 $t$”作为全局条件。
*   **动机**：在修复任务中，初始退化样本与目标清晰样本的统计依赖关系各异。使用统一的时间 $t$ 无法准确描述不同样本在生成轨迹上的真实进度（即“流状态”），导致模型在处理不同退化程度的样本时缺乏灵活性，且容易过拟合于预设的传输路径。

### 2. 方法论：判别式流匹配 (DFM)
*   **核心思想**：提出“判别式流状态假设”，认为预训练判别模型提取的潜在表示（DFSR）隐式编码了生成传输的当前状态。
*   **关键技术**：
    *   **状态表示**：利用冻结的判别式编码器（如 DCCRN）提取样本的潜在特征 $z = E_\phi(x_\zeta)$。
    *   **条件化替换**：将 CFM 速度场网络 $v_\theta$ 的输入从显式时间 $t$ 替换为判别式流状态表示 $z$。
    *   **训练目标**：通过最小化预测速度场与目标速度场之间的均方误差来优化网络，使生成过程由样本当前的“状态”而非“时间”驱动。
    *   **自适应推理**：根据 $z$ 预测样本的修复难度，动态调整函数评估次数（NFE）和步长，实现计算资源的按需分配。

### 3. 实验设计
*   **数据集/场景**：
    *   **语音增强**：DNS Challenge 数据集（合成非混响及真实录音）。
    *   **图像去噪**：MNIST 和 Fashion-MNIST 数据集。
    *   **其他**：语音去混响任务。
*   **对比基线**：包括传统 CFM (FlowSE)、自主修正流 (ARF)、扩散模型 (SGMSE+)、以及多种判别式模型（DCCRN, GCRN 等）。
*   **评估指标**：SI-SDR（信号重建）、PESQ（感知质量）、SCOREQ（DNN 评估）、DNSMOS（非侵入式评估）。

### 4. 资源与算力
*   **硬件**：模型在单张 NVIDIA A100 GPU 上进行训练。
*   **训练时长**：语音增强任务训练了 800,000 步；图像去噪任务训练了 16 个 epoch。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了从语音到图像的多种模态，并进行了消融实验（如不同判别式编码器的影响、不同输入特征的影响）。
*   **客观性**：使用了统计显著性检验（Wilcoxon 符号秩检验），证明了性能提升的稳健性。
*   **充分性**：实验设计非常详尽，不仅对比了 SOTA 方法，还通过可视化（t-SNE）和单调性分析验证了流状态假设的合理性，具有很高的说服力。

### 6. 主要结论与发现
*   判别式表示能有效捕捉生成修复的进度，且在潜在空间中呈现出向清晰数据流形演进的一致轨迹。
*   DFM 框架在语音增强和图像去噪任务中均优于传统时间条件化方法。
*   该方法天然支持自适应推理，在降低平均计算成本的同时提升了修复质量。

### 7. 优点
*   **创新性**：巧妙地将判别式建模与生成式流匹配结合，解决了 CFM 在修复任务中的“时间歧义”问题。
*   **灵活性**：通过自适应推理实现了计算效率与修复质量的平衡，具有极高的实用价值。
*   **通用性**：该方法不依赖特定的判别模型架构，具有良好的跨模型迁移能力。

### 8. 不足与局限
*   **强假设依赖**：假设判别模型形成马尔可夫链（即 $x_t \to z \to \hat{x}_1$），这在复杂架构中可能不完全成立。
*   **额外开销**：引入了额外的判别式编码器，虽然通过减少 backbone 参数进行了补偿，但仍增加了推理时的计算复杂度。
*   **任务依赖**：在某些任务（如去混响）中，若判别模型本身性能较弱，DFM 的性能增益会受到限制，说明其效果高度依赖于判别特征的质量。

（完）
