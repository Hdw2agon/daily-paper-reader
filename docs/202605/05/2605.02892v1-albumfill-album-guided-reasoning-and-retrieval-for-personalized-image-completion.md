---
title: "AlbumFill: Album-Guided Reasoning and Retrieval for Personalized Image Completion"
title_zh: AlbumFill：面向个性化图像补全的相册引导推理与检索
authors: "Yu-Ju Tsai, Brian Price, Qing Liu, Luis Figueroa, Daniil Pakhomov, Zhihong Ding, Scott Cohen, Ming-Hsuan Yang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02892v1"
tags: ["query:q6"]
score: 9.0
evidence: 免训练的个性化图像补全与检索
tldr: 针对个性化图像补全中身份一致性难以维持且缺乏参考图的问题，本文提出 AlbumFill 框架。该框架无需训练，利用视觉语言模型从个人相册中推理缺失语义并检索身份一致的参考图，辅助补全模型恢复遮挡区域。此外，研究者还发布了一个包含 5.4 万个样本的数据集，实验证明了该方法在保持身份一致性方面的有效性。
selection_source: fresh_fetch
motivation: 现有个性化图像补全方法在缺乏明确参考图时难以保持身份一致性，且依赖手动提供参考。
method: 提出一种无需训练的框架，利用视觉语言模型推理语义线索并从个人相册中检索参考图，进而引导图像补全。
result: 引入了包含 5.4 万个人像样本的大规模数据集，并证明了身份一致性参考检索对提升补全质量的关键作用。
conclusion: AlbumFill 通过结合推理与检索，有效解决了个人相册场景下的个性化图像补全难题。
---

## 摘要
个性化图像补全旨在恢复个人照片中的遮挡区域，同时保持身份和外观的一致性。现有方法要么依赖于通用的图像修复模型，这些模型往往难以维持身份一致性；要么假设已经明确提供了合适的参考图像。在实际应用中，合适的参考图像通常不会被明确提供，这就要求系统在个人照片集中搜索身份一致的图像。我们提出了 AlbumFill，这是一个无需训练的框架，它能从个人相册中检索身份一致的参考图像，以进行个性化补全。给定一张遮挡图像和一个个人相册，视觉语言模型会推断缺失的语义线索，以引导组合图像检索，检索到的参考图像随后被基于参考的补全模型使用。为了促进这一任务的研究，我们引入了一个包含 5.4 万个以人为中心的样本及其关联相册图像的数据集。在多个基准模型上的实验证明了个性化补全的难度，并突显了身份一致性参考检索的重要性。项目主页：https://liagm.github.io/AlbumFill/

## Abstract
Personalized image completion aims to restore occluded regions in personal photos while preserving identity and appearance. Existing methods either rely on generic inpainting models that often fail to maintain identity consistency, or assume that suitable reference images are explicitly provided. In practice, suitable references are often not explicitly provided, requiring the system to search for identity-consistent images within personal photo collections. We present AlbumFill, a training-free framework that retrieves identity-consistent references from personal albums for personalized completion. Given an occluded image and a personal album, a vision-language model infers missing semantic cues to guide composed image retrieval, and the retrieved references are used by reference-based completion models. To facilitate this task, we introduce a dataset containing 54K human-centric samples with associated album images. Experiments across multiple baselines demonstrate the difficulty of personalized completion and highlight the importance of identity-consistent reference retrieval. Project Page: https://liagm.github.io/AlbumFill/

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AlbumFill** 的框架，旨在解决个人相册场景下的个性化图像补全问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着个人摄影的普及，用户拥有大量包含一致身份（如面部、发型、服装）的相册。在修复这些照片中的遮挡区域时，通用补全模型往往无法保持特定人物的身份特征，而现有的“基于参考的补全”方法通常假设用户会手动提供完美的参考图，这在实际应用中并不现实。
*   **核心问题**：如何在没有手动指定参考图的情况下，自动从庞大的个人相册中检索出身份一致且语义相关的图像，并利用其引导图像补全，从而实现高质量的个性化修复。

### 2. 方法论
AlbumFill 是一个**无需训练（Training-free）**的模块化框架，包含三个关键阶段：
1.  **遮挡视觉推理（Masked Visual Reasoning）**：
    *   利用预训练的视觉语言模型（VLM，如 Qwen3-VL）分析图像的可见部分。
    *   模型推断遮挡区域可能包含的语义内容（如“穿着紫色衬衫的孩子”），并生成描述性的文本线索。
2.  **相册检索（Album Retrieval）**：
    *   采用**组合图像检索（CIR）**技术。将“可见区域的图像特征”与“推理出的文本线索”结合，形成多模态查询向量。
    *   在个人相册中搜索相似度最高的图像作为参考图。
3.  **基于参考的补全（Reference-based Completion）**：
    *   将原始遮挡图像与检索到的参考图输入到扩散模型（如 UniReal）中。
    *   模型利用参考图中的精细外观（纹理、颜色、身份细节）来填充遮挡区域，确保结果在身份和环境上都具有一致性。

### 3. 实验设计
*   **数据集（AlbumFill Benchmark）**：基于 CUFED 数据集构建，包含 **5.4 万个**以人为中心的样本，涵盖 1,727 个个人相册。每个相册平均有约 31 张照片，具有多样的姿态、服装和背景。
*   **对比方法**：
    *   **通用补全**：BrushNet（有/无提示词）。
    *   **多模态大模型（MLLM）直接编辑**：BAGEL、Gemini 3 Pro。
    *   **基于参考的补全**：MimicBrush、CompleteMe、UniReal。
*   **评估指标**：
    *   检索性能：Recall@K, mAP@K。
    *   补全质量：PSNR, SSIM（像素级）；LPIPS, DINO, CLIP, DreamSim（感知与语义级）。

### 4. 资源与算力
*   **硬件**：实验在配备 **NVIDIA A100 GPU** 的机器上运行。
*   **算力说明**：由于该框架是**无需训练**的，主要算力消耗在于推理阶段。论文未详细说明具体的推理总时长，但强调了其模块化设计允许灵活更换不同的预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：在 5.4 万个样本上进行了大规模测试，涵盖了不同遮挡比例（小、中、大）的分析。
*   **消融实验**：
    *   验证了 VLM 推理对检索准确性的提升作用（尤其在大面积遮挡时）。
    *   验证了“身份一致性参考”的必要性（通过对比使用错误身份参考图的效果）。
    *   对比了不同 VLM（LLaVA, InternVL, Qwen3）和不同检索模型（iSEARLE, LinCIR 等）的表现。
*   **充分性评价**：实验设计全面，不仅有客观指标，还引入了 Gemini 2.5 Pro 作为第三方评估器来评价推理指令的质量，体现了评价的客观性和公平性。

### 6. 主要结论与发现
*   **身份一致性至关重要**：实验证明，使用来自同一相册的身份一致参考图，其补全效果显著优于通用生成模型或使用错误身份参考图的结果。
*   **推理引导检索的有效性**：VLM 生成的语义线索能显著提高在相册中找到正确参考图的概率，尤其是在遮挡比例超过 50% 的挑战性场景下。
*   **现有 MLLM 的局限**：尽管 Gemini 3 Pro 等模型很强大，但在处理大面积遮挡和保持结构连续性方面，仍不如专门的基于参考的扩散补全模型稳定。

### 7. 优点
*   **实用性强**：针对个人相册这一真实场景，解决了“参考图从哪来”的痛点。
*   **无需训练**：直接利用现有的 VLM、检索模型和生成模型，易于部署和升级。
*   **高质量基准**：发布了专门针对该任务的大规模数据集，填补了领域空白。

### 8. 不足与局限
*   **依赖预训练模型性能**：框架的效果高度依赖于底层 VLM 的推理能力和检索模型的准确度。
*   **检索失败风险**：如果个人相册中确实不存在相似姿态或服装的参考图，补全质量会大幅下降。
*   **领域局限**：目前主要集中在“以人为中心”的场景，对于非人类对象或极其复杂的社交场景（多人混杂）的泛化能力尚待验证。

（完）
