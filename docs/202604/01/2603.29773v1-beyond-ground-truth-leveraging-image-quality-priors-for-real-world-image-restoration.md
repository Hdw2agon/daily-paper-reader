---
title: "Beyond Ground-Truth: Leveraging Image Quality Priors for Real-World Image Restoration"
title_zh: 超越真值：利用图像质量先验进行真实场景图像修复
authors: "Fengyang Xiao, Peng Hu, Lei Xu, XingE Guo, Guanyi Qin, Yuqi Shen, Chengyu Fang, Rihan Zhang, Chunming He, Sina Farsiu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29773v1"
tags: ["query:q3"]
score: 8.0
evidence: 利用质量先验进行无标签图像修复
tldr: 针对真实场景图像修复中Ground-Truth质量不一导致模型性能受限的问题，本文提出IQPIR框架。该方法引入预训练无参考图像质量评估（NR-IQA）模型提取的质量先验，通过质量条件Transformer、双分支码本结构及离散表示优化策略，引导模型生成感知质量更优的图像。实验证明该方法在真实图像修复任务中超越了现有技术，并具有良好的通用性。
selection_source: fresh_fetch
motivation: 现有方法过度依赖质量参差不齐的Ground-Truth，导致模型往往只能收敛到平均质量水平而非最优感知质量。
method: 提出IQPIR框架，利用NR-IQA模型提取质量先验，通过质量条件Transformer、双分支码本及离散表示优化策略引导修复过程。
result: 在真实世界图像修复实验中，该方法不仅超越了现有最先进技术，还证明了其作为通用增强策略的有效性。
conclusion: 通过引入图像质量先验，可以有效解决GT质量不一致问题，显著提升真实场景下图像修复的感知性能。
---

## 摘要
真实场景图像修复旨在从非受控条件下捕获的退化低质量（LQ）输入中恢复高质量（HQ）图像。现有方法通常依赖于真值（GT）监督，并假设 GT 提供了完美的参考质量。然而，GT 仍可能包含感知保真度不一致的图像，导致模型收敛到训练数据的平均质量水平，而非达到可实现的最高感知质量。为了解决这些问题，我们提出了一种名为 IQPIR 的新型框架，该框架引入了从预训练无参考图像质量评估（NR-IQA）模型中提取的图像质量先验（IQP），以显式引导修复过程生成感知最优的输出。我们的方法通过三个关键机制将 IQP 与学习到的码本先验协同整合：(1) 质量条件 Transformer，其中源自 NR-IQA 的分数作为条件信号，引导预测的表示趋向最大感知质量。该设计提供了一种即插即用的增强方案，在无需修改结构的情况下与现有修复架构兼容；(2) 双分支码本结构，解耦通用特征和 HQ 特有特征，确保对通用结构信息和质量敏感属性的全面表示；(3) 基于离散表示的质量优化策略，减轻了连续潜空间中常见的过度优化效应。在真实场景图像修复上的大量实验表明，我们的方法不仅超越了前沿方法，还可以作为现有方法的一种通用质量引导增强策略。代码已开源。

## Abstract
Real-world image restoration aims to restore high-quality (HQ) images from degraded low-quality (LQ) inputs captured under uncontrolled conditions. Existing methods typically depend on ground-truth (GT) supervision, assuming that GT provides perfect reference quality. However, GT can still contain images with inconsistent perceptual fidelity, causing models to converge to the average quality level of the training data rather than achieving the highest perceptual quality attainable. To address these problems, we propose a novel framework, termed IQPIR, that introduces an Image Quality Prior (IQP)-extracted from pre-trained No-Reference Image Quality Assessment (NR-IQA) models-to guide the restoration process toward perceptually optimal outputs explicitly. Our approach synergistically integrates IQP with a learned codebook prior through three key mechanisms: (1) a quality-conditioned Transformer, where NR-IQA-derived scores serve as conditioning signals to steer the predicted representation toward maximal perceptual quality. This design provides a plug-and-play enhancement compatible with existing restoration architectures without structural modification; and (2) a dual-branch codebook structure, which disentangles common and HQ-specific features, ensuring a comprehensive representation of both generic structural information and quality-sensitive attributes; and (3) a discrete representation-based quality optimization strategy, which mitigates over-optimization effects commonly observed in continuous latent spaces. Extensive experiments on real-world image restoration demonstrate that our method not only surpasses cutting-edge methods but also serves as a generalizable quality-guided enhancement strategy for existing methods. The code is available.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Ground-Truth: Leveraging Image Quality Priors for Real-World Image Restoration》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心痛点**：传统的真实场景图像修复（RWIR）模型高度依赖真值（Ground-Truth, GT）监督。然而，在现实数据集中（如 FFHQ），GT 图像的感知质量往往参差不齐（平均分仅在 5-8 分之间）。
*   **研究动机**：如果模型仅以不完美的 GT 为目标，其性能会收敛到训练集的“平均质量水平”，难以生成真正高质量（9 分以上）的图像。
*   **整体含义**：本文提出 **IQPIR** 框架，旨在打破“GT 即完美”的假设，通过引入预训练的无参考图像质量评估（NR-IQA）模型作为先验，显式引导模型追求超越 GT 的最高感知保真度。

### 2. 论文提出的方法论
核心思想是将 **图像质量先验（IQP）** 与 **离散码本先验（Codebook Prior）** 协同整合，具体包含三个关键技术：
*   **双分支码本结构（Dual-branch Codebook）**：
    *   设计了“通用码本（Common）”和“高质量码本（HQ+）”。
    *   在训练阶段，根据 NR-IQA 评分 $S$ 是否超过阈值 $S_{thr}$ 来决定是否激活 HQ+ 码本。这实现了通用结构特征与高质量细节特征的解耦。
*   **质量条件 Transformer（Quality-conditioned Transformer）**：
    *   将 NR-IQA 提取的质量分数 $S$ 转换为嵌入向量，并与低质量图像特征融合。
    *   在推理时，通过手动将输入分数设为最大值（如 9 分），引导 Transformer 预测出对应最高质量的码本索引。
*   **基于离散表示的质量优化策略**：
    *   直接将 IQA 分数作为损失函数（$L_{quality}$）进行反向传播。
    *   **核心创新点**：利用码本的离散空间限制输出范围，有效解决了在连续潜空间中直接优化 IQA 指标时容易出现的“过度优化（Over-optimization）”或产生对抗伪影的问题。

### 3. 实验设计
*   **应用场景与数据集**：
    *   **盲人脸修复**：在 FFHQ 上训练，在 LFW-Test、WebPhoto-Test、WIDER-Test 上测试。
    *   **低光照增强**：使用 LOL-v1、LOL-v2（真实/合成）数据集。
    *   **水下图像增强**：使用 UIEB 数据集。
    *   **背光图像增强**：使用 BAID 数据集。
*   **Benchmark 与对比方法**：
    *   对比了 RestoreFormer、CodeFormer、DifFace、DAEFR、WaveFace、MambaIR、Reti-Diff 等 SOTA 方法。
    *   使用了包括 TOPIQ、Musiq、Arniqa、Q-Align、CLIP-IQA 在内的多种先进感知指标。

### 4. 资源与算力
*   **硬件环境**：模型在 **1 台 NVIDIA H200 GPU** 上进行训练。
*   **训练细节**：使用 PyTorch 实现，采用 Adam 优化器（动量 0.9, 0.999），Batch Size 为 32。
*   **算力说明**：文中未明确给出具体的总训练时长，但提到其推理时间（约 5.63ms）与基础模型相比仅有微小增加，证明了方法的效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 类主流修复任务，每类任务均在多个标准数据集上验证。
    *   **消融实验**：针对质量条件、双码本结构、质量优化策略分别做了拆解实验（Break-down ablation）。
    *   **通用性验证**：将 IQPIR 作为插件集成到 DifFace、Reti-Diff、CIDNet 等 6 种不同架构中，均取得了性能提升。
    *   **用户研究**：邀请 12 名参与者对结果进行主观评分。
*   **评价**：实验设计非常充分且客观。不仅关注全参考指标（PSNR/SSIM），更侧重于多种无参考感知指标，并验证了下游任务（目标检测）的增益，具有很强的说服力。

### 6. 主要结论与发现
*   **超越 GT 是可能的**：通过质量先验引导，模型生成的图像在感知质量上可以优于训练集中的 GT。
*   **离散空间的优势**：离散码本能有效过滤 IQA 优化过程中的噪声和对抗样本，确保高分图像同时具备高视觉真实度。
*   **即插即用**：该方法不改变现有模型的主干结构，可作为一种通用的增强策略提升其他修复算法的上限。

### 7. 优点（亮点）
*   **视角独特**：挑战了“GT 完美论”，解决了真实数据集中标注质量不一的本质问题。
*   **可控性**：通过调节输入的质量分数，实现了对修复强度和感知质量的连续控制。
*   **鲁棒性**：通过集成多个 IQA 模型（Ensemble）来消除单一模型的评估偏差。

### 8. 不足与局限
*   **先验偏差风险**：模型性能高度依赖于预训练 NR-IQA 模型的准确性。如果 IQA 模型本身存在偏见（例如对某种纹理有过度偏好），修复结果可能会继承这些偏差。
*   **计算依赖**：虽然推理快，但训练阶段需要预先计算整个数据集的 IQA 分数，且需要维护双码本，增加了训练初期的复杂性。
*   **极端退化限制**：对于信息完全丢失的极端退化图像，质量先验可能无法凭空产生合理的结构信息。

（完）
