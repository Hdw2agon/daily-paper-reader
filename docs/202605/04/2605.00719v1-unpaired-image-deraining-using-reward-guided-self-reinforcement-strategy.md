---
title: Unpaired Image Deraining Using Reward-Guided Self-Reinforcement Strategy
title_zh: 基于奖励引导自强化策略的非成对图像去雨
authors: "Yinghao Chen, Yeying Jin, Xiang Chen, Yanyan Wei, Ziyang Yan, Yaowen Fu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00719v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 无监督图像去雨与修复
tldr: 针对无监督图像去雨中缺乏强约束和收敛困难的问题，本文提出了一种基于奖励引导自强化策略的去雨框架（RGSUD）。该方法通过动态回收训练过程中产生的高质量去雨图像作为“奖励”，并利用自强化训练阶段将这些奖励转化为伪对齐监督，从而约束优化空间并提升去雨质量。实验证明该方法在多个数据集上达到了SOTA水平，且具有良好的通用性。
selection_source: fresh_fetch
motivation: 无监督去雨因缺乏配对监督导致网络难以收敛，但训练中偶尔出现的高质量结果可被利用来引导优化。
method: 提出包含奖励回收和自强化训练两个阶段的框架，利用基于图像质量评估的机制动态收集并利用高质量输出。
result: 在合成、真实配对及非配对数据集上均达到SOTA性能，主客观评价指标均优于现有无监督方法。
conclusion: 该自强化策略能有效提升无监督去雨的稳定性和质量，且具有极强的泛化能力，可适配多种现有网络。
---

## 摘要
无监督去雨因其能够在没有成对监督的情况下学习真实世界的雨水分布而受到关注。然而，缺乏强约束使得网络难以收敛，特别是在雨水退化具有复杂多样性的情况下。一个关键的动机是，训练过程中偶尔会出现高质量的去雨结果，这些结果可以被用来引导优化过程。为了克服这些挑战，我们提出了 RGSUD（奖励引导自强化无监督图像去雨），它包含两个关键阶段：奖励回收和自强化（SR）训练。在前一阶段，我们提出了一种基于图像质量评价（IQA）的动态奖励回收机制，该机制在训练过程中选择最优的去雨输出，并持续收集高质量的去雨图像。在后一阶段，我们将这些奖励纳入模型的优化过程，约束优化空间并提高去雨输出与清晰图像之间的一致性。通过利用基于 IQA 的自强化损失和动态更新的奖励，我们提高了合成伪成对数据的质量并稳定了优化过程。大量实验表明，我们的方法在多个数据集上达到了 SOTA 性能，包括成对合成图像、成对真实图像和非成对真实图像，在主观和客观 IQA 指标上均优于现有的无监督去雨方法。此外，我们证明了自强化策略可以适配于其他无监督去雨方法，且我们的去雨框架在现有的有监督去雨网络中表现出强大的泛化能力。

## Abstract
Unsupervised deraining has attracted attention for its ability to learn the real-world distribution of rain without paired supervision. However, the lack of strong constraints makes it difficult for the network to converge, especially with the complex diversity of rain degradation. A key motivation is that high-quality deraining results occasionally emerge during training, which can be leveraged to guide the optimization process. To overcome these challenges, we introduce RGSUD (Reward-Guided Self-Reinforcement Unsupervised Image Deraining), comprising two key stages: reward recycling and self-reinforcement (SR) training. For the former stage, we propose an Image Quality Assessment (IQA)-based dynamic reward recycling mechanism that selects optimal derained outputs during training and continuously collects high-quality deraining images. In latter stage, we incorporate these rewards into the model's optimization process, constraining the optimization space and improving alignment between derained outputs and clean images. By leveraging IQA-based self-reinforced loss and dynamically updated rewards, we enhance the quality of synthesized pseudo-paired data and stabilize the optimization. Extensive experiments demonstrate that our method achieves SOTA performance across multiple datasets, including paired synthetic, paired real, and unpaired real images, outperforming existing unsupervised deraining approaches in both subjective and objective IQA metrics. Additionally, we show that the self-reinforcement strategy is adaptable to other unsupervised deraining methods and our deraining framework demonstrates strong generalization across existing supervised deraining networks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **RGSUD**（Reward-Guided Self-Reinforcement Unsupervised Image Deraining）的框架，旨在解决无监督图像去雨中由于缺乏成对监督而导致的优化空间过大、难以收敛的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：无监督去雨方法（如基于 CycleGAN 或对比学习的方法）虽然能处理真实世界的雨水分布，但由于缺乏成对的真值（Ground Truth）约束，模型在训练时往往难以收敛到最优解，且容易产生伪影。
*   **研究动机**：作者观察到，在无监督训练过程中，网络偶尔会生成高质量的去雨结果。受强化学习启发，作者提出：是否可以“回收”这些中间产生的高质量结果作为“奖励（Reward）”，来引导后续的训练过程，从而压缩优化空间并提升性能？

### 2. 方法论
该方法分为两个阶段，核心思想是利用视觉语言模型（VLM）驱动的图像质量评估（IQA）来动态筛选最优输出。
*   **核心组件**：包含去雨器（Derainer，默认使用 NAFNet）、退化估计模块（DEM）、生成器 G 和判别器 D。
*   **第一阶段：奖励回收（Reward Recycling）**：
    *   在非成对数据上进行基础训练。
    *   利用预训练的 VLM 模型（DACLIP-IQA）对去雨输出进行实时打分。
    *   建立一个动态更新的奖励池，如果当前生成的去雨图像得分高于池中存储的旧图像，则进行替换。这些奖励图像被视为“伪真值”，但不参与梯度回传。
*   **第二阶段：自强化训练（Self-Reinforcement Training）**：
    *   **伪对齐监督**：引入自强化损失 $L_{re}$，计算当前去雨输出与回收的“奖励图像”之间的距离，提供显式的优化信号。
    *   **退化细化**：将奖励图像输入 DEM 模块，以获取更准确的雨痕特征，从而合成更高质量的伪成对数据（Pseudo-paired data），形成性能提升的正向循环。

### 3. 实验设计
*   **数据集**：
    *   **合成配对数据集**：Rain100L, Rain200L, DID-Data, DDN-Data。
    *   **真实配对数据集**：SPA-Data, RealRain1K-L, 以及夜间去雨数据集 Night-Rain。
    *   **真实非配对数据集**：SIRR, Real3000。
*   **Benchmark 与对比方法**：
    *   对比了经典的无监督方法：CycleGAN, DerainCycleGAN, NLCL, DCD-GAN, CSUD。
    *   对比了最新的扩散模型/桥接方法：Fr-Diff, Tp-Diff, DehazeSB。
    *   同时参考了 SOTA 有监督方法：MPRNet, Restormer, DRSformer, NeRD-Rain。
*   **评价指标**：PSNR, SSIM（有参考）；CLIP-IQA, MUSIQ, Q-Align, DeQA-Score（无参考/感知评价）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA Tesla V100 GPU**。
*   **训练细节**：使用 Adam 优化器，初始学习率 $2 \times 10^{-4}$，输入图像裁剪为 $256 \times 256$ 的补丁。
*   **时长**：文中提到在应用 SR 策略时，在基础训练后额外增加了约 3 小时的训练时间（针对插件实验部分）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 9 个不同类型的数据集上进行了测试，涵盖了从合成到真实、从白天到黑夜的多种场景。
*   **消融实验**：非常充分。作者验证了：
    1.  SR 策略本身的有效性（PSNR 提升达 1dB 以上）。
    2.  去雨器底座的可替换性（证明了在 Restormer, DRSformer 等模型上均有效）。
    3.  SR 策略作为插件在其他无监督框架（如 CSUD, DCD-GAN）上的通用性。
    4.  不同 IQA 指标（如 NIMA, MUSIQ）对奖励回收效果的影响。
*   **客观性**：通过下游任务（目标检测 YOLOv8 和语义分割 DeepLabv3+）的性能提升，进一步客观验证了去雨效果。

### 6. 主要结论与发现
*   **性能领先**：RGSUD 在几乎所有无监督指标上均达到 SOTA，甚至在某些数据集上接近有监督方法的表现。
*   **自强化效应**：通过回收高质量中间产物作为奖励，可以有效弥补无监督学习中“数据一致性项”的缺失。
*   **VLM 的价值**：证明了基于 VLM 的 IQA（如 DACLIP-IQA）比传统指标更能准确捕捉图像的感知质量，是理想的奖励引导信号。

### 7. 优点
*   **创新性强**：首次将“奖励回收”和“自强化”概念引入无监督去雨，巧妙利用了训练过程中的随机优选结果。
*   **通用性极佳**：该策略不绑定特定架构，可作为通用插件提升现有无监督或有监督模型的泛化能力。
*   **效率高**：推理阶段不需要 VLM 参与，不增加推理延迟和参数量。

### 8. 不足与局限
*   **初始性能依赖**：如果第一阶段的去雨器表现极差（如 NLCL 实验所示），回收到的“奖励”质量过低，则自强化策略的效果会大打折扣。
*   **阶段转换阈值**：从第一阶段切换到第二阶段的时机（Epoch 数）目前主要依靠经验设定，缺乏自动化的触发机制。
*   **计算开销**：虽然推理不增压，但训练阶段需要运行 VLM 进行打分，且需要维护一个奖励图像池，增加了训练时的存储和计算负担。

（完）
