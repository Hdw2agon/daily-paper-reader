---
title: "From Draft to Draft-Free: One-Step Video Object Removal via Privileged Distillation and Fast Planting"
title_zh: 从草图到无草图：通过特权蒸馏与快速植入实现的一步式视频物体移除
authors: "Zizhao Chen, Ping Wei, Guang Dai, Jingdong Wang, Mengmeng Wang"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14976v1"
tags: ["query:q1"]
score: 8.0
evidence: 视频对象移除与修复
tldr: 针对视频物体移除任务中传统方法伪影多及扩散模型推理慢的问题，本文提出D2DF框架。该框架通过优先特权一致性蒸馏（PPCD）将多步细化能力迁移至单步生成模型，并引入自引导快速植入（SGFP）模块在潜空间生成伪草图，实现了无需外部草图输入的单步高效视频修复。实验证明，该方法在保持高保真度的同时，将单视频处理时间缩短至约1秒，在质量与效率上均达到了SOTA水平。
selection_source: fresh_fetch
motivation: 旨在解决现有视频物体移除方法在视觉真实感与推理效率之间难以兼得，且扩散模型依赖多步迭代导致实用性受限的问题。
method: 提出一种结合优先特权一致性蒸馏与自引导快速植入模块的框架，通过蒸馏教师模型的细化能力并自主生成潜空间伪草图，实现单步视频修复。
result: 实验表明该方法在多个指标上超越了传统及多步生成方法，且单视频去噪处理仅需约1秒，显著提升了处理速度。
conclusion: D2DF框架成功实现了高质量、高效率的单步视频物体移除，为实时视频编辑和生成式修复提供了极具竞争力的解决方案。
---

## 摘要
视频物体移除是视频编辑中一项基础且具有挑战性的任务。尽管近期取得了进展，现有方法通常分为两类：基于光流或注意力机制的传统方法往往会引入明显的伪影，并产生不自然的结果；相比之下，基于扩散模型的方法提高了视觉逼真度，但需要多个去噪步骤，限制了其实用性。为了解决这些问题，我们提出了 From-Draft-to-Draft-Free (D2DF) 框架，该框架将把粗糙草图转化为精细视频的能力蒸馏到一步式视频生成模型中。在 D2DF 中，首先训练一个教师模型，通过多个步骤将低质量的移除结果（“草图”）细化为高保真视频。然后，通过先验特权一致性蒸馏 (PPCD)，我们将这种能力蒸馏到一个学生模型中，使其能够在草图约束下执行一步式移除。为了消除对草图的依赖，我们引入了基于时空掩码 Transformer (Temporal Masked Transformer) 的自引导快速植入 (SGFP) 模块，该模块在潜空间中自主生成场景一致的伪草图，从而实现完全无需草图的一步式模型。大量实验表明，草图约束版本和无需草图版本在多个指标上均达到了最先进的性能，在质量和效率上都超越了传统方法和多步生成方法。单个视频的去噪过程仅需约 1 秒。

## Abstract
Video object removal is a fundamental yet challenging task in video editing. Despite recent progress, existing methods typically fall into two categories. Traditional approaches based on optical flow or attention mechanisms often introduce noticeable artifacts and yield unnatural results. In contrast, diffusion-based methods improve visual realism but demand multiple denoising steps, limiting their practicality. To address these issues, we propose From-Draft-to-Draft-Free (D2DF), a framework that distills the ability of transforming coarse drafts into refined videos into a one-step video generation model. Within D2DF, a teacher model is trained to refine low-quality removal results ("drafts") into high-fidelity videos by multiple steps. Then, through Prior-Privileged Consistency Distillation (PPCD), we distill this capability into a student model that performs one-step removal conditioned on the draft. To eliminate draft dependency, we introduce a Self-Guided Fast Planting (SGFP) module based on our Temporal Masked Transformer that autonomously generates scene-consistent pseudo-drafts in latent space, enabling a fully draft-free one-step model. Extensive experiments show that both draft-conditioned and draft-free versions achieve state-of-the-art performance on multiple metrics, surpassing traditional and multi-step generative methods in both quality and efficiency. The denoising process for a single video takes only about 1 second.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Draft to Draft-Free: One-Step Video Object Removal via Privileged Distillation and Fast Planting》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：视频物体移除（Video Object Removal, VOR）旨在消除视频中的特定物体并修复背景。
*   **核心痛点**：
    1.  **传统方法（如基于光流或Transformer）**：处理速度快，但生成能力弱，在大面积遮挡下容易产生结构扭曲和模糊，结果仅能算作“草图”（Draft）。
    2.  **扩散模型（Diffusion-based）**：视觉真实感强，但推理速度慢（需数十步迭代），且由于缺乏强先验，容易产生“幻觉”（生成语义错误的内容）。
*   **研究目标**：开发一种既能保持扩散模型的高质量生成，又能实现单步（One-Step）极速推理，且不依赖外部工具生成草图的自主框架。

### 2. 方法论
论文提出了 **D2DF（From-Draft-to-Draft-Free）** 框架，分为三个阶段：
*   **阶段 I：草图引导的扩散教师（D-LDM）**：训练一个多步扩散模型，以传统方法生成的“草图”作为条件输入，学习如何将低质量草图细化为高保真视频。
*   **阶段 II：优先特权一致性蒸馏（PPCD）**：
    *   **核心思想**：传统的一致性蒸馏在处理带噪草图时轨迹不稳定。PPCD 在蒸馏过程中为教师模型提供 Ground Truth（GT）作为“特权先验”，迫使教师生成一条极其稳定的“黄金轨迹”。
    *   **技术细节**：学生模型（D2DF-DG）学习将“草图+噪声”直接映射到这条黄金轨迹的终点，从而实现高质量的单步细化。
*   **阶段 III：自引导快速植入（SGFP）模块**：
    *   **核心思想**：为了摆脱对外部草图工具的依赖，设计了一个轻量级的**时空掩码 Transformer（TMT）**。
    *   **技术细节**：该模块直接在潜空间（Latent Space）利用未遮挡区域的信息重建“伪草图”。通过再次蒸馏，得到完全自主的单步生成模型（D2DF-DF）。

### 3. 实验设计
*   **数据集**：
    *   **RORD**：真实世界物体移除数据集（含2761个训练场景）。
    *   **ROVI**：包含9091个物体移除实例的大规模数据集。
    *   **VPLM**：用于评估的视频物体移除任务集。
*   **Benchmark（评价指标）**：PSNR、SSIM（像素级相似度）、VFID（视频分布相似度）、LPIPS（感知质量）以及 Flow warping error（时间一致性）。
*   **对比方法**：包括传统 SOTA（ProPainter, E2FGVI, FuseFormer, FGT）和基于扩散模型的 SOTA（ROSE, DiffuEraser, FLoED, MiniMax-Remover）。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上进行。
*   **模型配置**：采用 **CogVideoX-5B-I2V** 作为预训练权重。
*   **训练细节**：学习率设为 $5 \times 10^{-5}$（教师）和 $2 \times 10^{-5}$（学生）；Batch size 为 4。
*   **时长说明**：文中未明确给出总训练小时数，但提到推理时单视频（25帧）去噪仅需约 **1.03秒**，包含编解码的总处理时间在10秒以内。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了详尽的定量对比，并针对不同遮挡比例（0-20% 到 40% 以上）进行了鲁棒性测试。
*   **消融实验**：分别验证了 PPCD 蒸馏策略、SGFP 模块的各个组件（Transformer层、空间窗口约束等）以及三阶段框架的有效性。
*   **客观性**：引入了 Zero-shot（在 Camera-Bench 数据集上）测试和跨模型草图兼容性测试，证明了模型不仅在特定分布下有效，且具有很强的泛化能力。实验设计全面、对比公平。

### 6. 主要结论与发现
*   **性能领先**：D2DF-DG（草图引导版）在几乎所有指标上均达到 SOTA；D2DF-DF（无草图版）在摆脱外部依赖的情况下，性能依然优于现有的多步扩散模型。
*   **效率飞跃**：相比于 ROSE 等扩散模型，D2DF 的推理速度提升了 **40倍以上**。
*   **大面积修复优势**：在极端遮挡（信息无法通过帧间传播获取）的情况下，D2DF 展现了比传统方法更强的生成式修复能力。

### 7. 优点
*   **创新性强**：PPCD 引入“特权先验”解决了蒸馏过程中的轨迹不稳定问题，是提升单步生成质量的关键。
*   **高效实用**：将复杂的视频修复简化为单步生成，极大地降低了实际应用中的计算成本。
*   **端到端自主**：SGFP 模块实现了从输入到输出的完全闭环，不再需要预处理工具。

### 8. 不足与局限
*   **极端场景模糊**：论文坦诚在某些特定场景下，单步去噪可能会产生轻微的模糊感，视觉锐度仍有提升空间。
*   **复杂遮挡处理**：在处理多个物体之间存在复杂动态遮挡关系（如物体间互相穿插）时，模型仍可能产生不完美的修复结果。
*   **算力门槛**：虽然推理快，但基于 5B 参数量的视频扩散模型进行微调和蒸馏，训练阶段仍需要较高的显存和算力支持。

（完）
