---
title: "RevealLayer: Disentangling Hidden and Visible Layers via Occlusion-Aware Image Decomposition"
title_zh: RevealLayer：通过遮挡感知的图像分解解耦隐藏层与可见层
authors: "Binhao Wang, Shihao Zhao, Bo Cheng, Qiuyu Ji, Yuhang Ma, Liebucha Wu, Shanyuan Liu, Dawei Leng, Yuhui Yin"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11818v1"
tags: ["query:q1"]
score: 9.0
evidence: 遮挡内容的可靠恢复与遮挡补全
tldr: RevealLayer是一个基于扩散模型的图像分层框架，旨在将RGB图像分解为多个RGBA图层。针对自然图像中遮挡恢复难、层分离不准等问题，该方法引入区域感知注意力、遮挡引导适配器及复合损失函数，实现了精确的层分离与遮挡内容恢复。此外，研究者构建了高质量数据集RevealLayer-100K和基准测试集RevealLayerBench，实验证明其性能优于现有方法。
selection_source: fresh_fetch
motivation: 现有图像分层方法在处理复杂自然图像时，面临遮挡补全困难、层解耦不稳以及高质量多层数据集匮乏的挑战。
method: 提出包含区域感知注意力模块、遮挡引导适配器和复合损失函数的扩散模型框架，实现对隐藏层与可见层的解耦与重构。
result: 通过引入RevealLayer-100K数据集和RevealLayerBench基准，实验证明该方法在层分解精度和遮挡区域恢复方面均显著优于现有技术。
conclusion: RevealLayer通过创新的架构设计和高质量数据集，有效解决了自然图像分层中的遮挡与边界难题，推动了图像编辑与合成技术的发展。
---

## 摘要
最近基于扩散的方法在图像图层分解方面取得了实质性进展。然而，由于遮挡补全、鲁棒的图层解耦以及精确的前景边界等方面的困难，准确分解复杂的自然图像仍然具有挑战性。此外，高质量多图层自然图像数据集的稀缺限制了该领域的发展。为了应对这些挑战，我们提出了 RevealLayer，这是一个基于扩散的框架，可将 RGB 图像分解为多个 RGBA 图层，从而实现精确的图层分离和自然图像中被遮挡内容的可靠恢复。RevealLayer 包含三个关键组件：(1) 区域感知注意力模块，用于解耦隐藏层和可见层；(2) 遮挡引导适配器，利用上下文信息增强重叠区域；(3) 复合损失函数，用于强制生成清晰的 alpha 边界并抑制残留伪影。为了支持训练和评估，我们推出了 RevealLayer-100K，这是一个通过自动化算法与人工标注协作构建的高质量多图层自然图像数据集，并进一步建立了 RevealLayerBench，用于评估通用自然场景下的图层分解性能。大量实验表明，RevealLayer 在图层分解方面始终优于现有方法。

## Abstract
Recent diffusion-based approaches have made substantial progress in image layer decomposition. However, accurately decomposing complex natural images remains challenging due to difficulties in occlusion completion, robust layer disentanglement, and precise foreground boundaries. Moreover, the scarcity of high-quality multi-layer natural image datasets limits advancement. To address these challenges, we propose RevealLayer, a diffusion-based framework that decomposes an RGB image into multiple RGBA layers, enabling precise layer separation and reliable recovery of occluded content in natural images. RevealLayer incorporates three key components: (1) a Region-Aware Attention module to disentangle hidden and visible layers; (2) an Occlusion-Guided Adapter to leverage contextual information to enhance overlapping regions; and (3) a composite loss to enforce sharp alpha boundaries and suppress residual artifacts. To support training and evaluation, we introduce RevealLayer-100K, a high-quality multi-layer natural image constructed through a collaboration between automated algorithms and human annotation, and further establish RevealLayerBench for benchmarking layer decomposition in general natural scenes. Extensive experiments demonstrate that RevealLayer consistently outperforms existing approaches in layer decomposition.

---

## 论文详细总结（自动生成）

这是一份关于论文《RevealLayer: Disentangling Hidden and Visible Layers via Occlusion-Aware Image Decomposition》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将一张复杂的自然场景RGB图像，根据用户提供的边界框（Bounding Box），精确地分解为一个背景层和多个带透明度（RGBA）的前景图层。
*   **研究动机**：
    *   **现有方法局限**：传统的级联式方法（分割+抠图+修复）容易产生误差累积；现有的端到端扩散模型缺乏显式的图层语义控制，且在处理复杂遮挡、恢复被遮挡内容以及保持边缘清晰度方面表现不佳。
    *   **数据匮乏**：缺乏大规模、高质量的自然场景多图层数据集，限制了模型在真实世界场景中的泛化能力。

### 2. 方法论
RevealLayer 是一个基于 **FLUX.1 [dev] (DiT架构)** 的扩散模型框架，核心思想是通过区域感知和遮挡引导来实现图层解耦。
*   **核心架构**：将图像、背景和各前景层编码为潜在表示（Latent），拼接成变长序列进行联合建模。
*   **关键技术细节**：
    1.  **区域感知注意力 (RAA)**：引入注意力掩码（Mask），限制不同图层Token之间的交互，防止特征泄露，确保图层间的独立性。
    2.  **遮挡引导适配器 (OGA)**：在特征提取阶段，将原始图像的局部上下文信息与各层潜在特征融合，增强模型对重叠区域和被遮挡内容的重构能力。
    3.  **复合损失函数**：
        *   **硬约束 Alpha 损失 ($L_\alpha$)**：采用类似 Focal Loss 的对数惩罚，强制模型生成锐利、精确的前景边缘。
        *   **软约束正交损失 ($L_{orth}$)**：在像素空间抑制图层间的相关性，消除背景层中可能残留的前景伪影。
*   **数据管线**：开发了包含自动化提取、生成式合成及遮挡增强的管线，并引入人工审核确保质量。

### 3. 实验设计
*   **数据集**：
    *   **RevealLayer-100K**：包含10万组高质量自然场景多图层数据（训练集）。
    *   **RevealLayerBench**：包含200组复杂场景的基准测试集。
*   **评估任务与对比方法**：
    *   **物体移除**：对比 PowerPaint, SmartEraser, ObjectClear, OmniPaint 等。
    *   **图像抠图**：对比 SAM 系列 (SAM-H, SAM2, SAM3) 和 MAM。
    *   **多层分解**：对比 Qwen-Image-Layered 和 CLD。
*   **指标**：PSNR, SSIM, LPIPS, FID, SoftIoU，以及基于大模型的 Q-Insight 评分和人工评价。

### 4. 资源与算力
*   **模型配置**：基于 Flux.1 [dev]，使用 **LoRA** 进行微调（Rank=64）。
*   **训练细节**：使用 Prodigy 优化器，学习率 1.0，全球批大小（Batch Size）为 8，训练 50,000 次迭代。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量，但考虑到 Flux.1 的参数量和 1024 像素的分辨率，通常需要高性能计算集群（如 A100 或 H100 级别）。

### 5. 实验充分性
*   **实验规模**：论文进行了多维度的定量和定性评估，涵盖了物体移除、抠图和分层分解三个主流任务。
*   **消融实验**：系统地验证了 RAA、OGA 以及两种新增损失函数的有效性，证明了各组件对性能的提升。
*   **鲁棒性测试**：专门测试了模型对“不准确边界框”（偏移、过大、过小）的容忍度。
*   **评价客观性**：引入了第三方大模型评分（Q-Insight）和双盲人工评测，实验设计较为客观、公平。

### 6. 主要结论与发现
*   RevealLayer 在自然场景下的图层分解性能显著优于现有 SOTA 方法，尤其在**遮挡补全**和**背景保真度**上优势明显。
*   **RAA 和 OGA 模块**是实现图层解耦的关键，能有效解决特征干扰问题。
*   高质量的 **RevealLayer-100K 数据集**对提升模型处理真实世界复杂布局的能力至关重要。

### 7. 亮点
*   **端到端可控性**：通过简单的 Bounding Box 实现了高度可控的多层分解，无需复杂的 Mask 输入。
*   **遮挡恢复能力**：能够可靠地推测并补全被前景遮挡的背景内容，而不仅仅是简单的抹除。
*   **数据集贡献**：填补了自然场景多图层高质量数据集的空白。

### 8. 不足与局限
*   **极端情况失效**：当用户提供的边界框严重偏离目标、物体被极度遮挡或存在极其密集的重复纹理时，模型仍可能出现歧义。
*   **透明物体挑战**：处理半透明物体（如玻璃、烟雾）与复杂背景的完全分离仍有提升空间。
*   **推理开销**：由于引入了额外的注意力机制和多层联合建模，推理时的显存占用和计算耗时高于单层生成模型。

（完）
