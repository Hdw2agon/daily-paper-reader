---
title: "Edge-Efficient Image Restoration: Transformer Distillation into State-Space Models"
title_zh: 边缘高效的图像恢复：将 Transformer 蒸馏至状态空间模型
authors: "Srinivas Soumitri Miriyala, Sowmya Vajrala, Sravanth Kodavanti, Vikram Nelvoy Rajendiran, Sharan Kumar Allur"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02794v1"
tags: ["query:q7"]
score: 8.0
evidence: 结合Transformer和SSM的混合图像修复
tldr: 该研究针对边缘设备上图像恢复任务中Transformer延迟高的问题，提出了一种结合Transformer与状态空间模型（SSM）的混合框架。通过将Transformer块的特征蒸馏到轻量级SSM块中，并利用多目标搜索策略（ENS）自动寻找最优的混合架构，在保持高质量恢复效果的同时，显著提升了在移动端硬件上的推理速度。
selection_source: fresh_fetch
motivation: 旨在解决Transformer在边缘设备处理高分辨率图像时因自注意力机制导致的巨大延迟问题。
method: 采用特征蒸馏将Transformer块转化为SSM替代块，并通过多目标搜索策略（ENS）自动优化混合U-Net架构。
result: 在Snapdragon 8 Elite平台上，该方法在去模糊、去雨和去噪任务中分别实现了3.4倍、1.74倍和1.17倍的加速。
conclusion: 该框架成功平衡了图像恢复的精度与边缘设备的运行效率，证明了SSM作为Transformer替代方案在移动端的潜力。
---

## 摘要
我们提出了一种用于混合图像恢复的模块化框架，该框架集成了 Transformer 和状态空间模型（SSM）块，重点在于提高边缘硬件上的运行效率。虽然 Transformer 通过自注意力机制提供了强大的全局建模能力，但其注意力算子在移动设备上会产生显著的延迟，尤其是在处理高分辨率输入时。相比之下，诸如 Mamba 之类的 SSM 提供了具有较低运行时开销的线性时间序列建模，但在细粒度恢复任务上可能表现不佳。为了平衡准确性和效率，我们将轻量级 SSM 块训练为 Transformer 块的特征蒸馏替代品，并利用它们构建混合 U-Net 风格的架构。为了自动发现有效的模块组合，我们引入了高效网络搜索（ENS），这是一种多目标搜索策略，可从预对齐的组件中选择特定任务的混合配置。ENS 在优化恢复质量的同时惩罚 Transformer 的使用，作为延迟的轻量级代理，并能够在无需重复硬件分析的情况下进行架构发现。在骁龙 8 Elite CPU 上，Restormer 基准测试的推理时间为 10119.52 毫秒。相比之下，通过 ENS 发现的混合模型显著缩短了运行时间：ENS-Deblurring 运行时间为 2973 毫秒（快 3.4 倍），ENS-Deraining 为 5816 毫秒（快 1.74 倍），ENS-Denoising 为 8666 毫秒（快 1.17 倍），同时保持了具有竞争力的恢复质量。

## Abstract
We propose a modular framework for hybrid image restoration that integrates transformer and state-space model (SSM) blocks with a focus on improving runtime efficiency on edge hardware. While transformers provide strong global modeling through self-attention, their attention kernels incur substantial latency on mobile devices, especially for high-resolution inputs. In contrast, SSMs such as Mamba offer lineartime sequence modeling with lower runtime overhead but may underperform on fine grained restoration tasks. To balance accuracy and efficiency, we train lightweight SSM blocks as feature-distilled surrogates of transformer blocks and use them to construct hybrid U-Net-style architectures. To automatically discover effective block combinations, we introduce Efficient Network Search (ENS), a multi-objective search strategy that selects task-specific hybrid configurations from pre-aligned components. ENS optimizes restoration quality while penalizing transformer usage, serving as a lightweight proxy for latency and enabling architecture discovery without repeated hardware profiling. On a Snapdragon 8 Elite CPU, the Restormer baseline requires 10119.52 ms for inference. In contrast, ENS-discovered hybrids significantly reduce runtime: ENS-Deblurring runs in 2973 ms (3.4x faster), ENS-Deraining in 5816 ms (1.74x faster), and ENS-Denoising in 8666 ms (1.17x faster), while maintaining competitive restoration quality.

---

## 论文详细总结（自动生成）

这篇论文由三星研究院（印度）的研究团队撰写，旨在解决高性能图像恢复模型在移动边缘设备上部署时的延迟瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 架构（如 Restormer）虽然在图像恢复任务中表现卓越，但其自注意力机制在处理高分辨率图像时，在移动端硬件（如手机 CPU/GPU）上会产生巨大的延迟。
*   **研究动机**：
    *   **硬件不友好**：尽管一些线性 Transformer 减少了理论计算量（FLOPs），但其算子在边缘设备上的实际运行效率依然低下。
    *   **SSM 的潜力与不足**：状态空间模型（如 Mamba）具有线性推理复杂度和更好的硬件友好性，但在捕捉精细纹理和全局上下文方面往往逊于 Transformer。
    *   **目标**：寻找一种既能保持 Transformer 高精度，又能发挥 SSM 高效率的混合架构。

### 2. 方法论
论文提出了一个模块化的混合框架，核心包含以下三个关键步骤：
*   **特征级知识蒸馏（FwKD）**：
    *   将 Transformer 块（Restormer）作为“教师”，将 SSM 块（MambaIR）作为“学生”。
    *   通过最小化两者输出特征之间的 $L_2$ 距离，将 MambaIR 训练成 Restormer 块的“数字孪生”替代品。这使得 SSM 块能够学习到 Transformer 的全局感受野。
*   **混合搜索空间设计**：
    *   基于 U-Net 骨干网络，将编码器、解码器和瓶颈层的 8 个阶段定义为搜索维度。
    *   每个阶段可选不同数量或类型的 Restormer 或 MambaIR 块，构建了一个包含 34,560 种可能配置的搜索空间。
*   **高效网络搜索（ENS）**：
    *   采用**多目标贝叶斯优化（BO）**，利用“预期超体积改进（EHVI）”策略进行搜索。
    *   **双目标函数**：一是最大化恢复质量（PSNR），二是最小化架构复杂度（通过惩罚 Transformer 的使用量来作为延迟的轻量级代理）。
    *   搜索过程无需重新训练模型，仅需对预对齐的模块进行插拔式评估。

### 3. 实验设计
*   **任务与数据集**：
    *   **去雨**：Rain13K（训练），Rain100H/L、Test100/1200/2800（测试）。
    *   **去模糊**：GoPro（运动模糊）、DPDD（离焦模糊）、RealBlur、HIDE。
    *   **去噪**：SIDD、SenseNoise。
*   **基准模型（Benchmark）**：以 **Restormer** 为主要教师模型和性能上限基准。
*   **对比方法**：包括传统的 CNN 方法（MPRNet、NAFNet）、Transformer 方法（Uformer、SwinIR）以及最新的 SSM 方法（MambaIR、VMambaIR）。

### 4. 资源与算力
*   **训练算力**：预训练 22 个 MambaIR 替代块（蒸馏过程）大约消耗了 **20 个 NVIDIA A100 GPU 小时**。
*   **搜索开销**：ENS 搜索过程运行了 500 次函数评估，由于是基于预训练模块的“插拔式”评估，搜索效率极高。
*   **硬件测试平台**：在 **骁龙 8 Elite (SM8750)** 移动平台上进行了实际延迟分析。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的图像恢复任务，并在多个主流公开数据集上进行了验证。
*   **消融实验**：
    *   验证了特征蒸馏（FwKD）的必要性（不进行蒸馏 PSNR 会大幅下降）。
    *   验证了 ENS 搜索的有效性（对比随机选择和固定比例混合）。
    *   验证了框架的通用性（将 Uformer 蒸馏至 SSM 同样有效）。
*   **评价**：实验设计全面，不仅对比了学术指标（PSNR/SSIM），还重点对比了工业界关注的移动端实际延迟，具有很强的说服力和客观性。

### 6. 主要结论与发现
*   **显著加速**：在骁龙 8 Elite 上，相比 Restormer 基准，ENS 发现的混合模型在去模糊任务上快了 **3.4 倍**，去雨快了 **1.74 倍**，去噪快了 **1.17 倍**。
*   **精度保持**：混合架构在大幅降低延迟的同时，恢复质量（PSNR）与纯 Transformer 模型持平甚至在某些任务上有所超越。
*   **互补性**：感受野分析（ERF）显示，蒸馏后的 SSM 块获得了类似 Transformer 的全局覆盖能力，而混合架构使 Transformer 层的注意力更加聚焦。

### 7. 优点
*   **硬件导向**：不盲目追求低 FLOPs，而是针对移动端芯片的算子特性进行优化。
*   **模块化与训练友好**：通过块级蒸馏实现了“一次训练，多次搜索”，避免了神经架构搜索（NAS）中常见的巨大计算开销。
*   **通用性强**：该框架不局限于特定的模型，可推广至任何“高性能-高延迟”与“轻量级-低延迟”的模型对。

### 8. 不足与局限
*   **依赖代理指标**：搜索过程中使用“Transformer 计数惩罚”作为延迟的代理指标，虽然高效，但可能无法完全捕捉某些复杂算子在特定硬件上的底层优化差异。
*   **级联误差风险**：块级独立蒸馏假设模块间是解耦的，但在端到端微调前，多个替代块的级联可能会产生微小的特征偏移。
*   **应用限制**：目前主要针对 U-Net 风格的恢复模型，对于非分层架构的适用性尚未讨论。

（完）
