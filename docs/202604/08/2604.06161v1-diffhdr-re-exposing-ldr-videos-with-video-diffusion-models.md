---
title: "DiffHDR: Re-Exposing LDR Videos with Video Diffusion Models"
title_zh: DiffHDR：利用视频扩散模型对 LDR 视频进行重曝光
authors: "Zhengming Yu, Li Ma, Mingming He, Leo Isikdogan, Yuancheng Xu, Dmitriy Smirnov, Pablo Salamanca, Dao Mi, Pablo Delgado, Ning Yu, Julien Philip, Xin Li, Wenping Wang, Paul Debevec"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06161v1"
tags: ["query:q4"]
score: 9.0
evidence: 将LDR到HDR的转换建模为生成式辐射修复任务
tldr: DiffHDR 针对 8 位 LDR 视频中高光和阴影细节丢失的问题，提出将 LDR 到 HDR 的转换建模为视频扩散模型潜空间中的生成式辐射修复任务。通过在 Log-Gamma 色彩空间中利用预训练视频扩散模型的时空先验，该框架能合成逼真的 HDR 辐射并恢复量化像素的连续辐射，支持文本或参考图像引导，并解决了 HDR 视频数据稀缺问题，显著提升了视频的保真度和时空稳定性。
selection_source: fresh_fetch
motivation: 现有的 LDR 到 HDR 转换技术难以在过曝或欠曝区域恢复真实的细节，限制了 HDR 显示和后期重曝光的应用。
method: 将 LDR 到 HDR 转换视为生成式辐射修复任务，在 Log-Gamma 空间利用视频扩散模型的时空先验进行合成，并开发了从静态 HDRI 地图合成训练数据的流水线。
result: 实验表明，DiffHDR 在辐射保真度和时间稳定性方面显著优于现有最先进方法，生成的 HDR 视频具有极大的重曝光宽容度。
conclusion: DiffHDR 通过结合扩散模型先验与创新的数据合成方法，为高质量 LDR 视频向 HDR 转换及可控重曝光提供了有效的解决方案。
---

## 摘要
大多数数字视频以 8 位低动态范围 (LDR) 格式存储，由于饱和和量化，原始高动态范围 (HDR) 场景辐射度的大部分信息都丢失了。这种高光和阴影细节的丢失阻碍了将准确的亮度映射到 HDR 显示器，并限制了后期制作流程中具有实际意义的重曝光。尽管已经提出了通过动态范围扩展将 LDR 图像转换为 HDR 的技术，但它们难以在过曝和欠曝区域恢复真实的细节。为解决这一问题，我们提出了 DiffHDR，这是一个将 LDR 到 HDR 的转换建模为视频扩散模型潜空间内生成式辐射度修复任务的框架。通过在 Log-Gamma 颜色空间中运行，DiffHDR 利用预训练视频扩散模型的时空生成先验，在过曝和欠曝区域合成合理的 HDR 辐射度，同时恢复量化像素的连续场景辐射度。我们的框架还支持由文本提示或参考图像引导的可控 LDR 到 HDR 视频转换。针对成对 HDR 视频数据的稀缺问题，我们开发了一套流水线，从静态 HDRI 地图合成高质量的 HDR 视频训练数据。大量实验表明，DiffHDR 在辐射度保真度和时间稳定性方面显著优于现有最先进的方法，能够生成具有较大重曝光宽容度的真实 HDR 视频。

## Abstract
Most digital videos are stored in 8-bit low dynamic range (LDR) formats, where much of the original high dynamic range (HDR) scene radiance is lost due to saturation and quantization. This loss of highlight and shadow detail precludes mapping accurate luminance to HDR displays and limits meaningful re-exposure in post-production workflows. Although techniques have been proposed to convert LDR images to HDR through dynamic range expansion, they struggle to restore realistic detail in the over- and underexposed regions. To address this, we present DiffHDR, a framework that formulates LDR-to-HDR conversion as a generative radiance inpainting task within the latent space of a video diffusion model. By operating in Log-Gamma color space, DiffHDR leverages spatio-temporal generative priors from a pretrained video diffusion model to synthesize plausible HDR radiance in over- and underexposed regions while recovering the continuous scene radiance of the quantized pixels. Our framework further enables controllable LDR-to-HDR video conversion guided by text prompts or reference images. To address the scarcity of paired HDR video data, we develop a pipeline that synthesizes high-quality HDR video training data from static HDRI maps. Extensive experiments demonstrate that DiffHDR significantly outperforms state-of-the-art approaches in radiance fidelity and temporal stability, producing realistic HDR videos with considerable latitude for re-exposure.

---

## 论文详细总结（自动生成）

以下是对论文《DiffHDR: Re-Exposing LDR Videos with Video Diffusion Models》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：大多数数字视频以8位低动态范围（LDR）格式存储，由于量化和饱和（过曝/欠曝），丢失了原始场景的大量高动态范围（HDR）辐射度信息。这导致在HDR显示器上亮度映射不准确，且限制了后期制作中的重曝光（Re-exposure）灵活性。
*   **研究动机**：现有的LDR转HDR方法多为确定性的像素翻译模型，难以在信息完全丢失的剪裁区域（如过曝的天空或极暗的阴影）合成逼真的细节。作者认为这是一个“一对多”的生成问题，因此提出利用预训练视频扩散模型的强大先验来“幻觉”出缺失的辐射度。

### 2. 方法论
*   **核心思想**：将LDR到HDR的转换建模为视频扩散模型潜空间（Latent Space）中的**生成式辐射修复（Generative Radiance Inpainting）**任务。
*   **关键技术细节**：
    *   **Log-Gamma 色彩映射**：提出一种非线性变换公式 $T(x) = \frac{\log(1+\gamma x)}{\log(1+\gamma M)} \cdot \frac{1}{\gamma}$，将高动态范围的辐射度压缩到预训练视频VAE可处理的范围内，从而无需微调VAE即可保留其生成先验。
    *   **HDR视频数据合成流水线**：利用16K分辨率的静态HDRI地图（来自Polyhaven），通过Blender渲染模拟相机运动（缩放、旋转），并加入曝光偏移、异方差相机噪声、剪裁和量化，生成高质量的成对训练数据。
    *   **曝光感知控制机制**：
        *   **亮度掩码检测**：自动识别过曝和欠曝区域，并进行时间平滑处理。
        *   **上下文聚焦提示（Context-Focused Prompting）**：设计结构化文本提示（如“过曝区域：云层；欠曝区域：木纹”），通过修改交叉注意力层实现局部区域的精准引导。
    *   **模型架构**：基于VACE（14B参数的视频扩散模型）作为骨干，使用LoRA进行参数高效微调。

### 3. 实验设计
*   **数据集**：
    *   **合成/基准数据集**：SI-HDR（图像）、Cinematic Video（电影视频）、Polyhaven合成视频（预留测试集）。
    *   **真实场景**：从Pexels收集的野外视频、由Veo2生成的LDR视频。
*   **对比方法**：HDRCNN、MaskHDR、SingleHDR、LEDiff（当前最先进的扩散模型方法）等。
*   **评估指标**：
    *   **参考指标**：HDR-VDP3、FovVideoVDP（视频质量感知）。
    *   **非参考/感知指标**：PU21-PIQE、FID、DOVER、CLIPIQA、MUSIQ。

### 4. 资源与算力
*   **算力投入**：使用了 **8台 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，训练步数为 **10,000步**。
*   **精度处理**：DiT部分使用 BF16 精度以提高效率，VAE部分使用 FP32 精度以避免HDR解码中的色带（Banding）伪影。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从静态图像到动态视频、从合成数据到真实野外视频的全面评估。
*   **消融实验**：针对Log-Gamma映射、数据增强策略、掩码引导、上下文聚焦提示等核心组件均做了详细的对比实验。
*   **客观性**：通过引入多种HDR专用指标（如HDR-VDP3）和通用视频质量指标（DOVER），并结合多级曝光下的定性视觉对比，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：DiffHDR在辐射度保真度和时间稳定性方面显著优于现有SOTA方法，特别是在处理严重过曝区域时。
*   **VAE兼容性**：证明了通过合理的色彩映射（Log-Gamma），LDR预训练的VAE可以直接用于HDR任务，避免了昂贵的VAE重训练。
*   **可控性**：该框架成功实现了通过文本或参考图像对HDR重建过程进行语义引导，为后期制作提供了极大的创作空间。

### 7. 优点
*   **创新性映射**：Log-Gamma映射巧妙地解决了HDR数据与LDR预训练模型之间的分布偏移问题。
*   **高质量数据生成**：开发的HDRI到视频的合成流水线有效缓解了HDR视频数据匮乏的痛点。
*   **时空一致性**：利用视频扩散模型的先验，生成的HDR视频在帧间过渡上非常平滑，无明显闪烁。

### 8. 不足与局限
*   **计算开销**：基于14B参数的大模型，推理成本较高，难以实现实时处理。
*   **数据偏差风险**：训练数据主要源于静态HDRI合成，虽然加入了模拟运动，但与具有复杂动态物体（如行人、流体）的真实视频仍存在一定领域差距。
*   **幻觉风险**：作为生成式模型，在极端缺失信息的情况下，生成的细节可能与原始场景不完全一致（虽然视觉上合理）。

（完）
