---
title: "Noise-Robust Conditional Flow Matching: Generating Clean Samples from Noisy Datasets"
title_zh: 噪声鲁棒的条件流匹配：从噪声数据集中生成干净样本
authors: "Adrian Urbański, Gabriel della Maggiora, Artur Yakimovich"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2608.00064v1"
tags: ["query:q3"]
score: 9.0
evidence: 在无干净参考的情况下从噪声数据中学习干净分布
tldr: 本研究针对科学成像中难以获取干净训练数据的问题，提出了噪声鲁棒条件流匹配（NR-CFM）框架。该方法允许直接从受噪声污染的观测数据中学习纯净数据的分布，无需成对的干净参考。NR-CFM 针对加性高斯白噪声提供了闭式解修正，并能通过数据驱动方式处理复杂协方差的噪声。实验表明，该方法在极低信噪比（0.001）下仍能生成高质量图像，性能优于 NR-GAN 且与 Ambient Diffusion 相当。
selection_source: fresh_fetch
motivation: 传统的生成模型在噪声数据上训练会复现噪声，而科学领域往往难以获取高质量的干净参考数据。
method: 提出 NR-CFM 框架，通过为高斯噪声引入闭式或数据驱动的端点修正，实现从单次污染观测中学习纯净分布。
result: 在多种噪声设置下，NR-CFM 的表现优于 NR-GAN，并在极低信噪比环境下展现出强大的生成能力。
conclusion: NR-CFM 为从噪声数据中学习生成模型提供了一个高效且稳定的解决方案，特别适用于信噪比极低的科学成像场景。
---

## 摘要
生成模型学习其训练数据的统计特性，因此高质量的生成依赖于干净且具有代表性的数据集。在科学成像中，采集过程通常会产生带噪声的测量结果，而收集干净的参考数据可能成本高昂、不切实际甚至无法实现。直接在这些测量结果上进行训练会导致模型重现受损的数据。这可以通过直接从噪声数据中学习干净的总体分布来规避。条件流匹配（CFM）结合了简单的回归目标、稳定的训练、高效的采样以及强大的图像生成性能，使其成为处理该场景的自然框架。我们引入了噪声鲁棒条件流匹配（NR-CFM），这是一种从每张图像的一个受损观测值中学习的无条件生成器。NR-CFM 为加性高斯白噪声提供了闭式干净端点修正，并为具有更复杂协方差结构的通用高斯损坏学习数据驱动的修正。在评估的各种损坏设置中，NR-CFM 在大多数情况下优于 NR-GAN，并且在高噪声区间内与 Ambient Diffusion 保持竞争力。我们进一步在信噪比低至 0.001 的科学数据上评估了 NR-CFM，它能够从严重受损的测量结果中生成合理的粒子图像。

## Abstract
Generative models learn the statistical properties of their training data, so high-quality generation depends on clean and representative datasets. In scientific imaging, acquisition often yields noisy measurements, while collecting clean references can be costly, impractical or even unattainable. Training directly on these measurements results in a model that reproduces the corrupted data. This can be circumvented by learning the clean population distribution directly from the noisy data. Conditional flow matching (CFM) combines a simple regression objective with stable training, efficient sampling, and strong image-generation performance, making it a natural framework for this setting. We introduce Noise-Robust Conditional Flow Matching (NR-CFM), an unconditional generator that learns from one corrupted observation per image. NR-CFM provides a closed-form clean endpoint correction for additive white Gaussian noise and learns a data-driven correction for general Gaussian corruptions with more complex covariance structure. Across the evaluated corruption settings, NR-CFM outperforms NR-GAN in most cases and remains competitive with Ambient Diffusion in the high-noise regime. We further evaluate NR-CFM on scientific data at signal-to-noise ratios as low as $0.001$, where it generates plausible particle images from severely corrupted measurements.

---

## 论文详细总结（自动生成）

以下是对论文《Noise-Robust Conditional Flow Matching: Generating Clean Samples from Noisy Datasets》的结构化总结：

### 1. 核心问题与整体含义
在科学成像（如冷冻电镜、医学影像）中，获取的数据往往包含大量噪声，而获取对应的“干净”参考图像通常成本极高或物理上不可行。传统的生成模型（如 GAN、扩散模型）如果直接在噪声数据上训练，会学习并复现这些噪声。
**核心问题**：如何在**仅有单次噪声观测值**且**缺乏干净参考数据**的情况下，训练出一个能够生成纯净样本的无条件生成模型？
**研究意义**：该研究将条件流匹配（CFM）框架扩展到了噪声鲁棒领域，为科学研究中从低信噪比数据中提取统计分布提供了新的工具。

### 2. 方法论
论文提出了 **NR-CFM（噪声鲁棒条件流匹配）** 框架，其核心思想是学习观测到的“噪声桥”（Noisy Bridge）的流场，并利用得分函数（Score）恒等式将其修正回“干净桥”。

*   **观测桥训练**：模型首先在受损观测值 $y$ 上训练一个标准的 CFM 速度场 $v_\theta$，学习从高斯先验到噪声分布的映射。
*   **得分恒等式与修正**：
    *   **加性高斯白噪声（AWGN）**：推导出了闭式解。利用 Tweedie 公式，通过已学习的速度场 $v_\theta$ 直接计算后验均值修正项，无需额外训练。
    *   **通用高斯损坏**：针对空间结构化或信号相关的复杂高斯噪声，引入辅助网络 $s_\phi$。该网络通过“再污染”（Re-corruption）策略训练，即对已有的噪声观测值添加已知的微小扰动，学习噪声协方差的局部结构。
*   **采样流程**：
    *   从高斯先验出发，沿学习到的速度场进行 ODE 积分。
    *   **端点映射（Endpoint Map）**：为了避免 $t=1$ 时的数值奇异性，在 $t_{cut}$（如 0.95）处停止积分，并应用修正公式直接跳跃到预测的干净端点。

### 3. 实验设计
*   **数据集**：
    *   **自然图像**：CIFAR-10, LSUN Bedroom, FFHQ, CelebA-HQ。
    *   **科学成像**：CryoBench (IgG-1D)，模拟冷冻电镜下的蛋白质粒子图像。
*   **对比基准（Benchmarks）**：
    *   **NR-GAN**：针对未知/已知噪声的 GAN 变体。
    *   **Ambient Diffusion**：目前处理噪声数据生成的最先进扩散模型方法。
    *   **AmbientGAN**：在测量空间进行对抗训练的早期方法。
*   **噪声场景**：涵盖了信号无关噪声（固定/变动方差）、信号相关噪声（乘性噪声）、空间相关噪声（布朗高斯噪声）以及极低信噪比（SNR = 0.001）的科学数据。

### 4. 资源与算力
论文明确记录了实验的硬件与超参数：
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   CIFAR-10：每张 GPU batch size 为 128，训练 200,000 步。
    *   LSUN/FFHQ：每张 GPU batch size 为 64。
    *   使用了梯度裁剪（norm 1.0）和指数移动平均（EMA, 0.9999）。
    *   辅助网络训练同样为 200,000 步。

### 5. 实验数量与充分性
实验设计非常充分且具有针对性：
*   **多维度对比**：在 NR-GAN 提出的 10 多种复杂噪声设置下进行了全面测试。
*   **消融实验**：对采样截止时间 $t_{cut}$ 和辅助扰动范围 $\delta, \kappa$ 进行了详细的消融研究，验证了超参数的敏感性。
*   **压力测试**：在 SNR 低至 0.001 的极端环境下测试，这在同类论文中较为罕见，证明了方法的鲁棒性。
*   **公平性**：采用了标准的 FID 指标，并尽可能在相同的网络容量（如 DDPM++ 或 ADM 架构）下与基准方法对比。

### 6. 主要结论与发现
*   **性能领先**：在 CIFAR-10 的所有高斯噪声设置下，NR-CFM 的 FID 表现均显著优于 NR-GAN。
*   **高噪声优势**：在 AWGN 设置下，虽然在低噪声时略逊于 Ambient Diffusion，但在**高噪声区间（$\sigma=0.2$）**，NR-CFM 反超了 Ambient Diffusion，表现出更强的抗噪能力。
*   **科学应用潜力**：在 CryoBench 实验中，NR-CFM 能够从肉眼几乎无法辨认的噪声图像（SNR 0.001）中恢复出清晰的蛋白质结构特征。

### 7. 优点
*   **理论完备**：为 CFM 引入了严谨的后验均值修正理论，将得分匹配与流匹配在噪声处理上统一起来。
*   **灵活性高**：不仅支持简单的白噪声，还能通过辅助网络处理复杂的、信号相关的空间结构噪声。
*   **训练稳定**：继承了 CFM 回归目标的稳定性，相比 GAN 类方法更容易收敛。

### 8. 不足与局限
*   **依赖噪声模型**：NR-CFM 要求已知噪声的协方差类型或测量元数据。如果噪声模型被错误指定，性能可能会大幅下降（而 NR-GAN 在某些未知噪声下更具通用性）。
*   **高斯假设**：目前的方法论主要建立在高斯噪声假设之上，对于泊松噪声或其他非指数族噪声的直接支持尚需进一步扩展。
*   **计算开销**：对于复杂噪声，需要训练额外的辅助网络，且采样时需要 ODE 求解器，计算成本高于单步生成模型。

（完）
