---
title: "TM-BSN: Triangular-Masked Blind-Spot Network for Real-World Self-Supervised Image Denoising"
title_zh: TM-BSN：用于真实场景自监督图像去噪的三角掩码盲点网络
authors: "Junyoung Park, Youngjin Oh, Nam Ik Cho"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04484v1"
tags: ["query:q2"]
score: 9.0
evidence: 无需地面真值的自监督图像去噪
tldr: 针对真实场景中sRGB图像存在的空间相关噪声，本文提出了一种三角掩码盲点网络（TM-BSN）。该方法通过引入三角掩码卷积，在原始分辨率下构建与去马赛克噪声模式匹配的菱形盲点，有效排除了相关噪声干扰。结合知识蒸馏技术，该模型在无需下采样的情况下实现了高效且高精度的自监督去噪，在多个真实世界基准测试中达到了领先水平。
selection_source: fresh_fetch
motivation: 传统的盲点网络假设噪声像素相互独立，无法有效处理真实sRGB图像中由ISP流水线（如去马赛克）引起的空间相关噪声。
method: 提出三角掩码卷积以构建符合噪声相关性几何特征的菱形盲点，并利用知识蒸馏将多方向预测融合至轻量化U-Net中。
result: 在真实世界图像去噪基准测试中，该方法显著优于现有的自监督去噪方案，且无需复杂的预处理或下采样。
conclusion: TM-BSN通过精确建模噪声空间相关性，证明了在保持原始分辨率的前提下，自监督去噪可以达到极高的性能和效率。
---

## 摘要
盲点网络 (BSN) 通过阻止访问目标像素来实现自监督图像去噪，从而在没有地面真值 (ground-truth) 监督的情况下进行干净信号估计。然而，这种方法假设像素级噪声是相互独立的，但在真实世界的 sRGB 图像中，由于相机图像信号处理 (ISP) 流水线产生的空间相关噪声，这一假设并不成立。虽然一些方法采用下采样来消除噪声相关性，但它们改变了噪声统计特性，并限制了网络利用完整上下文信息的能力。在本文中，我们提出了三角掩码盲点网络 (TM-BSN)，这是一种新型盲点架构，能够准确建模真实 sRGB 噪声的空间相关性。这种相关性源于去马赛克 (demosaicing) 过程，其中每个像素都是由具有空间衰减权重的相邻样本重建而成的，从而产生菱形模式。为了使感受野与这种几何形状对齐，我们引入了三角掩码卷积，将卷积核限制在其上三角区域，从而在原始分辨率下创建一个菱形盲点。这种设计在排除相关像素的同时充分利用了不相关的上下文，消除了对下采样或后处理的需求。此外，我们利用知识蒸馏将来自多个盲点预测的互补知识迁移到轻量级 U-Net 中，从而提高了准确性和效率。在真实世界基准测试上的大量实验表明，我们的方法达到了最先进的性能，显著优于现有的自监督方法。我们的代码可在 https://github.com/parkjun210/TM-BSN 获取。

## Abstract
Blind-spot networks (BSNs) enable self-supervised image denoising by preventing access to the target pixel, allowing clean signal estimation without ground-truth supervision. However, this approach assumes pixel-wise noise independence, which is violated in real-world sRGB images due to spatially correlated noise from the camera's image signal processing (ISP) pipeline. While several methods employ downsampling to decorrelate noise, they alter noise statistics and limit the network's ability to utilize full contextual information. In this paper, we propose the Triangular-Masked Blind-Spot Network (TM-BSN), a novel blind-spot architecture that accurately models the spatial correlation of real sRGB noise. This correlation originates from demosaicing, where each pixel is reconstructed from neighboring samples with spatially decaying weights, resulting in a diamond-shaped pattern. To align the receptive field with this geometry, we introduce a triangular-masked convolution that restricts the kernel to its upper-triangular region, creating a diamond-shaped blind spot at the original resolution. This design excludes correlated pixels while fully leveraging uncorrelated context, eliminating the need for downsampling or post-processing. Furthermore, we use knowledge distillation to transfer complementary knowledge from multiple blind-spot predictions into a lightweight U-Net, improving both accuracy and efficiency. Extensive experiments on real-world benchmarks demonstrate that our method achieves state-of-the-art performance, significantly outperforming existing self-supervised approaches. Our code is available at https://github.com/parkjun210/TM-BSN.

---

## 论文详细总结（自动生成）

以下是对论文《TM-BSN: Triangular-Masked Blind-Spot Network for Real-World Self-Supervised Image Denoising》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的自监督盲点网络（BSN）假设噪声在像素间是相互独立的。然而，在真实世界的sRGB图像中，由于相机ISP流水线（特别是**去马赛克/Demosaicing**过程）的作用，噪声呈现出显著的**空间相关性**。
*   **研究动机**：现有的解决相关噪声的方法（如像素打乱下采样 PD）会改变噪声统计特性并导致纹理丢失；而一些在原始分辨率下工作的BSN采用矩形盲点，未能充分利用不相关的上下文信息。本文旨在设计一种与真实噪声相关性几何特征（菱形模式）相匹配的架构，以实现更精准的去噪。

### 2. 方法论
*   **核心思想**：通过设计一种特殊的卷积掩码，在原始图像分辨率下构建一个**菱形盲点**，使其精确覆盖去马赛克产生的相关噪声区域，同时保留尽可能多的不相关像素用于信号恢复。
*   **关键技术细节**：
    *   **三角掩码卷积 (TMC)**：将3×3卷积核限制在其上三角区域（其余部分置零）。通过堆叠多层TMC，感受野会沿对角线方向逐渐扩张。
    *   **TM-BSN架构**：采用四个旋转分支（0°、90°、180°、270°）分别提取特征。通过**特征平移操作 (Feature Shifting)** 引入偏移量 $s$，使目标像素脱离自身的感受野，最终四个分支聚合形成一个中心对称的菱形盲点。
    *   **知识蒸馏 (Knowledge Distillation)**：利用TM-BSN作为教师网络，通过改变偏移量 $s$ 生成多个互补的去噪预测，并将其知识迁移到一个轻量级的学生网络（U-Net）中。这不仅提升了性能，还显著降低了推理延迟。

### 3. 实验设计
*   **数据集**：
    *   **SIDD (Smartphone Image Denoising Dataset)**：使用SIDD Medium进行训练，在Validation和Benchmark集上评估。
    *   **DND (Darmstadt Noise Dataset)**：用于跨数据集泛化测试及完全自监督设置下的评估。
*   **对比方法**：
    *   **传统方法**：BM3D, WNNM。
    *   **监督学习**：DnCNN, CBDNet, Restormer等。
    *   **自监督学习（主要对手）**：AP-BSN, LG-BPN, CVF-SID, PUCA, AT-BSN, APR-RD, TBSN等。
*   **评价指标**：PSNR（峰值信噪比）和 SSIM（结构相似性）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   **TM-BSN训练**：50万次迭代（500k iterations），Batch Size为4，采用Adam优化器和余弦退火学习率调度。
    *   **知识蒸馏训练**：20万次迭代（200k iterations），Batch Size为8。
    *   **推理速度**：蒸馏后的学生模型在256×256图像上的推理时间仅为 **3.21ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流真实世界基准数据集上进行了详尽测试，并提供了SIDD官方榜单和Kaggle竞赛的对比数据。
*   **消融实验**：
    *   针对**训练偏移量 $s$** 进行了对比（$s=4, 5, 6, 7$），验证了 $s=5$ 是平衡相关噪声排除与上下文利用的最佳点。
    *   针对**蒸馏偏移量集合 $S$** 进行了组合测试，确定了最佳的教师监督信号组合。
*   **客观性**：为了公平对比，作者使用了竞争对手公开的Checkpoint进行重新评估，并统一了SSIM的计算方式，实验设计较为严谨、客观。

### 6. 主要结论
*   **性能领先**：TM-BSN在SIDD和DND基准测试中均达到了SOTA（最先进）水平，超越了所有现有的自监督去噪方法。
*   **纹理保护**：相比于下采样方法，TM-BSN能更好地保留木纹、织物等精细纹理，避免了过度平滑。
*   **效率提升**：通过知识蒸馏，模型在保持高精度的同时，参数量和计算量大幅减少，适合实时应用。

### 7. 优点
*   **几何对齐**：首次明确提出并利用了去马赛克噪声的“菱形”相关性几何特征，理论依据充分。
*   **无需下采样**：在原始分辨率下操作，避免了像素打乱带来的伪影和信息损失。
*   **灵活性**：通过简单的特征平移即可调节盲点大小，无需重新设计复杂的网络结构。

### 8. 不足与局限
*   **噪声假设限制**：虽然处理了空间相关性，但仍基于噪声均值为零的假设，对于具有强偏置（Bias）的非典型噪声可能效果受限。
*   **计算成本**：虽然最终的学生模型很轻量，但教师网络（TM-BSN）由于包含四个旋转分支，训练阶段的计算开销相对较大。
*   **特定ISP依赖**：该方法针对sRGB图像的去马赛克特征设计，若图像处理流程发生重大变化（如非Bayer阵列），菱形盲点的优越性可能需要重新评估。

（完）
