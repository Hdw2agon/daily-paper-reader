---
title: "GuardPaint:SpeculativeSafetyDecodingforText-to-ImageGeneration"
title_zh: GuardPaint：文本到图像生成的投机性安全解码
authors: "Shreyash Dhoot, Paras Dhiman, Arsh Abbas Naqvi, Aranbi Dutta, Aman Chadha, Vinija Jain, Amitava Das"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21869v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于安全视觉修复的手术级图像补全
tldr: 针对文生图模型易受对抗性提示词攻击产生违规内容的问题，本文提出 GuardPaint 框架。该方法无需修改基础模型，通过在扩散过程中引入轻量级审计器监控中间图像，并对不安全区域进行局部修复。实验表明，GuardPaint 在多种主流模型和攻击方式下均能显著降低违规生成率，同时保持了极高的图像质量和提示词忠诚度。
selection_source: fresh_fetch
motivation: 现有的文生图安全防护主要集中在生成前后的过滤，缺乏对扩散过程中的实时干预和对违规内容的精细化修复。
method: 提出一种投机性安全解码框架，利用审计器定位不安全区域，并通过策略对齐的修复器进行局部重绘和择优选择。
result: 在 SDXL 和 FLUX.1 等多个模型上，该方法有效抵御了五类主流越狱攻击，显著减少了有害内容的生成。
conclusion: GuardPaint 提供了一种高效且非侵入式的安全增强方案，在保障生成内容合规性的同时兼顾了视觉质量。
---

## 摘要
文本到图像 (T2I) 扩散模型提供了强大的视觉生成能力，但其可控性也带来了严峻的安全挑战：对抗性提示词可以将去噪轨迹引导至违反政策的内容，如露骨裸体或血腥暴力。现有的安全防护措施大多在生成前通过提示词过滤或在生成后通过图像分类起作用，这使得扩散过程本身处于无保护状态，且通常只能产生拒绝响应而非安全的视觉修复。我们介绍了 GuardPaint，这是一个用于安全 T2I 生成的投机性解码框架，它在不修改基础模型的情况下干预扩散轨迹。一个轻量级审计器监控中间图像，定位不安全区域，并仅在需要时触发精准的修复 (inpainting) 补救。候选修复由符合政策的修复器生成，并通过受保护的锦标赛机制进行选择，该机制仅在编辑能够提高政策合规性同时保持提示词忠实度和感知质量时才接受编辑。在 SneakPrompt、MMA、PGJ、DACA 和 RABell 五种越狱家族，以及包括 SD 1.5、SDXL、SD 3.5 和 FLUX.1-dev 在内的 UNet/流匹配模型上，GuardPaint 降低了攻击成功率和有害生成，且对图像质量、提示词忠实度和良性行为的影响极小。内容警告：本文包含涉及裸体和暴力的示例，部分读者可能会感到不安、痛苦或反感。

## Abstract
Text-to-image (T2I) diffusion models offer powerful visual generation, but their controllability creates a critical safety challenge: adversarial prompts can steer the denoising trajectory toward policy-violating content such as explicit nudity or graphic violence. Existing safeguards mostly act before generation through prompt filtering or after generation through image classification, leaving the diffusion process itself unguarded and often yielding only refusal rather than safe visual repair.   We introduce GuardPaint, a speculative decoding framework for safe T2I generation that intervenes inside the diffusion trajectory without modifying the base model. A lightweight auditor monitors intermediate images, localizes unsafe regions, and triggers surgical inpainting repair only where needed. Candidate repairs are generated by a policy-aligned inpainter and selected through a guarded tournament that accepts edits only when they improve policy compliance while preserving prompt fidelity and perceptual quality.   Across five jailbreak families SneakPrompt, MMA, PGJ, DACA, and RABell and UNet/flow-matching models including SD~1.5, SDXL, SD~3.5, and FLUX.1-dev. GuardPaint reduces attack success and harmful generations with minimal degradation to image quality, prompt fidelity, and benign behavior.   Content warning: This paper contains examples involving nudity and violence that some readers may find disturbing, distressing, or offensive.

---

## 论文详细总结（自动生成）

### 论文总结：GuardPaint - 文本到图像生成的投机性安全解码

#### 1. 核心问题与整体含义
*   **研究背景**：当前的文本到图像（T2I）扩散模型（如 SDXL, FLUX.1）虽然生成能力强大，但面临严重的安全性挑战。对抗性提示词（越狱攻击）可以绕过传统的提示词过滤器，诱导模型生成违规内容（如裸体、暴力）。
*   **核心痛点**：现有的防御机制多为“边界防御”（生成前过滤提示词或生成后分类图像）。这种方式要么容易被绕过，要么只能采取“全盘拒绝”的策略（如输出黑图），无法在保留用户合法创作意图的同时修复违规部分。
*   **研究目标**：提出一种名为 **GuardPaint** 的模块化解码层，在不修改基础模型权重的前提下，通过监控扩散轨迹并进行局部“手术级”修复，将违规图像转化为合规的视觉替代方案。

#### 2. 方法论
GuardPaint 采用了一种受大语言模型“投机解码”启发的框架，核心流程如下：
*   **审计器-评分器 (Auditor-Scorer)**：一个基于 ResNet-101 的多任务模型，在扩散过程的特定步数（通常是后期）对中间潜变量进行解码和扫描。它负责：1) 触发修复流程；2) 定位不安全区域（生成热力图/掩码）；3) 为修复候选评分。
*   **策略对齐修复器 (Policy-Aligned Inpainter)**：基于 SD 1.5 Inpainting 模型，通过两个阶段训练：
    *   **阶段 1 (Refusal SFT)**：监督微调，学习如何将违规区域替换为合规内容（如给裸露部位加上衣服）。
    *   **阶段 2 (BCO 优化)**：使用二元分类器优化（Binary Classifier Optimization），根据人类偏好对齐修复效果，增强其在对抗场景下的稳健性。
*   **受保护的锦标赛 (Guarded Tournament)**：针对检测到的违规区域，采样 $N$ 个不同的修复配置（如不同的 CFG、掩码扩张度等）。通过一个锦标赛机制，仅当修复结果在提高合规性的同时，满足提示词忠实度（Fidelity）和感知质量（Quality）的阈值时，才接受该编辑。
*   **潜空间重插入 (Latent Reinsertion)**：
    *   对于 **UNet 架构**（如 SD 1.5/SDXL）：使用 **Null-text Inversion** 将修复后的像素无缝映射回扩散轨迹。
    *   对于 **流匹配架构**（如 FLUX.1/SD 3.5）：利用 **Flow-ODE** 传输机制进行潜变量融合。

#### 3. 实验设计
*   **数据集**：构建了一个包含 **8.2万** 对图像-提示词的语料库，涵盖安全、裸体和暴力类别。使用 InternVL-3.5 (8B) 进行高质量标注。
*   **基准测试 (Benchmark)**：采用 **JailBreakDiffBench** 协议。
*   **攻击方法**：对比了五类主流黑盒越狱攻击：SneakPrompt, MMA, PGJ, DACA, 和 RABell。
*   **对比模型**：涵盖了四代 T2I 架构，包括 SD 1.5, SDXL, SD 3.5 (Medium & Large-Turbo), 以及 FLUX.1-dev。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A6000 (48GB)** GPU 上运行。
*   **推理开销**：
    *   对于 UNet 架构（SD 1.5/SDXL），每个审计步（$N=5$）增加约 **7-8 秒** 的延迟。
    *   对于 FLUX.1-dev，由于 Flow-ODE 重插入的复杂性，每个审计步增加约 **20 秒**。
*   **训练细节**：审计器和修复器的训练步数约为数千步，文中未明确给出总训练机时，但强调了其轻量化特性。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文在 5 种不同的模型架构和 5 种攻击家族上进行了交叉验证，实验矩阵完整。
*   **消融实验**：详细探讨了审计步数 ($X$) 和锦标赛候选数量 ($N$) 对攻击成功率 (ASR) 的影响，证明了 $N=5$ 是质量与延迟的最佳平衡点。
*   **客观性**：使用了标准化的基准测试和自动评估指标（AlignS, BLIP 分数），并提供了定性的视觉对比，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **显著降低风险**：GuardPaint 在所有测试场景下均有效降低了攻击成功率。例如，在 SD 1.5 上，DACA 攻击的 ASR 从 8.25% 降至 1.94%。
*   **保持生成质量**：由于采用了局部修复和锦标赛门控机制，图像的非违规区域和整体提示词忠实度几乎不受影响（指标波动在 $\pm 0.05$ 以内）。
*   **架构通用性**：证明了该方法可以作为“插件”应用于从早期的 DDPM 到最新的 Rectified Flow 变压器模型。

#### 7. 优点
*   **非侵入式**：无需修改或重新训练庞大的基础模型，保护了原始模型的生成分布。
*   **视觉修复而非拒绝**：相比直接封禁，提供了语义连贯的安全替代方案，提升了用户体验。
*   **多准则决策**：锦标赛机制确保了修复操作不会以牺牲图像质量或提示词忠实度为代价。

#### 8. 不足与局限
*   **推理延迟**：虽然是“投机”触发，但一旦触发修复，数秒至数十秒的额外延迟在实时应用中可能难以接受。
*   **分类体系局限**：目前仅针对裸体和暴力，无法处理更复杂的社会偏见、文化敏感性或版权侵权等安全问题。
*   **修复器代差**：统一使用 SD 1.5 作为修复器，在修复 FLUX.1 等高分辨率、高容量模型生成的图像时，可能会出现风格不一致或细节丢失。
*   **对抗审计器风险**：如果攻击者了解审计器的架构，可能会针对审计器本身设计“隐形”的违规内容。

（完）
