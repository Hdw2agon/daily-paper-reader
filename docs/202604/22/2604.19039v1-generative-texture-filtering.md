---
title: Generative Texture Filtering
title_zh: 生成式纹理滤波
authors: "Rongjia Zheng, Shangwei Huang, Lei Zhu, Wei-Shi Zheng, Qing Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19039v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 利用无标签数据集进行纹理去除和结构保留的生成式方法
tldr: 本研究提出了一种基于生成式模型的纹理滤波方法，旨在利用预训练生成模型的强大图像先验。通过两阶段微调策略（小规模成对数据监督微调和大规模无标签数据强化微调），该方法在去除纹理的同时能有效保留图像结构。实验证明，该方法在处理复杂纹理和结构保护方面显著优于现有技术，具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 传统纹理滤波方法在处理复杂纹理时难以平衡纹理去除与结构保留，且缺乏对深层图像先验的有效利用。
method: 提出一种两阶段微调策略，结合小样本监督学习与基于奖励函数的强化学习来优化预训练生成模型。
result: 实验结果表明，该方法在纹理去除和结构保护方面均显著优于现有方法，并能有效处理以往的挑战性案例。
conclusion: 利用预训练生成模型的强大先验并结合强化学习微调，是提升纹理滤波性能和泛化能力的有效途径。
---

## 摘要
我们提出了一种用于纹理滤波的生成式方法，该方法表现出惊人的性能和泛化能力。我们的核心思想是通过充分利用预训练生成模型中强大的学习图像先验来增强纹理滤波。为此，我们提出通过两阶段策略对预训练生成模型进行微调。具体而言，我们首先在极小规模的成对图像集上进行监督微调，然后在量化纹理去除和结构保留质量的奖励函数指导下，在大型无标签数据集上进行强化微调。大量实验表明，我们的方法明显优于以往的方法，并能有效处理以前具有挑战性的案例。我们的代码可在 https://github.com/OnlyZZZZ/Generative_Texture_Filtering 获取。

## Abstract
We present a generative method for texture filtering, which exhibits surprisingly good performance and generalizability. Our core idea is to empower texture filtering by taking full advantage of the strong learned image prior of pre-trained generative models. To this end, we propose to fine-tune a pre-trained generative model via a two-stage strategy. Specifically, we first conduct supervised fine-tuning on a very small set of paired images, and then perform reinforcement fine-tuning on a large-scale unlabeled dataset under the guidance of a reward function that quantifies the quality of texture removal and structure preservation. Extensive experiments show that our method clearly outperforms previous methods, and is effective to deal with previously challenging cases. Our code is available at https://github.com/OnlyZZZZ/Generative_Texture_Filtering.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generative Texture Filtering》（生成式纹理滤波）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在去除图像纹理（Texture Removal）的同时，完美保留图像的结构（Structure Preservation）。
*   **研究背景**：纹理滤波是计算摄影学的基础任务。现有方法存在两大局限：
    *   **泛化性差**：学习型方法受限于训练数据集规模，难以处理未见过的纹理；传统方法则需要针对特定图像繁琐地调整参数。
    *   **有效性不足**：基于局部统计或强假设的方法在复杂场景下表现不佳。
*   **核心动机**：利用预训练生成模型（如 Qwen-Image-Edit）中蕴含的强大图像先验知识，将其转化为一种具备强泛化能力的生成式纹理滤波工具。

### 2. 方法论
论文提出了一种**两阶段微调策略**，将纹理滤波建模为图像生成问题：
*   **第一阶段：监督微调 (SFT)**
    *   **目的**：为模型提供纹理滤波的基础能力，缩小强化学习的搜索空间。
    *   **细节**：仅使用 **10 对**成对图像（输入与对应的滤波结果）进行微调。研究发现，极小规模的数据足以提供良好的初始化。
*   **第二阶段：强化微调 (RFT)**
    *   **目的**：在无标签的大规模数据上进一步优化性能。
    *   **核心技术：奖励函数 (Reward Function)**。基于“高斯金字塔最底层几乎无纹理且保留结构”的观察，设计了三个奖励项：
        1.  **纹理去除奖励 ($R_{texture}$)**：计算滤波结果与其高斯金字塔最底层（经超分辨率上采样后）的 SSIM 相似度。相似度越高，说明纹理去除越彻底。
        2.  **结构保留奖励 ($R_{structure}$)**：计算滤波结果与原图在金字塔最底层的相似度。
        3.  **图像保真奖励 ($R_{fidelity}$)**：确保颜色和整体结构与原图一致。
*   **算法流程**：使用流匹配（Flow Matching）目标函数，结合 DPM-Solver 进行快速采样，通过策略优化（Policy Optimization）鼓励模型生成高奖励的结果。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：500 对具有 Ground Truth (GT) 的图像，用于定量评估（PSNR, SSIM）。
    *   **真实世界数据集**：从 Flickr 爬取并筛选的 2,000 张具有多样纹理的图像，用于定性评估。
    *   **训练集**：利用文本生成图像模型合成了 10,000 张涵盖各类纹理（马赛克、砖块、拼图等）的无标签图像。
*   **对比方法 (Benchmarks)**：
    *   传统方法：RTV, BTF, GSF, PTF。
    *   学习型方法：ULS, SSTF。
    *   直接调用多模态大模型：GPT-4o, FLUX.1, Qwen-Image-Edit (Vanilla)。

### 4. 资源与算力
论文明确记录了实验所用的算力资源：
*   **监督微调 (SFT)**：使用 **4 台 24GB RTX 4090 GPU**，训练时长约 **4 小时**。
*   **强化微调 (RFT)**：使用 **8 台 80GB A800 GPU**，训练时长约 **10 小时**。
*   **推理效率**：在单张 A800 上，处理 512x512 图像约需 **1.2 秒**（4 步扩散采样），1024x1024 图像约需 3 秒。

### 5. 实验数量与充分性
*   **实验规模**：进行了定量对比、定性对比、消融实验、超参数分析、不同预训练模型适配性分析以及下游应用展示。
*   **充分性与客观性**：
    *   **消融实验**完整覆盖了 SFT、RFT、奖励函数各分量、上采样方式等关键变量，证明了设计的必要性。
    *   **泛化性测试**在完全未见的合成和真实数据集上进行，结果具有说服力。
    *   **对比实验**涵盖了从 2012 年到 2025 年的代表性算法，基准选择合理。

### 6. 主要结论与发现
*   **生成式先验的威力**：预训练生成模型通过微调可以极大地提升纹理滤波的质量，尤其是在处理以往方法难以应对的复杂纹理时。
*   **两阶段策略的有效性**：极少量的监督数据（10 对）配合大规模强化学习，比单纯增加监督数据量更有效。
*   **奖励函数的创新**：利用高斯金字塔的特性来量化“纹理去除”和“结构保留”是行之有效的评价指标。

### 7. 优点与亮点
*   **首创性**：提出了首个基于生成式模型的纹理滤波框架。
*   **极低的数据依赖**：仅需 10 对成对数据即可启动，极大地降低了对高质量标注数据的需求。
*   **强大的泛化能力**：能够处理各种未见过的、极具挑战性的纹理类型。
*   **多任务潜力**：在细节增强、图像抽象化、反半色调（Inverse Halftoning）等应用中表现出色。

### 8. 不足与局限
*   **颜色一致性风险**：在某些极端情况下，为了追求极致的平滑度，模型可能会错误地移除局部的颜色差异（颜色漂移）。
*   **计算开销**：虽然比直接调用大模型快，但相比于传统的局部滤波器（如双边滤波），其推理速度仍有提升空间。
*   **超参数敏感性**：奖励函数中三个权重的平衡（$\lambda_1, \lambda_2, \lambda_3$）对最终效果有显著影响，可能需要针对特定风格进行微调。

（完）
