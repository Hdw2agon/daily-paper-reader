---
title: "DocPure: Prompt-Free Unified Document Restoration via Degradation-Aware Structure-Guided Wavelet Modulation"
title_zh: DocPure：通过退化感知结构引导小波调制的无提示统一文档修复
authors: "Lingming Su, Wanglong Lu, Tao Wang, Kaihao Zhang, Nan Zhang, Liyan An, Hanli Zhao"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09536v1"
tags: ["query:q7"]
score: 8.0
evidence: 统一文档修复与退化感知自编码器
tldr: 针对文档图像在获取和传输中面临的多种退化问题，本文提出DocPure，一个无需提示词的统一修复框架。该框架通过退化感知结构自编码器预测清晰结构先验，并结合结构引导的小波交互机制，利用低频信息调制高频恢复，实现了去模糊、去噪、去压缩伪影和去阴影等多种任务的高效统一修复，且推理时无需手动提示。
selection_source: fresh_fetch
motivation: 现有的统一文档修复方法通常依赖手动提示词或特定任务模型，难以在无先验信息的情况下处理多种复杂的退化。
method: 提出一种包含退化感知路由正则化的结构自编码器来提取结构先验，并利用结构引导的小波交互机制在频域和空间域间进行特征调制。
result: 在去模糊、去噪、去压缩伪影和去阴影等多个任务上的实验表明，DocPure 的性能优于现有的最先进方法。
conclusion: DocPure 证明了通过结合退化感知结构先验和小波调制，可以在无需推理提示的情况下实现高质量的统一文档图像修复。
---

## 摘要
高质量的文档图像对于信息存档和下游自动处理至关重要。然而，在不受控的采集和传输过程中，它们经常受到各种退化的损害。虽然已经提出了统一文档修复技术来修复多种退化图像，但它们通常面临训练多个特定退化模型、依赖手动特定任务提示或跨任务数据配对等困难。为了解决这些局限性，我们提出了 DocPure，这是一个实现退化感知文档修复的无提示统一框架。我们设计了一个带有退化信息路由正则化的退化感知结构自编码器，用于从退化输入中预测清晰的结构先验。该模型在推理时无需提示，退化标签仅在训练期间作为路由正则化的辅助监督。此外，我们引入了一种结构引导的小波交互机制，以桥接频域特征和空间语义。在结构引导的小波交互机制中，跨频自适应调制利用低频子带调制高频恢复，确保结构一致性。大量实验表明，与现有先进方法相比，DocPure 在去模糊、去噪、压缩伪影去除和去阴影等各种任务中均取得了优异的性能。

## Abstract
High-quality document images are pivotal for information archiving and downstream automatic processing. However, they are frequently compromised by diverse degradations during uncontrolled acquisition and transmission. While unified document restoration techniques have been proposed to restore images from multiple degradations, they often struggle with training multiple degradation-specific models, reliance on manual task-specific prompts, or cross-task data pairing. To address these limitations, we propose DocPure, a prompt-free unified framework that achieves degradation-aware document restoration.   We design a degradation-aware structure auto-encoder with degradation-informed routing regularization to predict clean structural priors from degraded inputs. The model is prompt-free at inference, and degradation labels are only used as auxiliary supervision for the routing regularization during training.   Furthermore, we introduce a structure-guided wavelet interaction mechanism to bridge frequency-domain features and spatial semantics. Within the structure-guided wavelet interaction mechanism, a cross-frequency adaptive modulation utilizes low-frequency sub-bands to modulate high-frequency recovery, ensuring structural consistency. Extensive experiments demonstrate that DocPure achieves strong performance compared with state-of-the-art methods across various tasks, including deblurring, denoising, compression artifact reduction, and deshadowing.

---

## 论文详细总结（自动生成）

### DocPure：基于退化感知结构引导小波调制的无提示统一文档修复总结

#### 1. 核心问题与整体含义（研究动机和背景）
文档图像在采集和传输中常受模糊、噪声、压缩伪影和阴影等多种退化影响，严重降低了视觉质量和下游 OCR 识别率。现有的修复方法存在三大局限：
*   **效率低**：针对不同退化需训练多个特定任务模型。
*   **依赖人工干预**：现有的“全能型（All-in-one）”模型通常需要手动输入退化类型的提示词（Prompt）。
*   **数据限制**：部分方法依赖严格的跨任务配对数据（即同一张图对应多种退化），这在现实中难以获取。
**研究动机**：开发一个**无提示（Prompt-Free）**的统一框架，能够自动识别退化模式并实现高质量的文档修复。

#### 2. 方法论：核心思想与关键技术
DocPure 采用双分支架构，结合了空间语义和频域特征：
*   **退化感知结构自编码器 (DASAE)**：
    *   **核心思想**：从退化图像中自动提取清晰的结构先验（Structure Map）。
    *   **关键技术**：设计了**多分支退化编码器**和**退化感知门控 (DPG)**。通过**退化信息路由正则化**（最小化路由权重与退化标签之间的 KL 散度），使模型在训练时学习区分退化类型，而在推理时无需标签即可自动选择合适的处理路径。
*   **结构引导小波交互模块 (SWIM)**：
    *   **核心思想**：利用小波变换将图像分解为高低频，结合预测的结构图引导修复。
    *   **关键技术**：引入**跨频自适应调制 (CFAM)**，利用低频子带中的结构信息来调制高频细节的恢复，抑制过度修复产生的伪影，确保文字笔画的锐利度和一致性。
*   **损失函数**：结合了像素级（BCE）、区域级（Dice）和边缘级（Boundary Loss）损失，以及路由正则化损失。

#### 3. 实验设计
*   **任务场景**：涵盖去模糊（Deblurring）、去噪（Denoising）、压缩伪影去除（CAR）和去阴影（Deshadowing）四大任务。
*   **数据集**：使用了 12 个多样化数据集，包括 TDD、FUNSD、BCSD（合成与真实）、FSDSRD、RDD（真实阴影）、OSR 等。
*   **Benchmark 与对比方法**：
    *   **通用修复模型**：DE-GAN, DocDiff, DocStain。
    *   **全能型模型**：DocNLC, DocRes。
*   **评价指标**：PSNR、SSIM 以及基于 PaddleOCR 和 Nougat 的字符/单词识别准确率。

#### 4. 资源与算力
*   **硬件**：使用 NVIDIA GeForce RTX 4090 GPU 进行训练。
*   **训练时长**：
    *   DASAE 网络：约 60 小时。
    *   修复主网络：约 100 小时。
    *   **总计**：约 160 小时。
*   **推理效率**：在 1024×1024 分辨率下，推理延迟约为 253.9ms，优于扩散模型（DocDiff）和部分注意力模型（DocStain）。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上进行了广泛测试，涵盖了从合成退化到真实场景的迁移。
*   **消融实验**：非常充分。分别验证了 DASAE（结构先验）、SWIM（小波交互）、CFAM（跨频调制）以及路由正则化的有效性。
*   **客观性**：通过 t-SNE 可视化证明了路由机制能有效分离不同退化特征；使用了两种不同的 OCR 引擎评估下游任务表现，确保了结果的公正性。

#### 6. 主要结论与发现
*   **性能领先**：DocPure 在去模糊和去压缩伪影任务上排名第一，在去噪和去阴影任务上表现出极强的竞争力（排名第一或第二）。
*   **无需提示**：证明了通过内部结构预测和频域特征交互，可以完全取代人工提示词，实现自动化的退化感知修复。
*   **泛化性强**：在未见过的文档样式和布局上（Cross-domain），DocPure 依然保持了稳定的修复质量和高 OCR 准确率。

#### 7. 优点
*   **全自动统一化**：真正实现了“一个模型处理多种退化”，且推理阶段完全自动化。
*   **频域结合空间域**：利用小波变换捕捉文档图像特有的高频笔画特征，修复效果比纯空间域方法更锐利。
*   **训练约束宽松**：不需要跨任务的配对数据，降低了数据收集难度。

#### 8. 不足与局限
*   **退化集合限制**：模型基于已知的退化集合训练，对于完全未知的退化类型（如未见过的纸张折痕）或极其严重的混合退化，修复能力有限。
*   **算力开销**：虽然比扩散模型快，但由于采用了多分支编码器，其参数量（38.15M）和计算量（446.82 GFLOPs）高于一些轻量级模型。
*   **高分辨率内存占用**：在处理超高分辨率文档时，显存占用较高，可能限制其在移动端或嵌入式设备上的部署。

（完）
